# The ten pieces of work — order, definition of done, what re-enters the loop

Ruled by Gabe on 2026-09-19 (D-016). Evidence and the technical work of each piece: `gaps-endpoint.raw.json` (the evaluation)
and `gaps-endpoint.effects.raw.json` (what each piece adds); the readable version is `gaps-endpoint.html`.

## Order

| # | piece | cost (estimate) | where it lands |
|---|---|---|---|
| 1 | The route, in order | hours | the lab's panels + the inventory; fixes the app band drawn in registration order |
| 2 | What an empty slot means | hours | one helper over the lab's twelve lit-or-hollow decisions + the norms per stage |
| 3 | What each ending carries | hours | `gen-endpoint-facts.py` (cases and headers onto the drawn ending) |
| 4 | Whose write it is | hours | the Data panel: the endpoint's own writes apart from the gate's; one table set in every layout |
| 5 | Proof you can open | about a day | `gen-endpoint-facts.py` (asserts · role · the values tests give a setting) + `gen-robot.js` |
| 6 | Inside the calls | about a day | `gen-endpoint-facts.py` (functions{} · dependencies{} · function facts joined to the chain) |
| 7 | How common each piece is | about a day | `gen-endpoint-facts.py` (one loop over the 80 endpoints) |
| 8 | Field rules, and a line to open | about a day | `gen-endpoint-facts.py` (schemas{} · models{} · anchors) |
| 10 | What the screen does on each ending | days | a new part in the frontend reason arm (`_a3_fe_reason.py`), then the lab carry |
| 11 | What stays alive during the request | days | a new `inflight` part in the kinds arm, then the lab carry |
| 9 | Why a touch happens, and why it survives | days | LATER — trigger in D-016 |

## Definition of done, for every piece

1. **The facts are generated.** They reach `_lab-ep.js` through `gen-endpoint-facts.py`; nothing is typed. A fact the feed lacks is said so in the feed's own state words.
2. **The inventory row is measured.** Every row that says "to measure when piece N lands" gets its smallest · middle · largest over the 80 endpoints.
3. **Corrections are made with what the lab already has.** A wrong picture is fixed; a new fact is shown with existing components (chain rows, chips, the four states). NO new display design lands inside a piece — how the card finally shows a fact is decided by the loop (M3 → variants → tests), by seeing.
4. **The probe can fail on it.** `probe-eplab.mjs` gains asserts that measure the drawn thing, and at least one is proven to fire on a mutant.
5. **The example becomes real.** The "after this piece" rows on `gaps-endpoint.html` can be read off the lab.
6. **One commit per piece, by explicit path**, with the numbers in the message.

For the two generation pieces (10 · 11), before the lab carry: the new part ships with its battery (FIRE and SILENT cases), a dry run on the four study targets with the numbers in the commit message, `suite-doctor.sh` CLEAN, and propagation to the twins. Heavy checks run one at a time on this machine.

## What re-enters the loop afterwards

- The **(proposed)** rows of `inventory-endpoint.md` are rated by Gabe on `rate-endpoint.html`.
- **M1 round 2** runs on the larger inventory (round 1 stays a record: `m1-round1.inventory.md`, `m1-endpoint.json`). The eleven prisms are ruled; round 2 checks where the new attributes make their home and whether the running count of answerable questions moved as the evaluation predicted (1 of 20 → 16).
- Then step 3 of the loop: M3, the channel budget, for the first prism.

## Log

| date | piece | commit | note |
|---|---|---|---|
| 2026-09-20 | 1 The route, in order | see `git log -- docs/design/workflow-panel/gen-endpoint-facts.py` | the app band now runs CORS → RateLimit → Idempotency (it was drawn in registration order); every route carries checks passed · fired; the through-route is "first run", 10 of 10; 39 of 88 check rows carry their condition, a passed one too. Probe 1013 → 1025, a mutant fails 3 by name. A power cut hit mid-mutant: the facts file on disk was the mutant, restored byte-identical from the generator. |
| 2026-09-20 | 2 What an empty slot means | see `git log -- docs/design/workflow-panel/_lab-ep-panels.js` | one `slotState()` replaces the twelve count-only decisions: lit · hollow (its reading ran, zero) · hatched (its reading did not run here) · blank; the hover says which, before the plain line. The norms per stage now cover all six topics (36 lines, one dash each). Probe 1025 → 1036; a mutant that calls an unread slot a zero fails 4 by name. With every reading on, nothing on this endpoint is hatched — the difference shows on a feed built with a reading off. |
| 2026-09-20 | 3 What each ending carries | see `git log -- docs/design/workflow-panel/gen-endpoint-facts.py` | the 422 ending now carries its 42 rules (31 value · 7 unknown-field · 2 missing · 2 length), each with its field, kind, rule, line and shape; the ending's record names the headers it sends (429 Retry-After · 401 WWW-Authenticate) or says none. Measured: rules 1 · 4 · 42 over 42 of 80 endpoints; 0 · 2 · 5 endings per endpoint send a header. Probe 1036 → 1048; a mutant without rules and headers fails 6 by name. |
| 2026-09-20 | 4 Whose write it is | see `git log -- docs/design/workflow-panel/gen-endpoint-facts.py` | ONE table set for every layout: 15 tables here (13 known to both sources · `users` and `ai_spend_log` to a route's steps only), each saying how it was found, in the portrait record; races counted by owner (own 10 uncaught + 1 handled · the gate's 1); the GATE stage says its provision once; a step's hover says whose write it is. Measured: an uncaught race anywhere on 79 of 80 endpoints, on their OWN steps on 11; the map's access edges miss 0 · 4 · 7 tables per endpoint that the routes' steps know. Probe 1048 → 1058; a mutant fails 7 by name. Two older checks had pinned the gap itself (`users` never drawn) and were rewritten to the completed picture. |
| 2026-09-20 | 5 Proof you can open | see `git log -- docs/design/workflow-panel/gen-endpoint-facts.py` | ONE roster of 29 cases, each saying why it came here: 12 test this endpoint · 14 only set something up · 3 prove an ending from the service side; 134 more pass through a helper (counted, never listed). Every join on an ending carries what the test really asserted and sent; a case chip stops guessing from the test's name. The settings this endpoint's switches read say which values tests give them. The robot keeps its five states and counts; its 10 unmeasured conditions each say why. Measured: 324 calls test an endpoint — 302 assert the status, 155 a body field, 16 the message, 2 a code. Probe 1058 → 1067; a mutant fails 4. NOT done, by the evaluation's own exclusion: joining a non-status assertion to the arm it proves (a new part in the tests arm). |
| 2026-09-20 | 6 Inside the calls | see `git log -- docs/design/workflow-panel/gen-endpoint-facts.py` | A call on a route OPENS: under `complete_setup` sit its 4 failures, 3 saves and 1 savepoint, each a chain row one step in — a failure one call down names the answer it becomes here and the step that raises it; a failure two calls down shows its condition and says the answer was NOT READ (the feed follows one call; no ending is invented). A call that opens nothing says what the map read of it (lines · what it returns · whether it calls anything · whether it touches a table) or that the map read nothing. A function record gains three rows — what its own code shows it doing (four roles, a mark when it holds two, 'none of the four' said out loud) · what sits inside it · the map's facts. The Security part says the order the helpers are resolved in, once, on the deps header (5 here, 2 of them in no map list), and every dep says its place, whether it can end a request, and that it runs closing code after the handler. Measured over the 80 endpoints: 59 failures inside calls on 29 endpoints (26 answered · 7 uncaught on 6 endpoints · 21 deeper than the reading on 14 · 5 not joined); 1 · 3 · 7 functions per endpoint hold two roles; the session helper runs after the handler on 79 of 80. Probe 1067 → 1088; a mutant fails 7. WORDING CORRECTED against the evaluation: the feed says a yielding helper runs 'after the handler' — 'after the answer is sent' is the framework's scope rule and is not read. NOT done: whether a step runs once or once per item (Q19's other half, piece 9). `gen-endpoint-facts.py` is 849 lines, past the 800 budget — the split waits for piece 8. |
| 2026-09-20 | 7 How common each piece is | see `git log -- docs/design/workflow-panel/_ep_pieces.py` | ONE tally (`_ep_pieces.py`), read twice: the facts generator counts every piece this endpoint carries over the app's 80 endpoints — status · switch kind · catch outcome · media type · rate tier · repeat key and its idiom · login scheme · method — and `gen-pieces-digest.py` counts the same keys over the four study apps into the committed `pieces-digest.json` (gustify 80 · gastify 49 · tier3 499 · keypro says `absent`, shown as 'not measured', never as 0). Each count sits on the card where its piece is already drawn: a chain row, a switch chip, an ending's record, the idempotency fact, the login gate, the 'Untested exits' verb (a test names 4 of its 9 endings, places 3–6 of 80, 28 endpoints name none), the header's app-level card — 'everything', one level up — (11th of 81 by functions behind · middle 4 · biggest 135 · its proof · async 80 of 81, the mark separates nothing here — a place, no score; the RISK card stays about this endpoint alone: an older ruling pinned in the probe says no app-wide comparison there, and it held against this piece's own work text), the fetching hook (28 of 81 have no screen; 2 fetches matched nothing; 5 build their path at run time). Here: repeat key 6 of 80 and in NEITHER other backend; picks-a-value switch 11 of 80; rate tiers 23 · 56 · 1. The word beside a count follows one stated rule (the norm ≥ 9 in 10 · rare ≤ 1 in 10 · only here = 1); it is a count, never a grade. CORRECTED: piece 6 divided forms counts by the map's 81 nodes (BOOT included) — forms counts now read 'of 80'. NOT done here, by the definition of done: drawing a mark ONLY when it is unusual — that is display design for the variants step; the word is generated for it. |
| 2026-09-20 | 8 Field rules, and a line to open | see `git log -- docs/design/workflow-panel/_ep_joins.py` | One join of four feed blocks the lab never read (`schemas{}` · `models{}` · `functions{}.at` · `middleware{}.registered_at`), no new extraction. A body FIELD's card says whether it must be sent, its limits, the validator that also checks it and its line (`household_name` · must be sent · 1 to 120 characters · `_strip_name` refuses a blank · schemas/setup.py:26); its shape says what it does with a field nobody declared (refused — `extra = forbid`). A COLUMN's card says whether it may be empty, what the database writes when nobody sets it, what a delete of the row it points at does, and its line; the table's unique and check rules ride the MODEL card, so the record keeps its five ruled rows. A function opens at its line where a feed knows one (7 of 28 here) and says so where none does. The 26 app-wide step rows of the routes open at the line the step is added to the application. WRONG PICTURE FIXED in seven places: the lab showed `setup.py:254` as the handler's line — 254 is the FILE'S LENGTH; the handler is at :183. A probe assert had pinned the wrong number; it is rewritten. JOIN GAP FOUND by the probe: the map draws a column by its Python attribute (`order`), the feed keys it by its database name (`display_order`) — joined through the feed's own `attr`, and the card says the database name. Measured over the 80 endpoints with no join hole: body fields with a rule 0 · 2 · 29 (45 endpoints read a body), must be sent 0 · 0 · 9, limited 0 · 0 · 17; functions with a line 322 of 989. Probe 1101 → 1118; a mutant fails 6. SPLIT DONE: `gen-endpoint-facts.py` 863 → 708 lines, the joins live in `_ep_joins.py` (the split moved no fact — the only differences were this piece's own anchors). NOT in any feed: which body fields a given ending actually read. |
| 2026-09-20 | 10 What the screen does on each ending — THE LAB HALF (the two cheap steps; the generation part is still owed) | see `git log -- docs/design/workflow-panel/_ep_joins.py` | The lab now carries `frontend.reasons.readers`: every ending's record has an 'on the client' section — its own branch and where it is, or that it falls to the general case (here 7 of the 9 routed endings do; the two 409s share ONE branch at SetupScreen.tsx:43 that reads the status only), whether the call is tried again (never — retry is off for this kind of call), and on the success what is refetched, what is filled in at once and which guards read it. TWO TYPED NAMES REMOVED from the generator (`RequireSetup.tsx`, `InitialSetupScreen.tsx`): a guard is now tied by a provable join — it reads a query whose key this endpoint's hook refreshes on success — and the join found a SECOND guard the typed name had hidden (`RedirectIfSetupComplete`). Measured over the 80 endpoints: 6 have client code that reads their failures; 47 endings routed, 33 to the general case, 10 sharing a branch. STILL OWED, the generation part: `does[]` per reason site in `_a3_fe_reason.py` (the message, a call, the element rendered) with its battery, dry run on the four targets, suite-doctor and twins — the lab row `what it does` is wired and stays empty until the feed carries it. |
| 2026-09-20 | 10 (generation part) and 11 — PLANNED, NOT LANDED | drafts: `docs/design/element-forms/plans/slice-11e-does.plan.md` · `docs/design/element-forms/plans/kinds-inflight.plan.md` (+ the raw findings beside each, `.plan.json`) | Two read-only planning runs (4 readers + 1 designer each) read the arm, the extractor's rows, the rails and the REAL code of the four study apps, and wrote a full design for each part: row schema, class or detector rules, rosters with their cites, a function-level code plan, FIRE and SILENT battery cases with mutants, a landing order with costs, and the risks. Nothing under `templates/center/generators/` changed. BOTH designs stop at the same step — the amendment's own process (the operator rules on the D-decisions, then says 'land it') — because each touches an earlier ruling: **10** adds a `useNavigate` roster (it classes a row and builds NO guard row, so it is not the V40a build trigger — but that needs his word), a new state word `returned` beside the estate's `beyond one level`, and roster entries cited to packages no study app installs (sonner · react-hot-toast · react-toastify · react-i18next); **11** reuses the word `inflight`, which already names the board's projection, moves `arms.kinds.bytes` (the one value that is not 'only added'), and lifts two readers out of the contract arm whose bytes depend on walk order. Sizes, by the designs' own landing orders: 10 about 1–2 days plus ~20 min of serial runs (dry run · doctor · twins); 11 about 4 days. |
| 2026-09-20 | THE REVIEW PAGE — how the operator checks all of the above | `review-leftovers.html` · https://claude.ai/artifact/TYtFyh7fZcDbeZzYcJFuwZ | He asked for a review plan, then for the plan as an artifact in the same system as his other decision pages. The page puts 57 things in front of him (36 norm lines + 21 decisions), in order of risk: what I WROTE first (the 36 lines, the words door and lock, the slot sentences, the cut-offs, the roles, the through-route and its hidden tie-break, the order the helpers resolve in, the test roles, one wording), then the six layouts of his I touched (each with a lab picture taken by REAL clicks and the numbered clicks to see it live), then piece 10 (land it or hold it, with its real cost and its small payoff said), then piece 11 (when, and its name), then a link to the 20 rows waiting for a rating. Dashed = my pick, filled = his; one text to paste back. A content review (4 agent reviewers + 1 verifier, 68 raw → 65 → 58 confirmed or partly, 7 refuted) found that my own page MISLED in three places and cost time in thirty-three: the vocab card sized a sweep of ~150 drawn strings as an 11-line edit; the piece 10 card showed only upside; several click paths pointed at the case that did not show the claim. All applied. LAB DEFECTS the review surfaced, NOT fixed yet (they wait for his rulings on the page): the place and the async count take `BOOT lifespan` as an endpoint (81, not 80); the helpers' order guesses a place the clipped signature does not show; `roles_note` omits `named-only`; the through-route tie-break is a hidden id sort. Probe 19/0, a mutant fails 6; chrome gate 35/36 (centring, by rule). |
| 2026-09-20 | THE REVIEW PAGE — impacts | `review-leftovers.html` Version 5 | He asked for the why AND the impact of each decision. Every option of every decision now says what it sets in motion, with the size of the work (nothing changes · words only · a lab change · a lab facts change · the design only · the map's generators); the option in force is lit; each card says whether the lab is worth opening for it (2 are). A second review (2 agent reviewers + 1 verifier: 31 raw → 27 → 26 confirmed or partly) corrected the impacts. WHAT IT FOUND ABOUT THE LAB ITSELF: **only the Data topic's 6 norm lines are drawn anywhere — the other 30 lines of piece 2 are written ahead of a surface that does not exist** (every reader is `STAGE_EXPECT.data`); striking a line would today draw an empty space, so it needs lab code, not words; a sweep of door and lock cannot reach two sentences the lab LIFTS from the suite station's legend (one is pinned verbatim by the probe) — that is a suite change; the always-open call sits on 5 of the 14 routes, not all; the cut-offs behind 'the norm' and 'rare' are written twice (the facts script and the probe's own copy); the older risk-card ruling is pinned by TWO probe asserts; landing piece 10 means a spec block first and a SECOND commit before the lab shows anything; starting piece 11 means a spec he approves again with two rulings this page does not ask (the scorecard slot, the order of a second pass). |
| 2026-09-20 | RULING D-017, and the review page made lighter | `decisions.md` D-017 · `review-leftovers.html` Version 6 | Reading the page he ruled: a fact ABOUT THE CODE is shown; a fact ABOUT THE MAP (how we found it, which reading ran, a guess, a roster, a word of our framework) is nice to have, hidden by default, behind a "more information" option toggled later. Applied at once: **the lab** — the table record is back to his four rows; `found by` (piece 4) is gated on `window.MOREINFO`, off, no control yet, the fact stays generated (lab probe 1127 → 1128: hidden by default, back with the switch on). **The page** — every decision carries a nature; the 9 about the map (the slot sentences · the guess mark on the helpers' order · one wording · found by · four roster and word choices of piece 10 · piece 11's internal name) are folded into one closed section and keep my pick; the 30 norm lines that are drawn nowhere fold too; a card opens on one sentence, the why, and options that ARE their impact rows, with the full story, the clicks and the numbers behind "more". What is left in front of him: 6 norm lines and 12 decisions. probe-review 22 → 28, a mutant (the three folds open by default · a bad line · a pick that is no option · a token left) fails 11 by name. |
| 2026-09-20 | HIS RULING ON THE REVIEW (D-018), and the lab changes it authorized | `decisions.md` D-018 · `review-leftovers.ruled.json` (the page is now a record; `gen-review.js` refuses to regenerate it) | 47 of 57 his, the rest left to my pick by D-017; the five layout cards then ruled by his follow-up 'go with the recommendation'. LANDED in the lab: **the sweep** — door and lock left all 157 drawn strings the lab owns (no selector, key or id touched; the one 'door' left is the legend sentence the lab lifts from the suite, as ruled); **the tie rule** — the through-route is the success ending passing the most checks, and NONE is named when two tie (7 of the 80 endpoints tie at the top; the old rule picked by a hidden id); **the guess mark** — a helper's place says whether the signature, the feed's order or a guess placed it, on hover cards only by D-017 (9 of 80 endpoints carry a guessed place, because the map clips long signatures); **the count** — an endpoint is a node with an HTTP verb, so the app's startup leaves the counts: 80 endpoints, this one 10th of 80 by functions behind, the biggest now a real endpoint (GET /recipes, 48), async 80 of 80; **what sits inside a call** is a rail pick with three states (always open · closed until clicked · closed), open by default until he has seen all three, and it rides the command panel's copy line. Probe 1128 → 1133 passed, 0 failed (five pins updated to the ruled words and the new control; one heuristic taught that the kind's own name proves no repetition). STILL OWED from this ruling: the two norm lines he ruled 'reword' with no note — proposed to him, not landed; piece 10's generation part (next); piece 11 after it. |
| 2026-09-20 | PIECE 10 — the generation part LANDED, and its carry into the lab | suite `b1a1e2c` (Slice 11e) · twins `e59e9fea` · `4dfebfff` (local; push/PR is his) · the lab carry in its own commit | Ruled "land it" (D-018). Every client comparison now keeps what its branch DOES: `does[]`, `does_state`, `ctx`; arms only add, proven on the battery and on the four study apps against old-code feeds from a clean worktree. A four-lens review of the real feeds, each finding tried by a skeptic, found eleven defects BEFORE the commit — the worst on tier3, where a comparison that only picks a value claimed a sign-out 38 lines away — all fixed; eighteen source mutants, eighteen caught; goldens 67 of 67; suite-doctor CLEAN. Measured: gustify 12 sites · 6 read, gastify 3 · 1, tier3 45 · 32, keypro 1 · 0; 69 rows, 30 of them a call the rosters do not know (a long tail of project helpers, by his ruling never a roster entry). THE LAB: the feed was rebuilt at the lab's own head 05007957 (the twin stood there for the minute of the build, then went back to its branch, clean) and is equal to the old input once the new keys are stripped; the client section's "what it does" row now reads the branch — here both 409 endings reach one branch that hands back what `t(“auth.setup.error.in_progress”)` gives; `from` and `by` stay out of the drawn row (D-017). Over the 80 endpoints: 6 have a client that reads their failures, 14 of 47 routed endings have their own branch, 9 branches, 7 say what they do. Probe 1137/0, two mutants caught (a lie in the panel's words · a row dropped in the join). Traps: a dry run reads the working tree and writes the lab's input; the lab needs ONE head across its feeds, so a newer feed cannot simply be dropped in. |
| 2026-09-21 | HIS READ OF THE LAB — he could not find piece 10's row, and why | `docs/design/workflow-panel/walk-what-it-does.mjs` · `shots/what-it-does/` (ten pictures + `walk.json`) | The click path handed to him ("Data tab → click a 409 ending") was written from the probe's SCRIPT calls (`showTab` · `selectExit`), not from the screen, and no such control exists there. A walk with REAL mouse clicks from a cold load now proves the route and photographs each step: COMMAND → PATHS (Q) → the first cell, 14 (Q) → the cell `409 setup in progress` (F) → the portrait's SECOND icon, which says "Exit" on hover → scroll the portrait one notch → "on the client" · "what it does". From inside the DATA group, where he stood, the keys are B · Q · Q · F. The row is drawn, 12px, not clipped. MEASURED OBSTACLES, his to rule when display work opens (no change made): (1) the record of an ending sits behind an icon-only tab, 26×24, no word on it until hovered, and its hint does not mention the client; (2) the row is below the fold of the portrait (top 635 against a bottom of 651); (3) after the click on the first cell the mouse rests on the first ending, whose hover card covers 9 of the 14 ending cells, the wanted one among them; (4) HIS OWN: the word Data names a part button on the left AND a group in COMMAND — two controls, one name, two different things (the left one picks the middle panel, the right one opens seven distributions); (5) the tab says "Exit" where the rest of the lab says "ending". STANDING RULE FROM THIS: a click path he is asked to follow comes from a real-click walk with the words read off the controls, or it is not handed over. |
| 2026-09-21 | PIECE 11 — the spec, PROPOSED (no code) | `docs/design/element-forms/amendment-1.md` Slice 12 · D30–D32 · the full design stays `plans/kinds-inflight.plan.md` | He followed the walk, saw piece 10's row and asked what is pending. Piece 11 opens as he ruled: with a spec he approves. Eight kinds of row for what is alive while a request runs (state · contextvar · dependency-value · background · lock · cache · built-once · setting-once), each saying where it is set, where it is read and whether it goes with the answer. THREE RULINGS ARE HIS — R1 the slot's number (U15, the next free one on his scorecard) · R2 what `dies` describes (proposed: the slot that carries the value, not the object behind it) · R3 dependency-value rows in or out (proposed: in, behind an option). The rest is framework and mine by D-017: an own stage, no id letter, 12b deferred with its trigger. The landing order carries piece 10's lessons: a review of the REAL feeds before the commit, every new feed saved before the next step, the lab feed built at the lab's own head, and a real-click walk before any click path is handed over. ALSO PENDING, his: the two norm rewordings · pushing (suite 183 commits ahead of its remote, gustify 35, gastify 15 — all only on this machine) · piece 9, whose trigger fired when pieces 1 and 6 landed (recommended: after piece 11) · the rating pass over the 24 rows that still carry only my rating (recommended: after piece 11). |
| 2026-09-21 | HIS RULING D-019 — and what it set in motion the same hour | `decisions.md` D-019 · `amendment-1.md` Slice 12 AUTHORIZED | "rewordings: ok · push the suite: yes · piece 9: after 11 · rating pass: after 11 · R1: U15 · R2: carrier · R3: in · land it". PUSHED: `graft-adoption` at `ee59102` on both remotes (it stood 183 and 198 commits ahead). THE TWO NORM LINES now read "the rows the login check reads or creates to know who is calling" (tables · GATE — the fact about this one endpoint and the last door word are gone) and "the app-wide checks — the rate limit and the origin rules; a repeat key where the app reads one" (security · EDGE); `endpoint-stages.md` follows. `roles_note` now names all five roles (it left out named-only). Probe 1137/0 · plain-audit clean. PIECE 11 lands in the spec's order, the carrier lift first. |
