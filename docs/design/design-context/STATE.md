# Where loop 1 stands

One page, kept short on purpose. Read this before the transcript, and before `method.md`.
Updated 2026-09-22 · suite `8f1bbbf` on `graft-adoption`.

---

## Ruled and done

| | |
|---|---|
| **The inventory** | 61 attributes, **fully ruled** — 26 at 3 · 25 at 2 · 9 at 1. Nothing is a proposal (D-020). |
| **The blocks** | **11, kept** (D-015, re-ruled D-021). The arithmetic's cut said 8; his eleven stand, and the round-2 cells stand as data. |
| **The ten pieces** | All landed. The last, piece 11, is Slice 12 of the element forms (`320a7b4`). |
| **The generation** | Element forms amendment 1 complete through Slice 12. 96 mutants, 96 red. Doctor CLEAN. |
| **The navigator** | `brainmap-endpoint.html` — the 11 blocks as a tree, five placements, grouped four ways (Flat · standpoint · section · **By stage**), Keep only as a control. Probe 158/0. |
| **The lab** | `endpoint-lab.html` — the card's section map is its third rail tab, wired both ways, with a feedback copy. Probe 1211/0. |
| **The matrices** | Round 1 is a RECORD (byte-identical, its own frozen inputs). **Round 2 is a new page** that says it draws a cut the arithmetic did not choose, and shows both. 76/0 and 87/0. |

## Ruled this session

- **D-021** — the card stays eleven blocks; the brain map groups by stage AND by section, toggleable.
- **D-022** — where each block lives in the lab (a page · no page yet · a running header), and what a click does (option C:
  a click always Shows; Keep only is its own control).

Both are BUILT, proven, and committed: `f513cea` (the lab) · `82b19c9` (the matrices) · `8f1bbbf` (the brain map).

## Open — each needs one word from you, by LOOKING

1. **Which stage rule is the default.** Under *By stage*, a block that spans rows can go **under every stage it touches**
   (the default, marked as the agent's pick) or **under Across the stages** (D-021 as recorded). The recorded rule leaves all
   six stages and the bay empty — see it: the grouping rail, then the sub-option under it.
2. **Endings and Proof sit at six rows each** — the widest reading of your record. Narrow either if that is not what you meant.
3. **Keep only keeps the node you opened LAST.** The ruling did not say which node; this was chosen, and it is the one
   assumption in the control.
4. **The round-2 matrices page shows the two cuts as a switch, not side by side** — marked as the agent's pick, because two
   copies of a 65×20 matrix do not fit one column.

## His to run

- **The twins are unpushed**: gustify 36 commits ahead of `origin/staging`, gastify 16 ahead of its branch. Both clean.
  The suite is pushed; the twins' push and PR were always his.

## Next, in order

1. **The second section in the lab** (his ask, 2026-09-22, not started): split the right side below the middle panel, the rail
   and the command panel, and show *everything* an API endpoint has in indented form — highlighting which parts the panels are
   drawing as he navigates. It is the section map turned outward: the map answers "which block", this answers "which of the
   endpoint's own facts is on screen right now".
2. **M3** — the channel budget for the first prism.
3. **The gallery** (step 4 of the method): 3–5 variants of the card at true size, one fader moved each, judged by seeing.
4. **Piece 9** — "Why a touch happens, and why it survives".
5. **12b** — the second round of in-flight detectors.

## The two rules that governed everything here

- **Display is decided by SEEING built options**, never from text. Every choice the agent would make alone is built as a
  switchable rail option, and the page marks which one is the agent's pick.
- **Facts are generated, never typed.** A number inside an authored sentence is a token the generator fills — and since this
  session, the brain map's generator refuses to build if one is typed anywhere in its words.
