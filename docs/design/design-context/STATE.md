# Where loop 1 stands

One page, rewritten (never appended) whenever the position moves. Read it before `method.md` (the method) and
`decisions.md` (every ruling, D-001 …). Updated 2026-09-22 · suite `b9d6a75` on `graft-adoption`, plus the uncommitted
work named under Next.

## In plain words

Loop 1 is the API endpoint card, and its first half is done. The inventory is fully ruled (61 rows: 60 rated, plus
`findings`, an alarm channel only — D-013, D-020). The questions are his. M1 ran twice and his eleven blocks stand (D-015,
D-021); the eleven prisms are kept. All ten leftover pieces landed, the last as Slice 12 of the element forms (`320a7b4`).
The brain map and the lab's section map are built (D-021, D-022).

What is left is the display half — M3, the gallery, the tests — and, before it, today's consolidation (D-024): the brain map
folds into the endpoint lab with a FIELD layer, and one page shows every endpoint of one app. Nothing propagates to the twins
(D-023): every page reads the frozen lab feed.

## Rules in force

| rule | where |
|---|---|
| No propagation; pages read the frozen feed | D-023 |
| Display is decided by seeing built options; generation comes first | D-025.1 |
| A choice made alone is a rail option, the agent's pick marked | D-025.2 |
| Facts are generated, never typed | D-025.3 |
| A click path comes from a real-click walk | D-025.4 |
| A ruling goes into `decisions.md` in the same commit that acts on it | D-025.5 |
| Analysis, not diagnosis | D-025.6 |
| Explanations are pictures, one part per page | D-025.7 |
| He reviews and rates on a generated decision page | D-025.8 |
| Two navigation systems are explained on a page before he rules | D-025.9 |
| Regions: COMMAND concepts · MIDDLE specifics · PORTRAIT detail | D-008 |
| Hovers: very short on a control, richer on an element | D-009 |
| About the code is shown; about the map sits behind "more information" | D-017 |
| The kinds of ending are fixed slots, drawn even when empty | D-012 |
| One stage spine for every endpoint | D-001 |
| Six parts; nothing the station shows is lost | D-026 |
| The Pattern Book laws | D-027 |
| Go shallow | D-028 |
| One kind at a time; the lab is the API endpoint kind only | D-029 |
| A click always shows; Keep only is its own control | D-022 |
| The two words D-018 swept stay out of the agent's strings | D-018 |
| Artifacts start finished, and are centred | D-004, D-005 |

## Open for you — the ones that block the next build first

The next build is the fold (D-024).

1. **Which stage rule is the default.** Blocks the fold: it carries a default into the lab. BUILT as two options. Look:
   `brainmap-endpoint.html` → the grouping rail → *By stage* → the switch under it. The agent's pick (marked) puts a block
   under every stage it touches; D-021 as recorded puts it under *Across the stages*, which leaves all six stages and the bay
   empty.
2. **The two words D-018 swept.** They come from your own `questions.md` and your stage spine, which the brain map quotes
   word for word. Blocks the fold: those strings would enter the lab. Should the lab reword them, or does D-018 leave your
   own text alone? Look: the *By stage* branch labels in `brainmap-endpoint.html`. Not an option; it needs your word.
3. **Where the brain map lives in the lab**: the rail tab it is today · a column right of the panels · full width below the
   middle (your second-section sketch). NOT built yet: the fold builds the three as rail options, the pick marked, and you
   pick by looking.
4. **"The four standpoints the blocks name."** The brain map credits these words to you, and the blocks name nine. The
   transcript shows the phrase first in the agent's own message (2026-09-21), not in anything you typed (your sketch was not
   checked). Unless you meant four, all nine stand. Look: `brainmap-endpoint.html` → grouping → by standpoint.
5. **The other brain-map picks.** Each is BUILT as the agent's pick and marked; no alternative is built. Endings and Proof sit
   at six rows each · Keep only keeps the node opened last · the sweep starts from the last-opened node · what the root
   opens · whether opening a second branch closes the first. Look: `brainmap-endpoint.html`, with two branches open; Keep
   only sits in the Filter group at the end of the rail. Also: `matrices-round2.html` shows the two cuts as a switch, not
   side by side.
6. **Which face M3 starts from.** Blocks M3, not the fold. Look: `inventory-endpoint.md` § "The face this inventory
   suggests" against the face M2 gives by rule on `matrices-round2.html`.
7. **The command layout**: its picks and the colour-by option (B13) are BUILT as options and have never been ruled. Two known
   conflicts come with it: PATHS lists status codes in COMMAND (D-008 says kinds), and controls carry long hovers (D-009).
   Look: `workflow-panel/endpoint-lab.html` → the COMMAND block of the rail. Waits for the display step.
8. **The five click obstacles from 2026-09-21**: the icon-only Exit tab · a row below the portrait's fold · the resting hover
   covering most ending cells · "Data" naming two controls · "Exit" vs "ending". Look: `program-leftovers.md`, and the
   pictures from `workflow-panel/walk-what-it-does.mjs`. These can wait until the display work opens.
9. **Per-arm acceptance of the generation.** Look: `element-forms/goldens/acceptance.html`. Not needed for the fold, but no
   station feed carries arm data until you accept. D20's split trigger has also fired on the lab feed: `forms.json` is now
   larger than `archmap.json`.

## Next, in order

1. **The fold** — its steps and its retirement condition are D-024 Consequence (2); D-032 lists what it inherits. Step one
   is in the working tree, not committed: `gen-brainmap.js` also writes the tree as `brainmap-endpoint.json` (`--check`
   says both outputs are current; the page is byte-identical to `b9d6a75`), and `_ep_sectionmap.py` reads the JSON. Before
   the rail changes: pin his head-bar, part-buttons and frame lines word for word, with a comment on whose each pinned line
   is (D-027 — today only his data line is pinned).
2. **The all-endpoints page** for gustify (D-024): built in the working tree, not committed —
   `workflow-panel/gen-all-endpoints.py` → `all-endpoints.html`, with its probe, its walk and the walk's pictures. The
   layout is an option on the page, the agent's pick marked; it waits for his look.
3. **M3**: the channel budget for the first prism.
4. **The gallery**: 3–5 variants of the card at true size, one fader each, judged by seeing. Then the tests, and promote.

Later: piece 9 (D-016) · 12b, the second round of in-flight detectors (D-019) · the robot for every kind (his 2026-09-17 ask,
`workflow-panel/robot-brief.md`) · the "more information" toggle (D-017, due at the display step) · pages for the three blocks
with none yet: Overview and risk, In-flight state, Standard or specialist (D-022) · a `suite-backlog.md` line for the parked
WORLD region (D-031).
