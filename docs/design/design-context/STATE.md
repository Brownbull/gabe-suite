# Where loop 1 stands

One page, kept short on purpose. Read this before the transcript, and before `method.md`.
Updated 2026-09-22 · suite `eae286e` on `graft-adoption`, pushed to both remotes.

---

## Ruled and done

| | |
|---|---|
| **The inventory** | 61 attributes, **fully ruled** — 26 at 3 · 25 at 2 · 9 at 1. Nothing is a proposal (D-020). |
| **The blocks** | 11, ruled by hand (D-015). M1 round 2 now says **8** — see *Open* below. |
| **The ten pieces** | All landed. The last, piece 11, is Slice 12 of the element forms (`320a7b4`). |
| **The generation** | Element forms amendment 1 is complete through Slice 12. 96 mutants, 96 red. Doctor CLEAN. |
| **The navigator** | `brainmap-endpoint.html` — the 11 blocks as a tree, five placements, the moves each node offers. |
| **The lab** | `endpoint-lab.html` — the card's section map is its third rail tab, wired both ways, with a feedback copy. |

## Open — each needs one word from Gabe

1. **Eight blocks or eleven.** Round 2 re-cut the matrix to 8: Proof folded into Endings, and Stages + Gates +
   Standard-or-specialist merged. Six blocks held. The cut is parameter-free and the merge survived four attempts to
   prove it an artifact. His eleven are RULED, so nothing was changed. **Gates the matrices page.**
2. **Stages or sections** for the brain map's third grouping rail. Nothing in the data maps a block to one of his six
   stages, so the inventory's seven sections were used rather than inventing a mapping.
3. **The eleven block→surface pairings** in the lab's section map — the whole join is the agent's proposal. The four
   worth arguing with first: Endings → the command panel · In-flight state → the portrait's path record · Standard or
   specialist → Security · Stages and order → the Data panel's stage blocks. The tab's feedback copy exists for this.
4. **The moves** each node offers in the brain map (Show · Keep only · Open · Follow, against the middle · the portrait ·
   the map · here). A proposal, marked as one on the page.

## His to run

- **The twins are unpushed**: gustify 36 commits ahead of `origin/staging`, gastify 16 ahead of its branch. Both clean.
  The suite is pushed; the twins' push and PR were always his.

## Next, in order

1. **The second section in the lab** (his ask, 2026-09-22, not started): split the right side below the middle panel,
   the rail and the command panel, and show *everything* an API endpoint has in indented form — highlighting which parts
   the panels are drawing as he navigates. It is the section map turned outward: the map answers "which block", this
   answers "which of the endpoint's own facts is on screen right now".
2. **M3** — the channel budget for the first prism. Waits on ruling 1.
3. **The gallery** (step 4 of the method): 3–5 variants of the card at true size, one fader moved each, judged by seeing.
4. **Piece 9** — "Why a touch happens, and why it survives". Its trigger fired long ago; he ruled it after piece 11.
5. **12b** — the second round of in-flight detectors. Its trigger fired on gastify and tier3; today those gaps are said
   on the rows, never hidden.

## The two rules that governed everything here

- **Display is decided by SEEING built options**, never from text. Every choice the agent would make alone is built as a
  switchable rail option.
- **Facts are generated, never typed.** A number inside an authored sentence is a token the generator fills.
