{
 "_about": "The content review of review-leftovers.html, 2026-09-20: four agent reviewers (truth · exact places · fairness of the choices · can he read it) and one agent verifier over the deduped union. Never hand-edited. Every CONFIRMED or PARTLY finding of importance 2 or 3 was applied; the refuted ones are kept with the verifier's reason.",
 "raw": 68,
 "unique": 65,
 "confirmed": [
  {
   "id": "vocab",
   "claim": "The rest of the lab now says \"endpoint\" and \"login check\".",
   "problem": "Both halves are false. The lab's drawn text still says \"door\"/\"lock\" in 128 quoted strings across 124 lines of _lab-ep-panels.js and 24 more in endpoint-lab.html — section headers (`sechd(\"key\", \"This door's deps\"` :500), a legend row (`t: \"the door\"` :460), hover-card bodies (:88-89, :115, :120, :145, :203), and the GATE plain line itself (:999 \"the lock on the door — who may knock…\"). \"login check\" appears ZERO times in either drawn file (only inside generator Python: gen-endpoint-facts.py:599, _ep_pieces.py:52). The card therefore prices the vocab ruling as 11 lines of work when replacing the words means ~150 drawn strings across two files; the operator rules \"replace them\" on a false cost.",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:460, :500, :999 (128 quoted-string hits on 124 lines); docs/design/workflow-panel/endpoint-lab.html:99, :292-302 (24 hits); grep \"login check\" in both files = 0 matches; review.words.json decisions[0].what",
   "fix": "what: \"{oldWords} of the {norms} lines still say \\\"door\\\" or \\\"lock\\\". So does the rest of the lab: {doorElsewhere} more drawn strings in _lab-ep-panels.js and endpoint-lab.html still say them — panel heads, hover cards, the header \\\"This door's deps\\\", the GATE stage's own plain line (\\\"the lock on the door\\\"). \\\"login check\\\" appears in only two generated lines. Replacing the words in these {oldWords} lines alone leaves them disagreeing with their neighbours; replacing them everywhere is a sweep of the whole lab.\" — {doorElsewhere} is a NEW generated token in gen-review.js (count /\\b(doors?|lock)\\b/ inside quoted strings of the two files, outside the STAGE_EXPECT literal; about 140 today), never typed. options: [\"sweep\",\"replace them everywhere in the lab\"] · [\"replace\",\"replace them in these lines only\"] · [\"keep\",\"keep door and lock\"]; mine: \"sweep\". why: \"One word per thing. The evaluation page says endpoint and the lab's newer rows do too; its older strings still say door, so only a sweep gets to one word.\"",
   "severity": "wrong",
   "lens": "truth",
   "index": 0,
   "verdict": "CONFIRMED",
   "importance": 3,
   "why": "The second sentence is false and it is the one that prices the ruling. Outside comments, _lab-ep-panels.js still has 127 'door(s)' inside quoted strings on 122 lines (e.g. :69, :87-89, :460 't: \"the door\"', :500 \"This door's deps\", :999 'the lock on the door') and endpoint-lab.html has 25. 'login check' is in neither drawn file (0 hits); it exists only in two GENERATED strings of _lab-ep.js (users.found_why, piece words 'its login check can create a row'). Exactly 11 STAGE_EXPECT lines carry door/lock (checked :1237-1280). So 'replace them' is sold as an 11-line edit while the lab around it still says door ~150 times. The card's `why` ('the rest of the lab already say endpoint') repeats the same false claim."
  },
  {
   "id": "vocab",
   "claim": "11 of the 36 lines still say \"door\" or \"lock\". The rest of the lab now says \"endpoint\" and \"login check\".",
   "problem": "The second sentence is false, and it is the sentence that sizes the work. \"door\" appears 173 times across the lab (144 in _lab-ep-panels.js, 29 in endpoint-lab.html) — the 11 STAGE_EXPECT lines are ~6% of them. Many of the other 162 are DRAWN, not comments: G2PLAIN.data \"every table this door reads or writes…\" (_lab-ep-panels.js:3072), DSTG GATE plain \"the lock on the door — who may knock\" (:999), the Data panel head \"every table this door touches\" (:69), the Widening card \"also returned by … other door(s)\" (:200). And \"login check\" appears NOWHERE in the lab (0 hits in _lab-ep-panels.js and endpoint-lab.html); it is a phrase from the evaluation prose, not a word the lab draws. Ruling \"replace them\" therefore buys a 11-line edit while the operator believes the rest is already done.",
   "evidence": "docs/design/design-context/review.words.json:173; grep -o '\\bdoor\\b' docs/design/workflow-panel/_lab-ep-panels.js | wc -l = 144, endpoint-lab.html = 29; grep -n 'login check' on both files = 0 hits; drawn instances at _lab-ep-panels.js:69, :999, :3072",
   "fix": "Same final fix as index 0: the sentence with the generated token {doorElsewhere}, the line \"\\\"login check\\\" appears in only two generated lines\", the third option [\"sweep\",\"replace them everywhere in the lab\"] as the dashed pick, and the corrected why.",
   "severity": "wrong",
   "lens": "where",
   "index": 14,
   "verdict": "PARTLY",
   "importance": 3,
   "why": "Same defect as index 0 and it is real: the rest of the lab still says door and nothing drawn says 'login check'. But the finding's number is inflated — 173 (144 + 29) counts block comments too; outside comments it is 125 in _lab-ep-panels.js (127 door(s) inside quoted strings) and 25 in endpoint-lab.html. And 'login check appears NOWHERE in the lab' is not quite right: two generated strings in _lab-ep.js that the lab draws carry it (users.found_why; the piece words 'its login check can create a row'). The fix must not type '173'."
  },
  {
   "id": "p10",
   "claim": "\"It adds, on every place the client reads a failure, the list of what that branch does.\" · why: \"The lab half already shows where each branch is. This fills the one empty row left on that section.\"",
   "problem": "Purely upside; the plan's own numbers say the row often stays empty and the payoff is small. (a) The design predicts 4 of the 14 existing sites carry a WORD and no list at all (2 `returned`, 2 `no-rows`), and risk 4 says in so many words that if `returned` plus `no-rows` is most of a target \"the operator may read that as the part being empty\". (b) The lab half measured that only 6 of this app's 80 endpoints have client code that reads their failures — so the filled row appears on 6 endpoints in 80. (c) Risk 15: nothing downstream reads `frontend{}` — form_drift, pulse S20, review FORM DRIFT and gabe-map are all blind to it, so no rail notices if `does[]` regresses on a twin. None of the three is on the card; the operator is ruling 1–2 days of work on \"it fills the one empty row\".",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/element-forms/plans/slice-11e-does.plan.json plan.risks[8] (\"10 read · 2 returned · 2 no-rows\"), risks[4], risks[15]; /home/khujta/projects/gabe_lens/docs/design/design-context/program-leftovers.md:51 (\"6 have client code that reads their failures\"); claim at review.words.json:482 and :498",
   "fix": "what: \"It adds, on every place the client reads a failure, the list of what that branch does. In this app {feReaders} of {appEndpoints} endpoints have client code that reads their failures, so the row appears there; the design predicts that some of those places carry a word instead of a list (returned · no-rows).\" ({feReaders} read from the measured inventory row, never typed.) why: \"The lab half already shows where each branch is; the row for what the branch does is not drawn until this lands. The cost beyond the build: nothing downstream reads this block, so only its own battery would catch a regression. If that row would not change how you read an ending, hold it.\"",
   "severity": "misleading",
   "lens": "fair",
   "index": 34,
   "verdict": "CONFIRMED",
   "importance": 3,
   "why": "The card is upside only and its `why` is false (index 2). The author's own review-plan.md (step 3) carried the counter-argument the page dropped: 'Before ruling, look at what it fills … If that row would not change how you read an ending, hold it.' Measured and logged: 6 of 80 endpoints have client code that reads their failures (program-leftovers.md piece 10; inventory-endpoint.md:117). slice-11e-does.plan.json risks[8] predicts '10 read · 2 returned · 2 no-rows' for the 14 existing sites, risks[4] warns a word-only result 'may read as the part being empty', risks[15] says nothing downstream reads frontend{} so only its own battery catches a regression."
  },
  {
   "id": "vocab",
   "claim": "\"11 of the 36 lines still say \\\"door\\\" or \\\"lock\\\". The rest of the lab now says \\\"endpoint\\\" and \\\"login check\\\".\"",
   "problem": "Both halves of the second sentence are false, and the falsehood hides the cost of the option the page recommends. The lab still says \"door\" in ~150 live (non-comment) places outside the 36 norm lines, and the phrase \"login check\" appears nowhere in the lab at all. The operator reading this card believes he is approving an 11-line edit; \"replace them\" actually means either 11 lines that then disagree with 150 neighbours, or a ~160-site sweep. The recommended pick is priced wrong.",
   "evidence": "docs/design/design-context/review.words.json:173 (the claim). Counter-evidence: `docs/design/workflow-panel/_lab-ep-panels.js` has 125 occurrences of \\bdoor\\b outside block comments (e.g. :69 \"every table this door touches\", :87 \"table(s) this door touches\", :999 \"the lock on the door\") and `docs/design/workflow-panel/endpoint-lab.html` has 25 (e.g. :298 \"the URL the frontend calls — the door's address\"); grep for \"login check\" across both files returns 0.",
   "fix": "Same final fix as index 0.",
   "severity": "wrong",
   "lens": "voice",
   "index": 40,
   "verdict": "CONFIRMED",
   "importance": 3,
   "why": "Same defect as index 0, with counts that match mine: 125 'door' outside block comments in _lab-ep-panels.js, 25 in endpoint-lab.html, 0 'login check' in both drawn files (two generated strings in _lab-ep.js carry it). The recommended pick is priced as an 11-line edit. The third option is the right repair; the typed '150'/'160' in the proposed sentence must become a generated token."
  },
  {
   "id": "case-roles",
   "claim": "Every test case is stamped with why it called this endpoint. … [option] these four, in these words",
   "problem": "Two errors. (a) Two of the four words say the test did NOT call the endpoint: `service-raises` = \"proves an ending from the service side, without calling the endpoint\" (3 cases here) and, in the full roster, `named-only` = \"listed by the map, makes no call here\". (b) The roster has FIVE words, not four. `named-only` is minted at gen-endpoint-facts.py:634 and worded at _lab-ep-panels.js:2819, but gen-review.js:43 derives the list from `Object.keys(L.tests.roles)` — the roles PRESENT on this endpoint (4) — so the fifth word is never shown and cannot be ruled, while the option text says \"these four, in these words\". The template hardcodes \"The four reasons\" (review.tpl.html:223), so on an endpoint that does carry five the sentence and the list disagree.",
   "evidence": "docs/design/workflow-panel/gen-endpoint-facts.py:634 and :646 (roles_note lists only four); docs/design/workflow-panel/_lab-ep-panels.js:2819 (ROLEWORD has five); docs/design/design-context/gen-review.js:43; docs/design/design-context/review.tpl.html:223; review.words.json decisions[4]",
   "fix": "title: \"The reasons a test case is listed on this endpoint\". what: \"Every test case is stamped with how it reached this endpoint — or that it never called it. The lab can stamp {roleWords} words; {rolesHere} occur here, and the rest are shown with a count of 0 so you rule the whole set.\" option: [\"ok\",\"these words are right\"]. why: \"The words come from how the test really calls the endpoint, not from its name.\" gen-review.js:43 → Object.keys(ROLEWORD).map(k => ({role:k, words:ROLEWORD[k], n:L.tests.roles[k]||0})), keeping the die() for a role in L.tests.roles that has no word; add TOK.roleWords and TOK.rolesHere. review.tpl.html:223 heading → \"The \" + v.length + \" reasons the lab can stamp, in its own words, with their counts here\". In the lab, add named-only to roles_note at gen-endpoint-facts.py:646.",
   "severity": "wrong",
   "lens": "truth",
   "index": 1,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Both halves hold. ROLEWORD at _lab-ep-panels.js:2819-2820 declares FIVE words (act · arranged · service-raises · helper-arranged · named-only); gen-endpoint-facts.py:634 mints `named-only`, and :646 roles_note lists only four. gen-review.js:43 builds the list from Object.keys(L.tests.roles) = the four roles present here ({act 12, arranged 14, service-raises 3, helper-arranged 134}), so the fifth is never shown while the option says 'these four, in these words'. And 'why it called this endpoint' is wrong for the 3 service-raises cases here, whose own words say 'without calling the endpoint'. 'four' is typed in the title, the option, the why and review.tpl.html:223, against the generator's own law (gen-review.js:59)."
  },
  {
   "id": "p10",
   "claim": "The lab half already shows where each branch is. This fills the one empty row left on that section.",
   "problem": "There is no empty row. `clientRows` pushes the \"what it does\" row only when `rt.does` is truthy (`if (rt && rt.does)`), and the generation part is not built, so the row is omitted entirely rather than drawn empty. The shoot run confirms it: review-shots/clicks.json records `ending.client = [\"its branch\",\"tried again\"]` — two rows, no third. The recommendation \"land it\" rests on filling a hole the operator cannot see, and the omission also contradicts his own D-012 (an empty slot is drawn hollow, never removed).",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:2835; docs/design/design-context/review-shots/clicks.json → ending.client; docs/design/design-context/decisions.md:75-79 (D-012); review.words.json decisions[12].why",
   "fix": "why: \"The lab half already shows where each branch is. The row for what the branch does is not drawn at all today — the lab leaves it out while the feed has nothing for it (_lab-ep-panels.js line 2835). This part is what would make it appear.\"",
   "severity": "wrong",
   "lens": "truth",
   "index": 2,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "_lab-ep-panels.js:2835 is `if (rt && rt.does) out.push([\"what it does\", …])` — the row is pushed only when the feed carries `does`, and every route in LABEP.forms.frontend.readers[0].routes has does: null. review-shots/clicks.json ending.client = [\"its branch\",\"tried again\"] and ending-sections.png shows exactly those two rows under ON THE CLIENT. There is no empty row to fill; the operator cannot see the hole the recommendation rests on."
  },
  {
   "id": "s5note",
   "claim": "These rows were added after your last rating. Each one now shows a real value from this endpoint.",
   "problem": "The second sentence is false by the rating sheet's own design. gen-rate-sheet.js returns `null` for `in-flight-values` (:93) and `events-published` (:81); the `LANDS`/`NONE` map (:147-148) then prints \"nothing to quote yet · piece 11 of the work brings it\". The generator's own comment at :86 says so: \"real values where the lab already holds the fact, else honest-empty until the piece lands\". The inventory agrees — its cardinality cell for that row reads \"to measure when piece 11 lands\".",
   "evidence": "docs/design/design-context/gen-rate-sheet.js:81, :86, :93, :147-148; docs/design/design-context/inventory-endpoint.md:73; review.words.json ui.s5note",
   "fix": "s5note: \"These rows were added after your last rating. Each one shows what this endpoint really has, except in-flight values, which has nothing to quote until piece 11 is built. The rows for events, tasks and outside services read \\\"none on this endpoint\\\" — a measured zero — and the row for what the screen does shows where the branch is, not yet what it does. Rate those on what the row would be worth, or leave them.\"",
   "severity": "wrong",
   "lens": "truth",
   "index": 3,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "The sentence is false for in-flight values: gen-rate-sheet.js:93 returns null and :147-148 print 'nothing to quote yet · piece 11 of the work brings it'; inventory-endpoint.md:73 says 'to measure when piece 11 lands'. review-plan.md:52-53 carried the caveat ('except in-flight values') and the page dropped it. But the finding's fix is wrong about events-published: that row prints 'none on this endpoint' (NONE map, :148) — a measured zero, not a row waiting for a piece."
  },
  {
   "id": "ending-sections",
   "claim": "Two new sections on an ending's record … An ending's record gained IN THIS APP, which counts how many endpoints can give the same answer, and ON THE CLIENT …",
   "problem": "Only ON THE CLIENT is a section (`ptSec(\"on the client\")`, _lab-ep-panels.js:3519). IN THIS APP is a ROW appended to the record's top block via `commonRows` (:3498) — there is no `ptSec(\"in this app\")` anywhere in the file. The operator is told to \"scroll the record down\" to find two sections and will find one section plus a row near the top. Second, the row counts endpoints that can answer the same STATUS CODE (`commonRows(F, \"status:\" + e.status)` → \"can answer 409 · 53 of 80\"), not the same answer: a 409 \"setup in progress\" and a 409 \"consent required\" are different answers with the same code.",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:3498 vs :3519; the full `ptSec(\"…\")` roster in that file contains no \"in this app\"; review.words.json decisions[id=ending-sections].what",
   "fix": "title: \"Two new rows and a new section on an ending's record\". what: \"An ending's record gained two rows near the top, IN THIS APP and IN THE OTHER APPS, which count how many endpoints can answer with the same status code, and lower down the section ON THE CLIENT, which says which client branch the ending reaches.\" where step 3: \"Read the rows IN THIS APP and IN THE OTHER APPS just under the row AT, then scroll down to the section ON THE CLIENT.\" options: [\"keep\",\"keep the rows and the section\"] · [\"move\",\"move them to hover cards\"] · [\"option\",\"build them as an option\"].",
   "severity": "misleading",
   "lens": "truth",
   "index": 5,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "_lab-ep-panels.js:3496-3498 appends commonRows(F, \"status:\" + e.status) as ptRow rows right after `at`, BEFORE ptSec(\"response\") (:3499); the only new section is ptSec(\"on the client\") at :3519 — no ptSec(\"in this app\") exists. ending-sections.png shows it: rows IN THIS APP ('can answer 409 — 53 of 80 endpoints · common') and IN THE OTHER APPS under AT, then the sections RESPONSE and ON THE CLIENT. The count is per STATUS CODE (key status:409), not per answer — this endpoint has two different 409s."
  },
  {
   "id": "rules-on-hover",
   "claim": "Every column now carries its rule: may it be empty, what fills it, what a delete elsewhere does, its line. … [where] Part DATA, click the table block locations. In the portrait, hover the field order.",
   "problem": "The delete row is drawn only for a foreign-key column — `if (c.fk) out.push([\"if the row it points at is deleted\", …])` — and the column the card sends him to has no FK: `locations.order` carries `\"fk\": null`. `locations` has exactly one FK column, `household_id`. So the one demonstration the card names is the one where a quarter of the claimed rule is missing, and \"Every column\" is wrong regardless.",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:2859 (`if (c.fk)`); LABEP.data.tables[table=locations].rules.columns.order.fk = null (only household_id has one); review.words.json decisions[id=rules-on-hover].what and .where",
   "fix": "what: \"Every column now carries its rule: may it be empty, what fills it, its line — and, on a column that points at another table, what a delete there does. I put it on the hover card, because you had already ruled on the record's rows.\" where: \"Part DATA, click the table block locations.\" · \"In the portrait, hover the field household_id — the one column of this table that points at another table.\" Re-take column-rule.png on household_id in shoot-review.mjs.",
   "severity": "misleading",
   "lens": "truth",
   "index": 6,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "columnRuleRows (_lab-ep-panels.js:2859) pushes the delete row only `if (c.fk)`. LABEP.data.tables[locations].rules.columns.order.fk is null; household_id is the table's only FK column (CASCADE → households.id). column-rule.png (taken on `order`) indeed shows no delete row. So the one demonstration the card names lacks a quarter of what the sentence claims, and 'Every column … what a delete elsewhere does' is false."
  },
  {
   "id": "thresholds",
   "claim": "In the lab: pick any ending, open its record, read the row IN THIS APP.",
   "problem": "Not any ending. `exitPortrait` swaps `rateRows(F)` in for `commonRows` when the status is 429, and `rateRows` emits \"rate tier\" and \"the other tiers\" — no IN THIS APP row and no word at all. Two of this endpoint's fourteen endings are 429s (rate limit (sensitive), rate limit (global)), so the instruction fails on 2 in 7 picks. The card's opening sentence \"Every count of a piece carries a word\" is wrong for the same reason.",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:3498 (`e.status === 429 ? rateRows(F) : commonRows(…)`) and :2877-2879 (rateRows emits no `r.word`); LABEP.forms.paths — two paths with status 429; review.words.json decisions[1].what and .where",
   "fix": "what: \"Almost every count of a piece carries a word. Two cut-offs I chose decide the norm and rare, and one rule sits above both: a piece only one endpoint carries always reads only here. The rate tier is the exception — it shows its tiers and no word.\" where: \"COMMAND panel, PATHS, Walk a path, pick setup in progress.\" · \"In the portrait header, click the second icon, Exit.\" · \"Read the row IN THIS APP, just under the row AT — the word ends the line. A rate-limit ending shows its rate tier there instead, with no word.\" · \"In code: docs/design/workflow-panel/_ep_pieces.py line 65, the function word_for.\"",
   "severity": "misleading",
   "lens": "truth",
   "index": 7,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "_lab-ep-panels.js:3498: `e.status === 429 ? rateRows(F) : commonRows(…)`; rateRows (:2877-2879) emits 'rate tier' and 'the other tiers' with no `word` and no IN THIS APP row. Two of the 14 endings in LABEP.forms.paths are 429s, so 'pick any ending' fails on those, and 'Every count of a piece carries a word' is false for the rate tier."
  },
  {
   "id": "copy",
   "claim": "NORM LINES (ok · strike · reword) — 36 lines, only the ones you touched are listed",
   "problem": "The paste-back text contradicts itself two lines later. After printing the touched norms, `buildText` appends `untouchedDoubt` — every norm whose pick is not \"ok\" and that carries no verdict — so data.GATE is listed even when untouched. Worse, a norm the operator gave a NOTE but no verdict satisfies BOTH filters (`touched` tests `s.v || s.n`; `untouchedDoubt` tests only `!s.v`) and is printed twice in the text he pastes back to me.",
   "evidence": "docs/design/design-context/review.tpl.html:259 (touched filter), :261 (the claim), :263 (untouchedDoubt)",
   "fix": "review.tpl.html:261 → out.push(\"NORM LINES (ok · strike · reword) — \" + D.norms.length + \" lines; listed: the ones you touched, and any I doubt that you left alone\"); :263 → var untouchedDoubt = D.norms.filter(function (n) { return n.mine !== \"ok\" && touched.indexOf(n) < 0; }); and print \"none touched\" only when both lists are empty.",
   "severity": "wrong",
   "lens": "truth",
   "index": 8,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "review.tpl.html:259 `touched` = s && (s.v || s.n); :263 `untouchedDoubt` = n.mine !== \"ok\" && !s.v. A doubted line (data.GATE) with a note and no verdict satisfies both and is printed twice; and the header at :261 says 'only the ones you touched are listed' while the doubted line is listed even untouched (after 'none touched')."
  },
  {
   "id": "p11",
   "claim": "About four days of work, and its own evaluation ranks it lowest of the three parts. The only decision today is when.",
   "problem": "\"The only decision today is when\" is contradicted by the card directly under it: the p11 options are after-10 · now · **drop**, and \"drop it\" is not a when. It also reopens a settled ruling without saying so — D-016 already put piece 11 in DO, explicitly overriding the agent's \"later\". Offering a silent drop against his own decision, under a note that says no such decision is being asked, is the kind of thing that gets clicked by accident.",
   "evidence": "review.words.json ui.s4note vs decisions[id=p11].options; docs/design/design-context/decisions.md:104 (D-016 — \"DO … 11 What stays alive during the request (the agent had said 'later' on 8, 10 and 11)\")",
   "fix": "s4note: \"About four days of work. Of the three pieces that need a new generator arm — piece 9 (ruled later), piece 10 and this one — the evaluation ranks this one last. You ruled it do in D-016, so the first card asks when, and dropping it would reopen that ruling; the second card asks whether it keeps its name.\" p11 option label: [\"drop\",\"drop it — this reopens D-016\"].",
   "severity": "misleading",
   "lens": "truth",
   "index": 9,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "ui.s4note says 'The only decision today is when', yet the p11 card offers [\"drop\",\"drop it\"] and step 4 holds a second card (p11-name). decisions.md:104 (D-016) already rules piece 11 DO, overriding the agent's 'later' — a silent drop option reopens that ruling without saying so. The finding's 'importance 2 against 3 and 3' could not be verified and must not enter the fix."
  },
  {
   "id": "place-card",
   "claim": "The place reads {place} of {placeOf} by functions behind. → \"11th of 81\"",
   "problem": "81 is not the endpoint count. `context.risk.of` is 81 because the ranking includes the non-endpoint `BOOT lifespan` root (it is `heaviest[0]`, behind 135, and the `behind_bins` sum to 81); the app has 80 endpoints, which is the denominator every other number on this page uses (\"53 of 80 endpoints\", \"79 of 80\"). The sentence gives a bare \"of 81\" with no unit, so the operator rules on where to put a number whose denominator silently disagrees with the rest of the page.",
   "evidence": "LABEP.context.risk = {rank:11, of:81, behind_max_of:\"BOOT lifespan\", behind_bins summing to 81}; docs/design/workflow-panel/pieces-digest.json apps.gustify.endpoints = 80; review.words.json decisions[id=place-card].what",
   "fix": "what (first sentences): \"The place reads {place} of {placeOf} by functions behind — how many functions each one reaches, heaviest first. The {placeOf} are the app's {appEndpoints} endpoints plus {placeTop}, the app's startup, which the map draws as an endpoint and which is the heaviest; among the endpoints alone this one is {placeAmongEndpoints}.\" New generated tokens: appEndpoints = L.feedwide.forms_endpoints, placeTop = L.context.risk.behind_max_of, placeAmongEndpoints = ord(rank - 1) when the top of `heaviest` is not one of the forms endpoints.",
   "severity": "unclear",
   "lens": "truth",
   "index": 10,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "The page quotes the lab faithfully (place-everything.png: '11th of 81 by functions behind · … the biggest 135 (BOOT lifespan)'), so the sentence is not false. But LABEP.context.risk.of = 81 because the ranking includes the non-endpoint root BOOT lifespan (heaviest[0], 135 behind), while every other count on the page is 'of 80' (feedwide.pieces.of = 80, forms_endpoints = 80). The sentence gives a bare 'of 81' with no unit."
  },
  {
   "id": "roles-raise",
   "claim": "[facts] only a raise answered here: 5 · … / a raise alone never counts: 5 · …",
   "problem": "Two of the three options are indistinguishable on this endpoint and nothing says so. The only function marked by a raise alone is `_load_setup_result`, and both of its raises carry `here_word: \"beyond one level\"` with empty `here`/`uncaught_here` — so `answered()` is false and options \"answered\" and \"never\" yield the identical five names. The operator is asked to choose between three rules while the evidence shown separates only two, with no line telling him the middle one changes nothing here.",
   "evidence": "docs/design/design-context/gen-review.js:36 (`answered`) and :41; LABEP.forms.inside.functions[fn=apps/api/services/setup.py::_load_setup_result].raises[*].here_word = \"beyond one level\"; docs/design/design-context/review.tpl.html:219",
   "fix": "Add one GENERATED line under the three counts in review.tpl.html (roles branch), shown when ifAnswered and ifNever are equal: \"On this endpoint the last two options give the same list: the only function marked by a raise alone is \" + v.onlyRaise.join(\", \") + \", and this endpoint answers none of its raises (the map reads them as beyond one level). The two options differ only where an endpoint answers such a raise.\"",
   "severity": "unclear",
   "lens": "truth",
   "index": 11,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Recomputed from LABEP: onlyRaise = [_load_setup_result]; its two raises both carry here_word 'beyond one level' with empty `here`, refusals = 0, so gen-review.js:36 `answered` is false and ifAnswered == ifNever == [setup_complete, get_auth_context, complete_setup, claim, bearer_scheme]. review.tpl.html:219 prints the two identical lists with no line saying so — three options, evidence for two."
  },
  {
   "id": "ui.s5note",
   "claim": "These rows were added after your last rating. Each one now shows a real value from this endpoint.",
   "problem": "False for at least four of the 20 (proposed) rows, and the review plan this page was built from carried the caveat that the page drops. inventory-endpoint.md:73 (in-flight values) reads \"to measure when piece 11 lands\"; :64 (tasks dispatched) reads \"0 of 80 here (the app has no queue)\"; :63 (events published) is \"1 of 80 endpoints publishes\" and this is not it; :117 (what the screen does) says the DOES half \"waits for the generation part\". review-plan.md:52 said \"Every one now shows a real value from this endpoint, except in-flight values\" — the caveat was cut when the sentence moved onto the page.",
   "evidence": "docs/design/design-context/review.words.json:50; docs/design/design-context/inventory-endpoint.md:63, :64, :73, :117; docs/design/design-context/review-plan.md:52",
   "fix": "Same final s5note as index 3: \"These rows were added after your last rating. Each one shows what this endpoint really has, except in-flight values, which has nothing to quote until piece 11 is built. The rows for events, tasks and outside services read \\\"none on this endpoint\\\" — a measured zero — and the row for what the screen does shows where the branch is, not yet what it does. Rate those on what the row would be worth, or leave them.\"",
   "severity": "wrong",
   "lens": "where",
   "index": 15,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "inventory-endpoint.md:73 (in-flight values: 'to measure when piece 11 lands'), :64 (tasks: '0 of 80 here'), :63 (events: 1 of 80 publishes, not this one), :117 (the DOES half 'waits for the generation part'); gen-rate-sheet.js prints 'nothing to quote yet' for in-flight and 'none on this endpoint' for events/tasks ('none drawn' for outside services — LABEP.conns.out has no dispatches/consumes). review-plan.md:52 carried the in-flight caveat the page dropped."
  },
  {
   "id": "ending-sections",
   "claim": "Two new sections on an ending's record — An ending's record gained IN THIS APP … and ON THE CLIENT … / step 3: Scroll the record down.",
   "problem": "Only one of the two is a section, and the step sends the operator away from the other. ON THE CLIENT is a real section (`ptSec(\"on the client\")`, _lab-ep-panels.js:3519) low in the record. IN THIS APP is a ROW appended to the record's FIRST rows block, immediately after `at` and BEFORE `ptSec(\"response\")` (:3498). \"Scroll the record down\" moves past it, and the operator hunting for a second heading will not find one.",
   "evidence": "docs/design/design-context/review.words.json:398, :399, :404; docs/design/workflow-panel/_lab-ep-panels.js:3496-3498 (IN THIS APP as a ptRow in the top block) vs :3519 (ptSec on the client)",
   "fix": "Same final fix as index 5 — title \"Two new rows and a new section on an ending's record\"; step 3: \"Read the rows IN THIS APP and IN THE OTHER APPS just under the row AT, then scroll down to the section ON THE CLIENT.\"",
   "severity": "misleading",
   "lens": "where",
   "index": 16,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same evidence as index 5: IN THIS APP is a ptRow in the record's first block right after `at` (_lab-ep-panels.js:3496-3498), before ptSec(\"response\"); only ON THE CLIENT is a section (:3519). 'Scroll the record down' moves past the row. (ending-sections.png also shows a second new row, IN THE OTHER APPS, that the card never mentions.)"
  },
  {
   "id": "found-by",
   "claim": "Click the table block locations in the GATE stage. … why: 2 of this endpoint's 15 tables are known to a route's steps only, so the row changes whether you trust the list.",
   "problem": "The step points at a table that shows the uninteresting value. `locations` has found: \"both\", so its FOUND BY row reads \"the map's edge and a route's steps\" — one of 13 such tables. The two tables the `why` sells the row on are `users` (\"only the login check touches it, before the handler runs\") and `ai_spend_log`. The operator is asked to keep a row on the strength of a case the click path never shows them; the embedded screenshot shows the same boring value.",
   "evidence": "docs/design/design-context/review.words.json:320, :338; docs/design/workflow-panel/_lab-ep.js → LABEP.data.tables: locations.found = \"both\", users.found = \"route effects\", ai_spend_log.found = \"route effects\"; LABEP.forms.counts.tables_found = {both: 13, \"route effects\": 2}",
   "fix": "where: \"Part DATA.\" · \"Click the table block users in the GATE stage — one of the two tables only a route's steps know.\" · \"Read the row found by in the portrait on the right, and hover it for the reason.\" · \"Then click locations to see the same row on a table both sources found.\" why: \"{routeOnly} of this endpoint's {tables} tables ({routeOnlyNames}) are known to a route's steps only, so the row changes whether you trust the list.\" ({routeOnlyNames} generated from L.data.tables where found === \"route effects\"). Re-take found-by.png on users in shoot-review.mjs.",
   "severity": "misleading",
   "lens": "where",
   "index": 17,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "LABEP.data.tables: locations.found = 'both' (found-by.png shows 'the map's edge and a route's steps' — one of 13 such); the two tables the `why` is about are users and ai_spend_log (found = 'route effects'; forms.counts.tables_found = {both 13, route effects 2}). The effect steps place users under GATE only (dependency steps) and ai_spend_log under HANDLER, so users is one block away in the same band."
  },
  {
   "id": "case-roles",
   "claim": "The four reasons a test came to this endpoint … option: \"these four, in these words\"",
   "problem": "The lab declares FIVE role words, not four, and the option hides the fifth. ROLEWORD = { act, arranged, service-raises, helper-arranged, named-only } — `named-only: \"listed by the map, makes no call here\"`. The page shows four only because this endpoint's roster happens to carry no named-only case (LABEP.tests.roles has four keys). Ruling \"these four, in these words\" therefore approves four of the five words the lab can draw, and the fifth — the one that is hardest to word — goes unruled. The generator does not guard this either: gen-review.js:43 words whatever roles are present, and the count \"four\" is typed in three places (the title, the option label, and the facts-box heading in review.tpl.html:223).",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js:2819-2820 (five ROLEWORD entries); docs/design/design-context/review.words.json:271, :280; docs/design/design-context/gen-review.js:43; docs/design/design-context/review.tpl.html:223",
   "fix": "Same final fix as index 1 (count-free title and option, all ROLEWORD entries listed with 0 where absent, generated counts in the heading and the sentence).",
   "severity": "misleading",
   "lens": "where",
   "index": 18,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 1: ROLEWORD has five entries (_lab-ep-panels.js:2819-2820); the page shows the four present in L.tests.roles (gen-review.js:43) and types 'four' in the title, the option, the why and review.tpl.html:223."
  },
  {
   "id": "p10-word",
   "claim": "The design proposes a new word for it. … where: docs/design/element-forms/plans/slice-11e-does.plan.md, the section State words.",
   "problem": "Two problems. (1) The design's word is `returned`, and it is already shipped in the plan's roster — DOES_STATES = (\"read\",\"mixed\",\"empty\",\"returned\",\"no-rows\",\"unread\") — and the page's own p10 facts box prints it as one of \"The 6 words a branch can carry\" with the plain line \"a comparison handed back to the caller, who decides\". So the card asks the operator to choose between \"beyond one level\" and a word the page has just shown as settled, without saying that picking the first one replaces it. (2) There is no section called \"State words\": the file's headings are What is written · How a row is classed · Rosters · Code plan · Battery · What the endpoint lab reads · Landing order · Risks. The words live in a bold paragraph at :167 inside \"How a row is classed\".",
   "evidence": "docs/design/design-context/review.words.json:525, :527; docs/design/element-forms/plans/slice-11e-does.plan.md:195 (DOES_STATES), :167-173 (the bold \"State words\" paragraph), headings at :1/:6/:90/:188/:266/:373/:501/:567/:589; docs/design/design-context/gen-review.js:52 (states come from DOES_STATES) and review.tpl.html:236",
   "fix": "what: \"Some client functions only hand the comparison back, and their caller decides what happens. The design names that returned — one of the six words on the card above — and calls it its own spelling of the map's beyond one level. Ruling beyond one level renames it.\" where: \"docs/design/element-forms/plans/slice-11e-does.plan.md line 167, the bold block State words under the heading How a row is classed.\"",
   "severity": "unclear",
   "lens": "where",
   "index": 22,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "slice-11e-does.plan.md has no section 'State words'; its headings are What is written (:6) · How a row is classed (:90) · Rosters (:188) · Code plan · Battery · What the endpoint lab reads · Landing order · Risks. The words are a bold block at :167 under 'How a row is classed'. The design's word is `returned` (DOES_STATES at :195; :172-174 'It is this part's spelling of `beyond one level`'), which the p10 facts box on the same page already shows as one of the six words; the card never says which of its two options is the design's."
  },
  {
   "id": "roles-raise",
   "claim": "\"A function is marked as deciding an ending when it holds a fork, a catch, the answer, or a raise.\"",
   "problem": "False, and it hides a whole option. The code lights \"decides an ending\" from FIVE things, not four: the fifth is a dependency that can end the request (`decides[r[\"key\"]].add(\"can end the request\")`). On this very endpoint `bearer_scheme` is marked as deciding an ending by that fifth lighter ALONE — it holds no fork, no catch, no answer and no raise. So the operator is asked to rule on the raise while a lighter he has never been shown is doing the same work, and the three options (any-raise · answered · never) cannot touch it. The stated rule in the feed is wrong the same way.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_ep_joins.py:142-144 (the fifth lighter) and :169 (the rule string that omits it); LABEP.functions.does rows in /home/khujta/projects/gabe_lens/docs/design/workflow-panel/_lab-ep.js → `bearer_scheme :: can end the request` / does = [decides an ending, gives context]; claim at /home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:222",
   "fix": "what: \"A function is marked as deciding an ending when it holds a fork, a catch, the answer or a raise — and a helper is marked when one of the endpoint's endings is its own. The raise is the doubtful one: the map follows a raise one call up, and deeper than that it does not read which answer the raise becomes.\" Facts box, one more generated line: \"Marked because it can end the request: \" + names of rows whose why['decides an ending'] includes 'can end the request'. Fix the rule string at _ep_joins.py:169 to add \"· a helper whose own exit ends the request\". No new option.",
   "severity": "wrong",
   "lens": "fair",
   "index": 25,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "The sentence is incomplete: _ep_joins.py:142-144 lights 'decides an ending' from a FIFTH source — a dependency with exits ('can end the request') — and the rule string at :169 omits it too. In LABEP.functions.does, bearer_scheme is marked by that alone and get_auth_context by it plus a catch. But the proposed fourth option is refuted: a helper whose own exit IS one of the endpoint's endings (the 401s) is not the doubtful case, and a second question on one card breaks the one-verdict mechanism."
  },
  {
   "id": "place-card",
   "claim": "\"The place reads 11th of 81 by functions behind.\"",
   "problem": "The 81 is not 81 endpoints. The map counts `BOOT lifespan` (the app's startup) as an endpoint node, and it is the heaviest thing in the list (135 functions behind), so it takes place 1 and pushes every real endpoint down one. The app has 80 endpoints; among them this one is 10th of 80. This is the exact denominator bug the program already corrected once — piece 7's log says \"piece 6 divided forms counts by the map's 81 nodes (BOOT included) — forms counts now read 'of 80'\" — and it survives on the number the operator is being asked to place.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/gen-endpoint-facts.py:475 (`n_endpoints` counts kind==endpoint) and :530 (`_behind` built from the same set); LABEP.context.risk in _lab-ep.js = {rank: 11, of: 81, behind_max: 135, behind_max_of: \"BOOT lifespan\", behind_bins summing to 81}; the earlier correction at /home/khujta/projects/gabe_lens/docs/design/design-context/program-leftovers.md:49; claim at review.words.json:454",
   "fix": "Same final sentence as index 10 (say what the 81 are, with generated tokens, and give the place among the endpoints alone). Separately, log the lab defect: the ranking and the async count in gen-endpoint-facts.py take BOOT lifespan as an endpoint; fix there so the card reads 'of 80', and let {placeOf} follow.",
   "severity": "wrong",
   "lens": "fair",
   "index": 26,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "Confirmed that 81 = 80 endpoints + BOOT lifespan: gen-endpoint-facts.py:475/530 rank every node of kind endpoint, LABEP.context.risk = {rank 11, of 81, behind_max_of 'BOOT lifespan'}, and program-leftovers.md records the same denominator being corrected to 'of 80' for the forms counts. But the page quotes the lab's own drawn words (place-everything.png), and the lab card names BOOT lifespan beside the number — so the page is unit-less, not wrong. Whether BOOT stays in the ranking is a generator fix ('its place' and 'async 80 of 81' in endpoint-lab.html placeRows), not a sentence on this page."
  },
  {
   "id": "case-roles",
   "claim": "Title: \"The four reasons a test came to this endpoint\" · option: \"these four, in these words\"",
   "problem": "There are FIVE roles, not four. `named-only` — \"listed by the map, makes no call here\" — is minted by the generator and worded by the lab, but it is zero on this endpoint, so the page's fact list (built from the roles present here) never shows it and the operator rules four words while a fifth can be stamped on any other endpoint. It is also the one role whose words do not describe a reason a test CAME here (it says the test never called), which is precisely the kind of thing he should rule on. The lab's own roles_note has the same hole.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/gen-endpoint-facts.py:634 mints `named-only`, :646 `roles_note` lists only four; /home/khujta/projects/gabe_lens/docs/design/workflow-panel/_lab-ep-panels.js:2820 gives its words; gen-review.js:43 derives the list from `L.tests.roles`, which holds only the four present here; claim at review.words.json:271 and :280",
   "fix": "Same final fix as index 1 — count-free title and option, the facts box lists every ROLEWORD entry with its count here (0 for named-only), counts generated; add named-only to roles_note at gen-endpoint-facts.py:646.",
   "severity": "wrong",
   "lens": "fair",
   "index": 27,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 1 (five ROLEWORD entries; named-only minted at gen-endpoint-facts.py:634 and missing from roles_note at :646; list built from the roles present here). The proposed replacement ('The five reasons…', 'these five') just types a different count."
  },
  {
   "id": "norms.security.EDGE",
   "claim": "security · EDGE: \"the app-wide checks — the rate limit, the origin rules, the repeat key\" — carried on the page with Claude's pick \"ok\".",
   "problem": "This is the same defect the page flags on data·GATE (\"true of this app and not of the kind\"), and it is the one line the feed can disprove by itself: a repeat key is RARE here — 6 of 80 endpoints — and neither other backend read has a single one (gastify 0 of 49, tier3 0 of 499). A norm line that names, as what is normal at the EDGE of any endpoint of this kind, a piece this app's own tally calls rare and the other apps do not have at all, is wrong in exactly the way the operator is being asked to catch — but it is pre-verdicted \"ok\", so he has to find it himself while the page implies one line was doubted and the rest checked.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_lab-ep-panels.js:1275; LABEP.feedwide.pieces row `repeat:key` in _lab-ep.js = {n: 6, of: 80, word: \"rare\", elsewhere: gastify 0/49 · tier3 0/499}; `norm_doubts` holds only `data.GATE` at /home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:81-83",
   "fix": "Add to norm_doubts: \"security.EDGE\": \"It names the repeat key as an app-wide check. In this app's own tally a repeat key is rare, and neither other backend that was read has one.\" (no typed counts; the pick becomes reword through the existing mechanism). Suggested rewording for the note field, not for the lab: \"the app-wide checks — the rate limit, the origin rules, and a repeat key where the app reads one\".",
   "severity": "wrong",
   "lens": "fair",
   "index": 28,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "security.EDGE (_lab-ep-panels.js:1275) names 'the repeat key' as an app-wide check of the kind. LABEP.feedwide.pieces row repeat:key = 6 of 80 · 'rare', elsewhere gastify 0 of 49 · tier3 0 of 499. That is the same class of defect the page already doubts on data.GATE ('true of this app and not of the kind'), yet this line is pre-picked ok and norm_doubts holds only data.GATE."
  },
  {
   "id": "p11",
   "claim": "Section note: \"About four days of work, and its own evaluation ranks it lowest of the three parts. The only decision today is when.\"",
   "problem": "Contradicted by the page itself: the same section carries a second card, p11-name (\"The name inflight\"), which is not about when. A reader who believes the note stops at the first card. It also makes the section read as smaller than it is, on the most expensive item on the page.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:44 (ui.s4note) against the decision `p11-name` with \"step\": 4 at the same file :585-630",
   "fix": "Same final s4note as index 9.",
   "severity": "wrong",
   "lens": "fair",
   "index": 29,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "ui.s4note ('The only decision today is when') sits over two step-4 cards: p11 and p11-name (review.words.json decisions with step 4). The proposed replacement still keeps the unreferenced 'three parts' and types a count."
  },
  {
   "id": "step-2-options",
   "claim": "Section note: \"Each of these added something to a record or a card you had already ruled on. For each one: keep it, move it, or have me build it as a switchable option.\"",
   "problem": "The one option the operator most plausibly wants on a record he already ruled is missing: take it out. Four of the five cards (found-by, fn-rows, ending-sections, rules-on-hover) offer only keep · move · option — every branch keeps the addition somewhere. The section's own framing says these landed on records he had already settled, which is exactly the case where \"my record had four rows for a reason\" is the answer; only `open-call` has a near-equivalent (\"closed\").",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:32 (ui.s2note) and the options arrays at :328-336 (found-by), :350-364 (fn-rows), :405-419 (ending-sections), :432-446 (rules-on-hover)",
   "fix": "Add to those four cards: [\"off\",\"take it off the record for now — the fact stays in the feed\"]. s2note as in index 4.",
   "severity": "misleading",
   "lens": "fair",
   "index": 30,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "True that found-by, fn-rows, ending-sections and rules-on-hover offer only keep · move · option, so every branch keeps the addition on screen somewhere. But 'the record goes back as it was' must not mean dropping the FACT: D-016 ruled these pieces do, and the programme's definition of done says the facts land while 'how the card finally shows a fact is decided by the loop'. The option that fits both is taking the addition off the ruled record until the variants step."
  },
  {
   "id": "resolution-order",
   "claim": "Absent from the page: \"the order the helpers are resolved in\", said once on the deps header, with each helper carrying \"resolved 2nd of 5\".",
   "problem": "The page puts the sibling claim up for a ruling — `after-handler` exists precisely because \"after the answer is sent\" was a framework rule the map does not read — but this one, which is the same class of claim and is stated as fact on every endpoint, is not on the page. And it is not read from the framework either: the top order comes from a TEXT SEARCH of the guard signature string (`gsig.find(name + \")\")`), a guard whose name+\")\" is not found falls to the end of the list, sub-helpers are spliced in depth-first, and a helper asked for twice keeps its first place. On this endpoint that ordering is what makes `bearer_scheme` \"1st of 5\".",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_ep_joins.py:98 (the text-search sort key), :101-107 (depth-first splice, first-place-wins), :122 (the rule sentence); LABEP.security.resolution.rows[*].order in _lab-ep.js; drawn at _lab-ep-panels.js:1927 (\"resolved 1st of 5\")",
   "fix": "Add a step-1 card `dep-order`: what: \"Every helper says where it sits in the order they are resolved. That order is my reading, not the framework's: the handler's helpers as its signature names them, each one's own helpers first, a helper asked for twice keeping its first place. I find it by searching the signature's text, which the map clips when it is long; a helper whose name falls past the clip is placed last without saying so.\" facts: generated from L.security.resolution.rows (name · order · asked_by). where: \"Part SECURITY, the header This door's deps, then any helper.\" · \"In code: docs/design/workflow-panel/_ep_joins.py line 98.\" options: [\"keep\",\"say the order as it is\"] · [\"mark\",\"say the order, and mark a helper whose place the signature did not give\"]; mine mark.",
   "severity": "misleading",
   "lens": "fair",
   "index": 32,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "The mechanism is as described: _ep_joins.py:98 sorts the guards by gsig.find(name + \")\") with len(gsig) as the fallback, and gsig is the c4 graph's CLIPPED signature (LABEP.identity.gsig ends '…Depends(get_settings' cut to 'Depend…'), so a helper past the clip silently goes last; :101-107 splice sub-helpers first, first place wins; the order is drawn as fact ('resolved 1st of 5', _lab-ep-panels.js:516/:1927). It is the same class of claim as the after-handler card — a framework rule the map does not read. On this endpoint the result is right (get_settings is last anyway). The option 'do not say an order' is overkill."
  },
  {
   "id": "through",
   "claim": "\"One success route is named as the route that passes every check. I picked the success ending that passes the most checks.\" — options: the one passing the most checks · name no route.",
   "problem": "The rule has a second half neither the card nor the feed's rule sentence says: when two success routes pass the same number of checks, the winner is decided by the path's hash id (`sorted(..., key=(-passed, p[\"id\"]))`). That is arbitrary and it is not stable against anything a reader can see — and it is invisible on this endpoint, where the three success routes pass 10, 9 and 8. Since the card is ruling a rule that runs on all 80 endpoints, the tie is the part that needs his word, and there is no option for it.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/gen-endpoint-facts.py:216 (`_succ = sorted(..., key=lambda p: (-p[\"n\"][\"passed\"], p[\"id\"]))`) and :219 (the rule sentence, which stops at \"the most checks\"); LABEP.forms.through in _lab-ep.js (passed 10 · others 9, 8); claim at review.words.json:249",
   "fix": "what, add: \"When two success routes pass the same number of checks, today the generator picks by the route's internal id — a tie-break nobody can see. There is no tie on this endpoint.\" Add option [\"tie-none\",\"the one passing the most checks — and none named when two tie\"] and make it the dashed pick (name nothing rather than guess); extend the rule string at gen-endpoint-facts.py:219 to say which tie rule is in force.",
   "severity": "misleading",
   "lens": "fair",
   "index": 33,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "gen-endpoint-facts.py:216 sorts success routes by (-passed, p[\"id\"]) — a tie is broken by the route's hash id; the rule string at :219 stops at 'the most checks'. Invisible here (first run 10 · already done 9 · replay 8 in LABEP.forms.through) but the generator is per endpoint, so the rule being ruled has an unstated arbitrary half."
  },
  {
   "id": "p11-name",
   "claim": "Options: \"keep the name\" · \"rename, see my note\".",
   "problem": "The plan proposes a concrete alternative name and a deadline, and the card carries neither. Risk 7: the design names `alive` as the rename, and says a rename \"is cheap only BEFORE step 3\". Asking for a rename \"see my note\" costs the operator the work of inventing a word Claude already has, and hides that deciding later is not free.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/element-forms/plans/kinds-inflight.plan.json plan.risks[7] (\"A rename (`alive`) is cheap only BEFORE step 3\"); options at /home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:618-627",
   "fix": "options: [\"keep\",\"keep the name\"] · [\"alive\",\"rename it to alive\"] · [\"rename\",\"another name, see my note\"]. what, add: \"The design's own alternative is alive, and it says a rename is cheap only before its third landing step, when the registry is written.\"",
   "severity": "misleading",
   "lens": "fair",
   "index": 35,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "kinds-inflight.plan.json plan.risks[7]: 'A rename (`alive`) is cheap only BEFORE step 3.' The card offers keep · 'rename, see my note' — it withholds the word the design already has and the fact that deciding later costs more."
  },
  {
   "id": "p11-scope",
   "claim": "Options: \"after piece 10\" · \"now, before piece 10\" · \"drop it\". what: \"It reads in general what today is found by one name search: the values alive during a request, and the ones that live as long as the server.\"",
   "problem": "Two things missing. (1) A smaller build is an option the design itself carries and the card does not: dependency-value rows sit behind `OPTIONS.inflight_dep_values` and are named \"the first thing to cut if scope must shrink\", and landing step 14 already defers a 12b. So the real choice is four days · a smaller first pass · after 10 · drop, and the operator can only pick timing. (2) The cost that decides it is absent: this part lands in the FIRST kinds stage, and a raise there restores the whole stage — `middleware{}` and `dependencies{}` vanish and the failure cascades through paths, effects, contract and tests. That is a blast radius no other item on the page has, and the card says only \"about four days of work\".",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/element-forms/plans/kinds-inflight.plan.json plan.risks[5] (\"first thing to cut\"), plan.risks[0] (the isolation cascade), plan.landing_order[14] (12b deferred); options at /home/khujta/projects/gabe_lens/docs/design/design-context/review.words.json:592-606",
   "fix": "Add option [\"small\",\"a smaller first pass — without the dependency-value rows, the design's own first cut\"] and to what: \"The design names one thing to cut if the scope must shrink — the rows for values a helper hands to the handler — and does not say how much time that saves.\" Do NOT add the cascade sentence.",
   "severity": "misleading",
   "lens": "fair",
   "index": 36,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "Half holds. risks[5]: dependency-value rows are 'built behind OPTIONS.inflight_dep_values and the first thing to cut if scope must shrink' — a smaller build the card does not offer. The other half is a MISREAD: the design gives the part its OWN stage precisely so a raise costs this part only (kinds-inflight.plan.md:126-127 'Why an own stage…'; C39 at :154 proves middleware{}/dependencies{} stay present; risk 0 ends 'The own stage is load-bearing'). Adding the cascade sentence would put a false cost on the page. landing_order[14] ('12b … DECISION: defer') is later scope, not a smaller option."
  },
  {
   "id": "thresholds",
   "claim": "\"Every count of a piece carries a word. The word comes from two cut-offs that I chose.\"",
   "problem": "Three rules, not two. `word_for` tests `n <= 1` FIRST and returns \"only here\", overriding both cut-offs; only then does it compare the share. So a piece one endpoint carries never reads \"rare\" whatever the cut-offs are, and neither option on the card can change that. The facts table beside it prices only the two pairs, so the third rule is invisible in the evidence too.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_ep_pieces.py:65-72 (`if n <= 1: return \"only here\"` before the share test); the two-pair tally at /home/khujta/projects/gabe_lens/docs/design/design-context/gen-review.js:30-32; claim at review.words.json:195",
   "fix": "what as in index 7: \"… Two cut-offs I chose decide the norm and rare, and one rule sits above both: a piece only one endpoint carries always reads only here. …\" No new option.",
   "severity": "unclear",
   "lens": "fair",
   "index": 37,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "_ep_pieces.py:65-72: `if n <= 1: return \"only here\"` runs before the share test, so there are three rules; review-plan.md step 1 listed all three ('the norm ≥ 9 in 10 · rare ≤ 1 in 10 · only here = 1') and the page kept two. The facts table shows an 'only here' column but never says where it comes from. The proposed extra option pair is refuted: a card holds one verdict, and 'other, see my note' already covers it."
  },
  {
   "id": "p10",
   "claim": "\"The design lists 19 landing steps and 16 risks. About one to two days of building, then about 20 minutes of runs that must go one at a time.\"",
   "problem": "The \"about 20 minutes\" understates the plan's own stated costs by roughly half. This is the cost side of a land/hold decision, so an undercount here is the one thing that can flip the answer. The plan names two separate dry-run passes, not one.",
   "evidence": "docs/design/design-context/review.tpl.html:237 (the typed sentence). The plan it summarises: docs/design/element-forms/plans/slice-11e-does.plan.md:571 (OLD-code feeds, \"about 10–12 min total; run ALONE\"), :580 (NEW-code dry run, \"about 10–12 min\"), :584 (suite-doctor, \"≈5–6 min\"), :586 (twins, \"minutes each\"), :587 (lab carry ≈2 min plus a browser-gated probe, \"minutes, alone\"). Sum ≥ 35 min, realistically 45.",
   "fix": "review.tpl.html:237 → \"The design lists \" + v.steps + \" landing steps and \" + v.risks + \" risks. About one to two days of building, then the machine runs, one at a time: \" + v.runs + \".\" with v.runs built in gen-review.js from the COST: text of the landing steps that carry minutes (today: two dry runs of about 10–12 minutes each · the doctor at about 5–6 · then minutes each for the centre battery, the twins and the lab's browser check — most of an hour, not 20 minutes).",
   "severity": "wrong",
   "lens": "voice",
   "index": 41,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "review.tpl.html:237 TYPES 'about 20 minutes of runs'. The plan's own COST lines (slice-11e-does.plan.json landing_order): step 2 'about 10–12 min total; run ALONE', step 11 'about 10–12 min', step 15 '≈5–6 min', plus step 9 (tests/center, minutes, alone), step 17 (twins, minutes each) and step 18 (≈2 min feed + a browser-gated probe). The 20 came from the log's one-line summary, not from the plan; and a typed number in the template breaks the page's own law."
  },
  {
   "id": "s5note",
   "claim": "\"These rows were added after your last rating. Each one now shows a real value from this endpoint.\"",
   "problem": "The second sentence is false for at least one of the 20 rows and misleading for a second. The in-flight row cannot show a value because piece 11 is not built — which is the very thing step 4 asks him to schedule. The author's own plan document carries the exception; the page dropped it. Rating a row whose facts do not exist yet is the one thing this section must not ask for.",
   "evidence": "docs/design/design-context/review.words.json:50. Contradicted by docs/design/design-context/inventory-endpoint.md:73 (in-flight values — cardinality \"to measure when piece 11 lands\", \"feed gap: needs a new generation part (piece 11)\") and :117 (what the screen does on this ending — \"What the branch DOES … waits for the generation part\"). The author's own source says it: docs/design/design-context/review-plan.md:52-53, \"Every one now shows a real value from this endpoint, except in-flight values.\"",
   "fix": "Same final s5note as index 3.",
   "severity": "wrong",
   "lens": "voice",
   "index": 42,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 3/15; this finding names the right two rows (inventory-endpoint.md:73 in-flight values, :117 what the screen does) and cites the dropped caveat at review-plan.md:52-53."
  },
  {
   "id": "roles-raise",
   "claim": "The facts box lists three lines: \"any raise counts: 6 · …\", \"only a raise answered here: 5 · setup_complete, get_auth_context, complete_setup, claim, bearer_scheme\", \"a raise alone never counts: 5 · setup_complete, get_auth_context, complete_setup, claim, bearer_scheme\".",
   "problem": "Two of the three options show byte-identical evidence. The operator is asked to choose between \"only a raise this endpoint answers\" and \"a raise alone never counts\" while the page shows him the same list for both, so the card cannot be decided by seeing. The reason is invisible: the only function marked by a raise alone is `_load_setup_result`, and this endpoint does not answer its raise, so both options drop the same one function.",
   "evidence": "docs/design/design-context/gen-review.js:41 builds `ifAnswered` and `ifNever` from the same rows; the generated payload has them identical (`decisions[roles-raise].facts.v.ifAnswered` == `.ifNever` == [setup_complete, get_auth_context, complete_setup, claim, bearer_scheme]); `.onlyRaise` == [\"_load_setup_result\"]. Rendered at docs/design/design-context/review.tpl.html:219.",
   "fix": "Same generated line as index 11; keep all three options.",
   "severity": "misleading",
   "lens": "voice",
   "index": 43,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 11, verified from the payload inputs: ifAnswered and ifNever are the same five names; onlyRaise = [_load_setup_result]. Whether any endpoint in the app answers such a raise cannot be read from this lab's feed, so 'drop the middle option' is not supportable."
  },
  {
   "id": "case-roles",
   "claim": "Card title \"The four reasons a test came to this endpoint\"; option \"these four, in these words\"; facts heading \"The four reasons, in the lab's own words, with their counts here\".",
   "problem": "The lab's roster has FIVE role words, not four. The fifth, `named-only` (\"listed by the map, makes no call here\"), is never shown, so he is asked to approve a closed set while one member is hidden. \"Four\" is also a typed count in a heading fed by a list whose length is computed per endpoint — on an endpoint that has all five roles the heading will say four beside five rows. This is the page's own stated law broken (\"a sentence never carries a typed count\").",
   "evidence": "docs/design/workflow-panel/_lab-ep-panels.js ROLEWORD declares five keys: act · arranged · service-raises · helper-arranged · named-only. docs/design/design-context/gen-review.js:43 builds the list from `Object.keys(L.tests.roles)` (this endpoint has four). The word \"four\" is typed at docs/design/design-context/review.words.json:271 and :280, and at docs/design/design-context/review.tpl.html:223. The law is stated at docs/design/design-context/gen-review.js:59.",
   "fix": "Same final fix as index 1 (this finding's wording is the basis: count-free title and option, heading generated, all words listed with 0 where absent).",
   "severity": "misleading",
   "lens": "voice",
   "index": 44,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 1, plus the correct observation that 'four' is a typed count beside a computed list (review.words.json title, option, why; review.tpl.html:223), against gen-review.js:59."
  },
  {
   "id": "found-by",
   "claim": "where: \"Part DATA. / Click the table block locations in the GATE stage. / Read the portrait on the right.\" why: \"2 of this endpoint's 15 tables are known to a route's steps only, so the row changes whether you trust the list.\"",
   "problem": "The click path points at the one kind of table that does NOT demonstrate the claim. `locations` is found by BOTH, so its FOUND BY row reads \"both\" and shows him nothing the other four rows did not already say. The two tables the `why` is about are `users` and `ai_spend_log`, and `users` sits in the same GATE stage, one block away.",
   "evidence": "docs/design/design-context/review.words.json:318-322 (the click path) and :338 (the why). The data: `LABEP.data.tables` gives locations.found == \"both\", users.found == \"route effects\", ai_spend_log.found == \"route effects\"; `LABEP.forms.counts.tables_found` == {both: 13, \"route effects\": 2}. `users` is a GATE-stage table (a dependency step touches it, docs/design/workflow-panel/_lab-ep-panels.js:1012 stgOf).",
   "fix": "Same final fix as index 17.",
   "severity": "misleading",
   "lens": "voice",
   "index": 47,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 17, same evidence (locations.found = both; users and ai_spend_log = route effects; users sits under GATE)."
  },
  {
   "id": "thresholds",
   "claim": "where step 1: \"In the lab: pick any ending, open its record, read the row IN THIS APP.\"",
   "problem": "Two faults. First, this is the vaguest click path on the page — every other card gives named panel, named control, named block, while this compresses four clicks into six words; he will not find IN THIS APP without hunting. Second, IN THIS APP is one of the two sections the NEXT section asks him to approve or move, so step 1 sends him to read something whose existence step 2 puts in question.",
   "evidence": "docs/design/design-context/review.words.json:198 (the vague path) against the exact form used two cards later at :401-405 (\"COMMAND panel, PATHS, Walk a path, pick setup in progress. / In the portrait header, click the second icon, Exit. / Scroll the record down.\") — that same click path lands on the record that holds IN THIS APP. The section is the subject of the `ending-sections` card, review.words.json:399.",
   "fix": "Same where as index 7: \"COMMAND panel, PATHS, Walk a path, pick setup in progress.\" · \"In the portrait header, click the second icon, Exit.\" · \"Read the row IN THIS APP, just under the row AT — the word ends the line. A rate-limit ending shows its rate tier there instead, with no word.\" · \"In code: docs/design/workflow-panel/_ep_pieces.py line 65, the function word_for.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 52,
   "verdict": "PARTLY",
   "importance": 2,
   "why": "The click path is the vaguest on the page and the proven one two cards later lands on the same record — confirmed. But the finding's own fix repeats the page's error: IN THIS APP is a ROW under AT, not a section (_lab-ep-panels.js:3496-3498; ending-sections.png), so 'Scroll down to the section IN THIS APP' would send him wrong."
  },
  {
   "id": "data.GATE",
   "claim": "The copy text builds its norm section from two filters: `touched` (any line with a verdict OR a note) and `untouchedDoubt` (any line I doubt that has no verdict).",
   "problem": "A line I doubt, on which he types a note but presses no button, matches both filters and is printed twice, with identical text. data.GATE is exactly that line — the only one marked \"reword\" — and a note without a verdict is the most likely thing he does to it. Two identical lines in the paste-back read as two separate rulings on the same line.",
   "evidence": "docs/design/design-context/review.tpl.html:259 (`touched` = `s && (s.v || s.n)`) and :263 (`untouchedDoubt` = `n.mine !== \"ok\" && !(state.v[n.id] || {}).v`). With state.v[\"data.GATE\"] = {v: null, n: \"…\"} both predicates are true.",
   "fix": "review.tpl.html:263 → var untouchedDoubt = D.norms.filter(function (n) { return n.mine !== \"ok\" && touched.indexOf(n) < 0; }); plus the header wording of index 8.",
   "severity": "wrong",
   "lens": "voice",
   "index": 58,
   "verdict": "CONFIRMED",
   "importance": 2,
   "why": "Same defect as index 8: with state.v[\"data.GATE\"] = {v:null, n:\"…\"} both filters at review.tpl.html:259 and :263 are true, and data.GATE — the one doubted line, the likeliest to get a note without a button — prints twice."
  },
  {
   "id": "s2note",
   "claim": "For each one: keep it, move it, or have me build it as a switchable option.",
   "problem": "Two of the six step-2 cards do not offer those three. `open-call` offers option / keep / closed — no \"move it\". `place-card` offers everything / risk / both — no \"build it as an option\" at all, which is the one verb that card most needs, since its own `why` concedes it is a display choice. The note promises a uniform triple the cards do not deliver, so the operator reads each card expecting a verb that is not there.",
   "evidence": "review.words.json decisions[9].options (open-call) and decisions[12? → id place-card].options; review.words.json ui.s2note",
   "fix": "s2note: \"Each of these added something to a record or a card you had already ruled on. Most cards offer keep it · move it · take it off for now · build it as a switchable option; the open call and the place card carry their own choices.\" (drop 'take it off for now' if finding 30's option is not added)",
   "severity": "wrong",
   "lens": "truth",
   "index": 4,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "Of the six step-2 cards, open-call offers option · keep · closed (no 'move') and place-card offers everything · risk · both (no 'build it as an option'); the note promises one triple for all. Both cards' option sets are complete for their own question, so only the note needs to change."
  },
  {
   "id": "checked",
   "claim": "Three corrected facts were checked against the app's own source today.",
   "problem": "All three facts hold (api/setup.py is 254 lines with the route at :183; main.py adds Idempotency :124, RateLimit :127, CORS :132, so they run in reverse; RedirectIfSetupComplete.tsx exists and is a route guard). But \"today\" is a wallclock word baked into a page whose generator states \"No wallclock: same inputs, same bytes\" — the sentence is false on every later read, and this page is meant to be reopened while the operator works through five steps.",
   "evidence": "docs/design/design-context/gen-review.js:11; apps/gustify/apps/api/api/setup.py:183 (254 lines); apps/gustify/apps/api/main.py:124,:127,:132; apps/gustify/apps/web/src/routes/RedirectIfSetupComplete.tsx; review.words.json ui.checked",
   "fix": "ui.checked: \"Three facts I had corrected were checked against the app's own source on 2026-09-20.\"",
   "severity": "unclear",
   "lens": "truth",
   "index": 12,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "ui.checked says 'today' in a committed page whose generator states 'No wallclock: same inputs, same bytes' (gen-review.js:11); review-plan.md:58 carried the date 2026-09-20. The three facts themselves hold (re-checked: apps/api/api/setup.py is 254 lines, the route decorator is at :183; main.py adds Idempotency :124, RateLimit :127, CORS :132; RedirectIfSetupComplete.tsx exists)."
  },
  {
   "id": "p11-name",
   "claim": "Your prism is called In-flight state, and the part's files carry their own prefix.",
   "problem": "The name is not his. prisms.md's column header is \"prism (working name)\" — the working names were authored by the agents, and what the table records in his words is the question \"what did we save temporarily — cache, or something ephemeral…\". D-015 records him ruling \"keep\" on eleven with \"no note\", not naming them. This is the same class of claim already caught and fixed on this page (crediting him with an agent's work), and here it carries the whole recommendation. The second half is loose too: the plan's new modules are `_a3_forms_inflight.py` and `_a3_forms_carrier.py` — the prefix `_a3_forms_` is shared with every other forms module; only the suffix is the part's own.",
   "evidence": "docs/design/design-context/prisms.md:17 (header \"prism (working name)\") and :21; docs/design/design-context/decisions.md:95-97 (D-015, \"eleven keep, no note\"); docs/design/element-forms/plans/kinds-inflight.plan.json → plan.code_plan sections A and C; review.words.json decisions[id=p11-name].why",
   "fix": "why: \"The prism you kept in D-015 is called In-flight state, and the part's files sit under the forms prefix (_a3_forms_inflight.py), apart from the board's write-inflight.py.\"",
   "severity": "unclear",
   "lens": "truth",
   "index": 13,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "The sentence does not claim he coined the name: D-015 (decisions.md:95-97) lists 'In-flight state' among the eleven prisms he ruled keep, and prisms.md's table sits under 'The standpoints Gabe named'. So 'Your prism is called In-flight state' is true; only the looseness holds — the header reads 'prism (working name)', and the plan's containment (risk 7) is the shared `_a3_forms_` prefix that keeps the part's files apart from the board's write-inflight.py, not a prefix of the part's own."
  },
  {
   "id": "fn-rows",
   "claim": "The function record gained DOES, INSIDE and MAP FACTS. … read the row DOES in the portrait … the row ROLE … the row AFTER THE HANDLER … I added a fifth, FOUND BY",
   "problem": "Six of the nine row labels the page quotes in capitals are drawn lowercase in the lab, so the operator is scanning for a label that is not there. Only the path/exit record rows and section heads are uppercased by CSS: `.ptrow .k` and `.ptsec` carry text-transform:uppercase (_lab-ep.css:742, :744) — that is ROUTE, IN THIS APP, ON THE CLIENT. The table/function record rows use `.ptrec .rcrow .k`, which has no text-transform (:1125), so they read \"found by\", \"does\", \"inside\", \"map facts\"; hover-card keys use `#hover .cprow .cpk`, which also has none (:181, :500), so they read \"role\" and \"after the handler\".",
   "evidence": "docs/design/workflow-panel/_lab-ep.css:1125 and :181/:500 (no text-transform) vs :742 and :744 (uppercase); docs/design/design-context/review.words.json:316, :344, :225, :275, :297",
   "fix": "Write the four record-row labels as the lab draws them: found-by what → \"I added a fifth, found by, …\"; fn-rows what → \"The function record gained the rows does, inside and map facts.\"; roles-raise where → \"… click a function block, read the row does in the portrait.\" Leave ROLE, AFTER THE HANDLER, ROUTE, IN THIS APP and ON THE CLIENT in capitals.",
   "severity": "unclear",
   "lens": "where",
   "index": 19,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "Four labels, not six. The record rows use `.ptrec .rcrow .k` (_lab-ep.css:1125, no text-transform): found-by.png and fn-rows.png show 'found by', 'does', 'inside', 'map facts' in lowercase. But hover-card keys ARE uppercase: the station card CSS in _station.js sets `.cprow .cpk{ text-transform:uppercase }` and column-rule.png shows 'MAY BE EMPTY', 'DECLARED AT' — so ROLE and AFTER THE HANDLER are written as drawn. Changing the lab's CSS is display design and out of scope here."
  },
  {
   "id": "ui.s4note",
   "claim": "About four days of work, and its own evaluation ranks it lowest of the three parts.",
   "problem": "\"the three parts\" has no referent anywhere on the page — the page names piece 10 and piece 11 and nothing else, so the reader cannot tell what piece 11 is being ranked against or whether \"lowest\" is a small or a large gap. The claim is supportable: the evaluation's three bundles with effort \"arm\" are why-a-touch-why-it-survives (piece 9) · what-the-screen-does (piece 10) · alive-during-the-request (piece 11), ranked in that order, and piece 9 is already deferred by D-016.",
   "evidence": "docs/design/design-context/review.words.json:44; docs/design/design-context/gaps-endpoint.raw.json → judge.bundles (the three entries with effort \"arm\", alive-during-the-request last); docs/design/design-context/program-leftovers.md:9-21",
   "fix": "Same final s4note as index 9: \"About four days of work. Of the three pieces that need a new generator arm — piece 9 (ruled later), piece 10 and this one — the evaluation ranks this one last. You ruled it do in D-016, so the first card asks when, and dropping it would reopen that ruling; the second card asks whether it keeps its name.\"",
   "severity": "unclear",
   "lens": "where",
   "index": 20,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "'the three parts' has no referent on the page. The claim is supportable: gaps-endpoint.raw.json judge.bundles has three entries with effort 'arm' (why-a-touch-why-it-survives · what-the-screen-does · alive-during-the-request) and the last says 'Lowest priority of the three arms (importance 2 …)'; piece 9 is LATER in D-016."
  },
  {
   "id": "place-card",
   "claim": "In the header strip, hover the third small icon on the right, the globe.",
   "problem": "Ambiguous count. The right pile is [source, above] and renders four glyphs: the source chip (file icon plus the text apps/api/api/setup.py:183, because HEADCFG.mode is \"value\" and source's text is \"auto\") followed by the three icon-only up-chips cluster · entity · everything. The globe is the third BARE icon but the fourth glyph from the left; an operator counting icons stops on the entity chip.",
   "evidence": "docs/design/design-context/review.words.json:458; docs/design/workflow-panel/endpoint-lab.html:353 (right: [\"source\",\"above\"]), :451-458 (the source chip carries its value), :484-497 (three chips from I.above, globe last), :328-337 (chip() draws no text when mode !== \"label\" and value is null)",
   "fix": "where step 1: \"In the header strip, hover the last small icon on the right — the globe, after the file path.\"",
   "severity": "unclear",
   "lens": "where",
   "index": 21,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "endpoint-lab.html HEADCFG.right = [\"source\",\"above\"]: the source chip draws a file icon plus the path, then the three up-chips (layers · entity · globe). lab-map.png shows four glyphs on the right, so the globe is the last icon, the fourth glyph. The word 'globe' still lets him find it — polish only."
  },
  {
   "id": "after-handler",
   "claim": "The leftovers page said the database session closes after the answer is sent.",
   "problem": "\"The leftovers page\" is not a page the operator can open from this card — the page never names it and it is not in the where list. The claim itself is true: the evaluation's after-row reads \"get_session · runs before the handler · can refuse nothing · closes after the answer · 79 of 80 endpoints\", and the correction is logged in the programme. But as written it is an unverifiable reference on a card whose whole purpose is to let the operator check a wording change.",
   "evidence": "docs/design/design-context/review.words.json:294; docs/design/design-context/gaps-endpoint.effects.raw.json → verified.pieces[\"inside-the-calls\"].after_rows[5]; docs/design/design-context/program-leftovers.md:48 (\"WORDING CORRECTED against the evaluation\")",
   "fix": "what: \"The evaluation page, gaps-endpoint.html (piece 6, Inside the calls), said the database session closes after the answer is sent. The map only says its closing code runs after the handler. I changed the lab to the map's words.\" Use 'the evaluation page' on the vocab card too.",
   "severity": "unclear",
   "lens": "where",
   "index": 23,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "'The leftovers page' names nothing he can open and the where list has only a lab path. The claim itself is true: gaps-endpoint.effects.raw.json verified.pieces[inside-the-calls].after_rows[5] reads 'get_session · … closes after the answer · 79 of 80 endpoints', and program-leftovers.md logs the wording correction. decisions.md and program-leftovers.md call that page 'the evaluation page' (gaps-endpoint.html)."
  },
  {
   "id": "ui.checked",
   "claim": "Three corrected facts were checked against the app's own source today.",
   "problem": "\"today\" floats. gen-review.js is deliberately wallclock-free (\"No wallclock: same inputs, same bytes\"), so the page carries no date of its own; read next week the sentence claims a check that did not happen then. The source plan carried the date instead. I re-verified all three against gustify today and they hold: setup.py is 254 lines with the route decorator at :183 (def setup_complete at :184), main.py registers Idempotency :124 · RateLimit :127 · CORS :132, and RedirectIfSetupComplete.tsx returns Navigate/Outlet.",
   "evidence": "docs/design/design-context/review.words.json:65; docs/design/design-context/gen-review.js:11; docs/design/design-context/review-plan.md:57-59 (carries the date); /home/khujta/projects/apps/gustify/apps/api/api/setup.py:183-184, apps/api/main.py:124/127/132, apps/web/src/routes/RedirectIfSetupComplete.tsx:20-38",
   "fix": "ui.checked: \"Three facts I had corrected were checked against the app's own source on 2026-09-20.\"",
   "severity": "unclear",
   "lens": "where",
   "index": 24,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "Same defect as index 12: 'today' in a wallclock-free generated page (gen-review.js:11); review-plan.md:57-59 carries 2026-09-20. The three facts re-verify against gustify."
  },
  {
   "id": "slot-states",
   "claim": "Absent from the page: the four slot states and the rule that picks one — lit · hollow · hatched · blank, and the hover words \"the reading ran and found none — a measured zero\" / \"the … reading did not run on this feed — a zero here would be a guess\" / \"this kind of element has no such slot\".",
   "problem": "This is the whole output of piece 2 and it is authored judgement of exactly the kind step 1 exists for — four invented words plus a rule that decides, for twelve command decisions, whether a zero is shown as measured or as unread. It is on no card. Worse, it cannot be checked in the lab either: with every reading on, nothing on this endpoint is hatched, so the state the words exist to carry is invisible. Nobody but the operator can rule it and the page never asks.",
   "evidence": "/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_lab-ep-panels.js:2809-2816 (`slotState` / `slotWhy`), used at :2931-3315; the log's own note at /home/khujta/projects/gabe_lens/docs/design/design-context/program-leftovers.md:44 (\"With every reading on, nothing on this endpoint is hatched\"); zero occurrences of \"hollow\", \"hatched\" or \"slot\" in review.words.json",
   "fix": "Add a step-1 card `slot-words` with two options only: what: \"A command that counts nothing says why on its hover. I wrote the three sentences: \\\"the reading ran and found none — a measured zero\\\" · \\\"the … reading did not run on this feed — a zero here would be a guess\\\" · \\\"this kind of element has no such slot\\\". With every reading on, the second never shows on this endpoint.\" where: \"In code: docs/design/workflow-panel/_lab-ep-panels.js line 2813, the function slotWhy.\" options: [\"ok\",\"these sentences are right\"] · [\"reword\",\"reword, see my note\"]; mine ok. The generator should lift the sentences (move them into a literal first), never retype them.",
   "severity": "misleading",
   "lens": "fair",
   "index": 31,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "True: slotWhy's three hover sentences (_lab-ep-panels.js:2813-2815) are authored, appear on no card (0 hits for hollow/hatched/slot in review.words.json), and the hatched one cannot be seen on this endpoint (program-leftovers.md piece 2 note). But the four states themselves are not new judgement: the evaluation's 'empty-means-what' bundle named lit · hollow · hatched · blank and he ruled that piece do in D-016; the programme lists 'the four states' as existing components. The proposed option 'fold hollow and hatched into one' would break the honest-empty rule (a measured zero is not an unknown)."
  },
  {
   "id": "p11",
   "claim": "Facts heading \"What the card would say on this endpoint\", above a five-item list whose first item is \"Idempotency-Key · header · set at middleware/idempotency.py:25 · read at api/setup.py:191 · carried on the request\" and whose other four begin \"would say: …\".",
   "problem": "The heading says all five are what the card WOULD say, but the first is what the card says TODAY — the one row that already exists, found by the name search the card is meant to replace. The reader counts five new rows and overstates the gain. The \"would say:\" prefix on four of five makes the inconsistency look like a typo rather than a distinction.",
   "evidence": "docs/design/design-context/review.tpl.html:238 (the heading) over docs/design/design-context/gaps-endpoint.effects.raw.json, verified.pieces[key=\"alive-during-the-request\"].after_rows — item 0 has no \"would say:\" prefix, items 1–4 do. The source field for today's single row is named at the same entry: fields[1].source \"LABEP.forms.repeat.key — the one instance that exists today\".",
   "fix": "review.tpl.html p11 branch: split v.rows by the prefix — heading \"What the lab says today\" over the rows without 'would say: ', heading \"What it would say after\" over the rest with the prefix stripped.",
   "severity": "misleading",
   "lens": "voice",
   "index": 45,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "gaps-endpoint.effects.raw.json verified.pieces[alive-during-the-request].after_rows: item 0 ('Idempotency-Key · header · set at … · read at …') has no 'would say:' prefix and its field source says 'the one instance that exists today'; items 1–4 start 'would say:'. review.tpl.html:238 heads all five 'What the card would say on this endpoint'."
  },
  {
   "id": "topics",
   "claim": "The 36 lines are grouped under the headings \"tables\", \"shapes\", \"functions\", \"tests\", \"the client\", \"security\", while the click paths on the cards say \"Part DATA\", \"part FUNCTIONS\", \"part TESTS\", \"part SECURITY\".",
   "problem": "Three of the six headings are not the names of the things they stand for. The lab's parts are called Data, Schemas, Functions, Tests, Widening, Security. A reader who strikes a line under \"the client\" and then opens the lab to check it will find no part called \"the client\" — and no page tells him that \"the client\" means Widening, or \"shapes\" means Schemas. Two words for one thing, and the second word is the one he must type into the lab.",
   "evidence": "docs/design/design-context/review.words.json:74-79 (topics map: data→tables, schemas→shapes, widening→the client). The lab's own part names: docs/design/workflow-panel/_lab-ep-panels.js:3585 (data · word: \"Data\"), :3602 (schemas · word: \"Schemas\"), :3622 (widening · word: \"Widening\"). Rendered as the group heading at docs/design/design-context/review.tpl.html:197.",
   "fix": "Render the group heading as the lab's part name with the plain word beside it — \"Data — tables\", \"Schemas — shapes\", \"Widening — the client\" (Functions, Tests, Security unchanged) — by carrying partWord (the topic key as the part button reads) on each norm in gen-review.js, and use partWord in the paste-back label.",
   "severity": "misleading",
   "lens": "voice",
   "index": 46,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "review.words.json topics maps data→tables, schemas→shapes, widening→'the client', while the lab's part buttons read DATA · SCHEMAS · FUNCTIONS · TESTS · WIDENING · SECURITY (lab-map.png) and the cards themselves say 'Part DATA', 'part FUNCTIONS'. Two words for one thing, and the paste-back labels ('the client · GATE') use the second word. He can rule the lines without opening the lab, so polish."
  },
  {
   "id": "place-card",
   "claim": "\"The place reads 11th of 81 by functions behind. The work text asked for it on the risk card. An older ruling of yours, pinned in the probe, says the risk card carries no comparison with the rest of the app, so I put it on the header's card for the whole app.\"",
   "problem": "Three faults in three sentences. \"11th of 81 by functions behind\" uses an unexplained measure — nothing on the page says what \"functions behind\" counts or which end of the order is 1st, so the number is unreadable. \"The work text\" names a document the reader has never met. \"pinned in the probe\" uses \"probe\" with no gloss. And the third sentence runs 34 words, well over the length he reads in one pass.",
   "evidence": "docs/design/design-context/review.words.json:454. The measure: `LABEP.context.risk` = {rank: 11, of: 81, behind_max: 135, behind_median: 4} — rank is by count of functions reachable behind the endpoint, 1st = the most. The pin exists at docs/design/workflow-panel/probe-eplab.mjs:2800 (\"the risk card stays about this endpoint alone (the older ruling holds)\").",
   "fix": "After the place sentence of index 10: \"The evaluation's plan for piece 7 asked for it on the risk card. An older ruling of yours, pinned in the probe (probe-eplab.mjs line 2800), says the risk card carries no comparison with the rest of the app, so I put it on the header's card for the whole app.\"",
   "severity": "misleading",
   "lens": "voice",
   "index": 48,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "'The work text' is an unnamed document (it is the evaluation's work text for piece 7 — gaps-endpoint.raw.json judge.bundles[how-common].work: 'on the risk chip's card'). The rest is refuted: 'functions behind' is the lab's own drawn phrase (place-everything.png), 'probe' is the project's standing word on every log row he rules from, and sentence length is taste. The pin exists at probe-eplab.mjs:2800."
  },
  {
   "id": "rules-on-hover",
   "claim": "\"Every column now carries its rule: may it be empty, what fills it, what a delete elsewhere does, its line. I put the rule on the hover card, because the record's rows are ruled.\"",
   "problem": "\"rule\" and \"ruled\" carry two different meanings in one sentence — a constraint on a database column, and a decision you already made. The page uses a third sense in its section title, \"Sentences and rules I wrote\" (policies I chose), and the verb again in the lede (\"Rule what you disagree with\"). One word, three concepts, on a page whose own law is one term per concept.",
   "evidence": "docs/design/design-context/review.words.json:427 (both senses in one sentence); :23 (\"Sentences and rules I wrote\"); :11 (\"Rule what you disagree with\"); :195 (\"cut-offs that I chose\" — the same concept as \"rules I wrote\", said a fourth way).",
   "fix": "what, last sentence: \"I put it on the hover card, because you had already ruled on the record's rows.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 50,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "Only the clause 'because the record's rows are ruled' is genuinely ambiguous (ruled = lined? carrying rules? decided by you?). Renaming 'rule' to 'constraint' is refuted: 'its rule' is the lab's own drawn label (_lab-ep-panels.js:2846, :2856) and piece 8 is ruled under the name 'Field rules'; 'rule' for a stated policy is the feed's own field name (does.rule, through.rule)."
  },
  {
   "id": "vocab",
   "claim": "where step 2: \"In code: docs/design/workflow-panel/_lab-ep-panels.js line 1237, the block named STAGE_EXPECT.\"",
   "problem": "\"block\" here means a JavaScript object literal, but everywhere else on the page \"block\" means a clickable tile in the lab — \"Click the table block locations\", \"Click the block complete_setup\", \"Part DATA, click the table block locations\". A reader who has just been told to click blocks will look for a block in the lab named STAGE_EXPECT.",
   "evidence": "docs/design/design-context/review.words.json:176 (code sense) against :320, :348 and :429 (lab-tile sense). The declaration is confirmed at docs/design/workflow-panel/_lab-ep-panels.js:1237.",
   "fix": "where step 2: \"In code: docs/design/workflow-panel/_lab-ep-panels.js line 1237, the declaration STAGE_EXPECT.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 51,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "On this page 'block' is the lab's clickable tile ('Click the table block locations', 'Click the block complete_setup'); the vocab card uses it for a JavaScript declaration (_lab-ep-panels.js:1237 `var STAGE_EXPECT = …`). 'In code:' limits the confusion — polish."
  },
  {
   "id": "open-call",
   "claim": "\"Under the call complete_setup the route now lists the 8 things inside it.\"",
   "problem": "\"8 things\" names nothing. The reader cannot tell whether he is being shown 8 lines of code, 8 callees, or 8 facts, and so cannot judge whether always-open is too much. The 8 is a sum of five named kinds that the sentence throws away.",
   "evidence": "docs/design/design-context/review.words.json:372 ({insideRows}); the token is built at docs/design/design-context/gen-review.js:63 as `nIn.raises + nIn.refusals + nIn.commits + nIn.savepoints + nIn.swallows`, and on this call `LABEP.forms.inside.calls[\"apps/api/services/setup.py::complete_setup\"].n` == {raises: 4, refusals: 0, commits: 3, savepoints: 1, swallows: 0}.",
   "fix": "what: \"Under the call {callName} the route now lists the {insideRows} rows inside it — {insideKinds}.\" with {insideKinds} generated from the same counts in the lab's words, zero kinds skipped (today: \"4 failures, 3 saves and 1 savepoint\").",
   "severity": "unclear",
   "lens": "voice",
   "index": 53,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "TOK.insideRows (gen-review.js:63) is raises + refusals + commits + savepoints + swallows = 4 + 0 + 3 + 1 + 0 on complete_setup; the lab itself words them 'failures' and 'saves' (open-call.png: '4 failures · 3 sav…'). '8 things' throws the kinds away. Polish — the picture beside the card shows them."
  },
  {
   "id": "s4note",
   "claim": "\"About four days of work, and its own evaluation ranks it lowest of the three parts. The only decision today is when.\"",
   "problem": "\"the three parts\" names nothing the page ever lists — the reader has met piece 10 and piece 11, two parts, and \"its own evaluation\" names a document he has not seen. The phrase appears nowhere in the repo outside this sentence and the plan it was copied from, so there is no ranking he can go and check.",
   "evidence": "docs/design/design-context/review.words.json:44 and docs/design/design-context/review-plan.md:48 are the only two occurrences of \"three parts\" in the repo (grep across docs/**). The evaluation entry itself, docs/design/design-context/gaps-endpoint.effects.raw.json verified.pieces[key=\"alive-during-the-request\"], carries no rank, verdict or cost field.",
   "fix": "Same final s4note as index 9.",
   "severity": "unclear",
   "lens": "voice",
   "index": 54,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "The missing referent is real (same as index 20). But the ranking does exist where he can check it — gaps-endpoint.raw.json judge.bundles[alive-during-the-request]: 'Lowest priority of the three arms' — the finding looked only in the effects file. Its replacement ('the largest of the pieces still unbuilt, and the one that changes the least on screen') is a new claim nothing supports."
  },
  {
   "id": "roles-raise",
   "claim": "\"The raise is the doubtful one, because the answer it becomes is sometimes not read.\"",
   "problem": "The clause is opaque without the backend model behind it. A raise becomes an HTTP answer only when this endpoint translates it; otherwise it escapes as a 500 and the endpoint never declares it. \"the answer it becomes is sometimes not read\" gives him none of that, so he cannot judge the option he is being asked to pick.",
   "evidence": "docs/design/design-context/review.words.json:222. The distinction is the one the generator computes at docs/design/design-context/gen-review.js:36 (`answered` = the function has refusals, or a raise whose here_word is \"translated\").",
   "fix": "what, second sentence: \"The raise is the doubtful one: the map follows a raise one call up, and deeper than that it does not read which answer the raise becomes.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 55,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "The clause is terse ('not read' by whom?). But the proposed rewrite is FALSE: 'beyond one level' means the feed did not read which answer the raise becomes ('the feed follows a failure ONE call down … the answer it becomes is not read', _ep_joins.py reading string; program-leftovers.md piece 6: 'no ending is invented') — it does not mean the raise escapes as a 500."
  },
  {
   "id": "p11",
   "claim": "\"It reads in general what today is found by one name search: the values alive during a request, and the ones that live as long as the server.\"",
   "problem": "\"one name search\" is the whole point of the card and it is left unexplained. Today the lab has exactly one such row because the generator greps for a single word; the reader cannot see that today's one row is an accident of naming rather than a measurement.",
   "evidence": "docs/design/design-context/review.words.json:588. The source says it plainly: docs/design/design-context/gaps-endpoint.effects.raw.json, verified.pieces[key=\"alive-during-the-request\"].fields[0].source — \"no feed carries it (ContextVar, lru_cache, BackgroundTasks, 'inflight' all grep to 0 …); nearest is forms.json endpoints[...].repeat.key, found by a name filter for 'idempot'\".",
   "fix": "what: \"Today the lab shows one such value, and only because its name contains the word the filter searches for. This part would read them in general: every value alive only while a request runs, and every one that stays in the server between requests.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 56,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "The card's point is left implicit. The source says it outright: gaps-endpoint.effects.raw.json alive-during-the-request.abstraction ('in place of the single key that happened to carry the right word in its name') and fields[0].source ('found by a name filter for 'idempot''). The proposed sentence is faithful to that."
  },
  {
   "id": "checked",
   "claim": "\"Three corrected facts were checked against the app's own source today.\"",
   "problem": "\"today\" is a wallclock claim written into a page whose generator advertises that it carries no wallclock — the file is committed and read later, and the word silently becomes false. It is also the only date-bearing sentence on the page, so nothing else corrects it.",
   "evidence": "docs/design/design-context/review.words.json:65. The contract is stated at docs/design/design-context/gen-review.js:11 (\"No wallclock: same inputs, same bytes.\"). The real date is recorded at docs/design/design-context/review-plan.md:58 (\"checked against the app's source on 2026-09-20\").",
   "fix": "ui.checked: \"Three facts I had corrected were checked against the app's own source on 2026-09-20.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 60,
   "verdict": "CONFIRMED",
   "importance": 1,
   "why": "Same defect as index 12; review-plan.md:58 has the date. An authored date in the sentence keeps the build byte-identical (ui texts are not passed through fill(), so a token would need generator work for no gain)."
  },
  {
   "id": "p10-toast",
   "claim": "\"Three popular libraries share the name toast, but none of the four apps installs one. One app has its own toast with the same shape.\"",
   "problem": "\"the four apps\" appears here for the first and only time on the page, unnamed, and \"One app\" is never named either. He cannot tell whether the four are his four study apps or four of something else, and cannot check the claim. \"library idiom\" in the title is a second unearned term — the page never says a roster is a short list of library names the reading recognises.",
   "evidence": "docs/design/design-context/review.words.json:546 (the sentence) and :545 (the title \"toast as a library idiom\"). The four are named in docs/design/workflow-panel/pieces-digest.json, apps: gastify · gustify · keypro · tier3; the packages are cited at docs/design/design-context/program-leftovers.md:52 (sonner · react-hot-toast · react-toastify).",
   "fix": "what: \"Three popular libraries — sonner, react-hot-toast and react-toastify — share the name toast, but none of the four study apps installs one. One app, tier3, has its own toast with the same shape.\"",
   "severity": "unclear",
   "lens": "voice",
   "index": 61,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "Naming the libraries and the apps helps, but the finding's fix names the WRONG app: the project toast is tier3's ('tier3's 135-file `toast` is its own design-system store, imported from `@opal/layouts`', slice-11e-does.plan.md:238), not gustify's (gustify's pushToast is deliberately left out of the rosters, :245). 'roster' and 'idiom' are the suite's standing words (CLAUDE.md 'idiom roster'), so the title stays."
  },
  {
   "id": "after-handler",
   "claim": "\"The leftovers page said the database session closes after the answer is sent.\"",
   "problem": "\"The leftovers page\" names no page the reader can open, and he is currently reading a page whose eyebrow says \"Review · the leftovers program\" — so the most natural reading is that this page said it, which would be self-contradictory.",
   "evidence": "docs/design/design-context/review.words.json:294; the eyebrow is :5 (\"Review · the leftovers program\"). The page meant is docs/design/design-context/gaps-endpoint.html.",
   "fix": "Same final sentence as index 23 (names gaps-endpoint.html as the evaluation page, keeps 'the map').",
   "severity": "unclear",
   "lens": "voice",
   "index": 63,
   "verdict": "PARTLY",
   "importance": 1,
   "why": "Same defect as index 23 — and the point that the eyebrow 'Review · the leftovers program' makes 'the leftovers page' read as THIS page is right. But the proposed rewrite swaps 'the map' for 'the committed data', a second word for the suite's core term."
  }
 ],
 "refuted": [
  {
   "id": "app-names",
   "claim": "Absent from the page: every piece row on this endpoint's cards names the other study proje",
   "why": "Taste. The other apps are the operator's own study apps, their names are in the committed pieces-digest.json, and the row is already in front of him on the ending-sections card (ending-sections.png shows 'IN THE OTHER APPS gastify 16 of 49 · tier3 4 of 499 · keypro not measured'). The log's 'in NEITHER other backend' is prose summary, not a competing display choice. The only real gap — the card text never mentions that second row — is closed by the fix at index 5."
  },
  {
   "id": "norms.tests",
   "claim": "The six `tests` norm lines — \"a test for each app-wide refusal\", \"a test for each way the ",
   "why": "Opinion, not evidence. STAGE_EXPECT is by design 'the EXPECTATION for this topic … it never grades what it found' (_lab-ep-panels.js:1236 comment), and for the tests topic what a stage holds IS tests, so the norm is 'a test for each…'. D-012 and the element-forms idea make an empty expected slot a finding on purpose; a norm is not a score. The page already asks him to strike any line that grades; pre-flagging six lines on a reviewer's reading would itself steer him."
  },
  {
   "id": "s1note",
   "claim": "\"The lab reads its facts out of the map, and a probe recounts them. These are different: I",
   "why": "Taste. 'the map' and 'a probe' are the suite's and the programme's standing terms (every log row in program-leftovers.md uses both), and replacing them with 'the committed codebase data' / 'a script' would break one term per concept. 'These' reads against the section title directly above it, 'Sentences and rules I wrote'."
  },
  {
   "id": "copy",
   "claim": "The pasted text lists each decision by its raw id — e.g. \"through: most-checks (my pick, n",
   "why": "Taste. The paste-back is read by Claude against review.words.json, the ids are unique keys, and the header carries the review hash and head that pin the version (review.tpl.html:260), so no id can be acted on wrongly. The step headings already group them, and titles of up to ten words per line would make the text he pastes harder to scan, not easier."
  },
  {
   "id": "rules-on-hover",
   "claim": "Step 2 runs found-by → fn-rows → open-call → ending-sections → rules-on-hover → place-card",
   "why": "Taste, and moot after the fixes: step 2 follows the order of the pieces (4 · 6 · 6 · 7/10 · 8 · 7), and once found-by points at users (index 17) and rules-on-hover at locations → household_id (index 6) the two cards no longer click the same block, so 'Same block as the card above' would be false."
  },
  {
   "id": "p10-nav",
   "claim": "\"To class a call as a navigation, the reading must know the hook useNavigate. You struck t",
   "why": "Taste. 'roster', 'hook' and 'guard' are the operator's working vocabulary: he ruled on hook roles (D2) and struck 'the nav-hook guard' himself (amendment-1.md:1149, §A4 V40a — the card's where line cites it correctly). Replacing them with paraphrases ('the short list the reading recognises') breaks one term per concept."
  },
  {
   "id": "skip",
   "claim": "\"You can skip the probe counts, the mutant runs, the commit messages and the generator cod",
   "why": "Taste. 'probe' and 'mutant' are the programme's own words on every log row he rules from (program-leftovers.md: 'Probe 1013 → 1025, a mutant fails 3 by name'); the paraphrases ('the runs that prove a check can still fail') are longer and introduce second names for settled terms."
  }
 ]
}
