{
 "title": "Slice 11e — what a client branch does (leftovers piece 10, the generation part)",
 "status": "DRAFT — not landed; waits for the operator's ruling and an explicit 'land it'",
 "plan": {
  "schema": "The new keys sit inside the existing `reason` part. There is no new part, no registry change, no finding and no id letter.\n\n**Where it is written**\n- forms.json → `frontend.reasons.sites[]`, on every site: status, detail and code alike.\n- Assembled in `/home/khujta/projects/gabe_lens/templates/center/generators/_a3_fe_reason.py:254-262`.\n- One stats key: `arms.frontend.stats.reason.does`.\n- Nothing else is touched: `readers{}`, `origins`, `endpoints`, `branch`, the site `id` (hashed at :254 from name · recv · member · op · value · n) and the three findings.\n- forms.json is written with `sort_keys=True`, so key order is irrelevant.\n\n**Site-level additions (three keys)**\n- `does`: always a list, in extractor walk order, capped at `FF.DOES_CAP` (24). Over the cap the site also carries `does_more: <int dropped>`.\n- `does_state`: always present, one of `read · mixed · empty · returned · no-rows · unread`.\n  - This keeps the estate's rule of a word beside an empty list (the `_o()` precedent at :108).\n- `ctx`: the comparison row's own extractor ctx, verbatim, written only when non-empty.\n  - Example: `[\"callback:useEffect\"]`. It gives the row-level `in` something to be relative to.\n\n**Row schema (every does row)**\n- Required:\n  - `class`: one of `FF.DOES_CLASSES`.\n  - `k`: the extractor row kind actually read, one of `call|new|ret|throw|jsx`.\n  - `at`: `\"<file>:<line>\"`.\n- Rows derived from a call or new:\n  - `callee`: verbatim, extractor-clipped at 80.\n  - `args`: the extractor's first-3 literal-resolved list, verbatim, nulls kept, only when the row has it.\n  - `props`: the literal props of the first object argument, verbatim, only when present.\n  - `from`, one of:\n    - `bind:<leaf of the call that binds the callee's base in this body>`, for example `bind:useState`, `bind:useAuth`.\n    - `param`: the base is a parameter of the body.\n    - `fe:<file>#<name>` or `ext:<name>`: what `_a3_fe_forms._resolve` returns.\n    - absent: a global such as `console`, `fetch`, `window`, or unresolved.\n- Per class:\n  - navigate: `to` (str or null) plus `tag` or `callee`.\n  - throw: `callee`, `value` (text, clipped 80), and `args`/`props` absorbed from the same-line `new`/`call` sibling.\n  - render: `tag`, plus `tags` when read from a ret row.\n  - return: `value` (raw text or null) plus exactly one of:\n    - `bare:true` for `return;`\n    - `null:true`\n    - `literal:\"<unquoted text>\"`, only when the whole value is one quoted literal with no `${}` and is not clipped\n    - `obj:{…}`, the extractor's returned-object map.\n  - message: `key` (args[0] when a str, else null).\n  - surface: `level` (the leaf when in the roster, else null).\n  - state: `how` = `setter|store|storage`.\n  - refresh: `how` = `invalidate|write|refetch`, `key` = raw `opts.queryKey` / args[0].\n  - retry: `how` = `mutate|mutateAsync|refetch|fetch`.\n  - request: `method`, `path`, and `endpoint` only when the forms hold it.\n  - log: `level`.\n  - other: `callee` always.\n- Optional on any row, written only when true or non-empty:\n  - `returned:true`: the row is a call whose value the branch returns. The ret row was merged into it.\n  - `in:[ctx…]`: callbacks entered inside the branch, which is the row ctx beyond the site's ctx.\n  - `when:[{pred,neg?}…]`: conditions inside the branch that also gate this row. It is `guards[n+1:]` plus `after[len(site.after):]`.\n  - `by:\"name\"`: the row's guard matched only through the named-constant alternative of the regex, not the literal.\n  - `reads:[\"detail\"|\"code\"…]`: which refusal members the row's texts name. This is the evidence behind the word `reads`.\n  - `passes:true`: the receiver's base is among the call's refs. This is the evidence behind the word `passed`.\n  - `clipped:true`: `value` or `callee` sits at the 80-character cap.\n- No ids. A row is addressed by `at` plus its position in `does[]`. This follows the §A4 V33 precedent at amendment-1.md:115, where `b s a q m t` were struck. Two rows can share an `at`, for example `toast.error(t(\"k\"))`.\n\n**Stats**\n- `stats.reason.does` = `{\"rows\":int, \"narrowed\":int, \"sites\":{all six states:int}, \"classes\":{all twelve classes:int}}`.\n- `narrowed` counts sites where the structural selection kept fewer rows than `_branch`'s textual `inside`.\n- Every class and every state is listed, so a zero is a measured zero.\n\n**Worked example 1: gustify, RequireSetup**\n- Source, verified by reading: `/home/khujta/projects/apps/gustify/apps/web/src/routes/RequireSetup.tsx`\n  - :31 `const { signOut } = useAuth();`\n  - :37 `useEffect(() => {`\n  - :38 `if (isError && isApiError(error) && error.status === 401) {`\n  - :39 `void signOut();`\n- The site after the change. Pre-existing keys are unchanged. This is predicted from source, not yet produced by a run:\n```json\n{\"id\":\"r-<unchanged>\",\"piece\":\"fe:apps/web/src/routes/RequireSetup.tsx#RequireSetup\",\"at\":\"apps/web/src/routes/RequireSetup.tsx:38\",\"receiver\":\"error\",\"reads\":\"status\",\"op\":\"===\",\"value\":401,\"branch\":\"none\",\"origins\":[\"…unchanged…\"],\"endpoints\":[\"endpoint:GET /me\"],\n \"ctx\":[\"callback:useEffect\"],\"does_state\":\"read\",\n \"does\":[{\"class\":\"other\",\"k\":\"call\",\"at\":\"apps/web/src/routes/RequireSetup.tsx:39\",\"callee\":\"signOut\",\"from\":\"bind:useAuth\"}]}\n```\n- The else arm at :41 (`pushToast({tone:\"info\",message:t(…)})`) carries the guard with `neg:true`, so it is not in does[]. It belongs to the exits the site does not cover.\n\n**Worked example 2: gustify, SetupScreen.tsx:43**\n- This is the 409 behind both `reason-collapsed` goldens.\n- Source: `function setupErrorMessage(error: unknown, t: TFunction)` … `if (error.status === 409) return t(\"auth.setup.error.in_progress\");`\n- Result: `\"does_state\":\"read\",\"does\":[{\"class\":\"other\",\"k\":\"call\",\"at\":\"apps/web/src/features/auth/SetupScreen.tsx:43\",\"callee\":\"t\",\"args\":[\"auth.setup.error.in_progress\"],\"from\":\"param\",\"returned\":true}]`.\n- Its class is `other`, not `message`. `t` is a parameter typed by gustify's own `useT`, not a library binder. The key is still readable as args[0].",
  "classification": "**Which rows are selected**\n- Two selections run over one regex and must not be confused.\n- Textual selection, for the word. `_inside(rows, site)` is today's :76-78, verbatim for status sites. It returns `(pat, later, inside)`.\n  - `_branch(later, inside, recv)` is today's :79-86, byte for byte.\n  - The five words keep their meanings and the three findings cannot move.\n  - `_pat(site)` adds a named group `(?P<lit>…)` around the literal alternative. For an int this is the same language as today.\n  - For a string `right` the literal alternative is quoted: `[\"'`]<escaped>[\"'`]`. Today's unquoted pattern can never match `error.code === \"setup_locked\"`. That is harmless today only because `_branch` runs for status sites alone.\n- Structural selection, for `does[]`. A row of `inside` is the site's own when all of the following hold. Here `n = len(site.guards)`, `sg`/`sa`/`sc` are the comparison row's own guards, after and ctx, and `g`, `after`, `ctx` are the row's.\n  1. `len(g) > n and g[:n] == sg and not g[n].get(\"neg\") and pat.search(g[n][\"pred\"])`.\n     - The branch guard sits exactly one deeper than the comparison's own guards.\n     - This holds by construction in the extractor. `_a3_fe_extract.mjs` :417-421 walks an `if` condition with `(guards, passed, ctx)` and its then-arm with `[...guards,{pred}]`. :366-375 does the same for a ternary and for `cond && <jsx/>`.\n  2. ctx never diverges: `all(a == b for a, b in zip(sc, ctx))`.\n     - A textually identical `if` in a sibling callback is excluded.\n     - A comparison inside a callback within the condition still owns the statement-level rows.\n  3. `after[:len(sa)] == sa`, and no pred in `after[len(sa):]` matches `pat`.\n     - A row that runs only after this comparison was ruled out cannot be in its branch.\n  4. If the match came through the name alternative (`m.group(\"lit\") is None`), the row belongs only to the nearest preceding comparison.\n     - That is the greatest `line <=` the row's line among cmp rows with the same `left`·`op` and equal guards.\n     - The row is kept only when that comparison is the site, and it carries `by:\"name\"`.\n- The invariant `does ⊆ inside` holds, so `branch in (value, unread)` implies `does == []`.\n- The word is never recomputed from the narrowed rows. Doing so would move `reason-collapsed`. It is deferred to its own ruling.\n\n**Rows skipped and merged (one pass, grouped by line)**\n- A `cmp` row is skipped. It is a site of its own.\n- A `let` row is skipped. It is an alias, not an action.\n- A `throw` absorbs the same-line `new`/`call` row whose callee equals `throw.callee`. It takes that row's `args`/`props` and the sibling is dropped.\n- A `ret` whose value, after stripping a leading `await ` or `void `, starts with a same-line call's `callee + \"(\"`:\n  - the call row is kept with `returned:true`\n  - the ret row is dropped.\n- A `ret` with `jsx` is the single render or navigate row.\n  - Every `jsx` row at a line >= that ret's line in the same ctx is dropped, because a returned tree is one row.\n  - A `jsx` row under the guard with no such ret is kept. This is the conditional render case.\n- A same-line `new URL(<str>)` row feeds `to` for a `NAV_STATIC` call and is dropped.\n\n**Class ladder (first match wins; order = `FF.DOES_CLASSES`)**\n- ret and throw rows:\n  1. navigate: the throw callee's leaf is in `NAV_THROWS`, or `ret.jsx` is in `NAV_ELEMENTS`.\n     - `to` comes from the same-line jsx `props.to`, or from the call's `props.to` / args[0].\n     - The lookup is this function's own same-line lookup. `_a3_fe_forms._nav` refuses ctx-ed siblings.\n  2. throw.\n  3. render: `ret.jsx` is set.\n  4. merged into its call (see above).\n  5. return.\n- jsx rows: navigate when the tag is in `NAV_ELEMENTS`, else render.\n- call and new rows:\n  1. navigate, any of:\n     - the callee equals a name bound by a `NAV_BINDERS` call\n     - the base is bound by a `NAV_ROUTER_BINDERS` call and the leaf is in `NAV_ROUTER_METHODS`\n     - the callee is in `NAV_STATIC`\n     - an undotted callee is in `NAV_CALLS`\n     - `NAV_PLATFORM` fullmatches.\n  2. retry: the leaf is in `MUTATE_CALLS|REFETCH_MEMBERS` and it is the same request as the receiver.\n     - \"Same request\" means the callee base equals the receiver base, or both names were bound by the same call row.\n     - Example: `const {error, refetch} = useMe()`.\n     - Also: `_fetch_call(r)` resolves through `reach.endpoint` to a key in the site's own `endpoints`.\n  3. refresh:\n     - leaf in `INVALIDATE_CALLS` gives `how: invalidate`\n     - leaf in `CACHE_WRITE_CALLS|SEED_CALLS` gives `how: write`\n     - leaf in `REFETCH_MEMBERS` on another base gives `how: refetch`.\n  4. request: `_fetch_call(r)` is not None, or a dotted `MUTATE_CALLS` leaf on another base.\n  5. message: an undotted callee bound by a `MESSAGE_BINDERS` call.\n     - When the roster names a member, the bound member must match.\n  6. surface: the base is in `SURFACE_CALLS`, and the callee equals the base or the leaf is in `SURFACE_LEVELS`.\n  7. state, any of:\n     - the callee is index 1 of a `STATE_BINDERS` bind list\n     - its binder `_resolve`s to an id in `store_ids`, the fe structure arm's proven `kind:\"store\"` pieces, passed in\n     - `STORAGE_WRITES` fullmatches.\n  8. log: `LOG_CALLS` fullmatches.\n  9. other: always emitted with `callee` and `args`.\n- The guiding rules:\n  - `retry` is claimed only when the same request is provably re-sent. Everything else is `request` or `refresh`.\n  - A class comes from a library binder or a library callee shape, never from the local name. `navigate`, `t`, `refetch` and `setX` are just locals.\n  - The row always shows `callee` and `from`, so the reader sees both the idiom and its provenance.\n  - Matching is by leaf or shape with no binding disproof. This is consistent with every existing roster in `_a3_forms_fe.py`. `NAV_THROWS` and `INVALIDATE_CALLS` are leaf-matched today.\n\n**State words (`does_state`), evaluated in this order**\n- `read`: at least one row was kept.\n- `mixed`: rows were kept, but a later comparison with the same left · op · value, equal guards, equal after and non-diverging ctx exists in the body.\n  - The walker matches a branch by its condition's text and cannot split two such branches.\n  - The name `shared` is avoided on purpose. The lab already uses `shared` for endings that share one site.\n- `empty`: rows were structurally inside but every one was a cmp or let.\n- `returned`: no own row, and a `ret` row with equal guards and ctx, at a line <= the site's, whose `value` text contains `site.left` exists.\n  - The comparison is the function's return value and its callers decide what happens. This is not followed.\n  - It is this part's spelling of `beyond one level`. The operator may prefer that phrase.\n- `unread`: no own row, and a guard at or below the site is >= 120 characters (the extractor's silent clip) or the body is `truncated`.\n- `no-rows`: no own row otherwise.\n  - Either the comparison feeds a value (a const, a ternary of literals), or its branch holds only statements the walker emits no row for: an assignment, a `yield`, a `continue`.\n  - The walker cannot tell these apart without an extractor change.\n  - This replaces the word `value`'s over-claim for does. `branch:\"value\"` itself is untouched.\n\n**What is honestly not read, and said so**\n- The else arm, the fall-through of a `!==` site, and exits routed to `rest` get no does. There is no site to hang them on.\n- An early-exit inversion (`if (s !== 409) return; …`) is not followed.\n- `switch (err.status)` emits no cmp row, so there is no site. This is a pre-existing blind spot.\n- A textually negated pred such as `!(e.status === 409)` still matches. This is residual: the word has the same flaw. One optional line checks that `pred[:m.start()]` does not end in `!(`.",
  "rosters": "Add one block to `/home/khujta/projects/gabe_lens/templates/center/generators/_a3_forms_fe.py` after `REASON_MEMBERS` (:39). It is data only, about 16 lines, taking the file from 56 to about 72.\n\n```python\n# ── what a reason branch does (Slice 11e) ───────────────────────────────────────\nDOES_CLASSES = (\"navigate\", \"throw\", \"render\", \"return\", \"retry\", \"refresh\", \"request\", \"message\", \"surface\", \"state\", \"log\", \"other\")   # ladder order\nDOES_STATES = (\"read\", \"mixed\", \"empty\", \"returned\", \"no-rows\", \"unread\")\nDOES_CAP = 24\nNAV_BINDERS = frozenset({\"useNavigate\"})                   # react-router v6/v7 · @tanstack/react-router: const navigate = useNavigate(); navigate(\"/x\") / navigate({ to })\nNAV_ROUTER_BINDERS = frozenset({\"useRouter\"})              # next/navigation · next/router · @tanstack/react-router\nNAV_ROUTER_METHODS = frozenset({\"push\", \"replace\", \"navigate\"})   # next: router.push/replace · TanStack: router.navigate({ to })\nNAV_CALLS = frozenset({\"redirect\", \"permanentRedirect\"})   # next/navigation: CALLED, not thrown; the thrown TanStack redirect stays NAV_THROWS\nNAV_STATIC = frozenset({\"NextResponse.redirect\", \"Response.redirect\"})   # next/server route handlers · the Fetch API static\nNAV_PLATFORM = r\"(?:window\\.)?location\\.(?:assign|replace|reload)\"          # DOM Location (location.href = … is an assignment: no flow row)\nREFETCH_MEMBERS = frozenset({\"refetch\"})                   # @tanstack/query-core QueryObserverResult.refetch\nMESSAGE_BINDERS = {\"useTranslations\": None, \"useTranslation\": \"t\"}       # next-intl: the bound value IS t · react-i18next: the destructured member t\nSURFACE_CALLS = frozenset({\"toast\"})                        # the export sonner · react-hot-toast · react-toastify share: toast(msg) and toast.<level>(msg)\nSURFACE_LEVELS = frozenset({\"error\", \"success\", \"warning\", \"warn\", \"info\", \"message\", \"loading\"})   # sonner levels + react-toastify's warn\nSTATE_BINDERS = frozenset({\"useState\", \"useReducer\"})       # react: index 1 of the bound pair is the setter / dispatch\nLOG_CALLS = r\"console\\.(?:error|warn|info|log|debug)\"      # the Console API\n```\n\n**Existing rosters reused, matched by leaf exactly as `_a3_fe_forms`/`_a3_fe_controls` already do**\n- `NAV_ELEMENTS`, `NAV_THROWS` (:8-9)\n- `INVALIDATE_CALLS` (:26)\n- `SEED_CALLS` (:27)\n- `CACHE_WRITE_CALLS` (:35)\n- `MUTATE_CALLS` (:38)\n- `STORAGE_WRITES` (:34)\n- `REASON_MEMBERS` (:39)\n\n**Evidence in the four study apps**\n- Read this session: the four `package.json` files.\n- From the readers: call sites.\n- Present and used in a reason branch or its body:\n  - react-router-dom ^7.18.1 (gustify)\n  - @tanstack/react-router ^1.169.2 (gastify)\n  - next 16.3.3 (tier3) and next 16.2.10 (keypro):\n    - `NextResponse.redirect` at tier3 `auth/oauth/callback/route.ts:37`\n    - `router.push` at tier3 connector `page.tsx:170`\n  - next-intl ^4.13.7 (tier3):\n    - `const t = useTranslations(\"auth\")`, `lib/auth/components.tsx:47`\n  - @tanstack/react-query (gustify ^5.62, gastify ^5.100.10)\n  - react `useState`\n  - `console.*`\n  - `window.location.reload()` (gustify `SettingsContainer`).\n- Cited to a public package API only. No study app depends on the package, so these are unverified locally:\n  - sonner, react-hot-toast, react-toastify (`SURFACE_*`)\n  - react-i18next (`useTranslation`).\n- tier3's 135-file `toast` is its own design-system store, imported from `@opal/layouts`.\n  - tsconfig paths `@opal/*` make it in-repo.\n  - It matches the shape, and its row will say `from: fe:…#toast`.\n  - That is the honest reading: a project module with the library's shape.\n\n**Deliberately left out**\n- Any project-local name. These are never rostered:\n  - signOut, pushToast, writeModesCache, setRollbackNotice, isApiError, useT (gustify)\n  - translate, uploadFailed (gastify)\n  - logout, readError, errorDetail, authErrorRedirect, finishConnectorDeletion, writeExportIdToUrl, setSessionFetchError (tier3)\n  - isUniqueViolation (keypro).\n- `t`, `translate`, `i18n.t`, `formatMessage` by name.\n  - A one-letter local means three different things across the apps.\n  - react-intl's `useIntl().formatMessage` is in no study app. Add it when a target uses it.\n- Error class names such as RedirectError and RateLimitedError. They are data on a throw row (`callee`), never a roster.\n- SWR's bare `mutate(key)`.\n  - It collides with the `MUTATE_CALLS` leaf.\n  - Refresh inside a reason branch measured zero across all four apps.\n- react-router v5 `useHistory`. It is in no app.\n- `alert`/`confirm`. Not found.\n- Store action names.\n  - `state` comes from `STATE_BINDERS` or the fe arm's proven `kind:\"store\"` pieces.\n  - It never comes from a `set*` name pattern.\n- The `setTimeout + continue` retry shape, `location.href =` and `yield`. The extractor emits no row for `continue`, assignment or `yield`.\n\n**V40a**\n- V40a (amendment-1.md:1149) stays struck. `NAV_BINDERS` classifies a does row. It does not build the nav-hook guard. D29 must say so.",
  "code_plan": "Smallest change first. All paths are under `/home/khujta/projects/gabe_lens/templates/center/generators/`. No new module, so `propagate.sh` takes its update-only path.\n\n**1. `_a3_forms_fe.py`**\n- After :39, add the roster block. About 16 lines.\n\n**2. `_a3_forms_build.py:394`**\n- One edit: pass a sixth argument to `FER.reason_part(...)`:\n  - `{p[\"id\"] for p in (fe or {}).get(\"pieces\") or [] if p.get(\"kind\") == \"store\"}`\n- `:408 built` and `_a3_forms.py:312` are not touched. `does` is not a registered part.\n\n**3. `_a3_fe_reason.py`**\n- 291 lines today, about 375 after, against a budget of 800.\n\n**3a. Constants, after :45**\n- `READ_M_RX = re.compile(r\"\\.(\" + \"|\".join(FF.REASON_MEMBERS) + r\")\\b\")`. `READ_RX` is left untouched.\n- `STORAGE_RX`, `NAV_PLATFORM_RX`, `LOG_RX`, compiled from the rosters.\n- `LIT_RX`: a whole quoted literal, in `\"`, `'` or a backtick, with no `${`.\n\n**3b. :75-78 becomes two functions**\n- `_pat(site: dict) -> re.Pattern`:\n  - Today's expression with `(?P<lit>…)` around the literal alternative.\n  - For an int `right` the literal alternative is `re.escape(str(v)) + r\"\\b\"`, the same language as :76.\n  - For a str `right` it is the quoted form.\n- `_inside(rows, site) -> tuple[re.Pattern, list, list]` returns `(pat, later, inside)`, with `later`/`inside` exactly as :77-78.\n\n**3c. `_branch(later: list, inside: list, recv: str) -> str`**\n- The body is old :79-86, verbatim.\n- This is the safety property. Every word-dependent assert passes unchanged: F13 :271/:274/:279/:281, F14 :301/:308, and the finding at :278.\n\n**3d. New helpers**\n- `_binders(rows) -> dict`:\n  - Built from every `call` row with `binds`.\n  - Maps the local name to `(binder callee, member, index, binder row line)`.\n  - Rename form: `member:alias` gives local = alias.\n  - A name bound by two different callees is dropped as ambiguous.\n- `_params(body) -> set`:\n  - String params plus the locals of destructured lists.\n- `_from(base, binders, params, bindings, file, local) -> str|None`, with precedence:\n  1. bind\n  2. param\n  3. `_resolve`'s `fe:`/`ext:`\n  4. None.\n- `_does_row(r, sibs, recv, eps, reach, binders, params, bindings, file, local, stores) -> dict`:\n  - The class ladder plus the per-class keys and the optional decorations.\n- `_does(rows, body, site, pat, inside, recv, eps, reach, file, stores) -> tuple[list, str, bool, int]`:\n  - Runs the structural `own()` test and the skip/merge pass, in row order.\n  - Applies the cap, then computes the state.\n  - Returns `(does, state, narrowed, more)`.\n  - `_fetch_call` (:118) and `reach.endpoint` (:160) are reused as they are.\n\n**3e. `reason_part` signature, :237**\n- Add `store_ids: set | None = None`.\n- The battery's four-argument `reason()` helper keeps working. With None, a store-bound call reads as `other`.\n\n**3f. The site loop, :257-262**\n- The order of edits matters. `does` needs `s[\"endpoints\"]` for the retry-by-endpoint rule.\n```python\npat, later, inside = _inside(rows, r)            # lifted ABOVE the member test: detail/code sites get does too\nif member == \"status\":\n    s[\"branch\"] = _branch(later, inside, recv)   # unchanged word\n…origins / endpoints exactly as today…\nif r.get(\"ctx\"): s[\"ctx\"] = list(r[\"ctx\"])\ns[\"does\"], s[\"does_state\"], nar, more = _does(rows, body, r, pat, inside, recv, s[\"endpoints\"], reach, file, store_ids or set())\nif more: s[\"does_more\"] = more\n```\n\n**3g. Stats, :287-289**\n- Add `\"does\": {\"rows\", \"narrowed\", \"sites\": {state: n for FF.DOES_STATES}, \"classes\": {c: n for FF.DOES_CLASSES}}`.\n\n**3h. Docstring, :20-22**\n- Extend it with the does contract:\n  - the selection\n  - the state words\n  - no ids\n  - the word is never recomputed from does.\n\n**Not changed**\n- `_a3_fe_extract.mjs`. There is no re-freeze for extractor reasons, and `tests/frontend` is untouched.\n- `_a3_forms.py`: no FINDINGS change and no ARMS change.\n- `form_drift.py`, `mapquery.py` and the example `forms.json` (`regen-example.sh:38` pins arms none).\n- The CLAUDE.md capability table. There is no skill version bump.\n\n**Docs, in the same commit**\n- `/home/khujta/projects/gabe_lens/templates/center/generators/README.md:60`: extend the `_a3_fe_reason` clause with:\n  - \"and what each branch does: the call with its literal arguments, the value returned, the element rendered, a navigation, a retry, a cache refresh — each classed against library idioms, `other` when none matches\".\n- `/home/khujta/projects/gabe_lens/CLAUDE.md:66`: one \"Slice 11e\" sentence after the Slice 11d sentence.\n- `/home/khujta/projects/gabe_lens/docs/design/element-forms/amendment-1.md`:\n  - an \"as built, Slice 11e\" block under :1214 in the 11c shape: Capture · Classification · Not built · Goldens on gustify\n  - D29 after :1429\n  - an added \"Not built\" line at :1212.\n- `/home/khujta/projects/gabe_lens/docs/design/element-forms/goldens/render-acceptance.py:8-10`: add `\"11e\": \"frontend · reason does\"` to `ARM`.\n\n**D29, in the house form**\n- CHOSE:\n  - keys inside the reason part\n  - rows addressed by `at` and order (V33)\n  - no finding\n  - `branch` and every prior key untouched.\n- ASSUMED:\n  - a then-arm row carries the comparison's own guards plus the condition (`_a3_fe_extract.mjs:366-375`, `:417-421`)\n  - no target needs react-intl, SWR revalidation or an assignment row to be read usefully.\n- BREAKS IF:\n  - `narrowed` or `no-rows` dominate a dry run. Then the extractor needs a branch ordinal and assign/yield/continue rows, which forces a re-freeze under F16.\n  - Or the operator wants `branch` recomputed from the narrowed rows. That moves `reason-collapsed` counts and gets its own commit.",
  "battery": "All of this lives in `/home/khujta/projects/gabe_lens/tests/forms-frontend/run.sh`, in the house style: `py \"<Fn> · FIRE…; SILENT on…\"`, `at(rel, text)` for lines, in-memory mutants on a deep copy that must flip the verdict. The highest case id today is F20.\n\n**Fixture**\n- Add one file, `tests/forms-frontend/fixture/src/routes/SetupFailure.tsx`. It is mounted nowhere, has no button or anchor and no `<Navigate>`, so the guards, controls and router counts stay put.\n- A component `SetupFailure` binds:\n  - `useCompleteSetup`, `useMe`, `useNavigate`, `useQueryClient`\n  - `useTranslations(\"setup\")`\n  - `useState`\n  - an import of `toast` from \"sonner\"\n  - `const LOCKED = 423`.\n- Inside `const onFail = () => {…}` it holds three branches on `complete.error?.status`:\n  - 401: `navigate(\"/login\"); return;`\n  - LOCKED: `complete.mutate({});`\n  - 500, in this order:\n    - `toast.error(t(\"failed\"));`\n    - `queryClient.invalidateQueries({queryKey:[\"me\"]});`\n    - `me.refetch();`\n    - `setNote(\"failed\");`\n    - `console.warn(\"setup failed\");`\n    - `void fetch(\"/api/v1/me\");`\n    - `setTimeout(() => setNote(null), 10);`\n    - `throw new Error(\"failed\");`\n- Statuses 401, 423 and 500 are all produced by `FORMS_R`'s POST /setup/complete, and none is the shared 409. So no new finding fires and F14 stays as it is.\n- Re-freeze `flow.frozen.json` with F16's own command (run.sh:378).\n\n**Existing asserts that move; reconcile them on the OLD generator first**\n- F8 :152: `len(sites) == 14` becomes 17.\n- F13 :291: `rs` sites, reads_status, joined, routed and rest.\n  - Predicted: 17 · 14 · 14 · 12 · 13.\n  - Confirm by the run.\n- F13 :286: `sorted(rd)` gains `(\"SetupFailure\", \"complete.error\")`.\n- F9: add `json.dumps(reason(), sort_keys=True) == json.dumps(reason(), sort_keys=True)`.\n\n**F21 · what each branch does, from the REAL capture**\n- FIRE:\n  - (a) SetupForm 409 at `error.status === 409) return`: does == one `return` row with `literal \"setup in progress\"`.\n  - (b) conflictMessage 409: a `return` row with `reads == [\"detail\"]`. `branch` is still `reads`.\n  - (c) ErrorBody `error.code === \"setup_locked\"`: exactly one `render` row, `tag \"p\"`, `k \"ret\"`. The same-line jsx row is merged.\n  - (d) The detail site `\"consent required\"`: `literal \"consent\"`. This proves the quoted pattern, so a detail or code site gets rows.\n  - (e) RequireSetup:\n    - `[other · callee signOut · from \"bind:useAuth\"]`\n    - site `ctx == [\"callback:useEffect\"]`\n    - `pushToast` is absent, because its guard is negated.\n  - (f) SetupFailure:\n    - 401: `[navigate to \"/login\" from \"bind:useNavigate\", return bare:true]`.\n    - LOCKED: `[retry how \"mutate\" by \"name\"]`.\n    - 500: classes in order `[surface, message, refresh, refresh, state, log, request, other, state, throw]`, with:\n      - message `key \"failed\"`, `from \"bind:useTranslations\"`\n      - refresh[0] `how invalidate`, `key [\"me\"]`\n      - refresh[1] `how refetch`\n      - request `GET /api/v1/me`, `endpoint \"endpoint:GET /me\"`\n      - the second state row has `in == [\"callback:setTimeout\"]`\n      - throw has `callee \"Error\"`, `args [\"failed\"]`, and no separate `new` row.\n- SILENT:\n  - `me.error.status === 401` (a const): `[]` plus `no-rows`.\n  - Both `errors.ts` sites: `returned`.\n  - The session store: `no-rows`.\n  - No site in `client.ts`.\n  - Every row class is in `FF.DOES_CLASSES`.\n  - Every site has a `does_state` in `FF.DOES_STATES`.\n  - `does_state in (read, mixed)` exactly when `len(does) > 0`.\n- Mutants, by JSON round-trip on the SetupFailure body:\n  - `\"useNavigate\"`→`\"useNav\"`: navigate flips to other. The class comes from the binder, never from the name `navigate`.\n  - fetch path → `/api/v1/setup/complete` plus `props.method POST`: request flips to retry with `how fetch`.\n  - `complete.mutate`→`save.mutate`: retry flips to request.\n  - `toast.error`→`notify.error`: surface flips to other.\n  - `useTranslations`→`useT`: message flips to other with `args` kept. This is the gustify shape.\n\n**F22 · selection and state words, FIRE+SILENT, all transplants of REAL rows**\n- (a) Copy the 401 branch's rows into a sibling `callback:onRetry` with a different action.\n  - The first site's does excludes it.\n  - `FER._inside` still holds it, and `narrowed == 1`.\n- (b) A later identical `if` whose rows carry the first's pred negated in `after` is excluded.\n- (c) A later non-exiting twin with equal guards and after: the earlier site reads `mixed`, the later reads `read`.\n- (d) By name:\n  - the LOCKED row is absent from the 401 site and present on the LOCKED site with `by \"name\"`\n  - rewriting the guard text to `=== 423` makes `by` disappear.\n- (e) Cut the 401 guard pred to 120 characters with the comparison lost: `[]` plus `unread`.\n- (f) A branch holding only a nested cmp: `empty`.\n- (g) `truncated:true` with no rows: `unread`.\n- (h) A `neg:true` guard never enters does.\n- (i) 30 transplanted rows: `len(does) == 24` and `does_more == 6`.\n\n**F23 · the word is untouched**\n- `{at: branch}` of the 14 original sites equals the words F13 and F14 already assert.\n- Under F22(a), with a planted twin row reading `.detail`:\n  - the first site's `branch` flips to `reads`, which is today's behaviour, kept\n  - no row of its does carries `reads`\n  - so does narrowing never leaks into the word.\n- The site key set minus `{does, does_state, does_more, ctx}` is a subset of the 11 prior keys.\n- The stats keys are the nine plus `does`.\n\n**Source mutants (hand-applied, each must turn the named case red, then reverted; record in the commit)**\n- S1: `own()` returns True. F22 a, b, d go red.\n- S2: drop the nearest-preceding clause. F22 d goes red.\n- S3: `_pat` unquoted for str. F21 d goes red.\n- S4: the ladder always returns other. F21 goes red.\n- S5: the word computed from the narrowed rows. F23 goes red.\n- S6: skip the merge pass. F21 c and f go red.\n\n**Goldens**\n- File: `/home/khujta/projects/gabe_lens/docs/design/element-forms/goldens/expected.json`, slice `\"11e\"`.\n- Every source below was read this session.\n- `does-setupscreen-409` (V):\n  - get: `frontend.reasons.sites` where `at` = `apps/web/src/features/auth/SetupScreen.tsx:43` → first → does → first → args\n  - eq `[\"auth.setup.error.in_progress\"]`.\n- `does-requiresetup-signout` (V):\n  - get: the site at `apps/web/src/routes/RequireSetup.tsx:38` → does → pluck callee\n  - has `signOut`\n  - and `from` eq `bind:useAuth`.\n- `does-cooking-403-literal` (V):\n  - get: the site at `apps/web/src/features/cooking/cookingSessionModel.ts:381` → does → first → literal\n  - eq `cooking.error.start_allergen`.\n- `does-isclienterror-returned` (P):\n  - get: the site at `apps/web/src/lib/api/errors.ts:73` → `does_state`\n  - eq `returned`.\n- `does-stats` (U):\n  - `arms.frontend.stats.reason.does.rows` exists.\n\n**Landing-time law proof (not a permanent case)**\n- A committed \"before\" file would become tautological after its first re-bless, so this is done once at landing.\n- On the re-frozen flow, dump `reason()` with the OLD generator.\n- After the edit, strip `does`, `does_state`, `does_more` and `ctx` from the sites and `does` from the stats.\n- The `json.dumps(sort_keys=True)` outputs must be equal.\n- Record \"N sites · 0 prior keys differ\" in the commit message.",
  "lab_carry": "This is a separate commit that closes leftovers piece 10. Its definition of done: facts generated, never typed; corrections made with existing components only; the probe can fail. How the card finally shows a fact is decided by seeing, not here.\n\n**1. `/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_ep_joins.py:278`**\n- `does` already passes through via `st.get(\"does\")`.\n- Add:\n  - `\"does_state\": st.get(\"does_state\")`\n  - `\"does_more\": st.get(\"does_more\")`\n  - `\"branch\": st.get(\"branch\")`\n  - `\"site_ctx\": st.get(\"ctx\")`.\n- A `rest` route has no site, so all of them stay None.\n- What the general case does is not read by this part. The lab should say so, not leave a blank.\n- Add `n.does_rows` per reader. Its smallest · middle · largest over the 80 endpoints is the inventory row this piece owes.\n\n**2. `/home/khujta/projects/gabe_lens/docs/design/workflow-panel/_lab-ep-panels.js:2835`**\n- This line must change in the same commit that regenerates the facts.\n  - `String(rt.does)` on a list of dicts prints `[object Object]`.\n  - `[]` is truthy in JS, so it prints an empty row.\n- Replace it with a `doesWords(rt)` row, gated on `rt.own_branch`.\n- When `does_state` is undefined, the feed predates the reading. Say \"not read — this feed predates the reading\" (not_emitted).\n- For `read`/`mixed`, join one phrase per row with \" · \". One neutral phrase per class:\n  - navigate: goes to “<to>”; with no target: \"leaves the screen, target not read\"\n  - throw: throws <callee>\n  - render: shows <tag>\n  - return: gives back “<literal>” · null · nothing (bare) · <value>\n  - message: message “<key>”\n  - surface: shows a notice (<callee>)\n  - state: sets state (<callee>)\n  - refresh: refreshes <key>\n  - retry: sends the request again\n  - request: calls <method> <path>\n  - log: writes to the console\n  - other: calls <callee>(“<args[0]>”).\n- Append to the phrase:\n  - \"and returns it\" on `returned`\n  - \"later, inside <in>\" on `in`\n  - \"only when <when>\" on `when`\n  - \"(matched by a named constant)\" on `by`.\n- For `mixed`, append \"two checks with the same text share these lines\".\n- For the no-row states:\n  - `returned`: the check is handed back to its caller. What happens next is decided there and is not followed.\n  - `no-rows`: nothing the reading can see happens under this check. It feeds a value, or the branch only assigns.\n  - `unread`: a condition here was too long to read.\n  - `empty`: the branch only compares again.\n- For `does_more`, show \"+N more\".\n\n**3. The sentence at :2831**\n- It hard-codes \"it reads the <reads> only\".\n- With the feed it can be measured: any does row with `reads` means \"its branch reads the detail\". This is the same evidence as `branch:\"reads\"`.\n\n**4. `/home/khujta/projects/gabe_lens/docs/design/workflow-panel/probe-eplab.mjs`**\n- Add one assert near :2695: on POST /setup/complete's 409 ending, the drawn \"what it does\" text contains `auth.setup.error.in_progress`.\n- Add one mutant: blank `does` in the facts, and the assert flips.\n\n**5. Feed**\n- The lab needs an all-arms-on gustify build.\n  - Command: `scripts/forms-dryrun.sh --arms all gustify`. Run it alone.\n  - Then copy `~/.cache/gabe-map-baselines/.check/gustify/forms.json`.\n- Then run `python3 docs/design/workflow-panel/gen-endpoint-facts.py --forms <that file>`.\n- Expected on the lab's endpoint:\n  - Both 409 endings share the one site at SetupScreen.tsx:43.\n  - Its does is `other · t(\"auth.setup.error.in_progress\") · returned`.\n  - So the record reads \"calls t(“auth.setup.error.in_progress”) and returns it\" for both.\n  - This is the fact behind `reason-collapsed`, now visible as what the client does.",
  "landing_order": [
   "0 · Pre-flight: `git -C /home/khujta/projects/gabe_lens log -1`; `df -h /mnt/c` (alarm under 40 GB free); `du -sh /var/log` (alarm over 2 GB); `ps -eo stat,comm | awk '$1 ~ /^D/'` and confirm no other heavy job is running. COST: seconds. PASS: clean tree on graft-adoption, headroom OK, nothing else building.",
   "1 · Spec first, because suite changes get a draft and an explicit 'land it'. In /home/khujta/projects/gabe_lens/docs/design/element-forms/amendment-1.md: a draft Slice 11e block under :1214; DECISION D29 after :1429; a note at :1149 that V40a stays struck; no change to the §A1 arms row (:76) or the id row (:115), since `does` is not a part and mints no letter. COST: writing only. PASS: the operator says go on D29 — inside the reason part, no ids, no finding, `branch` untouched, the class list, the state words.",
   "2 · OLD-code feeds for the 'existing keys keep their values' proof: `bash scripts/forms-dryrun.sh --arms frontend gustify`, then `cp ~/.cache/gabe-map-baselines/.check/gustify/forms.json <scratch>/old.gustify.json`; repeat serially for gastify, tier3, keypro. COST: two builds per target (gustify ≈1 min a build, tier3 ≈2); about 10–12 min total; run ALONE. PASS: each prints 'on vs off: forms.json moved — OK'. Cheaper variant: gustify and tier3 only (12 and 45 of the 61 sites).",
   "3 · Fixture: write tests/forms-frontend/fixture/src/routes/SetupFailure.tsx. Re-freeze with F16's own command (run.sh:378): `GABE_TS_DIR=<gustify apps/web or gastify web> GABE_FE_FLOW=1 node templates/center/generators/_a3_fe_extract.mjs tests/forms-frontend/fixture tests/forms-frontend/flow.frozen.json tests/forms-frontend/fixture`. COST: one node run over ~35 files, seconds, run alone. PASS: `git diff --stat` shows only the new .tsx and flow.frozen.json.",
   "4 · Reconcile fixture churn on the OLD generator: `bash tests/forms-frontend/run.sh`. Expected reds are only F8 :152 (14→17 sites) and F13 :286/:291 (the new reader tuple and the moved counts). Update those asserts and re-run to green. Then dump `reason()` on the new frozen flow to `<scratch>/reasons.old.json` with a 5-line python using the battery prelude. COST: not stated anywhere in the repo; about 20 python subprocesses plus 2 node runs, estimated under a minute (unverified). PASS: all passed, 0 failed, F16 not skipped. If any OTHER case goes red, the fixture file disturbed a part it should not have; simplify the file before going on.",
   "5 · Rosters: templates/center/generators/_a3_forms_fe.py, add the Slice 11e block after :39 (56 → about 72 lines). COST: none. PASS: data only, flat, no import.",
   "6 · Generator: templates/center/generators/_a3_fe_reason.py — `_pat`, `_inside`, `_branch` body verbatim, `_binders`, `_params`, `_from`, `_does_row`, `_does`, the site-loop edit at :257-262, stats at :287, the docstring; 291 → about 375 lines. Plus the one-line sixth argument at _a3_forms_build.py:394. COST: none. PASS: `python3 -c 'import _a3_fe_reason'` with PYTHONPATH set to the generators directory.",
   "7 · Unit law proof: dump NEW `reason()` on the same flow; strip `does`, `does_state`, `does_more` and `ctx` from the sites and `does` from the stats; compare `json.dumps(sort_keys=True)` with reasons.old.json. COST: seconds. PASS: byte-equal; record 'N sites · 0 prior keys differ'.",
   "8 · Battery: add F21, F22 and F23, extend F9 and F13 in tests/forms-frontend/run.sh (395 → about 460 lines); `bash tests/forms-frontend/run.sh`. Then hand-apply source mutants S1–S6 one at a time; each must turn its named case red; revert each. COST: about a minute per run (estimate). PASS: green with F16 LIVE not skipped; six mutants killed and listed for the commit message.",
   "9 · Neighbour batteries, serially: `bash tests/forms-core/run.sh` (seconds), then `bash tests/center/run.sh`. COST: tests/center runs several full centre builds, so minutes, and it must run ALONE. PASS: both green. tests/center :445-455 proves arms only add on endpoints, and :411 proves a raising extend_frontend writes no frontend key.",
   "10 · `./install.sh`. COST: ≈5 s. PASS: exit 0. Without it suite-doctor reports template DRIFT (suite-doctor.sh:76-85).",
   "11 · NEW-code dry run on all four targets, serially and alone: `bash scripts/forms-dryrun.sh --arms frontend <name>`, copying each `.check/<name>/forms.json` to `<scratch>/new.<name>.json`. COST: about 10–12 min. PASS, part one: arms none stays BYTE-IDENTICAL against the blessed baseline, and with arms on only forms.json moves. PASS, part two, a seconds-long strip-and-diff per target: `frontend.reasons` with the does keys stripped equals OLD, and everything else equals OLD except `arms.frontend.stats.reason.does` and `arms.frontend.bytes`. `bytes` is a size measure and the one prior key that legitimately moves. Record per target: sites · rows · the six states · the twelve classes · narrowed.",
   "12 · Read the numbers before trusting them. Open about 10 tier3 sites and check that rows match source. If `narrowed` is more than a handful, or `no-rows` is most of the former `value` sites, that is the BREAKS-IF in D29: say so in the commit and do not tune rosters to the targets.",
   "13 · Goldens: add the five `11e` goldens to docs/design/element-forms/goldens/expected.json; add `\"11e\"` to the ARM dict at render-acceptance.py:8-10; `python3 docs/design/element-forms/goldens/check-goldens.py <scratch>/new.gustify.json --slice 11e`; then re-render acceptance.html with render-acceptance.py. COST: seconds. PASS: every 11e golden MATCH, none MISMATCH or MISSING; the two 11c goldens still MATCH.",
   "14 · Docs: extend the `_a3_fe_reason` clause at templates/center/generators/README.md:60; add the Slice 11e sentence at CLAUDE.md:66; finalise the amendment's as-built block with the measured numbers. COST: none. PASS: no capability-table or version change, so doctor parity is unaffected.",
   "15 · `bash scripts/suite-doctor.sh`. COST: ≈5–6 min (46 batteries, eight of them launch a browser), ALONE. PASS: CLEAN, with no 'SKIPPED coverage' INFO for forms-frontend.",
   "16 · Commit by explicit path, `git log -1` before and after (a concurrent `git add -A` has swallowed a commit on this branch before). The message carries the unit proof, the per-target numbers, the six mutants and the line counts (_a3_fe_reason.py ≈375, run.sh ≈460, all under 800). Trailer: Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>.",
   "17 · Twins: `bash templates/center/generators/propagate.sh <twin-root> --check` (seconds, writes nothing), then the real run per twin. COST: minutes each, because it runs `<twin>/scripts/refresh_center.sh regen`. Both changed generators already exist on the twins, so this is the update-only path. Push and PR are the operator's.",
   "18 · Lab carry, in its own commit closing leftovers piece 10: an all-arms gustify feed (`forms-dryrun.sh --arms all gustify`, ≈2 min, alone) → gen-endpoint-facts.py --forms → _ep_joins.py:278 + _lab-ep-panels.js:2835 + the probe assert and mutant in probe-eplab.mjs. COST: the probe is browser-gated, minutes, alone. PASS: the 409 ending's record shows the call and its key, read off the feed; the probe fails on the blanked-does mutant; the inventory row carries smallest · middle · largest does rows over the 80 endpoints."
  ],
  "risks": [
   "`branch` and `does` can disagree by design. `does` is the structurally narrowed subset of the word's textual `inside`, so a site can read `branch: reads` while no does row carries `reads`. This exposes a pre-existing over-capture in the word: a body-wide text match, no ctx filter, and a named-constant alternation that lets `x.status === TEAPOT` match every site on that left. Recomputing the word from the narrowed rows is more correct, but it can add or remove `reason-collapsed` findings and the two 11c goldens. It needs its own ruling and commit, never this one. The `narrowed` stat is the evidence for that ruling.",
   "The structural test (`own()`) rests on the extractor threading guards, after and ctx as read at _a3_fe_extract.mjs:366-375 and :417-421 for if, ternary and &&-JSX. It was read, not run. Switch cases synthesise guards and emit no cmp row, so they never reach it. A construct that pushes rows with a different guard depth would silently drop real rows into `no-rows`. F22 and the per-target `narrowed` count are the tripwires.",
   "Known mis-attributions that remain, each flagged on the row or site. Non-exiting sequential twins read `mixed`. A by-name row goes to the nearest preceding comparison, which is wrong when a value-only comparison of the same left·op sits between a named `if` and its rows. A comparison inside a callback in the condition loses branch rows that sit in another callback, because ctx diverges. A textually negated pred such as `!(e.status === 409)` still matches.",
   "The extractor emits no row for an assignment, a `yield`, or `continue`/`break`. tier3's `errorMsg = t(…)` followed by a later `toast.error(errorMsg)` reads as a message row only, because the toast sits outside the branch. svcSS.ts:68's default-object substitution, services/lib.tsx's yielded packet and apiServices.ts:272's retry loop read `no-rows` or a lone `other: setTimeout`. `no-rows` says this out loud, but the true doing is invisible until the extractor grows those kinds. That would mean re-freezing flow.frozen.json and re-running tests/frontend.",
   "The 15 `value` sites stay one hop short. A predicate function reads `returned`; a const predicate reads `no-rows`. The const's name is unknowable because a non-call, non-alias initializer emits no row. Following the caller's `if (isX(error))` branch is a second walk and is deliberately not built. If `returned` plus `no-rows` is most of a target, the operator may read that as the part being empty.",
   "`surface` and `useTranslation` are cited to packages that no study app installs (sonner, react-hot-toast, react-toastify, react-i18next). The four package.json files were read this session. They are justified by public API shape only. tier3's 135-file `toast` is a project module. The expectation that its rows carry `from: fe:…#toast` through the tsconfig `@opal/*` paths was not confirmed by any capture: tier3 has no node_modules and is parsed with a borrowed typescript.",
   "Matching is by leaf or shape with no binding disproof, the same as every existing roster. A project function named `toast` or `redirect` is therefore classed as the library idiom. The `from` key is the only tell. If the operator wants a project binding to disprove the idiom, tier3's toasts become `other`.",
   "`message` is zero on gustify and gastify by construction, because both have project-local i18n (`useT`, `translate`). The marquee lab example (SetupScreen.tsx:43) is class `other` with the key in `args`. The lab must render `other` rows with their first literal argument or the fact is lost.",
   "Battery count churn from the new fixture file is predicted, not measured: sites 17, routed 12, rest 13, and no new finding, because 401, 423 and 500 are all produced by FORMS_R and none is the shared 409. If the file disturbs guards, hooks, controls or stores counts, simplify the file first. The predicted does[] for the 14 existing sites (10 read · 2 returned · 2 no-rows; classes return 8 · render 1 · other 1) is a source reading, not a run.",
   "F16 LIVE needs a resolvable typescript (run.sh:15-19 looks in gustify apps/web and gastify web). Without one the re-freeze cannot be made and suite-doctor reports SKIPPED coverage, which is not a full CLEAN. The re-freeze is a node run and must not overlap any other heavy job on this WSL box.",
   "`arms.frontend.bytes` and forms.json's size change when the keys are added. They are the only prior values that move, and D29 must say so or the 'existing keys keep their values' claim reads as broken. Per-site payload is bounded by DOES_CAP 24. tier3's 45 sites are the largest expected growth. Bytes were not measured.",
   "The lab half is wired to a string today (`String(rt.does)` at _lab-ep-panels.js:2835, passthrough at _ep_joins.py:278). A facts regen from a new feed without the lab edit prints `[object Object]`, and an empty list prints a blank row because `[]` is truthy in JS. The two must land together in step 18.",
   "About 5% of sites are not API reasons (gastify useScanToDraft.ts:67, gustify recipeFacetOptions.ts:167, keypro's SQLSTATE comparison in a backend adapter typed as a `fe:` piece). They will carry does rows or `returned`. They are harmless, since they reach no endpoint and route nothing, but they count in the stats.",
   "Adding `useNavigate` to a roster may be read as the V40a build trigger (amendment-1.md:1149). It is not: it classifies a does row and builds no guard row. This needs the operator's explicit word in D29.",
   "`returned` is a new state word beside the estate's `beyond one level`. The operator's state-word sweep may prefer the existing phrase. `mixed` was chosen because the lab already uses `shared` for endings that share one site.",
   "Nothing downstream reads frontend{} by design (D16): form_drift, pulse S20, review FORM DRIFT and gabe-map are all blind to it. No rail will notice if does[] regresses on a twin. Only tests/forms-frontend and the goldens protect it."
  ]
 },
 "readers": [
  {
   "key_facts": [
    "Row shape is fixed by push() at _a3_fe_extract.mjs:352-361 — {k, line, [guards], [after], [ctx], ...extra}; line is the 1-based START line only, no column, no end.",
    "Seven row kinds exist and no more: call · new · ret · throw · jsx · cmp · let. There is NO 'assign' kind — assignments, +=, property writes emit nothing (the walker falls through at :411).",
    "ret.value is RAW SOURCE TEXT clipped to 80 chars, never a resolved literal: return \"setup in progress\" is recorded as \"\\\"setup in progress\\\"\" WITH the quotes (:425, :473).",
    "A returned CALL emits TWO rows on the same line with the same guards: the ret row (text value) and a call row pushed by expr() at :430 — that sibling call row is the only place a message key like t(\"x\") is readable as args[0].",
    "i18n is NOT handled anywhere — no t()/useTranslation/i18n roster in the extractor or in _a3_forms_fe.py (grep clean). It would be a new roster entry in _a3_forms_fe.py, per that file's stated law.",
    "guards[] = {pred, neg?} from enclosing ifs, ternary arms, cond && <jsx/>, and switch cases; pred is the WHOLE condition text clipped at 120 chars (:362, :194) — the clip is silent and is why _a3_fe_reason._branch has an 'unread' verdict.",
    "after[] = earlier sibling ifs whose then-branch exited (return/throw, no else), recorded negated (:422) — the 'already ruled out' stack that threads into nested blocks.",
    "ctx[] values are exactly: try · catch:<var> · finally · loop · callback · callback:<name> · prop:<host>.<key> (:390-398, :436-438, :445, :461).",
    "A call row carries args (first 3 arguments resolved, positional nulls) + refs (same 3 as dotted-name TEXT) + props (first object arg's literal-valued keys) + opts (that object as a tree, depth 4). Only the FIRST object argument and only 3 arguments are ever read (:381-387).",
    "opts records a function as {fn, line, params} but ONLY inside an object argument — a positional callback's parameters (.then((r)=>…), .catch((e)=>…)) are NEVER recorded (amendment-1.md:1212 says so).",
    "cmp rows cover only the 8 operators in CMP_OPS (:347); a switch on a status produces NO cmp row, only synthesised guards (:462-465).",
    "cmp.right is the resolved literal when it resolves, else clipped text with rx:true; left_lit (the resolved left) exists in code at :409 but appears NOWHERE in flow.frozen.json.",
    "jsx rows carry tag · props (attributes: literal first, else clipped text 60, bare attr → true) · optional spread (40) · optional up={tag,attrs} for the nearest asChild/on* ancestor; a JSX FRAGMENT pushes no row, and JSX children become sibling rows under the same guards.",
    "resolveLit (:199-230): strings/numbers/bools, template spans with '*' for unknown, array literals with spreads spliced, factory calls returning one array literal, and identifier/property hops through the checker — LIT_DEPTH = 3, only symbol hops spend budget.",
    "binds is attached retroactively to the call row by line + a ctx-PRESENCE parity check (:449) — not ctx content; `const x = (await f()).data` gets no binds at all.",
    "ROW_CAP = 600 rows per body then truncated:true and rows stop (:192, :353) — no truncated body exists in the fixture, so nothing downstream is proven against it.",
    "Bodies are only module-level named functions, export-default expressions, const-assigned functions (memo/forwardRef unwrapped), store actions, and object-arg properties (Route.beforeLoad) plus <module>; top-level classes and anonymous `export default function(){}` get NO body (:547-568).",
    "A nested named `function` declaration inside a body loses its name — ctx becomes bare 'callback' (:466 → :377), unlike `const fn = () => …` which keeps 'callback:<name>' (:445).",
    "Store rows are duplicated: the store body holds every action's rows via callback: ctx AND each action gets its own `<Store>.<key>` body; _a3_fe_reason.py:247 skips the dotted body to avoid double-counting.",
    "tests/forms-frontend/run.sh:378-379 (F16 LIVE) re-derives flow.frozen.json BYTE FOR BYTE, and F15 proves the structure arm is identical with and without flow keys — any extractor edit must re-freeze the fixture in the same commit."
   ],
   "open_questions": [
    "Should does[] include rows whose ctx puts them in a callback (e.g. RequireSetup's signOut()/pushToast() inside `callback:useEffect` under a 401 guard)? They run after render, not in the branch's own turn — _branch ignores ctx entirely today.",
    "A branch has no id: rows are matched to it only by guard predicate TEXT, which is clipped at 120 chars. Does does[] need an extractor change (a branch ordinal on each guard) rather than a Python-side regex, given that any extractor edit forces a re-freeze of flow.frozen.json under F16?",
    "ret.value is text, so 'the value returned (e.g. a message key)' is only readable as a literal when the return is a CALL (sibling call row's args[0]). Should the extractor resolveLit the return expression too — a schema change — or should does[] read the sibling call row and report a bare string return as its quoted text?",
    "An imperative navigation (`navigate(\"/x\")`, `router.push(\"/x\")`) has no roster: _a3_forms_fe.py only knows NAV_ELEMENTS (<Navigate>/<Redirect>) and NAV_THROWS (redirect/notFound). Is adding a NAV_CALLS roster in scope for this part?",
    "A client that tells refusals apart with `switch (err.status)` emits no cmp row at all, so it is invisible to both _site() and does[]. Is that an accepted honest-empty, or a gap this part should name?",
    "Should does[] count a row that sits in a DEEPER nested branch (a guard list strictly longer than the site's) as 'what the branch does', or only rows at the branch's own guard depth?"
   ],
   "report": "## The extractor's flow rows — exact schema, as built\n\nSource of truth: `/home/khujta/projects/gabe_lens/templates/center/generators/_a3_fe_extract.mjs` (685 lines; the FLOW section is lines 183–577, gated by `GABE_FE_FLOW === '1'` at `:191`). Fixture: `/home/khujta/projects/gabe_lens/tests/forms-frontend/flow.frozen.json` (34 files, one line, 83 KB). The battery re-derives it byte for byte (`tests/forms-frontend/run.sh:378-379`, case **F16 LIVE**) — any extractor edit for `does[]` must re-freeze that file in the same commit.\n\n---\n\n### 1. Where flow sits in the capture\n\n`rec.flow = flowOf(sf)` is written per file at `_a3_fe_extract.mjs:676`, last key of each `byFile[<rel path>]` record. Shape:\n\n```\nbyFile[\"src/routes/SetupForm.tsx\"] = { story, imports, bindings, exports, file_refs, flow }\nflow = { bodies: { \"<name>\": BODY, … },  routes?: [ ROUTETREE, … ] }      // :576\nBODY = { line, params?, rows: [ROW…], truncated?: true, wraps?: [...] }   // :546, :557, :574\n```\n\n- `bodies` keys are **sorted**; `rows` are in walk order, never sorted (`:576`).\n- `routes` is **omitted** when the file holds no route array (`:576`).\n- `flow` is present even when `bodies` is `{}` (e.g. `src/routes/paths.ts` in the fixture).\n- Files scanned: only under `WEB/src` (or `WEB`), no `.d.ts`, no declaration files, none under `node_modules|dist|build|storybook-static|coverage|.next|.turbo`, and **no test files** (`isTest` at `:64`) — `_a3_fe_extract.mjs:582`.\n\n**Which functions become bodies** (`flowOf`, `:544-577`):\n| body key | source | line |\n|---|---|---|\n| `<fnName>` | a module-level `function` declaration **with a name** | `:548` |\n| `default` | `export default <fn expression>` (ExportAssignment) | `:549` |\n| `<constName>` | `const X = () => …`, unwrapping `memo()`/`forwardRef()` (`fnOfDecl` `:482`) | `:554` |\n| `<constName>.<key>` | a function-valued property of the object a **store** initializer's function returns (`storeActions` `:497`) | `:557` |\n| `<constName>.<key>` | a function-valued property of any object argument in the declaration's call chain — e.g. `createFileRoute(\"/x\")({ beforeLoad })` → `Route.beforeLoad` | `:560-566` |\n| `<module>` | module-scope expression statements + non-function variable initializers | `:570-574` |\n\n`add()` is **first-wins** — a duplicate name is silently skipped (`:546`).\n\n---\n\n### 2. The ROW schema, per kind\n\nEvery row is built by `push()` (`:352-361`): `{ k, line, [guards], [after], [ctx], …extra }` — key order is exactly that, `line` is the 1-based **start** line (`lineOf` `:195`), there is no column and no end line.\n\n**Universal fields**\n\n- `guards: [{pred: string, neg?: true}]` — enclosing `if` conditions, ternary arms (`:366-370`), the left side of `cond && <jsx/>` (`:372-376`), and switch cases (`:462-465`). Emitted only when non-empty. `pred` is `clip(text, 120)` (`:362`, `:194`) — a longer condition is **truncated silently** (this is what `_a3_fe_reason._branch`'s `unread` word detects, `_a3_fe_reason.py:80`). A switch case pred is synthesised as `clip(subject,60) + ' === ' + clip(case,40)`.\n- `after: [{pred, neg: true}]` — earlier **sibling** `if`s in the same block whose then-branch ended in `return`/`throw` and that had no `else` (`:422`). This is the \"already ruled out\" stack; it threads down into nested blocks.\n- `ctx: [string]` — the callback nesting, outermost first. Values: `try` · `catch:<var>` · `finally` · `loop` (`:436-438`, `:461`), `callback` (a function expression in any position whose name is unknown), `callback:<name>` (a named `const fn = () => …`, `:445`, or the call host of a positional callback, `:390`+`:393`), `prop:<host>.<key>` (a function-valued property of a call's object argument, `:398`). Emitted only when non-empty.\n\n**`k: \"call\"` / `k: \"new\"`** (`:379-405`) — 156 + 6 rows in the fixture\n\n| field | meaning | line |\n|---|---|---|\n| `callee` | `clip(<expression text>, 80)`. May be a whole chain: `\"fetch(path, { method: options.method ?? \\\"GET\\\" }).then\"` | `:380` |\n| `args` | the **first 3** arguments through `resolveLit`, positional `null` for unresolved. Emitted only if ≥1 non-null | `:381,383` |\n| `refs` | the same 3 arguments as raw **text** when it matches `NAME_RX = /^[A-Za-z_$][\\w$]*(\\??\\.[A-Za-z_$][\\w$]*)*$/` (`:303`), else `null`. Emitted only if ≥1 non-null | `:384-385` |\n| `props` | the **first** object-literal argument's top-level `PropertyAssignment`s whose initializer resolves to a literal, flat `{key: literal}`. Shorthand (`{ body }`) is **not** here | `:387` |\n| `opts` | the same object as a tree (`optTree`, `:333`): literals resolved · functions as `{fn: \"inline\"\\|<name>, line, params?}` · nested objects to depth 4 then `{expr}` · a non-literal as `{ref: clip(text,60)}` · a `.map` callback parameter as `{ref, each: [...]}` (`forwardOf` `:311`) · a spread as `\"...<expr>\": true`. Emitted whenever an object argument exists — **including `{}`** | `:387`, `:323-344` |\n| `binds` | attached retroactively by the variable-statement handler, not by the call itself (see §4) | `:450` |\n\n**`k: \"ret\"`** (`:423-430` for `return`, `:469-478` for a concise arrow body) — 151 rows\n\n| field | meaning | line |\n|---|---|---|\n| `value` | **raw source text, `clip(…, 80)`** — never a resolved literal. `return \"setup in progress\"` → `\"\\\"setup in progress\\\"\"` **with the quotes**. `return;` → `null` | `:425`, `:473` |\n| `implicit: true` | a concise arrow body (`() => x`) rather than a `return` statement | `:473` |\n| `jsx` + `tags` | root tag name, plus up to **6** descendant tag names incl. the root (`jsxTags` `:232`) | `:426`, `:474` |\n| `null: true` | literally `return null` | `:427` |\n| `obj` | a returned object literal: `{key: clip(valueText,80)}`; a method is `\"method\"`, a shorthand its own name; a spread adds `\"...<expr>\": true` and, within depth 2, merges the spread constant's own fields (`objText` `:260`) | `:428`, `:475` |\n\n**`k: \"cmp\"`** (`:407-410`) — 24 rows. Only the 8 operators in `CMP_OPS` (`:347`): `=== !== == != < > <= >=`. **A `switch` produces no `cmp` row** — only synthesised guards (`:462-465`).\n\n| field | meaning |\n|---|---|\n| `left` | `clip(text, 60)`, always text |\n| `op` | the operator token text |\n| `right` | `resolveLit(right)` when it resolves (number/string/bool/array), **else** `clip(text, 60)` |\n| `rx: true` | the right side did **not** resolve — `right` is text, not a value |\n| `left_lit` | the left side's literal when it resolves (`left` still holds the text). **Not present anywhere in the fixture** |\n\n**`k: \"throw\"`** (`:431-434`) — 4 rows: `value` = `clip(text, 80)`, `callee` = `clip(expr.expression, 60)` when throwing a call/new. The inner `new`/`call` row is pushed **separately on the same line** by the following `expr()`.\n\n**`k: \"jsx\"`** (`:378`) — 77 rows: `tag` (`tagOf` `:231`), `props` (every JSX attribute: `resolveLit` first, else `clip(text,60)`, a bare attribute → `true`; `jsxProps` `:233`), optional `spread` = `clip(firstSpreadAttr, 40)` (`:245`), optional `up = {tag, attrs}` — the nearest ancestor JSX element **not crossing a function boundary** that carries `asChild` or any `on*` attribute (`jsxUp` `:248`). A **JSX fragment pushes no row**. JSX children are walked with the same guards (the branch falls through to `forEachChild` at `:411`), so `<form>` and its `<p>` children are sibling rows.\n\n**`k: \"let\"`** (`:451-456`) — 1 row in the fixture: `binds` + `alts` (the names an alias may hold), only when the initializer is not a call, not a function, and **every** alternative matches `NAME_RX`. `const active = redo ? save : complete` → `{\"k\":\"let\",\"line\":72,\"binds\":[\"active\"],\"alts\":[\"save\",\"complete\"]}`.\n\n**There is no `assign` kind.** `x = y`, `obj.prop = v`, `+=` produce **no row** — the operator is not in `CMP_OPS`, so the walker falls through to the children (`:411`). Likewise `await` is transparent: the awaited call gets its row with no marker that it was awaited.\n\n---\n\n### 3. Literal resolution (`resolveLit`, `:199-230`)\n\n- string / no-substitution template / number (with `_` separators stripped) / `true` / `false` → the value.\n- **Template expression** → head + spans, an unresolvable span as `*`; returns `null` if nothing at all was known (`:207-211`). Fixture: `` `${PATHS.login}?next=1` `` → `\"/login?next=1\"`; `` `/api/v1/recipes/${id}` `` → `\"/api/v1/recipes/*\"`.\n- **Array literal** → array, unresolvable element `*`, a spread of a resolvable array spliced in (`:212-219`).\n- **Call** → `factoryLit` (`:277`): a function whose body is one array literal, parameters as `*`. `queryKeys.recipes.detail(id)` → `[\"recipes\",\"detail\",\"*\"]`.\n- **Identifier / property access** → the checker's symbol (aliases followed) to a `VariableDeclaration` / `PropertyAssignment` / `EnumMember` initializer. **Only a symbol hop spends the budget**, `LIT_DEPTH = 3` (`:193`, `:206`).\n- Everything else → `null`.\n- `unparen` (`:198`) transparently strips `( )`, `as`, `!`, `satisfies`.\n\n**i18n is NOT handled — not found.** There is no roster for `t()`, `useTranslation`, `i18n` anywhere in the extractor or in `_a3_forms_fe.py` (grep clean across `_a3_fe_extract.mjs`, `_a3_forms_fe.py`, `_a3_fe_reason.py`, `_a3_fe_forms.py`, `_a3_fe_controls.py`). A `return t(\"setup.locked\")` yields a `ret` row whose `value` is the text `t(\"setup.locked\")` **plus** a separate `{k:\"call\", callee:\"t\", args:[\"setup.locked\"]}` row on the same line with the same guards. The message key is therefore reachable — via the sibling `call` row's `args[0]`, never via `ret.value` — and the `does[]` roster would have to name `t`/`i18n.t`/`formatMessage` in `_a3_forms_fe.py` the way `NAV_THROWS` names `redirect`.\n\n---\n\n### 4. How each thing the task names is recorded\n\n**A `return <string>` inside an if-branch** — one `ret` row carrying `guards` (the if predicates, innermost last) and `after` (the earlier exited ifs). `value` is the **quoted source text**. `SetupForm.tsx:16`:\n```json\n{\"k\":\"ret\",\"line\":16,\"guards\":[{\"pred\":\"isApiError(error)\"},{\"pred\":\"error.status === 409\"}],\"value\":\"\\\"setup in progress\\\"\"}\n```\n\n**A `return <call>` inside an if-branch** — the `ret` row (text `value`), **plus** a `call` row on the same line with the same guards, pushed by `expr(st.expression, …)` at `:430`. Two rows, same line.\n\n**JSX rendered inside a branch** — a `jsx` row with `guards`; when it is the returned expression, a `ret` row with `jsx`/`tags` too. `RequireAuth.tsx:13`:\n```json\n{\"k\":\"ret\",\"line\":13,\"guards\":[{\"pred\":\"status === \\\"anon\\\"\"}],\"after\":[{\"pred\":\"status === \\\"pending\\\"\",\"neg\":true}],\"value\":\"<Navigate to={`${PATHS.login}?next=1`} replace />\",\"jsx\":\"Navigate\",\"tags\":[\"Navigate\"]}\n{\"k\":\"jsx\",\"line\":13,\"guards\":[{\"pred\":\"status === \\\"anon\\\"\"}],\"after\":[{\"pred\":\"status === \\\"pending\\\"\",\"neg\":true}],\"tag\":\"Navigate\",\"props\":{\"to\":\"/login?next=1\",\"replace\":true}}\n```\nA conditional render inside a return (`{expired ? <p>expired</p> : null}`) gives a `jsx` row with `guards:[{\"pred\":\"expired\"}]` while the enclosing `ret` row carries **no** guard (`SetupForm.tsx:77`).\n\n**A call's literal arguments and object options** — `args` (first 3, resolved) + `refs` (first 3, as names) + `props` (literal-valued keys of the first object argument) + `opts` (the full tree). `useToggleThing.ts:18`:\n```json\n{\"k\":\"call\",\"line\":18,\"ctx\":[\"prop:useMutation.mutationFn\"],\"callee\":\"apiFetch\",\"args\":[\"/api/v1/things/toggle\",null],\"props\":{\"method\":\"POST\"},\"opts\":{\"method\":\"POST\",\"body\":{\"ref\":\"done\"}}}\n```\n\n**A navigation** — three shapes, all already rostered in `_a3_forms_fe.py`: a `jsx` row whose `tag` is in `NAV_ELEMENTS` (`:8`) with the resolved `props.to`; a `throw` row whose `callee` leaf is in `NAV_THROWS` (`:9`) plus its `call` row carrying `props:{to:\"/items\"}` (`reports.tsx:5`); an imperative `navigate(\"/x\")` would be a plain `call` row with `args[0]` — **no roster for it exists today** (`NAV_ELEMENTS`/`NAV_THROWS` only).\n\n**A refetch / retry / cache invalidation** — plain `call` rows; the rosters live in `_a3_forms_fe.py:26-27` (`INVALIDATE_CALLS`, `SEED_CALLS`) and `:35` (`CACHE_WRITE_CALLS`). The resolved key rides `props.queryKey` / `opts.queryKey`:\n```json\n{\"k\":\"call\",\"line\":21,\"ctx\":[\"prop:useMutation.onSettled\"],\"callee\":\"queryClient.invalidateQueries\",\"props\":{\"queryKey\":[\"things\",\"list\"]},\"opts\":{\"queryKey\":[\"things\",\"list\"]}}\n{\"k\":\"call\",\"line\":12,\"ctx\":[\"prop:useMutation.onSuccess\",\"callback:INVALIDATIONS.setupComplete.map\"],\"callee\":\"queryClient.invalidateQueries\",\"opts\":{\"queryKey\":{\"ref\":\"queryKey\",\"each\":[[\"me\"],[\"settings\"],[\"recipes\"]]}}}\n```\nA retry is a `call` row too — `apiFetch`'s 401 re-fetch at `client.ts:11` (below).\n\n**`binds` (the value a call is given to)** — attached retroactively at `:446-450`: after walking a variable declaration's initializer, the walker finds the `call` row on the initializer's line and sets `binds`. Object pattern → `\"propertyName:local\"` (or the bare name), array pattern → names with `\"\"` for holes. `const { response } = await apiClient.PATCH(...)` → `binds:[\"response\"]`; `const [mapError, setMapError] = useState(...)` → `binds:[\"mapError\",\"setMapError\"]`.\n\n**Body `params`** (`paramsOf` `:304`) — a string per parameter, an **array of keys** for a destructured object (`\"propertyName:local\"` when renamed), **`null`** for an array pattern or anything else. Omitted when the function takes no parameters. Only bodies and `opts`-hosted functions get `params` — see the limit below.\n\n---\n\n### 5. Five real rows inside a status-comparison branch\n\n**(a) `src/routes/RequireSetup.tsx:16-19` — the best `does[]` specimen: two calls in the two arms of a 401 comparison, one with a literal argument**\n```json\n{\"k\":\"cmp\",\"line\":16,\"ctx\":[\"callback:useEffect\"],\"left\":\"(error as { status?: number }).status\",\"op\":\"===\",\"right\":401}\n{\"k\":\"call\",\"line\":17,\"guards\":[{\"pred\":\"isError && error && (error as { status?: number }).status === 401\"}],\"ctx\":[\"callback:useEffect\"],\"callee\":\"signOut\"}\n{\"k\":\"call\",\"line\":19,\"guards\":[{\"pred\":\"isError && error && (error as { status?: number }).status === 401\",\"neg\":true},{\"pred\":\"isError\"}],\"ctx\":[\"callback:useEffect\"],\"callee\":\"pushToast\",\"args\":[\"retrying\"]}\n```\nNote the guard `pred` is the **whole** `if` condition, not the comparison alone — `_a3_fe_reason._branch` (`_a3_fe_reason.py:76`) therefore matches it with a regex built from `left op right`, and `_norm_recv` (`:52`) undoes the `as` cast.\n\n**(b) `src/routes/SetupForm.tsx:16` — a status branch returning a message string**\n```json\n{\"k\":\"cmp\",\"line\":16,\"guards\":[{\"pred\":\"isApiError(error)\"}],\"left\":\"error.status\",\"op\":\"===\",\"right\":409}\n{\"k\":\"ret\",\"line\":16,\"guards\":[{\"pred\":\"isApiError(error)\"},{\"pred\":\"error.status === 409\"}],\"value\":\"\\\"setup in progress\\\"\"}\n```\n\n**(c) `src/routes/RequireAuth.tsx:12-13` — a status branch that navigates** (quoted in full in §4).\n\n**(d) `src/lib/api/client.ts:5-11` — a 503 branch that throws, and a 401 branch that retries the fetch**\n```json\n{\"k\":\"cmp\",\"line\":5,\"ctx\":[\"callback:then\"],\"left\":\"r.status\",\"op\":\"===\",\"right\":503}\n{\"k\":\"throw\",\"line\":6,\"guards\":[{\"pred\":\"r.status === 503\"}],\"ctx\":[\"callback:then\"],\"value\":\"new ApiError(503)\",\"callee\":\"ApiError\"}\n{\"k\":\"new\",\"line\":6,\"guards\":[{\"pred\":\"r.status === 503\"}],\"ctx\":[\"callback:then\"],\"callee\":\"ApiError\",\"args\":[503]}\n{\"k\":\"cmp\",\"line\":10,\"left\":\"response.status\",\"op\":\"===\",\"right\":401}\n{\"k\":\"call\",\"line\":11,\"guards\":[{\"pred\":\"response.status === 401\"}],\"callee\":\"fetch\",\"refs\":[\"path\",null],\"opts\":{\"method\":{\"ref\":\"options.method ?? \\\"GET\\\"\"}}}\n```\n\n**(e) `src/routes/SetupForm.tsx:47-50` — a fetch bound by `binds`, then an `after` chain of status branches**\n```json\n{\"k\":\"call\",\"line\":47,\"callee\":\"apiClient.PATCH\",\"args\":[\"/api/v1/settings\",null],\"refs\":[null,\"body\"],\"binds\":[\"response\"]}\n{\"k\":\"cmp\",\"line\":48,\"left\":\"response.status\",\"op\":\"===\",\"right\":422}\n{\"k\":\"ret\",\"line\":48,\"guards\":[{\"pred\":\"response.status === 422\"}],\"value\":\"\\\"invalid\\\"\"}\n{\"k\":\"cmp\",\"line\":49,\"after\":[{\"pred\":\"response.status === 422\",\"neg\":true}],\"left\":\"response.status\",\"op\":\"===\",\"right\":204}\n{\"k\":\"ret\",\"line\":50,\"after\":[{\"pred\":\"response.status === 422\",\"neg\":true},{\"pred\":\"response.status === 204\",\"neg\":true}],\"value\":\"\\\"unknown\\\"\"}\n```\n\n**(f) bonus — `src/routes/SetupForm.tsx:32-33`, a `code` branch rendering an element** (the `jsx` + `ret` pair under one guard):\n```json\n{\"k\":\"ret\",\"line\":33,\"guards\":[{\"pred\":\"isApiError(error) && error.code === \\\"setup_locked\\\"\"}],\"value\":\"<p>locked</p>\",\"jsx\":\"p\",\"tags\":[\"p\"]}\n{\"k\":\"jsx\",\"line\":33,\"guards\":[{\"pred\":\"isApiError(error) && error.code === \\\"setup_locked\\\"\"}],\"tag\":\"p\",\"props\":{}}\n```\n\n---\n\n### 6. Known limits (what is NOT emitted)\n\n1. **`ROW_CAP = 600` per body** (`:192`, `:353`) — above it rows are dropped and the body gets `truncated: true`. **No `truncated` body exists in the fixture**, so nothing proves the flag downstream.\n2. **Clipping**: `pred` 120 · `callee` 80 · `ret.value` 80 · `throw.value` 80 · `objText` values 80 · `cmp.left`/`cmp.right` 60 · JSX prop text 60 · `opts` `{ref}` 60 · `jsxSpread` 40 · switch-case subject 60 + case 40 · `jsxTags` cap **6**. Truncation is silent — there is no \"was clipped\" flag except `cmp.rx`.\n3. **Only the first 3 arguments** get `args`/`refs` (`:381`, `:384`). A 4th literal argument is invisible.\n4. **Only the first object-literal argument** gets `props`/`opts` (`:386`, `.find`). A second object argument is dropped.\n5. **A positional callback's parameters are never recorded.** `optVal` (`:326`) records `{fn, line, params}` only for functions inside an **object** argument; `.then((r) => …)` / `.catch((e) => …)` bodies are walked (ctx `callback:then`) but their parameter names are lost. Stated as \"Not built\" in `docs/design/element-forms/amendment-1.md:1212`.\n6. **`switch` emits no `cmp` row** (`:462-465`) — a client that tells refusals apart with `switch (err.status)` is invisible to the reason part today.\n7. **No assignment rows.** Also no rows for string concatenation, `delete`, property writes, or a variable declaration whose initializer is neither a call, a function, nor an all-names alias.\n8. **Top-level `class` declarations produce no body** — `flowOf` (`:547-568`) handles only `FunctionDeclaration`, `ExportAssignment`, `VariableStatement`. So do `export default function () {}` (anonymous — `st.name` is falsy at `:548`).\n9. **A nested named `function` declaration inside a body loses its name**: `stmts`'s fall-through (`:466`) hands it to `expr`, which pushes ctx `'callback'` with no name (`:377`) — unlike `const fn = () => …`, which keeps `callback:<name>` (`:445`).\n10. **A branch has no identity.** Rows inside a branch are recognisable only by the **text** of a non-negated `guards[].pred` (exactly what `_a3_fe_reason._branch` does at `_a3_fe_reason.py:76-78`). Two `if`s with identical predicate text in one body are indistinguishable, and a predicate clipped at 120 chars breaks the match (today's `unread` verdict, `:80`).\n11. **`binds` can be mis-attached**: the match is `r.line === lineOf(init) && !r.ctx === !ctx.length` (`:449`) — line + a *presence* parity check on ctx, never ctx content. Two calls on one line at the same nesting depth can take the wrong binds. And a call that is not the direct initializer (`const x = (await f()).data`) gets **no** binds and no `let` row.\n12. **Store rows are duplicated**: the store body (`usePrefsStore`) holds every action's rows via `callback:` ctx, and each action also gets its own body (`usePrefsStore.remove`). `_a3_fe_reason.py:247` skips the `.`-named body when its stem exists — any new part must do the same or double-count.\n13. **A `does[]` collector must decide about ctx.** Rows inside a branch may also sit in a callback (`ctx: [\"callback:useEffect\"]` in specimen (a)) — they run after render, not in the branch's own turn. `_branch` today ignores ctx entirely.\n14. **`ret.value` is text, not a literal** — the single biggest gap for \"the value returned (e.g. a message key)\". Quotes are part of the string; a call's key must be read from the sibling `call` row's `args`.\n15. **Determinism contract**: files sorted, bodies sorted, refs deduped + sorted, no wallclock (`:11-13`). `tests/forms-frontend` F15 also proves the **structure** arm is identical with and without the `flow` keys, and F16 proves the no-flag run emits no `flow` key at all — both constrain any edit.\n\n---\n\n### 7. Consumer anchors (for reusing, not re-deriving)\n\n- `_a3_fe_reason.py:75-86` `_branch()` — today's one-word verdict; `_texts()` at `:70` already flattens `value · callee · left · refs · props.values()` into the strings it scans, and `READ_RX` (`:45`) is the `detail|code` test. This is the exact function `does[]` replaces.\n- `_a3_fe_forms.py:353` `_leaf(callee)` · `:91` `_resolve(callee, bindings, file, local)` (binding-aware target id) · `:39` `_rid(letter, piece, parts)` (the `x-`/`g-`/`r-` id minting, no lines) · `:114` `_nav(row, rows)` (the ret/throw → navigation pair) · `:124` `_norm(to)`.\n- Rosters to extend for `does[]` classification: `_a3_forms_fe.py:8-9` (`NAV_ELEMENTS`, `NAV_THROWS`), `:17` (`EFFECT_HOSTS`), `:23-27` (`CACHE_QUERY_CALLS`, `CACHE_MUTATION_CALLS`, `INVALIDATE_CALLS`, `SEED_CALLS`), `:31-38` (`HANDLER_PROPS`, `CACHE_WRITE_CALLS`, `MUTATE_CALLS`), `:39` (`REASON_MEMBERS`). The file's header states the law: *\"The extractor carries no roster; a new idiom is an edit HERE.\"*\n- The flow run itself: `_a3_forms_build.py:356-363` (second extractor invocation, `GABE_FE_FLOW=1`, `FLOW_TIMEOUT = 300` from `_a3_forms_fe.py:18`)."
  },
  {
   "key_facts": [
    "`_branch()` at `templates/center/generators/_a3_fe_reason.py:75-86` already collects the rows a does[] needs into `inside` (later rows whose non-negated guard matches the site predicate) and throws them away for one word — the rows are there, the walk is not new.",
    "Extractor rows already carry what does[] wants: `call` rows have `callee` + `args` (first 3 resolved literals) + `refs` + `props` + `opts` (`_a3_fe_extract.mjs:380-389`); `ret` rows have `value` (clipped 80) + `jsx` tag (`:427-434`); `jsx` rows have `tag`/`props` (`:378`). No extractor change is needed unless a NEW field is wanted.",
    "Rosters that already exist in `_a3_forms_fe.py`: `INVALIDATE_CALLS:26`, `SEED_CALLS:27`, `CACHE_WRITE_CALLS:35`, `MUTATE_CALLS:38`, `NAV_ELEMENTS:8`, `NAV_THROWS:9`. NOT FOUND: any roster for a navigate HOOK (`useNavigate`/`navigate(…)`/`router.push`) or for a per-query `refetch()`/retry call — both must be added as new frozensets.",
    "The frontend arm's parts are a registry tuple: `_a3_forms.py:312` `\"frontend\": {\"slice\": 11, \"parts\": (\"guards\", \"hooks\", \"client\", \"reason\", \"controls\", \"stores\")}` and a hardcoded twin in `_a3_forms_build.py:408` `built = (\"guards\", \"hooks\", \"client\", \"reason\", \"controls\", \"stores\")` — adding a part to one and not the other turns `arms.frontend.reason` from null to `partial — …` and fails the battery.",
    "`tests/forms-frontend/run.sh:147` asserts `sorted(a[\"parts\"]) == sorted(F.ARMS[\"frontend\"][\"parts\"])` AND that every part reads `{present: True, reason: None}` — so a registered `does` part must ship built, in the same commit.",
    "`tests/forms-frontend/run.sh:291` asserts the reason stats dict EXACTLY (`rs == {\"sites\": 14, \"reads_status\": 11, …}`); any new stats key from does[] fails F13 until that dict is updated.",
    "`tests/forms-frontend/run.sh:149` and `:152` assert `stats[\"findings\"]` as exact dicts — a new finding id changes both.",
    "Row ids are `<letter>-<sha10>` via `_a3_fe_forms._rid` (`:39-40`). Letters MINTED: `x` exit, `e` effect, `c` control, `p` chain row, `r` reason site. Letters `b s a q m t` were planned and STRUCK at §A4 V33 (`amendment-1.md:115`) — `d` is free, and a new letter needs that §A1 table row edited.",
    "`flow.frozen.json` (83 KB) is re-derived by `GABE_TS_DIR=<web> GABE_FE_FLOW=1 node _a3_fe_extract.mjs <fixture> <out> <fixture>` and compared byte-for-byte by F16 LIVE (`tests/forms-frontend/run.sh:377-392`); without a resolvable typescript F16 SKIPs by name and suite-doctor prints `INFO … SKIPPED coverage` (`suite-doctor.sh:111-115`).",
    "The fixture's reason branches today return strings or call `setMapError(…)` (`tests/forms-frontend/fixture/src/routes/SetupForm.tsx:14-36`) — NO navigation, refetch or invalidation inside a reason branch, so proving those does[] classes needs new fixture source and a re-blessed `flow.frozen.json`.",
    "`propagate.sh:43` greps `^\\s*(import|from) <mod>\\b` over every suite `GEN/*.py`, so a NEW `_a3_*.py` imported even at an indentation (as `_a3_fe_reason` is, at `_a3_forms_build.py:393`) lands on a twin as `NEW, required by …`; a module imported by nothing only prints `NEW (not vendored)` and never lands (`:47-49`).",
    "`install.sh:107-118` copies `templates/*/` recursively into `~/.claude/templates/gabe/<sub>/`; `suite-doctor.sh:76-85` hashes every repo template against its install, so any generator edit without `./install.sh` (≈5 s) reports DRIFT.",
    "No consumer reads the frontend arm: `form_drift.py:37-38` NAG/COUNT lists only endpoint-arm findings, and `mapquery.form_summary` (`skills/gabe-map/scripts/mapquery.py:659-673`) keeps a fixed key tuple and never touches `frontend` — a does[] finding is invisible to pulse S20 and gabe-map BY DESIGN (D16), so no skill version bump and no CLAUDE.md capability-row change.",
    "The committed example feed `templates/center/shell/example/codebase-graph-station/forms.json` is an ARMS-OFF build (top keys: endpoints · framework · head · kind · present · stats · version; no `arms`), and `regen-example.sh:38` pins `GABE_FORMS_ARMS=none` — a frontend-arm change moves NO example byte.",
    "Goldens are data: `docs/design/element-forms/goldens/expected.json` holds 62 goldens, 2 of them slice `11c`; each is `{id, slice, mark(V·R·P·U), says, get:[path steps], eq|has|len|gte|exists}`. `check-goldens.py` is read-only and exits non-zero on mismatch/missing. `render-acceptance.py:8-10` carries a fixed slice→arm-label dict that a new slice label (e.g. `11e`) must be added to.",
    "The slice protocol and exit criteria are `amendment-1.md:300-317`: own battery in the house style, every mutant must flip a check, dry-run on gustify · gastify · tier3 · keypro serially, numbers in the commit message, a `gen/README.md` module-table row, every module under 800 lines, suite-doctor CLEAN. `_a3_fe_reason.py` is 291 lines, `_a3_forms_fe.py` 56 — both far under budget.",
    "`scripts/forms-dryrun.sh:11` states the cost: every target builds TWICE (gustify ≈1 min a build, tier3 ≈2) and it must run ALONE; usage `scripts/forms-dryrun.sh [--arms a,b|all] [name…]`, targets from `tests/baselines/targets.conf` (gustify · gastify · tier3 · keypro).",
    "The last decision in §A5 is D28 (`amendment-1.md:1429`) — a new ruling for does[] takes D29.",
    "The 'arms only add' law is enforced at `tests/center/run.sh:445-455` by a `subset()` walk over `off[\"endpoints\"]` vs `on[\"endpoints\"]`, plus `:450` which asserts the arm NAME list exactly (arm names, not parts — unaffected by a new part)."
   ],
   "open_questions": [
    "Is `does` a REGISTERED part of the frontend arm (a 7th entry in `_a3_forms.ARMS[\"frontend\"][\"parts\"]`, reported in `arms.frontend.parts`) or a key written INSIDE the existing `reason` part (`frontend.reasons.sites[].does[]`)? The registered-part path costs `_a3_forms.py:312` + `_a3_forms_build.py:408` + the F8 parts assert; the inside-reason path costs none of them. The task text says both ('a new part … inside the frontend arm's reason part'), so this must be ruled before any file is touched.",
    "Do does[] rows mint their own ids? Only `x e c p r` have ever been minted (`amendment-1.md:115`); `b s a q m t` were struck at §A4 V33. If yes, pick `d` and edit that §A1 table row; if no, the rows are addressed by their `at` (the V33 precedent for claims, limiters and service raises).",
    "Does the part add a FINDING (e.g. 'a branch that reads a refusal and does nothing with it')? If yes it needs an `_a3_forms.FINDINGS` entry with `arm: \"frontend\"`, a slot and a `pulse` word, plus updates to the two exact findings dicts at `tests/forms-frontend/run.sh:149` and `:152` — and it still will not reach pulse S20 (`form_drift.py:37-38`) until the deferred §A4 step-7 consumer uptake.",
    "Which navigate and refetch idioms go in the roster? Today `_a3_forms_fe.py` has no navigate-hook and no per-query-refetch roster, and §A4 V40a explicitly STRUCK the nav-hook guard 'with a build trigger by operator ruling' (`amendment-1.md:1149`) — adding `useNavigate`/`navigate(…)` to a does[] roster may be that trigger, or may need its own ruling.",
    "Must the fixture grow new source? `SetupForm.tsx` has no navigation, refetch or invalidation inside a reason branch, so proving those classes means new `.tsx` + a re-blessed `flow.frozen.json` — which needs a resolvable typescript (`GABE_TS_DIR`) on this machine. If the part ships proving only call · value · element from the existing capture, the frozen file never moves and F16 stays green untouched.",
    "Is a new sub-slice label wanted (`11e`) or does this land under `11c`? A new label needs a row in `render-acceptance.py`'s ARM dict (`:8-10`) or the acceptance sheet renders that section with an empty arm name."
   ],
   "report": "> Read-only pass. No battery, build, doctor, dry run, browser, npm or node program was run. Every claim below is a file read or a grep.\n\n---\n\n## 0 · What the ground already gives you\n\n**The rows does[] wants are already captured and already collected.**\n\n`_a3_fe_reason._branch()` — `templates/center/generators/_a3_fe_reason.py:75-86`:\n\n```python\ndef _branch(rows: list, site: dict, recv: str) -> str:\n    pat = re.compile(re.escape(site[\"left\"]) + r\"\\s*\" + re.escape(site[\"op\"]) + …)\n    later = [r for r in rows if r is not site and r[\"line\"] >= site[\"line\"]]\n    inside = [r for r in later if any(not g.get(\"neg\") and pat.search(g[\"pred\"]) for g in r.get(\"guards\") or [])]\n    if not inside:\n        return \"unread\" if any(len(g[\"pred\"]) >= 120 …) else \"value\"\n    if any(READ_RX.search(t) for r in inside for t in _texts(r)):\n        return \"reads\"\n    …\n    return \"none\"\n```\n\n`inside` **is** the does[] row set. The function reduces it to one word and drops it. A does[] part keeps `inside` and classifies each row.\n\n**The extractor already emits every field the five does[] classes need** (`_a3_fe_extract.mjs`, 685 lines):\n\n| does[] class | row kind | fields already emitted | anchor |\n|---|---|---|---|\n| the call with its literal arguments | `call` | `callee`, `args` (first 3 resolved literals), `refs`, `props`, `opts` | `:379-389` |\n| the value returned (a message key) | `ret` | `value` (clipped 80), `obj`, `null` | `:427-432` |\n| the element rendered | `ret` / `jsx` | `jsx` + `tags` on a return; `tag` + `props` on a `jsx` row | `:430`, `:378` |\n| a navigation | `jsx` (`<Navigate>`) · `throw` (`redirect`) · `call` (`navigate(…)`) | `tag` · `callee` · `callee`+`args` | `:378`, `:436`, `:381` |\n| a refetch / retry / invalidation | `call` | `callee`, `args`, `opts` | `:381-389` |\n\n**So `_a3_fe_extract.mjs` needs NO change** unless a field is genuinely missing (e.g. a 4th+ argument — `args`/`refs` are capped at 3 slices, `:381`/`:384`). If it does change, note the trap: a new `.mjs` never lands on a twin (`propagate.sh:47-49`), but the existing one lands through the update loop (`:25-32`).\n\n**Roster state in `_a3_forms_fe.py` (56 lines, data only):**\n\n| needed for | exists? | anchor |\n|---|---|---|\n| cache invalidation | YES — `INVALIDATE_CALLS` | `:26` |\n| cache seed | YES — `SEED_CALLS` | `:27` |\n| cache write | YES — `CACHE_WRITE_CALLS` | `:35` |\n| mutate / retry-the-call | YES — `MUTATE_CALLS` | `:38` |\n| navigation element / throw | YES — `NAV_ELEMENTS`, `NAV_THROWS` | `:8-9` |\n| **navigation HOOK** (`useNavigate`, `navigate(…)`, `router.push`) | **not found** | — |\n| **per-query refetch** (`refetch()`, `query.refetch`) | **not found** | — |\n\nThe file's own header states the law you must keep: *\"The extractor carries no roster; a new idiom is an edit HERE, never in `_a3_fe_forms`.\"* (`_a3_forms_fe.py:1-5`)\n\n---\n\n## 1 · THE CHECKLIST — every file that must change, in landing order\n\n### Step 1 — the SPEC, before any code\n\n**`docs/design/element-forms/amendment-1.md`** — four edits:\n\n1. **§A1 arms table row** `:76` — `| \\`frontend\\` | \\`guards\\` · \\`hooks\\` · \\`client\\` · \\`reason\\` · \\`controls\\` · \\`stores\\` | 11 | top-level \\`frontend{}\\` |`. Add `does` to the parts list **only if** it is a registered part.\n2. **§A1 id table** `:115` — `| \\`<l>-<sha10>\\` | a frontend row (\\`x e c p r\\` — exit · effect · control · path · reason site; \\`b s a q m t\\` were planned and never minted, struck at §A4 V33) |`. Add the letter here if does[] rows carry ids. `d` is free.\n3. **§A2 Slice 11** `:1119-1238` — a new sub-slice block in the house shape: a `**… (as built, Slice 11e).**` paragraph naming the modules touched, the `**Capture** / **Classification** / **Not built** / **Goldens on gustify**` bullets, and the battery case ids. Slice 11c's block at `:1207-1214` is the template to copy.\n4. **§A5** `:1342-1433` — the last ruling is **D28** (`:1429`), so a does[] decision takes **D29**, in the house form `DECISION D29: <subject> / CHOSE: … / ASSUMED: … / BREAKS IF: …`.\n\nOptionally **§A6 Traps** (`:1436+`) if the part discovers a new landing or reading trap.\n\nState words are binding: the envelope vocabulary at `:47` is `switched off` · `not built yet (slice n)` · `needs <unit>` · `absent: <why>` · `partial — <part>: <reason>` · `error: …` · `null`; row-level words elsewhere are `unknown` · `absent` · `beyond one level` (the §A4 state-word sweep, CLAUDE.md:66).\n\n### Step 2 — the ROSTER\n\n**`templates/center/generators/_a3_forms_fe.py`** — add the new frozensets beside the Slice 11 block. A does[] section would sit after `REASON_MEMBERS` (`:39`), e.g. `NAV_CALLS`, `REFETCH_CALLS`, and a `DOES_KINDS` ordering tuple. Data only, flat, `.py` for the generator copy glob. **Never a project name-list** (the file's own header and the `_CACHE_CALLEES` precedent in CLAUDE.md).\n\n### Step 3 — the GENERATOR\n\n**`templates/center/generators/_a3_fe_reason.py`** (291 lines, budget 800 — comfortable):\n\n- `_branch()` `:75-86` → return `(word, does_rows)` or split into `_branch()` + `_does()`. Keep the branch WORD unchanged: F13 `:271`, `:274`, `:279`, `:281` and F14 `:301`, `:308` all assert branch words, and §A4's `reason-collapsed` rule (`_a3_fe_reason.py:278`) gates on `branch in (\"none\", \"value\")`.\n- the site dict `:254-256` gains `does`.\n- `_texts()` `:70-72` is the existing row→strings helper; a classifier will want its sibling for `callee`/`args`.\n- module docstring `:1-29` — the reason part's contract is stated there; extend the \"A status site's `branch` says…\" paragraph.\n- `stats` `:287-289` — any new counter here **breaks `tests/forms-frontend/run.sh:291`** (exact-dict assert).\n\nA separate module (e.g. `_a3_fe_does.py`) is legal and lands on twins as long as some suite `GEN/*.py` imports it at any indentation (`propagate.sh:43`, the §A4 step-8 fix).\n\n### Step 4 — the REGISTRY (only if `does` is a registered PART)\n\n**`templates/center/generators/_a3_forms.py:312`**\n```python\n\"frontend\": {\"slice\": 11, \"parts\": (\"guards\", \"hooks\", \"client\", \"reason\", \"controls\", \"stores\")},\n```\n**`templates/center/generators/_a3_forms_build.py:408`**\n```python\nbuilt = (\"guards\", \"hooks\", \"client\", \"reason\", \"controls\", \"stores\")\n```\nThese two must move **together** — `built` is the hardcoded twin of the registry tuple. Miss one and `arms.frontend.reason` flips from `null` to `partial — does: not built yet (slice 11)` (`:411-413`).\n\n**`_a3_forms.py:147-218` FINDINGS** — only if the part emits a finding. The shape is `{\"arm\": \"frontend\", \"slot\": \"<Kn|Un>\", \"pulse\": \"nag\"|\"count\", \"says\": \"<one reader-side sentence>\"}` (see `reason-collapsed` `:204-205`). Ids must be unique feed-wide (§A1 `:279-283`).\n\n**`_a3_forms_build.py:394`** — the `reason_part(...)` call site; its return arity changes if the part returns a 4th value.\n\n### Step 5 — the EXTRACTOR (probably NOT needed)\n\n**`templates/center/generators/_a3_fe_extract.mjs`** — only if a field is genuinely absent. If it changes, `flow.frozen.json` must be re-blessed (step 6) and `tests/frontend/run.sh` re-run (it proves no `flow` key without the flag, `:18-19`).\n\n### Step 6 — the FIXTURE and the FROZEN CAPTURE\n\n**`tests/forms-frontend/fixture/src/**`** — the reason branches today (`fixture/src/routes/SetupForm.tsx:14-36`) return strings and call `setMapError(…)`. **No navigation, refetch or invalidation lives inside a reason branch.** Proving those does[] classes requires new fixture source.\n\n**`tests/forms-frontend/flow.frozen.json`** (83,361 B) — re-derived by the exact command F16 LIVE runs (`run.sh:378`):\n\n```\nGABE_TS_DIR=<a tree with node_modules/typescript> GABE_FE_FLOW=1 \\\n  node templates/center/generators/_a3_fe_extract.mjs \\\n    tests/forms-frontend/fixture <out.json> tests/forms-frontend/fixture\n```\n\n`run.sh:15-19` auto-discovers a typescript at `/home/khujta/projects/apps/gustify/apps/web` or `/home/khujta/projects/apps/gastify/web`. Without one, F16 SKIPs by name and suite-doctor prints `INFO … SKIPPED coverage` (`suite-doctor.sh:111-115`) — green but **not a full CLEAN**.\n\nNote: `tests/forms-frontend` has its OWN fixture dir deliberately, so `tests/frontend/fixture` counts never move (`run.sh:6-7`, amendment `:1226`).\n\n### Step 7 — the BATTERY\n\n**`tests/forms-frontend/run.sh`** (395 lines). House style, read from the file itself:\n\n- **Case shape** — `py \"<Fn> · <what it proves>; SILENT on <the case that must not fire>\" <<'PY' … PY` (`:26-58`). Each case is a python subprocess with a shared prelude giving `FLOW` · `GRAPH` · `FORMS` · `FORMS_R` · `run(flow)` · `reason(flow, forms)` · `G(fr, name)` · `at(rel, text, nth)`.\n- **`at(rel, text, nth)`** `:51-54` reads the fixture SOURCE for the line — assertions never hardcode line numbers.\n- **FIRE + SILENT in one case** — the naming convention and the asserts both carry it: F5 `:104-108`, F19 `:251-264`, F20 `:175-187`.\n- **Mutation-proof** — deep-copy `FLOW`, rewrite rows via a `json.dumps`/`replace` round-trip, re-run, assert the verdict FLIPS. Two worked examples: F6 `:113-118` (`\"data.wants_p\"` → `\"!data.wants_q\"` flips `unproven` → `exclusive`), F18 `:345-349` (cut the caller's rows + set `truncated` → `called` flips True → False). F14 `:303-315` mutates `FORMS_R` instead of the flow, four ways.\n- **Determinism** — F9 `:189-191` asserts two runs are byte-identical; a does[] with a set-ordered classifier breaks it.\n- **The cases you must EDIT, not just add:**\n  - `:147` — `sorted(a[\"parts\"]) == sorted(F.ARMS[\"frontend\"][\"parts\"])` **and** every part `{present: True, reason: None}`.\n  - `:149`, `:152` — `stats[\"findings\"]` exact dicts.\n  - `:152` — `len(f6[\"frontend\"][\"reasons\"][\"sites\"]) == 14`.\n  - `:291` — `rs == {\"sites\": 14, \"reads_status\": 11, \"reads_detail\": 1, \"reads_code\": 2, \"joined\": 11, \"unknown\": 3, \"endpoints\": 3, \"routed\": 9, \"rest\": 11}` — exact dict, any new stats key fails here.\n  - `:298-301`, `:308` — F14's finding lists, if a finding is added.\n- **Summary line last** — `echo \"forms-frontend: $pass passed, $fail failed, $skip skipped\"` `:394`; a line printed after it never counts (amendment §A6 `:1460`).\n\n**`tests/forms-core/run.sh`** — `:424` asserts `list(a) == list(F.ARM_ORDER)`; `:428` asserts the kinds/short part SETS. Frontend parts are not asserted there, so no change unless you add an arm (you are not).\n\n**`tests/center/run.sh`** — `:450` asserts the arm NAME list exactly (names, not parts — unaffected); `:445-455` is the **\"arms only add\"** law as a `subset()` walk over `off[\"endpoints\"]` vs `on[\"endpoints\"]`; `:411` asserts a raising `extend_frontend` writes no `frontend` key. All three stay green for a frontend-arm-internal change — but they are the ones that would catch a leak.\n\n### Step 8 — the GENERATOR MODULE TABLE\n\n**`templates/center/generators/README.md:60`** — the single mega-row for the forms modules carries a `_a3_fe_reason` clause: *\"(frontend.reason — every status, detail and code comparison a client makes, walked back through hook values, aliases, parameters, object arguments and caught errors to the endpoints its receiver holds, each endpoint's exits routed to the branch that catches them)\"*. Extend that clause. This is an explicit slice exit criterion (`amendment-1.md:316` — \"A `gen/README.md` module-table row\").\n\n### Step 9 — the GOLDENS\n\n**`docs/design/element-forms/goldens/expected.json`** — 62 goldens, 2 of them `\"slice\": \"11c\"`. A golden is hand-entered data:\n\n```json\n{\"id\": \"reason-collapsed-two\", \"slice\": \"11c\", \"mark\": \"V\",\n \"says\": \"two branches show one message for two refusals (SetupScreen 409, cookingSessionModel 409)\",\n \"get\": [\"arm_findings\", \"frontend\", {\"where\": {\"id\": \"reason-collapsed\"}}], \"len\": 2}\n```\n\n`get` steps are a key · an index · `{\"where\":…}` · `{\"where_not\":…}` · `{\"values\":true}` · `{\"pluck\":k}` · `{\"first\":true}` · `{\"sorted\":true}` · `{\"keys\":true}` (`check-goldens.py:27-66`); the comparison is one of `eq` · `len` · `gte` · `has` · `exists` (`:75-94`); `mark` is V verified in source · R read · P projected · U unknown until measured (`:12`).\n\n**Does a new key need a golden? Not mechanically** — `check-goldens.py` iterates only the goldens in the file (`:144`), so an unmentioned key is simply unchecked. But the slice protocol says *\"The slice's goldens reproduced\"* (`amendment-1.md:311`) and the §A2 sub-slice block must carry a **Goldens on gustify — reproduced** paragraph, so in practice you add 2–3, read in gustify source, mark `V`.\n\n**`docs/design/element-forms/goldens/acceptance.html`** is GENERATED, never authored (`render-acceptance.py:1-3`). Its slice→arm label dict is hardcoded at `:8-10` — a new slice label (`\"11e\"`) needs a row there or the section renders with an empty arm name.\n\n**`docs/design/element-forms/review-a4.md`** — `render-acceptance.py:35-38` splices the text between `## Landed` and `## Refuted` into the sheet as a fix ledger. Only touch it if the part fixes a recorded §A4 finding.\n\n### Step 10 — CLAUDE.md\n\n**`/home/khujta/projects/gabe_lens/CLAUDE.md:66`** — the element-forms bullet. The house pattern is one sentence per sub-slice appended in order; Slice 11c's sentence is the template:\n\n> *\"Slice 11c: the `reason` part (`_a3_fe_reason`) — every status, detail and code comparison a client makes, walked back three hops (…) to the endpoints its receiver can hold, and each endpoint's exits routed to the first branch that covers them, with the findings `reason-collapsed` … · `branch-unproduced` · `client-detail-unmatched`.\"*\n\nName: the part, its module, what it writes, its finding(s), its battery. **No CLAUDE.md capability-table row changes and no skill version bump** — no skill file changes (see §2).\n\n### Step 11 — INSTALL + DOCTOR\n\n`./install.sh` then `scripts/suite-doctor.sh`. Mandatory: `install.sh:107-118` copies `templates/*/` into `~/.claude/templates/gabe/<sub>/`, and `suite-doctor.sh:76-85` hashes every repo template against its install **in both directions** — an un-installed generator edit reports DRIFT.\n\n### Step 12 — DRY RUN\n\n`scripts/forms-dryrun.sh --arms frontend <target…>` — the slice protocol requires it on gustify · gastify · tier3 · keypro, **serially** (`amendment-1.md:304`), with the numbers in the commit message (`:305`).\n\n### Step 13 — PROPAGATE to the twins\n\n`templates/center/generators/propagate.sh <twin-root> [--check]`. Update-only for existing generators (`:25-32`); a NEW `.py` imported by any suite `GEN/*.py` at any indentation is landed as `NEW, required by …` (`:38-48`, the grep at `:43` is `^\\s*(import|from) ${mod}\\b`); a NEW `.mjs` is never landed (`:47-49`). A real run then executes `<twin>/scripts/refresh_center.sh regen` (`:91`).\n\n---\n\n## 2 · What does NOT change (proven, not assumed)\n\n| surface | why it stays put | anchor |\n|---|---|---|\n| `skills/gabe-pulse/scripts/form_drift.py` | `NAG = (\"shared-status\",\"reason-lost\",\"escape-500\",\"http-swallowed\")`, `COUNT = (\"text-only\",\"undeclared\",\"declared-unproduced\")` — zero frontend findings. Uptake deferred (D16, `amendment-1.md:1369-1372`). | `form_drift.py:37-38` |\n| `skills/gabe-map/scripts/mapquery.py` | `form_summary` keeps a fixed `keep` tuple over endpoint rows; the string `frontend` appears nowhere in the file. | `mapquery.py:659-673` |\n| `tests/pulse-angles/run.sh` S20 | Asserts only endpoint-arm finding text. | `:465-479` |\n| `tests/gabe-map/run.sh` | grep for `forms` returns nothing. | — |\n| `templates/center/shell/**` (the Gabe Universe, board, etc.) | Phase 6 station surfaces wait on the generation (CLAUDE.md:66, `amendment-1.md:1345`). Nothing renders `frontend{}`. | — |\n| `templates/center/shell/example/codebase-graph-station/forms.json` | Committed ARMS-OFF build (no `arms` key); `regen-example.sh:38` pins `GABE_FORMS_ARMS=none`. | verified by reading the file's top keys |\n| `tests/element-forms/run.sh` (C0–C13) | *\"never edited\"* | `amendment-1.md:308` |\n| CLAUDE.md capability table + `README.md` skill count | No skill file changes → version parity (`suite-doctor.sh:117-129`) and count parity (`:159-164`) unaffected. | — |\n\n---\n\n## 3 · The suite-doctor invariants a generator change can trip\n\n`scripts/suite-doctor.sh` — exit 0 CLEAN, exit 1 DRIFT:\n\n| invariant | what trips it | anchor |\n|---|---|---|\n| **install parity (templates)** | editing a generator without `./install.sh`; also a stale file left only in `~/.claude` | `:76-85` |\n| **battery sweep** | every zero-arg `tests/*/run.sh` must exit 0; a non-zero is `harness FAILING`. This is where your new battery case is enforced the day it lands. | `:101-116` |\n| **SKIPPED-coverage INFO** | F16 LIVE skipping (no typescript) prints `INFO … passed but SKIPPED coverage` — green, **explicitly not a full CLEAN** | `:111-115` |\n| **portability lint** | the literal operator path `/home/khujta` inside `skills/ templates/ scripts/ prompts/ schemas/ install.sh`. `tests/` is NOT scanned, so a fixture is safe; a generator or `forms-dryrun.sh` default is not. | `:165-172` |\n| **version / count parity** | only if a skill file or a `(N skills)` claim moves — neither does here | `:117-129`, `:159-164` |\n| **docsite staleness** | `scripts/checkers/docsite-staleness.sh`; only if a published docs page cites a changed source | `:188-194` |\n| **archived-skill lint, MCP registration** | untouched | `:180-198` |\n\nEscape hatch for a parity-only check: `GABE_DOCTOR_NO_BATTERIES=1 bash scripts/suite-doctor.sh` — it says out loud it is **not** a full CLEAN (`:99-100`, `:203-206`).\n\n---\n\n## 4 · Verification steps, in order, with command and cost\n\n| # | command | cost | what it proves |\n|---|---|---|---|\n| 1 | `bash tests/forms-frontend/run.sh` | **not stated** in the battery header or CLAUDE.md. ~17 python subprocesses + 2 node extractor runs (F16). The node runs dominate and SKIP without typescript. | the part FIREs and stays SILENT; mutants flip; determinism (F9); the parts/stats/findings dicts |\n| 2 | `bash tests/forms-core/run.sh` | not stated (AST-only, hermetic — seconds class, like `tests/register` <1 s) | the arm registry, closure, envelope words survive |\n| 3 | `bash tests/frontend/run.sh` | not stated; runs the extractor, SKIPs without typescript | no `flow` key without `GABE_FE_FLOW=1`; the frozen structure capture re-derives |\n| 4 | `bash tests/center/run.sh` | not stated; runs several **full center builds** over a fixture — among the heaviest in the sweep | \"arms only add\"; a raising `extend_frontend` exits 0 and writes no `frontend` |\n| 5 | `python3 docs/design/element-forms/goldens/check-goldens.py <forms.json> [--slice 11e] [--only <id>] [--quiet]` | read-only, seconds | every golden MATCH / MISMATCH / MISSING; exit 1 on either |\n| 6 | `python3 docs/design/element-forms/goldens/render-acceptance.py docs/design/element-forms/goldens/expected.json <forms.json> docs/design/element-forms/goldens/acceptance.html docs/design/element-forms/review-a4.md` | seconds | regenerates the operator's acceptance sheet |\n| 7 | `./install.sh` | **≈5 s** (CLAUDE.md) | the edited generators reach `~/.claude/templates/gabe/center/generators/` |\n| 8 | `bash scripts/suite-doctor.sh` | **≈5–6 min** — 46 batteries, 2,666 assertions (CLAUDE.md). Eight batteries launch a headless browser. | install parity + every invariant + the whole sweep |\n| 8b | `GABE_DOCTOR_NO_BATTERIES=1 bash scripts/suite-doctor.sh` | seconds | parity + invariants only — says out loud it is not a full CLEAN |\n| 9 | `bash scripts/forms-dryrun.sh --arms frontend gustify` (then `gastify`, `tier3`, `keypro`) | **every target builds TWICE** — gustify ≈1 min a build, tier3 ≈2 (`forms-dryrun.sh:11`). **Run ALONE.** | arms-off byte-identical vs the blessed baseline; arms-on moves **only** `forms.json`; one JSON line per target with each arm's present/reason/stats/bytes and wall seconds |\n| 10 | `bash templates/center/generators/propagate.sh <twin-root> --check` | seconds (writes nothing) | which twin files drift; a NEW required generator is named |\n| 11 | `bash templates/center/generators/propagate.sh <twin-root>` | **minutes** — it runs `<twin>/scripts/refresh_center.sh regen` at `:91` | the twin regenerates from its own data |\n\n**Serial only.** `amendment-1.md:1474` and the machine rule both state it: one heavy job at a time on this WSL2 box; check `df -h /mnt/c` and `du -sh /var/log` before copying gustify or tier3 into scratch.\n\n---\n\n## 5 · Landing-order summary (one line each)\n\n1. `docs/design/element-forms/amendment-1.md` — §A1 arms row `:76` + id letters `:115`, §A2 Slice 11 sub-slice block, §A5 **D29**, §A6 trap if any.\n2. `templates/center/generators/_a3_forms_fe.py` — the new idiom frozensets (nav-hook, refetch), beside `REASON_MEMBERS:39`.\n3. `templates/center/generators/_a3_fe_reason.py` — `_branch()` `:75-86` keeps `inside` as `does[]`; site dict `:254`; stats `:287`; docstring `:1-29`.\n4. *(only if a registered part)* `_a3_forms.py:312` **and** `_a3_forms_build.py:408` together; *(only if a finding)* `_a3_forms.py:147-218`; `_a3_forms_build.py:394` if the return arity moves.\n5. *(only if a field is missing)* `_a3_fe_extract.mjs`.\n6. `tests/forms-frontend/fixture/src/**` + `tests/forms-frontend/flow.frozen.json` re-blessed via the F16 command (`run.sh:378`).\n7. `tests/forms-frontend/run.sh` — new FIRE+SILENT+mutant case, and EDIT `:147`, `:149`, `:152`, `:291`, `:298-301`, `:308`.\n8. `templates/center/generators/README.md:60` — extend the `_a3_fe_reason` clause.\n9. `docs/design/element-forms/goldens/expected.json` (+ `render-acceptance.py:8-10` if a new slice label), regenerate `acceptance.html`.\n10. `CLAUDE.md:66` — one sentence in the element-forms bullet.\n11. `./install.sh` → `scripts/suite-doctor.sh`.\n12. `scripts/forms-dryrun.sh --arms frontend` on all four targets, serially; numbers into the commit message.\n13. `propagate.sh <twin> --check`, then the real run, per twin.\n\n**Commit discipline** (`amendment-1.md:306`, `:1473`): commit by explicit path, `git log -1` before and after — a concurrent session's `git add -A` has swallowed a commit on this branch before.\n"
  },
  {
   "key_facts": [
    "61 reason sites total: gustify 12 · gastify 3 · tier3 45 · keypro 1; 54 status sites (with a branch word) + 7 detail/code sites (no branch word).",
    "Branch words today: none 31 · value 15 · passed 3 · reads 3 · unread 2 — 'none' is the majority and is almost always a lie.",
    "Of 18 'none' arms opened, 17 DO something: 9 throws, 7 fallback returns, 2 sign-outs, 2 navigations, plus toasts, setStates, a retry loop and a yield.",
    "Biggest idiom class is THROWS (9): bare Error or a typed subclass (RedirectError, RateLimitedError, ApprovalConflictError, PaymentMethodRequiredError) — tier3/web/src/lib/fetcher.ts:84, app/craft/services/apiServices.ts:436/865/888, lib/billing/svc.ts:85.",
    "Second is RETURNS A FALLBACK VALUE (7): return null · undefined as T · new Response(\"\") · bare return — e.g. tier3 lib/indexing/svc.ts:162 treats a 503 as success, gastify web/src/hooks/useTransactionDraft.ts:25 returns null on 404.",
    "Message keys are everywhere (7): returned (gustify cookingSessionModel.ts:381 `return \"cooking.error.start_allergen\"`), or assigned then toasted (tier3 lib/auth/components.tsx:370 → toast.error(errorMsg) at :377).",
    "'value' (15 sites) means the doing is ONE HOP away at the consumer — tier3 lib/fetcher.ts:39 → skipRetryOnAuthError:56 suppresses the SWR retry; providers/SettingsProvider.tsx:23 → renders <ErrorPage/> at :26.",
    "'unread' (2 sites) is a false alarm: useChatSessionController.ts:220/222 is a nested ternary tripping the len(pred)>=120 clip at _a3_fe_reason.py:80; it maps 404→\"not_found\" then calls setSessionFetchError.",
    "CACHE INVALIDATION AND REFETCH: not found in any reason branch in the four apps — they live in mutation onSettled/onSuccess and in effects on domain state, never on an HTTP status. Those classes must be allowed to be honestly empty.",
    "The only true retry is tier3 app/craft/services/apiServices.ts:272 — `await new Promise(r => setTimeout(r, retryDelayMs)); continue;` on a 409 restore-lock.",
    "TRAP: tier3's toast is NOT sonner — it is the in-repo design-system store web/lib/opal/src/layouts/toast/store.ts, imported from \"@opal/layouts\" in 135 files; tier3/web/package.json has no sonner/react-hot-toast. Roster the SHAPE (toast.<level>(msg)), not the package.",
    "TRAP: `t(...)` means three different things — next-intl useTranslations (tier3, ^4.13.7), gustify's own useT() (apps/web/src/i18n/useT.ts:18), gastify's translate() (web/src/lib/i18n.ts:2598). Key the roster on the BINDER, not the name.",
    "LIBRARY idioms confirmed present: NextResponse.redirect (next/server, tier3 auth/oauth/callback/route.ts:37), router.push (next/navigation, connector page:170), react-router useNavigate/<Navigate> (gustify), queryClient.setQueryData (TanStack), SWR onErrorRetry, window.location.href/.reload().",
    "PROJECT-LOCAL names that must NOT enter a roster: signOut · pushToast · writeModesCache · setRollbackNotice (gustify) · translate · uploadFailed (gastify) · logout · readError · errorDetail · authErrorRedirect · finishConnectorDeletion · writeExportIdToUrl · setSessionFetchError (tier3) · isUniqueViolation (keypro).",
    "Extractor row-kind gaps (_a3_fe_extract.mjs:378-466, kinds jsx·call·new·cmp·ret·throw·let): NO yield row (tier3 app/app/services/lib.tsx:259 yields a StreamingError packet), NO assignment row (errorMsg = t(...) survives only as the t call), NO continue/break row (the retry loop reads as a lone setTimeout).",
    "What the rows ALREADY give does[]: throw rows carry value+callee; ret rows separate bare `return;` (value:null) from `return null` (null:true) and carry jsx/tags and obj; call rows carry callee + 3 literal args + props/opts/binds/refs; ctx already names try · catch:e · loop · callback:NAME · prop:CALLEE.KEY.",
    "3 of 61 sites are not API reasons at all: gastify useScanToDraft.ts:67 (.some over scan signals), gustify recipeFacetOptions.ts:167 (taxonomy .filter), keypro postgres-user-repository.ts:169 (PostgreSQL SQLSTATE 23505 in a BACKEND adapter typed as a fe: piece).",
    "Negated polarity is real: tier3 lib/settings/svcSS.ts:99/100/114 compare `!== 403`/`!== 401`, so the rows under the guard are the THROW and the silent 403/401 arm is invisible to _branch's text match.",
    "A default-object substitution exists: tier3 lib/settings/svcSS.ts:68 replaces a 401/403 settings fetch with an 11-field Settings literal — a distinct class from 'returns null'.",
    "The literal ARGUMENT is the readable fact more than the callee: toast.error(t(\"toasts.alreadyRunning.message\")) · throw new RateLimitedError(body.detail || \"You've reached your usage limit.\") · router.push(\"/admin/indexing/status\")."
   ],
   "open_questions": [],
   "report": "## What client reason-branches really do — four study apps, read-only survey\n\n### 0. The feed as it stands\n\n`frontend.reasons.sites[]` totals **61 sites**: gustify 12 · gastify 3 · tier3 45 · keypro 1.\n54 are `status` sites (they carry a `branch` word), 7 are `detail`/`code` sites (no `branch` word at all — printed `-` below).\n\nBranch-word tally today:\n\n| app | none | value | passed | reads | unread | (no word: detail/code) |\n|---|---|---|---|---|---|---|\n| gustify | 5 | 5 | 0 | 0 | 0 | 2 |\n| gastify | 1 | 0 | 0 | 0 | 0 | 2 |\n| tier3 | 25 | 10 | 3 | 3 | 2 | 2 |\n| keypro | 0 | 0 | 0 | 0 | 0 | 1 |\n\n`none` is 31 of 54 — and, as the evidence below shows, **almost every `none` branch does something**. `none` currently means only \"no `.detail`/`.code` read, and the receiver was not handed to a call\"; it does not mean the branch is empty.\n\nThe classifier is `_branch()` at `/home/khujta/projects/gabe_lens/templates/center/generators/_a3_fe_reason.py:75-86` — it already computes `inside` (the rows under the branch's guard) and then discards them for one word.\n\n---\n\n### 1. Opened sites — what the code inside the branch actually does\n\n**GUSTIFY** (`/home/khujta/projects/apps/gustify`)\n\n| # | file:line | word | code | idiom class |\n|---|---|---|---|---|\n| 1 | `apps/web/src/features/auth/SetupScreen.tsx:43` | `none` | `if (error.status === 409) return t(\"auth.setup.error.in_progress\");` | **message-key** (returned, i18n call) |\n| 2 | `apps/web/src/features/cooking/cookingSessionModel.ts:381` | `none` | `if (error.status === 403) {`<br>`  return \"cooking.error.start_allergen\";` | **message-key** (returned, bare literal) |\n| 3 | `…/cookingSessionModel.ts:384` | `none` | `return \"cooking.error.start_not_found\";` | **message-key** |\n| 4 | `…/cookingSessionModel.ts:389` | `none` | `return \"cooking.error.start_concurrent_cap\";` | **message-key** |\n| 5 | `apps/web/src/features/cooking/components/GustifyGenerateView.tsx:442` | `-` | `if (error?.code === \"insufficient_credits\") {`<br>`  return (<div … data-cooking-generate-credits-error=\"true\"> … <p>{t(\"cooking.generate.no_credits_title\")}</p> …` | **renders element** (+ message-keys) |\n| 6 | `apps/web/src/features/cooking/useRecipeFilterModes.ts:235` | `value` | `if (context) writeModesCache(queryClient, (modes) => modes.filter(…));`<br>`setRollbackNotice(modeRollbackNotice(\"create\", { atSlotCap: isApiError(error) && error.status === 409 }));`<br>`console.warn(\"[recipe-filter-modes] create failed — optimistic row rolled back\", error);` | **cache rollback** + **setState** + **log** (inside `useMutation` `onError`) |\n| 7 | `apps/web/src/features/settings/SettingsContainer.tsx:202-203` | `value` ×2 | `const settingsExpired = isApiError(settingsQuery.error) && settingsQuery.error.status === 401;` → drives `{sessionExpired ? t(\"auth.action.back_to_signin\") : t(\"auth.action.retry\")}` inside a returned `<ProfileChrome><EmptyState …><Button onClick={() => window.location.reload()}>` | **predicate → renders element + message-key + reload** |\n| 8 | `apps/web/src/lib/api/errors.ts:73` | `value` ×2 | `return isApiError(value) && value.status >= 400 && value.status < 500;` (`isClientError`; the doc-comment says \"used by query/client.ts so the retry policy skips client (4xx) errors\") | **predicate → retry policy** |\n| 9 | `apps/web/src/routes/RequireSetup.tsx:38` | `none` | `useEffect(() => {`<br>`  if (isError && isApiError(error) && error.status === 401) { void signOut(); }`<br>`  else if (isError) { pushToast({ tone: \"info\", message: t(\"account.error.reload_retry\") }); }` | **sign-out** (+ else-arm **notify**), inside `useEffect` |\n\n**GASTIFY** (`/home/khujta/projects/apps/gastify`)\n\n| # | file:line | word | code | idiom class |\n|---|---|---|---|---|\n| 10 | `web/src/hooks/useTransactionDraft.ts:25` | `none` | `if (response.status === 404) return null;` (inside a TanStack `queryFn`) | **returns fallback value** (swallow) |\n| 11 | `web/src/hooks/useScanUpload.ts:27` | `-` | predicate `detail.code === \"SCAN_QUOTA_EXHAUSTED\"`; consumer at `:69-75`: `uploadFailed(detail.message ?? translate(\"txnNew.scanQuotaExhaustedFallback\"), { code: \"quota_exhausted\", used: detail.used, limit: detail.limit }); return;` | **predicate → store action + message-key** |\n| 12 | `web/src/hooks/useScanToDraft.ts:67` | `-` | `return signals.some((s) => s.code === \"synthesized_service_item\" && s.severity === \"needs_review\");` | **NOT a response reason** (array element in `.some()`) |\n\n**TIER3 / onyx** (`/home/khujta/projects/repo-study/tier3`)\n\n| # | file:line | word | code | idiom class |\n|---|---|---|---|---|\n| 13 | `web/src/lib/fetcher.ts:84` | `none` | `if (res.status === 403) { const redirect = new RedirectError(DEFAULT_AUTH_ERROR_MSG, res.status, await res.json()); throw redirect; }` | **throws typed error** |\n| 14 | `web/src/lib/fetcher.ts:39` / `:44` | `value` ×4 | predicates `isAuthStatusError` / `isNotFoundError`; consumer `skipRetryOnAuthError:56` → `if (isAuthStatusError(error)) return;` (SWR `onErrorRetry`) | **predicate → suppresses retry** |\n| 15 | `web/src/app/craft/services/apiServices.ts:436` | `reads` | `if (body?.error_code === RATE_LIMITED_ERROR_CODE) { throw new RateLimitedError(body.detail \\|\\| \"You've reached your usage limit.\", { scope, reset_at, retry_after_seconds }); }`<br>`throw new Error(body?.detail \\|\\| \\`Failed to create turn: ${res.status}\\`);` | **reads code → throws typed error with fields** |\n| 16 | `…/apiServices.ts:483` | `none` ×2 | `if (res.status === 404 \\|\\| res.status === 409) { return null; }` | **returns fallback value** |\n| 17 | `…/apiServices.ts:515` | `none` | `if (res.status === 409) { return new Response(\"\"); }` | **returns synthesized value** |\n| 18 | `…/apiServices.ts:865` and `:888` | `reads` ×2 | `const body = (await res.json().catch(() => ({}))) as { detail?: string };`<br>`throw new ApprovalConflictError(body.detail ?? \"decision conflict\");` | **reads detail → throws typed error** |\n| 19 | `…/apiServices.ts:272` | `none` | `if (res.status === 409 && attempt < maxRetries) {`<br>`  await new Promise((resolve) => setTimeout(resolve, retryDelayMs));`<br>`  continue;` | **retries the call** (the only true retry in 61 sites) |\n| 20 | `web/src/app/craft/v1/tasks/api.ts:68` | `passed` | `if (!res.ok && res.status !== 204) { await readError(res, \"Failed to delete scheduled task\"); }` | **passes receiver to a helper that throws** |\n| 21 | `web/src/app/auth/oauth/callback/route.ts:37` (twin `oidc/callback/route.ts:18`) | `none` | `if (response.status === 401) { return NextResponse.redirect(new URL(\"/auth/create-account\", getDomain(request))); }` | **navigates** (library) |\n| 22 | `…/oauth/callback/route.ts:45` (twin `oidc:26`) | `passed` | `const errorRedirect = await authErrorRedirect(request, response);`<br>`for (const cookie of response.headers.getSetCookie()) errorRedirect.headers.append(\"set-cookie\", cookie);`<br>`return errorRedirect;` | **navigates via project helper** |\n| 23 | `web/src/hooks/useChatSessionController.ts:220`/`:222` | `unread` ×2 | `const type = response.status === 404 ? \"not_found\" : response.status === 403 ? \"access_denied\" : \"unknown\";`<br>`setSessionFetchError({ type, detail });` | **maps to a reason constant + setState** (the \"clipped condition\" was just a nested ternary) |\n| 24 | `web/src/lib/skills/api.ts:39` | `none` | `if (res.status === 204) { return undefined as T; }` | **returns fallback value** |\n| 25 | `web/src/lib/auth/hooks.ts:99` | `none` | `const sessionEnded = !inAuthFlow && userError?.status === 403 && hasSeenAuthenticatedUser;` → latches `parseSessionEndReason(userError)` into state | **predicate → setState (reason latch)** |\n| 26 | `web/src/lib/auth/hooks.ts:136` | `none` | `useEffect(() => { … if (userError?.status === 403 && hasSeenAuthenticatedUser) { logout(); } }, …)` | **sign-out**, inside `useEffect` |\n| 27 | `web/src/lib/auth/components.tsx:370` | `none` | `if (response.status === 429) { errorMsg = t(\"emailPasswordForm.tooManyRequests.error\"); }`<br>… `toast.error(errorMsg); return;` (`:377`) | **message-key → toast** |\n| 28 | `web/src/lib/auth/components.tsx:412` | `none` | same shape on `loginResponse.status === 429`; `toast.error(errorMsg)` at `:423` | **message-key → toast** |\n| 29 | `web/src/lib/billing/svc.ts:85` | `none` | `if (response.status === 402) { throw new PaymentMethodRequiredError(detail); }` | **throws typed error** |\n| 30 | `web/src/lib/indexing/svc.ts:162` | `none` | `if (response.status === 503) { return; }` (doc: \"a 503 … is treated as success\") | **bare return (deliberate swallow)** |\n| 31 | `web/src/lib/languageModels/costOverrides.ts:92` | `none` | `if (response.ok \\|\\| response.status === 404) { return; }` | **bare return (swallow)** |\n| 32 | `web/src/lib/settings/svcSS.ts:68` | `value` ×2 | `if (settingsResponse.status === 403 \\|\\| settingsResponse.status === 401) { settings = { auto_scroll: true, application_status: ApplicationStatus.ACTIVE, … 11 fields }; }` | **substitutes a default object** |\n| 33 | `…/svcSS.ts:99`/`:100`/`:114` | `none` ×3 | `else if (enterpriseResponse.status !== 403 && … !== 401) { throw new Error(\\`fetchSettingsSS: enterprise failed status=… body=…\\`); }` | **throws** (negated polarity: the 403/401 arm is the silent one) |\n| 34 | `web/src/app/ee/admin/export-logs/page.tsx:176-177` | `value` ×2 | `if (isTerminal4xx) { if (status !== undefined) toast.error(t(\"toasts.lostAccess.message\")); }` … `writeExportIdToUrl(null); setExportId(null);` (inside `useEffect`, after `console.error`) | **toast + URL write + setState** |\n| 35 | `web/src/app/ee/admin/export-logs/page.tsx:274` | `none` | `if (response.status === 429) { toast.error(t(\"toasts.alreadyRunning.message\")); return; }` | **toast + message-key** |\n| 36 | `web/src/app/craft/components/OpencodeDebugLogs.tsx:161`/`:163` | `none` ×2 | `setStatus(\"error\");`<br>`setErrorMessage(response.status === 404 ? t(\"error.endpointDisabled\") : response.status === 409 ? t(\"error.noSandbox\") : t(\"error.http\", { status: response.status }));` | **setState ×2 + message-key** (ternary arm, not a block) |\n| 37 | `web/src/app/app/services/lib.tsx:243` | `none` | `if (response.status === 429 && data.error_code === RATE_LIMITED_ERROR_CODE) { const streamingError: StreamingError = { error: data.detail ?? \"You've reached your usage limit.\", error_code: …, is_retryable: false, details: rateLimitDetails };`<br>`  yield streamingError; return; }` | **yields a structured packet** |\n| 38 | `web/src/app/admin/discord-bot/hooks.ts:20` | `none` | `if (res.status === 403) { // managed externally`<br>`  return null; }` (an SWR fetcher) | **returns fallback value** |\n| 39 | `web/src/app/admin/connector/[ccPairId]/page.tsx:249` | `none` | `if ((hasLoadedOnce && ccPairError?.status === 404) \\|\\| …) { finishConnectorDeletion(); }` → defined `:170` as `router.push(\"/admin/indexing/status\")` | **navigates, one hop away** |\n| 40 | `web/src/app/ee/agents/stats/[id]/AgentStats.tsx:87` | `none` | `if (res.status === 403) { throw new Error(t(\"agentStats.permissionDenied.message\")); }` → the enclosing catch does `setError(err.message)` | **throws message-key → setState** |\n| 41 | `web/src/app/auth/forgot-password/utils.ts:40` (twin `web/src/lib/auth/svc.ts:90`) | `-` ×2 | `if (error?.detail?.code === \"RESET_PASSWORD_INVALID_PASSWORD\") { throw new Error(error.detail.reason \\|\\| messages.invalidPassword); }` | **throws, detail-derived message** |\n| 42 | `web/src/providers/SettingsProvider.tsx:23` | `value` ×2 | `const isAuthError = (err) => err instanceof FetchError && (err.status === 401 \\|\\| err.status === 403);` → `:25` `if (error && !isAuthError(error)) return NEXT_PUBLIC_CLOUD_ENABLED ? <CloudError /> : <ErrorPage />;` | **predicate → renders element** |\n\n**KEYPRO** (`/home/khujta/projects/repo-study/others/keypro-front`)\n\n| # | file:line | word | code | idiom class |\n|---|---|---|---|---|\n| 43 | `src/adapters/postgres/postgres-user-repository.ts:169` | `-` | `return typeof error === \"object\" && … error.code === \"23505\";` → `duplicateOutcome:164` returns `\"duplicate-email\"` / `\"duplicate-username\"` | **NOT a response reason** (PostgreSQL SQLSTATE, in a backend adapter file the fe arm typed as a `fe:` piece) |\n\nPlus `apps/web/src/features/cooking/model/recipeFacetOptions.ts:167` (gustify, `-`): `.filter((value) => value.group === \"region\" || value.code === \"fusion\")` — a taxonomy list filter, **not a response reason**.\n\n---\n\n### 2. Tally of idiom classes (over the 43 opened branch-arms)\n\n| class | count | where |\n|---|---|---|\n| **throws** (bare `Error` or a typed subclass) | 9 | fetcher.ts:84 · apiServices:436/865/888 · billing:85 · svcSS:99/100/114 · AgentStats:87 · forgot-password:40 · auth/svc:90 |\n| **returns a fallback value** (`null`, `undefined`, `new Response(\"\")`, bare `return`) | 7 | useTransactionDraft:25 · apiServices:483/515 · skills/api:39 · discord-bot/hooks:20 · indexing/svc:162 · costOverrides:92 |\n| **predicate only — the doing is at the caller** | 7 | errors.ts:73 · fetcher:39/44 · SettingsProvider:23 · useScanUpload:27 · auth/hooks:99 · keypro:169 |\n| **setState / store action** | 7 | useChatSessionController:220/222 · OpencodeDebugLogs:161/163 · export-logs:176 · AgentStats (catch) · useRecipeFilterModes:235 · useScanUpload consumer |\n| **message key returned or assigned** (i18n call or literal key) | 7 | SetupScreen:43 · cookingSessionModel:381/384/389 · components.tsx:370/412 · export-logs:274 · OpencodeDebugLogs:161/163 |\n| **notify / toast** | 5 | components.tsx:377/423 · export-logs:182/275 · RequireSetup:41 (`pushToast`) |\n| **navigates** | 5 | oauth/callback:37 · oidc/callback:18 · oauth:45 · oidc:26 · connector page:249 (one hop) |\n| **renders an element** | 3 | GustifyGenerateView:442 · SettingsContainer:205 · SettingsProvider:26 |\n| **logs** | 4 | useRecipeFilterModes:236 · export-logs:171 · indexing/svc:171 · HooksPage:322 |\n| **signs out** | 2 | RequireSetup:39 (`signOut`) · auth/hooks:137 (`logout`) |\n| **passes the receiver to a helper** | 2 | tasks/api:68 (`readError`) · apiServices:452 (`errorDetail`) |\n| **retries the call** | 1 | apiServices:272 (`setTimeout` + `continue`) |\n| **suppresses retry** (policy) | 2 | fetcher:56 (SWR `onErrorRetry`) · errors.ts:73 (TanStack retry policy) |\n| **cache write / rollback** | 1 | useRecipeFilterModes:234 (`writeModesCache` → `queryClient.setQueryData`) |\n| **substitutes a default object** | 1 | svcSS:68 (11-field `Settings` literal) |\n| **yields a structured packet** | 1 | services/lib.tsx:259 (`yield streamingError`) |\n| **nothing / not a response reason** | 3 | useScanToDraft:67 · recipeFacetOptions:167 · keypro:169 |\n\n**Cache invalidation and refetch: NOT FOUND in any reason branch across the four apps.** A grep for `refetch(` · `invalidateQueries` · `mutate(` within four lines of every status/detail/code comparison returns only: `useRecipeFilterModes` (the rollback above), and hits in `onSettled`/`onSuccess` mutation callbacks and effects keyed on *domain* state, never on an HTTP status (`useCookingSessions.ts:204`, `GustifyGenerateSheet.tsx:77`, `craft/v1/tasks/[id]/page.tsx:66`, `connector page:167`). The `refetch`/`invalidate` classes must be allowed to come out honestly empty.\n\n---\n\n### 3. Callee names per class — LIBRARY vs project-local\n\n**LIBRARY idioms (safe roster entries; each is package- or platform-backed):**\n\n| class | idiom | library / cite |\n|---|---|---|\n| navigate | `NextResponse.redirect(new URL(...))` | `next/server` (tier3 `web/package.json:82` next 16.3.3; keypro next 16.2.10) |\n| navigate | `router.push` / `router.replace` (from `useRouter`) | `next/navigation` |\n| navigate | `navigate(...)` (from `useNavigate`), `<Navigate to>` | `react-router-dom` (gustify `^7.18.1`) — already in `FF.NAV_ELEMENTS` |\n| navigate | `window.location.href = …`, `window.location.assign`, `window.location.reload()` | platform |\n| notify | `toast.error` / `.success` / `.warning` / `.info` / `toast(...)` | **API SHAPE of sonner / react-hot-toast** — see the trap below |\n| cache | `queryClient.setQueryData` · `setQueriesData` · `invalidateQueries` · `refetchQueries` · `resetQueries` · `removeQueries` · `cancelQueries` | TanStack Query (already `FF.INVALIDATE_CALLS` / `SEED_CALLS` / `CACHE_WRITE_CALLS`) |\n| refetch | `refetch()` (query member) · `mutate(key)` (SWR revalidation) | TanStack Query · SWR (tier3 `swr ^2.1.5`) |\n| retry policy | an early `return` inside an SWR `onErrorRetry` callback | SWR |\n| message | `useTranslations(ns)` → the bound `t(key, params?)` | **next-intl** (tier3 `^4.13.7`, e.g. `lib/auth/components.tsx:4,291`) |\n| message | `useTranslation()` → `t(key)` | react-i18next (**not present** in these four apps) |\n| setState | a setter bound from `useState` (`const [x, setX] = useState(…)`) | React — detect by the `binds` pair, not by a `set*` name list |\n| log | `console.error` / `.warn` / `.log` | platform |\n| throw | `throw` + `new <Name>Error(...)` | language; record the class name as data, never roster it |\n| retry | `await new Promise(r => setTimeout(r, ms))` + `continue` | plain JS shape |\n\n**Two traps for the roster:**\n\n1. **tier3's `toast` is NOT a library.** It is defined in the monorepo's own design-system package, `web/lib/opal/src/layouts/toast/store.ts` (a `useSyncExternalStore` module store), imported as `import { toast } from \"@opal/layouts\"` — 135 files. tier3's `web/package.json` has **no** `sonner`, `react-hot-toast` or `react-toastify` dependency. So a package-import test finds zero toasts in the largest app; a *shape* test (`<ident>.<level>(msg)` where `<ident>` is named `toast`, plus the level roster) finds all of them and also matches sonner/react-hot-toast. Roster the shape, and record the import source per site so the page can say which it was.\n2. **`t(...)` is a one-letter local name in every app but means three different things.** tier3 = next-intl (`useTranslations`). gustify = its own `useT()` over `shared/locale/es-CL.json` (`apps/web/src/i18n/useT.ts:18`). gastify = `translate(key)` from `web/src/lib/i18n.ts:2598`. The roster cannot key on `t`; key on the BINDER (`useTranslations` / `useTranslation` → library; anything else → project) and still record the literal key, which is the readable fact either way.\n\n**Project-local names that must NOT enter any roster** (each appears in exactly one app):\n\n- gustify — `signOut`, `pushToast`, `writeModesCache`, `modeRollbackNotice`, `setRollbackNotice`, `isApiError`, `isClientError`, `useT`, `mockupIcon`\n- gastify — `translate`, `uploadFailed`, `uploadComplete`, `startUpload`, `isQuotaExhaustedDetail`\n- tier3 — `logout`, `readError`, `errorDetail`, `authErrorRedirect`, `finishConnectorDeletion`, `writeExportIdToUrl`, `setSessionFetchError`, `parseSessionEndReason`, `isAuthStatusError`, `isNotFoundError`, and the error classes `RedirectError`, `FetchError`, `RateLimitedError`, `ApprovalConflictError`, `PaymentMethodRequiredError`, plus the packet type `StreamingError`\n- keypro — `isUniqueViolation`, `constraintOf`, `duplicateOutcome`\n\n---\n\n### 4. What this means for `does[]` (facts, not design)\n\n- **`none` is the word to kill first.** 31 of 54 status sites carry it; of the 18 `none` arms I opened, **17 do something** (9 throws, 5 fallback returns, 2 sign-outs, 2 navigations, toasts, setStates, a retry loop, a yield). The only genuinely empty-ish ones are the deliberate swallows (`indexing/svc.ts:162`, `costOverrides.ts:92`) — and even those are a fact worth naming (\"503 treated as success\").\n- **`value` (15 sites) is one hop short.** The comparison sits in a predicate body, a `const`, or a ternary arm; the doing is at the consumer — `fetcher.ts:39` → `skipRetryOnAuthError:56`; `errors.ts:73` → the query retry policy; `SettingsProvider.tsx:23` → `<ErrorPage />`; `OpencodeDebugLogs.tsx:161` → the enclosing `setErrorMessage(...)` call. Without following one hop (for a function whose body *is* the comparison, and for the call that encloses a ternary arm) the class is empty by construction.\n- **`unread` was a false alarm.** Both instances (`useChatSessionController.ts:220/222`) are a nested ternary tripping the `len(g[\"pred\"]) >= 120` clip at `_a3_fe_reason.py:80`; the branch maps 404→`\"not_found\"`, 403→`\"access_denied\"` and calls `setSessionFetchError`.\n- **Extractor row-kind gaps** (`templates/center/generators/_a3_fe_extract.mjs:378-466` — the kinds are `jsx` · `call` · `new` · `cmp` · `ret` · `throw` · `let`):\n  - **no `yield` row** — `services/lib.tsx:259` will read as an empty branch;\n  - **no assignment row** — `errorMsg = t(\"…\")` reaches the rows only as the `t` call (args captured), so the later `toast.error(errorMsg)` joins to the key only by the shared name in `refs`, never by data flow;\n  - **no `continue` / `break`** — the retry loop at `apiServices.ts:272` shows only the `setTimeout` promise;\n  - what *is* already there and sufficient: `throw` rows carry `value` + `callee` (so `new RateLimitedError` and its literal args survive), `ret` rows distinguish a bare `return;` (`value: null`) from `return null` (`null: true`) and carry `jsx`/`tags` for a rendered element and `obj` for an object literal, `call` rows carry `callee` + up to 3 literal `args` + `props`/`opts` + `binds` + `refs`, and `ctx` already names `try` / `catch:e` / `loop` / `callback:NAME` / `prop:CALLEE.KEY` — which is how an `onError` callback, a `useEffect` and a catch arm can be told apart without new machinery.\n- **The literal argument is the readable fact**, more than the callee: `toast.error(t(\"toasts.alreadyRunning.message\"))` · `throw new RateLimitedError(body.detail || \"You've reached your usage limit.\")` · `return \"cooking.error.start_allergen\"` · `router.push(\"/admin/indexing/status\")`.\n- **Roughly 5% of sites are not API reasons at all** (3 of 61: `useScanToDraft.ts:67`, `recipeFacetOptions.ts:167`, keypro `:169`). All three are `<name>.code === \"<string>\"` where the receiver is an array element or a DB driver error, and all three already carry an `unknown` origin with a reason. They would produce `does: [returns boolean]`.\n- **keypro is not evidence for the frontend rosters**: its single site is in `src/adapters/postgres/`, a backend repository file the fe arm typed as a `fe:` piece.\n- **Negated polarity is real**: `svcSS.ts:99/100/114` compare `!== 403` / `!== 401`, so the branch that runs is the *failure* arm and the silent arm is the fall-through. `_branch`'s `pat` already matches on `left op right` text, so a `!==` site's rows are the throw, not the swallow — the swallow is invisible."
  },
  {
   "key_facts": [
    "Flow rows live per FILE then per BODY: flow.byFile[f].flow.bodies[name].rows — a body is a module-level fn, a store action `<Store>.<key>`, a call-option fn property, or `<module>` (_a3_fe_extract.mjs:544-577); the piece id is derived as `fe:<file>#<owner>` at _a3_fe_reason.py:249, so several bodies share one pid.",
    "The flow is a SECOND extractor run with GABE_FE_FLOW=1 and a 300s timeout (_a3_forms_build.py:355-363); no web source raises _Absent and the whole feed is restored.",
    "_branch(rows, site, recv) at _a3_fe_reason.py:75-86 is handed the RAW cmp row, not the assembled site dict (call at :259), so it reads the un-normalised `left`.",
    "Its regex (:76) is `<left><op>(<literal>|any dotted name)`; `later` is every other row with line >= the site's line over the WHOLE body (:77); `inside` is the rows whose guard stack matches that text non-negated (:78) — textual, not structural, and with no ctx filter.",
    "The five words: `unread` (inside empty + a guard pred >= 120 chars, the clip cap), `value` (inside empty), `reads` (a row text matches \\.(detail|code)\\b), `passed` (an inside call row's refs names the receiver's base), `none` (:80-86).",
    "_branch is called ONLY when member == 'status' (:258-259) — detail and code sites carry no `branch` and never compute `inside` today.",
    "A site writes: id (r-<sha10>, hashed from [name, recv, member, op, value, n]) · piece · at · receiver · reads · op · value · optional `classified: False` · optional `branch` · origins[] · endpoints[] (_a3_fe_reason.py:254-262).",
    "readers{} rows are {piece, fn, receiver, routes[{exit, status, site}]}; `site` is the first covering site id in line order, else the string 'rest' (:271-275).",
    "Smallest change: split :76-78 into `_inside(rows, site) -> (inside, clipped)`, keep `_branch(inside, clipped, recv)` verbatim for the word, and add `s['does'] = _does(inside, file, …)` at :257 ABOVE the `if member == 'status'` test so detail/code sites get it too.",
    "Only `reason-collapsed` reads `branch` (:278, fires on 'none'|'value' + a shared-status + an equality op); `branch-unproduced` and `client-detail-unmatched` (:281-285) never do — so a new key is safe as long as `inside` selection is unchanged.",
    "tests/forms-frontend/run.sh:291 asserts the reason stats dict by EXACT equality (9 keys) — any new stats key fails F13 and must be updated in the same commit; site-key assertions there are tuple-based and inert.",
    "Sizes: _a3_fe_reason.py 291 lines, _a3_fe_forms.py 612, _a3_forms_build.py 426, _a3_forms_fe.py 56 — the 800-line budget is report-never-gate, so the part fits in the reason module without a new file.",
    "Rosters belong in _a3_forms_fe.py (data only, 'a new idiom is an edit HERE'); already there: NAV_ELEMENTS/NAV_THROWS, INVALIDATE_CALLS, SEED_CALLS, CACHE_WRITE_CALLS, MUTATE_CALLS, STORAGE_WRITES, EFFECT_HOSTS, CONTROL_TAGS/HANDLER_PROPS.",
    "NOT FOUND anywhere: a roster for an imperative navigate() from useNavigate, a toast/notify call, an instance .refetch(), or an i18n t() message key — each would be a new _a3_forms_fe.py entry.",
    "Extractor caps bounding does[]: ROW_CAP 600 rows per body (truncated:true), callee clipped 80, only the first 3 arguments literal-resolved into `args` (nulls for unresolved), `refs` first 3, ret.value clipped 80, jsx tags capped 6, opts tree depth 4.",
    "forms.json is written with sort_keys=True (build_center_a3.py:2209, 2312), so key order on the site dict never affects output bytes; the frontend write runs after the c4 graph (:2304-2320).",
    "Stats path: reason_part returns 9 keys → arms.frontend.stats['reason'] (_a3_forms_build.py:402); absence words are present · not_emitted · absent · unreadable (form_drift.py:52), mirrored by the lab's readers_state (_ep_joins.py:286).",
    "The arm's honest-empty law is a reason string beside an empty list (never a bare [] or 0) — _o() at :108-109 and every `unknown` origin at :175/191/217/232/234; a does: [] wants a word beside it.",
    "The lab half already reads site['does'] per route (docs/design/workflow-panel/_ep_joins.py:278) and renders `String(rt.does)` (_lab-ep-panels.js:2835) — a list of dicts would print [object Object].",
    "Frontend row id letters are x e c p r only; b s a q m t were struck at §A4 V33 (amendment-1.md:115), so an id letter for a does row re-opens a struck ruling — address such a row by its `at`.",
    "form_drift, pulse S20, review FORM DRIFT and gabe-map never read frontend{} — a new key there reaches no drift detector; goldens assert only arm_findings.frontend."
   ],
   "open_questions": [
    "Should does[] be written for detail and code sites too? The lab reads rt.does for every routed exit, but _branch (and therefore `inside`) runs only for status sites today — lifting the call out of the `if member == 'status'` is required, and it changes nothing else.",
    "What word stands beside an empty does[]? For a status site `branch` already says value/unread/none; a detail or code site has no such word, so either a `does_state` key or a per-site reason is needed to keep the no-bare-[] law.",
    "Should a does row record its `ctx`? Rows nested in a callback inside the branch (an onSuccess, a .then) are in `inside` today and would otherwise be claimed as the branch's own immediate action.",
    "Does the over-capture in `inside` get fixed with this pass or inherited? Two textually identical `if`s in one body share rows, and the regex's name alternation matches any named-constant comparison on the same left — invisible as a word, visible as rows.",
    "Do does rows need ids at all, given the §A4 V33 ruling that struck the unminted frontend letters (b s a q m t)? Addressing a row by its `at` is the estate's fallback.",
    "Which stats keys should the reason part gain for does[] (e.g. rows, sites with none, by kind)? Any addition breaks tests/forms-frontend F13's exact stats equality and must land with the battery edit.",
    "Does `_lab-ep-panels.js:2835` (`String(rt.does)`) get updated in the same pass, or does the generation land first and the lab row follow?"
   ],
   "report": "## Reason arm — raw facts for the `does[]` design\n\nAll paths are absolute-relative to `/home/khujta/projects/gabe_lens`. Read-only pass: no battery, build, node or browser was run.\n\n---\n\n### 1. Where the flow rows come from, and their scope\n\n**Origin.** `templates/center/generators/_a3_forms_build.py:355-363` — `_flow(repo)` detects the web root (`_a3_fe._detect_web_root`) and runs the extractor a **second** time with `env={\"GABE_FE_FLOW\": \"1\"}` and `timeout=FF.FLOW_TIMEOUT` (300s, `_a3_forms_fe.py:18`). No web source → `(None, \"no web source\")` → `_Absent` (build:381-382).\n\n**Shape.** `_a3_fe_extract.mjs:676` sets `rec.flow = flowOf(sf)` per file, so the feed is:\n\n```\nflow.byFile[<repo-relative file>] = { story, imports[], bindings{}, exports[], file_refs, flow: { bodies{}, routes[]? } }\nflow.byFile[f].flow.bodies[<name>] = { line, params?, rows[], truncated?, wraps? }\n```\n\n**Scope is per FILE → per BODY, not per piece.** `flowOf` (mjs:544-577) makes a body for: every module-level function declaration / `export default fn` / function-valued `const` (mjs:547-556); every **store action** `<Store>.<key>` (mjs:557, via `storeActions` mjs:497-502); every function-valued property of an object a declaration's call is given — `<name>.<key>`, e.g. `Route.beforeLoad` (mjs:560-566); and `<module>` for module-scope statements (mjs:570-574).\n\nThe piece id is **derived**, not carried: `_a3_fe_reason.py:249` `pid = f\"fe:{file}#{name.split('.', 1)[0]}\"`. Several bodies can map to one pid.\n\n**Rows.** `bodyRows` (mjs:350-481). Kinds: `ret` · `throw` · `call` · `new` · `cmp` · `jsx` · `let`. Every row carries `k`, `line`, and optionally `guards[]` (the conditions above it, `{pred, neg?}`), `after[]` (earlier exits it passed), `ctx[]` (the callback context it sits in: `'try'`, `'catch:<name>'`, `'finally'`, `'loop'`, `'callback'`, `'callback:<name>'`, `'prop:<host>.<key>'`). Per kind (mjs:378-409, 423-456):\n\n| kind | keys | caps |\n|---|---|---|\n| `call` / `new` | `callee` (clip 80), `args` (first **3** arguments, literal-resolved, `null` per unresolved slot), `refs` (first 3 arguments that are bare names), `props` (literal properties of the first object-literal argument), `opts` (that object as a tree, depth 4), `binds` (names the declaration it feeds binds) | mjs:379-388 |\n| `ret` | `value` (clip 80, `null` for a bare `return`), `jsx` (tag) + `tags` (≤6 nested tags), `null: true`, `obj` (returned object literal, key→text), `implicit: true` (concise arrow body) | mjs:423-430, 469-477 |\n| `throw` | `value` (clip 80), `callee` (clip 60) | mjs:431-434 |\n| `jsx` | `tag`, `props`, `spread`, `up` | mjs:378 |\n| `cmp` | `left` (clip 60), `op`, `right` (literal, else clipped text + `rx: true`), `left_lit` | mjs:407-409 |\n| `let` | `binds`, `alts` (the names an alias may hold) | mjs:451-456 |\n\n**Floors that bound any `does[]`:** `ROW_CAP = 600` rows per body (mjs:192) → the body carries `truncated: true` (counted in `guards_part` stats only, `_a3_fe_forms.py:306`; the reason part does not count it); `clip` default 120 chars (mjs:194) — guard predicates are clipped at 120, which `_branch` uses as its `unread` signal.\n\n---\n\n### 2. What `_branch()` computes, exactly\n\n`_a3_fe_reason.py:75-86`. Signature `_branch(rows: list, site: dict, recv: str) -> str`. **`site` here is the raw `cmp` ROW `r`, not the assembled site dict** (call site `:259`), so it reads `site[\"left\"]` (the *un-normalised* left text — `error?.status` keeps its `?.`), `site[\"op\"]`, `site[\"right\"]`, `site[\"line\"]`.\n\n**The regex** (`:76`):\n\n```python\npat = re.compile(re.escape(site[\"left\"]) + r\"\\s*\" + re.escape(site[\"op\"]) + r\"\\s*(?:\" + re.escape(str(site[\"right\"])) + r\"\\b|[A-Za-z_$][\\w$.]*)\")\n```\n\ni.e. `<left><op>(<the literal>|any dotted name)` — the alternation lets the branch be found when the guard text compares against a named constant.\n\n**`later`** (`:77`) = every row of the body other than the site row with `line >= site[\"line\"]` (`>=`, so a one-line `if (x) return \"…\"` is included). No `ctx` filter, no block-scope filter.\n\n**`inside`** (`:78`) = rows of `later` carrying a **non-negated** guard whose `pred` the regex matches:\n\n```python\ninside = [r for r in later if any(not g.get(\"neg\") and pat.search(g[\"pred\"]) for g in r.get(\"guards\") or [])]\n```\n\nSo \"inside\" is *textual*, not structural: it is every row in the body whose guard stack names this comparison positively — including rows nested in callbacks (`ctx`), and including a later, textually identical `if`.\n\n**The five words:**\n\n| word | condition | line |\n|---|---|---|\n| `unread` | `inside` empty **and** some guard pred in `later` is ≥ 120 chars (the clip cap) | `:80` |\n| `value` | `inside` empty otherwise — the comparison feeds a value, no branch holds rows | `:80` |\n| `reads` | any text of an inside row matches `READ_RX = \\.(?:detail|code)\\b` (`:45`); texts = `value`, `callee`, `left`, `refs[]`, `props{}`.values() (`_texts`, `:70-72`) | `:81-82` |\n| `passed` | an inside `call` row has a `refs[]` entry whose base equals `recv`'s base — the receiver handed to a call | `:83-85` |\n| `none` | otherwise | `:86` |\n\n`_branch` is called **only for `member == \"status\"`** (`:258-259`), so detail/code sites carry no `branch` key today and never have `inside` computed.\n\n---\n\n### 3. How a site record is assembled — every key\n\nLoop: `_a3_fe_reason.py:243-264`.\n\n- `:244` files present in `transport` (the client part's wrapper files) are skipped whole.\n- `:247` a body whose name has a `.` and whose owner body exists in the same file is skipped — \"a store action: its rows are the store body's\" (the store body already contains the action rows; battery asserts the dedup at `tests/forms-frontend/run.sh:292`).\n- `:249` `rows`, `pid`, `n = 0` (per-body site counter, for id uniqueness).\n- `:251` `_site(r)` (`:59-67`) admits a row only when `r[\"k\"] == \"cmp\"`, `not r.get(\"rx\")` (right resolved to a literal), `r[\"op\"] in OPS` (`:42-43`, eight operators), `SITE_RX.fullmatch(str(r[\"left\"]))` where `SITE_RX = (.+?)\\??\\.(status|detail|code)` (`:44`, members from `FF.REASON_MEMBERS`), the value type checks (status: `int`, not `bool`, 100–599; detail/code: `str`), and the normalised receiver (`_norm_recv` `:52-56`: drops `?.`, unwraps `(error as {…})`) fullmatches `NAME_RX`.\n\n**Keys written on a site** (`:254-262`), in write order:\n\n| key | value | line |\n|---|---|---|\n| `id` | `_rid(\"r\", pid, [name, recv, member, r[\"op\"], value, n])` → `r-<sha10>` (`_a3_fe_forms._rid`, `:39-40`) | `:254` |\n| `piece` | `fe:<file>#<owner export>` | `:254` |\n| `at` | `f\"{file}:{r['line']}\"` | `:254` |\n| `receiver` | normalised receiver text | `:255` |\n| `reads` | `status` \\| `detail` \\| `code` | `:255` |\n| `op` | the comparison operator | `:255` |\n| `value` | the int status or the string | `:255` |\n| `classified` | `False` **only** when `fe_ids` was given and `pid` is not in it (§A4 V34) | `:256` |\n| `branch` | one of the five words — **status sites only** | `:258-259` |\n| `origins` | deduped by `json.dumps(sort_keys=True)`, sorted by that key; each `{kind, via[], endpoints[], reason?}` with `endpoints` defaulted to `[]` | `:260-261` |\n| `endpoints` | sorted union of the origins' endpoints | `:262` |\n\n`groups[(pid, name, recv)]` (`:264`) collects sites for routing.\n\n**`readers{}`** (`:271-275`): per group and per endpoint the group reaches,\n\n```python\nreaders[ep].append({\"piece\": pid, \"fn\": name, \"receiver\": recv,\n                    \"routes\": [{\"exit\": x.get(\"id\"), \"status\": x.get(\"status\"), \"site\": <first covering site id> or \"rest\"}, …]})\n```\n\n`produced(ep)` (`:266-268`) reads `endpoints[ep][\"produced\"]`, else the first variant's. `_covers` (`:89-94`) matches a status site by operator on `x[\"status\"]`, or a detail/code site by equality against `x[site[\"reads\"]]` (also matching a quoted substring).\n\nOrigin kinds (`_Reach.origins`, `:171-234`): `hook` · `cache-call` · `fetch` · `alias` (via `let` alts) · `catch` · a parameter walk (call sites / JSX prop / object-argument property) · `unknown` **always with a `reason`** (`:175, 191, 217, 232, 234`). `HOPS = 3` (`:41`). `_o()` (`:108-109`) is the estate's honest-empty helper: an empty `endpoints` list always carries a `reason`.\n\n---\n\n### 4. Where `does[]` can be attached with the smallest change\n\nEverything needed is already in scope at `_a3_fe_reason.py:250-264`: `file`, `name`, `rows`, `r`, `recv`, `pid`. Bindings for resolving a callee to a piece are reachable too — `reach.files[file].get(\"bindings\")` and `reach.local[file]` (built at `:147`), and `_resolve`/`_leaf` are already imported (`:38`).\n\n**Smallest shape (one file, three edits):**\n\n1. Split the row selection out of `_branch` — it is the only place `inside` exists:\n   ```python\n   def _inside(rows, site) -> tuple[list, bool]:   # the rows the branch holds, and whether a condition was clipped\n   ```\n   keeping `pat`/`later`/`inside` verbatim from `:76-78` and returning `(inside, any(len(g[\"pred\"]) >= 120 for r in later for g in r.get(\"guards\") or []))`.\n2. `_branch(inside, clipped, recv) -> str` keeps `:80-86` byte-for-byte (the word logic is untouched, so the findings cannot move — §5).\n3. At `:257-259`, compute `inside, clipped = _inside(rows, r)` **above** the `if member == \"status\"` test, keep `s[\"branch\"] = _branch(inside, clipped, recv)` inside it, and add `s[\"does\"] = _does(inside, file, …)` unconditionally.\n\nPoint 3 matters: **today `_branch` runs only for status sites** (`:258`), while the lab reads `rt.does` for every routed exit regardless of `reads` (`docs/design/workflow-panel/_ep_joins.py:278`, `docs/design/workflow-panel/_lab-ep-panels.js:2835`). A detail/code site gets no `does[]` unless the call is lifted out of that `if`.\n\nAn alternative one-line variant — `s[\"branch\"], s[\"does\"] = _branch(rows, r, recv)` — is smaller but leaves detail/code sites without `does[]`.\n\n**Precedent for an optional site key:** `classified: False` (`:256`) is already written conditionally, so an extra key on a site is in keeping. Site `id` is hashed from `[name, recv, member, op, value, n]` only (`:254`) — adding `does` cannot move ids.\n\n**Rosters.** `_a3_forms_fe.py` is the declared home (\"a new idiom is an edit HERE, never in `_a3_fe_forms`\", `:1-5`; 56 lines). Already present and reusable: `NAV_ELEMENTS` / `NAV_THROWS` (`:8-9`), `EFFECT_HOSTS` (`:17`), `CACHE_QUERY_CALLS` / `CACHE_MUTATION_CALLS` (`:23-24`), `INVALIDATE_CALLS` (`:26`), `SEED_CALLS` (`:27`), `CACHE_WRITE_CALLS` (`:35`), `PERSIST_CALLS` / `STORAGE_WRITES` (`:33-34`), `CLIENT_HOOKS` (`:37`), `MUTATE_CALLS` (`:38`), `HTTP_VERBS` (`:36`), `CONTROL_TAGS` / `HANDLER_PROPS` (`:30-31`). Also `_a3_fe._STORE_CALLEES` / `_ROUTER_CALLEES` / `_CACHE_CALLEES` (`_a3_fe.py:53-72`).\n\n**Not found** — no roster anywhere for: an imperative navigation (`navigate(\"/x\")` from `useNavigate()`; `NAV_ELEMENTS`/`NAV_THROWS` cover only a returned `<Navigate>` element and a thrown `redirect`/`notFound`), a toast/notify call, an instance `refetch()`, or an i18n `t(...)` message key. Each would be a new entry in `_a3_forms_fe.py`.\n\n**Reusable classifiers:** `_a3_fe_forms._nav(row, rows)` (`:114-121`) turns a `ret`/`throw` row into `(is_nav, to)` but needs the whole `rows` list (it scans the same line for the element/call that carries `to`). `_a3_fe_forms._exits` (`:132-150`) is the guard part's kind ladder — `nav` · `outlet` · `render` · `null` · `throw` · `value` — the nearest existing vocabulary for \"what a body does at an exit\".\n\n---\n\n### 5. How the findings use the branch word\n\n- **`reason-collapsed`** (`:276-280`) is the only finding that reads `branch`: it fires when `s[\"reads\"] == \"status\"` **and** `s[\"op\"] in (\"===\", \"==\")` **and** `s[\"value\"]` is a status the endpoint's `shared-status` finding names **and** `s[\"branch\"] in (\"none\", \"value\")`. Nothing else on the site is read except `at`, `id`, `value`.\n- **`branch-unproduced` / `client-detail-unmatched`** (`:281-285`) never read `branch`: they use `s[\"endpoints\"]`, `_provable` (`:97-105`) and `_covers`.\n- Findings sort at `:290` by `(id, at, endpoint)`.\n\n**Conclusion:** a new `does` key on the site dict cannot disturb the findings **provided `_branch`'s five words keep their exact meanings**. The risk is not the key, it is any refactor that changes which rows land in `inside` — `reads`/`passed`/`none` all read from that list. Keeping `_inside` verbatim is the safety property.\n\n`F.FINDINGS` entries live at `_a3_forms.py:202-215` (`redirect-loop`, `reason-collapsed`, `branch-unproduced`, `client-detail-unmatched`, `dead-control`, `action-uncalled`, `no-rollback`); the slot ids are `U7`/`K2`/`K1`/`K3`. The frontend slot registry (which block a reader opens) is `_a3_forms.py:62-66`, where `\"hook\": {… \"reason\": {\"name\": \"Reason sites\", \"block\": \"frontend.reasons\"}}`.\n\n---\n\n### 6. File sizes vs the 800-line CODE budget\n\n| file | lines |\n|---|---|\n| `templates/center/generators/_a3_fe_reason.py` | **291** (509 of headroom) |\n| `templates/center/generators/_a3_fe_forms.py` | **612** (188 of headroom) |\n| `templates/center/generators/_a3_forms_build.py` | 426 |\n| `templates/center/generators/_a3_forms_fe.py` | 56 (data only) |\n| `templates/center/generators/_a3_fe_controls.py` | 340 |\n| `templates/center/generators/_a3_fe_extract.mjs` | 685 |\n| `tests/forms-frontend/run.sh` | 395 |\n\nThe budget is report-never-gate (CLAUDE.md, ruling R9): state the numbers in the commit message if a file crosses 800. The reason module has room for the whole part without a new file. If a new generator module *is* minted, `templates/center/generators/propagate.sh:38-47` auto-lands it on a twin only when a suite generator imports it — an **indented** `import` counts (the frontend parts are lazily imported, `propagate.sh:33-37`, `_a3_forms_build.py:383, 393, 395`).\n\n---\n\n### 7. How the arm reports stats and state words\n\n**Stats from `reason_part`** (`:286-289`), nine keys, exactly:\n\n```python\n{\"sites\", \"reads_status\", \"reads_detail\", \"reads_code\", \"joined\", \"unknown\", \"endpoints\", \"routed\", \"rest\"}\n```\n\n`joined` = sites with endpoints; `unknown` = sites without; `routed`/`rest` count the route rows.\n\n**Where they land.** `_a3_forms_build.extend_frontend` (`:366-426`): `:394` calls `FER.reason_part(flow, forms, hooks, frontend[\"client\"][\"transport\"], {p[\"id\"] for p in (fe or {}).get(\"pieces\") or []})`; `:402` `stats.update({… \"reason\": rstats, …})`; `:401` findings merge; `:403` `json.dumps(frontend, …)` is \"the write's own test\"; `:405-407` findings go to `forms[\"arm_findings\"][\"frontend\"]` and are counted into `stats[\"findings\"]`; `:408-413` every part of `F.ARMS[\"frontend\"][\"parts\"]` (`guards · hooks · client · reason · controls · stores`, `_a3_forms.py:312`) gets `{\"present\": True, \"reason\": None}`; the arm row is `{present, reason, version, options, stats, parts, bytes}`.\n\n**State words.** `_Absent` (`:47-49`) and the two `except` arms (`:414-421`) restore the feed from a deepcopy snapshot and write `_off(reason, \"frontend\")` = `{\"present\": False, \"reason\", \"version\": 1, \"options\": {}, \"stats\": {}, \"parts\": {p: {\"present\": False, \"reason\"}}}` (`:128-133`). Downstream words: `skills/gabe-pulse/scripts/form_drift.py:52` — `present · not_emitted (no file) · absent (the pass said why) · unreadable`; the lab mirrors them at `docs/design/workflow-panel/_ep_joins.py:286` (`readers_state = present | absent | not_emitted`).\n\nInside the block itself the estate's honest-empty law is a **reason string beside an empty list**, never a bare `[]` — `_o()` (`:108-109`) and every `unknown` origin (`:175, 191, 217, 232, 234`). §A4's state-word sweep struck bare `0`/`[]` across the arms. A `does: []` therefore wants a word beside it (the existing candidates `value` / `unread` / `none` already carry that meaning for a status site; a detail/code site has no such word today).\n\n**`forms.json` is written with `sort_keys=True`** (`templates/center/generators/build_center_a3.py:2209, 2312`), so key order in the site dict is irrelevant to the output bytes. The frontend write happens after the c4 graph (`build_center_a3.py:2304-2320`) and prints `json.dumps(_fa[\"stats\"], sort_keys=True)[:200]`.\n\n---\n\n### Traps found while reading\n\n1. **`inside` is textual and body-wide.** `later` is `line >= site[\"line\"]` over the whole body (`:77`) and the match is on the guard's *text* (`:78`). Two consequences a `does[]` will show as wrong rows: (a) a second, textually identical `if (error.status === 409)` later in the body contributes its rows to the first site; (b) the regex alternation `[A-Za-z_$][\\w$.]*` (`:76`) makes `error.status === TEAPOT` match the 409 site's pattern whenever the right side is a named constant. Today this only perturbs a word; as rows it becomes visible.\n2. **Rows inside nested callbacks are in `inside`.** No `ctx` filter. A call inside an `onSuccess` given within the branch reads as the branch's own action unless `does[]` records `ctx`.\n3. **`_branch` receives the raw row.** `site[\"left\"]` is un-normalised (`error?.status`), while `s[\"receiver\"]` is normalised (`:255`, `_norm_recv`). A `does[]` that wants to say \"the receiver was handed to X\" must use `recv`, as `_branch` does at `:83-84`.\n4. **Detail/code sites have no branch today** (`:258`) — see §4.\n5. **The lab already renders `String(rt.does)`** (`_lab-ep-panels.js:2835`), which will print `[object Object]` for a list of row dicts. The lab half is design material, but the shape of `does[]` decides whether that one line needs an edit.\n6. **The battery asserts the reason stats dict by exact equality** — `tests/forms-frontend/run.sh:291`: `assert rs == {\"sites\": 14, \"reads_status\": 11, …}`. Any new stats key fails F13 and must be updated in the same commit (repo convention: behavior edits update the battery in the same commit). F13's site assertions (`:266-293`) read named keys via tuples, so a new *site* key is inert there; F8 (`:152`) asserts the findings dict and `len(sites) == 14`, also inert.\n7. **Goldens are inert.** `docs/design/element-forms/goldens/expected.json` carries only `reason-collapsed-two` and `reason-collapsed-setupscreen` — both on `arm_findings.frontend`, neither on sites.\n8. **`map_census kind=forms` / `form_drift` never read `frontend`** (`skills/gabe-pulse/scripts/form_drift.py` reads endpoints + findings only), so a new frontend key does not reach pulse S20 or review FORM DRIFT.\n9. **Id letters:** `docs/design/element-forms/amendment-1.md:115` — frontend row letters are `x e c p r` (exit · effect · control · path · reason site); `b s a q m t` \"were planned and never minted, **struck at §A4 V33**\". A `does` row with its own id letter would re-open a struck decision; the estate's fallback for an unminted row is to address it by its `at`.\n10. **Both station copies rule** does not apply here (that is the shell's two-file law), but `propagate.sh` twin propagation does — `_a3_fe_reason.py` and `_a3_forms_fe.py` already exist on twins and propagate as updates.\n\n### The fixture, as concrete `does[]` material\n\n`tests/forms-frontend/fixture/src/routes/SetupForm.tsx` gives one of each shape the brief names: `if (error.status === 409) return \"setup in progress\";` (a literal message returned, `ret.value`), `return error.detail ?? \"conflict\";` (branch `reads`), `ErrorBody`'s `return <p>locked</p>;` (`ret.jsx` + `tags`), `loadMe`'s `if (res.status === 401) return \"signed out\";` (a `fetch` origin), and `const expired = … me.error.status === 401` (branch `value`, no rows at all). 14 sites, 11 status / 1 detail / 2 code.\n"
  }
 ]
}