{
 "title": "The `inflight` part of the kinds arm (leftovers piece 11)",
 "status": "DRAFT — not landed; waits for the operator's ruling and an explicit 'land it'",
 "plan": {
  "schema": "THE BLOCKS (all written only when the kinds arm is selected and the part ran; arms off = no key at all, D12)\n\n1) Per endpoint (and per variant): `endpoints[<key>].inflight` = a LIST of rows in REQUEST ORDER (middleware by `order.runs` ascending, then dependencies in FastAPI resolution order with sub-dependencies before their parent, then route-wrapper callables [12b], then the handler; a site one call down sorts under the station that called it; ties by line, kind, name). Written as `[]` when the part ran and found nothing — an absent key always means \"part off\" (the `switches[]`/`tasks == {}` precedent). Request-scoped rows are complete; a process-scoped thing this endpoint meets is a short row carrying `ref` plus this endpoint's own `read_at`.\n\n2) Top level: `forms[\"inflight\"] = {\"process\": {<key>: row}, \"rules\": {<rule id>: {scope, dies, says, source}}}`.\n   - `process` keys carry no line number (the ids-leaf law): `built-once:<file>::<Cls>.<attr>` · `setting-once:<file>::<Cls>.<setting or property>` · `cache:<file>::<qual>`. Each row adds `applies_to` (count of endpoints that carry a ref row with applies != false).\n   - `rules` holds ONLY the rules some row used; a row carries the rule id, never free text. No `x:`-style id is minted: rows are addressed by (kind, carrier, name, set_at) and process rows by their map key — the §A4 V33 precedent (amendment-1.md:121). Record as D29.\n\n3) Row keys\n   always: `kind` (state · contextvar · dependency-value · background · lock · cache · built-once · setting-once) · `name` · `scope` (request | process | unknown) · `set_at` (file:line | \"unknown\") · `set_by` (the STATION: `middleware:<Cls>` | dependency fid | handler fid | `<file>::<Cls>.__init__`; \"unknown\" when no write was found) · `set_in` (middleware | dependency | route-wrapper | handler | init) · `read_at` [ {at, fn, in, via?} ] capped at INFLIGHT.read_cap with `reads_more: n` · `reads` (found | none in scope) · `dies` ('with the answer' | 'with the server process' | 'unknown') · `rule`.\n   optional, any kind: `set_fn` + `set_via` (the write is one call down: the function holding it and the call site in the station) · `also_set_at` [file:line] (other writes of the same name in scope — tier3's four `usage_credential` writes are ONE row) · `set_cond: true` (the write sits under an if/loop/except/match) · `applies: false` (a pass-through arm above the site proves this path exempt, D19) · `from` {kind: header|state, name} · `ref`.\n   state: `carrier` (request.state | app.state) · `receiver: \"unproven\"`.\n   contextvar: `var` (`<file>::<NAME>`) · `label` (ContextVar's first string argument) · `reset_at` · `through: \"structlog\"`.\n   dependency-value: `param` · `type` (annotation text) · `dependency` (key into dependencies{}) · `teardown` (copied from the dependency form).\n   background: `param` · `task` (resolved fid, when a project function) · `args` [unparsed, cap 120] · `after_answer: true` · `queues` (found | none in scope | beyond one level) + `passed_at`.\n   lock: `call` (unparsed, cap 60) · `held` (with-block | call) · `released_at` · `released: \"when its transaction ends\"`.\n   cache: `fn` · `decorator` · `maxsize` (literal | \"default 128\" | \"unbounded\") · `as_dependency: true`.\n   built-once (top level): `owner` (`middleware:<Cls>`) · `attr` · `class` · `class_at` · `holds` [ {attr, as, at} ] (the container attributes its own __init__ builds) · each read_at entry adds `call`, `key` (first positional argument through S.locals_once, as `_rate` reads it) and `scope` (\"all\" | [path prefixes] via P._path_prefixes).\n   setting-once (top level): `setting` | `expr` + `fields` (a property, through S.resolve_self_attr) · `settings_class` · `value` + `state: \"default\"` + `env` + `declared_at` (the same keys contract `_arg` writes) · `hands_to` [ {attr, param?, at} ].\n\n4) State words. scope: request | process | unknown. dies: the three of the brief, nothing else. reads: found | none in scope (NEVER \"never read\"). set_at/set_by: a site or \"unknown\". queues: found | none in scope | beyond one level (the last only when the BackgroundTasks parameter is visibly passed on as an argument). value state: default | defined | unknown. A framework-cited rule on a lock file below `_a3_forms_short.FRAMEWORK_MIN` writes `dies: \"unknown\"`, `rule: \"framework-gate-closed\"`, `would_be: <rule id>`, and `stats.inflight.framework_gate` says `closed: fastapi <v> < 0.136.1` (the _a3_forms_effects.py:620-622 precedent).\n\n5) Envelope. `arms.kinds.parts.inflight = {present, reason}` · `arms.kinds.stats.inflight = {endpoints, rows, by_kind{all 8, zeros included}, by_dies{3}, by_rule{}, process, names{\"state:<attr>\" | \"contextvar:<NAME>\": {set_on, read_on}}, caches_deeper, reach_truncated, unplaced_middleware, framework_gate}` · `arms.kinds.options` gains `inflight_scope: \"one-level\"`, `inflight_dep_values: true`. THE ONE EXISTING VALUE THAT MOVES when kinds is on: `arms.kinds.bytes` (the arm's own size accounting — it moved the same way at Slice 8). Everything else is added keys.\n\n6) Worked example — gustify `endpoint:POST /setup/complete` (lines read 2026-09-20 from /home/khujta/projects/apps/gustify/apps/api and the arms-on feed ~/.cache/gabe-map-baselines/.check/gustify/forms.json, head 05007957). Middleware run order there: CORSMiddleware 0 · RateLimitMiddleware 1 · IdempotencyMiddleware 2, so the limiter rows come first.\nendpoints[...].inflight = [\n {\"kind\":\"setting-once\",\"name\":\"rate_limit_active\",\"scope\":\"process\",\"ref\":\"setting-once:apps/api/middleware/rate_limit.py::RateLimitMiddleware.rate_limit_active\",\"set_at\":\"apps/api/middleware/rate_limit.py:98\",\"set_by\":\"apps/api/middleware/rate_limit.py::RateLimitMiddleware.__init__\",\"set_in\":\"init\",\"read_at\":[{\"at\":\"apps/api/middleware/rate_limit.py:108\",\"fn\":\"apps/api/middleware/rate_limit.py::RateLimitMiddleware.dispatch\",\"in\":\"middleware\"}],\"reads\":\"found\",\"dies\":\"with the server process\",\"rule\":\"setting-at-init\"},\n ... (`trusted_proxy_hops` :99 read :111 · `_sensitive` built :103 read :116 · `rate_limit_sensitive_per_minute` :104 · `rate_limit_window_seconds` :101 also :104 · `_global` built :100 read :120 · `rate_limit_per_minute` :101) ...\n {\"kind\":\"state\",\"name\":\"idempotency_key\",\"carrier\":\"request.state\",\"scope\":\"request\",\"set_at\":\"apps/api/middleware/idempotency.py:25\",\"set_by\":\"middleware:IdempotencyMiddleware\",\"set_in\":\"middleware\",\"from\":{\"kind\":\"header\",\"name\":\"Idempotency-Key\"},\"read_at\":[{\"at\":\"apps/api/middleware/idempotency.py:31\",\"fn\":\"apps/api/middleware/idempotency.py::get_idempotency_key\",\"in\":\"handler\",\"via\":\"apps/api/api/setup.py:191\"}],\"reads\":\"found\",\"dies\":\"with the answer\",\"rule\":\"request-state\"},\n {\"kind\":\"dependency-value\",\"name\":\"ctx\",\"param\":\"ctx\",\"type\":\"AuthContext\",\"scope\":\"request\",\"dependency\":\"apps/api/auth/context.py::get_auth_context\",\"teardown\":false,\"set_at\":\"apps/api/auth/context.py:90\",\"set_by\":\"apps/api/auth/context.py::get_auth_context\",\"set_in\":\"dependency\",\"read_at\":[{\"at\":\"apps/api/api/setup.py:197\",\"fn\":\"apps/api/api/setup.py::setup_complete\",\"in\":\"handler\"}],\"reads\":\"found\",\"dies\":\"with the answer\",\"rule\":\"dep-solved\"},\n {\"kind\":\"dependency-value\",\"name\":\"session\", ... \"dependency\":\"apps/api/db.py::get_session\",\"teardown\":true,\"set_at\":\"apps/api/db.py:51\",\"dies\":\"with the answer\",\"rule\":\"dep-teardown\"},\n {\"kind\":\"dependency-value\",\"name\":\"settings\", ... \"dependency\":\"apps/api/config.py::get_settings\",\"scope\":\"process\",\"ref\":\"cache:apps/api/config.py::get_settings\",\"dies\":\"with the server process\",\"rule\":\"dep-cached\"}\n]\ninflight.process[\"built-once:apps/api/middleware/rate_limit.py::RateLimitMiddleware._sensitive\"] = {\"kind\":\"built-once\",\"name\":\"_sensitive\",\"scope\":\"process\",\"owner\":\"middleware:RateLimitMiddleware\",\"attr\":\"_sensitive\",\"class\":\"SlidingWindowLimiter\",\"class_at\":\"apps/api/middleware/rate_limit.py:43\",\"holds\":[{\"attr\":\"_hits\",\"as\":\"defaultdict\",\"at\":\"apps/api/middleware/rate_limit.py:52\"}],\"set_at\":\"apps/api/middleware/rate_limit.py:103\",\"set_by\":\"apps/api/middleware/rate_limit.py::RateLimitMiddleware.__init__\",\"set_in\":\"init\",\"read_at\":[{\"at\":\"apps/api/middleware/rate_limit.py:116\",\"fn\":\"...::RateLimitMiddleware.dispatch\",\"in\":\"middleware\",\"call\":\"self._sensitive.allow(key, now)\",\"key\":\"f'{ip}:sensitive'\",\"scope\":[\"/api/v1/setup/complete\",\"/api/v1/recipe-creation\",\"/api/v1/cooking/sessions\",\"/api/v1/pantry/reset/apply\"]}],\"reads\":\"found\",\"dies\":\"with the server process\",\"rule\":\"middleware-init\",\"applies_to\":23}\ninflight.process[\"setting-once:...::RateLimitMiddleware.rate_limit_sensitive_per_minute\"] = {... \"setting\":\"rate_limit_sensitive_per_minute\",\"value\":20,\"state\":\"default\",\"env\":\"GUSTIFY_RATE_LIMIT_SENSITIVE_PER_MINUTE\",\"declared_at\":\"apps/api/config.py:139\",\"set_at\":\"apps/api/middleware/rate_limit.py:104\",\"hands_to\":[{\"attr\":\"_sensitive\",\"param\":\"limit\",\"at\":\"apps/api/middleware/rate_limit.py:104\"}],\"dies\":\"with the server process\",\"rule\":\"setting-at-init\",\"applies_to\":23}\nThese reproduce the five `after_rows` of /home/khujta/projects/gabe_lens/docs/design/design-context/gaps-endpoint.effects.raw.json -> verified.pieces[10]. 23 is what the feed says today (23 of 80 endpoints carry the `_sensitive` limiter in rate{}). NOT confirmed by reading: the field list `S.resolve_self_attr` returns for the `rate_limit_active` property, and `ctx`/`session` read line :197 against the feed's head.",
  "detectors": "WHERE THE PART LOOKS — stations, built once per function and memoised by fid (a dependency 79 endpoints share is read once). No repo-wide grep: a test conftest, a startup seed or an unreached helper is never a station, which by construction removes the truth reader's traps 1 (test writes), 3 (unreached caches) and 4 (startup locks — gustify's 8 advisory locks read as 0).\n S1 middleware: every `forms[\"middleware\"]` form with `kind: project`, by `order.runs` ascending; node = its `method` fid (dispatch else __call__). Third-party/unknown classes are never walked.\n S2 dependencies: the handler's direct dependencies in `MW._dep_params` order, each preceded by its transitive `dependencies{}.subdeps`, each once (FastAPI solves a dependency once per request — fastapi/dependencies/utils.py:664-684). Node = what `P._dep_node` says FastAPI calls. A CALLED dependency expression (`Depends(factory(...))`, name ends in \")\", the _a3_forms_mw.py:464 test) walks the nested def the factory `return`s by name and NEVER the factory body — that body runs at import, not in the request. No returned def found -> the station is skipped and counted in `stats.inflight.factories_unread`.\n S3 route-wrapper callables (12b).\n S4 the handler.\n Every station is read in its own body (nested defs, lambdas, classes excluded) plus ONE call level through `R.callee` (incl. self.m()), center-vendored files skipped via `G._is_center`. One visitor `_visit(fn)` yields (node, guards, cond): guards in `P._events`' own text form so `P._path_prefixes` reads them; cond as `S._self_assigns` defines it.\n\nRUN ORDER: stations -> D6 built-once -> D7 setting-once -> D8 cache table -> per endpoint D1 state · D2 contextvar · D3 dependency-value · D4 background · D5 lock · process refs. PRECEDENCE when two detectors see one fact: a dependency whose callee is cached is ONE dependency-value row with `ref` (no second cache row on that endpoint) · a ContextVar set from a request.state read is two rows joined by `from` · a state write whose value is a header read carries `from: header` and no header row (a header is input, not in-flight state) · a parameter typed in F.INJECTED_TYPES is never a dependency-value.\n\nD1 state. Shapes (single-node predicates lifted into the carrier leaf): write = Assign/AnnAssign/AugAssign target `<x>.state.<attr>`, or `setattr(<x>.state, \"attr\", v)`; read = `getattr(<x>.state, \"attr\", ...)` with the name through `S.resolve_const` (tier3's `_RATE_LIMIT_USER_ID_STATE_KEY`), or a Load `<x>.state.<attr>`. RECEIVER PROOF added on top (contract's `_read` stays receiver-agnostic so repeat{} cannot move): leaf of `<x>` in INFLIGHT.app_receivers -> carrier app.state, scope process, rule app-state; `<x>` a parameter annotated with a leaf in INFLIGHT.request_types, or the first non-self parameter of a middleware dispatch -> request.state; `self` -> not a row; anything else -> kept with `receiver: \"unproven\"`, scope and dies unknown. One row per (carrier, name) per endpoint; first write in request order is set_at, the rest also_set_at. `from`: the written value's own header hit, else a local bound from a header read (the `held` logic of `_state_setter`). Emits rule request-state only when a write with a proven receiver is in scope; read with no write -> set_at/set_by \"unknown\", dies \"unknown\", rule no-write-found. MUST NOT CLAIM: \"never read\" · that the object is destroyed (the row is about the carrier, as the brief rules for request.state) · request scope for `request.app.state` · a merge with a cookie or header of the same name (tier3 `referral_source`).\n\nD2 contextvar. Shape: Call `<recv>.set|reset|get(...)` where `<recv>` RESOLVES (`P._resolve`, two import hops; `mod.NAME` through `m.imports`) to a module-level assignment (`_Mod.assigns` reads AnnAssign too — _a3_paths_read.py:94-97, gastify's form) whose value is a Call with leaf in INFLIGHT.contextvar_ctors. Never matched by method leaf alone — `dict.get` is not a read. Binder calls (leaf in INFLIGHT.contextvar_binders) give one row per keyword with `through: \"structlog\"`. dies: a `reset(<token>)` inside a `finally` of the SAME function as the set, or a scoped binder used as a `with` -> 'with the answer' + reset_at (rules cv-reset · cv-scoped); set with no reset -> 'unknown' (cv-no-reset); get with no set in scope -> scope and dies unknown, set_at \"unknown\" (cv-get-only). An accessor (`get_current_user_id()`) is covered exactly one level down; deeper is not read. MUST NOT CLAIM: that a no-reset value leaks, or that it dies with the task — nothing in the source says either (truth trap 11).\n\nD3 dependency-value (in the piece's after_rows and D-013's three known instances; NOT in the brief's list — ASSUMPTION, behind OPTIONS.inflight_dep_values). Shape: a handler parameter whose `Depends` default, `Annotated[..., Depends()]` or module alias names a dependency that `P._dep_target` resolves; param->dependency read by an 8-line private reader, `MW._dep_params` is NOT edited (three other arms consume it). read_at = Load sites of the parameter in the handler. Rules: callee decorated by INFLIGHT.cache_decorators -> process, dep-cached, ref · every `return` of the callee is a bare Name resolving to a module-level assignment -> process, dep-module-object · `dependencies{}.teardown` true -> dep-teardown · else dep-solved. Only the handler's own parameters; a sub-dependency's parameters are not rows. MUST NOT CLAIM anything about an unresolved dependency (no row).\n\nD4 background. Shape: a parameter annotated with a leaf in INFLIGHT.background_types (handler, dependency, or the callee one level down) and Calls `<param>.add_task(f, ...)`; also `background=BackgroundTask(...)` on a returned Response. Emits name = unparsed callable, task fid when `P._resolve` finds it, args, after_answer true, dies 'unknown', rule after-the-answer. A parameter with no add_task in scope still gets a row: `queues: \"none in scope\"`, or `\"beyond one level\"` + passed_at when the parameter is an argument of a call. MUST NOT CLAIM when the task ends, nor that a ContextVar is still set inside it (tier3 `drain_delete_loop` — undecidable from source).\n\nD5 lock. Shapes: Call leaf in F.LOCK_CALLS whose receiver name does not match INFLIGHT.lock_not_receivers · Attribute call `with_for_update` · a str Constant matching INFLIGHT.lock_sql. held = with-block when the call is a With/AsyncWith item. Rules: with-block -> lock-with-block · a release (INFLIGHT.release_calls, or pg_advisory_unlock text) in a `finally` of the same function -> lock-released + released_at · with_for_update and xact advisory locks -> lock-transaction ('unknown' + released text) · else lock-open ('unknown'). MUST NOT CLAIM a lock the endpoint's scope does not reach, nor a release it did not see; a lock OBJECT (threading.Lock()) is not a lock row.\n\nD6 built-once. For each project middleware class: `Cls.__init__` -> every `self.<attr> = <Call>` (`S._self_assigns`, its conditional flag -> set_cond) whose constructor is NOT a settings read. `class`/`class_at`/`holds` when `S._class` resolves the constructor in the repo (holds = its own __init__'s `self.x = <leaf in INFLIGHT.containers>(...)` or a dict/list/set literal). read_at = Load sites of `self.<attr>` in dispatch plus self.m() one level, each with guards -> scope. MUST NOT CLAIM an object built inside dispatch (per request), nor anything for a third-party class.\n\nD7 setting-once. In the same __init__: every Attribute `<local>.<name>` whose local `S._typed_local` types as a settings class (`S._is_settings`). `<name>` in `S.fields` -> value/env/declared_at with `state: \"default\"` (the environment can override; never \"the live value\"); a directly assigned property -> `S.resolve_self_attr` (expr · fields · unresolved); otherwise `state: \"unknown\"` with `expr`. hands_to = the self attribute it is assigned to, or the built object's attribute plus the constructor parameter it fills. One row per (class, setting): first read is set_at, others also_set_at (gustify's window_seconds at :101 and :104).\n\nD8 cache. A function met by a station — called in scope (R.callee) or itself the dependency — whose decorator name (`C._dec_name`) is in INFLIGHT.cache_decorators. Top-level row once; per-endpoint ref rows where met. Deeper reach is COUNTED, never listed: `stats.inflight.caches_deeper` from one `R.bfs` over `FN.roots` restricted to handler and dependency roots (the switches-arm \"one past it only counted\" precedent). MUST NOT CLAIM pure-versus-external, unreached caches, or the hand-rolled TTL cache of tier3 (no detector -> no row).\n\n12b — NOT in the first landing; chosen by the dry run's `unknown`/`none in scope` counts on gastify and tier3: route-wrapper callables (slowapi `key_func`/`exempt_when`; gastify's user_id READ lives there) · `@app.middleware(\"http\")` function stations (tier3 sets its ContextVars there; today they are only NAMED by `MW._unscanned`, counted here as `unplaced_middleware`) · module-level and `global`-built objects (gastify `limiter`/`metrics`, gustify `_breaker`). Until then those rows say no-write-found / none in scope — true, and said.",
  "rosters": "One new constant in /home/khujta/projects/gabe_lens/templates/center/generators/_a3_forms.py, placed after LOCK_CALLS (:294) — `INFLIGHT = {...}`, about 50 lines (331 -> ~385). CONTRACT[\"key_name\"] (:263) is NOT moved or widened. Cites below were read on this machine 2026-09-20: fastapi 0.136.3 · starlette 1.3.1 · uvicorn 0.48.0 (gustify venv), slowapi 0.1.9 · structlog 25.5.0 (gastify venv), CPython 3.12.3 functools.\n\n request_types = (\"Request\",\"HTTPConnection\",\"WebSocket\") — a parameter so annotated is the connection; `HTTPConnection.state` is a State over `scope[\"state\"]` (starlette/requests.py:189-195).\n app_receivers = (\"app\",) — `self.state = State()` once per application (starlette/applications.py:51).\n contextvar_ctors = (\"ContextVar\",) · contextvar_ops = (\"set\",\"reset\",\"get\") — contextvars, PEP 567: `reset(token)` restores the value before the matching `set`.\n contextvar_binders = {\"bind_contextvars\":\"set\", \"bound_contextvars\":\"scoped\", \"clear_contextvars\":\"clear\", \"unbind_contextvars\":\"reset\"} — structlog/contextvars.py:96 · :112 · :170.\n cache_decorators = {\"lru_cache\": 128, \"cache\": None} — Lib/functools.py lru_cache (:479 in 3.12) · cache (:651): the cache lives on the wrapper function object. `cached_property` is deliberately absent (instance-scoped); `alru_cache`/`cachetools` absent until a source is read.\n background_types = (\"BackgroundTasks\",) · background_calls = (\"add_task\",) · background_kw = (\"background\",) — starlette/background.py:26-34; FastAPI attaches the injected tasks to the response (fastapi/routing.py:680-681); Starlette runs them after the body is sent (starlette/responses.py:170).\n lock_calls = LOCK_CALLS (reused, unchanged) · lock_methods = (\"with_for_update\",) — SQLAlchemy Select/Query.with_for_update, SELECT ... FOR UPDATE held to the end of the transaction (the idiom is already in CONTRACT.claim_idioms) · lock_sql = r\"(?i)\\bpg_(try_)?advisory_(xact_)?lock(_shared)?\\b\" — PostgreSQL 9.28.10 Advisory Lock Functions: xact locks end with the transaction, session locks at unlock or disconnect · lock_not_receivers = r\"(?i)(^|_)(pool|engine)$\" — asyncpg/aiopg `Pool.acquire()` is a connection checkout (the receiver-regex precedent is EFFECTS.session_names) · release_calls = (\"release\",\"unlock\").\n containers = (\"dict\",\"list\",\"set\",\"deque\",\"defaultdict\",\"OrderedDict\",\"Counter\",\"TTLCache\",\"LRUCache\",\"Lock\",\"RLock\",\"Semaphore\") — what `holds[]` names.\n wrapper_callables = {\"limit\": (\"key_func\",\"exempt_when\"), \"shared_limit\": (\"key_func\",\"exempt_when\")} — 12b; slowapi/extension.py:783-790 · :823-829, called at :496-499; wrappers.py:35-40 calls `exempt_when()` with no arguments.\n read_cap = 8.\n rules = {\n  request-state: request · with the answer · starlette/requests.py:189-195\n  app-state: process · with the server process · starlette/applications.py:51\n  no-write-found: request · unknown · \"read here, no write in this endpoint's scope — set beyond one call level, by a registration the stack scan does not read, or lifespan state copied into every request\" · uvicorn/protocols/http/h11_impl.py:217 · httptools_impl.py:234 · starlette/routing.py:638-642\n  receiver-unproven: unknown · unknown\n  cv-reset · cv-scoped: request · with the answer · PEP 567 / structlog/contextvars.py:170\n  cv-no-reset: request · unknown · cv-get-only: unknown · unknown\n  dep-solved: request · with the answer · fastapi/dependencies/utils.py:598-684 (`values` :620 and `dependency_cache` :627 are built per call)\n  dep-teardown: request · with the answer · fastapi/dependencies/utils.py:578-591 · :667-672 (a generator dependency enters the request's AsyncExitStack)\n  dep-cached · dep-module-object: process · with the server process\n  after-the-answer: request · unknown · starlette/responses.py:170 · fastapi/routing.py:680-681\n  lock-with-block · lock-released: request · with the answer\n  lock-transaction · lock-open: request · unknown\n  functools-cache: process · with the server process\n  middleware-init · setting-at-init: process · with the server process · starlette/applications.py:57-89 — `app = cls(app, *args, **kwargs)` (:76) runs once, the stack is built when `middleware_stack is None` (:88-89)\n  framework-gate-closed: scope kept · unknown\n }\n The fastapi- and starlette-cited rules open only at `P._vt(fw[0]) >= P._vt(SH.FRAMEWORK_MIN)` (0.136.1). ASSUMPTION: the starlette lines were read at 1.3.1 and ride FastAPI's gate — no starlette floor of their own exists in the registry.\n\nAlso in _a3_forms.py: OPTIONS gains `inflight_dep_values: True` and `inflight_scope: \"one-level\"` (the part refuses any other value rather than claim it — the setting_constants precedent); KINDS[\"endpoint\"] gains `\"U15\": {\"name\": \"In-flight state\", \"question\": \"what is alive while this request runs — where it is set, where it is read, whether it goes with the answer\", \"block\": \"inflight[]\", \"arm\": \"kinds\"}` and KINDS[\"middleware\"] gains `\"memory\": {\"name\": \"Kept in memory\", \"block\": \"inflight.process\"}`. U1-U14 are allocated in /home/khujta/projects/gabe_lens/docs/design/logic-map/element-forms.html; U15 is the next free number and is the operator's to confirm. No FINDINGS entry — an analysis, never a grade; pulse S20, review FORM DRIFT and map-baseline.sh stay untouched.",
  "code_plan": "All under /home/khujta/projects/gabe_lens/templates/center/generators/.\n\nA · NEW LEAF `_a3_forms_carrier.py` (~45 lines) — the shapes of a request carrier, ONE definition for both arms (the `_a3_forms_falsify.py` precedent). Imports `ast`, `_a3_forms_settings as S`, `_a3_paths as P`.\n - `text(repo, m, node) -> str | None` — contract's `_text` (:58-65) moved verbatim.\n - `hit(repo, m, n) -> dict | None` — the four shapes of `_read` (:129-140) tested on ONE node in the same order, returning `{\"header\": ...}` / `{\"state\": ...}` / None. Equivalence holds because no node can satisfy two shapes and a shape whose name does not resolve falls through to None exactly as today.\n - `state_target(t) -> str | None` — the target test of `_state_setter` (:162-163).\n No walker moves — walk order is what makes repeat{} bytes.\n\nB · `_a3_forms_contract.py` — three edits, nothing else: `import _a3_forms_carrier as CR` at column 0; `_text = CR.text`; `_read` becomes `for n in ast.walk(node): got = CR.hit(repo, m, n); if got: return got` then `return {}` (same ast.walk order, same first hit); `_state_setter`'s any(...) becomes `any(CR.state_target(t) == attr for t in n.targets)` — its LIFO `_own` walk, its first-hit return, its hardcoded dispatch/__call__ pair, `_key` and `_KEY` stay byte-for-byte. The readers' open question about sorting `_state_setter` by line is answered NO: it could move `repeat.key.set_at` on a middleware that writes one attribute twice. 525 -> ~515.\n\nC · NEW MODULE `_a3_forms_inflight.py` (~430 lines; 800 budget; named `_a3_forms_*` so forms-core K8 scans its private imports). Column-0 imports: ast · Path · `_a3_code as C` · `_a3_forms as F` · `_a3_forms_carrier as CR` · `_a3_forms_fn as FN` · `_a3_forms_reach as R` · `_a3_forms_settings as S` · `_a3_forms_short as SH` · `_a3_graft as G` · `_a3_paths as P`. `_a3_forms_mw` is imported LAZILY inside `inflight_part` (mw imports this leaf at column 0; propagate.sh:33-43 lands indented imports) for `MW._handlers`, `MW._dep_params`, `MW._exempts`. It imports no arm and the contract arm never imports it.\n `_visit(fn)` (~30) · `_census(repo, m, node, qual)` memo by fid, cleared at the top of every run (~70: the D1/D2/D4/D5 shapes plus self-attribute loads) · `_one_down` (~15) · `_closure(node)` a factory's returned nested def (~10) · `_station` (~15) · `_stations(repo, forms, MW, v, m, fn, dec)` (~40) · `_held(repo, m, fn)` header-bound locals (~10) · `_receiver(fn, role, expr)` (~15) · `_param_deps` (~12) · `_state_rows` (~45) · `_cv_rows` (~40) · `_dep_rows` (~30) · `_bg_rows` (~25) · `_lock_rows` (~25) · `_init_rows(repo, forms)` built-once + setting-once with read scopes (~60) · `_cache_rows` + deeper count (~30) · `_gate(repo, rel)` (~8) · `inflight_part(repo, forms, amap) -> (block, [], stats)` (~40): writes `v[\"inflight\"]`, returns the top-level block, sorts everything, returns `{\"absent\": ...}` through the runner only when no handler could be read. The six lines of contract `_arg` (:366-376) are DUPLICATED here rather than moved — every value still comes from `S.fields`; DECISION deferred: fold into S when a third reader appears.\n\nD · `_a3_forms_mw.py` — +3 lines (516 -> 519): `import _a3_forms_inflight as IF  # the inflight part (Slice 12)` beside :18-19; `PARTS` (:27) gains \"inflight\"; the builder tuple at :504 gains `(\"inflight\", IF.inflight_part)`. The triple return fits the loop; the part sets the per-endpoint key itself. `_dep_params`, `dependency_forms.chain`, `middleware_forms` are not edited.\n\nE · `_a3_forms.py` — INFLIGHT, the two OPTIONS, U15 + `memory`; `ARMS[\"kinds\"]` parts gains \"inflight\", `part_slices: {\"inflight\": 12}`; `ARM_STAGES` gains its OWN stage `(\"kinds\", (\"inflight\",))` right after `(\"kinds\", (\"middleware\",\"dependencies\"))`; `ARM_NEEDS` gains `\"kinds.inflight\": (\"kinds.middleware\", \"kinds.dependencies\")`.\n Why an own stage: `_a3_forms_build.py:264-300` snapshots and restores per STAGE. Sharing stage 1, a raise in this part would take middleware{} and dependencies{} down with it and cascade `needs kinds.middleware` through paths.paths, effects, contract and tests. Alone, a raise costs this part only and reads `error: ...`. A hard need inside one stage is impossible anyway (`unit_needs(u) - ok` is taken before the stage runs, :255).\n Why early and not the last kinds stage: it reads nothing effects or contract write; and /home/khujta/projects/gabe_lens/tests/forms-core/run.sh:500 (`kinds_k == [\"tasks\"]`) holds only while the LAST kinds stage is unchanged. :427 (first failing unit stays kinds.middleware) and :441 (`closure({\"contract\"})` — contract does not need this unit) are untouched by reading; none of this was run.\n\nF · `_a3_forms_build.py` — one line beside :23-26: `import _a3_forms_carrier  # noqa: F401`. RUNNERS is not edited (kinds already has a runner).\n\nG · `/home/khujta/projects/gabe_lens/tests/forms-core/run.sh:786` — the exact KINDS[\"endpoint\"] set gains \"U15\". No other literal moves.",
  "battery": "All cases in /home/khujta/projects/gabe_lens/tests/forms-kinds/run.sh, ids C30-C39, inserted BEFORE the summary line at :704 (a line printed after it never counts). No new tests/ directory — `tests/inflight/` is the BOARD projection's battery. Same style: the real orchestrator over `_a3_paths.build` on a synthetic tree, AST only, each case editing the fixture in-test so it can FAIL. Expected 705 -> ~935 lines, 18 -> 28 assertions, still seconds.\n\nFIXTURE — a THIRD copy, the file's own precedent (`S8` at :182): `IFX=\"$T/ifx\"; cp -r \"$A\" \"$IFX\"` so C14-C29 keep their exact counts. `$A` already carries `@lru_cache get_settings` and `Gate.__init__`, so existing cases must not pin whole key sets (read: they do not — :476 tests an intersection). Prelude gains `variant_if(name, edit)` beside `variant8` (:382). Added to the copy:\n - `state.py`: `REQ = ContextVar(\"req\", default=None)` · `WHO: ContextVar[str | None] = ContextVar(\"who\", default=None)` · `SEEN = ContextVar(\"seen\")` · `def current_seen(): return SEEN.get()`.\n - `middleware/keys.py`: `KeyStamp(BaseHTTPMiddleware)`, dispatch unannotated: `key = request.headers.get(HDR)` · `request.state.req_key = key` · `tok = REQ.set(key)` · `try: return await call_next(request)` · `finally: REQ.reset(tok)`.\n - `middleware/count.py`: `Window.__init__` builds `self._hits = defaultdict(deque)`; `Count.__init__`: `s = settings or get_settings()` · `self._on = s.limit_enabled` · `self._win = Window(s.limit_n, 60)`; dispatch: `if not self._on: return await call_next(request)` · `if request.url.path.startswith(\"/flight/park\"):` + `if not self._win.allow(key): return JSONResponse(..., 429)`. `config.py` gains `limit_n: int = 5`.\n - `deps.py`: `who(request: Request)` writes `request.state.user_id` under an `if`, calls `mark(u)` which does `WHO.set(u)` (no reset); factory `need(scope)` returning `_check(request: Request)` that reads `getattr(request.state, \"user_id\", None)`; `plain()` returning a value; `def shared(): return REGISTRY` with a module-level `REGISTRY = Registry()`.\n - `api/flight.py`: `POST /flight/park` (Depends(who), Depends(need(\"a\")), a `session` from the generator dependency, `settings: Annotated[Settings, Depends(get_settings)]`, `tasks: BackgroundTasks`; body: `key = read_key(request)` one level · `tasks.add_task(send, key, who=ctx)` · `with guard.lock(\"k\"):` · `q.with_for_update()` · `pool.acquire()` · `load_table()` decorated `@lru_cache`) · `GET /flight/plain` (nothing) · `GET /flight/deep` (reads `req_key` TWO calls down; has a `tasks: BackgroundTasks` it never uses; calls `current_seen()`). `services/tables.py` adds `@lru_cache def unused_cached()` and a cached function reached only at depth 2. `main.py` registers KeyStamp and Count.\n\nC30 state FIRE + SILENT + mutation: on /flight/park the `req_key` row equals exactly {kind state, carrier request.state, scope request, set_at at(\"middleware/keys.py\",\"request.state.req_key =\"), set_by \"middleware:KeyStamp\", set_in middleware, from {header, the HDR value}, read_at[0] = {at: the getattr line in read_key, via: the handler call line, in handler}, reads found, dies \"with the answer\", rule request-state}. /flight/plain carries the same row with `reads: \"none in scope\"`; /flight/deep likewise (two levels is not read, and nothing claims otherwise). MUTATION: patch the write to `pass` -> /flight/park's row reads set_at \"unknown\", dies \"unknown\", rule no-write-found; /flight/plain loses the row — nothing found, nothing invented.\nC31 dependency write, factory-closure read: `user_id` has set_by \"deps.py::who\", set_in dependency, set_cond true; read_at names the `_check` closure line, never the factory body. SILENT: /flight/plain has no `user_id` row. MUTATION: move the read out of `_check` into `need`'s own body -> the read disappears (import time is not the request).\nC32 contextvar: REQ -> dies \"with the answer\", reset_at = the finally line, from absent; WHO (set one level down via `mark`, no reset) -> dies \"unknown\", rule cv-no-reset, set_via = the call line in `who`; SEEN on /flight/deep -> scope \"unknown\", set_at \"unknown\", rule cv-get-only. MUTATION: delete the `finally` reset -> REQ flips to cv-no-reset. SILENT: a `cfg.get(\"x\")` in the handler makes no row.\nC33 built-once + setting-once: `inflight.process[\"built-once:middleware/count.py::Count._win\"]` has class Window, holds [{attr _hits, as defaultdict}], rule middleware-init, read_at[0].scope == [\"/flight/park\"], applies_to == 1; `setting-once:...::Count.limit_n` has value 5, state default, env APP_LIMIT_N, hands_to [{attr _win, param limit}]; `...Count.limit_enabled` applies_to == 3. /flight/plain carries no `_win` ref. SILENT: `Stamp` (no __init__) and CORSMiddleware add nothing. MUTATION: build `Window(...)` as a local inside dispatch -> the process row is gone and no built-once row is invented.\nC34 background: /flight/park row {kind background, name \"send\", args [\"key\",\"who=ctx\"], after_answer true, queues found, dies \"unknown\", rule after-the-answer}; /flight/deep's unused parameter -> `queues: \"none in scope\"`. MUTATION: hand `tasks` to a helper that hands it on again -> `queues: \"beyond one level\"` with passed_at.\nC35 lock: `with guard.lock(\"k\")` -> held with-block, rule lock-with-block; `with_for_update` -> dies \"unknown\", released \"when its transaction ends\"; `pool.acquire()` -> no row. MUTATION: the `with` becomes a bare `guard.lock(\"k\")` -> rule lock-open. A startup function in main.py that takes an advisory lock -> `by_kind.lock` does not count it.\nC36 cache: per-endpoint ref + `inflight.process[\"cache:services/tables.py::load_table\"]` {decorator lru_cache, maxsize \"default 128\", applies_to 1}; `unused_cached` has no row; `stats.inflight.caches_deeper == 1`. MUTATION: drop the decorator -> both rows gone.\nC37 dependency-value: `ctx` -> dep-solved · `session` -> dep-teardown, teardown true · `settings` -> scope process, ref to the cache row, and NO second cache row for that fid on the endpoint · a parameter on `Depends(shared)` -> dep-module-object · the `tasks` and `request` parameters make no dependency-value row. MUTATION: the generator's `yield` becomes `return` -> dep-solved.\nC38 off · honest-empty · determinism · ONLY ADDS: `build(IFX, None)` has no \"inflight\" key top-level or on any endpoint; `build(IFX, \"contract\")` likewise (contract's closure does not pull the part) and its `repeat` blocks equal those of `build(IFX, \"kinds,contract\")` endpoint by endpoint; a variant with the new files and both registrations removed writes `inflight == {\"process\": {...only what $A itself holds...}, \"rules\": {...}}` and `[]` on the health endpoint; two builds byte-identical under `json.dumps(sort_keys=True)`. THE WHOLE-FEED PROOF: OFF = a build with `F.ARM_STAGES` monkeypatched to drop `(\"kinds\", (\"inflight\",))` (a part in no stage never runs — _a3_forms_build.py:245-248), ON = the normal build, both with every backend arm selected; after deleting from ON the top-level `inflight`, every endpoint's `inflight`, `arms.kinds.parts.inflight`, `arms.kinds.stats.inflight`, the two `arms.kinds.options.inflight_*` keys and `arms.kinds.bytes` (from both), the two serialise identically. MUTATION: a patched part that also touches `v[\"repeat\"]` makes the comparison fail.\nC39 isolation: with `IF.inflight_part` monkeypatched to raise, `arms.kinds.parts.inflight.present is False`, reason starts \"error:\", `middleware{}`/`dependencies{}` are present and paths · effects · contract still read present. MUTATION (the design's own): with the part moved into stage 1, the same raise takes `kinds.middleware` down — the case fails, which is why the stage is its own.\n\nGENERATOR MUTANTS — run serially through the battery's `GEN_OVERRIDE` hook (:9) on a scratch copy of the generators; record \"3 mutants, 3 killed\" in the commit: (1) the reset test forced true -> C32 red · (2) `_closure` returning the factory itself -> C31 red · (3) `CR.hit`'s getattr shape returning `{\"state\": \"x\"}` -> C30 red AND /home/khujta/projects/gabe_lens/tests/forms-contract/run.sh E7 red (:401-402 pins `repeat.key` by dict equality — the standing guard on the lift).\n\nGOLDENS — /home/khujta/projects/gabe_lens/docs/design/element-forms/goldens/expected.json, slice \"12\", 13 entries on gustify; `render-acceptance.py:9-11` ARM map gains `\"12\": \"kinds · inflight\"`. Every `get` starts `[\"endpoints\",\"endpoint:POST /setup/complete\",\"inflight\",{\"where\":{...}},{\"first\":true}, ...]` unless noted:\n inflight-key-set-at (R) where kind state + name idempotency_key -> set_at == \"apps/api/middleware/idempotency.py:25\" (the literal repeat.key.set_at already pins) · inflight-key-from-header (R) from.name == \"Idempotency-Key\" · inflight-key-read (R) read_at pluck at has \"apps/api/middleware/idempotency.py:31\" · inflight-key-read-via (R) pluck via has \"apps/api/api/setup.py:191\" · inflight-key-dies (V) == \"with the answer\" · inflight-login-context (P) where param ctx -> dependency == \"apps/api/auth/context.py::get_auth_context\" · inflight-session-teardown (V) where param session -> rule == \"dep-teardown\" · inflight-settings-process (V) where param settings -> scope == \"process\" · inflight-sensitive-counter (R) [\"inflight\",\"process\",\"built-once:apps/api/middleware/rate_limit.py::RateLimitMiddleware._sensitive\",\"set_at\"] == \"apps/api/middleware/rate_limit.py:103\" · inflight-counter-holds (V) same row, holds pluck attr has \"_hits\" · inflight-cap-read-once (R) the rate_limit_sensitive_per_minute row: value == 20 · inflight-key-read-on-six (V) [\"arms\",\"kinds\",\"stats\",\"inflight\",\"names\",\"state:idempotency_key\",\"read_on\"] == 6 (the six call sites the truth reader listed) · inflight-none-here (V) by_kind.contextvar == 0, by_kind.background == 0, by_kind.lock == 0 (a grep says 8 locks; the endpoint answer is 0).\n gastify, tier3 and keypro are dry-run NUMBERS in the commit message, not goldens.",
  "landing_order": [
   "0 · PRE-FLIGHT, seconds: `git log -1` (branch graft-adoption, a concurrent session's `git add -A` has swallowed a commit here before); `df -h /mnt/c` and `du -sh /var/log` (alarm under 40 GB free or over 2 GB); confirm nothing heavy is running. Baseline the one recorded red rail BEFORE editing: `bash tests/center/run.sh` (~44 s, alone) — docs/center/data/facts.json (2026-09-16, head 92dc6ed) records it rc=1 and no reader could say whether that is stale.",
   "1 · SPEC FIRST, no code (~half a day) — the operator's iterate-before-implement rule. Write the 'Slice 12 · in-flight state' section into /home/khujta/projects/gabe_lens/docs/design/element-forms/amendment-1.md in the file's own shape (Generates · Modules with lines · Algorithm · Goldens · Battery · Dry run record · Effort), a Map-keys row for `inflight{process, rules}`, an ids-table sentence (no id, V33), two module-map rows, and D29 (no id; rows addressed by their site) · D30 (`dies` is the CARRIER's lifetime and only a roster rule may say it) · D31 (an own stage, for isolation). FOUR operator rulings wanted before code: the word `inflight` (it also names the board projection) · slot U15 · dependency-value rows in or out · the 12b order.",
   "2 · THE LIFT, alone, a pure refactor (~2 h): `_a3_forms_carrier.py` + the three contract edits + the build import. Verify serially: `bash tests/forms-contract/run.sh` (E7 pins repeat.key by equality), then `bash tests/forms-core/run.sh` (K8 import scan) — seconds each. Real-data proof: copy the current ~/.cache/gabe-map-baselines/.check/gustify/forms.json to the scratchpad, run `bash scripts/forms-dryrun.sh --arms all gustify` ALONE (~2 min), and compare the two with a python one-liner — expect byte-identical. That number goes in the commit message.",
   "3 · REGISTRY (~1 h): INFLIGHT, OPTIONS, U15 + `memory`, ARMS / ARM_STAGES / ARM_NEEDS in `_a3_forms.py` (331 -> ~385); tests/forms-core/run.sh:786 gains \"U15\". Run forms-core: before step 4, `arms.kinds.parts.inflight` must read `not built yet (slice 12)`.",
   "4 · THE MODULE (~1.5 days): `_a3_forms_inflight.py` (~430 lines) + the 3-line wiring in `_a3_forms_mw.py`. Build order inside: `_visit` and `_census` -> stations incl. the factory closure -> D1 state -> D3 dependency-value -> D6/D7 init rows -> D8 cache -> D2 contextvar -> D4 background -> D5 lock. The first four alone reproduce the piece's five gustify after_rows.",
   "5 · BATTERY (~1 day): fixture copy `IFX` + C30-C39. Run one at a time: forms-kinds (~5 s) · forms-core (~2 s) · forms-contract · forms-paths · forms-effects · forms-short · forms-tests (ARM_STAGES moved, so each is re-run; seconds each) · tests/element-forms (~1 s, the endpoint pass must be untouched). Then the three generator mutants through GEN_OVERRIDE, serially.",
   "6 · `./install.sh` (~5 s) — suite-doctor hashes templates/ file by file against ~/.claude/templates/gabe/ and reads any generator edit as DRIFT until this runs.",
   "7 · DRY RUN, ALONE, one target at a time (~10-12 min total; every target builds twice): `bash scripts/forms-dryrun.sh --arms kinds gustify` first, then `--arms all` on gustify · gastify · tier3 · keypro. Record per target: the arms-none leg BYTE-IDENTICAL · `arms.kinds.parts.inflight` · `stats.inflight` (rows, by_kind, by_dies, by_rule, process, caches_deeper, unplaced_middleware, framework_gate) · `arms.kinds.bytes` before and after · `seconds` against the last recorded run (the part adds a second R.bfs — watch tier3). Then the whole-feed only-adds comparison on gustify against the step-2 copy, with the C38 strip list.",
   "8 · GOLDENS (~1 h): 13 entries, slice \"12\"; the ARM map row; `python3 docs/design/element-forms/goldens/check-goldens.py <gustify arms-on forms.json> --slice 12`, then the full check (expect 62 + 13, 0 mismatch, 0 missing); regenerate acceptance.html with render-acceptance.py. Seconds, no browser.",
   "9 · DOCS in the same commit: templates/center/generators/README.md:60 (two module rows) · the CLAUDE.md element-forms bullet (one sentence, plus the forms-part-versus-board-projection line) · docs/design/design-context/decisions.md D-013 (its revisit trigger fired) · inventory-endpoint.md:73 (smallest · middle · largest rows per endpoint over the 80, read from the feed with a one-liner) · program-leftovers.md (one Log row with the numbers).",
   "10 · `bash scripts/suite-doctor.sh` ALONE (~5-6 min; eight batteries launch a browser) -> CLEAN, read against the step-0 baseline for tests/center. Then `python3 docs/center/generators/write_facts.py --only forms-kinds,forms-core` (seconds).",
   "11 · COMMIT by explicit path, one commit for the generation half. The message states: dry-run numbers for the four targets · the step-2 byte-identity result · '3 mutants, 3 killed' · size-budget numbers (tests/forms-kinds/run.sh 705 -> ~935 and tests/forms-core/run.sh 839 sit over the 800 line; `_a3_forms_inflight.py` ~430 · `_a3_forms.py` ~385 · `_a3_forms_mw.py` 519). `git log -1` before and after.",
   "12 · TWINS, one at a time (minutes each — propagate ends in the twin's `refresh_center.sh regen`): `bash templates/center/generators/propagate.sh /home/khujta/projects/apps/gustify --check`, then without `--check`; then gastify. Both new `.py` files auto-land as 'required' (imported at column 0 by `_a3_forms_mw` · `_a3_forms_contract` · `_a3_forms_build`). Push and PR stay the operator's.",
   "13 · THE LAB CARRY — the piece's second half, a second commit (~1 day); see lab_carry.",
   "14 · 12b, decided by the step-7 numbers, not now: route-wrapper callables · `@app.middleware(\"http\")` function stations · module-level and `global`-built objects. DECISION: defer. TRIGGER: gastify's `state:user_id` reads `read_on: 0`, or tier3's cv-get-only + no-write-found rows outnumber its proven rows."
  ],
  "risks": [
   "ISOLATION — placed in the first kinds stage, a raise in this part restores the whole stage (_a3_forms_build.py:291-300): middleware{} and dependencies{} vanish and `needs kinds.middleware` cascades through paths.paths, effects, contract and tests. The own stage is load-bearing; C39 proves it.",
   "THE LIFT — repeat{}'s bytes come from walk ORDER (`ast.walk` first hit in `_read`; a LIFO `_own` first hit in `_state_setter`). Only single-node predicates move. Guards: forms-contract E7's dict equality (:401-402), mutant 3, and the before/after dry-run comparison. Fallback if the comparison is not identical: drop the lift and let the new leaf keep a private copy of the four shapes (~20 duplicated lines, zero contract edits).",
   "NOT CONFIRMED by any reader or by me: whether tests/center's recorded rc=1 (facts.json, 2026-09-16, head 92dc6ed) is stale or live. Baseline it before editing or a red doctor will read as this change's fault.",
   "NOT CONFIRMED: the forms-core literals (:427 · :441 · :500) were checked by READING against the proposed ARM_STAGES, not by running. :500 survives only while the LAST kinds stage stays `functions · tasks · handlers`.",
   "ASSUMPTION — `dies` describes the CARRIER (the request.state slot, the solved parameter, the with block), not the object behind it. The brief rules that way for request.state; extending it to dependency values and with-block locks is my reading. A dependency that returns a process object is caught only in two shapes (cached callee · bare module-level Name); any other singleton reads `dep-solved`. If the operator wants `unknown` there, it is a one-line change in the rules roster.",
   "ASSUMPTION — dependency-value rows are in the piece's after_rows and in D-013's three known instances but NOT in the brief's detector list. Built behind OPTIONS.inflight_dep_values and the first thing to cut if scope must shrink.",
   "ASSUMPTION — U15 and the slice label '12' are mine. U1-U14 are the operator's scorecard; a different number costs one registry line and one forms-core literal.",
   "NAME — `inflight` already names the board projection (write-inflight.py · docs/site/center/inflight.json · tests/inflight · a CLAUDE.md bullet). Kept because program-leftovers.md:19 and the prism 'In-flight state' use the word; contained by the `_a3_forms_` prefix, by putting cases in tests/forms-kinds, and by always writing 'the forms inflight part' against 'the in-flight projection'. A rename (`alive`) is cheap only BEFORE step 3.",
   "`arms.kinds.bytes` MOVES on every kinds-on feed — a changed value, not an added key. It is the arm's own size accounting and moved the same way at Slice 8, but it is the one exception to 'only adds' and the battery strips it explicitly. Arms-off feeds, baselines (map-baseline.sh reads no arm stat) and the committed example feed do not move.",
   "FEED GROWTH — process ref rows repeat per endpoint: gustify roughly 80 endpoints x ~12 rows x ~220 B, about 0.2 MB on a 5.1 MB arms-on feed. Measure `bytes` at the dry run; if it matters, keep refs to {kind, name, ref, read_at} only.",
   "COST — `caches_deeper` runs a second R.bfs (the functions part runs its own later, in memory, unshared). R.callee and P._events are memoised so it should be cheap, but tier3 (512 endpoints) is unmeasured: compare `seconds`. If it grows, drop the deeper count — never listed, only counted — rather than touch `_a3_forms_reach.py`.",
   "COVERAGE FLOOR, said and not hidden — v1 misses gastify's state READ (a slowapi key_func inside the route wrapper), tier3's ContextVar SETS inside `@app.middleware(\"http\")` closures, module-level and lazy `global` singletons (gastify `limiter`/`metrics`, gustify `_breaker`), tier3's hand-rolled TTL cache, instance-dependency state, and anything inside a nested def other than a factory's returned closure. The rows say `none in scope` / `no-write-found` / `unknown`; `unplaced_middleware` and `factories_unread` count what was not walked.",
   "RECEIVER PROOF — an unannotated `request` outside a middleware dispatch reads `receiver: unproven` with dies `unknown`. Correct but possibly noisy on a loosely typed app; the dry run's `by_rule.receiver-unproven` tells.",
   "LEAF-NAME LOCK ROSTER — F.LOCK_CALLS matches by callee leaf (`acquire`, `lock`). `lock_not_receivers` removes pool checkouts; any other collision shows its own `call` text to the reader. Analysis only, so a wrong hit grades nothing — but it is still a wrong row.",
   "CITES — starlette lines were read at 1.3.1, uvicorn at 0.48.0, slowapi at 0.1.9, functools line numbers at CPython 3.12 while the venvs run 3.13. Only FastAPI has a version gate in the registry; the starlette rules ride it. A twin pinning an older starlette would carry cites nobody read.",
   "STREAMING — under BaseHTTPMiddleware a `finally` reset runs when call_next returns, which for a streamed body is before the body ends. 'with the answer' stays true as an upper bound; the row does not say 'before the last byte'.",
   "PRIVATE-API DRIFT — the leaf uses `S._typed_local`, `S._self_assigns`, `S._is_settings`, `MW._exempts`, `MW._dep_params`; forms-core K8 proves only `P.`, `G.` and PYDI privates exist. A rename elsewhere breaks this part — said (`error: ...`, own stage), never silent, and C33/C37 catch it.",
   "NO CONSUMER READS THE BLOCK until the lab carry (D16): pulse S20, review FORM DRIFT, gabe-map `form` and map-baseline.sh are untouched on purpose. If the carry slips, the generation half ships facts nothing shows.",
   "PROCESS — `git add -A` from a concurrent session on graft-adoption: commit by explicit path. WSL: every battery, the dry run and the doctor strictly one at a time; none of them was run for this design."
  ],
  "lab_carry": "After the generation half is committed, doctor-clean and on both twins (program-leftovers.md:26). Input: the arms-on feed the dry run leaves at ~/.cache/gabe-map-baselines/.check/gustify/forms.json, passed with `--forms`.\n\n1) /home/khujta/projects/gabe_lens/docs/design/workflow-panel/gen-endpoint-facts.py — in `forms_slice()` beside the pass-through at :224 (`\"repeat\": ep.get(\"repeat\"), \"auth\": ..., \"rate\": ...`) add `\"inflight\": _inflight(fj, ep)`, RESOLVED for a renderer: each row's `rule` replaced by `{id, says, source}` from `fj[\"inflight\"][\"rules\"]`; each `ref` replaced by the top-level `fj[\"inflight\"][\"process\"][ref]` record (holds · hands_to · value · applies_to) merged under the endpoint's own read_at; a ref the feed cannot resolve kept bare with `unresolved: True` (the function's own law). Part off -> `None` with a `why` in the feed's words, read from `arms.kinds.parts.inflight.reason`. Nothing typed by hand.\n\n2) /home/khujta/projects/gabe_lens/docs/design/workflow-panel/_ep_joins.py — three joins, all from the feed: the state row whose name equals `repeat.key.through` minus `request.state.` IS the repeat key's route ('The repeat key's route' stops being the only instance) · a built-once row's `attr` == `rate.limits[].limiter` gives the counter 'what it counts by' (`limits[].key`) · a dependency-value row's `dependency` == a `security.guards` entry gives the gate its value. The database-session row arrives already carrying `teardown` — the fact piece 6 put on the card.\n\n3) What the card reads, in the operator's five after_rows: what it is called (`name`, or for a header-sourced state row `from.name`) · where it is set and by what (`set_at` + `set_in`: a middleware, a gate, the handler) · where it is read (`read_at[].at`, `via`) · whether it goes with the answer (`dies` + the rule's `says`). `reads: none in scope` and `dies: unknown` are shown as the feed's words with existing components (chain rows, chips, the four states) — NO new display design inside the piece; how the card finally shows it is decided by seeing.\n\n4) Inventory: inventory-endpoint.md:73 gets smallest · middle · largest rows per endpoint over the 80, measured with `--out` in a loop so the lab's own `_lab-ep.js` is never touched. D-013's revisit trigger fires — 'request-scoped state' becomes ratable as a 2 if the general reading finds state no other row carries; on gustify, expect it to find exactly the three instances D-013 names plus the process half nothing carried before.\n\n5) /home/khujta/projects/gabe_lens/docs/design/workflow-panel/probe-eplab.mjs — asserts that measure the DRAWN thing: drawn in-flight rows == `LABEP.forms.inflight.length`; every drawn lifetime word is one of the three; the idempotency row shows set :25 and read :31. One proven on a mutant: drop the state row from a scratch copy of `_lab-ep.js` -> the count assert fires. The probe launches a browser: run it alone.\n\nFollow-up named, not built: once the general reading has soaked, `repeat.key` could be derived from the in-flight row that a precondition requires and whose `from` is a header, retiring the `(?i)idempot` name filter. That changes repeat{} bytes, so it is its own reviewed pass, never part of this piece."
 },
 "readers": [
  {
   "key_facts": [
    "`_read()` (_a3_forms_contract.py:125-141) and `_state_setter()` (:144-166) never touch the name regex — `_KEY` (compiled at :27 from `F.CONTRACT['key_name']` = r'(?i)idempot', _a3_forms.py:263) is used at exactly ONE site, `_key():192`, as a post-filter on the assembled row.",
    "`_read(repo, m, node)` matches four AST shapes in `ast.walk` order and returns on the FIRST hit: `X.headers.get(name)` (:129-132), `X.headers[name]` (:133-134), `getattr(X.state, 'attr', …)` (:135-138), and any Load `X.state.attr` (:139-140). Receivers are matched by leaf name only — `request` is never required. It returns at most ONE single-key dict (`{}` | `{'header':…}` | `{'state':…}`) and carries no line number.",
    "`_read` is pure apart from the shared `_MODS` parse cache (reached via `_text`:58-65 → `S.resolve_const` _a3_forms_settings.py:310 → `P._resolve`/`P._mod`). It needs no endpoint node, no forms feed, no archmap, no middleware stack.",
    "`_state_setter(repo, amap, attr)` needs only `amap['app_middleware']` (rows `{cls,file,line,scope,order}` from `_a3_code.parse_app_middleware`, _a3_code.py:2377-2413, where `scope` is always the floor 'all'). It resolves the class (P._resolve, 2 import hops), takes `Cls.dispatch` else `Cls.__call__` (:151, the two names hardcoded, not F.MIDDLEWARE_METHODS), pre-binds locals assigned from a header read (`held`, :154-160), then returns the FIRST `X.state.<attr> = …` write as `{header, set_at, via}` (:161-165).",
    "`_state_setter` walks with `_own()` (:45-51), a LIFO stack walk — its 'first' hit is NOT source order (contrast `_key`:171, which sorts by lineno). It reads no call level (a helper that sets state is missed), no dependency or handler, and misses `@app.middleware('http')` functions and `FastAPI(middleware=[…])` (named, never placed, by `_a3_forms_mw._unscanned`:242-263).",
    "`repeat{}.key` final keys are `name` · `carrier` ('header'|'state') · `read_at` · optional `through` · optional `set_at` (assembled in `_key`:183-191, `local` popped in `_repeat`:280). `_state_setter`'s `via` (the middleware class) is computed and DISCARDED — the feed carries no `set_by` today.",
    "`_key` only accepts a handler local whose assigned value is an `ast.Call` (:175-176), so `key = request.state.idem` (a bare Attribute) is never seen; it follows ONE call level via `R.callee` (:178-180) and `read_at` is the assignment line in the handler, not the read site.",
    "The contract arm imports F · R (_a3_forms_reach) · S (_a3_forms_settings) · P (_a3_paths) · PYDI (_a3_stacks_pydi) at :20-24. It does NOT import `_a3_forms_ids` and mints no ids — it reuses the `x:` ids stamped by `_a3_forms_build._write_ids` (:186-211). It also does NOT use `P._events`, so the guard/try/after stacks are unavailable to its readers; it uses its own `_own()` and `_bodies()`.",
    "Only `_a3_forms_build.py` imports `_a3_forms_contract` (:33,:43). No arm imports another arm; shared code lives in leaf modules imported at column 0 in `_a3_forms_build.py` (:23-26,29,34, each `# noqa: F401`) so `propagate.sh` (:25,33-45) lands them on twins.",
    "`F.LOCK_CALLS` (_a3_forms.py:294) has exactly one consumer: `_a3_forms_task.py:116` in `tasks_part`, collecting `{at, call}` for every Call in the task body whose callee leaf is in the set, placed at `form['concurrency']['locks']` (:122). Leaf-name match only, no receiver check, no with/async-with distinction; endpoints read no locks today.",
    "NOT FOUND anywhere in the generators: `ContextVar`/`contextvars`; any reading of `@lru_cache` (one comment only, `_a3_forms_mw.py:341`); any `.add_task(` reading — `BackgroundTasks` appears only inside `F.INJECTED_TYPES` (_a3_forms.py:131) to EXCLUDE the parameter from validation (`_a3_paths._validated_params`:210, `_a3_forms_schema._param`:702).",
    "A counter/limiter built in a middleware `__init__`, and a setting read there, are read TODAY only on the rate path: `_rate` (:379-428) needs a 429 middleware row whose pred matches `^not\\s+self\\.(\\w+)\\.\\w+\\((.*)\\)$` (:388), then `S._self_assigns` (_a3_forms_settings.py:202) + `_arg` (:366-376) + `S._typed_local`/`S.fields`. Any other shape is invisible.",
    "A new kinds part must be added in four places: `F.ARMS['kinds']['parts']` (_a3_forms.py:303-304), `_a3_forms_mw.PARTS` (:27, published by `run.parts = PARTS` :516 and read by `_a3_forms_build._built`:106-111), a tuple in `F.ARM_STAGES` (:318-322 — kinds runs in TWO stages, before and after effects/contract), and, if it depends on another unit, `F.ARM_NEEDS` (:324-327).",
    "Kinds part builders have the signature `(repo, forms, amap) -> (dict, findings, stats)` (`_a3_forms_fn.py:139`, `_a3_forms_task.py:85,199`), dispatched at `_a3_forms_mw.py:504-508`; a runner can report one part absent with `{'absent': {part: reason}}` (_a3_forms_build.py:286-290) or restore the whole feed with `{'present': False, 'reason'}` (:276-277).",
    "Reusable inputs for a middleware→dependency→handler walk already in the feed: `forms['middleware']` (per class: registered_at, order{registered,runs,of}, outer/inner, method fid, pass_through, exits — _a3_forms_mw.py:276-322), `forms['dependencies']` (kind, at, exits, effects, teardown, subdeps, applies_to — :406-483), and the root roster `_a3_forms_fn.roots` (:104-135) + `R.bfs` at `F.OPTIONS['reach_depth']`=4.",
    "Name collision to avoid confusing: `tests/inflight/run.sh` already exists and belongs to the BOARD's in-flight projection (`skills/gabe-cc-update/scripts/write-inflight.py`), not to element forms."
   ],
   "open_questions": [
    "Should the lifted readers live in a new shared leaf (e.g. `_a3_forms_state.py`) imported at column 0 by both `_a3_forms_contract` and the new kinds part, or should the contract arm keep private copies? The house rule ('an arm imports no arm', _a3_forms_contract.py:11-12; leaves imported at column 0 in _a3_forms_build.py:23-34 for propagate.sh) points at the leaf, but that means editing the contract arm's call sites in the same pass.",
    "`_read` returns one row with no line number; in-flight rows need `set_at`/`read_at[]` per site. Is the intended shape a new multi-hit reader beside `_read` (keeping `_read`'s single-hit contract for repeat{}), or a widened `_read` whose first element the contract arm takes?",
    "`_state_setter` returns only the FIRST setter and only a header-sourced value, in LIFO walk order. For the general reading, should a state write with a non-header value be recorded with `set_by` and no source (`dies: 'with the answer'`), and should the walk be lineno-sorted — which would change the value `repeat{}.key.set_at` reports on a middleware with two writes to one attribute?",
    "Which stage should an `inflight` part run in — the early kinds stage (with middleware/dependencies) or the late one (with functions/tasks/handlers)? A process-scoped row that wants the effects `steps{}` or the reach roster implies the late stage plus an `ARM_NEEDS` entry; reading only the middleware stack implies the early one.",
    "`mw['scope']` is always the floor 'all' (_a3_code.py:2381-2383), so 'per endpoint where it applies' for a middleware-set state row is every endpoint unless the pass-through arms exempt it (`_a3_forms_mw._exempts`:197-201). Should the new rows reuse the middleware exits' `applies_to`/`exempt` accounting (:311-313) or state 'all' plainly?"
   ],
   "report": "## The readers to lift — raw facts\n\nAll paths below are absolute under `/home/khujta/projects/gabe_lens/templates/center/generators/`. Read-only pass: no battery, build, dry run or program was executed.\n\n### 0 · Files and sizes\n- `_a3_forms_contract.py` — 525 lines (the CONTRACT arm, Slice 7).\n- `_a3_forms.py` — 331 lines (the slot REGISTRY, data only).\n- `_a3_forms_reach.py` — 74 · `_a3_forms_ids.py` — 123 · `_a3_forms_task.py` — 245 · `_a3_forms_mw.py` — 516 · `_a3_forms_build.py` — 426 · `_a3_paths.py` — 797 · `_a3_paths_read.py` — 374 · `_a3_forms_settings.py` — 463.\n\n### 1 · `_read()` — `_a3_forms_contract.py:125-141`\nSignature `_read(repo: Path, m, node) -> dict`. `m` is a `_a3_paths_read._Mod`; `node` is ANY ast node (a call's value, an assign's value, a whole `FunctionDef` — `_key` passes all three).\n\nBody, line by line:\n- `:128` `for n in ast.walk(node)` — breadth-first over every child, **returns on the first match**, so the result is walk-order-first, not source-order-first. No line number is recorded and **at most one row** comes back.\n- `:129-132` `ast.Call` whose `func` is an `Attribute` with `attr == \"get\"` and `P._leaf(n.func.value) == \"headers\"`, with ≥1 arg → `_text(repo, m, n.args[0])` → `{\"header\": <name>}`. `_leaf` returns the attribute name, so the receiver is never checked: `anything.headers.get(X)` matches, not only `request.headers.get(X)`.\n- `:133-134` `ast.Subscript` whose `.value` leaf is `\"headers\"` and whose slice resolves to text → `{\"header\": …}` (`_text` is called twice here).\n- `:135-138` `getattr(<x>.state, \"attr\", …)`: `func` is `Name(\"getattr\")`, `len(args) >= 2`, `P._leaf(n.args[0]) == \"state\"`, `args[1]` resolves to a str → `{\"state\": attr}`.\n- `:139-140` any `ast.Attribute` in `Load` context whose `.value` is an `Attribute` with `attr == \"state\"` → `{\"state\": n.attr}`. Again receiver-agnostic (`self.state.x` and `foo.bar.state.x` match).\n- `:141` `return {}`.\n\nReturn shape: `{}` | `{\"header\": str}` | `{\"state\": str}` — one key, no site, no ordering. `_text` (`:58-65`) resolves a literal `str` or a module constant through `S.resolve_const` (`_a3_forms_settings.py:310-323`, which follows one import hop and refuses a name the module reassigns or augments).\n\n### 2 · `_state_setter()` — `_a3_forms_contract.py:144-166`\nSignature `_state_setter(repo: Path, amap: dict, attr: str) -> dict | None`.\n- `:146` iterates `amap[\"app_middleware\"]` in the list's own order. That list is `_a3_code.parse_app_middleware` (`_a3_code.py:2377-2413`): rows `{cls, file, line, scope, order}`, sorted by `(file, line)` then stamped with `order`; **`scope` is always the floor `\"all\"`** (docstring `_a3_code.py:2381-2383`). `_state_setter` does NOT re-sort by `order` (`_a3_forms_mw.middleware_forms:267` does).\n- `:147` `P._resolve(repo, P._mod(repo, mw[\"file\"]), mw[\"cls\"])` — module parse + name resolution following up to 2 import hops (`_a3_paths_read.py:103-122`).\n- `:148-149` skip when unresolved or the name is not a class in that module.\n- `:151` `cm.defs[f\"{cls}.dispatch\"]` else `cm.defs[f\"{cls}.__call__\"]` — the two names are hardcoded here in that order (the registry's own `F.MIDDLEWARE_METHODS`, `_a3_forms.py:136`, is not used).\n- `:154-160` **pass 1** — over `_own(node)`, every `Assign`/`AnnAssign` whose value `_read()`s as a `header` binds `held[local_name] = header`. A value that reads as `state` is dropped here. A name assigned twice keeps whichever the `_own` walk sees last.\n- `:161-165` **pass 2** — an `Assign` with a target `<x>.state.<attr>` where `t.attr == attr` and `t.value.attr == \"state\"` → returns `{\"header\": <the value's own `_read` header, else `held[value.id]` when the value is a bare Name>, \"set_at\": f\"{cm.rel}:{n.lineno}\", \"via\": cls}`. **First hit wins**, and `_own` (`:45-51`) is a LIFO stack walk, so \"first\" is reverse-ish, not source order (contrast `_key:171`, which sorts by `lineno`).\n- `:166` `return None`.\n\nWhat it does not see: dependencies, handlers, helper methods the dispatch calls (no call level at all — unlike `_key`), `@app.middleware(\"http\")` functions and `FastAPI(middleware=[…])` registrations (those are named, never placed, by `_a3_forms_mw._unscanned:242-263`), and any write whose value is not a header read or a Name previously bound to one (then `header` is `None`).\n\n### 3 · Where the name filter actually sits\n`_a3_forms_contract.py:26-27` `CT = F.CONTRACT` · `_KEY = re.compile(CT[\"key_name\"])`, with `key_name = r\"(?i)idempot\"` at `_a3_forms.py:263`.\nThe regex is used **exactly once**, at `_a3_forms_contract.py:192`, inside `_key()`:\n```\nif _KEY.search(str(out.get(\"name\") or got.get(\"state\") or \"\")):\n    return out\n```\nSo **`_read()` and `_state_setter()` are already name-filter-free** — neither touches `_KEY`. The gate is a post-filter on the assembled row, and it tests the HEADER name first (`out[\"name\"]`), falling back to the raw state attribute only when no header was resolved: a `request.state.idem_key` set from a header called `X-Request-Id` is tested on `X-Request-Id`.\n\n### 4 · How `repeat{}.key` is assembled\n`_key()` — `_a3_forms_contract.py:169-194`:\n- `:171` the handler's `Assign` statements from `_own(fn)`, **sorted by lineno**.\n- `:172-173` single `Name` target only.\n- `:174` `Await` unwrapped.\n- `:175-176` the value **must be an `ast.Call`** — `key = request.state.idem` (a bare Attribute) is never picked up; only `key = request.headers.get(...)`, `key = getattr(request.state, \"…\", None)` or `key = helper(request)`.\n- `:177` `_read` on the call; `:178-180` if empty, ONE call level via `R.callee(...)` then `_read` over the callee's whole def node.\n- `:183` `out = {\"local\": <target name>, \"read_at\": f\"{m.rel}:{st.lineno}\"}` — `read_at` is the **assignment line in the handler**, not the header/state read site.\n- `:184-185` header → `name=<header>`, `carrier=\"header\"`.\n- `:186-191` state → `_state_setter(repo, amap, got[\"state\"])`; `name=setter[\"header\"]` (may be `None`), `carrier=\"header\"` when a header was found else `\"state\"`, `through=f\"request.state.{attr}\"`, and `set_at=setter[\"set_at\"]` when a setter was found.\n- `:192` the filter · `:194` `None`.\n\n`_repeat()` — `:276-284` returns `{\"state\": \"missing\"|\"n/a\"|\"defined\", \"key\": …, \"required\": …, \"claims\": …}`; `state` is `missing` on a mutating method (`F.MUTATING_METHODS`, `_a3_forms.py:95`) and `n/a` otherwise (`:279`). `local` is **popped** off the key dict (`:280`) and used to find the precondition that requires it (`:281`, matching `preconditions` rows with no `via` and pred `\"<local> is None\"` / `\"not <local>\"`).\n\n**Final `repeat{}.key` keys: `name` · `carrier` (\"header\"|\"state\") · `read_at` · optional `through` · optional `set_at`.** Note `_state_setter`'s `via` (the middleware class) is computed and then **discarded** by `_key` — the feed today carries no `set_by`. That is exactly the field the new rows want.\n\n`claims` comes from `_claims` (`:241-273`, follows the local into callees to `CT[\"follow_depth\"]`=2 by position/keyword, matching `Model(col=<local>)` against unique columns from `uniques()` `:85-121`), with `_arms` (`:207-221`) and `_idioms` (`:224-229`). `race` is read out of the already-built `forms[\"steps\"]` (`:254`) — the effects arm's `st:` rows (`I.ident(\"st\", …)` at `_a3_forms_effects.py:395`).\n\n### 5 · Helper leaves the contract arm relies on\nImports, `_a3_forms_contract.py:20-24`: `_a3_forms as F` · `_a3_forms_reach as R` · `_a3_forms_settings as S` · `_a3_paths as P` · `_a3_stacks_pydi as PYDI`. **It does not import `_a3_forms_ids`** and mints no ids of its own — `_auth`/`_rate`/`_responses` reuse the `x:` ids already stamped on `produced` rows by `_a3_forms_build._write_ids` (`_a3_forms_build.py:186-211`).\n\n- `R.callee(repo, m, qual, fn, call)` — `_a3_forms_reach.py:25-37`; wraps `P._callee` and adds `self.m()` → `Class.m`, memoised on `id(call)` in `_CALLEES` (`:18`, cleared by `reset_caches`). Used at `:179` and `:266`. `R.bfs` (`:40-74`) is **not** used by contract.\n- `P._mod` / `P._resolve` / `_Mod` — `_a3_paths_read.py:68-122` (per-file table of `imports`, `defs` keyed `fn` and `Class.method`, `classes`, `assigns`, `consts`; `_MODS` cache keyed `(repo, rel)`).\n- `P._leaf` (`_a3_paths_read.py:42-47`) · `P._unp` (`:50-54`) · `P._literal` (`:57-65`).\n- `P._find_handler` (`_a3_paths.py:447-457`) — used by contract's own `_handlers` generator (`_a3_forms_contract.py:32-42`, yields `(key, variant, module, fn, decorator)`).\n- **`P._events` is NOT used by the contract arm** — it walks with its own `_own()` (`:45-51`) and `_bodies()` (`:197-204`), so the guard / try / after stacks that `_events`/`_walk` build (`_a3_paths_read.py:263-333`) are unavailable to `_read`/`_state_setter` as written. `_a3_forms_task.py:107` and `_a3_forms_fn.py` do use `P._events`.\n- `S.*` (`_a3_forms_settings.py`): `resolve_const:310` (through `_text`), and on the rate/response paths `_class:30`, `_chain:35`, `_is_settings:52`, `fields:127`, `_typed_local:171`, `_self_assigns:202`, `locals_once:433`, `is_path:442`.\n- `PYDI._ann_name` at `:331` (auth only).\n\n### 6 · Calling them from another module without the name filter\n- **`_read(repo, m, node)` is pure** in the sense that matters: it needs only a `Path`, a `_Mod` and an ast node. No forms feed, no endpoint node, no archmap, no middleware stack. Its only side effect is the shared `_MODS` parse cache through `S.resolve_const` → `P._resolve` → `P._mod`. Deterministic. **Limits to widen for a general in-flight reading:** it returns at most ONE row, carries no `lineno`, does not say whether the hit was a read or a write context (the `Load` check at `:139` excludes stores, so the `_state_setter` pass has its own target matcher), and its receiver checks are leaf-name-only.\n- **`_state_setter(repo, amap, attr)`** needs `amap[\"app_middleware\"]` and nothing else — not the forms feed, not the endpoint. It is per-attribute and returns ≤1 setter, only from a middleware class's `dispatch`/`__call__`, only when the value traces to a header. Also: since every `scope` is `\"all\"`, a middleware-set state row applies to every endpoint unless the pass-through arms say otherwise (`_a3_forms_mw._pass_through:173-201`, `_exempts:197-201`, `applies_to`/`exempt` at `:311-313`).\n- **`_key(repo, amap, m, fn)`** is the only one that needs the handler node, and it is the one carrying the filter.\n- Module-level state in `_a3_forms_contract.py` is `CT` (`:26`), `_KEY` (`:27`), `_WRITES` (`:28`) — all immutable; importing the module is side-effect-free beyond its own imports.\n- **Import direction is the real constraint.** The module docstring states \"the arm … imports no arm\" (`:11-12`), and today only `_a3_forms_build.py:33,43` imports it. `_a3_forms_mw.py:18-19` imports `_a3_forms_fn as FN` and `_a3_forms_task as TK` — its own parts, never a sibling arm. The house pattern for shared code is a LEAF module imported at column 0 in `_a3_forms_build.py` (`:23-26,29,34`, each `# noqa: F401`) so `propagate.sh` lands it on a twin — `propagate.sh:25,33-45` copies a NEW generator only when some other generator imports it by a column-0-or-indented `import <mod>`. So lifting `_read`/`_state_setter` into e.g. `_a3_forms_state.py` and having both `_a3_forms_contract` and the new kinds part import it matches the existing shape; importing `_a3_forms_contract` from `_a3_forms_mw` would be the first arm→arm import in the family.\n\n### 7 · Wiring a new `inflight` part into the KINDS arm (facts, not advice)\n- Parts roster: `F.ARMS[\"kinds\"][\"parts\"]` = `(\"middleware\",\"dependencies\",\"functions\",\"tasks\",\"handlers\")` — `_a3_forms.py:303-304`; mirrored as `_a3_forms_mw.PARTS` (`:27`) and published via `run.parts = PARTS` (`:516`), which `_a3_forms_build._built:106-111` reads to decide \"not built yet\".\n- Stage order: `F.ARM_STAGES` (`_a3_forms.py:318-322`) runs kinds **twice** — `(\"kinds\", (\"middleware\",\"dependencies\"))` first, `(\"kinds\", (\"functions\",\"tasks\",\"handlers\"))` after `effects` and `contract`. A part listed in no stage never runs (`_a3_forms_build.py:245-248`).\n- Hard needs: `F.ARM_NEEDS` (`:324-327`) — e.g. `\"paths.paths\": (\"kinds.middleware\",)`, `\"kinds.functions\": (\"effects\",)`. Soft: `F.ARM_SOFT` (`:329`).\n- Runner protocol: `run(forms, ctx)`; `ctx` carries `amap` (a private deepcopy), `repo`, `cfg`, `selected`, `parts`, `ok`, `soft` (`_a3_forms_build.py:265-267`). Return `{\"version\",\"stats\"}`; `{\"absent\": {part: reason}}` marks one part absent while the stage stands (`:286-290`); `{\"present\": False, \"reason\"}` restores the whole feed snapshot (`:276-277,291-295`). Each part builder in the kinds arm has the signature `(repo, forms, amap) -> (dict, findings, stats)` (`_a3_forms_fn.py:139`, `_a3_forms_task.py:85,199`) and is dispatched at `_a3_forms_mw.py:504-508`.\n- Top-level blocks already written this way: `steps{}` `middleware{}` `dependencies{}` `functions{}` `tasks{}` `handlers{}` `schemas{}` `conditions{}` `settings{}` `models{}` `migrations{}` `mirrors{}` `test_cases{}` `frontend{}`. The orchestrator's `_added` diff (`:136-145,278,314-328`) credits and, for needed-only arms, deletes keys.\n- Already-built inputs a later kinds stage can read: `forms[\"middleware\"]` carries per-class `registered_at`, `order{registered,runs,of}`, `outer`/`inner`, `method` fid `file::Cls.dispatch`, `pass_through`, `exits` (`_a3_forms_mw.py:276-322`); `forms[\"dependencies\"]` carries per-fid `kind`, `at`, `exits`, `effects`, `teardown`, `subdeps`, `applies_to` (`:406-483`). Reach roster: `_a3_forms_fn.roots(repo, forms, amap)` (`:104-135`) seeds handlers + dependency fns + task roots + event handlers, walked by `R.bfs` to `F.OPTIONS[\"reach_depth\"]`=4 (`_a3_forms.py:74`).\n- Ids leaf if rows need them: `I.ranked(prefix, [(tuple, position)], audit)` (`_a3_forms_ids.py:92-94`), `I.ident` (`:22-25`), `I.fn_at` (`:42-59`), `I._line` (`:33-35`).\n- Findings would have to be registered in `F.FINDINGS` (`_a3_forms.py:147-218`, each with `arm`/`slot`/`pulse` `nag|count`) to reach pulse S20 / review FORM DRIFT — an analysis-only part adds none and leaves S20 untouched.\n- **Name collision:** `tests/inflight/run.sh` already exists and is the BOARD's in-flight projection battery (`skills/gabe-cc-update/scripts/write-inflight.py`, PLAN.json + git), unrelated to a forms `inflight` part. The forms batteries are `tests/forms-core`, `forms-paths`, `forms-kinds`, `forms-short`, `forms-effects`, `forms-contract` (484 lines), `forms-tests`, `forms-frontend`, `tests/element-forms`.\n\n### 8 · `LOCK_CALLS` today\n- Defined `_a3_forms.py:294`: `frozenset({\"lock\", \"acquire\", \"Lock\", \"RedisLock\", \"advisory_lock\", \"try_advisory_lock\"})`, in the Slice-8 block beside `TASK_KW:289`, `TASK_CONCURRENCY_KW:292`, `RETRY_CALLS:293`, `BUS_PUBLISH:295`, `BUS_REGISTER:296`, `CONCURRENT_CALLS:297`.\n- **One use only**, `_a3_forms_task.py:116` inside `tasks_part`:\n  `locks = [{\"at\": f\"{m.rel}:{n.lineno}\", \"call\": P._unp(n.func, 60)} for n in _own(node) if isinstance(n, ast.Call) and P._leaf(n.func) in F.LOCK_CALLS]`\n  placed on the task form at `form[\"concurrency\"][\"locks\"]` (`:122`), beside `F.TASK_CONCURRENCY_KW` keywords. Scope: the TASK function's own body (`_own`, `:29-35` — outside nested defs, but it does descend into `with` bodies and context expressions), matched by **callee leaf name only** — no receiver check, no context-manager vs plain-call distinction, no `async with`. Endpoints read no locks today.\n- `CONCURRENT_CALLS` is used at `_a3_forms_task.py:163` (`_bus`, execution concurrent vs sequential).\n\n### 9 · What is NOT read anywhere today (searched across `*.py` and `*.mjs` in the generators dir)\n- `ContextVar` / `contextvars` / a `.set()`/`.reset()`/`.get()` token pair — **not found**. No generator mentions them.\n- `lru_cache` / `functools.cache` — **one mention, a comment only**: `_a3_forms_mw.py:341` (\"`@lru_cache` on a dependency is not one [a gate decorator]\"). Nothing records it as a fact.\n- `BackgroundTasks` — appears only as a member of `F.INJECTED_TYPES` (`_a3_forms.py:131`), used to EXCLUDE such a parameter from validation at `_a3_paths._validated_params:210` and `_a3_forms_schema._param:702`. No `.add_task(` reading anywhere — **not found**.\n- A counter/limiter built in a middleware `__init__` — read today ONLY on the rate path: `_a3_forms_contract._rate:379-428` resolves `self.<limiter>` via `S._self_assigns` (`_a3_forms_settings.py:202`) and the constructor arguments via `_arg` (`:366-376`), but only when a 429 middleware row exists whose `pred` matches `^not\\s+self\\.(\\w+)\\.\\w+\\((.*)\\)$` (`:388`). A limiter with no 429 row, or any other pred shape, is invisible.\n- A setting read once inside a middleware `__init__` — read only inside that same `_arg` path (`:370-375`, `S._typed_local` + `S._is_settings` + `S.fields`, yielding `state: \"default\"` with `env` and the field's declaration site via `_field_at:358-363`). There is no general \"settings read in `__init__`\" pass.\n- `request.state` is read by no module other than `_a3_forms_contract.py` (grep for `.state` across the generators returns only this module plus unrelated `\"state\"` value keys).\n"
  },
  {
   "key_facts": [
    "Registration is five edits: _a3_forms.py:303 ARMS['kinds']['parts'] (+part_slices at :304), _a3_forms.py:318-322 ARM_STAGES (kinds appears TWICE — stage 1 at :319 middleware+dependencies, stage 2 at :321 functions+tasks+handlers), optional _a3_forms.py:324-327 ARM_NEEDS keyed 'kinds.inflight', _a3_forms_mw.py:27 PARTS + :516 run.parts, and the dispatch in _a3_forms_mw.py:504-508",
    "A part builder today receives only (repo, forms, ctx['amap']) from _a3_forms_mw.py:506 and returns (block, findings, stats); the full ctx (amap private deepcopy · repo · cfg · selected · parts · ok · soft) stops at run() (_a3_forms_build.py:265-267)",
    "The shared per-endpoint iterator is _a3_forms_mw._handlers(repo, forms) at :104-115, yielding (key, v, m, fn, dec); effects/switches/contract all use it",
    "A part reports absent for itself by returning {'absent': {'<part>': '<why>'}} (_a3_forms_build.py:286-289; example _a3_forms_schema.py:978-981) — that part alone reads present:false and is NOT added to ok; a RAISE or an unserializable value restores the whole stage's snapshot (:291-300)",
    "Middleware __init__ is ALREADY parsed twice: _a3_forms_contract._rate:388-401 reads a limiter built in __init__ via S._self_assigns + resolves constructor args to settings defaults with _arg():364-375, and _a3_forms_settings.resolve_self_attr:262-309 resolves self.<attr> to the settings fields behind it",
    "request.state is read by _a3_forms_contract._read:125-141 (header get/subscript, getattr(request.state,'attr'), any <x>.state.<attr> Load) and _state_setter:144-166 (walks every app_middleware dispatch/__call__ for an Assign to <x>.state.<attr>); both are name-agnostic — only _key():192 applies the r'(?i)idempot' filter from _a3_forms.py:263",
    "parse_app_middleware (_a3_code.py:2377-2415) records only {cls, file, line, order, scope:'all'} — no add_middleware keyword arguments; @app.middleware('http') and FastAPI(middleware=[…]) are unread and only NAMED by _a3_forms_mw._unscanned:242-263",
    "'One call down' is _a3_forms_mw._commits:378-403 (P._events → R.callee → scan the callee's own events one level); the dependency chain is dependency_forms.chain:449-468 over P._dep_target/P._dep_node (_a3_paths.py:251-267); the depth-4 breadth walk is _a3_forms_reach.bfs:40-74",
    "NOT FOUND anywhere in templates/center/generators: ContextVar/contextvars (zero hits), lru_cache/cache detection (only the prose comment _a3_forms_mw.py:341), BackgroundTasks handling beyond being SKIPPED as F.INJECTED_TYPES (_a3_forms.py:131, skipped at _a3_paths.py:210 and _a3_forms_schema.py:702), add_task (zero hits), and any scope/lifetime vocabulary",
    "F.LOCK_CALLS (_a3_forms.py:294) is read ONLY inside task bodies (_a3_forms_task.py:116) — never in a handler, dependency or middleware",
    "Id prefixes taken: x: g: r: b: p: sw: st: c: case:schema: plus frontend <l>-<sha10> letters x e c p r. k: w: e: were planned for claim/rate-window/service-raise and STRUCK at §A4 V33 (never minted — those rows are addressed by their `at`), which is the precedent for minting no id at all for analysis rows",
    "FINDINGS entries are {arm, slot, pulse: 'nag'|'count', says} at _a3_forms.py:147-218; emitted findings are {'id', 'slot', <subject keys>}. ALL kinds-arm findings land TOP-LEVEL (_a3_forms_mw.py:509-510, confirmed by goldens/expected.json:21,25) even though amendment-1.md:96 prose says dependency findings sit on the endpoint",
    "pulse S20's form_drift.py reads only the endpoint pass's own `findings` key (:78-94) against hardcoded NAG/COUNT rosters at :37-38 — no arm finding reaches S20 today",
    "Line counts: _a3_paths.py 797 (3 under the 800 cap) · _a3_forms_mw.py 516 · _a3_forms_settings.py 463 · _a3_forms_build.py 426 · _a3_forms.py 331 · _a3_forms_task.py 245 · _a3_forms_fn.py 216; already over: _a3_forms_schema.py 1001, _a3_code.py 3024, build_center_a3.py 2581",
    "Top-level blocks are written as forms['<name>'] inside the runner (middleware · dependencies · functions · tasks · handlers · conditions · schemas · models · migrations · settings · mirrors · steps · test_cases · frontend); per-endpoint keys are set on `v` from MW._handlers (switches · repeat · auth · rate · responses · framework_exits · paths · returns · branches)",
    "tests/forms-core/run.sh:500 asserts all(v['kinds_k'] == ['tasks']) — the fake writer records list(ctx['parts']) and the last kinds stage wins, so adding inflight to the SECOND kinds stage breaks that assertion while adding it to the first does not; :786 pins F.KINDS['endpoint'] exactly and :788 requires every non-endpoint kind slot to carry 'block'",
    "A new leaf module must be imported at column 0 in _a3_forms_mw.py (the _a3_forms_fn/_a3_forms_task pattern at :18-19) so propagate.sh:43 lands it on a twin as a hard dependency",
    "forms.json is written at build_center_a3.py:2199-2217 (and re-written for the frontend arm at :2304-2314); selection is center.config.json#forms_arms or GABE_FORMS_ARMS, parsed by _a3_forms_build.selection:57-70"
   ],
   "open_questions": [
    "Which kinds STAGE should inflight run in? Stage 1 (_a3_forms.py:319, beside middleware+dependencies) sees no steps{}/paths[]/switches[]; stage 2 (:321) sees everything effects and contract wrote but breaks tests/forms-core/run.sh:500 as written. If in-flight rows are to name the paths a value is read on, stage 2 plus ARM_NEEDS 'kinds.inflight': ('effects',) or ('paths',) is required.",
    "The five kinds part builders have three different return shapes (middleware 3-tuple with `unscanned`, dependencies 2-tuple, functions/tasks/handlers 3-tuple with stats). A part that writes BOTH a top-level block and per-endpoint keys fits none of them — it needs either its own `if \"inflight\" in parts:` block (the middleware/dependencies pattern) or a convention decision.",
    "No existing lifetime vocabulary exists in the feed, so `scope: request|process` and `dies: 'with the answer'|'with the server process'|'unknown'` are new words. The §A4 state-word sweep ruled `unknown` · `absent` · `beyond one level` as the honest-floor words — whether `dies: 'unknown'` satisfies that sweep or needs a reason field beside it is unruled.",
    "Is inflight a new PIECE kind in F.KINDS (its own slot rows with a `block`) or a new ENDPOINT slot (a U-number in F.KINDS['endpoint'] with `block` + `arm`)? The latter forces an edit to the exact-set assertion at tests/forms-core/run.sh:786 and implies a U-id that the amendment has not allocated.",
    "The brief says ANALYSIS, never a grade — so presumably no FINDINGS entry. But every other arm reports `stats.findings`, and the kinds runner always emits `stats['findings'] = {}` (_a3_forms_mw.py:492). Whether an analysis part should stay out of the findings channel entirely, or record zero-weight `pulse: 'count'` rows, is unruled.",
    "A general request.state census (every write and every read, middleware → dependency → handler → one call level) is a superset of _state_setter, which today takes one attribute name, reads only app middleware, and returns the first match. Whether to generalise _state_setter in place (it is the contract arm's, and contract runs at a different stage) or to write a second walker in the new leaf is a design call with a duplication cost either way.",
    "amendment-1.md:96 (§A4 V38) and _a3_forms_mw.py:509-510 disagree about where the kinds arm's endpoint-scoped findings live. If the inflight part emits anything endpoint-scoped, which of the two is binding needs the operator's word.",
    "Would the new block be surfaced to gabe-map (map_census kind=forms / touches `form`) and to a drift detector, as every other forms block is? mapquery.py reads forms.json lazily and needs no registration for a new key, but nothing would READ the block until a surface is added — and Phase 6 (station surfaces) is explicitly deferred behind the generation."
   ],
   "report": "## The kinds arm — how to add an `inflight` part (read-only survey)\n\nAll paths relative to `/home/khujta/projects/gabe_lens/templates/center/generators/` unless stated.\n\n---\n\n### 1 · Exact registration steps for a part named `inflight`\n\nThe kinds arm is ONE runner (`_a3_forms_mw.run`) that dispatches to per-part builders. Five edits register a sixth part:\n\n**(a) `_a3_forms.py:302-313` — the arm registry.** Add the part name to the tuple:\n```\n\"kinds\": {\"slice\": 3, \"parts\": (\"middleware\", \"dependencies\", \"functions\", \"tasks\", \"handlers\"),\n          \"part_slices\": {}},\n```\n`parts` is what `units_of()` expands (`_a3_forms_build.py:77-82`) and what the envelope's `arms.kinds.parts{}` enumerates. `part_slices` maps a part to the slice number printed in `\"not built yet (slice n)\"`; absent → `F.ARMS[\"kinds\"][\"slice\"]` = 3 (`_a3_forms_build.py:257`). A new part gets `part_slices: {\"inflight\": <n>}`.\n\n**(b) `_a3_forms.py:318-322` — `ARM_STAGES`.** The kinds arm appears TWICE, as two stages:\n- `:319` `(\"kinds\", (\"middleware\", \"dependencies\"))` — runs first, before `paths.returns`\n- `:321` `(\"kinds\", (\"functions\", \"tasks\", \"handlers\"))` — runs after `effects` and `contract`\n\n`inflight` must be added to one of them. Which one is a real decision: stage 1 sees no `steps{}`/`paths[]`/`switches[]`; stage 2 sees everything the earlier arms wrote. A part that wants to say \"this in-flight value is read on path p:… \" belongs in stage 2.\n\n**(c) `_a3_forms.py:324-327` — `ARM_NEEDS`, only if it reads another arm's output.** Keys are units: `\"kinds.functions\": (\"effects\",)` is the precedent (`kinds.functions` reads `forms[\"steps\"]`). `_needs()` (`_a3_forms_build.py:85-90`) looks up BOTH `\"kinds.inflight\"` and `\"kinds\"` — so a key on the bare arm name would apply to every kinds part; use the dotted unit. `ARM_SOFT` (`:329`) is the \"only when also selected\" table.\n\n**(d) `_a3_forms_mw.py:27` — `PARTS`, and `:516` `run.parts = PARTS`.** `_built()` (`_a3_forms_build.py:106-111`) reads `getattr(runner, \"parts\", None)`; a part named in `F.ARMS` but not in `run.parts` reports `\"not built yet (slice n)\"` and pulls in nothing. Both lines must list `inflight`.\n\n**(e) `_a3_forms_mw.py:504-508` — the dispatch loop.** Today:\n```python\nfor part, builder in ((\"functions\", FN.functions_part), (\"tasks\", TK.tasks_part), (\"handlers\", TK.handlers_part)):\n    if part in parts:\n        forms[part], got, st = builder(repo, forms, ctx[\"amap\"])\n        found += got\n        stats[part] = st\n```\nAn `inflight` builder that also writes per-endpoint keys does NOT fit this triple-return loop (it returns one top-level block). Either (i) add an explicit `if \"inflight\" in parts:` block above the loop, as `middleware` (`:492-498`) and `dependencies` (`:499-503`) do, or (ii) keep the loop and have the builder mutate `v[...]` itself while returning the top-level block. `middleware_forms` returns a 3-tuple `(block, findings, unscanned)`; `dependency_forms` a 2-tuple; `functions_part`/`tasks_part`/`handlers_part` a 3-tuple `(block, findings, stats)` — there is no single convention, only the runner's own unpacking.\n\n**(f) new leaf module.** `_a3_forms_fn.py` and `_a3_forms_task.py` are the precedent: a leaf imported at COLUMN 0 in `_a3_forms_mw.py:18-19` (`import _a3_forms_fn as FN` / `import _a3_forms_task as TK`). `propagate.sh:43` greps `^\\s*(import|from) <mod>\\b` across `$GEN/*.py` to decide a new generator is a hard dependency it must land on a twin — indented imports now count too (`propagate.sh:33-37`), but column 0 is what these leaves use.\n\n**(g) registry/doc touchpoints** (not code paths, but parity gates):\n- `_a3_forms.py:30-67` `KINDS` — a new PIECE kind gets an entry `{slot: {\"name\", \"block\"}}`; `tests/forms-core/run.sh:788` asserts every non-endpoint kind's slot carries `\"block\"`. If instead this is a new ENDPOINT slot, `F.KINDS[\"endpoint\"]` gains an entry with `\"block\"` + `\"arm\"` keys (`:36-44` pattern) — and `tests/forms-core/run.sh:786` pins that set EXACTLY (`{\"U3\",\"U7\",\"K1\",\"U6\",\"U8\",\"U9\",\"U11\",\"U12\",\"U14\",\"K2\",\"K3\",\"K4\"}`), so it must be edited in the same commit. The per-endpoint `slots{}` dict written by `_a3_paths._form` (`_a3_paths.py:748-751`) carries U3/U7/K1 only — arm slots are named in the registry, never in `slots{}` (comment `_a3_forms.py:35`).\n- `templates/center/generators/README.md:60` — the arms table lists every `_a3_forms_*` leaf and what it writes.\n- `docs/design/element-forms/amendment-1.md:78-87` — the arm/parts table (`kinds | middleware · dependencies · functions · tasks · handlers | 3 · 8 | top-level maps of the same names`).\n- `/home/khujta/projects/gabe_lens/CLAUDE.md` — the Slice-8 sentence naming the kinds parts.\n\n---\n\n### 2 · What a part receives\n\nThe runner signature is `run(forms, ctx) -> {\"version\", \"options\", \"stats\"}` (`_a3_forms_build.py:37-43, 269`). `ctx` is built at `_a3_forms_build.py:265-267`:\n\n| key | value |\n|---|---|\n| `amap` | a `copy.deepcopy` of the archmap made once at `:235` — a runner can never move the archmap that c4/levels are built from |\n| `repo` | `Path(repo)` |\n| `cfg` | `center.config.json` dict (or `{}`) |\n| `selected` | `frozenset` of selected arm names |\n| `parts` | tuple of the part names THIS stage runs (ARM_STAGES order) |\n| `ok` | `frozenset` of units that already succeeded |\n| `soft` | tuple of selected+succeeded soft needs |\n\nThe kinds part builders today receive only `(repo, forms, ctx[\"amap\"])` — `cfg`, `ok`, `soft` and `selected` stop at `run()`. `forms` is the LIVE feed, so a part reads whatever earlier stages wrote: `forms[\"steps\"]` (effects), `v[\"paths\"]`, `v[\"switches\"]`, `v[\"produced\"]` with `id`s, `forms[\"conditions\"]`, `forms[\"middleware\"]`, `forms[\"dependencies\"]`.\n\nThe shared per-endpoint iterator is `_a3_forms_mw._handlers(repo, forms)` (`:104-115`) yielding `(key, v, m, fn, dec)` — `v` is the endpoint form (or a `variants[]` entry), `m` its `_Mod`, `fn` the handler `ast` node, `dec` the route decorator. `_a3_forms_effects.run:735`, `_a3_forms_switch.run:353` and `_a3_forms_contract.run:508` all iterate it; `_a3_forms_fn.py:23-33` keeps a private copy without `dec`.\n\n---\n\n### 3 · Isolation, present/absent, stats — the orchestrator's contract\n\n`_a3_forms_build.extend_backend` (`:221-352`):\n- `:264` snapshots the whole feed per stage (`copy.deepcopy`).\n- `:275` `json.dumps([forms, options, stats], sort_keys=True)` is the write's own test — an unserializable value (a `set`) is treated as a failure.\n- `:276-277` a runner returning `present: False` raises `_Absent`; `:291-295` restores the snapshot and every unit of that stage reads `present: false` with the reason.\n- `:296-300` any other exception restores the snapshot and records `\"error: <Type>: <msg>\"` capped at 200 chars (`_ERR_CAP`).\n- `:286-289` the per-PART absent channel: a runner returns `{\"absent\": {\"<part>\": \"<why>\"}}` and that part alone reads `present: false` with the reason while the rest of the stage stands — and it is NOT added to `ok`. `_a3_forms_schema.run:978-989` is the working example (`\"no BaseSettings class in the project\"`). NOTE: this channel does not restore anything; a part using it must not have written.\n- `:278` `_added(snapshot, forms)` records the key paths the stage added — a needed-only arm's additions are deleted before the write (`:316-328`) unless a selected arm wrote inside them.\n- `:170-183` `_containers` creates `arm_findings` at top level and on every endpoint/variant BEFORE any stage, so no arm owns the container; empties are dropped at `:329`.\n- `:304-310` an arm is `present` when any part is; partly-failed reads `\"partial — <part>: <reason>; …\"`.\n\nStats land as `arms.kinds.stats`. The kinds runner merges per-part stats as `stats[part] = st` (`_a3_forms_mw.py:507`) and always carries `stats[\"findings\"]` (`:492, 511-512`). The kinds runner returns NO `options` today (`:513`) — `_a3_forms_switch` and `_a3_forms_schema` do.\n\n---\n\n### 4 · Middleware classes and their `__init__` — what already parses\n\n**Registration.** `_a3_code.parse_app_middleware` (`_a3_code.py:2377-2415`) scans the api dirs and their parents for `app.add_middleware(Cls, …)` and emits `[{cls, file, line, order, scope: \"all\"}]` onto the archmap as `app_middleware`. It records ONLY the class name — none of the `add_middleware` keyword arguments. `@app.middleware(\"http\")` functions and `FastAPI(middleware=[…])` are NOT read; `_a3_forms_mw._unscanned` (`:242-263`) NAMES them (`file:line @app.middleware fn`) and the count rides `stats[\"unscanned_registrations\"]`.\n\n**Class resolution.** `_a3_forms_mw.middleware_forms` (`:266-334`) does `P._resolve(repo, P._mod(repo, mw[\"file\"]), cls)` → `(cm, cname)` (the class in its OWN file), then `cm.defs[f\"{cname}.dispatch\"]` or `.__call__` (`F.MIDDLEWARE_METHODS`, `_a3_forms.py:136`). `_Mod` (`_a3_paths_read.py:68-100`) stores every class method as `defs[\"Cls.method\"]` — so `cm.defs[f\"{cls}.__init__\"]` is directly available.\n\n**`__init__` bodies ARE already read — two precedents:**\n- `_a3_forms_contract._rate` (`:378-409`): `init = cm.defs.get(f\"{cls}.__init__\")` → `S._self_assigns(init, attr)` → resolve the assigned constructor's class → its `__init__` parameter names → `_arg()` per argument (`:364-375`), which resolves a `settings.X` argument to the field's DEFAULT with `env`, `state: \"default\"` and the field's `file:line`. **This is exactly the \"a counter or limiter built in a middleware `__init__`\" reading**, but today it is gated on a 429 middleware row whose `pred` matches `^not\\s+self\\.(\\w+)\\.\\w+\\((.*)\\)$` — i.e. it only fires for a limiter that refuses. A general counter/dict built in `__init__` is never read.\n- `_a3_forms_settings.resolve_self_attr` (`:262-309`): `self.<attr>` inside a class → the settings field(s) behind it, expanding one-return properties transitively, with `settings_class`, `expands`, `unresolved`. **This is exactly the \"a setting read once inside a middleware `__init__`\" reading.** Supporting leaves: `_self_assigns` (`:202-236`, every `self.<attr>` write in source order with a `conditional` flag), `_typed_local` (`:171`), `_is_settings` (`:52`), `fields` (`:127`), `_class`/`_chain` (`:30/:35`), `resolve_const` (`:310`), `terms` (`:353`), `locals_once` (`:433`), `is_path`/`path_match` (`:442/:448`).\n\n---\n\n### 5 · `request.state` — what exists today\n\n`_a3_forms_contract._read` (`:125-141`) finds, inside one node: `request.headers.get(\"X\")`, `request.headers[\"X\"]`, `getattr(request.state, \"attr\", …)`, and any `<x>.state.<attr>` in Load context → `{\"header\": …}` or `{\"state\": …}`.\n\n`_a3_forms_contract._state_setter` (`:144-166`) walks EVERY `app_middleware` class's `dispatch`/`__call__` with `_own()` (`:45-51`, nodes outside nested defs), first collecting locals bound from a header read, then finding an `Assign` whose target is `<x>.state.<attr>`; returns `{\"header\", \"set_at\", \"via\": cls}` for the FIRST match.\n\nLimits worth naming: it takes an attribute name as input (one at a time, no census of every `request.state` write), it only reads app middleware (never a dependency or the handler), it returns only the first setter, and both functions are gated downstream by `_KEY = re.compile(F.CONTRACT[\"key_name\"])` = `r'(?i)idempot'` (`_a3_forms.py:263`, applied at `_a3_forms_contract.py:192`). The reading machinery itself is name-agnostic; only `_key()` (`:169-194`) filters.\n\n---\n\n### 6 · Dependencies, walked one call down\n\nThree mechanisms, all reusable:\n- **The chain.** `_a3_forms_mw.dependency_forms.chain` (`:449-468`): `_dep_params` (`:338-375`) gives the dependencies FastAPI resolves in FastAPI's own order (route `dependencies=[…]`, then each parameter — `Depends()` default, `Annotated[…, Depends()]`, a module alias via `C._dep_aliases` — then, on a route handler only, its non-route decorators). Each is resolved with `P._dep_target` + `P._dep_node` (`_a3_paths.py:251-267`: a function, a class's `__init__`, an instance's `__call__`, or a security scheme). Each form is built ONCE with its own subdeps, cycle-guarded by `path`. `applies_to` counts endpoints including transitive subdeps (`:442-447, 470-478`).\n- **One call down.** `_a3_forms_mw._commits` (`:378-403`) is the canonical shape: iterate `P._events(node)` for `kind == \"call\"`; if the call's leaf is in the family, record it; else `R.callee(repo, nm, nqual, node, e[\"node\"])` (`_a3_forms_reach.py:25-37`, memoised, also resolves `self.m()`) and scan THAT callee's own events one level. A commit after the first `yield` says `when: \"teardown\"`.\n- **Inherited exits.** `P._deps` (`_a3_paths.py:269-300`) recurses to `_DEP_MAX` accumulating rows/escapes.\n\nA breadth walk to `F.OPTIONS[\"reach_depth\"]` (=4, `_a3_forms.py:73-74`) is `_a3_forms_reach.bfs` (`:40-74`) → `{\"reached\": {fid: {root_fid: {depth, via, site}}}, \"truncated\": n}`; it skips vendored center files via `G._is_center`. `_a3_forms_fn.roots` (`:45-75`) seeds it from handlers + dependency fids + `amap[\"task_roots\"]` + `amap[\"dispatch\"][\"dispatches\"]`.\n\n---\n\n### 7 · What is NOT in the tree (would be new)\n\n- **`ContextVar` / `contextvars`** — zero hits across `templates/center/generators/*.py`. Not found.\n- **`@lru_cache` / `@cache`** — the only mention is a prose comment at `_a3_forms_mw.py:341` (\"`@lru_cache` on a dependency is not one\"), and it is honoured only by structure: `_dep_params` reads decorators at `:370` under `if dec is not None`, i.e. route handlers only. No detection, no roster. `C._dec_name` (`_a3_code.py:2079-2086`) is the decorator-name reader; `_a3_fe.py:68-72` `_CACHE_CALLEES` is the precedent for an idiom roster held as data.\n- **`BackgroundTasks`** — appears ONLY in `F.INJECTED_TYPES` (`_a3_forms.py:131`), where it is used to SKIP the parameter: `_a3_paths._validated_params:210` and `_a3_forms_schema._param:702` both drop it. `add_task` has zero hits anywhere. Not found.\n- **A general `request.state` census** — not found (only the single-attribute lookup in §5).\n- **A process-scoped / lifetime vocabulary** — not found. There is no existing `scope: request|process` or `dies:` field anywhere in the forms feed.\n- **Locks** — `F.LOCK_CALLS` (`_a3_forms.py:294`) = `{lock, acquire, Lock, RedisLock, advisory_lock, try_advisory_lock}`, read ONLY inside a task body (`_a3_forms_task.py:116`, over `_own(node)`). Never in a handler, dependency or middleware.\n\n---\n\n### 8 · Ids\n\n`_a3_forms_ids.ident(prefix, parts)` = `<prefix>:<sha1(canonical json)[:10]>` (`:22-25`); `ranked(prefix, entries)` (`:92-94`) adds the ordinal `n` that ranks a row's position among the DISTINCT positions an identical tuple holds feed-wide. `fn_at(repo, at)` (`:42-59`) turns `file:line` into `file::qual` so a blank line never changes an id; `via_sym` (`:28-30`) strips ` @ file:line`.\n\n**Prefixes in use** (amendment-1.md:105-115 + code):\n\n| prefix | object | minted at |\n|---|---|---|\n| `x:` | produced exit | `_a3_forms_build.py:191` · `_a3_forms_mw.py:101` (minting for exits no endpoint row carries) · `_a3_forms_schema.py:947` (framework exits) |\n| `g:` | precondition | `_a3_forms_build.py:191` |\n| `r:` / `b:` | return / branch | `_a3_forms_paths.py:334-336` |\n| `p:` | path | `_a3_forms_walk.py:407` |\n| `sw:` | switch | `_a3_forms_switch.py:189, 307, 329` |\n| `st:` | effect step | `_a3_forms_effects.py:395` |\n| `c:` | catch | `_a3_forms_effects.py:700` |\n| `case:schema:` | a 422 case (not hashed) | `_a3_forms_schema.py:658` |\n| `<l>-<sha10>` | frontend rows, letters `x e c p r` | `_a3_fe_forms.py` / `_a3_fe_reason.py` / `_a3_fe_controls.py` |\n\n**Free.** `k:` · `w:` · `e:` were reserved for claim · rate window · service raise and **struck at §A4 V33** — planned, never minted in any of the four feeds, \"a claim, a limiter and a service raise are addressed by their `at`\". They are unused but carry a strike record. Frontend letters `b s a q m t` were likewise planned and struck (different namespace, dash-joined).\n\n**Do in-flight rows need ids?** The V33 ruling is the precedent: a row nothing else points AT is addressed by its `at`, and no id is minted. The rows as briefed (`{name, scope, set_at, set_by, read_at[], dies}`) are uniquely addressable by `(name, set_at)`. Mint only if another arm, the station or a drift detector must reference one row. If minted, `i:` and `fl:` are wholly unclaimed; `k:` is free-but-struck.\n\n---\n\n### 9 · The findings catalogue\n\n`_a3_forms.FINDINGS` (`:147-218`) — one entry per finding id:\n```python\n\"dependency-commits\": {\"arm\": \"kinds\", \"slot\": \"K3\", \"pulse\": \"count\",\n                       \"says\": \"a dependency commits a transaction before the handler runs — a later refusal cannot undo it\"},\n```\n`pulse` ∈ `{\"nag\", \"count\"}`: nag counts toward S20 firing, count rides the line. An emitted finding is a dict `{\"id\": <catalogue id>, \"slot\": …, <subject keys>}` — e.g. `_a3_forms_mw.py:332-333` (`subject`, `status`, `at`, `exits`, `sites`), `_a3_forms_fn.py:212-215`, `_a3_forms_task.py:130-131`.\n\n**Where kinds findings land: TOP LEVEL.** `_a3_forms_mw.py:509-512` does `forms[\"arm_findings\"].setdefault(\"kinds\", []).extend(found)` for every kinds part, and counts into `stats[\"findings\"][id]`. The amendment's prose (`amendment-1.md:96`, §A4 V38) says the kinds arm's dependency findings sit on the endpoint's own `arm_findings` — the CODE and the goldens (`docs/design/element-forms/goldens/expected.json:21,25`, path `[\"arm_findings\",\"kinds\",…]`) say feed-wide. Follow the code. By contrast `_a3_forms_effects.run:749` and `_a3_forms_contract.run:521` write `v.setdefault(\"arm_findings\", {})` per endpoint.\n\n**Pulse does not see arm findings.** `skills/gabe-pulse/scripts/form_drift.py:78-94` reads only `f.get(\"findings\")` — the endpoint pass's own list — against the hardcoded rosters `NAG` (`:37`) and `COUNT` (`:38`), \"kept in sync with `_a3_forms.py FINDINGS[*]['pulse']` by contract\". An arm finding never reaches S20 today. The brief says the inflight part is an ANALYSIS, never a grade, so this is consistent with adding no finding at all — the catalogue entry is optional.\n\n---\n\n### 10 · Sizes vs the 800-line CODE budget\n\n`skills/gabe-commit/scripts/size-budget.sh` WARNs (never blocks; `--cap 800`) when a touched first-party file is or newly crosses 800 lines; the project rule (CLAUDE.md, ruling R9) is to STATE the numbers in the commit message.\n\n```\n_a3_paths.py          797   ← 3 lines from the cap\n_a3_forms_mw.py       516\n_a3_forms_settings.py 463\n_a3_forms_build.py    426\n_a3_forms.py          331\n_a3_forms_task.py     245\n_a3_forms_fn.py       216\n_a3_forms_ids.py      123\n_a3_forms_catch.py    119\n_a3_forms_reach.py     74\n(over cap already: _a3_forms_schema.py 1001 · _a3_code.py 3024 · build_center_a3.py 2581)\n```\nA new leaf `_a3_forms_inflight.py` keeps `_a3_forms_mw.py` (516) well clear; inlining the part there would push it toward ~700+. `_a3_forms.py` grows by whatever roster the part needs (the `TASK_KW`/`LOCK_CALLS` precedent was \"+~25 lines\", `amendment-1.md:848`).\n\n---\n\n### 11 · Top-level blocks vs per-endpoint keys\n\n**Top-level:** the runner assigns `forms[\"<name>\"] = dict(sorted(...))`. Written today: `forms[\"middleware\"]`, `forms[\"dependencies\"]`, `forms[\"functions\"]`, `forms[\"tasks\"]`, `forms[\"handlers\"]` (`_a3_forms_mw.py:493, 500, 506`), `forms[\"conditions\"]` (`_a3_forms_paths.py:421`), `forms[\"schemas\"|\"migrations\"|\"models\"|\"settings\"|\"mirrors\"]` (`_a3_forms_schema.py:966-992`), `forms[\"steps\"]` (`_a3_forms_effects.py:753`), `forms[\"test_cases\"]` (`_a3_forms_tests.py:237`), `forms[\"frontend\"]` (`_a3_forms_build.py:404`). Envelope keys the orchestrator owns: `head` `ids` `arms` `arms_ignored` `arms_error` `arm_findings`.\n\nMap-key conventions (`amendment-1.md:118-126`): `middleware:<Cls>` (a collision becomes `{\"variants\": […]}`, `_a3_forms_mw.py:318-321`), `dependencies`/`functions`/`handlers` keyed `file::qual` with a `levels_id: \"file#qual\"` field, `tasks` keyed `endpoint:TASK <name>`, `settings` keyed `setting:<name>`.\n\n**Per-endpoint:** mutate `v` from `MW._handlers(...)` — `v[\"switches\"]` (`_a3_forms_switch.py:363`), `v[\"repeat\"|\"auth\"|\"rate\"|\"responses\"]` (`_a3_forms_contract.py:513-516`), `v[\"framework_exits\"]` (`_a3_forms_schema.py:952`), `v[\"paths\"]`, `v[\"returns\"]`, `v[\"branches\"]`. The endpoint form's own keys (`_a3_paths._form:748-751`): `entity handler levels_id method path file line full_path declared produced preconditions findings slots{U3,U7,K1}`. Variants: an endpoint key with a collision holds `{\"variants\": [form, …]}` and every consumer iterates `e.get(\"variants\") or [e]`.\n\n---\n\n### 12 · Batteries a new kinds part touches\n\n- `tests/forms-core/run.sh` — `:420, 424, 426, 428, 433` derive from `F.ARMS`/`F.ARM_ORDER` and self-adjust. `:786` pins `F.KINDS[\"endpoint\"]` EXACTLY (edit needed only for a new endpoint slot). `:498-500` (case K2) asserts `k[\"parts\"][\"tasks\"][\"present\"]`, the `partial —` reason, and `all(v[\"kinds_k\"] == [\"tasks\"] …)` — the fake writer records `list(ctx[\"parts\"])` (`:392-396`) and the LAST kinds stage wins, so **adding `inflight` to the second kinds stage breaks `:500`; adding it to the first stage does not.**\n- `tests/forms-kinds/run.sh` (705 lines) — the kinds battery; drives the real orchestrator over `_a3_paths.build` on a synthetic FastAPI tree, AST only, each case mutation-proven. A new part ships its own cases here (the Slice-8 precedent used a fixture copy of its own so Slice 3's counts stayed exact, `amendment-1.md:904`).\n- `docs/design/element-forms/goldens/expected.json` + `check-goldens.py` + `render-acceptance.py` — the §A4 acceptance sheet, data-driven by JSON paths like `[\"arm_findings\",\"kinds\",{\"where\":{\"id\":…}}]`.\n- Repo law (CLAUDE.md): every checker ships fixture cases proving it can both FIRE and stay silent, in the same commit; a deterministic script that will run on real project data ships only after a dry run against a COPY — `scripts/forms-dryrun.sh` is the tool (it asserts an arms-on run moves `forms.json` and nothing else).\n\n### 13 · Where the feed is written\n\n`build_center_a3.py:2199-2217`: `forms: false` / `GABE_FORMS=0` skips it; `_a3_paths.build(amap, REPO_ROOT)` → `_a3_forms_build.extend_backend(_forms, amap, REPO_ROOT, CFG)` → `docs/site/center/forms.json` (`json.dumps(indent=1, sort_keys=True)`). The frontend arm re-writes it later at `:2304-2314`. Selection: `center.config.json#forms_arms` (`{arm: true}` map, list or comma string) or `GABE_FORMS_ARMS=a,b|all|none` for one run (`_a3_forms_build.selection:57-70`)."
  },
  {
   "key_facts": [
    "In all three apps the READ of a request.state value sits outside the handler's own call graph: gustify reads it one call level down (middleware/idempotency.py:31 via get_idempotency_key, 6 endpoint call sites), gastify reads it in a slowapi key_func invoked by the route decorator (app/rate_limit.py:134), tier3 reads it in a sibling dependency require_permission (onyx/auth/permissions.py:341-342). 'middleware -> dependency -> handler -> one call level' covers gustify only.",
    "The write sites are NOT mostly middleware: gustify writes from middleware dispatch (middleware/idempotency.py:25), gastify writes from a dependency get_auth_context (app/auth/deps.py:148), tier3 writes from dependencies (onyx/auth/users.py:2071,2146,2163,2177,2190,2573) and from a dependency-factory closure (onyx/server/middleware/rate_limiting.py:105). The existing _state_setter() in _a3_forms_contract.py:144 walks only app_middleware dispatch/__call__, so it finds gustify's and none of the other two.",
    "request.state tally (non-test writes / distinct attributes): gustify 1/1 (idempotency_key) - gastify 1/1 (user_id) - tier3 7/4 (usage_credential x4, token_scopes, referral_source, rate_limit_user_id). Test-only writes that would fool a reader: gastify tests/conftest.py:190, tier3 tests/unit/.../test_craft_gateway.py:21, test_llm_gateway_api.py:70, test_user_id_contextvar.py:59 (app.state.skip_oauth_refresh exists only in tests).",
    "ContextVar tally: gustify 0 definitions (absent entirely) - gastify 1 (app/rate_limit.py:36, set at :43 with NO reset, get at :50) - tier3 18 non-test definitions, 10 of them in shared_configs/contextvars.py. Three lifetime shapes in tier3's request path: set+finally reset in middleware (onyx/utils/client_ip.py:75-79, onyx/utils/middleware.py:126-130), set with no reset in middleware (onyx/utils/middleware.py:31,56), set+finally reset in a generator dependency seeded FROM request.state (onyx/auth/users.py:2215-2223).",
    "tier3 bridges request.state into a ContextVar inside one dependency: onyx/auth/users.py:2216-2217 does CURRENT_USAGE_CREDENTIAL_CONTEXTVAR.set(getattr(request.state, 'usage_credential', None)) with reset in finally at :2221-2222 - one fact with two carriers in the same request.",
    "@lru_cache tally: gustify 24 (config.py:387 get_settings maxsize=1; integrations/gemini.py:156 caches a live genai.Client/httpx pool; 18 JSON-vocabulary loaders in reference/catalogs.py; i18n/resolver.py:32) - gastify 1 (app/services/locations.py:43) - tier3 13 (onyx/auth/jwt.py:40 caches a NETWORK fetch of JWT keys and is reached from every JWT request via onyx/auth/users.py:2065). Bare @cache and @alru_cache: not found in any of the three. @cached_property: 3 in tier3 only.",
    "tier3's real settings cache is hand-rolled and invisible to a decorator grep: onyx/server/security/store.py:43-44 (_CACHE_LOCK = threading.RLock(), _CACHE: TTLCache keyed by tenant, TTL 10s), read/written at :346-347 and :366-375, on the auth path via onyx/auth/users.py:2061, with an explicit invalidate_security_cache().",
    "Only ONE middleware class across all three apps has an __init__ holding state: gustify's RateLimitMiddleware (apps/api/middleware/rate_limit.py:95-105) reads 5 settings once and builds two SlidingWindowLimiter counters (each a defaultdict(deque) at :52), read every request at dispatch :108,111,116,120. tier3 has ZERO middleware __init__; gastify has ZERO.",
    "Process state is built in three other places the brief does not name: a module-level singleton (gastify app/rate_limit.py:151 _ExemptibleLimiter with storage_uri='memory://'; app/observability.py:187 metrics = MetricsRegistry() whose __init__ at :55-63 holds a Lock + six dicts, mutated every request from app/middleware.py:34-36,48-50), a FACTORY CLOSURE (tier3 onyx/utils/middleware.py:119 route_map built once and closed over; onyx/server/middleware/rate_limiting.py:92 limiter), and a LAZY global singleton built on the first request (gustify services/ai_recipes.py:91-99 _breaker/_user_limiter/_ip_limiter reading settings once; gastify app/services/scan_worker.py:68,183 _active_scan_count).",
    "BackgroundTasks: gustify NOT FOUND (0 params, 0 add_task) - gastify 5 endpoint params, 6 add_task (app/api/scans.py:158,255; statements.py:158,243,471; scan_test_cases.py:122), with ownership_scope_id copied by value into process_scan (app/services/scan_worker.py:168-181) - tier3 6 params, 4 add_task all passing tenant_id explicitly (onyx/background/task_utils.py:272; onyx/db/projects.py:155; onyx/server/features/projects/api.py:108,556). tier3's drain_delete_loop then calls get_session_with_current_tenant() which reads the ContextVar, so whether the contextvar survives into the background task is undecidable from source -> dies:'unknown'.",
    "Locks: gustify has 8 pg_advisory sites and ZERO are request-reachable (all inside *_on_startup called from main.py:49,54,55,56) - gastify has 1 request-path row lock (app/api/groups.py:998 with_for_update, held to the commit at :1012) and 1 in-process Lock (app/observability.py:56, imported bare so 'threading.Lock()' misses it) - tier3 has 3 request-reachable advisory-lock wrappers (onyx/db/users.py:165 lock_group_membership called from 5 sites, ee/onyx/db/license.py:62 acquire_seat_lock, :49 acquire_license_store_lock, all released on the caller's commit) plus a CacheLock matching LOCK_CALLS exactly at ee/onyx/db/license.py:412-416 (.lock( then .acquire().",
    "tier3's lock KEY is derived from in-flight state: onyx/db/users.py:173-175 computes the advisory lock id from get_current_tenant_id(), i.e. from CURRENT_TENANT_ID_CONTEXTVAR.",
    "Read-site indirection defeats name grepping: tier3 reads request.state through a module constant (onyx/server/middleware/rate_limiting.py:49-51 getattr(request.state, _RATE_LIMIT_USER_ID_STATE_KEY, None), constant at :22) and reads every ContextVar through an accessor function (get_current_tenant_id, current_client_ip, get_current_user_id, get_current_usage_credential, get_current_incognito_record_mode) so the var name never appears at the consumer.",
    "Genuine per-request state can be invisible to a 'ContextVar(' grep: gastify app/middleware.py:17-18 uses structlog.contextvars.clear_contextvars()/bind_contextvars(request_id=...) inside RequestIdMiddleware.dispatch, merged at app/logging.py:14.",
    "tier3 marks every legitimate request.state read with '# ods: ignore[getattr]' (users.py:2190,2217, permissions.py:341, craft_gateway.py:14, rate_limiting.py:49, users.py:950) because the project bans bare getattr - a free high-precision marker in that repo, not portable.",
    "Whole-idiom absences are real and must not read as failures: gustify has no ContextVar, no BackgroundTasks, no threading/asyncio Lock, no with_for_update, and no request-reachable lock at all; gastify has no advisory lock; neither gastify nor tier3 has a middleware __init__ with state.",
    "Tally-inflating false positives: tier3 has dozens of module-level dicts that are frozen rosters not live state (onyx/llm/models.py:71, onyx/hooks/registry.py:8, onyx/cache/factory.py:20, onyx/connectors/freshdesk/connector.py:61,70,77) - only a handful are mutable caches (onyx/natural_language_processing/utils.py:98, onyx/connectors/confluence/onyx_confluence.py:77,78,82, onyx/llm/model_capabilities.py:325). A write site after definition is the discriminator, not the name. Also gastify alembic/versions/042_usage_counters_d96_tiers.py:75 'FOR UPDATE USING' is an RLS policy clause, not a lock.",
    "tier3's referral_source has two unrelated carriers in the same request: written to request.state at onyx/auth/users.py:2573 and read at :949-950, but also read straight from a cookie at :718-719 and :2721. Matching on the attribute name alone merges two different things.",
    "set() without reset() is neither a leak nor a lifetime proof: gastify app/rate_limit.py:43 and tier3 onyx/utils/middleware.py:31,56 set with no reset; onyx/utils/client_ip.py:78, onyx/utils/middleware.py:130 and onyx/auth/users.py:2221-2222 reset in a finally. The dies verdict should follow the presence of the finally: reset present -> 'with the answer', absent -> 'unknown'."
   ],
   "open_questions": [
    "Does the inflight part treat request.state.usage_credential (tier3, written 4x at onyx/auth/users.py:2071,2146,2163,2177, read once at :2217 only to seed CURRENT_USAGE_CREDENTIAL_CONTEXTVAR) as one row with four set_at values, four rows, or two rows chained by the bridge? The feeds force the choice; the source does not answer it.",
    "Should a decorator-supplied read site count as a read? gastify's only request.state reader (app/rate_limit.py:134) is a key_func passed to 32 @limiter.limit sites and is never called by app code. Excluding it makes gastify's one attribute set-but-never-read; including it needs a decorator-argument walk the arms do not currently do.",
    "What is the dies verdict for a contextvar whose consumer is reached only through a BackgroundTask (tier3 drain_delete_loop -> get_session_with_current_tenant -> CURRENT_TENANT_ID_CONTEXTVAR)? Starlette's context inheritance is not visible in the source; 'unknown' is honest but may be the majority verdict for tier3's background arm.",
    "Is an @lru_cache on a pure deterministic helper (tier3 onyx/external_apps/url_glob.py:12 _to_regex, onyx/llm/model_name_parser.py:232) meant to produce a process-scoped row at all? If yes, gustify's count jumps from ~3 meaningful to 24 and tier3's from ~4 to 13. A discriminator on externally-sourced values (file read, network fetch, env-built object) would cut it, but that is a judgement the registry would have to encode.",
    "Should the part read hand-rolled process caches (tier3 onyx/server/security/store.py:44 TTLCache + RLock on the auth path) when they carry no cache decorator? They are the most consequential process-scoped thing in that request path, but detecting them needs a mutable-module-global-with-a-write-site rule rather than a decorator rule.",
    "Only gustify has a middleware __init__ holding state, and only gastify/tier3 build process state in factory closures and lazy globals. Does 'a counter or limiter built in a middleware __init__' widen to cover those two, or does the part report honest-empty for the two apps that have no such __init__?",
    "I did not verify which of tier3's 51 with_for_update sites sit under an endpoint versus a celery task; I classified by directory only (ee/onyx/db 9, onyx/db/permission_sync_attempt 6, onyx/db/scheduled_task.py:327 is a queue claim). A per-site reach walk from endpoints was out of scope for a read-only grep pass."
   ],
   "report": "# In-flight state — ground truth in the three study apps\n\nRead-only sweep. No battery, build, browser or node program was run; only `grep`, `sed -n`, `cat -n`. Nothing was edited or created.\n\nRoots read: gustify `/home/khujta/projects/apps/gustify/apps/api` · gastify `/home/khujta/projects/apps/gastify/backend` · tier3 (onyx) `/home/khujta/projects/repo-study/tier3/backend`.\n\n---\n\n## 0. The headline, before the detail\n\n**In all three apps, the READ of a `request.state` value sits somewhere the handler's own call walk does not reach.** This is the single most load-bearing fact for the `inflight` part:\n\n| app | set at | read at | reachable from the handler? |\n|---|---|---|---|\n| gustify | middleware `dispatch` | a module helper `get_idempotency_key(request)` called by the handler | **yes** — exactly ONE call level |\n| gastify | a **dependency** (`get_auth_context`) | a **slowapi `key_func`** invoked by the route decorator's wrapper | **no** — not in the handler's call graph at any depth |\n| tier3 | a **dependency** (`_resolve_optional_user`) and a **dependency-factory closure** | `require_permission` (another dependency), a feature helper, and the writing module itself | **no** — a sibling dependency, never a callee |\n\nSo \"middleware → dependency → handler → one call level\" covers gustify only. gastify and tier3 both need the **decorator/dependency wrapper** as a read site, or their rows come out set-but-never-read.\n\n---\n\n## 1. `request.state` — writes and reads\n\n### gustify — 1 attribute, the one already modelled\n\n- `apps/api/middleware/idempotency.py:25` (write, in `IdempotencyMiddleware.dispatch`):\n  ```python\n  key: str | None = None\n  if request.method in _MUTATING_METHODS:\n      key = request.headers.get(IDEMPOTENCY_HEADER)\n  request.state.idempotency_key = key\n  ```\n- `apps/api/middleware/idempotency.py:31` (read):\n  ```python\n  key: str | None = getattr(request.state, \"idempotency_key\", None)\n  ```\n- The helper `get_idempotency_key(request)` (`middleware/idempotency.py:29`) is called by **six** endpoints: `api/cooking.py:126` · `api/recipe_creation.py:77` · `api/recipe_creation.py:241` · `api/setup.py:191` · `api/pantry.py:524` · `api/e2e_seed.py:74`.\n- Scope `request` · set_at `middleware/idempotency.py:25` · set_by `IdempotencyMiddleware.dispatch` · dies `with the answer`.\n- Note `key` is set **unconditionally**, to `None` on a GET — the attribute always exists; the guard is on the value, not on the write.\n\n### gastify — 1 attribute, written in a DEPENDENCY, read in a decorator key_func\n\n- `app/auth/deps.py:148-149` (write, inside `get_auth_context`, the app's `Auth` dependency):\n  ```python\n  # User-keyed rate limiting (RATE-LIMIT-PLAN): slowapi key_funcs + exempt_when evaluate\n  # inside the route wrapper, AFTER dependencies — so these stashes are visible to them.\n  request.state.user_id = str(user.id)\n  mark_rate_limit_identity(user.email)  # per-email exemption for e2e accounts (D117)\n  ```\n- `app/rate_limit.py:134` (read, inside `user_or_ip_key`, a slowapi `key_func`):\n  ```python\n  user_id = getattr(request.state, \"user_id\", None)\n  if user_id:\n      return f\"user:{user_id}\"\n  ```\n  `user_or_ip_key` is never called by app code — it is passed as `key_func=` to `@limiter.limit(...)` at `app/api/privacy.py:55,254,338`, `app/api/transactions.py:345,346,347,382,674` and others (32 `@limiter.limit`/`@limiter.shared_limit` sites in the app). `per_resource_key(\"transaction_id\")` (`app/rate_limit.py:140-148`) and `user_group_key` (`:115`) wrap it.\n- Scope `request` · set_at `app/auth/deps.py:148` · set_by `get_auth_context` (a dependency, **not** middleware) · dies `with the answer`.\n- `app/main.py:91`: `app.state.limiter = limiter` — an **app**-state write, process-scoped, required by slowapi's 429 handler.\n\n### tier3 — 4 attributes, 7 write lines, all in dependencies or a dependency factory\n\nAll in `onyx/auth/users.py` except the last:\n\n- `usage_credential` — written **4×**: `onyx/auth/users.py:2071` (JWT branch), `:2146` (session), `:2163` (PAT), `:2177` (API key). Example `:2146`:\n  ```python\n  if user is not None:\n      request.state.usage_credential = UsageCredentialIdentity(\n          UsageCredentialType.SESSION\n      )\n  ```\n  Read once, at `onyx/auth/users.py:2217`, to seed a ContextVar (see §2).\n- `token_scopes` — written `onyx/auth/users.py:2162`:\n  ```python\n  # Expose the token's scopes so require_permission can cap the\n  # request to them.\n  request.state.token_scopes = pat.scopes\n  ```\n  Read at **three** places: `onyx/auth/users.py:2190` (same function's fail-closed route check), `onyx/auth/permissions.py:341-342` (the `require_permission` gate — a separate dependency), `onyx/server/features/build/craft_gateway.py:14`.\n- `referral_source` — written `onyx/auth/users.py:2573` (OAuth callback), read `onyx/auth/users.py:949-950` inside `oauth_callback`.\n- `rate_limit_user_id` — written `onyx/server/middleware/rate_limiting.py:105`, inside a closure returned by `get_feedback_rate_limiters()`:\n  ```python\n  request.state.rate_limit_user_id = (\n      str(user.id) if user.account_type != AccountType.ANONYMOUS else None\n  )\n  await limiter(request, response)\n  ```\n  Read at `onyx/server/middleware/rate_limiting.py:49-51` — **through a module constant, not a literal**:\n  ```python\n  user_id: str | None = getattr(  # ods: ignore[getattr]\n      request.state, _RATE_LIMIT_USER_ID_STATE_KEY, None\n  )\n  ```\n  (`_RATE_LIMIT_USER_ID_STATE_KEY = \"rate_limit_user_id\"` at `:22`.)\n- `model_server/main.py:77` sets `app.state.gpu_type`, read `model_server/encoders.py:178` — a different app (the model server), process-scoped.\n\n**Lint tell worth knowing:** every legitimate `request.state` read in tier3 carries `# ods: ignore[getattr]`. The project bans bare `getattr`; the state reads are the enumerated exceptions. That comment is a free high-precision marker for this idiom in this repo (not portable).\n\n---\n\n## 2. ContextVar\n\n### gustify — **not found**. Zero `ContextVar(` in the whole api tree.\n\n### gastify — exactly one, plus an invisible library one\n\n- `app/rate_limit.py:36-38` (definition):\n  ```python\n  _current_user_email: ContextVar[str | None] = ContextVar(\n      \"gastify_rate_limit_user_email\", default=None\n  )\n  ```\n  Header comment at `:32-35` states the reason: *\"slowapi 0.1.9 calls `exempt_when()` with NO arguments (Limit.is_exempt), so the caller identity can't ride the request — it travels via this contextvar\"*.\n- Set: `app/rate_limit.py:43` inside `mark_rate_limit_identity`, called from `app/auth/deps.py:149` (the dependency) and mirrored in `tests/conftest.py:191`.\n- Get: `app/rate_limit.py:50` inside `_is_rate_limit_exempt`, which `_ExemptibleLimiter.limit` injects as the default `exempt_when` on **every** limit (`:59-61`).\n- **No `reset()` anywhere.** Set-without-reset; the value dies only because each request runs in its own task context.\n- **Invisible to a `ContextVar(` grep:** `app/middleware.py:17-18`\n  ```python\n  structlog.contextvars.clear_contextvars()\n  structlog.contextvars.bind_contextvars(request_id=request_id)\n  ```\n  A genuine per-request contextvar write, through a library wrapper, in `RequestIdMiddleware.dispatch`. Merged back at `app/logging.py:14` (`structlog.contextvars.merge_contextvars`). Logging-only.\n\n### tier3 — 18 definitions (2 more in tests); the rich case\n\nAll 10 shared ones in `shared_configs/contextvars.py`: `CURRENT_TENANT_ID_CONTEXTVAR:8` · `SESSION_TENANT_OVERRIDE_CONTEXTVAR:17` · `INDEXING_REQUEST_ID_CONTEXTVAR:22` · `ONYX_REQUEST_ID_CONTEXTVAR:27` · `INDEX_ATTEMPT_INFO_CONTEXTVAR:32` · `CURRENT_ENDPOINT_CONTEXTVAR:37` · `CURRENT_USER_ID_CONTEXTVAR:42` · `CURRENT_INCOGNITO_RECORD_MODE_CONTEXTVAR:48` · `CURRENT_CONTENT_FREE_SESSION_ID_CONTEXTVAR:55` · `CURRENT_USAGE_CREDENTIAL_CONTEXTVAR:67`. Others: `onyx/utils/client_ip.py:26` · `onyx/auth/session_tokens.py:79` · `onyx/llm/request_context.py:5,9` · `onyx/utils/logger.py:31,37` · `onyx/tracing/framework/scope.py:8,12`.\n\n**Three distinct lifetime shapes, all in the request path:**\n\n1. **set + `finally` reset in middleware** — `onyx/utils/client_ip.py:72-79`:\n   ```python\n   token = _CLIENT_IP_CONTEXTVAR.set(get_client_ip(request))\n   try:\n       return await call_next(request)\n   finally:\n       _CLIENT_IP_CONTEXTVAR.reset(token)\n   ```\n   Read via `current_client_ip()` (`:58`) at `onyx/utils/audit.py:242`, `onyx/utils/credential_audit.py:69`, `ee/onyx/utils/telemetry.py:21` — deep-stack, no `Request` threaded. Dies `with the answer`, provably.\n   Same shape at `onyx/utils/middleware.py:126-130` for `CURRENT_ENDPOINT_CONTEXTVAR`, whose readers are `onyx/utils/audit.py`, `onyx/utils/credential_audit.py`, `onyx/server/metrics/postgres_connection_pool.py`.\n\n2. **set with NO reset in middleware** — `onyx/utils/middleware.py:31` (`CURRENT_TENANT_ID_CONTEXTVAR`) and `:56` (`ONYX_REQUEST_ID_CONTEXTVAR`), both inside `@app.middleware(\"http\")` closures. Readers of the request-id: `onyx/utils/logger.py` (3), `onyx/utils/credential_audit.py`, `onyx/utils/audit.py`, `onyx/server/features/mcp/oauth.py` (2). Verdict for these should be `unknown`, not `with the answer` — nothing in the code returns them.\n\n3. **set + `finally` reset in a DEPENDENCY, seeded from `request.state`** — `onyx/auth/users.py:2215-2223`, inside the `optional_user` generator dependency:\n   ```python\n   token = CURRENT_USER_ID_CONTEXTVAR.set(str(user.id) if user is not None else None)\n   credential_token = CURRENT_USAGE_CREDENTIAL_CONTEXTVAR.set(\n       getattr(request.state, \"usage_credential\", None)  # ods: ignore[getattr]\n   )\n   try:\n       yield user\n   finally:\n       CURRENT_USAGE_CREDENTIAL_CONTEXTVAR.reset(credential_token)\n       CURRENT_USER_ID_CONTEXTVAR.reset(token)\n   ```\n   This is a **request.state → ContextVar bridge**: the same value has two carriers in the same request. If the part models them independently it will emit two unrelated rows for one fact. Readers go through accessor functions `get_current_user_id()` / `get_current_usage_credential()` (`shared_configs/contextvars.py:89,99`), so the ContextVar name never appears at the read site.\n\n**Accessor-function indirection is the norm, not the exception.** `get_current_tenant_id()` (`shared_configs/contextvars.py:72`), `current_client_ip()`, `get_current_user_id()`, `get_current_usage_credential()`, `get_current_incognito_record_mode()`. A `.get()` grep finds the accessor, not the consumer.\n\n---\n\n## 3. `@lru_cache` / `@cache` — process-scoped memoisation\n\nBare `@cache` and `@alru_cache`: **not found** in any of the three. `@cached_property`: 3 in tier3, 0 elsewhere.\n\n### gustify — 24 sites, the heaviest user by far\n\n- **The settings getter**, `apps/api/config.py:387-390`:\n  ```python\n  @lru_cache(maxsize=1)\n  def get_settings() -> Settings:\n      \"\"\"Process-wide cached settings. Clear with `get_settings.cache_clear()` in tests.\"\"\"\n      return Settings()\n  ```\n  Used as a FastAPI dependency at `api/recipe_creation.py:66,223,274`, `api/recipe_stream.py:119,165` (`Annotated[Settings, Depends(get_settings)]`) and called directly 20× outside tests. Fixed until restart.\n- **A cached SDK client**, `apps/api/integrations/gemini.py:156-157`:\n  ```python\n  @lru_cache(maxsize=4)\n  def _build_genai_client(api_key: SecretStr, timeout_ms: int) -> GenAIClient:\n      \"\"\"Process-cached ``genai.Client`` (reuses the httpx pool — roast G4).\"\"\"\n  ```\n  Holds a live connection pool. Process-scoped, request-reachable.\n- **18 reference-vocabulary loaders** in `apps/api/reference/catalogs.py` (`:58,73,82,100,112,131,146,169,179,187,195,208,220,233,247,268,398`), e.g. `allergen_codes()` at `:58`, reached from `api/recipes.py:328,334,339,346`, `schemas/preferences.py:132`, `schemas/settings.py:102`, `services/profile.py:104`. Each reads a JSON file once at first request.\n- `apps/api/i18n/resolver.py:32` `_load_raw(locale)` `maxsize=8` — reached from `api/locale_routes.py:47` via `resolve_locale`/`get_bundle`.\n- `apps/api/services/recipe_derive.py:22,29,66,260` — 4 more.\n\n### gastify — 1 site\n\n- `app/services/locations.py:43` `@lru_cache(maxsize=1)`.\n\n### tier3 — 13 sites, 2 of them interesting from a request\n\n- `onyx/auth/jwt.py:40` — `@lru_cache(maxsize=8) def _fetch_public_key_payload(...)`, comment at `:39`: *\"Keyed on the URL so a runtime settings change takes effect without a restart.\"* Caches a **network fetch** of JWT verification material, process-wide. Reached from `onyx/auth/users.py:2065` (`verify_jwt_token`) on every JWT-authenticated request.\n- `onyx/db/engine/pg_ssl.py:60` and `onyx/db/engine/iam_auth.py:92` — `@functools.cache`, connection-time.\n- `onyx/llm/model_capabilities.py:53` — `@lru_cache(maxsize=1)` with the comment *\"the copy.deepcopy is expensive, so we cache the result\"*.\n- Pure helpers with no lifetime meaning: `onyx/external_apps/url_glob.py:12` (`_to_regex`), `onyx/llm/model_name_parser.py:232`, `onyx/utils/variable_functionality.py:75`, `onyx/connectors/slack/utils.py:41`, `onyx/connectors/mediawiki/family.py:103`, `onyx/indexing/document_push.py:77`, `onyx/utils/gpu_utils.py:47`, `onyx/llm/multi_llm.py:364`, `ee/onyx/utils/encryption.py:15`.\n\n### The one that `@lru_cache` misses entirely — tier3's hand-rolled settings cache\n\n`onyx/server/security/store.py`:\n```python\n40:_CACHE_TTL_SECONDS = 10.0\n43:_CACHE_LOCK = threading.RLock()\n44:_CACHE: TTLCache[str, SecuritySettings] = TTLCache(\n```\nread/written at `:346-347` (`invalidate_security_cache`), `:366-375` (`get_security_settings`). `get_security_settings()` is called on the auth path at `onyx/auth/users.py:2061`. A tenant-keyed, TTL-bounded, lock-guarded **process cache of settings** with an explicit invalidator — the exact thing the part wants to report, and a decorator grep sees none of it.\n\n---\n\n## 4. Middleware `__init__` — counters, limiters, settings read once\n\n### gustify — the textbook case, `apps/api/middleware/rate_limit.py`\n\n```python\n 92 class RateLimitMiddleware(BaseHTTPMiddleware):\n 95     def __init__(self, app: ASGIApp, settings: Settings | None = None) -> None:\n 96         super().__init__(app)\n 97         s = settings or get_settings()\n 98         self._enabled = s.rate_limit_active      # fail-safe: always on in production (#32)\n 99         self._hops = s.trusted_proxy_hops        # trusted-proxy client-IP extraction (#33)\n100         self._global = SlidingWindowLimiter(\n101             s.rate_limit_per_minute, s.rate_limit_window_seconds,\n102         )\n103         self._sensitive = SlidingWindowLimiter(\n104             s.rate_limit_sensitive_per_minute, s.rate_limit_window_seconds,\n105         )\n```\n- **5 settings read once** at `:98-104` → fixed until restart. Registered `main.py:127` as `app.add_middleware(RateLimitMiddleware, settings=settings)`.\n- **Two counters**, each `SlidingWindowLimiter.__init__` (`:49-52`) building `self._hits: dict[str, deque[float]] = defaultdict(deque)`.\n- Read on every request at `dispatch` `:108` (`self._enabled`), `:111` (`self._hops`), `:116` (`self._sensitive.allow`), `:120` (`self._global.allow`).\n- Self-bounding: `SWEEP_THRESHOLD = 1024` at `:47`, swept at `:64-65`. Module docstring `:8-10` says *\"In-process state is per-replica\"*.\n- `IdempotencyMiddleware` (`middleware/idempotency.py:18`) has **no** `__init__` — no process state.\n\n### gastify — none in a middleware `__init__`; two module-level singletons instead\n\n- `app/rate_limit.py:151-156`:\n  ```python\n  limiter = _ExemptibleLimiter(\n      key_func=_client_key,\n      enabled=settings.rate_limit_enabled,\n      storage_uri=\"memory://\",\n      headers_enabled=True,  # X-RateLimit-* + Retry-After on 429\n  )\n  ```\n  A module-level limiter with in-memory storage — a process-scoped counter store reading `settings.rate_limit_enabled` once at import.\n- `app/observability.py:55-63` + `:187`:\n  ```python\n  55     def __init__(self) -> None:\n  56         self._lock = Lock()\n  57         self._counters: dict[str, int] = defaultdict(int)\n  58         self._gauges: dict[str, float] = defaultdict(float)\n  59         self._reservoirs: dict[str, list[float]] = defaultdict(list)\n  60         self._hist_counts: dict[str, int] = defaultdict(int)\n  ...\n  187 metrics = MetricsRegistry()\n  ```\n  Six dicts + a lock, built once, mutated on **every** request from `AccessLogMiddleware.dispatch` at `app/middleware.py:34-36,48-50`.\n- Both gastify middlewares (`RequestIdMiddleware:14`, `AccessLogMiddleware:25` in `app/middleware.py`) have **no** `__init__`.\n\n### tier3 — zero middleware `__init__`; state lives in a factory CLOSURE\n\nThe three `BaseHTTPMiddleware` subclasses (`onyx/utils/client_ip.py:66`, `onyx/server/auth/captcha_api.py:90`, `:154`) all define only `dispatch`. The rest are `@app.middleware(\"http\")` closures inside `def add_*_middleware(app)`. One of those builds process state in the factory body, not an `__init__` — `onyx/utils/middleware.py:107-126`:\n```python\n107 def add_endpoint_context_middleware(app: FastAPI) -> None:\n...\n117     Must be registered AFTER all routes are added to the app.\n119     route_map = _build_route_map(app)\n121     @app.middleware(\"http\")\n122     async def set_endpoint_context(...):\n125         handler = _match_route(route_map, request.url.path)\n126         token = CURRENT_ENDPOINT_CONTEXTVAR.set(handler or \"unmatched\")\n```\n`route_map` is a list of compiled regexes built once at startup and captured in the closure — process-scoped, fixed until restart, invisible to any `__init__` rule.\n\nSame shape at `onyx/server/middleware/rate_limiting.py:92-96`, where `limiter = RateLimiter(times=FEEDBACK_RATE_LIMIT_MAX_REQUESTS, seconds=FEEDBACK_RATE_LIMIT_WINDOW_SECONDS, identifier=user_scoped_rate_limit_key)` is built once in `get_feedback_rate_limiters()` and closed over by the dependency at `:98-108`.\n\n### gustify's third construction site — a LAZY singleton built on the first request\n\n`apps/api/services/ai_recipes.py:70-99`:\n```python\n70 # Per-process resilience state, shared across requests (mirrors RateLimitMiddleware).\n73 _breaker: CircuitBreaker | None = None\n74 _user_limiter: SlidingWindowLimiter | None = None\n75 _ip_limiter: SlidingWindowLimiter | None = None\n...\n86 def build_pipeline_deps(settings: Settings, model_tier: str = \"lite\") -> PipelineDeps:\n87     # Intentionally synchronous with NO await points: under the single-threaded asyncio\n88     # event loop that makes this check-then-set lazy init atomic ...\n91     global _breaker, _user_limiter, _ip_limiter\n92     if _breaker is None:\n93         _breaker = CircuitBreaker(\n94             settings.ai_breaker_fail_threshold, settings.ai_breaker_reset_seconds,\n95         )\n96     if _user_limiter is None:\n97         _user_limiter = SlidingWindowLimiter(settings.ai_rate_limit_user_per_min, 60.0)\n```\nSettings read once, on the first request that reaches the AI path. `CircuitBreaker.__init__` (`integrations/circuit_breaker.py:33-37`) holds `_failures` and `_opened_at`. `reset_ai_resilience()` at `:78` exists for tests. The `global` statement is the tell; `auth/verifier.py:86` (`global _firebase_app`) is the only other one in gustify.\n\ngastify has the same shape at `app/services/scan_worker.py:68,183,191-193,229-230` — `_active_scan_count` incremented/decremented around each background scan, mirrored into `metrics`.\n\n---\n\n## 5. BackgroundTasks — work that outlives the answer\n\n### gustify — **not found**. Zero `BackgroundTasks`, zero `.add_task(`.\n\n### gastify — 4 endpoints, 6 `add_task` sites\n\n- Params: `app/api/scans.py:61`, `:172`; `app/api/statements.py:77`, `:452`; `app/api/scan_test_cases.py:57`.\n- Calls: `app/api/statements.py:158`, `:243`, `:471`; `app/api/scans.py:158`, `:255`; `app/api/scan_test_cases.py:122`.\n- `app/api/scans.py:158`:\n  ```python\n  background_tasks.add_task(process_scan, scan_id, ownership_scope_id=auth.ownership_scope_id)\n  ```\n  `process_scan` (`app/services/scan_worker.py:168-181`) documents the hand-off explicitly: *\"`ownership_scope_id` is threaded from the dispatching request so the worker can set the RLS GUC for writing scope-bound tables (P43). When None … the scope is read from the acquired scan record.\"* — a request value deliberately **copied by value** into work that outlives the answer.\n\n### tier3 — 4 `add_task` sites, all the same target family\n\n- `onyx/background/task_utils.py:272` `bg_tasks.add_task(drain_delete_loop, tenant_id)` · `onyx/db/projects.py:155` `background_tasks.add_task(drain_processing_loop, tenant_id)` · `onyx/server/features/projects/api.py:108` `add_task(drain_project_sync_loop, tenant_id)` · `:556` `bg_tasks.add_task(drain_delete_loop, tenant_id)`.\n- Endpoint params at `onyx/server/features/projects/api.py:103,176,283,328,520` and `onyx/server/query_and_chat/chat_backend.py:728`.\n- **The ambiguity to record honestly:** `drain_delete_loop(tenant_id)` takes the tenant explicitly (`onyx/background/task_utils.py:173`) and then calls `get_session_with_current_tenant()`, which reads `CURRENT_TENANT_ID_CONTEXTVAR`. Whether that ContextVar is still set when the background task runs is not decidable from the source. `dies: 'unknown'` is the correct verdict for any contextvar whose consumer is reached through a background task.\n\n---\n\n## 6. Locks\n\n### gustify — 8 advisory-lock sites, **0 reachable from a request**\n\n`services/sandbox_seed.py:382` (`pg_try_advisory_lock`) + `:395` (`pg_advisory_unlock`) · `services/restriction_seed.py:145` · `services/catalog_seed.py:368` · `services/sensory_seed.py:90` · `services/progression.py:201`, `:471` — all inside `*_on_startup` functions called from `main.py:49,54,55,56`. Each is gated by `if settings.environment is Environment.LOCAL: return` and a SQLite check. `services/catalog_seed.py:367`: *\"Serialize concurrent replica boots; auto-released when this txn commits.\"*\n\n`POST /_e2e/seed` (`api/e2e_seed.py:68`) reaches `complete_setup` but not any advisory lock. No `threading.Lock`, no `asyncio.Lock`, no `with_for_update` anywhere in gustify.\n\n### gastify — 1 row lock in a request path, 1 in-process lock\n\n- `app/api/groups.py:993-999` (request path, the group-join endpoint):\n  ```python\n  # Serialize concurrent joins for this group by locking the scope row, then\n  # re-check the cap INSIDE the write transaction — the preview count is a stale\n  # snapshot, so two joiners could otherwise both pass it and overshoot the cap.\n  await db.execute(\n      select(OwnershipScope.id).where(OwnershipScope.id == group_id).with_for_update()\n  )\n  ```\n  Held until the commit at `:1012` — dies `with the answer`.\n- `app/observability.py:56` `self._lock = Lock()` — **imported bare** (`from threading import Lock`, `app/observability.py:10`), so `threading.Lock()` does not match it. Acquired on every request via `metrics.inc`/`observe` (`:67,71,75,80,98,102`).\n- The other `FOR UPDATE` hit, `alembic/versions/042_usage_counters_d96_tiers.py:75`, is an RLS **policy** clause (`FOR UPDATE USING (...)`), not a lock.\n\n### tier3 — 8 advisory-lock sites, 3 request-reachable; 68 in-process locks, mostly not in the request path\n\nRequest-reachable advisory locks:\n- `onyx/db/users.py:165-176` `lock_group_membership(db_session)`:\n  ```python\n  \"\"\"One lock for every membership write, admin access included, released on the\n  caller's commit. Take it before reading state the write depends on ...\"\"\"\n  db_session.execute(text(\"SET LOCAL lock_timeout = '10s'\"))\n  db_session.execute(\n      text(\"SELECT pg_advisory_xact_lock(:lock_id)\"),\n      {\"lock_id\": _membership_lock_id(get_current_tenant_id())},\n  )\n  ```\n  Called at `onyx/db/users.py:197`, `:250`, `ee/onyx/db/user_group.py:586`, `:789`, `:976`. **The lock id is derived from a ContextVar** (`get_current_tenant_id()`) — in-flight state feeding a lock key.\n- `ee/onyx/db/license.py:62-71` `acquire_seat_lock` — callers `ee/onyx/server/tenants/billing.py:269`, `:275`, `ee/onyx/server/scim/api.py:285`, `onyx/auth/users.py:493`, `onyx/onyxbot/slack/handlers/handle_message.py:367`, `:420`.\n- `ee/onyx/db/license.py:49-60` `acquire_license_store_lock` — caller `ee/onyx/utils/license.py:308`.\n- All three go through `_acquire_advisory_lock` (`ee/onyx/db/license.py:73-85`) / the inline equivalent, each bracketed by `SET LOCAL lock_timeout = '10s'` … `DEFAULT`, *\"released on the caller's commit or rollback\"*.\n\nA cache-backend lock matching `LOCK_CALLS` exactly — `ee/onyx/db/license.py:394-424`:\n```python\n@contextmanager\ndef _license_cache_lock(tenant_id: str | None, wait: bool) -> Generator[CacheLock | None]:\n    candidate = get_cache_backend(tenant_id=tenant_id).lock(\n        _LICENSE_CACHE_LOCK_KEY, timeout=_LICENSE_CACHE_LOCK_TIMEOUT_SEC\n    )\n    if candidate.acquire(blocking=wait, ...):\n```\nBoth `.lock(` and `.acquire(` in three lines. Backed by redis or postgres (`onyx/cache/postgres_backend.py:104,127` uses `pg_advisory_unlock`/`pg_try_advisory_lock`).\n\nBackground-only advisory lock: `onyx/background/periodic_poller.py:199`.\n\nThe 68 `threading.Lock()`/`asyncio.Lock()`/`threading.RLock()` sites cluster in `onyx/server/metrics` (8) · `onyx/server/features` (8) · `onyx/db/engine` (8) · `onyx/connectors/slack` (3) · `ee/onyx/server` (3) · `onyx/onyxbot/slack` (2) · tracing (3) · sandbox_proxy (4). The request-path ones are the caches: `onyx/server/security/store.py:43` (`_CACHE_LOCK`, on the auth path) and `onyx/llm/model_capabilities.py:329` (`_REASONING_PROBE_LOCKS`).\n\n51 `with_for_update` sites outside tests, concentrated in `ee/onyx/db` (9), `onyx/db/permission_sync_attempt.py` (6), `onyx/db/indexing_coordination.py`, `onyx/db/scheduled_task.py:327` (`skip_locked=True` — a queue claim, not a request), etc. Only a minority sit under an endpoint.\n\n---\n\n## 7. Tallies\n\nNon-test / test split. Counts are grep line counts, deduplicated by hand only where noted.\n\n| idiom | gustify | gastify | tier3 |\n|---|---|---|---|\n| `request.state.X =` (write) | **1** / 0 | **1** / 1 | **7** (6 distinct attrs) / 5 |\n| `getattr(..., state, ...)` (read) | **1** / 0 | **2** / 0 | **7** / 1 |\n| `app.state.` | 0 | **1** / 0 | **2** / 3 |\n| `ContextVar(` definitions | **0** | **1** / 0 | **18** / 2 |\n| ContextVar `.set(` in the request path | 0 | **1** (no reset) | **5** (3 with `finally` reset, 2 without) |\n| `@lru_cache` / `@functools.cache` | **24** / 0 | **1** / 0 | **13** / 0 |\n| bare `@cache` · `@alru_cache` | 0 · 0 | 0 · 0 | 0 · 0 |\n| `@cached_property` | 0 | 0 | 3 |\n| hand-rolled process cache (dict/TTLCache + lock) | 0 | **1** (`MetricsRegistry`) | **≥2** (`security/store.py`, `model_capabilities.py`) |\n| `BackgroundTasks` endpoint params | **0** | **5** | **6** |\n| `.add_task(` | **0** | **6** | **4** |\n| `threading`/`asyncio` `Lock()` | **0** | **1** (bare import) | **68** / 7 |\n| `pg_advisory*` sites | **8** (0 request-reachable) | **0** | **8** (3 request-reachable) |\n| `with_for_update` | **0** | **1** (+1 RLS policy false hit) | **51** / 22 |\n| middleware class with an `__init__` holding state | **1** | **0** | **0** |\n| process state built in a factory closure | 0 | **1** | **2** |\n| lazy `global` singleton built on first request | **2** | **1** | — |\n\n**Common vs rare, across the three:**\n- Common everywhere: `@lru_cache` (24 / 1 / 13 — but the *shape* differs wildly), a per-process limiter or counter object (all three), a request-scoped identity value carried out of band (all three).\n- Common in two: `BackgroundTasks` (gastify, tier3), ContextVar (gastify 1, tier3 18), advisory locks (gustify 8, tier3 8).\n- Rare / single-instance: a middleware `__init__` holding state (gustify only) · `with_for_update` in a request path (gastify 1, tier3 a minority of 51) · a `request.state` write from an actual middleware (gustify only — the other two write from dependencies).\n- **Absent from a whole app:** gustify has no ContextVar and no BackgroundTasks at all; gastify has no advisory lock; neither gastify nor tier3 has a middleware `__init__` with state. Any per-endpoint row set must be able to come back empty for a real app without that reading as a failure.\n\n---\n\n## 8. Things that would fool a naive reader\n\nOrdered by how much damage each does.\n\n1. **A `request.state` write in a test conftest.** `gastify tests/conftest.py:190-191` mirrors the production dependency exactly:\n   ```python\n   request.state.user_id = str(mock_auth_context.user.id)\n   ...\n   mark_rate_limit_identity(mock_auth_context.user.email)\n   ```\n   tier3 has five more: `tests/unit/onyx/server/features/craft/test_craft_gateway.py:21`, `tests/unit/ee/onyx/server/gateway/test_llm_gateway_api.py:70`, `tests/unit/shared_configs/test_user_id_contextvar.py:59` (`app.state.skip_oauth_refresh` — an attribute that **exists only in tests**), plus assertion reads at `tests/unit/onyx/auth/test_usage_credential_identity.py:51,97,121`. `tests/external_dependency_unit/auth/test_permission_projection_contract.py:148` builds a fake: `SimpleNamespace(state=SimpleNamespace(token_scopes=None))`. Without a test-path exclusion, tier3's `usage_credential` gains three phantom readers and gastify's one attribute doubles.\n\n2. **A ContextVar that is only logging.** `gastify app/middleware.py:17-18` (`structlog.contextvars.bind_contextvars(request_id=...)`) and `tier3 onyx/utils/logger.py:31,37` (`pruning_ctx`, `doc_permission_sync_ctx`). Real per-request state, zero effect on what the endpoint decides. tier3's `ONYX_REQUEST_ID_CONTEXTVAR` is borderline — three of its six readers are in `onyx/utils/logger.py`, but `onyx/utils/audit.py` and `onyx/utils/credential_audit.py` write it to audit rows.\n\n3. **An `@lru_cache` on a pure helper never reached by a request.** tier3 `onyx/external_apps/url_glob.py:12` (`_to_regex`), `onyx/llm/model_name_parser.py:232`, `onyx/connectors/mediawiki/family.py:103`, `onyx/connectors/slack/utils.py:41`, `onyx/utils/gpu_utils.py:47`. Memoisation of a deterministic function of its arguments — nothing is \"alive\" in the sense the part means. The discriminator is whether the cached value is **externally sourced** (a file, a network fetch, an env-built object): `gustify config.py:387`, `gustify integrations/gemini.py:156`, `tier3 onyx/auth/jwt.py:40` qualify; `_to_regex` does not.\n\n4. **A lock that only runs at startup.** All 8 gustify advisory locks. A bare `pg_advisory` grep reports 8; the honest per-endpoint answer is zero. Same trap for `with_for_update` in tier3: `onyx/db/scheduled_task.py:327` (`skip_locked=True`) is a background queue claim.\n\n5. **The read uses a module constant, not a literal.** `tier3 onyx/server/middleware/rate_limiting.py:49-51` reads through `_RATE_LIMIT_USER_ID_STATE_KEY`. The existing `_read()` in `_a3_forms_contract.py:135-137` would handle it (`_text` resolves a module constant via `S.resolve_const`), but only because the constant is defined in the same module at `:22`. A constant imported from elsewhere would be missed.\n\n6. **`Lock` imported bare.** `gastify app/observability.py:10` is `from threading import Lock`, so the call site at `:56` reads `Lock()`. A `threading.Lock()` pattern scores gastify at zero locks. Same class of miss for `from contextvars import ContextVar` (gastify `app/rate_limit.py:20`) vs `contextvars.ContextVar` (tier3).\n\n7. **A module-level dict that is a frozen roster, not live state.** tier3 has dozens: `onyx/llm/models.py:71` `_REASONING_EFFORT_RANK`, `onyx/connectors/freshdesk/connector.py:61,70,77`, `onyx/hooks/registry.py:8`, `onyx/cache/factory.py:20`. Only a handful are mutable caches (`onyx/natural_language_processing/utils.py:98` `_TOKENIZER_CACHE`, `onyx/connectors/confluence/onyx_confluence.py:77,78,82`, `onyx/llm/model_capabilities.py:325`). Name-matching on `_CACHE` inflates the tally several-fold; a **write site after definition** is the real test.\n\n8. **A `request.state` attribute written but never read in app code.** tier3's `usage_credential` is written at four sites and read at exactly one (`onyx/auth/users.py:2217`) — and that read only copies it into a ContextVar. Reporting four writes and one read as four rows over-counts a single fact.\n\n9. **`referral_source` has two unrelated sources.** Written to state at `onyx/auth/users.py:2573`, read from state at `:949`; but also read straight from a **cookie** at `:718-719` and `:2721`. Same name, same request, different carrier. Matching on the name alone merges two things.\n\n10. **`FOR UPDATE` inside an RLS policy.** `gastify alembic/versions/042_usage_counters_d96_tiers.py:75` — a Postgres policy clause, not a lock.\n\n11. **`set()` without `reset()` is not a leak, and not a lifetime proof either.** gastify `app/rate_limit.py:43` and tier3 `onyx/utils/middleware.py:31,56` all set with no reset. Under ASGI the value still dies with the task, but nothing in the source says so. The three sites at `onyx/utils/client_ip.py:78`, `onyx/utils/middleware.py:130`, `onyx/auth/users.py:2221-2222` do say so, in a `finally`. The verdict should follow the `finally`, not an assumption: reset present → `with the answer`; absent → `unknown`.\n\n---\n\n## 9. What this implies for the `inflight` part\n\nStated as facts, not as a design decision.\n\n- The write sites the three apps actually use are: **middleware `dispatch`** (gustify 1), **a FastAPI dependency** (gastify 1, tier3 5 of 7), and **a closure returned by a dependency factory** (tier3 1). The current `_state_setter()` in `_a3_forms_contract.py:144-166` walks `amap['app_middleware']` classes' `dispatch`/`__call__` only, so it finds gustify's and **none** of gastify's or tier3's.\n- The read sites are: **a module helper called by the handler** (gustify, one hop), **a sibling dependency** (tier3 `require_permission`), **a decorator-supplied `key_func`** (gastify, all 32 limited routes), and **an accessor function that names no variable** (tier3's five `get_current_*`). Only the first is inside \"handler → one call level\".\n- Two carriers can hold one value in one request (tier3 `request.state.usage_credential` → `CURRENT_USAGE_CREDENTIAL_CONTEXTVAR`, `onyx/auth/users.py:2216-2217`). Whether that is one row or two is a modelling choice the feeds force.\n- Process-scoped construction happens in three places, not one: a middleware `__init__` (gustify), a module-level singleton (gastify's `limiter`, `metrics`), and a lazy `global` init on the first request (gustify's `_breaker`/`_user_limiter`/`_ip_limiter`, `services/ai_recipes.py:91-99`). Only the first is in the brief.\n- \"A setting read once inside a middleware `__init__`\" occurs exactly **once** across all three apps (gustify `middleware/rate_limit.py:97-104`). \"Settings frozen for the process\" occurs in all three, but via `@lru_cache` (gustify `config.py:387`), import-time evaluation (gastify `app/rate_limit.py:153`), and a TTL cache (tier3 `onyx/server/security/store.py:44`)."
  },
  {
   "key_facts": [
    "A kinds part absent from `_a3_forms.ARM_STAGES` (_a3_forms.py:318-322) NEVER RUNS: _a3_forms_build.py:246 builds its `units` list from the stage tuple only. Adding the name to ARMS[\"kinds\"][\"parts\"] (:303) is not enough.",
    "`ARM_NEEDS` (_a3_forms.py:324-327) must be keyed exactly `\"kinds.<part>\"` — `_needs()` (_a3_forms_build.py:85-90) resolves both the unit and its bare arm name, so a `\"kinds\"` key would apply the need to every kinds part.",
    "The runner wiring is `_a3_forms_mw.PARTS` (:27) + the `run()` branch (:486-513) + `run.parts = PARTS` (:516). `_a3_forms_build.RUNNERS` (:41-43) needs no edit — kinds already has a runner.",
    "tests/forms-kinds/run.sh: 705 lines, 18 assertions, 3.7 s recorded. Next free case ids are C30+. Two fixtures on purpose — $A (:16-179, Slice 3 counts) and $S8 (:182, a copy for Slice 8) — a new idiom that moves either set of counts gets its own third copy.",
    "The six required case shapes in forms-kinds: FIRE (C14 :398) · honest-empty (C29 :690) · mutation (C28 :677) · arms-off writes no key (C17 :472) · determinism (:698) · 'arms only add', i.e. no endpoint `produced` row moves (:700-701).",
    "tests/forms-core/run.sh (839 lines, 19 assertions, 1.9 s): K1 FIRE :416-441 derives the kinds parts set from F.ARMS (no edit) but pins `sorted(B.closure({\"contract\"}))` as a LITERAL list at :441 — only a new need edge breaks it. K12 :784 pins KINDS membership. K8 :794 auto-scans any `_a3_forms_*.py`.",
    "forms-core K7 (:764-777) is the finding rail: a finding with `arm: \"endpoint\"` must also land in form_drift.py:37-38 (NAG/COUNT) and scripts/map-baseline.sh:104-106 (census sums) in the same commit. A finding with `arm: \"kinds\"` is outside K7 and invisible to every consumer by D16.",
    "Goldens ARE needed: docs/design/element-forms/goldens/expected.json holds 62 goldens on target gustify grouped by slice (1·2·3a·3b·4·5a·5b·6·7·8·9·10a·10b·10c·11a·11b·11c·11d); §A4 step 1 makes them the arm's data record and step 6 the operator's per-arm acceptance. render-acceptance.py:9-11's slice→arm-name `ARM` map is hand-kept (a missing row renders blank, no crash).",
    "No baseline re-bless: scripts/map-baseline.sh:102-107 measures only `forms.stats` plus the seven endpoint-pass finding ids — no arm stats. With arms OFF by default (D12), tests/baselines/*.sha256 and *.census.json do not move.",
    "No example regen: templates/center/shell/example/codebase-graph-station/forms.json has top keys ['endpoints','framework','head','kind','present','stats','version'] — no `arms` key (arms-off).",
    "No consumer changes required by D16 (amendment-1.md:1369-1372): form_drift.py reads endpoints[].findings only (:84-92) and never arm_findings; mapquery.form_summary drops every key outside its `keep` tuple (:666); map_census kind=forms reads forms.stats only (tools_wave2.py:413-432). §A4 step 7b lists the owed uptake.",
    "The narrow reading to generalise: _a3_forms_contract.py `_read()` :125 (header get/subscript, getattr(request.state,…), request.state.<attr>) and `_state_setter()` :144 (the app middleware writing request.state.<attr>, via amap['app_middleware']), narrowed only by `_KEY = re.compile(CT[\"key_name\"])` at :27 over `_a3_forms.CONTRACT['key_name'] = r'(?i)idempot'` (:263).",
    "Lifting shared logic to a leaf has a precedent: `_a3_forms_falsify.py` (134 lines, §A4 fix 3), imported at column 0 by _a3_paths.py:34, _a3_forms_schema.py:22, _a3_forms_walk.py:21.",
    "propagate.sh:43 greps `^\\s*(import|from) <mod>\\b` over every GEN/*.py, so a new `.py` reached that way is auto-landed on a twin as 'required' (:43-46); a new `.mjs` never lands (:47-49). Both twins (gustify, gastify) already vendor all 24 `_a3_forms*.py`.",
    "suite-doctor.sh hashes templates/ file-by-file against ~/.claude/templates/gabe/ (:74-86) — any generator edit reads as DRIFT until ./install.sh runs (≈5 s). The doctor itself is ≈5–6 min and runs every tests/*/run.sh (:101-116).",
    "`BackgroundTasks` is already in `_a3_forms.INJECTED_TYPES` (:131); `LOCK_CALLS` is at :294; ContextVar and lru_cache appear nowhere in the generators (grep over templates/center/generators/*.py returns nothing but a comment at _a3_forms_mw.py:341).",
    "State-word law (§A4 fix 6, review-a4.md:184-190): `unknown` · `absent` · `beyond one level`, never a bare 0 or []. F.STATES at _a3_forms.py:27. FRAMEWORK_MIN = \"0.136.1\" at _a3_forms_short.py:91; gated via `P._vt(fw[0]) >= P._vt(SH.FRAMEWORK_MIN)` (_a3_forms_schema.py:938-941) with the closed gate said out loud (_a3_forms_effects.py:620-622).",
    "The ids table (amendment-1.md:96-115) struck `k:` `w:` `e:` at §A4 V33 — 'a claim, a limiter and a service raise are addressed by their `at`' (:121). The brief's row shape carries no id, matching that precedent.",
    "scripts/forms-dryrun.sh (68 lines): every target builds TWICE (arms none, then arms-on); its header says gustify ≈1 min a build and tier3 ≈2, 'Run it ALONE (WSL: never beside a battery)'; exit 1 when run 2 moved anything but forms.json (:48-50). Roster tests/baselines/targets.conf: gustify · gastify · tier3 · keypro.",
    "The arms-on feed the endpoint lab reads comes from the dry run at ~/.cache/gabe-map-baselines/.check/<target>/forms.json — gen-endpoint-facts.py:12-13 (`--forms`) and :579.",
    "Name collision: `inflight` already names the board's in-flight projection — skills/gabe-cc-update/scripts/write-inflight.py, docs/site/center/inflight.{json,js}, battery tests/inflight/run.sh, and a CLAUDE.md bullet.",
    "_a3_forms_mw.py is 516 lines against the 800-line CODE budget (report-never-gate, state numbers in the commit). _a3_forms.py 331 · _a3_forms_contract.py 525 · _a3_forms_build.py 426 · _a3_paths.py 797 (post-split, with _a3_paths_read.py).",
    "The piece's own definition of done (program-leftovers.md:26): for generation pieces 10 and 11 the part ships with its battery (FIRE and SILENT), a dry run on the four study targets with the numbers in the commit message, suite-doctor CLEAN, and twin propagation — BEFORE the lab carry; one commit per piece, by explicit path.",
    "docs/design/design-context/decisions.md D-013 (:85) carries the explicit trigger this part fires: 'Revisit if: a generation arm reads request.state / context variables in general and finds state that no other row carries.' inventory-endpoint.md:73 carries the row 'to measure when piece 11 lands'.",
    "docs/center/data/facts.json (generated 2026-09-16, head 92dc6ed; current head babb15b) records tests/center at rc=1 / FAILING — possibly stale, verify before attributing a red doctor to this change.",
    "build_suite_center.py takes only `--out` (:1537-1540) — there is no per-page regen switch; the full refresh_suite_center.sh regen is browser-gated (minutes).",
    "Piece 11's full definition lives in docs/design/design-context/gaps-endpoint.effects.raw.json → verified.pieces[10] (key `alive-during-the-request`): five fields and five `after_rows` that read as ready-made gustify goldens."
   ],
   "open_questions": [
    "Does the new part hard-need `kinds.middleware` (request.state setters live in middleware) and/or `kinds.dependencies`? If so, `ARM_NEEDS` gains `\"kinds.<part>\"` and the ARM_STAGES placement must follow the need — and forms-core K1 FIRE's literal `sorted(B.closure({\"contract\"}))` at tests/forms-core/run.sh:441 must be re-checked.",
    "Does the part mint a new `KINDS` registry kind (with slots naming their blocks, pinned by forms-core K12) or ride as slots on the existing `endpoint` / `middleware` / `dependency` kinds? The brief's 'per endpoint plus top-level rows' shape fits either.",
    "Is the top-level block one map or two (request-scoped rows on endpoints, process-scoped rows top-level)? §A1's Map keys table needs the answer, and so does the `_added` byte accounting in _a3_forms_build.py.",
    "Should `_read()`/`_state_setter()` be lifted to a shared leaf (the _a3_forms_falsify precedent) or should the kinds part import `_a3_forms_contract` directly? The latter would be the first kinds→contract import edge; §A3's 'no arm imports another arm' rule is already relaxed elsewhere.",
    "Which slice label does the new part take in expected.json and render-acceptance.py's ARM map — a continuation (`12`) or a kinds sub-slice (`3c`/`8b`)?",
    "Does the name `inflight` stand given the collision with the board's in-flight projection (write-inflight.py, tests/inflight, docs/site/center/inflight.json)?",
    "Is tests/center's recorded rc=1 stale, or a live failure that will read as this change's fault at the doctor step?",
    "Does the part need `@lru_cache` detection to distinguish FastAPI dependency caching (per-request via the dependency cache) from process-scoped caching? The two have different `dies` values and the distinction is a framework rule, so it may need the FRAMEWORK_MIN gate."
   ],
   "report": "## The rails a new `kinds.inflight` part must pass\n\nRead-only survey. Every anchor below was opened; \"not found\" is stated where something is absent. Nothing was run, edited or created.\n\n---\n\n### 0 · Where the thing already has a name (flag first)\n\n**`inflight` is already the suite's word for a different feed.** `skills/gabe-cc-update/scripts/write-inflight.py` derives `docs/site/center/inflight.{json,js}` (the board's in-flight projection), its battery is `tests/inflight/run.sh`, and CLAUDE.md carries a bullet for it. A forms part keyed `inflight`, a module `_a3_forms_inflight.py` and battery cases named \"inflight\" will read ambiguously against it in grep, in the log and in the commit message. Decide the word before writing it 40 times.\n\nThe piece's own record: `docs/design/design-context/program-leftovers.md:19` (piece 11, \"a new `inflight` part in the kinds arm, then the lab carry\") and its full definition in `docs/design/design-context/gaps-endpoint.effects.raw.json` → `verified.pieces[10]` (key `alive-during-the-request`, five fields, `after_rows` naming the five lines it must produce on gustify).\n\n---\n\n### 1 · The spec, before any code\n\n`docs/design/element-forms/amendment-1.md` — the file's own convention for a part (§A2, e.g. Slice 8 at `:844-912`): **Generates · Modules (with est. lines) · Algorithm (numbered) · Goldens (per target, marked) · Battery (case ids) · Dry run — record · Effort**, then an \"As built\" paragraph after it lands.\n\nAlso owed in the same file:\n- **§A1 Map keys table** (`:117-127`) — a row for `inflight{}` naming its key shape, if it is a top-level map.\n- **§A1 ids table** (`:96-115`) — `k:` · `w:` · `e:` were **struck at §A4 V33**: \"a claim, a limiter and a service raise are addressed by their `at`\" (`:121`). The row shape in the brief (`{name, scope, set_at, set_by, read_at[], dies}`) carries no id, which follows that precedent. Say so explicitly rather than leave it open.\n- **§A3 module map** (`:1246-1266`) — a row if a new module lands.\n- **§A5 decisions** — D-numbers run D11…D28 (`:1344-1432`); the next is **D29**. Each is `DECISION / CHOSE / ASSUMED / BREAKS IF`.\n- **State-word law** (§A4 fix 6, `review-a4.md:184-190`): the sanctioned words are `unknown` · `absent` · `beyond one level` — never a bare `0` or `[]`. `F.STATES` is `(\"defined\",\"default\",\"missing\",\"n/a\",\"unknown\",\"external\")` at `_a3_forms.py:27`. The brief's `dies: 'unknown'` is already on-vocabulary.\n- **FRAMEWORK_MIN = `\"0.136.1\"`** at `templates/center/generators/_a3_forms_short.py:91`. Any rule cited to FastAPI's own source must be version-gated the same way: `P._vt(fw[0]) >= P._vt(SH.FRAMEWORK_MIN)` (`_a3_forms_schema.py:938-941`), and a closed gate must SAY so (`_a3_forms_effects.py:620-622`, `dependency_gate: closed: fastapi <v> < 0.136.1`). Relevant here because `BackgroundTasks` lifetime and dependency teardown ordering are framework rules, not source facts.\n\nAlso update `docs/design/design-context/decisions.md` **D-013** — its own trigger fires with this part: *\"Revisit if: a generation arm reads request.state / context variables in general and finds state that no other row carries — then request-scoped state is worth a 2 again.\"* (`decisions.md:85`).\n\n---\n\n### 2 · The registry edit — `templates/center/generators/_a3_forms.py` (331 lines, data only)\n\n| what | line | note |\n|---|---|---|\n| `ARMS[\"kinds\"][\"parts\"]` | `:303` | `(\"middleware\",\"dependencies\",\"functions\",\"tasks\",\"handlers\")` → add the part. `part_slices` (`:304`) only if its slice number differs from the arm's `slice: 3`. |\n| **`ARM_STAGES`** | `:318-322` | **A part absent from every stage NEVER RUNS** — `_a3_forms_build.py:246` builds `units` from the stage tuple only. Today kinds runs in two stages: `(\"kinds\", (\"middleware\",\"dependencies\"))` and `(\"kinds\", (\"functions\",\"tasks\",\"handlers\"))`. |\n| `ARM_NEEDS` | `:324-327` | Key it **exactly** `\"kinds.inflight\"`. `_needs()` (`_a3_forms_build.py:85-90`) looks up both the unit and its bare arm name, so a key `\"kinds\"` would apply the need to every kinds part. |\n| `KINDS` slot registry | `:30-67` | A new kind row; every slot must carry `\"block\"` — pinned by forms-core K12 (`tests/forms-core/run.sh:786-787`). |\n| `OPTIONS` | `:70-94` | Where a built-not-decided choice is stated (the module docstring: \"A new slot, a new framework rule or a new finding is an edit HERE, not in the walker\", `:15`). |\n| idiom rosters | beside `LOCK_CALLS` `:294`, `TASK_KW` `:289` | ContextVar / cache-decorator rosters go here. `BackgroundTasks` is **already known**: `INJECTED_TYPES` `:131`. |\n| `CONTRACT[\"key_name\"]` | `:263` | `r'(?i)idempot'`. **Do not move or widen it** — `_a3_forms_contract.py:27` compiles it as `_KEY` for U12. The general reading is a new roster beside it. |\n| `FINDINGS` | `:147-217` | Only if the part grades. The brief says analysis, never a grade → leave untouched. |\n\n**Finding trap (forms-core K7, `tests/forms-core/run.sh:764-777`):** a finding with `\"arm\": \"endpoint\"` must land in `skills/gabe-pulse/scripts/form_drift.py:37-38` (`NAG`/`COUNT`) **and** `scripts/map-baseline.sh:104-106` (the census sums) in the same commit, or K7 fails. A finding with `\"arm\": \"kinds\"` is outside K7's scope and is invisible to every consumer by **D16** (`amendment-1.md:1369-1372`).\n\n---\n\n### 3 · The reader module\n\nEither grow `_a3_forms_mw.py` (**516 lines**; the 800-line CODE budget is report-never-gate, state numbers in the commit) or add `templates/center/generators/_a3_forms_inflight.py`.\n\n- **Name it `_a3_forms_*.py`.** forms-core K8 (`tests/forms-core/run.sh:794-813`) globs `GEN.glob(\"_a3_forms_*.py\")` to prove every `P._private` / `G._private` / `_a3_stacks_pydi` import still exists. A module named otherwise escapes that guard.\n- **The existing narrow reading:** `_a3_forms_contract.py:125` `_read()` (header get/subscript, `getattr(request.state, …)`, `request.state.<attr>`) and `:144` `_state_setter()` (the app middleware that writes `request.state.<attr>`, via `amap[\"app_middleware\"]`). Both are already general in shape — only `_KEY` (`:27`) narrows them. The precedented route is lifting them to a shared **leaf** the way `_a3_forms_falsify.py` (134 lines) was lifted at §A4 fix 3, with contract then importing the leaf.\n- **Import rule for twins:** `propagate.sh:43` greps `^\\s*(import|from) <mod>\\b` over **every** `GEN/*.py` (any indentation since §A4 step 8). A new `.py` reached that way is auto-landed as \"required\"; a new `.mjs` **never lands** (`propagate.sh:47-49`).\n- **Cross-arm imports:** §A3 says \"no arm imports another arm\" (`amendment-1.md:1276-1279`), but that is already relaxed in fact — `_a3_forms_effects` imports `_a3_forms_mw` and `_a3_forms_walk`; `_a3_forms_schema` imports `_a3_forms_model`/`_mirror`/`_setting`/`_mw`. `_a3_forms_contract` is imported by **nothing** today; a kinds→contract import would be the first such edge.\n\n---\n\n### 4 · Wire the part — `templates/center/generators/_a3_forms_mw.py`\n\n- `PARTS` `:27` += the part name.\n- `run()` `:486-513` gains its `if \"<part>\" in parts:` branch plus its `stats` key; `run.parts = PARTS` at `:516` already publishes it to `_built()` (`_a3_forms_build.py:106-111`).\n- `_a3_forms_build.py` `RUNNERS` (`:41-43`) needs **no** edit — kinds already has a runner. It needs a column-0 import at `:27-35` only if a new module must reach the twins that way.\n- **Arm-reason trap:** `_a3_forms_build.py:310` sets a not-present arm's reason to `failed[0][1]` — the first failing unit in stage order. forms-core K1 FIRE pins `a[\"kinds\"][\"reason\"] == \"not built yet (slice 3) — needed by paths\"` (`tests/forms-core/run.sh:427`). Keep `middleware` first in the first kinds stage, or that literal moves.\n\n---\n\n### 5 · Battery — `tests/forms-kinds/run.sh` (705 lines · 18 assertions · **3.7 s** recorded)\n\nNext free ids are **C30+** (the file runs C14–C29; C17–C22 belong to Slice 3, C24–C29 to Slice 8 — `amendment-1.md:904`).\n\nPrelude helpers (`:350-396`): `build(repo, arms)` `:358` · `variant(name, edit)` `:372` (copies `$A`) · `variant8` `:382` (copies `$S8`) · `patch(d, rel, a, b)` `:388` (asserts exactly one occurrence) · `at(rel, text, nth)` `:378`.\n\nTwo fixtures exist on purpose: `$A` (`:16-179`, Slice 3's exact counts) and `$S8` (`cp -r \"$A\" \"$S8\"` at `:182`, \"so the Slice 3 cases keep their exact counts\"). **A new idiom that would move either set of counts gets its own third copy** — that is the file's own precedent.\n\nRequired case shapes, one each:\n| shape | precedent |\n|---|---|\n| FIRE — the rows appear with the right `scope`/`dies` | C14 `:398`, C26 `:652` |\n| SILENT / honest-empty — a tree with none of the idioms writes `{}` and a 0 stat | C29 `:690-697` |\n| MUTATION — an edit that takes the row away | C28 `:677-687` |\n| arms-off — `build(A, None)` writes no key at all | C17 `:472-477` |\n| determinism — two builds byte-identical | `:698-699` |\n| **\"arms only add\"** — `kp[\"endpoints\"][k][\"produced\"] == p[\"endpoints\"][k][\"produced\"]` | `:700-701` |\n\nTrap (§A6 `amendment-1.md:1460`): **a battery line printed after its summary line never counts** — the summary is at `:704`.\n\n---\n\n### 6 · `tests/forms-core/run.sh` (839 lines · 19 assertions · **1.9 s**)\n\nEdit only if a pinned literal moved:\n- **K1 FIRE `:416-441`** — `set(a[\"kinds\"][\"parts\"]) == set(F.ARMS[\"kinds\"][\"parts\"])` (`:428`) is derived, no edit. `sorted(B.closure({\"contract\"}))` at `:441` is a **literal list** — edit only if a need edge was added.\n- **K12 `:784-792`** — the `KINDS` membership set; add the kind name if one is minted.\n- **K7 `:764`** — only if a finding with `arm: \"endpoint\"` lands.\n- **K8 `:794`** — auto-scans a `_a3_forms_*.py` module, no edit.\n\n---\n\n### 7 · Goldens — `docs/design/element-forms/goldens/`\n\n`expected.json`: 62 goldens, `target: \"gustify\"`, slices `1 · 2 · 3a · 3b · 4 · 5a · 5b · 6 · 7 · 8 · 9 · 10a · 10b · 10c · 11a · 11b · 11c · 11d`; marks V 39 · P 13 · R 9 · U 1.\n\n**Yes, a new part needs goldens** — §A4 step 1 makes them the arm's data record and step 6 is the operator's per-arm acceptance read (`amendment-1.md:1293-1298, :1316`). Golden shape: `{id, slice, mark (V·R·P·U), says, get: [steps], one of eq|has|len|gte|exists}`; the path steps a key · an index · `{\"where\"/\"where_not\": {}}` · `{\"values\": true}` · `{\"pluck\": k}` · `{\"first\": true}` · `{\"sorted\": true}` · `{\"keys\": true}` (`check-goldens.py:27-66`). The five `after_rows` in `gaps-endpoint.effects.raw.json` → `verified.pieces[10]` are ready-made golden texts for gustify.\n\n`render-acceptance.py:9-11` — the `ARM = {...}` slice→arm-name map is **hand-kept**; a new slice label with no row renders a blank arm name (`ARM.get(sl, \"\")`, no crash).\n\nCommands (read-only, seconds, no browser):\n```\npython3 docs/design/element-forms/goldens/check-goldens.py <forms.json> --slice <new>\npython3 docs/design/element-forms/goldens/render-acceptance.py \\\n  docs/design/element-forms/goldens/expected.json <forms.json> \\\n  docs/design/element-forms/goldens/acceptance.html docs/design/element-forms/review-a4.md\n```\n\n---\n\n### 8 · Dry run — `scripts/forms-dryrun.sh` (68 lines) — **heavy, alone**\n\n```\nbash scripts/forms-dryrun.sh --arms kinds gustify\n```\n- Every target builds **twice** (arms `none`, then arms-on). Its own header (`:11`): *\"gustify ≈ 1 min a build, tier3 ≈ 2. Run it ALONE (WSL: never beside a battery).\"* → gustify ≈ 2 min; all four ≈ 10 min+.\n- Roster `tests/baselines/targets.conf` (gitignored, machine-local): **gustify · gastify · tier3 · keypro** (tier3 and keypro carry `GABE_TS_DIR=…/repo-study/tier0`).\n- Trusted only when no `SKIP|NO BASELINE|FAIL` line and a fresh manifest (`:28-35`); **exit 1 when run 2 moved anything but `forms.json`** (`:48-50`).\n- Its JSON line gives `arms.<arm>.{present,reason,stats,bytes}`, `ids`, `seconds` (`:61-65`) — those are the commit-message numbers (repo rule: a deterministic script ships only after a dry-run against a copy, numbers in the commit).\n- Machine pre-flight: `df -h /mnt/c` and `du -sh /var/log` first.\n- It writes the arms-on feed to `~/.cache/gabe-map-baselines/.check/<target>/forms.json` — which is what the lab reads (`gen-endpoint-facts.py:579`).\n\n---\n\n### 9 · Baselines — **no re-bless expected**\n\n`scripts/map-baseline.sh:102-107` reads only `forms.stats` and the **seven endpoint-pass** finding ids into `forms_endpoints · forms_rows · forms_unknown_rows · forms_nag · forms_count`. No arm stats are measured. With arms OFF by default (**D12**), `tests/baselines/*.sha256` and `*.census.json` (gustify: `forms_nag 39`, `forms_count 160`) do not move. Prove it with the dry run's arms-none leg reporting `BYTE-IDENTICAL`.\n\nSame for the committed example feed `templates/center/shell/example/codebase-graph-station/forms.json` — its top keys are `['endpoints','framework','head','kind','present','stats','version']`, **no `arms` key**. No `regen-example.sh` run is owed. (`regen-example.sh:61-62` already normalises `forms.head`.)\n\n---\n\n### 10 · Consumers — none required, by D16\n\n| consumer | what it reads | verdict |\n|---|---|---|\n| `skills/gabe-pulse/scripts/form_drift.py` | `endpoints[].findings` only (`:84-92`), `NAG`/`COUNT` hardcoded `:37-38`, `FORM_NAG_MIN=3` `:40` | **no edit** — it never sees `arm_findings`, and forms-core K7 `:777` asserts exactly that |\n| `mapquery.form_summary` `:659-673` | fixed `keep` tuple `:666` drops every other key; `forms_block` states at `:174-182` | **no edit**; `id` is still owed per §A4 step 7b |\n| `touches.form` — `tools.py:411-413` | calls `form_summary` | no edit |\n| `trace.from_form` — `tools_wave3.py:121-125` | `slots · findings · preconditions · variants` only | no edit |\n| `map_census kind=forms` — `tools_wave2.py:413-432` | `forms.stats` + the nag names | no edit; **an arms section is owed** (§A4 step 7b, `amendment-1.md:1323`) |\n| review FORM DRIFT | `form_drift.py --diff` over the endpoint pass | no edit |\n\nIf you *do* take the consumer uptake early (D16's own BREAKS IF, `:1372`), it costs a `gabe-map`/`gabe-pulse` SKILL.md version bump **plus the matching CLAUDE.md capability row in the same commit** — suite-doctor's P3 parity check fails otherwise (`scripts/suite-doctor.sh:120-129`) — plus cases in `tests/gabe-map/checks.py:657-678` and `tests/pulse-angles/run.sh:465-482`.\n\n---\n\n### 11 · Docs to touch in the same commit\n\n- `templates/center/generators/README.md:60` — the forms-arms module table row (names every module and arm; a new module/part belongs here).\n- `CLAUDE.md` — the element-forms bullet. No version row moves unless a skill version moves.\n- `docs/design/design-context/decisions.md:85` — D-013's revisit trigger fires.\n- `docs/design/design-context/inventory-endpoint.md:73` — the row reading *\"to measure when piece 11 lands\"* gets its smallest · middle · largest over the 80 endpoints (leftovers definition-of-done #2, `program-leftovers.md:26`).\n- `docs/design/design-context/program-leftovers.md` — one Log row, with the numbers.\n- `templates/center/shell/README.md` — §A4 step 8 names it as the feed list; **grep finds no \"forms\" in it today**, so verify before editing.\n\n---\n\n### 12 · Verification order and cost\n\n| # | command | cost | note |\n|---|---|---|---|\n| 1 | `bash tests/forms-kinds/run.sh` | ≈4 s | iterate here; run it directly, not through the doctor |\n| 2 | `bash tests/forms-core/run.sh` | ≈2 s | |\n| 3 | `bash tests/element-forms/run.sh` | ≈1 s | the endpoint pass's contract — must be untouched |\n| 4 | `bash tests/center/run.sh` | ≈44 s | pins the build wiring (`:358-410`); **its last record is rc=1 / FAILING** (see below) |\n| 5 | `./install.sh` | ≈5 s | `templates/` is hashed file-by-file against `~/.claude/templates/gabe/` (`suite-doctor.sh:74-86`) — any generator edit is DRIFT until this runs |\n| 6 | `bash scripts/forms-dryrun.sh --arms kinds gustify` (then the others) | ≈2 min/target, **alone** | the commit's numbers |\n| 7 | `python3 …/goldens/check-goldens.py <forms.json>` | seconds | expect 62 + the new ones, 0 mismatch / 0 missing |\n| 8 | `bash scripts/suite-doctor.sh` | **≈5–6 min, alone** | runs every `tests/*/run.sh` (`:101-116`); eight launch a headless browser |\n| 9 | `python3 docs/center/generators/write_facts.py --only forms-kinds,forms-core` | seconds | batteries auto-discovered (`_suite_data.py:270` globs `tests/*/run.sh`); no new dir → no new row. Full sweep is ≈5½ min |\n| 10 | `bash templates/center/generators/propagate.sh /home/khujta/projects/apps/gustify --check`, then without `--check`; then gastify | check seconds; real run ends in `refresh_center.sh regen` on the twin (`propagate.sh:91`) — minutes each, one at a time | both twins already vendor **all 24** `_a3_forms*.py` (verified); a new `.py` auto-lands as \"required\" (`:43-46`) |\n\nSuite-center page regen: **not found** — `build_suite_center.py` takes only `--out` (`:1537-1540`); there is no per-page switch. The full `refresh_suite_center.sh regen` is browser-gated (minutes).\n\n---\n\n### 13 · The lab carry (piece 11's second half)\n\nPer `program-leftovers.md:26`: for the two generation pieces, the part ships **battery + dry run + doctor + twins first**, then the lab.\n- `docs/design/workflow-panel/gen-endpoint-facts.py` (708 lines) — `--forms <arms-on forms.json>` at `:12-13`, `forms_slice()` at `:59-62`; plus `_ep_joins.py` for the joins.\n- `docs/design/workflow-panel/probe-eplab.mjs` — new asserts, **at least one proven to fire on a mutant** (definition of done #4).\n- **No new display design inside the piece** (definition of done #3).\n\n---\n\n### 14 · Two standing traps\n\n- **`tests/center` was FAILING at the last recorded sweep** — `docs/center/data/facts.json` (generated 2026-09-16, head `92dc6ed`; current head `babb15b`) records `center: rc=1, 178 assertions, note \"FAILING — last lines: ── regenerate the twin's center…\"`. Confirm whether that is stale before reading a red doctor as this change's fault.\n- **Commit by explicit path.** §A6 (`amendment-1.md:1473`) and memory: a concurrent session's `git add -A` has swallowed a commit on this branch (`graft-adoption`). Check `git log -1` before and after. Tree is clean right now."
  }
 ]
}