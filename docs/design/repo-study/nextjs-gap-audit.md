# Next.js gap audit — keypro-front vs the FastAPI+React-tailored center pipeline

Run 2026-09-11 (workflow `wf_908a1259-969`, 7 finder lenses on opus; verification stopped after 4 verdicts to save usage — every gap below is finder-claimed with file:line evidence; only the four marked VERIFIED were adversarially checked). Target: `repo-study/others/keypro-front` (Next.js 16 · hexagonal · server actions · raw `pg` SQL · vitest). 24 distinct gaps after dedup.

## Build order (the synthesis I owe, hand-written)

1. **config-only, today** — wire the junit capture (`commands.junit: pnpm vitest run --reporter=junit --outputFile=tests/results/web-junit.xml`) + `aliases {"@/": "src"}` on the web corpus: the whole 49-file test estate appears, and the three drawn-wrong all-clear pages (test-elements · guard cards · risk tab) get a corpus to be honest against.
2. **hours, the keystone** — `_a3_fe_extract.mjs`: read the file prologue directive (`"use client"` / `"use server"`) onto every export record; `_a3_fe.py` stamps it per piece. Three gaps hang on this one field: the server/client boundary, server actions as the WRITE boundary (client→server import binding = a call site, method POST), and the flat write channel.
3. **hours, the honesty rows** — station Sources row saying *no request arm ran* (`gabe-universe.html` + example twin); `test-elements` all-clear guarded on an empty roster; Next `page.tsx` default export is a route even without JSX (`_a3_fe.py:209`, hoist the `_NEXT_APP_RX` test above the JSX gate); `src/app` mclass not `config` on a Next tree; route-group label collision (`/ · layout` ×2).
4. **days, design first** — the two new arms that make journeys + d2w heat work here: server actions as endpoint-equivalent ROOTS (archmap endpoints → levels `endpoint:` nodes → `bk` chains → `byLabel`), and `_a3_sql.py` (CREATE TABLE → tables; `.query()` INSERT/UPDATE/DELETE/SELECT → access edges). Plus the gate family: `proxy.ts` matcher as middleware, `redirect()` in a route piece as a gate.

## Gaps


### silent-missing

#### `server-client-directives-not-extracted` · high · hours · lens hexagonal-structure

**Missed:** keypro's real top-level split is runtime, not folder: 13 files are client entry points carrying "use client" at line 1 (src/components/app/{app-sidebar,chat-screen,login-form,platform-shell,shell-context,users-screen}.tsx + src/components/ui/{chat/prompt-composer,feedback/confirm-dialog,forms/{checkbox,input,select,switch,textarea}}.tsx), 4 files are server RPC surfaces carrying "use server", and all 6 route pieces plus src/app/page.tsx are React Server Components that call authService/userService/chatService straight out of the DI container (src/app/(platform)/layout.tsx:14, src/app/(platform)/admin/users/page.tsx:12-18). The universe draws all 190 pieces as one undifferentiated "frontend". A reader cannot tell that PlatformShell ships to the browser while PlatformLayout — its own parent, one node away, in the SAME fe·platform cluster — runs on the server and holds the authoritative auth redirect. The same reader cannot see that ChatPage reaching chatService is an in-process DB read while ChatScreen reaching requestReply is a network hop.

**App:** src/components/app/chat-screen.tsx:1 ("use client"); src/app/(platform)/actions.ts:1, src/app/(platform)/admin/users/actions.ts:1, src/app/(platform)/chat/actions.ts:1, src/app/login/actions.ts:1 ("use server"); src/app/(platform)/layout.tsx:13-17 (server component, auth guard); 13 "use client" files verified at line 1 by head -1

**Pipeline:** templates/center/generators/_a3_fe_extract.mjs:198,260,277 — the per-file record is exactly {imports, exports, file_refs}; sf.statements[0] / the prologue is never inspected. `grep -rn 'use client|use server' templates/center/generators/*.py *.mjs` returns 0 hits across the whole generator set, and the same grep over templates/center/shell/gabe-universe.html returns 0 hits. The station's badge families (gabe-universe.html:4284-4290 __BADGE_COL) are method · role · feclass · mclass · hrole · pclass · delivery — there is no runtime/environment family, and __slot (gabe-universe.html:4384) only budgets two badge positions.

**Feed:** c4-graph.json fe.pieces key union = ['area','exports','feClass','fields','file','home','home_ev','homed_by','hrole','id','kind','label','mclass','members','name','shape','span'] — no env/runtime key on any of the 190 pieces. stats.fe has by_kind · by_feclass · by_hrole · by_mclass · by_channel and no by_env. No Sources row in gabe-universe.html mentions client vs server.

**Fix:** _a3_fe_extract.mjs: read the file prologue (ts.isExpressionStatement(sf.statements[0]) && ts.isStringLiteral(expression)) into rec.directive = 'client'|'server'. _a3_fe.py: stamp env onto every piece of that file (and propagate 'client' through the import closure of a directive file, which is the real Next boundary rule) plus stats.fe.by_env. gabe-universe.html: one new __BADGE_COL.env family + a __slot 2 + one legend row.

#### `server-actions-not-a-write-boundary` · high · days · lens hexagonal-structure

**Missed:** keypro has 11 exported server actions (logout · login · createUser · setUserEmail · changeUserRole · setUserActive · resetUserPassword · deleteUser · requestReply · requestReadiness · requestConversationReset). Four of them are the client→server network crossings: login-form.tsx:5,17 · users-screen.tsx:9-14,46,118-122 · chat-screen.tsx:39 · platform-shell.tsx:8. Each is a POST over the wire that ends in raw SQL (src/adapters/postgres) or the external AI backend (AGENT_API_URL). The pipeline's read/write channel and the whole FE write-spine heat are keyed on an HTTP METHOD taken from a fetch call site; with zero fetch sites there are zero write sinks, so all 58 call wires are tagged `chrome` and fed2w_max is 0. A reader who opens the FE write-heat toggle sees a completely flat field and concludes this is a read-only app. Worse, two of the four crossings are drawn as plain `imports` — the weakest wire, identical to a type import — because useActionState(createUser, EMPTY) PASSES the action rather than calling it, so no fecall is recorded (verified: UsersScreen→admin/users/actions.ts and LoginForm→login/actions.ts are `imports`; ChatScreen→chat/actions.ts and PlatformShell→chat/actions.ts are `fecall`).

**App:** src/components/app/users-screen.tsx:46,118-122 (six useActionState bindings to mutating actions); src/components/app/login-form.tsx:17,26 (useActionState(login,…) then <form action={action}>); src/components/app/chat-screen.tsx:39; src/app/(platform)/admin/users/actions.ts:51-128 (six mutating exports)

**Pipeline:** _a3_web.py:92-104 _CALL_RES roster is apiFetch | axios | fetch | openapiFetch (+sdkTable); _detect_idiom (_a3_web.py:266-274) is winner-take-all over those regexes, so a server-action reference can never register; with no hit _a3_web.py:472 returns {"present": True, "reason": "no REST api-call idiom detected", "screens": []}. _a3_fe.py:613 defines the write sinks as `_wsink = pieces with p.get("wsites")` — a write-METHOD fetch site — and _a3_fe.py:627-634 then tags every edge `chrome` unless its target is in touches_state/touches_write. _a3_code.py parses Python only, so there is no endpoint key-space for a bridge to match against either (c4 l2.nodes is empty).

**Feed:** c4-graph.json stats.web = {present:true, fetch_sites:0, screens:0, matched:0, sse:0, reason:"no REST api-call idiom detected"}. stats.fe by_channel = {chrome:58, read:0, write:0}; write_pieces 0; write_sites 0; fed2w_max 0; state_pieces 1. levels.json fn_nodes 0 / fn_edges 0 / cross_edges 0. c4-graph.json l2.nodes is an empty list. The station still renders the "FE write heat" legend toggle unconditionally (gabe-universe.html:6510) over a field with no fed2w values.

**Fix:** A directive-driven bridge arm rather than a regex idiom: once gap 1 emits rec.directive, treat an import-binding from a 'client' file to an export of a 'server' file as a call SITE (method POST, path `action:<file>#<export>`), emit it as a screen/site record in _a3_web.py's existing shape so _a3_fe.py:613-634 lights read/write/fed2w unchanged, and mint the action export as an endpoint-class node so the station's bridge wires and the journeys picker's byLabel have something to join. Separately and cheaply: gabe-universe.html:6096 should render st.web.reason — the feed already carries "no REST api-call idiom detected" and the Sources row prints only "0 fetches matched to endpoints · 0 unmatched" (grep for web.reason in the station returns 0 hits), so the WHY is emitted and thrown away.

#### `sql-in-ts-tables-not-drawn` · high · days · lens providers-streaming

**Missed:** keypro persists to PostgreSQL: `CREATE TABLE users` and `CREATE TABLE sessions`, with `sessions.user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE`. A reader of the universe cannot see that this app has a database at all — no table node, no column list, no FK wire. The FK matters twice over: _a3_graph.py:21-22 states "FK is the ONLY cross-entity signal" the L1 join makes, and this app's one real FK crosses auth (sessions) → users (users). That L1 edge can never exist, so the entity graph is built purely from call/import wires (L1 edge kinds: calls 10, imports 2, fk 0).

**App:** /home/khujta/projects/repo-study/others/keypro-front/src/adapters/postgres/schema.ts:29 (CREATE TABLE users), :54 (CREATE TABLE sessions), :56 (REFERENCES users(id) ON DELETE CASCADE); index DDL at :47, :52, :60

**Pipeline:** /home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:220 `def _table_of(node: ast.ClassDef)` — the only table detector in the pipeline, and its parameter is a Python AST ClassDef; its sole callers are _a3_code.py:661 (parse_models), :850 (_table_classes), :1686 (_model_table_map). parse_models at :644 is documented "SQLAlchemy entities via ast" and at :652 `tree, _src = _safe_parse(path)` returns None for any .ts, `continue`. model_census at :894 globs `dp.glob("*.py")`. A grep of every generator (*.py, *.mjs) for CREATE TABLE / INSERT INTO / drizzle / prisma / typeorm / knex returns zero hits — the gap is not "raw SQL specifically", it is that no TS data layer of any kind is read.

**Feed:** docs/site/center/c4-graph.json: every L2 entity block has `nodes: []` and `edges: []` (auth, chat, greeting, platform, users — all five, zero nodes total); no node id starts `d:` and no kind contains model/table/data; `cross_edges: []`; `stats.unresolved_tables: []`; `stats.minted_models: 0`. archmap.json: all 5 entities carry `models: []`; `model_census: {}`. L1 edge kinds = {calls: 10, imports: 2} — no `fk` kind exists.

**Fix:** New arm `templates/center/generators/_a3_sql.py`, built on the `_a3_web.py` contract (a separate source-reading arm with its OWN try/except, so a parser bug degrades to honest-empty and never touches graft/FK bytes). Pass 1 is DDL-only: regex the text of each claimed TS/JS file for `CREATE TABLE [IF NOT EXISTS] <name> ( ... )`, capture the column list and every `REFERENCES <t>(<c>)`, and return rows in exactly the shape parse_models already returns — `{cls, table, cols, fks}` (cls = PascalCase of the table when no class exists). Fold into `amap["entities"][slug]["models"]` in build_center_a3.py beside the parse_models call. NOTHING downstream changes: _a3_graph.py:648-658 builds the L2 model node from `model.get("cls")`/`model.get("table")` and :775-786 builds the FK edges from `model.get("fks")` — both are plain dict reads with no Python coupling, which is what makes this days and not a new rendering.

#### `sql-access-reads-writes-not-drawn` · high · days · lens providers-streaming

**Missed:** Every read and write this app performs is invisible, so the whole d2w/write-spine heat — the feature the station exists to show — is flat. `postgres-user-repository.ts` does 10 statements (SELECT×3, INSERT×1, UPDATE×4, DELETE×1, SELECT count×1), `postgres-session-repository.ts` 5 (INSERT×1, DELETE×3, SELECT×1), `schema.ts` 9 DDL + the seed's SELECT/INSERT. None draws a reads_from or writes_to wire, and no piece is ever on a write spine. The FE arm's blue→magenta FEBAND gradient is dead by construction here: `fed2w_max: 0` because the ONLY thing that can make a piece a write sink is a POST/PUT/PATCH/DELETE *fetch*, and this app has zero fetches — its writes go straight to SQL in the same process.

**App:** src/adapters/postgres/postgres-user-repository.ts:47,54,62,78,101,113,117,124,131,136 (10 statements through the `private async query` wrapper at :41); src/adapters/postgres/postgres-session-repository.ts:22,30,36,45,49 (5 statements); src/adapters/postgres/schema.ts:112 (`pool.query("SELECT count(*)::text AS count FROM users")`), :117 (INSERT INTO users)

**Pipeline:** Backend side: _a3_code.py:1700 `_orm_access(fnnode, m2t)` walks a Python ast.FunctionDef for the SQLAlchemy idioms in _ORM_WRITE_M/_ORM_WRITE_CORE/_ORM_READ_CORE/_ORM_COMMIT (:1671-1675) — session.add/select()/insert()/commit(). There is no string-SQL path. _a3_levels.py:400 `is_py = rfile.endswith(".py")` and :430 reads `FI[rfile + "::" + name].access`, which only a Python parse ever fills. Frontend side: _a3_fe.py:78 `_WRITE_METHODS = frozenset({"POST", "PUT", "PATCH", "DELETE"})` and :363 `w = sum(1 for c in _cs if (c.get("method") or "GET").upper() in _WRITE_METHODS)` — `_cs` are web-arm fetch call sites, so `wsites` (:365) can only be set by a fetch; :613 `_wsink = set(pid for pid, p in pieces.items() if p.get("wsites"))` is therefore empty. `_STORE_CALLEES` (:53) and `_CACHE_CALLEES` (:68) are client-state library rosters (zustand/jotai/react-query/swr) — neither has any hook for a `pool.query()` call. Confirmed by grep of _a3_fe_extract.mjs: its only `query`/`table` hits are queryKey and the store-type roster (:129, :150).

**Feed:** c4-graph.json `stats.access_edges: 0`; `stats.fe.by_channel: {chrome: 58, read: 0, write: 0}`; `stats.fe.fed2w_max: 0`; `stats.fe.write_pieces: 0`; `stats.fe.write_sites: 0`; `stats.fe.cache_pieces: 0`; `stats.fe.client_stores: 0`. archmap.json `function_insight: {}` (0 entries) and `model_insight: {}` (0 entries). levels.json `use_edges: []`, `schema_edges: []`, `schema_owner: {}`.

**Fix:** Second pass of the same `_a3_sql.py` arm: regex the string and template-literal arguments of `.query(...)` / a tagged `sql` template for `INSERT INTO <t>` · `UPDATE <t>` · `DELETE FROM <t>` (rw='w') and `FROM <t>` · `JOIN <t>` (rw='r'), attributing each site to the enclosing export/method by the D3 column-0 rule _a3_web.py already applies to fetch sites. Emit `{model, table, rw}` lists into `archmap.function_insight["<file>::<fn>"].access.ops` — the exact shape _orm_access returns, which _a3_graph.py:719-727 and _a3_levels.py:430 already consume unchanged. STATE THE COUPLING HONESTLY: the graph anchors access WIRES on endpoint nodes (`endpoint_access[bkey]`, _a3_graph.py:717) and levels anchors them on drawn fn nodes (which require a trace root), so with no endpoint and no server-action root the ops land on the archmap, `map_census` and `touches` but draw no wire. The ops themselves are days; the drawn wire additionally depends on server actions becoming trace roots.

#### `data-layer-emptiness-never-said` · high · hours · lens providers-streaming

**Missed:** A reader opening this universe sees 78 nodes of frontend structure and a Sources panel that names twelve inputs, none of which mentions models, tables or access. Nothing says "this map read 0 tables across 3 files containing 24 SQL statements". The suite's own honest-empty machinery — which does fire correctly for the schema arm, the twin pass, unparseable files and route mounts — has no equivalent for the data layer, so its absence is indistinguishable from a genuinely stateless app. The blind-spot reporter that would have caught it (element_census) is itself skipped for this repo.

**App:** All 24 SQL statements live in files this project's center.config.json claims — src/adapters/postgres/** is claimed under `code.web` by the platform/auth/users entities; `code.api`/`models`/`schemas`/`services` are all `[]` because those layers only accept Python.

**Pipeline:** (a) /home/khujta/projects/gabe_lens/skills/gabe-map/scripts/mapquery.py:314 — `map_health` has a `schemas_zero` sentinel but it is gated `if (n_ep and not n_sch)`; with 0 endpoints it is False, and there is no `models_zero`/`tables_zero` sentinel at all. (b) /home/khujta/projects/gabe_lens/templates/center/shell/gabe-universe.html:6070-6107 — the Sources section emits rows for scale, entity model, names, elements, cross-entity touches, files skipped, route mounts, twin pass, web bridge, graft wiring, frontend arm, homing evidence and levels feed; there is no models/tables/access row, and the `elements` row is gated on `st.elements.present`. (c) /home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:1014 `if layer in ("web", "mobile") and ".py" not in pat: continue` — every keypro claim is a `web` layer with no ".py" in the pattern, so `_claim_roots` returns [] and element_census produces nothing, which is why build_center_a3.py:2053-2055 never writes an `element_census` block. (d) /home/khujta/projects/gabe_lens/skills/gabe-pulse/scripts/angles.py:404 `s11_model_census` reads `archmap.model_census` and returns None on an empty `unclaimed`.

**Feed:** Ran map_health(archmap, c4) on the keypro center: `schemas_zero: false`, and the returned object has no models/tables key of any kind; every health_key block reads `state: "not_emitted"` because archmap.json carries no `route_mounts` sentinel, so the one thing the reader IS told is "an older map that never ran the pass — regen to know" about passes that in fact ran. Ran the pulse angles on the keypro root: s11_model_census → None, s13_route_file_census → None, s10_web_bridge → None; only s15_fe_unknown fires (1 Pascal .tsx export). archmap.json top-level keys are coverage, entities, function_insight, generated, guard_insight, head, model_census, model_insight, schema_homing, test_insight, version — no unparseable, no element_census, no file_census, no route_census.

**Fix:** Two cheap, independent pieces. (1) mapquery.py:314 — add a `models_zero` sentinel beside `schemas_zero`, ungated by endpoint count: when `sum(len(e.get("models") or []) for e in entities.values()) == 0` while entities claim N code files, say "the model arm extracted nothing across N claimed file(s) — an EMPTY arm, not a clean one", and give pulse an S19 angle reading it. (2) gabe-universe.html — add one `srcRow("model", "data layer", ...)` reading `stats.access_edges` + the entity model counts, so a zero is printed rather than omitted; while there, render `st.web.reason` in the existing web-bridge row (:6096 drops it, so "no REST api-call idiom detected" — which the FEED correctly carries — never reaches the page). A partial config-only mitigation exists today and is worth naming: claiming the .ts files under `code.models` makes parse_models return [] but registers them in `unparseable_files` (verified: 3 files, "syntax error at line 9/10/2"), which fires the station's "files skipped" row — mislabeled ".py file(s) the scanner could not parse".

#### `server-actions-not-roots` · high · new-arm · lens tests-estate — **VERIFIED confirmed** (→ silent-missing · high · said on page: False)

**Missed:** keypro-front has no HTTP handlers; every write and every read-with-an-argument enters through a React Server Action. There are 4 "use server" files exporting 13 actions (login · logout · requestReply · requestReadiness · requestConversationReset · createUser · setUserEmail · changeUserRole · setUserActive · resetUserPassword · deleteUser). The pipeline has no concept of them. Each FILE collapses to ONE `module` piece named "actions" with mclass `config` (because `src/app/...` matches _CONFIG_SEGS), so the app's entire request surface is drawn as four grey wiring modules — the LOWEST-value mclass — while the 13 action names survive only as a string list on `pieces[].exports`. A reader of the universe cannot see: that a request surface exists at all, which operations the app offers, the chain from an action to authService→userService→port→postgres adapter, any journey, any d2w heat, any gate. Nothing on the page says so.

**App:** src/app/(platform)/chat/actions.ts:1 ("use server") exports requestReply:38, requestReadiness:51, requestConversationReset:61 — each resolves the session and calls chatService; src/app/(platform)/admin/users/actions.ts:1 exports 6 mutations (createUser:51 … deleteUser:121), each calling userService through asActor:31; src/app/login/actions.ts:16 login; src/app/(platform)/actions.ts:7 logout

**Pipeline:** `grep -rn "use server" /home/khujta/projects/gabe_lens/templates/center/generators/` returns ZERO hits — no generator knows the directive. _a3_fe.py:194-216 classify_export has branches for route/store/hook/component/fe-type only; a non-Pascal `async function` export returns None and folds into the file's module piece. _a3_code.py:417 parse_endpoints is FastAPI-ast-only and is fed the entity `code.api` globs, which are `[]` for all five keypro entities. _a3_levels.py:235 declares _handlers "the handler ids — the ONLY roots of fn_edges", seeded at :241 from function_insight[].handler, :247 from amap.boot_roots, :254 from amap.task_roots — three Python-only sources. _a3_fe.py:387-388 gives the action files mclass `config` via _CONFIG_SEGS ("app", line 117).

**Feed:** c4-graph.json fe.pieces: `{kind:"module", id:"fe:src/app/(platform)/chat/actions.ts", name:"actions", mclass:"config", exports:[requestConversationReset, requestReadiness, requestReply]}` — same shape for the other 3. stats.fe.by_mclass.config = 4 (exactly the four action files). archmap.json has NO `endpoints` key at all; c4 `l2` has all five slugs present with `nodes: []`; stats.cross_edges 0; levels.json fn_nodes 0 / fn_edges 0 / use_edges 0; workflows.js and workflows.draft.js both honest-empty (the drafter reads endpoint nodes, draft-workflows.py:145-151).

**Fix:** A server-action arm, smallest first: (1) _a3_fe_extract.mjs — carry the file-level and function-level "use server" directive onto each export record (one `sourceFile.statements[0]` string-literal check); (2) _a3_fe.py classify_export — an exported async function in a "use server" file is kind `action` (new _PRINCIPAL rank above `module`), so each of the 13 becomes its own piece with a span and a home; (3) _a3_graph.py — mint each action as an L2 node in the endpoint column (label `ACTION <name>`, the same slot endpoints use) so cross_edges/bridge wiring has something to join to; (4) _a3_levels.py:241 — add the action ids to _handlers beside boot_roots/task_roots so the fn_edges walk has roots; (5) station legend row + tests/frontend + tests/arch-graph cases. Step (2) alone already gives the reader 13 named pieces instead of 4 "config" blobs.

**Verifier:** App holds: 4 "use server" files export 11 async server actions (not 13 — src/app/(platform)/actions.ts:7 logout · chat/actions.ts:38,51,61 · admin/users/actions.ts:51,72,85,95,108,121 · login/actions.ts:16); the feed's exports lists total 11. Pipeline holds: `grep -rniE 'use server|directive' templates/center/generators/` (incl. _a3_fe_extract.mjs) = 0 hits; _a3_fe.py:194-216 classify_export has no branch for a non-Pascal function → None → folds into the file's module piece; _a3_fe.py:117 _CONFIG_SEGS contains "app" and :388 assigns mclass "config"; _a3_levels.py:235-254 seeds _handlers only from function_insight.handler / boot_roots / task_roots (all Python). Feed holds: c4-graph.json fe.pieces has exactly 4 {kind:"module", name:"actions", mclass:"config"} pieces with the 11 names as strings; stats.fe.by_mclass.config=4; l2 nodes = 0 for all five slugs; endpoint nodes = 0; stats.cross_edges=0; levels.json fn_nodes/fn_edges/use_edges = 0/0/0; archmap.json has no `endpoints` key. Aggravating fact the finder missed: gabe-universe.html:4461-4463 tier presets put `module` in koff at T0, T1 and T2, and the station boots at T1 (:6558 `__uniSetTier(1)`), so the four action modules are INVISIBLE until the reader picks T3 "Everything"; when drawn they wear the `config` badge whose tooltip (:4296/:4750) reads "config — app wiring (an app/config/setup path)". Nothing says the floor: the Sources web row (:6096) renders "0 fetches matched to endpoints · 0 unmatched" and drops `reason`; the levels row (:6107) says "present — functions · communities · journeys" over a 0-fn feed; mapquery.py:292 map_health has no state for zero endpoints (`schemas_zero` fires only when n_ep>0) and reports web as present, everything else not_emitted; keypro's arch-endpoints.html shows a bare "0" under "the HTTP surface, parsed from the FastAPI decorators" and the sidebar's "config-only" is the adoption-rank label, not a request-surface statement.

#### `station-silent-about-missing-request-arm` · high · hours · lens tests-estate

**Missed:** All five gaps above would be forgivable if the station said so; it does not. The Sources section renders a `web bridge` row reading "0 fetches matched to endpoints · 0 unmatched", which a reader parses as "this frontend makes no requests" — while the emitter's own honest verdict, `reason: "no REST api-call idiom detected"`, is a field the row never prints. Every row that would name a backend gap is guarded on a stats key a Python-free repo never produces, so a repo with 0 .py files gets ZERO rows saying the backend arm found nothing; the five L2 entity columns just render empty. And the `levels feed` row asserts "present — functions · communities · journeys" purely because `window.GABE_LEVELS` is defined, over a feed with fn_nodes 0, fn_edges 0, use_edges 0.

**App:** n/a — this is the reporting layer over the app. The app's request surface is the 13 actions + proxy.ts cited above (src/app/**/actions.ts, src/proxy.ts:16); the point is that a reader of the universe is given no signal that any of it is absent.

**Pipeline:** gabe-universe.html:6107 `w.append(srcRow("layers","levels feed", window.GABE_LEVELS?"present — functions · communities · journeys":"absent", …))` — truthiness of the global, never a count. gabe-universe.html:6096 builds the web row from `st.web.extractor` + matched/unmatched/dynamic/unhomed/other_roots/sdk_methods and never reads `st.web.reason` (emitted at _a3_web.py:472). The gap rows are each guarded on an absent key: :6087 `if(st.elements&&st.elements.present)`, :6091 `if(st.unparseable&&st.unparseable.count)`, :6092 `if(st.route_mounts)`, :6094 `if(st.fn_similarity)`.

**Feed:** levels.js line 1 defines window.GABE_LEVELS (truthy) while levels.json has fn_nodes [] · fn_edges [] · use_edges [] · cross_edges [] — so the row renders "present — functions · communities · journeys" over nothing. c4-graph.json stats has no `unparseable`, `elements`, `route_mounts` or `fn_similarity` key (verified by key test), so none of those rows render. stats.web = {present:true, fetch_sites:0, matched:0, unmatched:[], reason:"no REST api-call idiom detected"} — the reason is in the feed and only in the feed.

**Fix:** Three edits in gabe-universe.html (and its example twin — the two-file law): (1) :6107 — count before claiming: `present — N function(s) · M call edge(s)`, and say "present but empty (no backend chains in this feed)" at zero; (2) :6096 — prepend `st.web.reason` when `st.web.fetch_sites===0`, so the row reads "no REST api-call idiom detected — 0 fetches"; (3) add one unconditional row: when the L2 columns hold zero endpoint nodes, say "backend arm: 0 endpoints — the scanner reads Python/FastAPI only; this repo declares no code.api files", read from the entity config's empty `code.api` claims. That single row is what turns every gap above from silent into said.

#### `junit-absent-reads-as-zero-tests` · high · config-only · lens docs-config-identity

**Missed:** keypro-front's entire verification estate is invisible. The test arm has exactly one input — a junit XML per corpus — and the repo has never produced one, so the center reports "0 tests · 0 failed", "0 case(s) in 0 file(s)", "0 file(s) · 0 unclaimed" and a nav badge of Tests 0. Only test-matrix.html says why ("No junit capture loaded"); every other surface prints the zeros as if a suite had run and found nothing. A reader concludes the app is untested when it is in fact the most thoroughly tested thing in the repo-study set (models, services, adapters, components, plus a live-postgres integration test).

**App:** 49 co-located specs: src/models/user.test.ts, src/services/{auth,chat,greeting,user}-service.test.ts, src/adapters/**/*.test.ts, src/components/**/*.test.tsx, src/app/(platform)/chat/actions.test.ts, src/adapters/postgres/postgres-integration.test.ts; 464 `it(`/`test(` call sites, 91 `describe(` blocks; package.json scripts.test = "vitest run"; vitest.config.mts (jsdom + tsconfigPaths)

**Pipeline:** templates/center/generators/_a3_tests.py:303-307 — `for c in D.CFG.get("corpora", []): j = D.load_junit(corpus); if not j: continue` — the whole join is junit-gated, and the ONLY on-disk test enumeration in the pipeline is the e2e spec glob (build_center_a3.py:863-867); templates/center/generators/_a3_feature.py:164-167 `kind_state` returns "no cases matched" whenever cases==0, never consulting the `present` flag it computes one function up at _a3_feature.py:105; build_center_a3.py:973-978 `_bucket_rows` prints `tests/results/<key>-junit.xml` unconditionally with no absent marker, while the e2e row three lines later does get a `not wired` tag

**Feed:** archmap.json test_insight: by_endpoint {} · by_function {} · by_model {} · exercises {} · case_home {} · case_own {} · by_file 90 entries all `reach: []`; tests.html "0 junit cases (0 web), 0 failed" and nav "Tests 0"; test-files.html "0 file(s) · 0 unclaimed"; test-corpora.html row "web | vitest | 0 | 0 | tests/results/web-junit.xml" (no absent marker); the single honest line is test-matrix.html "No junit capture loaded — the ledger renders from the corpus record, and there is none to read"

**Fix:** Two moves. (a) In the repo: center.config.json `commands.junit: ["pnpm vitest run --reporter=junit --outputFile=tests/results/web-junit.xml"]`, then `scripts/refresh_center.sh junit` — the loader already understands vitest's shape (_results_ingest.py:29 names it explicitly). (b) In the pipeline, so the next foreign repo is not silently zeroed: thread `present = junit_by.get(key) is not None` into build_center_a3.py:873-882 and :973-978 and into _a3_feature.kind_state so an absent corpus renders `not captured` + the command that would capture it, instead of 0/0.

#### `next-proxy-middleware-not-a-gate` · medium · hours · lens hexagonal-structure

**Missed:** keypro guards access in three layers and documents the split in the source: the edge proxy is optimistic (cookie only, cannot see the role), the (platform) and (platform)/admin server layouts are authoritative (they read the role from the DB), and the services refuse again on every operation. The pipeline has a first-class notion of exactly this — a `middleware` L2 column and a gates tool described as "the ASGI middleware on every request" — but both are FastAPI-only AST passes. So the universe draws one of the three layers, as a grey `mclass: logic` module named "proxy" sitting in the fe·platform cluster with no gate marking and no edge to the routes it matches. A reader sees no request gate at all in this app.

**App:** src/proxy.ts:1-20 — `export function proxy(request: NextRequest): NextResponse`, session-cookie redirect, docstring: "Proxy (Next.js 16's renamed middleware) … The authoritative guards are the (platform) and (platform)/admin server layouts."; it also exports `config` (the matcher).

**Pipeline:** _a3_code.py:2119 `_endpoint_middleware(node, route_dec, …)` and _a3_code.py:2374 `parse_app_middleware(repo, entity_code)` are Python-AST passes over FastAPI decorators and `app.add_middleware`; _a3_code.py:2330 `resolve_middleware_targets` resolves `Depends(...)` gate callables. Nothing in _a3_fe.py or _a3_fe_extract.mjs looks for a root `proxy.ts`/`middleware.ts` or a `config.matcher`. The `middleware` node kind exists in the L2 column order (_a3_graph.py:72) and is fed only from ep["middleware"] (_a3_graph.py:698-699), which is Python-only.

**Feed:** c4-graph.json fe piece = {"id":"fe:src/proxy.ts","kind":"module","mclass":"logic","area":"root","name":"proxy","exports":["config","proxy"]} — no gate/middleware marking. c4-graph.json l2.nodes is an empty list, so the `middleware` column is drawn with zero nodes; archmap.json carries no `route_mounts`, `route_census` or `file_census` key, so nothing names the omission either.

**Fix:** A small Next-side gate pass in _a3_fe.py (or a `_a3_next.py` beside it): a root-level `proxy.ts`/`middleware.ts` exporting `proxy`/`middleware` plus `export const config = { matcher: [...] }` mints a `middleware` L2 node whose matcher globs are resolved against the route pieces' labels, so the existing `middleware` column and the gates tool light up on a Next tree. The node kind, column and legend row already exist — only the emitter side is missing.

#### `route-guards-not-gates` · medium · days · lens tests-estate — **VERIFIED confirmed** (→ silent-missing · medium · said on page: False)

**Missed:** The app's authoritative authorization lives in server components: PlatformLayout redirects anonymous traffic, AdminLayout resolves the role and keeps non-admins out, AdminUsersPage re-checks before listing. In FastAPI these are `Depends(get_current_user)` / `Depends(require_permission(...))`, which the pipeline turns into `endpoint.middleware` entries with a `gate` flag — the thing `mcp__gabe-map__gates` answers on, the station paints as the level-2 gate floor, and journeys render as a gate step right after the endpoint. Here they are ordinary `route` pieces with one undifferentiated `fecall chrome` wire to container.ts. A reader asking "what guards /admin" gets nothing, and the station reports the app as entirely ungated.

**App:** src/app/(platform)/layout.tsx:14-17 `const user = await authService.currentUser(); if (!user) redirect("/login")`; src/app/(platform)/admin/layout.tsx:17-20 `if (!user || !isAdmin(user)) redirect("/chat")` (its own comment at :7-15 names itself the authoritative guard behind the optimistic proxy); src/app/(platform)/admin/users/page.tsx:11-14 and src/app/login/page.tsx:10-12 do the same.

**Pipeline:** The gate concept exists only on a FastAPI endpoint dict: _a3_code.py:2119 _endpoint_middleware (decorator / route-dep scan) and _a3_code.py:2330 resolve_middleware_targets, consumed at _a3_graph.py:698-699 `if ep.get("middleware"): enode["middleware"] = ep["middleware"]`. _a3_fe.py mints no gate/guard/middleware key on a `route` piece anywhere (classify_export :194-216 and the piece record built below carry kind/name/file/span/home/label only).

**Feed:** c4-graph.json stats.gate_endpoints 0 and stats.middleware_endpoints 0. The route piece records carry no gate field: `{"file":"src/app/(platform)/admin/layout.tsx","kind":"route","label":"/admin · layout","name":"AdminLayout","span":[16,23],"home":"fe·users"}`. Its only outbound wires in fe.edges are `fecall chrome -> fe:src/composition/container.ts` and `fecall chrome -> fe:src/models/user.ts` — the isAdmin role check is the same weight as any other call.

**Fix:** In _a3_fe.py, after pieces are built: a `route` piece whose span contains a call to `redirect(`/`notFound(`/`forbidden(` (the extractor already records per-export call callees for the store/cache/router rosters — reuse that list, no name-list of project symbols) gets `guard: true` plus the redirect target literal. Then _a3_graph.py, where it copies ep["middleware"] onto an endpoint node (:698), copies `guard` onto the fe route node so the station's existing gate badge and the `gates` MCP tool have a frontend arm. Cheapest honest version: emit the flag and count it in stats.fe, draw later.

**Verifier:** App: src/app/(platform)/admin/layout.tsx:17-20 `if (!user || !isAdmin(user)) redirect("/chat")` (+ (platform)/layout.tsx:14-17, admin/users/page.tsx:11-14, login/page.tsx:10-12). Pipeline: the only gate mint is _a3_code.py:2128 `"gate": _is_mw_gate(name)` on a FastAPI dep; _a3_fe.py has zero guard/redirect/gate handling; _a3_graph.py:698-699 copies middleware onto endpoint nodes only; gate_endpoints counted at _a3_graph.py:1504 over endpoint nodes only. Feed: c4-graph.json stats.gate_endpoints=0, middleware_endpoints=0; route piece keys are exactly area/file/home/homed_by/id/kind/label/name/span; "redirect" occurs 0 times in c4-graph/levels/archmap; AdminLayout's only wires are fecall chrome -> fe:src/composition/container.ts and fecall chrome -> fe:src/models/user.ts (the sole `gate` key in the JSON is $.models.stats.caps.gate=1, a seeded-model hub-floor cap). Live mcp__gabe-map__gates on keypro: endpoints_total 0 · gated_total 0 · reason "the map recorded no Depends on any endpoint" — FastAPI vocabulary, no frontend-guard floor named. Page: none of the 13 Sources rows (scale · entity model · names · elements · cross-entity touches · files skipped · route mounts · twin pass · web bridge · graft wiring · frontend arm · homing evidence · levels feed) mentions gates/guards/middleware; the station never reads stats.gate_endpoints; the legend skips zero-node kinds (gabe-universe.html:4860 `if(!K[k]) return;`) so no middleware row / role:gate badge renders; map_health (mapquery.py:292) has no gate block. Fix sketch viable: _a3_fe_extract.mjs:114-121 records every callee name per export regardless of resolution (externals {ext:true} at :217), so `redirect` is already in the per-export calls set.

#### `risk-tab-blind-to-uncaptured-corpus` · medium · hours · lens docs-config-identity

**Missed:** index.html's Risk tab is the page that exists to list unverified surfaces. It lists three: Coverage (no reporter wired), e2e junit, Manual angles. It has no row for the declared `web` corpus whose junit was never captured — the single largest unverified thing in the repo. The gap list is a hardcoded literal, not derived from the corpora, so a declared-but-uncaptured corpus can never appear there. The same literal also hardcodes another project's decision id: keypro's Risk tab says "e2e junit unverified — not wired — D121 local-only", ignoring this repo's own config (`junit_gap_tag: "not-wired"`, `not_wired_note: "no e2e suite in this repo"`) and citing a D121 that exists in no keypro document.

**App:** docs/site/center/center.config.json declares one corpus (key web, runner vitest) and e2e {runner: "none", junit_wired: false, junit_gap_tag: "not-wired", not_wired_note: "no e2e suite in this repo"}; the repo has 49 vitest files and no e2e suite at all

**Pipeline:** templates/center/generators/build_center_a3.py:1275 `_gaps = [("e2e junit", "not wired — D121 local-only")]` — unconditional (never reads E2E['junit_wired'] / ['junit_gap_tag'] / ['not_wired_note']), followed at :1276-1278 by rows for coverage and walks only; no loop over CORPORA adds a gap row for a corpus whose `load_junit` returned None

**Feed:** index.html Risk rows: "Coverage unverified | gap | no reporter wired", "e2e junit unverified | gap | not wired — D121 local-only", "Manual angles unverified | gap | .kdbp/walks.jsonl absent" — no web/unit corpus row; "D121" appears once in the whole emitted center and comes from the generator, not from keypro

**Fix:** In build_center_a3.py:1275, build `_gaps` from data: one row per corpus where `junit_by[key] is None` ("web junit unverified | <results>/web-junit.xml absent"), and render the e2e row from E2E's own `junit_gap_tag`/`not_wired_note`, skipping it entirely when `junit_wired` is true.


### drawn-wrong

#### `no-write-channel-without-fetch` · high · hours · lens tests-estate

**Missed:** The station's FE write-spine instrument (the blue→magenta FEBAND gradient banded by `fed2w`, the `__uniFED2W` toggle) has nothing to paint on this app, and all 56 call wires are labelled `chrome` — an active assertion that they touch neither state nor a write. But `ChatScreen → chat/actions.ts` IS the app's central write (requestReply, which drives the AI backend), `UsersScreen → admin/users/actions.ts` carries all 6 user mutations, and `LoginForm → login/actions.ts` is the session write. React Server Actions are invoked over HTTP POST by protocol, so the very `_WRITE_METHODS` law the detector encodes applies — it just never sees the call site because its only input is the fetch arm.

**App:** src/components/app/chat-screen.tsx imports and calls the chat actions; src/components/app/users-screen.tsx imports admin/users/actions.ts (6 mutations); src/components/app/login-form.tsx imports login/actions.ts and drives it via useActionState (src/app/login/actions.ts:6 names the hook); src/app/(platform)/admin/users/actions.ts:3 imports `revalidatePath` and calls it at :43 — an explicit post-write cache invalidation.

**Pipeline:** _a3_fe.py:78 `_WRITE_METHODS = frozenset({POST, PUT, PATCH, DELETE})` is applied only at :363-365 over `calls` handed in by the web arm (`pieces[_tp]["wsites"] = w`). _a3_fe.py:613 `_wsink = set(pid for pid, p in pieces.items() if p.get("wsites"))` — with no fetch sites the sink set is empty, so the BFS at :615-624 assigns no fed2w, and the channel tagger at :627-633 falls to `chrome` for everything that is not a store read.

**Feed:** c4-graph.json stats.fe.by_channel = {chrome 58, read 0, write 0}; stats.fe.fed2w_max 0; stats.web.fetch_sites 0. Decoding fe.edges: `fecall chrome | fe:src/components/app/chat-screen.tsx#ChatScreen -> fe:src/app/(platform)/chat/actions.ts` and `imports | fe:src/components/app/users-screen.tsx#UsersScreen -> fe:src/app/(platform)/admin/users/actions.ts` (the six admin mutations arrive as a bare `imports` edge with no channel at all).

**Fix:** Depends on the action arm's directive flag (gap server-actions-not-roots step 1), then two lines in _a3_fe.py: where `wsites` is set at :363-365, also set it for a piece whose target file carries the "use server" directive — a server-action call site is a POST by protocol, the same library-agnostic verb rule the code already states. _wsink at :613 then picks them up unchanged and the whole fed2w BFS, channel tagger and station heat light up with no station edit. Standalone fallback without the directive: treat an edge into a file matching `**/actions.{ts,tsx}` under an app router root as a write — cheaper but a path heuristic, which the arm's own design record rules against.

#### `untested-surface-false-all-clear` · high · hours · lens docs-config-identity

**Missed:** test-elements.html — the page whose whole job is naming what nothing verifies — prints "No untested elements at this altitude — every endpoint, model and function has at least one receipt." That is the strongest positive claim in the testing estate, and it is produced by an empty loop: the element roster it iterates (endpoints, models, schemas, function_insight) is the Python/FastAPI scanner's output, which is 0/0/0 for a TypeScript repo, and the receipts it checks against are also empty. Zero elements × zero receipts renders as a clean bill of health for an app with 223 exported symbols.

**App:** 137 ts/tsx files, 0 Python; 223 exported symbols across 87 files; raw SQL tables in src/adapters/postgres/schema.ts (CREATE TABLE users, sessions) and domain types in src/models/{user,chat,session,email,credentials}.ts — none of which the element roster can see

**Pipeline:** templates/center/generators/_a3_tests.py:568-604 `untested_surface` iterates `amap['endpoints']`, `amap['models'] + amap['schemas']` and `_a3_code.function_insight(repo)`, then at :601-603 falls through to the all-clear string when `rows` is empty — there is no guard for "the roster itself is empty" or "no corpus was read"; `untested_counts` (:607-624) has the same shape

**Feed:** archmap.json: entities[*].endpoints = 0 for all five, models 0, schemas 0; function_insight {} (0 entries); model_insight {} ; model_census {}; test_insight.by_* all empty — and test-elements.html renders "No untested elements at this altitude — every endpoint, model and function has at least one receipt." (tests.html card restates it as "0 element(s) no case or spec touches")

**Fix:** In _a3_tests.untested_surface/untested_counts, branch before the all-clear: if the roster is empty (`not amap['endpoints'] and not models and not fi`) say "no elements of these kinds were extracted from this repo — the element scanner is Python-only", and if no corpus was loaded say "no receipts were read" rather than asserting coverage. One string each, plus the roster/corpus emptiness check.

#### `guard-cards-unguarded-without-corpus` · high · hours · lens docs-config-identity

**Missed:** The Guard lens joins declared exports against the symbols tests import. With no corpus loaded the right-hand side is the empty set, so every export in the repo is unguarded by construction — and the board mints action cards from it. Two cards tell the reader that agent-run-types.ts and models/chat.ts have no test naming them and "a refactor here moves in the dark", when agent-run-mapper.test.ts and chat-service.test.ts import symbols from both. The card's hedge ("Name-matched, so this is a floor, not a coverage figure") describes a measurement limitation, not the absent input, so it reads as measured rather than unmeasurable.

**App:** src/adapters/agent-run/agent-run-mapper.test.ts:5 `import type { AgentRunEvent } from "./agent-run-types";` and agent-run-chat-assistant.test.ts:6 `import type { AgentContent, AgentRunEvent } from "./agent-run-types";` (file carded as 17/17 unguarded); src/services/chat-service.test.ts:4 `import type { AssistantReply, AssistantReplyChunk, ChatRequest, Conversation } from "@/models/chat";` (file carded as 14/14 unguarded)

**Pipeline:** templates/center/generators/_a3_guard.py:84 `named = _named_by_tests(exercises)` with :149 `unguarded = [d for d in declared if d not in named]` — no honest-empty branch when `exercises` is {} (contrast _a3_tests.derive_journeys:479-481, which returns {} rather than emit on an empty join); the card copy at _a3_guard.py:280-285 hedges the name-match but never the missing corpus

**Feed:** archmap.json guard_insight.totals: web_declared 223 · web_unguarded 223 · fn_named 0 · proofs_seen 0 (87 files); board.html renders 2 distinct Guard cards — "agent-run-types.ts — 17 unguarded exported symbols" and "chat.ts — 14 unguarded exported symbols", each "17 of 17 … A refactor here moves in the dark."

**Fix:** In _a3_guard.guard_insight, take the corpus state as an argument: when no corpus was loaded (all `present` False), emit the declared counts with `named: null` and have guard_moves (_a3_guard.py:237) emit no cards — the card is an action, and there is no action while the input is missing. The totals row can still say "223 declared, 0 joined — no corpus captured".

#### `next-app-dir-classed-as-config` · medium · hours · lens hexagonal-structure

**Missed:** The module class is decided purely by directory name. In a Vite/TanStack tree `src/app/` is the bootstrap folder (providers, store setup) — hence its place in the config roster. In a Next.js App Router tree `src/app/` IS the router, and everything that is not a page or layout there is application logic. Result: all four of keypro's server-action modules — the app's entire mutation surface, the only code path that writes to postgres or resets a conversation on the AI backend — wear mclass `config`, which the station's own legend glosses as "grey wiring". Every one of the app's four `config` modules is a server-action file; there is not a single genuine config module in the class. A reader scanning for where this app changes state reads past the only four nodes that do.

**App:** src/app/(platform)/admin/users/actions.ts:51-128 (createUser, setUserEmail, changeUserRole, setUserActive, resetUserPassword, deleteUser); src/app/(platform)/chat/actions.ts:38-66; src/app/login/actions.ts:16; src/app/(platform)/actions.ts:7

**Pipeline:** _a3_fe.py:117 `_CONFIG_SEGS = frozenset({"app", "config", "setup", "bootstrap", "env"})` and _a3_fe.py:384-389, where `segs = set(p["file"].split("/")[:-1])` then `"config" if segs & _CONFIG_SEGS`. For src/app/(platform)/actions.ts segs = {src, app, (platform)} → config. The test fires on the folder NAME with no check that _NEXT_APP_RX (already present at _a3_fe.py:127, and already matching six files in this tree) proves the folder is a router rather than a bootstrap dir.

**Feed:** c4-graph.json stats.fe.by_mclass = {config:4, logic:29, model:4}; the four config pieces are exactly fe:src/app/(platform)/actions.ts · fe:src/app/(platform)/admin/users/actions.ts · fe:src/app/(platform)/chat/actions.ts · fe:src/app/login/actions.ts. Station legend: __BADGE_COL.mclass comment at gabe-universe.html:4290 reads "config=grey wiring".

**Fix:** _a3_fe.py: pass a tree-level `next_app` flag (true when any file matches _NEXT_APP_RX) into the mclass pass at :384-389 and drop `app` from the segment test when it is set; better still, once gap 1 lands, give a "use server" module its own mclass (`action`) with its own badge, since that is what it is.

#### `jsxless-next-page-not-a-route` · medium · hours · lens hexagonal-structure

**Missed:** The one piece that decides whether a visitor sees the login page or the platform (`redirect((await authService.currentUser()) ? "/chat" : "/login")`) returns no JSX, so classify_export's Next-route rule is never reached: the `isDefault && _NEXT_APP_RX` clause sits INSIDE the `hasJsx` branch. The piece falls all the way through to the O1 honest-unknown promotion. Consequence in the drawn picture: route count is 6 when the app has 7 routes, the `/` label never appears anywhere in the universe, and the app's entry point is a grey unknown blob captioned "Home". It is also the single fe-unknown in the feed, so pulse S15 will nag about it forever as a classifier residue when it is in fact a perfectly well-defined Next page. This is a general Next idiom, not a keypro quirk — any redirect-only or notFound-only page/layout lands the same way.

**App:** src/app/page.tsx:5-7 — `export default async function Home() { redirect((await authService.currentUser()) ? "/chat" : "/login"); }`, no JSX in the body

**Pipeline:** _a3_fe.py:209-215 — `jsx = bool(ex.get("hasJsx"))` at :209, then the whole Pascal/route/component branch at :210-215 requires `jsx` to be true; the Next clause `(ex.get("isDefault") and _NEXT_APP_RX.search(path))` at :213 is nested inside it and is unreachable for a JSX-less default export. Falling to :216 `return None`, the caller's O1 path at _a3_fe.py:288-294 stamps `fe-unknown`. `route_label` (_a3_fe.py:180-191) is only consulted for pieces already classified as routes, so no `/` label is computed.

**Feed:** c4-graph.json fe piece `fe:src/app/page.tsx#Home` = {"kind":"fe-unknown","name":"Home","area":"app","span":[5,7]} with NO `label` key. stats.fe.by_kind = {component:32, fe-type:112, fe-unknown:1, hook:1, module:37, route:6, store:1}; stats.fe.excluded.pascal_no_jsx = 1. No route piece anywhere carries the label "/".

**Fix:** _a3_fe.py:209 — hoist the Next test above the JSX gate: `if ex.get("isDefault") and _NEXT_APP_RX.search(path): return "route"` before `jsx = bool(...)`. A default export of an app/**/{page,layout,template,error,loading,not-found} file is the route whatever it returns; the comment at :213 already says exactly this, the code just applies it one branch too late.

#### `next-route-group-label-collision` · medium · hours · lens hexagonal-structure

**Missed:** keypro's authoritative auth guard is src/app/(platform)/layout.tsx: it resolves the session server-side and redirects an anonymous request to /login before any platform content renders (its own docstring says so, and notes that src/proxy.ts is only optimistic). src/app/layout.tsx is html/fonts/globals.css chrome. After the group segment is stripped both resolve to the path `/`, both get the ` · layout` role suffix, and both home to fe·platform — so the station draws two nodes with byte-identical labels inside one cluster. A reader looking for where access is actually decided has to click both to find out which is which; a reader who clicks neither concludes the layout is duplicated.

**App:** src/app/(platform)/layout.tsx:13-17 (`const user = await authService.currentUser(); if (!user) redirect("/login")`) vs src/app/layout.tsx:41-54 (RootLayout: Barlow/IBM Plex fonts, globals.css, katex css)

**Pipeline:** _a3_fe.py:130 `_NEXT_GROUP_RX = re.compile(r"/(\([^/]*\)|@[^/]+)(?=/|$)")` and _a3_fe.py:165-175 `_next_route_from_file`, where :171 `d = _NEXT_GROUP_RX.sub("", d) or "/"` erases `(platform)` and :175 appends only the file ROLE — nothing carries the depth or the group, and nothing checks whether the stripped path already belongs to another route piece. The station then draws `label: p.label||p.name` (gabe-universe.html:1350), so the label is all the reader gets.

**Feed:** c4-graph.json fe.pieces holds two route pieces with the same label and the same home: {"id":"fe:src/app/(platform)/layout.tsx#PlatformLayout","label":"/ · layout","home":"fe·platform"} and {"id":"fe:src/app/layout.tsx#RootLayout","label":"/ · layout","home":"fe·platform"}.

**Fix:** _a3_fe.py:165-175 — when the group-stripped directory is empty (the label would be bare `/`), keep the innermost stripped group as a parenthesised disambiguator (`/ · layout (platform)`), or detect the label collision in build_fe and fall back to the export name for the loser. The URL-truth rule stays intact; only the display string gains the segment Next itself uses to tell the two trees apart.

#### `mclass-badges-shapes-as-the-data-layer` · medium · hours · lens providers-streaming

**Missed:** `mclass` is the badge family whose stated job is "what a module DOES". On keypro it teal-badges src/models/email.ts, session.ts, user.ts and user-management.ts as `model` — legend text "model — the feature's data layer", hover "the feature's data shapes and mappers — turns API replies into what screens show" — and those four files contain zero SQL and zero fetches; the hover's claim about API replies is simply false in an app with no REST calls. Meanwhile src/adapters/postgres/{schema,pool,config,postgres-user-repository,postgres-session-repository}.ts and src/composition/container.ts wear the cyan `logic` badge ("feature rules and calculations"). A reader asking the map "where does this app's data live?" is pointed away from the database. The `api` class is unreachable here for the same root cause — `"api" if p.get("sites")` and there are no fetch sites — so mclass collapses to path segments alone.

**App:** src/models/session.ts:1-25 is `export interface Session`/`SessionRecord` + `isExpired()` — pure data, and grep -c for pool|query|SELECT|INSERT|sql over all four src/models/*.ts returns 0 for each. All SQL lives in src/adapters/postgres/*.ts.

**Pipeline:** /home/khujta/projects/gabe_lens/templates/center/generators/_a3_fe.py:384-389 — `mc = ("render-fn" if p.pop("jsx") else "api" if p.get("sites") else "model" if segs & _MODEL_SEGS else "config" if segs & _CONFIG_SEGS else "lib" if segs & _LIB_SEGS else "logic")`, with `segs = set(p["file"].split("/")[:-1])` — a pure path-segment test. _a3_fe.py:116 `_MODEL_SEGS = frozenset({"model", "models", "types", "schema", "schemas", "dto"})` matches src/models/; no roster contains "adapters" or "postgres", so the store falls to `logic`. The badge is rendered at gabe-universe.html:1847 and its legend/hover strings are literals at :4296 and :4749.

**Feed:** c4-graph.json `stats.fe.by_mclass: {config: 4, logic: 29, model: 4}`. Enumerating pieces: the 4 `model` pieces are fe:src/models/{email,session,user-management,user}.ts; `logic` includes fe:src/adapters/postgres/{config,pool,postgres-session-repository,postgres-user-repository,schema}.ts and fe:src/composition/container.ts. No piece is `api` (0 in by_mclass).

**Fix:** Cheapest honest fix is at the two legend literals, not the classifier: gabe-universe.html:4296 and :4749 assert an API-reply mapper role — gate that wording on `GABE_C4.stats.web.fetch_sites > 0` and otherwise read "data shapes — the types the store's rows map to". The real fix rides gap sql-in-ts-tables-not-drawn: once `_a3_sql.py` marks a file as carrying DDL/DML, add a `store` branch to the mclass ladder at _a3_fe.py:386, ahead of the `_MODEL_SEGS` path test (evidence-first, path second — the same precedence the homing ruling already uses), so the postgres adapters badge as the store and src/models keeps the shapes badge honestly.

#### `next-route-needs-jsx` · medium · hours · lens tests-estate — **VERIFIED confirmed** (→ drawn-wrong · medium · said on page: True)

**Missed:** `/` is the app's routing entry: it resolves the session and sends the visitor to /chat or /login. It returns no JSX (a redirect throws), so it never reaches the Next App Router promotion the pipeline already wrote for exactly this case and falls through to the honest-unknown bucket. The reader's route roster is missing the root URL; the `/` node carries no URL label (the other 6 routes all do), so the journey anchor's path-aware lead can never match it; and pulse S15 nags it as unproven classification residue when the path alone decides it. This is the canonical shape of a Next redirect/gate page, so the miss generalises past this repo.

**App:** src/app/page.tsx:5-7 `export default async function Home() { redirect((await authService.currentUser()) ? "/chat" : "/login"); }` — a default export of app/page.tsx with no JSX.

**Pipeline:** _a3_fe.py:213 `or (ex.get("isDefault") and _NEXT_APP_RX.search(path))` — the comment says "a default export of an app/**/page.tsx IS the route, whatever its name", but the clause is inside the guard opened at :210 `if _PASCAL_RX.match(name) and jsx and kind in (...)`, with `jsx = bool(ex.get("hasJsx"))` at :209. With jsx False the whole expression is False, classify_export returns None, and build_fe lands it at _a3_fe.py:294 `k = "fe-unknown"` / :295 `stats_x["pascal_no_jsx"] += 1`.

**Feed:** c4-graph.json fe.pieces: `{"file":"src/app/page.tsx","id":"fe:src/app/page.tsx#Home","kind":"fe-unknown","name":"Home","span":[5,7]}` — no `label`, unlike every other route piece. stats.fe.by_kind = {route 6, fe-unknown 1} and stats.fe.excluded.pascal_no_jsx = 1: the single unknown in the whole repo is the root route.

**Fix:** _a3_fe.py classify_export: lift the Next test above the JSX guard — insert `if ex.get("isDefault") and _NEXT_APP_RX.search(path): return "route"` immediately after the _ROUTER_CALLEES check at :200, and drop the now-redundant clause at :213. The label path (_route_label :189 → _next_route_from_file) already handles the rest. Add a tests/frontend fixture: an app/page.tsx default export with no JSX must come out kind `route`, label `/`.

**Verifier:** All three legs hold. APP: src/app/page.tsx:5-7 `export default async function Home() { redirect(... ? "/chat" : "/login"); }` — default export, no JSX. PIPELINE (suite copy templates/center/generators/_a3_fe.py): :209 `jsx = bool(ex.get("hasJsx"))`; :210-211 guard `_PASCAL_RX.match(name) and jsx and kind in (...)`; the Next clause :213 `or (ex.get("isDefault") and _NEXT_APP_RX.search(path))` sits INSIDE that guard, so with jsx=False classify_export returns None → build_fe :294 `k = "fe-unknown"` / :295 `stats_x["pascal_no_jsx"] += 1` (O2 promotion can't rescue it: nothing renders a page). The finder's ":200" for the _ROUTER_CALLEES check is actually :203 — cosmetic. FEED (keypro docs/site/center/c4-graph.json): piece `{"id":"fe:src/app/page.tsx#Home","kind":"fe-unknown","label":null,"home":"fe·platform","span":[5,7]}` while all six other src/app pieces are kind route with URL labels (/chat · /login · /admin/users · /admin · layout · / · layout ×2); stats.fe.by_kind route=6 / fe-unknown=1; excluded.pascal_no_jsx=1 — the repo's single unknown IS the root route. Piece-index edge check: Home carries exactly one wire, fecall → fe:src/composition/container.ts (same as LoginPage/ChatPage), so it is DRAWN, under its own name, with the wrong kind and no URL — drawn-wrong, not missing. Not a deliberate floor: tests/frontend/run.sh:416 fixture for the Next rule uses hasJsx:True; no no-JSX page fixture exists. Pulse S15 (skills/gabe-pulse/scripts/angles.py:587-591) fires at n≥1 and points to "render it somewhere, or add the O3 proof" — misdirection for a redirect page. Impact stays medium: the app's routing entry is absent from the route roster, the card's fix advice is wrong, S15 repeats it every beat, and the shape (Next redirect/gate page) generalises; one leg is moot HERE — with zero endpoints there are no backend journeys for the path-aware anchor to miss. said_on_page: the node's kind is honestly said — the station's `unknown` legend row renders for this feed (gabe-universe.html:4864 `if(!K[k]) return` with K.unknown=1; :4740 "a Pascal .tsx export with no JSX of its own and nothing rendering it — could not be proven a component"; card ident :1203-1204) — but no Sources row counts pascal_no_jsx (:6117 prints only stories excluded), index.html says nothing, archmap.json has no map_health/route_census, and nothing tells the reader the root ROUTE is the thing unclassified.

#### `empty-e2e-spec-glob-counts-repo-root` · medium · hours · lens docs-config-identity

**Missed:** keypro has zero end-to-end specs and declares so (`e2e.runner: "none"`, `e2e_spec_glob: ""`). The Kinds table on tests.html nonetheless reports "journey | none (e2e) | — | 1 spec(s) on disk | no junit capture (not-wired)". The one is the repository directory itself: `REPO_ROOT / ""` is REPO_ROOT, and glob returns a path that exists. The honest empty configuration produces a fabricated artifact — the exact inverse of the estate's "named gap, not a zero" rule.

**App:** no e2e suite anywhere in keypro-front (no playwright/cypress dep in package.json, no *.spec.ts under any dir); docs/site/center/center.config.json paths.e2e_spec_glob = ""

**Pipeline:** templates/center/generators/build_center_a3.py:863-869 — `_spec_glob = CFG['paths'].get('e2e_spec_glob', …)` then `_all_specs = sorted(glob.glob(str(REPO_ROOT / _spec_glob), recursive=True))`; with an empty string `REPO_ROOT / ""` == REPO_ROOT and glob matches it (reproduced: glob.glob('/…/keypro-front') → ['/…/keypro-front']); `_app_specs` feeds the row at :890. _a3_feature.py:940-945 has the same construct for the per-entity Tests tab.

**Feed:** tests.html Kinds row: "journey | none (e2e) | — | 1 spec(s) on disk | — | no junit capture (not-wired)"

**Fix:** Guard the glob: `_all_specs = sorted(glob.glob(...)) if _spec_glob.strip() else []`, and filter to files (`os.path.isfile`) so a directory can never count as a spec — same two-line change at build_center_a3.py:867 and _a3_feature.py:945.

#### `fixtures-dir-not-test-support` · low · hours · lens docs-config-identity

**Missed:** The pipeline knows `.test.`, `.spec.`, `__tests__`, `__mocks__`, `/test/`, `/e2e/` — but not `__fixtures__`, the co-located-vitest idiom this repo uses. So src/adapters/agent-run/__fixtures__/{agent-run-example.ts, real-run.ts} are drawn as application modules in the frontend feed (and therefore in the Gabe Universe), sit in test_insight.by_file as app files awaiting coverage (including a .json fixture, which can never carry any), and are priced by the guard lens as 3 unguarded exported symbols owned by the chat entity. Recorded test data reads as product surface.

**App:** src/adapters/agent-run/__fixtures__/agent-run-example.ts (exports AGENT_RUN_EXAMPLE, AGENT_RUN_EXAMPLE_VISIBLE_ANSWER), __fixtures__/real-run.ts (exports REAL_RUN_EVENTS), __fixtures__/agent-run-response.json — all three consumed only by agent-run-*.test.ts

**Pipeline:** templates/center/generators/_a3_fe_extract.mjs:64 `const isTest = f => /\.(test|spec)\.(ts|tsx|js|jsx)$|\/__tests__\/|\/__mocks__\/|\/test\/|\/__regression__\/|\/e2e\/|\.e2e\./` — no `__fixtures__` branch; templates/center/generators/_a3_tests.py:60-73 `_is_test_support` recognizes only the results-dir prefix and `.test.`/`.spec.` basenames; _a3_web.py:51 `_SKIP_SUFFIXES` likewise

**Feed:** c4-graph.json fe.pieces contains 2 `__fixtures__` modules (agent-run-example, real-run) among its 190 pieces; archmap.json test_insight.by_file lists all three fixture paths (incl. the .json) as app files with coverage null; archmap.json guard_insight.files carries both fixture .ts files with declared 2 and 1, unguarded 2 and 1, entity "chat"

**Fix:** Add `\/__fixtures__\/` (and `\/fixtures\/`) to the isTest regex in _a3_fe_extract.mjs:64 and the same directory test to _a3_tests._is_test_support and _a3_web._SKIP_SUFFIXES' sibling check — one vocabulary shared by the three arms, so a fixture is never a piece, a file-map row, or a guard row.


### config-fixable

#### `corpus-aliases-knob-undocumented` · medium · config-only · lens docs-config-identity

**Missed:** Wiring the junit capture is not enough here. The web-corpus join resolves a test's imports through an `aliases` map that only exists in _a3_tests.py; it appears in neither center.config.template.json nor center.config.example.json, so an adopter cannot know to write `"aliases": {"@/": "src"}`. Without it, 37 import edges over 19 test files resolve to nothing and 13 app files — src/ports/*.ts (the entire hexagonal interface layer: user-repository, session-store, credential-verifier, password-hasher, chat-assistant, conversation-history, greeting-repository, session-repository) plus src/models/{chat,credentials,greeting,session}.ts and src/brand.ts — would still read as reached and named by no test. In a ports-and-adapters app the ports ARE the contract tests bind to, so the layer that matters most is the one the join drops. They are dropped silently: a bare specifier that is not relative and matches no alias returns "" and is not even recorded in `unmapped`.

**App:** 18 of 49 test files import via `@/…`; 37 resolvable alias edges, e.g. src/services/user-service.test.ts → @/ports/user-repository, src/services/auth-service.test.ts → @/ports/{credential-verifier,password-hasher,session-repository}, src/models/user.test.ts → @/models/user; tsconfig.json:21-23 `"@/*": ["./src/*"]`; vitest.config.mts uses vite-tsconfig-paths so the runner resolves them

**Pipeline:** templates/center/generators/_a3_tests.py:382 `aliases = c.get("aliases", {})` and :384-397 `_resolve` — a spec that is not `.`-relative is matched only against that map, and with an empty map `tgt is None` → `return ""` before the `unmapped` recording at :409-413, so the drop leaves no trace; the key is absent from both templates/center/generators/center.config.template.json and center.config.example.json (grep '"aliases"' → 0 hits in each; same for `root` and `route_prefix`, the two other corpora knobs _a3_tests.py:304/:383 reads)

**Feed:** archmap.json test_insight.by_file has `reach: []` for all 13 alias-only targets (src/ports/user-repository.ts, src/models/chat.ts, …); guard_insight lists src/models/chat.ts as 14 declared / 14 unguarded even though chat-service.test.ts names 4 of those symbols — the symbol join runs through the same `_resolve`

**Fix:** Add `"aliases": {"@/": "src"}` to the web corpus in docs/site/center/center.config.json; and add the three optional corpora knobs (`aliases`, `root`, `route_prefix`) with one-line comments to center.config.template.json so the next adopter sees them — a knob only the generator knows about is a knob nobody sets.

#### `unit-kind-mislabels-a-domain-suite` · low · config-only · lens docs-config-identity

**Missed:** The corpora page tells the reader that keypro's vitest corpus verifies "components and hooks in isolation — file-tier facts (imports + route literals)". Of the 49 test files, 22 are components; the other 27 are domain models, services, adapters, server actions and one live-postgres integration test — the opposite of "in isolation". The kind vocabulary is integration=through-HTTP · unit=components/hooks · journey=browser, which is a FastAPI+React split; a hexagonal TypeScript app whose unit corpus carries its domain has no honest row, and the `kind_detail` the config does carry ("models/services/adapters/components") is discarded on that page because the kind's canned sentence wins. The escape hatch that would fix it, `corpora[].description`, appears in no config template.

**App:** src/models/*.test.ts (3), src/services/*.test.ts (4), src/adapters/**/*.test.ts (13, incl. postgres-integration.test.ts which drives a real PostgreSQL when POSTGRES_HOST/DB are set), src/app/**/actions.test.ts (2), src/components/**/*.test.tsx (22), src/composition/container.test.ts

**Pipeline:** templates/center/generators/build_center_a3.py:957-969 — `_KIND_WHAT` hardcodes the three sentences and `_corpus_what` prefers `c['description']` then `_KIND_WHAT[kind]`, reaching `kind_detail` only for an unknown kind; `"description"` occurs 0 times in center.config.template.json and 0 times in center.config.example.json

**Feed:** test-corpora.html row: "web | vitest | 0 | 0 | tests/results/web-junit.xml | components and hooks in isolation — file-tier facts (imports + route literals), never dressed as case proof"; the config's kind_detail survives only in the tests.html Kinds row "vitest (models/services/adapters/components)"

**Fix:** Set `corpora[0].description` in docs/site/center/center.config.json ("domain models, services, adapters and components in-process; one env-gated postgres integration file"), and add `description` to center.config.template.json's corpora entry so the override is discoverable. A second corpus key would need a second junit file (a second vitest invocation with its own --outputFile) — worth noting but not worth splitting here.


### drawn-under-another-name

#### `proxy-not-a-gate` · medium · days · lens tests-estate — **VERIFIED confirmed** (→ drawn-under-another-name · medium · said on page: False)

**Missed:** proxy.ts is exactly what `app.add_middleware(...)` is in the FastAPI model: the one thing that runs before every matched request. It redirects unauthenticated traffic off `/`, `/chat/*`, `/admin/*` and signed-in traffic off `/login`. The pipeline already HAS a `middleware` L2 node kind and a `gated_by` wire with saturation handling — it just has no non-Python feeder. So the reader sees a node called "proxy", kind module, mclass `logic`, with no edges at all: indistinguishable from a util file, and the app's global request gate plus its 4-path matcher scope are invisible.

**App:** src/proxy.ts:16 `export function proxy(request: NextRequest)`; :21-31 the two redirect rules; :36-38 `export const config = { matcher: ["/", "/login", "/chat/:path*", "/admin/:path*"] }`

**Pipeline:** _a3_graph.py:1390 `_app_mw = amap.get("app_middleware") or []` is the ONLY source of middleware nodes (minted at :1403-1407); it is filled by _a3_code.py:2374 parse_app_middleware, which at :2394 iterates `sorted(d.glob("*.py"))` looking for `add_middleware(`. No .ts path reaches it, and `proxy`/`middleware.ts` appears nowhere in _a3_fe.py (the only `proxy` hit is _a3_fe.py:55, the valtio store-callee roster — an unrelated collision).

**Feed:** c4-graph.json fe.pieces contains `{id:"fe:src/proxy.ts", kind:"module", mclass:"logic", name:"proxy", exports:["config","proxy"]}` and NO edge in fe.edges names it (decoded all 236 edges: 0 touch fe:src/proxy.ts). c4 stats.middleware_endpoints 0, stats.gate_endpoints 0; archmap.json has no `app_middleware` key.

**Fix:** In build_center_a3.py beside the parse_app_middleware call (~:2070), add a 10-line TS feeder: if `src/proxy.ts` or `src/middleware.ts` (or `proxy|middleware.{ts,js}` at the app root) exists, read its `export const config = { matcher: [...] }` string literals and append `{cls:"proxy", scope:<matcher list>, file:...}` to amap["app_middleware"]. _a3_graph.py:1403 then mints the existing `middleware:proxy` node and the saturation rule already decides whether to draw per-route wires. No new node kind, no new legend row.

**Verifier:** APP holds: src/proxy.ts:16 `export function proxy(request: NextRequest)`, :21-31 the two redirect rules (no session → /login off `/`,`/chat*`,`/admin*`; session → /chat off `/login`,`/`), :36-38 `export const config = { matcher: ["/", "/login", "/chat/:path*", "/admin/:path*"] }`; it is the only proxy/middleware file in the tree. FEED holds: c4-graph.json fe.pieces has `{id:"fe:src/proxy.ts", kind:"module", mclass:"logic", area:"root", home:"fe·platform", exports:["config","proxy"]}`; 0 of 236 fe.edges touch it; no `middleware:*` L2 node exists; stats.middleware_endpoints=0, stats.gate_endpoints=0, and `stats.app_middleware` is ABSENT (the emitter writes it only when count>0 — _a3_graph.py:1523-1526 `if _app_mw_n else {}`); archmap.json has no `app_middleware` key and lists src/proxy.ts only as a plain `web` file (entities.platform.defines/files, guard_insight kind "web"). PIPELINE holds: _a3_graph.py:1390 `_app_mw = amap.get("app_middleware") or []` is the sole source of middleware nodes (minted :1403-1410); build_center_a3.py:2070-2072 fills it from _a3_code.parse_app_middleware, which (:2382-2389) derives its scan dirs from `code.api` globs — keypro's code.api is EMPTY so `dirs` is empty and it returns `[]` before even reaching the `*.py`/`add_middleware(` glob at :2394-2403; _a3_fe.py:55 and _a3_fe_extract.mjs:150 mention `proxy` only as the valtio store-callee idiom. Finder's minor inaccuracy: with a list-scope entry the graph's rule at _a3_graph.py:1401 (`_gated = _all_eps if _scope == "all" else []`) draws NO gated_by wires, and keypro has zero endpoint nodes anyway — so the sketched fix yields a `middleware:proxy` node carrying the matcher in `det.scope`, not wires; still the right fix, cost holds. SAID_ON_PAGE=false: the station's Sources rows (gabe-universe.html:6070-6110: entity model · names · elements · cross-entity touches · files skipped · route mounts · twin pass · web bridge · graft wiring · frontend arm · homing evidence · levels feed) carry no middleware/gate row; the station never reads `stats.app_middleware` (0 grep hits); mapquery.py:292-318 map_health has no middleware block; keypro index.html has 0 hits for middleware/proxy; the legend's Middleware row (:1221-1223) frames the kind as "add_middleware" and with no instance shows "—" (none drawn), never "not scanned for TS". The keypro station copy differs from the template only in sidebar/title substitutions. Class stays drawn-under-another-name (the file IS a node, its gate role/scope is not); impact medium — note proxy.ts:5-7 calls itself optimistic with the server layouts as the authoritative guards, but those layouts are drawn as plain routes with no gate semantics either, so the app's request-gating is invisible either way.


## Checked and NOT a gap (finder notes)

### hexagonal-structure
CHECKED AND FINE — do not re-raise:

1. `(platform)` route-group folding is CORRECT for the URL. `_NEXT_GROUP_RX` (_a3_fe.py:130) + `_next_route_from_file` (:165-175) produce exactly the right paths: `/chat`, `/admin/users`, `/login`, `/admin · layout`. The only defect is the bare-`/` collision between the two layouts (raised as next-route-group-label-collision); the group stripping itself is right.

2. `_NEXT_APP_RX` (_a3_fe.py:127) covers page|layout|template|error|loading|not-found — keypro only has page+layout and both roles are caught. Dynamic-segment handling (`[id]` → `:id`, `[...slug]` → `:slug*`, _a3_fe.py:172-174) is correct code; this app has no dynamic routes so it is untested here, not broken.

3. The frontend extractor ran cleanly on a Next 16 tree: 89 files, typescript 6.0.3 (borrowed via GABE_TS_DIR, said out loud in stats.fe.reason), 190 pieces / 236 edges, no silent drop. tsconfig resolution and the project-references fallback (_a3_fe_extract.mjs:36-52) were not needed and did not misfire.

4. The store/type extraction (D5) works on Next idioms: `fe:src/components/app/shell-context.tsx#ShellContext` carries all 8 members of ShellContextValue with their types, and `shape: "ShellContextValue | null"` from `createContext<…>()`. The one hook, `useShell`, is correctly hrole `deriver`.

5. The homing-evidence arm ran: `useShell` carries a `home_ev` block with a `stay` verdict (users split chat 1 / platform 1, share 0.5). stats.fe.homing = "config" — correct, keypro has no `features/<x>` layout, so the config's `code.web` claims are the honest homing source and the Sources row names it.

6. `feclass` is COINCIDENTALLY aligned with the client boundary here — the four `view` components (ChatScreen, LoginForm, PlatformShell, UsersScreen) happen to be exactly the four "use client" screens — but the class is derived from "rendered by a route", not from the directive. It is not coverage of the boundary and must not be read as such: a server component rendered by a route would also be `view`.

7. The route→client-component topology IS drawn correctly (renders 41, imports 33, fecall 56). Server routes reach their client screens, PlatformLayout imports ./actions, the wires exist. Only the SEMANTICS of the crossing (runtime, network, direction) are absent — which is why gap 1 and gap 2 are about meaning, not about missing edges.

8. `stats.fe.excluded.module_exports = 73` is honest and expected — the 73 are the non-Pascal, non-hook exports of module files (service functions, adapters, constants), which the FE arm has never claimed to piece out; they are counted, not hidden.

9. The 8 `detached` components (DocCard, SuggestionCard, Spinner, Tag, Tooltip, Checkbox, Switch, Textarea) are a rendered-by gap, not a boundary gap — the D1 definition ("no drawn renderer") is applied correctly and the station's legend gloss says exactly that. Out of this lens.

10. OVERLAP WARNING for the verifier: the fact that keypro's whole backend (src/services, src/adapters/postgres, src/composition, src/models — TypeScript, raw `pg` SQL, CREATE TABLE in src/adapters/postgres/schema.ts) is invisible because `_a3_code.py` parses Python only is a DIFFERENT lens's finding (backend-in-TypeScript). I deliberately did not raise it. My gap 2 is scoped to the server/client CROSSING being unscored (channel/fed2w), not to the absence of a backend scanner; they compound but they are separate fixes. Likewise archmap.json lacking `route_census`/`file_census`/`unparseable` (so nothing names which .ts files went unscanned) belongs to that lens.

### providers-streaming
Checked and NOT raised — do not re-raise these:

1. `stats.web` is HONEST IN THE FEED. It carries `present: true, fetch_sites: 0, matched: 0, reason: "no REST api-call idiom detected"`. _a3_web.py behaved exactly as designed for a non-REST app. The only defect is that gabe-universe.html:6096 never renders the `reason` field (it renders `extractor`, `matched`, `unmatched`, `dynamic`, `unhomed`, `other_roots`, `sdk_methods` — not `reason`), and mapquery.py:311-312 also drops it because `reason` is only surfaced when `present` is false. I folded that one-line rendering fix into gap `data-layer-emptiness-never-said` rather than raising it separately; it properly belongs to the web/bridge lens.

2. `stats.fe.present/reason` is honest: "typescript 6.0.3 · 89 files · typescript borrowed via GABE_TS_DIR — the project's own deps may be absent (third-party imports unresolved)". The FE structure arm did its job (190 pieces / 236 edges) and says how.

3. `stats.unresolved_tables: []` and `schema_homing` (4 empty lists) are CORRECT, not silent failures — there are no models to resolve FKs against and no Python schemas to home. Pulse S12 reading clean is honest given zero schemas.

4. The downstream graph consumers are fully idiom-agnostic and need NO change for either SQL gap — verified by reading them: _a3_graph.py:648-658 (L2 model node from `model.get("cls")`/`model.get("table")`), :775-786 (FK edges from `model.get("fks")` as `{col: "table.col"}`), :719-727 (access wires from `_op.get("rw")/("model")/("table")`). This is load-bearing for the fix costs I quoted: the missing piece is an emitter that fills two dicts, not a new rendering path.

5. The live-vs-double adapter swap (src/composition/container.ts:69-97 binds PostgresUserRepository/PostgresSessionRepository when `readPostgresConfig()` returns config, else InMemoryUserRepository/InMemorySessionRepository/FakeCredentialVerifier) is unmodeled — all five repository files draw as identical `module` pieces with no way to tell the production store from the test double, and in-memory-session-repository.ts sits in fe·auth beside the postgres one. I did NOT raise it: once tables + access land (gaps 1/2), the postgres modules carry table wires and the in-memory ones do not, which resolves the distinction with no new concept. A first-class port/adapter model is the architecture lens's call, not the data lens's.

6. `pg` is not drawn as a provider edge either — _a3_code.py:1816 `_PROVIDER_ROOTS` is keyed on Python import roots (genai, boto3, redis, httpx, langchain, pgvector, …) with no `pg`, and _a3_code.py:1855 resolves it from Python `import`/`from` statements. I folded this under gap 1 rather than raising a separate "database not a provider" item, since drawing the database as a provider edge would be a strictly worse model than drawing it as tables.

7. The pulse ANGLE family is not broken — s15_fe_unknown fires correctly on this repo (1 Pascal .tsx export). The family simply has no data-layer angle; that is the substance of gap 3, not a separate defect.

8. levels.json's `models` block (8 keys, seeded view present) and `stats.homing` (62 pieces weighed, 33 agree, 2 move candidates, 3 shared) are real and honest — they operate over fe pieces, which exist. `levels.json.homing` has 4 entries. No gap there.

9. One adjacent observation I leave to the server-actions/trace lens rather than claiming it: levels.json entity counts carry `hidden_fns` (auth 32, chat 113, greeting 4, …) — graft homed ~194 functions but `fn_nodes: 0`, because the trace walk seeds only from endpoint/BOOT/TASK roots and this app has none. It matters to my lens only as a dependency I stated inside gap 2: even a perfect SQL access detector draws no WIRE until something anchors the walk. Separately, `hidden_fns` is emitted in levels.json but rendered nowhere in gabe-universe.html (grep -c "hidden_fns" → 0), so that count never reaches a reader.

### tests-estate
Checked and NOT gaps (do not re-raise):

1. Next App Router route detection WORKS for JSX-returning files. `_NEXT_APP_RX` (_a3_fe.py:127) correctly caught 6 routes with real URL labels — `/chat`, `/login`, `/admin/users`, `/admin · layout`, `/ · layout` ×2 — and `_next_route_from_file` correctly drops the `(platform)` route group from the path. Only the no-JSX case fails (raised as next-route-needs-jsx). The duplicate `/ · layout` label on RootLayout and PlatformLayout is cosmetic (nested layouts genuinely both sit at `/`), not worth a gap.

2. No `route.ts` / Route Handlers exist in this app (0 files), so the absence of a Next Route-Handler parser costs keypro nothing. Worth noting for the program, not a gap for THIS app.

3. `generateMetadata` is absent; src/app/layout.tsx:35 has only a static `export const metadata` object, which correctly folds into `module_exports` (73). Not a request entry point — not raised.

4. Server action NAMES are not lost from the feed. `pieces[].exports` carries all 13 (e.g. `fe:src/app/(platform)/admin/users/actions.ts` → the 6 admin mutations). They are recoverable by a tool reading the JSON; they are simply not pieces, nodes or roots, and are drawn as one grey `config` module — that is the gap, not data loss.

5. `workflows.js` and `workflows.draft.js` are both honest-empty with a comment explaining how to fill them. That is correct behaviour given zero endpoint nodes (draft-workflows.py:145-151 reads endpoint nodes), so it is downstream of server-actions-not-roots, not a separate gap.

6. The frontend arm's own honesty is good: `stats.fe.reason` names the compiler version, the file count, and warns "typescript borrowed via GABE_TS_DIR — the project's own deps may be absent", and the Sources row at gabe-universe.html:6102 renders it. `stats.fe.homing = "config"` with the row saying "homed by the entity config's web claims (no feature layout)" is accurate for this flat src/{app,components,services,adapters} tree.

7. `stats.web.present = true` with a named reason is the emitter behaving exactly as _a3_web.py:19 promises; the defect is purely that the station never renders `reason` (folded into station-silent-about-missing-request-arm).

8. The `middleware` L2 node kind, its `gated_by` wire, the saturation rule and the legend row ALL already exist (_a3_graph.py:72, :1386-1407). proxy-not-a-gate is a feeder gap, not a graph-schema gap — which is why its fix is days, not a new arm.

9. commits.js carries 30 commits with touched fe piece ids, so the journeys tab is not literally empty — commit journeys render. Zero backend and zero workflow journeys, which is the consequence of gap 1.

10. The homing evidence arm ran and produced real verdicts over the TS pieces (`home_ev` on `fe:src/composition/container.ts` = "shared", 5 consuming entities; `fe:src/app/(platform)/chat/actions.ts` = "stay") — the membership machinery is framework-agnostic and works here. Not a gap for this lens.

11. `stats.fe.by_channel.read = 0` is a second-order consequence worth noting but not separately raised: the only store (`ShellContext`) draws no `uses-store` wire (by_rel has `uses-hook 2`, no `uses-store`), so the read channel is also flat. The write channel is the provable, high-impact half and is what I raised.

### docs-config-identity
CHECKED AND FINE — do not re-raise:

1. vitest junit IS a supported shape. _results_ingest.py:25-45 `_junit_file_of` names the vitest reporter explicitly ("vitest (junit reporter): suite name = 'src/foo.test.ts' relative path") and falls back classname → suite name → pytest dotted path. `pnpm vitest run --reporter=junit --outputFile=tests/results/web-junit.xml` is a working capture path; the root probe (_a3_tests.py:83-101) would resolve root="" since vitest keys are already repo-relative. Not run here — no node_modules in the target repo and I did not install (WSL2 serial-work rule).

2. C-ids are NOT expected and their absence is handled honestly. _a3_ledger.build_cases:118-124 groups by C-id when present and otherwise by `jf::name`, so all 464 unminted cases would render as identities ("unminted rows close their band", :145-150). test-claims.html says "No # CLAIMS authored on any card yet" and the claim-state legend explicitly includes "or junit absent". The repo's 0 C-ids is not a gap.

3. Test files are correctly kept out of the code map and the frontend feed. _a3_code.py:717 excludes `.test.` from the layer file rows (archmap by_file has 0 `.test.` entries out of 90), and _a3_fe_extract.mjs:64/186 excludes them from pieces (c4-graph fe.pieces: 0 test-file pieces of 190). No test file is drawn as an app component. Only __fixtures__ leaks (raised).

4. The corpus config SHAPE I wrote is accepted. `corpora` is a plain list (_a3_tests.py:303, build_center_a3.py:234) — nothing requires an "api" key or a second corpus; build_center_a3.py:78's pytest/vitest pair is only the default when the config declares none. `runner`/`kind`/`tag_class`/`kpi_detail` are render-only; the python-vs-web branch is decided by the TEST FILE's suffix (_a3_tests.py:321 `if disk.suffix == ".py"`), so a vitest corpus behaves correctly. What is missing from the shape are the optional knobs (raised as corpus-aliases-knob-undocumented).

5. test_rx coverage is adequate and its failure mode is already surfaced. Simulated over the 49 test paths: chat 14 · platform 20 · auth 7 · users 7 · greeting 2; exactly 1 unclaimed (src/adapters/postgres/postgres-integration.test.ts) and 2 double-claimed (chat/platform on sidebar-item + suggestion-card). test-files.html already carries an "unclaimed" count and the note "A file may be claimed twice — the counts are views, not a partition", so both states are said once a junit exists.

6. Coverage is an honestly-named gap, not a silent one. The center says "Coverage unverified · no reporter wired · named gap" and "coverage — no reporter wired". The read path exists (_results_ingest.load_coverage:127-143 wants `coverage.web.summary` = a vitest json-summary) but the repo has no @vitest/coverage-v8 provider, so it genuinely is unwired. Two things to know if it gets wired: the read-side `coverage` block is in neither config template (only `commands.coverage` is), and `coverage.web.src` defaults to "web/src" — a monorepo assumption keypro would have to override with "src". Not raised separately because the page already tells the truth today.

7. The env-gated postgres integration test degrades honestly: with no POSTGRES_HOST its cases land as `skipped` in junit and kind_state (_a3_feature.py:168-170) reports "N skipped" with a medium tag. Reads as debt rather than "needs a live database", but nothing is fabricated.

8. Minor parser noise, deliberately not raised: _a3_tests.py:425-437 splits an import clause on [,{}] and strips only a leading "type " WITH a trailing space, so `import type { X } from …` yields a spurious `uses` symbol literally named "type" alongside the real names. Harmless unless an app exports a symbol called `type`; the real names (X) are captured correctly, so the guard join is unaffected in this repo.

9. gabe-map's test tools are honest without a corpus: tools.py:743 says "no .kdbp/ — this repo mints no C-ids; the floor is a corpus artefact, not a registry". keypro has no .kdbp/, so `cases_for` reports that rather than a zero.

10. The Files/Cases/Claims pages' SOURCE sentences are correct as written ("Read from the junit capture at build time"); the defect is only that the counts beside them are bare zeros rather than "not captured" — folded into junit-absent-reads-as-zero-tests, not a separate gap per page.
