{
 "generated": "2026-09-02",
 "method": "11-batch Workflow sweep over all 30 skills (sweep -> adversarial verify -> completeness critic); 23 agents, 601 tool uses",
 "counts": {
  "surviving": 219,
  "killed": 14,
  "files": 48,
  "skills": 24
 },
 "landed_2026_09_02": {
  "tier0": {
   "commit": "a56131d",
   "what": "the tool floor + 3 defects + map_diff"
  },
  "tier1": {
   "commit": "4322c58",
   "what": "the spine — 87 edits, review/execute/commit/plan/push/red/next"
  },
  "tiers2_4": {
   "commit": "(this commit)",
   "what": "the remaining 124 — center stack, routers, satellites, authoring"
  },
  "note": "every surviving edit is APPLIED; `staged` is empty. `refuted` records what was killed and why."
 },
 "staged": [],
 "applied": [
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 11,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 11. L1 is stated the RIGHT way round (empty answer is not absence; grep -rn stays the proof), so it hardens E1 rather than weakening it. ONE factual defect CORRECTED: gabe-kdbp answers carry NO map@ stamp — mapquery.stamp() is gabe-map-only (mapquery.py:275, header `gabe-map · <tool> · map@<head> · <fresh|stale|unknown>`), while gabe-kdbp's header is `gabe-kdbp · <tool>` (`· no .kdbp` when absent, gabe-kdbp/SKILL.md:47). Proposed_text rewritten so each server is credited with the stamp it actually emits. `<fresh|stale|unknown>` kept because gabe-map/SKILL.md's own output contract uses that exact phrasing (freshness() also emits 'uncommitted regen', but the suite's existing vocabulary wins over exhaustiveness). L2/L3/L5 untouched.",
   "proposed_text": "- **E1 EVIDENCE** — every claim about code/state cites file:line or a command run THIS session; no citation → mark it `(assumed)` and verify before building on it. Absence claims (\"no X exists\") require a recorded search → 0 hits. A `mcp__gabe-map__*` / `mcp__gabe-kdbp__*` answer IS such a citation — a map answer stamps `map@<head> · <fresh|stale|unknown>`, a kdbp answer names the root it parsed `.kdbp/` from — but an EMPTY answer is NOT: the map is a FLOOR, never a scope, so the absence proof stays the recorded `grep -rn` → 0 hits.",
   "current_excerpt": "- **E1 EVIDENCE** — every claim about code/state cites file:line or a command run THIS session; no citation → mark it `(assumed)` and verify before building on it. Absence claims (\"no X exists\") require a recorded search → 0 hits.",
   "sweep_rationale": "YES to the batch note's first question. E1 is the one clause in the suite that already governs citations AND absence proofs, so it is the only correct home for the floor law — stated here it binds all 30 skills without touching one of them. The sentence does two jobs at once: it PROMOTES a tool answer to a first-class citation (so skills stop re-grepping to \"prove\" what the map already stamped) and it FORBIDS the dangerous direction (an empty answer standing as absence), which is L1 verbatim. Wording lifted from the server's own injected instructions block so the suite keeps one term per concept (\"floor\", \"map@<head>\")."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 14,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 14. Both named tools answer exactly this question: `find` searches by name OR doc text across entities/endpoints/models/schemas/functions/screens/FE pieces (tools_wave2.py:57-103, registry line 480), `touches` takes a file · model/schema · function · entity · endpoint · case id (tools.py:262, registry 642). L2 carried by the closing no-map clause; L4 untouched (no script displaced). ONE L1 tightening APPLIED: a `NEW` is itself an absence claim, so the Grep/Glob arm is not optional — the original wording ('then Grep/Glob for what the map does not carry') could be read as letting an empty map answer alone justify NEW. Corrected text makes the second arm mandatory and cites E1.",
   "proposed_text": "- **E4 REUSE FIRST** — before creating anything, print: `REUSE <path> | EXTEND <path> | NEW (searched <where> — none fit)`. The search behind a `NEW` starts at the map, not at Grep: `mcp__gabe-map__find` (by name or doc text) and `mcp__gabe-map__touches` (by file · model · endpoint · function) FIRST, then Grep/Glob — a `NEW` is an absence claim, so the Grep/Glob arm always runs (E1) and `<where>` names both. No map → the tool says so and Grep/Glob is the whole search. Recreating an existing artifact is a defect.",
   "current_excerpt": "- **E4 REUSE FIRST** — before creating anything, print: `REUSE <path> | EXTEND <path> | NEW (searched <where> — none fit)`. Recreating an existing artifact is a defect.",
   "sweep_rationale": "YES to the batch note's second question, with a correction of aim: E4 already has a `<where>` slot that today is filled with hand-grep, so the ask-first rule can be planted with zero new vocabulary. Naming BOTH arms in `<where>` keeps L1 intact (the map answer never stands alone as the search) and the honest-empty clause keeps L2 (a project with no center runs E4 exactly as it does now). This is the single highest-yield sentence in the pass: every skill that creates anything prints this line."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 19,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 19 (insert lands immediately before it). The section-not-E9 argument is CONFIRMED: all 30 SKILL.md pointers paste the literal enumeration 'E1 EVIDENCE · … · E7 REPORT WHERE' (verified across skills/*/SKILL.md), so an E9 would cost 30 edits and a section costs zero. Pointer paths resolve: ../../gabe-map/SKILL.md and ../../gabe-kdbp/SKILL.md both exist and both carry the full tool tables (E6 anchor satisfied). L1-L4 are all four stated. ONE factual defect CORRECTED: 'server not registered → the tool says so and names the reason' is FALSE — an unregistered stdio server means the `mcp__*` tools are absent from the session entirely; nothing answers. Rewrote that bullet so the honest-empty law covers both failure shapes (present-but-empty vs not-there-at-all), which is also what keeps L2 true for the suite repo itself (ruling R8: no .kdbp, and a suite-center repo answers honest-empty).",
   "proposed_text": "## The tool floor — stated once, ruled 2026-09-02\n\nThe suite ships two read-only MCP servers, registered once at user scope and answering from the\nproject the session is in: **gabe-map** — the committed command-center map as tools — and\n**gabe-kdbp** — a project's `.kdbp/` lifecycle state as tools. The rosters live in\n`../../gabe-map/SKILL.md` and `../../gabe-kdbp/SKILL.md` and the servers advertise them every\nsession; they are not re-listed here. Four laws govern how a skill reaches for them:\n\n- **ASK FIRST, THEN THE OLD METHOD.** Any step that asks *where · who calls · what touches ·\n  which entity owns · which cases cover · where does the project stand* asks the tool BEFORE it\n  greps, globs, or re-reads PLAN/PENDING/LEDGER by hand. The old method stays as the second arm,\n  never as the first.\n- **THE MAP IS A FLOOR, NEVER A SCOPE (E1).** An empty answer is not an absence proof and never\n  narrows a change's scope; `grep -rn` → 0 hits remains the absence proof.\n- **HONEST-EMPTY.** No center · no `.kdbp/` → the tool says so and names the reason; the servers\n  unregistered → the `mcp__*` tools are not in the session at all. Every step still completes on\n  both paths; a step that only works with the tools is a defect.\n- **TOOLS ARE NOT RAILS.** Enforcement stays on hooks, gates, and the deterministic scripts a\n  skill already owns; the D7 hooks watch Write/Edit, never `mcp__*` — a `_preview` tool composes\n  a row, the harness Write persists it. A tool supersedes a script only where it relays that same\n  script verbatim (`center_status` → `scripts/center_status.py`).\n",
   "current_excerpt": "## The findings contract — stated once, ruled 2026-08-10",
   "sweep_rationale": "INSERT as a new section immediately BEFORE line 19's heading (after the E1–E7 list). This is the batch's real payload: one section, 30 skills, no per-skill edit. It is deliberately a SECTION and not a new E-number — E8, the findings contract and the beat brief already live in this file without joining the `E1–E7` pointer enumeration that all 30 SKILL.md files paste, so a section costs zero downstream edits while an E9 would cost 30 (see the no-change on SKILL.md:14). The four bullets are L1–L4 written in the file's own register; naming the servers but POINTING at their tool tables avoids a third copy of the 22 names that would drift the next time a tool is renamed (map-spec §4 already binds names+instructions in one commit)."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 15,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 15. Every claim checks out against kdbp_tools.py: pending_row_preview reads the LIVE header (`cols = hdr or canon`, :499), mints max+1 across PENDING.md AND archive/PENDING-resolved*.md (:490-495), stamps `@<sha> <today>` (:504), flags recurrence by same File + >=3 shared tokens (:506-507); ledger_row_preview copies Gates verbatim ('the Gates cell is copied verbatim, never composed', :532). Both return `writes: nothing`. L3 is the reason this survives at all — the clause states inline that the previews write nothing and the hooks only ever see the Write, which is exactly the D14/D7 seam. Accepted as written.",
   "proposed_text": "- **E5 STATE SYNC** — actions that change reality (commit/merge/defer/pivot) write their state row in the SAME turn; a skipped write prints an enumerated skip code, never silence. Where the row is authored by hand, `mcp__gabe-kdbp__pending_row_preview` / `ledger_row_preview` compose it first — the file's own column order, the next `P-id`, the `Verified` anchor, the recurring flag, `Gates` verbatim — and the harness Write/Edit persists it; the previews write nothing, and the D7 hooks only ever see the Write.",
   "current_excerpt": "- **E5 STATE SYNC** — actions that change reality (commit/merge/defer/pivot) write their state row in the SAME turn; a skipped write prints an enumerated skip code, never silence.",
   "sweep_rationale": "E5 is where every hand-authored PENDING/LEDGER row in the suite is mandated, and hand-composing a row against a file's own column order is exactly the work the two preview tools do (kdbp_tools.t_pending_row_preview reads the live header, mints max+1, stamps `@<sha> <date>`, flags recurrence). The clause is written so it can NEVER be read as replacing the write: it states L3 inline (previews write nothing; the hooks watch the Write), which also inoculates the next editor against \"the tool can just write the row\"."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 26,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_status",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 26. This is L4's own named exception and the code proves it: t_center_status shells `<root>/scripts/center_status.py` and passes its text through (tools_wave2.py:345-357), and returns `{'reason': 'no scripts/center_status.py in this project (installed by /gabe-cc-init)'}` when the script is absent — so both the relay claim and the honest-empty claim are literal. The script keeps ownership of the links; the tool is only the reach. Accepted as written.",
   "proposed_text": "the script emits the link + step itself and the skill relays it verbatim — never re-composing a link by hand (reference implementation: gabe-cc-update `status` → `center_status.py`, reachable in any project as `mcp__gabe-map__center_status`, which runs that same script and relays its links and → next steps verbatim; no `scripts/center_status.py` there → the tool names the absence)",
   "current_excerpt": "the script emits the link + step itself and the skill relays it verbatim — never re-composing a link by hand (reference implementation: gabe-cc-update `status` → `center_status.py`)",
   "sweep_rationale": "This is L4's own named exception: center_status is the one tool that literally relays the script (tools_wave2.t_center_status shells the project's `scripts/center_status.py` and passes its output through). Saying so in the findings contract tells all 30 skills that the relay path exists without weakening the rule — the script stays the owner of the links; the tool is only how a skill reaches it when it does not know the project's script path."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 23,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__outline",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim inside line 23. Tool match is real but the original text OVER-CLAIMED and is CORRECTED: outline only carries `span` when the graft index resolves the file — without it every def gets `span: None, signature: None` and the tool says `signatures: 'unavailable — <reason>'` (tools_wave2.py:117-133). Stating the span unconditionally would let a reader treat a graft-less project as span-less-therefore-line-less, which is the L1 failure mode in miniature. Corrected text names the graft dependency and keeps grep as the anchor of last resort. `touches` claim verified: a bare symbol resolves through fn_by_bare to a `file::fn` record that also carries `lines` (tools.py:309-319, _fn_record :253-256). Leverage RAISED from high to suite-wide per rule F — the file is the one all 30 SKILL.md files point at.",
   "proposed_text": "A finding the operator cannot click is one they have to go find — never emit one. Where a finding names a symbol rather than a line, `mcp__gabe-map__outline` gives the file's definitions with kind, signature and — where the graft index resolves the file — the span, and `mcp__gabe-map__touches` resolves a bare symbol to its `file::fn` owner; the `#Lnn` comes from there before it comes from a grep, and from the grep whenever the map or the index is blind there.",
   "current_excerpt": "A finding the operator cannot click is one they have to go find — never emit one.",
   "sweep_rationale": "The LINK rule demands `path#Lnn` \"when a line is known\", and today the only way a model-derived finding learns that line is a grep. outline returns definitions with spans + signatures, which is the cheapest correct source for the anchor. Kept as first-look (\"before it comes from a grep\"), never as a replacement, because a symbol the map does not carry still needs the grep — L1."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 46,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 46; the insert lands after the brief's closing fence (line 45) and before this paragraph, so the rendered template stays clean — correct placement. Key list verified against kdbp_tools.py:321: plan_json is filtered to id/name/tier/complexity/types/cells/proof/proof_type/cases/scope/entities, and `records` carries Cases:/Reach: (:324). The honest-blank rule is preserved verbatim from the existing brief spec (entities empty → `none declared`, never a guess), and the no-.kdbp fallback keeps L2. Accepted as written.",
   "proposed_text": "The plan record behind ENTITY and FEATURE is asked for, not re-read: `mcp__gabe-kdbp__phase_context`\nreturns the phase's `plan_json` record (`entities`, `name`, `tier`, `proof`, `cells`) and its\n`records` (`Cases:` / `Reach:`) in one answer — `none declared` stays the honest blank when\n`entities` is empty, never a guess. No `.kdbp/` → the tool says so and PLAN.json/PLAN.md are the read.\n",
   "current_excerpt": "`NEXT` is deliberately absent — the E8 tail renders it from the router; the brief never",
   "sweep_rationale": "INSERT as a new paragraph immediately BEFORE line 46's paragraph (right after the brief's closing fence). The brief is printed by EVERY spine beat and its two data fields are \"verbatim from the plan record\" — i.e. a PLAN.json/PLAN.md re-read on every beat. phase_context returns exactly those keys (verified in kdbp_tools.t_phase_context: `plan_json` filtered to id/name/tier/cells/proof/proof_type/cases/scope/entities, plus `records`). Placing it outside the fenced template keeps the rendered brief clean — the fence is the output shape, this is its sourcing rule."
  },
  {
   "file": "skills/gabe-docs/references/execution-contract.md",
   "line": 12,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "survives": true,
   "leverage": "suite-wide",
   "reason": "Anchor verbatim at line 12. The resolve-vs-run split is the tool's own contract, not an inference: its note reads 'commands are RESOLVED, never run here — the gate runs them (gate-spec Step 2.0); a checker that cannot fail is non-evidence' (kdbp_tools.py:436), source 'a: BEHAVIOR.md ## Verify Commands' first, package.json candidates second (:437-461). Nothing about E2's ✅ discipline is loosened — the executing session still runs the command and still pastes cmd + exit/count. Leverage RAISED from high to suite-wide per rule F. The proposer's own note that this is the droppable one of the five inline edits is fair, but it breaks no law and I have no grounds to kill it.",
   "proposed_text": "- **E2 RUN-BEFORE-✅** — ✅ only after the command executed here (paste cmd + exit/count). The command comes from the project's binding, never a guessed flag — `mcp__gabe-kdbp__verify_commands` relays it (`.kdbp/BEHAVIOR.md ## Verify Commands` first, else manifest candidates) and never runs it; you do. Skipped = `⤫ skipped(<reason>)`, never ✅. Every printed number is copied from this run's output — never estimated.",
   "current_excerpt": "- **E2 RUN-BEFORE-✅** — ✅ only after the command executed here (paste cmd + exit/count). Skipped = `⤫ skipped(<reason>)`, never ✅. Every printed number is copied from this run's output — never estimated.",
   "sweep_rationale": "E2 governs WHICH command earns a ✅ but is silent on where that command comes from, and the suite's recurring defect is a guessed reporter flag. verify_commands resolves the binding and refuses to run it (its own `note` says so), which is precisely E2's separation of resolve-vs-run. WEAKEST of the five inline contract edits — drop this one first if the operator wants E1–E7 kept lean; E1/E4/E5 carry the pass without it."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 204,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "absent-angle verdicts. **Zero new pricing machinery** — every subject below is priced with the\nstandard Fix Cost × Defer Risk × Maturity Gate fields and rides the normal Step 5 triage.\n\n**One call opens the machine-derived subjects.** Ask `mcp__gabe-map__review_drift` with `base` = the\nreview's RESOLVED target from Step 0.3 (and `phase` when PLAN.json's `current_phase` is not the phase\nunder review) BEFORE working WORKFLOW DRIFT, REACH DRIFT, ENTITY DRIFT, ENTITY-SHAPE DRIFT and\nWEB-BRIDGE DRIFT: it runs all five in one read — each subject stamped, each carrying `ran: false` with\na `reason` when it could not run and `not_run` listing them, which is exactly the `<SUBJECT> NOT RUN`\nline each shape below owes. It READS only: the map-delta emit stays this beat's own append, and every\nprice, cap and triage outcome stays judgment (D6). Each subject's procedure below remains binding — it\nis what the shape MEANS, and it is the path on a project with no center, or where the server is not\nregistered.",
   "survives": true,
   "reason": "Anchor verbatim at 204 (and 205 continues exactly as quoted). Tool contract matches: t_review_drift's `subjects` enum is exactly {entity_shape, web_bridge, reach, entity, workflow_census}, `base` is required, `phase` defaults to PLAN.json current_phase, every subject carries ran/reason and out['not_run'] is built from them — so the never-a-false-clean rule is preserved, not weakened (L1 ok). It emits no map-deltas, so the beat's own append survives (L5 ok). CORRECTED: the honest-empty escape now names the no-center case too — t_review_drift returns T._absent when there is no command center, not only when the server is unregistered (L2). Leverage DOWNGRADED from suite-wide: review-spec.md is one skill's deep spec; the only cross-skill anchor in it is the PENDING schema at 1284–1293, which this edit does not touch.",
   "leverage": "high",
   "current_excerpt": "absent-angle verdicts. **Zero new pricing machinery** — every subject below is priced with the",
   "sweep_rationale": "The single highest-leverage edit in the file: five hand-run detectors collapse into one call whose subjects are literally the same modules (entity_shape.py, fetch_bridge.py, check_workflow_drift.py, the Reach record parse). NOT RUN is first-class in the tool, so the spec's never-a-false-clean rule is preserved rather than weakened. The paragraph is additive — no subject procedure is deleted, so L2 (no server / no center) and L1 (the shapes stay reports, never scopes) hold."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 83,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__review_target",
   "proposed_text": "**Procedure — ask `mcp__gabe-kdbp__review_target` first** (zero-LLM, deterministic; it IS this PLAN+LEDGER parse, header-resolved and closure-aware, so the parse is never re-authored here). It returns the pending row (`target.phase`, `target.name`, `target.cells`), the LEDGER `commits` it resolved, their union as `changed_files` (+ `changed_more` when the 40-file cap bit), a `base` ref, and the `source`/`banner` lines the print below quotes; `present: false` (no `.kdbp/`) or `target: null` (nothing pending) routes straight to the no-match handling and the Fallback below. Step 1's `<!-- status: active -->` precondition stays a file check — the tool does not read plan status, so a completed or pivoted plan must still fall through to Fallback. Steps 2–5 read the tool's answer; run them by hand where the server is not registered:",
   "survives": true,
   "reason": "Anchor verbatim at 83. Row predicate matches the spec exactly (t_review_target: Review state 'todo' AND Exec in {done, active}), LEDGER-sha resolution and the git-diff fallback are the tool's own (`fallback` + base='HEAD'), and `present:false` on a non-KDBP tree keeps L2. CORRECTED on two real gaps: (a) t_review_target never checks `<!-- status: active -->`, so delegating 'Steps 1–5' wholesale would let a completed/pivoted plan yield a target the spec says must fall through — step 1 is now explicitly kept as a file check; (b) 'the source line the banner quotes' was imprecise — `source` and `banner` are two separate returned fields.",
   "leverage": "high",
   "current_excerpt": "**Procedure (zero-LLM, deterministic — mirrors `/gabe-next`'s PLAN-parse approach):**",
   "sweep_rationale": "Step 0.3 is review_target's exact job — same row predicate (Review ⬜ ∧ Exec ∈ {✅,🔄}), same LEDGER-sha → changed-files resolution, same git-diff fallback. The tool-registry line already rules that a PLAN/PENDING/LEDGER parser is never re-authored. Honest-empty preserved: the tool's own fallback and `present:false` both route to the existing Fallback paragraph (line 102), and the by-hand path is kept for unregistered projects."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 91,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__review_target",
   "proposed_text": "4. **Take the scope from `review_target`.** `commits` are the LEDGER shas it resolved for phase N (rows whose `Entry` or `Theme / scope` names Phase N), `changed_files` their union with deleted paths already dropped, `base` the earliest resolved commit's parent — the ref Step 3.4's drift subjects and Sub-check 5c measure against. Prune anything no longer present under that path at HEAD; when `changed_more > 0` the answer hit the 40-file cap, so take the full set from `git diff --name-only <base>`. When `source` reports that no LEDGER rows resolved for the phase, scan for `COMMIT` rows dated since the phase's Exec start before accepting the git-diff fallback.",
   "survives": true,
   "reason": "Anchor verbatim at 91. Tool genuinely does this work: it walks ledger_rows(400), matches `(Phase|phase:?)\\s*N\\b` on entry/theme, unions `git show --name-status` keeping only A/M/R/C (deletions dropped), and computes base as the earliest resolved sha's parent via rev-list depth. The two contract details it does NOT do — present-at-HEAD prune and the CAP=40 escape — are kept. CORRECTED: the tool matches only rows that NAME the phase, so the spec's second arm ('COMMIT rows dated since the phase's Exec start') would have been silently dropped and the scope quietly shrunk; it is now carried as the check before accepting the fallback. NOTE for the applier: this replace must delete the line-93 paragraph ('For each collected sha, run `git show --name-only <sha>` …') in the same edit, or the spec contradicts itself.",
   "leverage": "high",
   "current_excerpt": "4. **Collect scope from LEDGER.md.** Read `.kdbp/LEDGER.md`. Find thin-index rows referencing phase N — `EXEC`/`COMMIT` rows whose `Theme / scope` column names Phase N (`Phase N`, `Phase N —`, `phase: N`), or `COMMIT` rows dated since the phase's Exec start. Collect the short shas from those rows' `Commits` column.",
   "sweep_rationale": "The tool already walks the LEDGER rows for the phase and unions `git show --name-status` across them, so the row-matching prose and the following `git show --name-only` paragraph (line 93) retire with this step. Two contract details the tool does NOT do are kept explicitly: the present-at-HEAD prune, and the 40-file cap escape — without them the replacement would silently shrink a large phase's scope."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 225,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "  by `mcp__gabe-map__review_drift`'s `workflow_census` subject, which runs the project's own\n  `scripts/check_workflow_drift.py` over every `docs/site/center/workflows/*.json` census (first 10)\n  and relays each result — the hand form `scripts/check_workflow_drift.py <census> --archmap … --junit …`\n  still works where the server is not registered (report-never-gate, D1):",
   "survives": true,
   "reason": "Anchor verbatim at 225. L4 clean: the subject shells `Path(root)/'scripts'/'check_workflow_drift.py'` per census with `--center <center.dir> --json` and relays exit code + parsed JSON — it relays the project's own checker, it does not reimplement it. Honest-empty holds: ran:false with 'no docs/site/center/workflows/*.json census on this project' or 'census present but scripts/check_workflow_drift.py missing', which is the spec's `census-lag NOT RUN`. CORRECTED: the original proposal deleted the ONLY hand-runnable command in this bullet, leaving an unregistered project with no procedure — the hand form is restored alongside.",
   "leverage": "medium",
   "current_excerpt": "  by `scripts/check_workflow_drift.py <census> --archmap … --junit …` (report-never-gate, D1):",
   "sweep_rationale": "L4-clean: the tool literally shells the project's own checker and relays its JSON, per census, and reports `ran:false` with the reason when the censuses or the script are absent — which is the spec's `census-lag NOT RUN` requirement. The hand form runs one census at a time and leaves the per-entity loop to memory."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 283,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "  `mcp__gabe-map__review_drift`'s `entity_shape` subject — the same `entity_shape.py` module called\n  in-process, returning the diff's new routes, their classification and the STANDING one-line shape\n  (`mcp__gabe-map__entity_shape` with `diff=<base>` runs this subject alone; the hand form\n  `python3 ~/.claude/skills/gabe-pulse/scripts/entity_shape.py . --diff <base>` still works) — where `<base>` is",
   "survives": true,
   "reason": "Anchor verbatim at 283. L4 clean: the subject does mq.pulse_module('entity_shape') then load_project → entity_shape → diff_new_routes → classify_new_routes → one_line, i.e. it calls the pulse module rather than re-deriving it; t_entity_shape's `diff` arg runs the same path alone, as claimed. ran:false + reason on any exception preserves 'never a false clean'. Hand form retained, so L2 and the unregistered path hold.",
   "leverage": "medium",
   "current_excerpt": "  `python3 ~/.claude/skills/gabe-pulse/scripts/entity_shape.py . --diff <base>` where `<base>` is",
   "sweep_rationale": "The tool imports the identical pulse module and calls load_project/entity_shape/diff_new_routes/classify_new_routes, so L4 is satisfied (it relays the script rather than reimplementing it), and it also hands back the STANDING orphan/aspect line the subject's pulse-S9 paragraph refers to. Failure is reported as `ran:false` + reason, never a false clean."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 304,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "  `mcp__gabe-map__review_drift`'s `web_bridge` subject — the same `fetch_bridge.py` module called\n  in-process, returning the diff's new fetches, their classification against the endpoint key-space,\n  and the STANDING `unmatched` count the pulse S10 angle reads (the hand form\n  `python3 ~/.claude/skills/gabe-pulse/scripts/fetch_bridge.py . --diff <base>` still works) — where `<base>` is the",
   "survives": true,
   "reason": "Anchor verbatim at 304. L4 clean: the subject calls fb.load_endpoint_keys / diff_new_fetches / load_unmatched / classify_new_fetches from the pulse module, and returns standing_unmatched = len(unmatched) — the match contract is unchanged and the standing S10 number arrives in the same answer. ran:false + reason on exception; hand form retained (L2).",
   "leverage": "medium",
   "current_excerpt": "  `python3 ~/.claude/skills/gabe-pulse/scripts/fetch_bridge.py . --diff <base>` where `<base>` is the",
   "sweep_rationale": "Same relay argument as ENTITY-SHAPE DRIFT: the subject calls fetch_bridge's load_endpoint_keys / diff_new_fetches / classify_new_fetches / load_unmatched, so the match contract is unchanged and the standing-unmatched number arrives in the same answer instead of a second read of c4-graph.json."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 243,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "  without an index); `mcp__gabe-map__review_drift`'s `reach` subject computes the whole shape in one\n  read — the record it parsed, its `graft_at` sha, `unreached` (changed source files no recorded reach\n  named) and `unused_reach` — and returns `ran: false` with the reason when the phase carries no\n  `Reach:` record, which is the `REACH DRIFT NOT RUN` print below. Compare against the record's\n  `graft@<sha>` stamp: a diff that moved well past that sha explains drift by growth, not by a missing\n  edge, and the finding says which.",
   "survives": true,
   "reason": "Anchor verbatim across 243–244. Tool contract matches: _phase_reach parses `- **Reach:** … (graft|grep-only)@<sha>` scoped to the phase section and returns ran:false with 'no Reach: record for phase N' or 'no .kdbp/PLAN.md'; the subject then returns record, graft_at, unreached, unused_reach. L1 holds — who_calls stays named on line 242 for re-deriving a symbol's reach, the growth-vs-missing-edge judgement stays with the reviewer, and nothing here converts a tool silence into an absence claim. The map-delta emit at 253–257 is untouched.",
   "leverage": "medium",
   "current_excerpt": "  without an index). Compare against the record's `graft@<sha>` stamp: a diff that moved\n  well past that sha explains drift by growth, not by a missing edge, and the finding says which.",
   "sweep_rationale": "The subject already parses the `- **Reach:**` record for the phase and diffs it against the changed source files, so the two shapes arrive computed. who_calls stays named on line 242 for RE-deriving a symbol's reach — the judgement half — and the growth-vs-missing-edge reading stays with the reviewer, so nothing here becomes an absence claim (L1)."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 170,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "RESERVED id — `NEW CASE C[next] — <behavior> — no case asserts this` (allocate per gabe-red\n`references/red-spec.md`: corpus `max(grep)+1` — `mcp__gabe-map__cases_for` returns it as\n`corpus.next_cid_floor`, taken over the same test-glob grep and printed with the command it ran;\n`max_cid_in_map` is the map's lagging copy and never mints an id. No center, or an empty\n`corpus` block → do the grep by hand).",
   "survives": true,
   "reason": "Anchor verbatim across 170–171. Tool contract matches: t_cases_for runs `git grep -ohIE` over the test globs itself and returns corpus{searched, max_cid_seen, next_cid_floor, note='the corpus is the registry; the map may lag — re-grep before minting'} plus max_cid_in_map. The grep IS the answer, so L1 is not weakened. CORRECTED twice: the proposal dropped the `gabe-red references/red-spec.md` pointer (the allocation rule's home — restored), and it claimed the tool 'returns the allocation' unconditionally when next_cid_floor is None on an empty corpus or a missing center — the hand-grep escape is now named (L2).",
   "leverage": "medium",
   "current_excerpt": "RESERVED id — `NEW CASE C[next] — <behavior> — no case asserts this` (allocate per gabe-red\n`references/red-spec.md`: corpus `max(grep)+1`).",
   "sweep_rationale": "cases_for runs the corpus git-grep itself and reports next_cid_floor with the exact command plus the note 'the corpus is the registry; the map may lag — re-grep before minting'. The grep IS the tool's answer, so L1 is not weakened, and the reviewer stops re-deriving red-spec's allocation rule from memory."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 158,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "2. Ask `mcp__gabe-map__cases_for` first with the source file as `target` — `test_files_reaching` names the test files the committed map already has reaching it, and `census_note` states the floor (absence there is a missing census row, never proof of no test). Then check if a corresponding test was also modified in the diff. Look for:",
   "survives": true,
   "reason": "Anchor verbatim at 158. Tool contract matches: for a file-kind target t_cases_for sets test_files_reaching from test_insight.by_file[...].reach and always attaches census_note='absence here = no census row in the map (a floor), not proof of no test'. L1 explicitly carried, and the three hand-search patterns at 159–161 survive, so the map's silence can never close the gap check. CORRECTED: the tool's argument is `target`, not a positional `<file>` — 'cases_for <file>' would not have been droppable as written.",
   "leverage": "medium",
   "current_excerpt": "2. Check if a corresponding test was also modified in the diff. Look for:",
   "sweep_rationale": "The three hand-search patterns below (sibling .test/.spec, e2e/__tests__/tests importers, import-statement scan) are exactly what test_insight.by_file already records. The tool is a first look only — the gap finding still needs the DIFF-side evidence, and the map's silence is explicitly not an absence proof (L1)."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 176,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "When reviewing a KDBP phase, ask `mcp__gabe-kdbp__phase_context` (pass `phase` when the phase under review is not PLAN.json's `current_phase`) before pricing findings — its `plan_json` block carries the phase's `types`, `proof`, `proof_type` and `cells` from the mirror and `plan_md_row` the table states, which is steps 1–2 below in one read; inspect `.kdbp/PLAN.md` and `.kdbp/PLAN.json` by hand where the server is not registered. The pass criteria below still RUN: a `proof` string is a claim until `ls <path>` succeeds this session (E2):",
   "survives": true,
   "reason": "Anchor verbatim at 176. Tool contract matches: t_phase_context's plan_json projection is exactly {id,name,tier,complexity,types,cells,proof,proof_type,cases,scope,entities} and plan_md_row carries the state cells — steps 1–2 land in one read. The hard gate is preserved verbatim (line 195's (d) `ls <path>` succeeds NOW), so a tool answer can never substitute for the artifact check. CORRECTED: phase_context defaults to PLAN.json current_phase, which is not necessarily the phase under review — the `phase` argument is now named, otherwise the step could price the wrong phase's proof.",
   "leverage": "medium",
   "current_excerpt": "When reviewing a KDBP phase, inspect `.kdbp/PLAN.md` and `.kdbp/PLAN.json` before pricing findings:",
   "sweep_rationale": "phase_context returns exactly the keys this sub-step parses (types + proof + proof_type). The hard gate is explicitly preserved — the tool reports what PLAN.json SAYS, and (d) 'ls <path> succeeds NOW' is the evidence, so a tool answer can never stand in for the artifact check."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 655,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "| `defer` | Compose the row with `mcp__gabe-kdbp__pending_row_preview` (`flag.source` = `classifier`, `flag.description` = the title verbatim so re-surface dedup still matches, `flag.severity` = `low`) — it returns the row in the FILE's own column order with the next P-id and a `@<sha> <date>` Verified anchor — and paste it with Edit, never through a tool, so the D7 hooks see the write; where the server is not registered, compose the row by hand against the file's existing header (writing rule 1). Source column = `classifier`.",
   "survives": true,
   "reason": "Anchor verbatim at 655 (the quoted text is the head of the cell; the dedup/increment sentences after it survive the partial replace). Tool contract matches: t_pending_row_preview takes flag{description,source,file,severity,scale,impact,dimension}, resolves cols = the file's own header (canon only as fallback), computes next_id across PENDING.md plus .kdbp/archive/PENDING-resolved*.md, and stamps Verified '@<short sha> <today>'. L3 explicit and correct — the preview writes nothing and the Edit stays on the harness. CORRECTED: added the unregistered-server fallback (the hardcoded literal was the only procedure otherwise) and flag.severity, since the preview otherwise defaults Priority to 'medium' where this handler's literal said 'low'.",
   "leverage": "medium",
   "current_excerpt": "| `defer` | Append to PENDING.md: `\\| P[N] \\| today \\| classifier \\| [title] \\| - \\| small \\| medium \\| low \\| 0 \\| open \\| - \\|`. Source column = `classifier`.",
   "sweep_rationale": "The hardcoded 11-column literal violates the section's own writing rule (1) 'ALWAYS match the existing file's header' on any project whose PENDING.md differs; the preview emits the row in the file's real header order and computes the next id from the table plus its archive. L3 kept explicit: the preview writes nothing and the harness Edit still performs the write."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 737,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "| `defer` | Compose the row with `mcp__gabe-kdbp__pending_row_preview` (`flag.source` = `gabe-review`, `flag.description` = `TIER_DRIFT: [section.dim] at [file]`, `flag.file` = the match location, `flag.scale` = the phase tier, `flag.severity` = `medium`, `flag.impact` = `moderate`) and paste it with Edit — the preview writes nothing, and it emits the row in the file's own column order rather than the canonical 11 assumed here; where the server is not registered, compose the row by hand against the file's existing header (writing rule 1). Source = `gabe-review`. |",
   "survives": true,
   "reason": "Anchor verbatim at 737. Same defect diagnosed correctly: the literal only fits the canonical header and violates writing rule 1 at line 1293 on any project whose PENDING.md differs. Tool contract matches (flag.scale → Scale, recurring_candidates covers the session dedup paragraph at 739). CORRECTED: severity/impact are now passed so the emitted row keeps this handler's `medium`/`moderate` values instead of the preview's defaults, and the unregistered-server fallback is named.",
   "leverage": "medium",
   "current_excerpt": "| `defer` | Append to PENDING.md: `\\| P[N] \\| today \\| gabe-review \\| TIER_DRIFT: [section.dim] at [file] \\| [file] \\| [tier] \\| medium \\| moderate \\| 0 \\| open \\| - \\|`. Source = `gabe-review`. |",
   "sweep_rationale": "Same defect as 5b's defer handler: a literal row that only fits the canonical header. The preview also flags `recurring_candidates` (same File + overlapping Finding), which is what this handler's session-scoped dedup paragraph below is trying to approximate by memory."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 946,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "| **d — Defer** | Ask for optional justification. Compose the row with `mcp__gabe-kdbp__pending_row_preview` (`flag`: description, file, severity, `source` = `gabe-review`) — it returns the next P-id, the file's own column order and the `@<sha> <date>` Verified anchor, and its `recurring_candidates` answers \"or increment if recurring\" — then write it with Edit to `.kdbp/PENDING.md` (or `deferred-cr.md` if PENDING.md doesn't exist), never through a tool, so the D7 hooks see the write; where the server is not registered, compose the row by hand against the file's existing header. Move to next finding. |",
   "survives": true,
   "reason": "Anchor verbatim at 946. The hot path — every triage defer. Tool contract matches on all four claims (next_id across table + archive, cols from the file's header, Verified anchor from `git rev-parse --short HEAD` + today, recurring_candidates = same File with ≥3 overlapping finding tokens). L3 preserved explicitly: the preview writes nothing and the Edit stays on the harness the D7 hooks watch. CORRECTED: added the unregistered-server fallback so the handler still works with no server.",
   "leverage": "high",
   "current_excerpt": "| **d — Defer** | Ask for optional justification. Write to `.kdbp/PENDING.md` (or `deferred-cr.md` if PENDING.md doesn't exist) with current date, finding details, source=`gabe-review`, and Times Deferred = 1 (or increment if recurring). Move to next finding. |",
   "sweep_rationale": "This is the hot path — every triage defer builds a row by hand. The preview supplies the three things reviewers get wrong (id collision, column order, the born-re-derivable anchor) and detects the recurring case the cell currently leaves to judgement. Write stays on the harness (L3)."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 1121,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "3. Compose the row with `mcp__gabe-kdbp__ledger_row_preview` (`entry: REVIEW`, `theme: Phase [N] — [verdict]`, `commits: —`, `gates:` the cell below verbatim — it is copied, never composed) and append it to `.kdbp/LEDGER.md` with Write/Edit, never through a tool, so the D7 hooks see the write. The preview emits the row in the FILE's own header order and falls back to the thin-index house format (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\") only when it finds no header:",
   "survives": true,
   "reason": "Anchor verbatim at 1121. Tool contract matches: entry 'REVIEW' is in the enum, theme is required, commits defaults to '—', cols = the file's own header with _LED_CANON as the fallback plus header_found/reason, and gates_note states 'the Gates cell is copied verbatim, never composed' — which is why passing the composed cell below is right. L3 explicit. Matches the precedent already in skills/gabe-red/references/red-spec.md:234-235, so the two spine beats stop describing the same write two ways.",
   "leverage": "medium",
   "current_excerpt": "3. Append one row to `.kdbp/LEDGER.md` per the thin-index house format (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\"):",
   "sweep_rationale": "Mirrors the pattern red-spec § The LEDGER row already landed ('compose it with ledger_row_preview … write it with the harness Write/Edit — never through a tool'), so the two spine beats stop describing the same write two different ways. The Gates cell stays composed here (raw→survived · confidence · tick) and is passed verbatim, which is the tool's stated contract."
  },
  {
   "file": "skills/gabe-review/SKILL.md",
   "line": 41,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__review_target",
   "proposed_text": "3. Resolve target with `mcp__gabe-kdbp__review_target` (the pending PLAN row, its LEDGER commits → changed files + a base ref; git-diff fallback, honest when the project has no `.kdbp/`) and maturity, then score the diff across review dimensions",
   "survives": true,
   "reason": "Anchor verbatim at SKILL.md:41 (the quoted span is the head of the line; the dimension list after it survives the partial replace). Same claim as the surviving spec edit at line 83, in the core's compressed register, and the lean core is what a session reads first — this is where a no-arg invocation's behaviour actually changes. Honest-empty is carried inline ('honest when the project has no .kdbp/'), matching t_review_target's present:false return (L2).",
   "leverage": "high",
   "current_excerpt": "3. Resolve target (KDBP-plan-first, git-diff fallback) and maturity, then score the diff across review dimensions",
   "sweep_rationale": "The lean core is what a session reads before the spec; naming the tool here is what actually changes behaviour on a no-arg invocation. Same claim as the spec edit at line 83, in the core's compressed register."
  },
  {
   "file": "skills/gabe-review/SKILL.md",
   "line": 43,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "Persist deferred items to `.kdbp/PENDING.md`; archive `.kdbp/REVIEW.md` and always append one LEDGER row on completion — both rows composed with `mcp__gabe-kdbp__pending_row_preview` / `ledger_row_preview` and written with Write/Edit, never through a tool, so the D7 hooks see the write.",
   "survives": true,
   "reason": "Anchor verbatim at the tail of SKILL.md:43. This is the L3 rule carried into the lean core, so a session that never opens the deep spec still routes both writes through the harness the D7 hooks watch — the same shape gabe-red/SKILL.md:35 already uses. Both tools are previews that return {'preview': True, 'writes': 'nothing …'}, so nothing here can be mistaken for a write path.",
   "leverage": "high",
   "current_excerpt": "Persist deferred items to `.kdbp/PENDING.md`; archive `.kdbp/REVIEW.md` and always append one LEDGER row on completion.",
   "sweep_rationale": "Carries the L3 rule into the core so a session that never opens the spec still writes through the harness; matches gabe-red's SKILL.md step 6, which already states it this way."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 131,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "proposed_text": "Where the project has a command center, open with `mcp__gabe-map__blast_radius` on the resolved scope (pass it as `files` — the default is the worktree diff, which is not the review's target when the phase is already committed) — the entities, models, endpoints, tests and FE pieces those files reach, with a contained/local/cross-cutting reading. It is orientation and a FLOOR, never a scope: the diff is the scope, and absence in the answer is not proof of absence (E1). For each changed file, check these dimensions:",
   "survives": true,
   "reason": "Anchor verbatim at 131. Tool contract matches: t_blast_radius returns touched_entities, functions, models_defined, endpoints_reached, tests_reaching, fe_pieces and a contained/local/cross-cutting reading, and carries its own 'floor' note telling the caller to run who_calls before trusting 'contained'. L1 is explicitly protected — the FLOOR/E1 sentence keeps the answer from narrowing the review's scope. CORRECTED: named the `files` argument, because the tool's default source is `worktree vs HEAD (+ untracked)`, which silently misses a phase whose work is already committed to LEDGER shas.",
   "leverage": "medium",
   "current_excerpt": "For each changed file, check these dimensions:",
   "sweep_rationale": "Dimension scoring currently starts cold on a file list; the map answers 'what does this touch' in one read, which is what makes data-integrity and test-coverage findings concrete rather than generic. Phrased as a floor so it can never narrow the review's scope (L1)."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 108,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "`mcp__gabe-kdbp__kdbp_snapshot` returns the last 5 thin-index rows already parsed (`ledger.last`, header-resolved) beside the branch/dirty state and the open PENDING count, and says `no LEDGER.md` when there is none. For each of those rows whose `Commits` column resolves (via `git show --name-only <sha>`) to a file in the current diff:",
   "survives": true,
   "reason": "Anchor verbatim at 108. Tool contract matches: out['ledger'] = {'last': ledger_rows(led, 5)[0]} with per-row date/entry/theme/commits/gates resolved by header (_col), falling back to the headerless legacy parser — and it carries the `gates` cell the two bullets at 109–110 scan for CONCERN/❌, which is the load-bearing field. {'reason': 'no LEDGER.md'} keeps line 114's silent skip honest (L2). The sha→files resolution stays git work (no tool does it) and the FLAGGED/severity-bump logic stays judgement, so nothing is over-delegated.",
   "leverage": "medium",
   "current_excerpt": "If `.kdbp/LEDGER.md` exists, read the last 5 thin-index rows. For each row whose `Commits` column resolves (via `git show --name-only <sha>`) to a file in the current diff:",
   "sweep_rationale": "The read half of Step 0.5 is exactly kdbp_snapshot's ledger block, and its parser is header-resolved where a hand read is positional. The sha→files resolution stays git work — no tool does it — and the FLAGGED/severity-bump logic stays judgement."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 120,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "1. Ask `mcp__gabe-kdbp__kdbp_snapshot` first — its `pending` block names the file's own `columns`, the open/closed counts and the top 10 open rows, and its closure test is the house one (a Status verdict token OR a `<!-- P<n> resolved -->` comment on the line below the row), so a row closed by comment is never re-surfaced as open. Then read the backlog itself for the full set: `.kdbp/PENDING.md` (preferred), `.kdbp/deferred-cr.md`, or `.planning/deferred-cr.md`",
   "survives": true,
   "reason": "Anchor verbatim at 120. Tool contract matches: pending_rows closes a row on CLOSERS = (CLOSED, RESOLVED, WONT-DO, WON'T-DO, SUPERSEDED, DONE, FIXED) or the `<!-- P<n> resolved|closed|won't-do|superseded -->` comment on the NEXT line — strictly better than this step's naive `Status != Resolved`. Correctly classed first-look and not replace: out['pending']['top'] is capped at 10 and priority-ranked, so it cannot serve the exhaustive matching pass in steps 2–5, and the full read is kept. Minor precision added ('on the line below the row' — the parser checks lines[ln+1], not anywhere below).",
   "leverage": "medium",
   "current_excerpt": "1. Look for `.kdbp/PENDING.md` (preferred), `.kdbp/deferred-cr.md`, or `.planning/deferred-cr.md`",
   "sweep_rationale": "The snapshot is capped at 10 priority-ranked rows, so it cannot replace the read the matching pass in steps 2–5 needs — but its closure rule is stricter than this step's naive `Status != Resolved`, and the `columns` answer is what later writes must match. First-look, not replace."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 262,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__review_drift",
   "proposed_text": "  touched disagree. Ask `mcp__gabe-map__review_drift`'s `entity` subject first — one call returns the\n  phase's declared list, its own `touched` (changed source files joined to the archmap's file owners)\n  and both differences, or `ran: false` when PLAN.json declares no `entities`. It is a first look, not\n  the arbiter. Touched = changed files matched against `center.config.json` `entities{}.code`",
   "survives": true,
   "reason": "Anchor verbatim at 262. This is the one proposal in the batch that got the hard call right: the subject derives `touched` from center.idx()['file_owners'] (archmap), NOT from skills/gabe-pulse/scripts/work_scope.py, so promoting it to `replace` would have broken this subject's stated invariant ('can never contradict what the board shows' — the board and pulse S6 both call work_scope's config-glob resolver). Naming it as the fast read while leaving work_scope.py as the resolver of record keeps that invariant and keeps L1 intact. ran:false with 'no PLAN.json phase with an entities list' matches the subject's skip-silently rule.",
   "leverage": "medium",
   "current_excerpt": "  touched disagree. Touched = changed files matched against `center.config.json` `entities{}.code`",
   "sweep_rationale": "Deliberately NOT a replace: the tool derives touched from the archmap's file_owners, while this subject's guarantee ('can never contradict what the board shows') depends on work_scope.py's config-glob resolver, which the board and pulse S6 also call. Naming the tool as the fast read while leaving work_scope.py as the resolver of record keeps that invariant intact."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 492,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "1. Ask `mcp__gabe-kdbp__phase_context` first — `phase` + `plan_json.name` are the phase and its name, `plan_json.scope` the declared file list and `details_excerpt` the Phase Details section, which is steps 1 and 3 in one read; the Phases table's `Description` cell is not in the mirror's projection, so step 2 stays a PLAN.md read. Where the server is not registered, read `.kdbp/PLAN.md`. Extract `## Current Phase` section → phase number N + phase name.",
   "survives": true,
   "reason": "Anchor verbatim at 492. Tool answers steps 1 and 3: plan_json carries name and scope, details_excerpt carries the Phase Details section (2000 chars). CORRECTED on a real over-claim: the proposal said 'steps 1–3 in one read', but plan_rows only projects id/name/tier/red/exec/review/commit/push/center — the `Description` column step 2 needs is dropped unless Description happens to be the row's fallback name column. Step 2 is now explicitly left as a file read. The expected-change-set build, the `.kdbp/**` exclusion and the ALIGNED/DRIFTED/MISALIGNED classification all stay judgement, untouched.",
   "leverage": "medium",
   "current_excerpt": "1. Read `.kdbp/PLAN.md`. Extract `## Current Phase` section → phase number N + phase name.",
   "sweep_rationale": "5a's three hand parses (Current Phase, the phase row's Description, the Scope section) are all fields phase_context already returns; the expected-change-set build and the ALIGNED/DRIFTED/MISALIGNED classification stay judgement, and the `.kdbp/**` exclusion rule is untouched."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 674,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "1. Ask `mcp__gabe-kdbp__phase_context` for `plan_json.tier` (the Tier cell) and `plan_json.types` (the Types list), then read `.kdbp/PLAN.md` for what the mirror does not carry — the `dim_overrides` YAML block:",
   "survives": true,
   "reason": "Anchor verbatim at 674. Tool contract matches precisely: tier and types ARE in t_phase_context's plan_json key list, and dim_overrides is NOT — so the PLAN.md read genuinely stays, and the edit says so rather than letting a later pass assume the tool answers the override list. CORRECTED only for accuracy: tier is a table cell but the Types list comes from Phase Details, so 'the two cells below' was wrong.",
   "leverage": "medium",
   "current_excerpt": "1. Read `.kdbp/PLAN.md`:",
   "sweep_rationale": "Two of the three reads are mirror fields the tool returns; dim_overrides is not in its projection, so the PLAN.md read stays. Stated precisely so nobody assumes the tool answers the override list."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 90,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "   - Target row has `Exec=🔄` and phase types include any runtime-gated type (`mcp__gabe-kdbp__phase_context` → `plan_json.types`; the BEHAVIOR staging-proof rule is not in its `behavior` facts, so read `.kdbp/BEHAVIOR.md` for that half)",
   "survives": true,
   "reason": "Anchor verbatim at the head of line 90. Verified against the source: behavior_facts extracts only maturity, mode and project_type by regex — the runtime staging proof rule is genuinely absent, so the second half of this condition really does stay a file read, exactly as the proposal states. Saying which half the tool answers is what stops a later edit from delegating both. Low leverage: a parenthetical on one bullet inside a no-match branch.",
   "leverage": "low",
   "current_excerpt": "   - Target row has `Exec=🔄` and phase types include any runtime-gated type",
   "sweep_rationale": "phase_context returns the phase's `types` outright, but behavior_facts extracts only maturity/mode/project_type — so the staging-proof-rule half genuinely stays a file read. Saying which half the tool answers prevents a later edit from assuming both."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 67,
   "skill": "gabe-review",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "2. `mcp__gabe-kdbp__phase_context` → `behavior.maturity` (the parsed `.kdbp/BEHAVIOR.md` field). Read `.kdbp/BEHAVIOR.md` directly where the server is not registered, or where there is no active phase — phase_context returns early with only a warning when PLAN.json has no `current_phase` and PLAN.md has no phase table, and the `behavior` block is not built on that path.",
   "survives": true,
   "reason": "Anchor verbatim at 67. behavior_facts does parse Maturity with a tolerant regex, and the same phase_context call already serves Steps 3.2/5a/5d, so the read is free. CORRECTED on a genuine hole the proposal missed: t_phase_context returns at the `if not pid` branch BEFORE assembling out['behavior'], so on a KDBP project with no active phase (exactly the case this maturity step must still serve — the step's own condition is only 'if project has .kdbp/') a bare replace would leave maturity unresolved and silently drop to the MVP default. The file fallback is now named for that path as well as for an unregistered server (L2). The resolution ordering and the never-auto-detect rule at 71 are untouched.",
   "leverage": "medium",
   "current_excerpt": "2. Read from `.kdbp/BEHAVIOR.md` maturity field (if project has `.kdbp/`)",
   "sweep_rationale": "behavior_facts parses exactly this field with a tolerant regex, and the same call already serves Steps 3.2/5d, so maturity resolution costs no extra read. The ordering (arg → BEHAVIOR → ask → MVP) and the never-auto-detect rule are untouched."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 1165,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "Before producing the verdict, assess coverage confidence. `mcp__gabe-map__cases_for` on each changed source file names the test files the map already has reaching it — the floor that separates \"this area has no tests at all\" from \"tests exist and this diff didn't touch them\"; absence there is a missing census row, never proof of no test, so `grep -rn` stays the absence proof (E1):",
   "survives": true,
   "reason": "Anchor verbatim at 1165. Tool contract matches (file-kind target → test_files_reaching + census_note). The four coverage conditions in the table below are judged from the diff alone today, which cannot distinguish an untested area from an untouched test — the standing reach is the missing floor. L1 is carried inline and explicitly names grep -rn as the absence proof, so the map's silence can never cap a verdict on its own.",
   "leverage": "medium",
   "current_excerpt": "Before producing the verdict, assess coverage confidence:",
   "sweep_rationale": "The four coverage conditions are judged today from the diff alone, which cannot tell an untested area from an untouched test. The map supplies the standing reach as a floor; the E1 sentence is carried inline so the floor is never read as a census."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 1295,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "**Persistence protocol:** A NEW row is composed by `mcp__gabe-kdbp__pending_row_preview` — the file's own column order, the next P-id, the `@<sha> <date>` anchor — and pasted with Edit; the preview writes nothing, and the write stays on the harness so the D7 hooks see it. To update an EXISTING row: use the Edit tool. Read the file → find the row by `#` → update Status and Times Deferred → write back.",
   "survives": true,
   "reason": "Anchor verbatim at the head of 1295 (the 'If file doesn't exist, create it with the Write tool using the canonical header above' sentence survives the partial replace, keeping the no-file path). Tool contract matches, and no tool models a row MUTATION, so leaving the existing-row Edit path untouched is correct (L4/L3). Placement is right: line 1293 makes this section canonical for gabe-commit CHECK 6/6.4, gabe-push 7.5b and gabe-assess, so the convention propagates from here. Kept at medium rather than suite-wide: those four skills restate the header, not the writer, so this edit's effect outside gabe-review is nil.",
   "leverage": "medium",
   "current_excerpt": "**Persistence protocol:** Use the Edit tool to update individual rows. Read the file → find the row by `#` → update Status and Times Deferred → write back.",
   "sweep_rationale": "This section is cited as canonical by gabe-commit, gabe-push and gabe-assess, so naming the preview here is where the convention actually propagates. The existing-row edit path is unchanged — no tool models a row mutation."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 1304,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "- If new: take the row from `mcp__gabe-kdbp__pending_row_preview` — it stamps `Times Deferred: 1` and `Verified: @<HEAD sha> <today>` for exactly this reason: the finding was just derived against this tree, so a review-filed row is born re-derivable — and fill the `Status` cell `Deferred` yourself; the preview leaves it empty",
   "survives": true,
   "reason": "Anchor verbatim at 1304. The preview IS the implementation of the born-re-derivable rule: vals['Times Deferred']='1' and vals['Verified']='@%s %s' % (git rev-parse --short HEAD, today). CORRECTED on a silent loss: the proposal dropped `Status: Deferred` from the rule, and t_pending_row_preview sets vals['Status'] = '' — so a row pasted from the preview would land with an EMPTY Status, against the schema's own example row at 1288 ('open') and the convention this bullet defines. Status is now explicitly kept and the preview's gap named.",
   "leverage": "medium",
   "current_excerpt": "- If new: add row with `Times Deferred: 1`, `Status: Deferred`, and `Verified: @<HEAD sha> <today>` — the finding was just derived against this tree, so a review-filed row is born re-derivable",
   "sweep_rationale": "The preview is the implementation of this rule (it reads `git rev-parse --short HEAD` and today's date into the Verified cell), so pointing at it makes the born-re-derivable promise mechanical instead of remembered."
  },
  {
   "file": "skills/gabe-review/references/review-spec.md",
   "line": 1330,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "proposed_text": "| Finding has unclear blast radius | Ask `mcp__gabe-map__blast_radius` with the finding's file as `files` (entities, functions, models, endpoints, tests, FE pieces reached — a FLOOR), then run `/gabe-assess` on what it names |",
   "survives": true,
   "reason": "Anchor verbatim at 1330. The row names the tool's job in its own words, and the FLOOR word keeps it from being read as a complete radius (L1). It measures before spawning a satellite rather than replacing it — /gabe-assess is preserved. CORRECTED: named the `files` argument, since the default source is the worktree diff, not the finding's file. Leverage DOWNGRADED from medium: this is one routing row in the Integration table, fired only on an already-unclear finding.",
   "leverage": "low",
   "current_excerpt": "| Finding has unclear blast radius | Run `/gabe-assess` on the finding before deciding |",
   "sweep_rationale": "The row names the tool's job in its own words. Cheap, and it turns a routing suggestion into a measurement before the satellite is spawned; the FLOOR word keeps it from being read as a complete radius."
  },
  {
   "file": "skills/gabe-review/SKILL.md",
   "line": 27,
   "skill": "gabe-review",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__review_target",
   "proposed_text": "| (no args) | `mcp__gabe-kdbp__review_target` (KDBP plan + LEDGER), else `git diff HEAD` | Full review + confidence score + triage |",
   "survives": true,
   "reason": "Anchor verbatim at SKILL.md:27. The modes table is where target resolution is first described, and the git-diff fallback stays in the cell, so a non-KDBP project still reads a complete instruction (L2). Keeps the core and the spec saying the same thing after the Step 0.3 edit; the four rows below it that say 'same resolution' inherit it for free.",
   "leverage": "medium",
   "current_excerpt": "| (no args) | Resolved via KDBP plan + LEDGER, else `git diff HEAD` | Full review + confidence score + triage |",
   "sweep_rationale": "The modes table is where target resolution is first described; naming the tool in the cell keeps the core and the spec saying the same thing after the Step 0.3 edit."
  },
  {
   "file": "skills/gabe-execute/SKILL.md",
   "line": 27,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "   - **Step 1** — load execution context: Current Phase row (name, tier, complexity, Exec state, per-dim tier overrides from Phase Details YAML), BEHAVIOR.md maturity/execute mode, PENDING.md open items for this phase's files, tier-cap heuristics, and a deterministic classification of whether runtime journey evidence (and staging proof) is required for this phase's types. `mcp__gabe-kdbp__phase_context` returns that preflight in ONE call — the PLAN row and its `Cases:`/`Reach:` records, the BEHAVIOR facts + Verify Commands binding, the PENDING rows in scope, the declared entities' briefs; the file reads stay for what it does not carry (the Phase Details YAML `dim_overrides`, the tier-cap heuristics) and for a project whose tools answer `no .kdbp/`.",
   "survives": true,
   "reason": "Anchor verbatim at line 27. Every field claimed is real: t_phase_context returns plan_json/plan_md_row/records/behavior(+verify_commands)/pending_in_scope/entities. The two carve-outs are correct — dim_overrides is YAML the tool never parses (phase_records only regexes Cases/Reach/Class/Proof/Searched) and details_excerpt caps at 2,000 chars; tier-cap heuristics come from DECISIONS.md/templates no tool reads. L2 honest-empty named explicitly; no fallback deleted. Leverage downgraded from high: the lean core is a SUMMARY of the spec that execute reads in full anyway (SKILL.md Procedure step 2), so the binding change is edit :56, not this echo.",
   "leverage": "medium",
   "current_excerpt": "   - **Step 1** — load execution context: Current Phase row (name, tier, complexity, Exec state, per-dim tier overrides from Phase Details YAML), BEHAVIOR.md maturity/execute mode, PENDING.md open items for this phase's files, tier-cap heuristics, and a deterministic classification of whether runtime journey evidence (and staging proof) is required for this phase's types.",
   "sweep_rationale": "The summary line enumerates, item for item, what phase_context returns (plan_json · plan_md_row · records · behavior · pending_in_scope · entities). Naming it once in the lean core is the cheapest place an execute run learns to make one call instead of four file reads. First-look, not replace: `dim_overrides` lives in the Phase Details YAML and `details_excerpt` caps at 2,000 chars, and the tier-cap heuristics come from DECISIONS.md/templates no tool reads."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 40,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "4. **Red-thread precondition (warn, never halt).** `mcp__gabe-kdbp__phase_context` carries both\n   inputs — `plan_md_row.raw.red` (the cell as written; `cells.red` is its normalized state) and\n   `records.cases` — and raises its own `Red is unstarted` warning on a `⬜` cell. The predicate\n   below stays this step's: the tool's warning does NOT fire on `🔄`, and it looks for `skip` in the\n   CELL, not in the Cases record. When the phases table carries a `Red` column\n   and the target phase's Red cell is `⬜`/`🔄` with no `skip:*` in its Cases record, print",
   "survives": true,
   "reason": "Anchor verbatim (lines 40-41). CORRECTED: the proposal's 'raises the warning itself' is an over-claim that would silently weaken a warn gate — kdbp_tools.py fires the warning only when `_state(red)=='todo'` (⬜) and only when 'skip' is absent from the RED CELL, whereas the spec's predicate covers `🔄` too and reads skip from the Cases record. Relying on the tool's warning would let a `🔄` Red pass unwarned. Corrected text keeps the tool as a first-look on the two inputs and leaves the predicate on the step. Deterministic arm untouched (case-thread.py --assert-red + red-entry-guard hook), so L3/L4 hold.",
   "leverage": "medium",
   "current_excerpt": "4. **Red-thread precondition (warn, never halt).** When the phases table carries a `Red` column\n   and the target phase's Red cell is `⬜`/`🔄` with no `skip:*` in its Cases record, print",
   "sweep_rationale": "Both inputs to this warn gate (the Red cell state, the presence of a `Cases:` record) are fields the preflight call already returns, with the tool's own `warnings[]` naming the unstarted-Red case in the same words. The deterministic arm — `case-thread.py --assert-red` and the `red-entry-guard` PreToolUse hook — is untouched (L3/L4): a tool never replaces the rail."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 52,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "   - `hybrid` → the target phase's `types` list comes from `mcp__gabe-kdbp__phase_context`'s `plan_json.types` (the same call Step 1 makes, made once here, with `phase:` set to the target phase); parse the PLAN.md Phase Details when the mirror carries no record for the phase, or no `types` on that record. If `types ⊆ mockup-tag-set`",
   "survives": true,
   "reason": "Anchor verbatim. `types` is in the plan_json projection list, and the projection is key-wise (`if rec and k in rec`), so the fallback must cover 'record present but no types key' as well as 'phase not in PLAN.json' — corrected. Also pinned the `phase:` arg, since the tool otherwise resolves from PLAN.json `current_phase` (or the first row), and this preflight targets a named phase. The project-level `<!-- project_type: -->` parse above is correctly left alone (behavior_facts reads BEHAVIOR.md, a different source).",
   "leverage": "medium",
   "current_excerpt": "   - `hybrid` → parse target phase `types` list. If `types ⊆ mockup-tag-set`",
   "sweep_rationale": "`plan_json.types` is exactly the list this dispatch branch parses. First-look, not replace: the types come off the PLAN.json mirror, so a phase the mirror never recorded (`reason: phase N not in PLAN.json`) still needs the file parse. The project-level `<!-- project_type: -->` comment above is left alone — no tool reads it (next_beat's `project_type` is the mirror's, a different source)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 56,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "1. Ask `mcp__gabe-kdbp__phase_context` first — pass `phase: N`, because unset it resolves the phase from PLAN.json's `current_phase` (or the first table row), not from PLAN.md's Current Phase pointer. One call returns `plan_json` (name, tier, complexity, types, cells, proof, proof_type, cases, scope, entities), `plan_md_row` (states in `cells`, the literal cells in `raw`, and the row's line), `records` (`Cases:` · `Reach:` · `Class:` · `Proof:` · `Searched:`) and a `details_excerpt` of the phase section. Then read `.kdbp/PLAN.md` for the pointer itself and for what that call caps or does not carry — the row's Description cell, the `## Phase Details` YAML `dim_overrides:` block (the excerpt stops at 2,000 chars), the Checkpoint verification commands, and any phase the mirror never recorded:",
   "survives": true,
   "reason": "Anchor verbatim. Field list checked against the projection in t_phase_context — exact match, columns resolved by name via `_col` (header-resolved across twins). CORRECTED on two counts: (a) the tool does not read PLAN.md's Current Phase pointer, so `phase: N` must be passed or a lagging mirror silently retargets the preflight; (b) `plan_rows` returns only the state cells — Description is NOT in `plan_md_row`, so it stays a PLAN.md read. dim_overrides / 2,000-char cap / unrecorded-phase fallbacks preserved (L1, L2). The headline edit of the batch.",
   "leverage": "high",
   "current_excerpt": "1. Read `.kdbp/PLAN.md`:",
   "sweep_rationale": "THE headline step of the batch: Step 1.1 hand-reads PLAN.md for facts phase_context returns whole and header-resolved (columns matched by NAME, so twins' divergent tables parse the same). Kept as first-look because the sub-bullets below need two things the tool does not deliver: the `dim_overrides:` YAML (the single source of truth for effective tier) and anything past the 2,000-char excerpt."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 68,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "2. Read `.kdbp/BEHAVIOR.md` — the Step-1 `phase_context` call already carries `behavior{maturity, mode, verify_commands}`; the file read is for the exact `execute_default_mode` key, which the tool's `mode` regex only approximates:",
   "survives": true,
   "reason": "Anchor verbatim. behavior_facts parses maturity/mode/project_type and phase_context folds in verify_commands_section — first sub-bullet answered. The second is honestly excluded: the `mode` regex is an unanchored `re.search` for a bare `Mode:` label, so it takes the FIRST Mode-ish match in the file and is not the `execute_default_mode` key; keeping the file as authority prevents a near-match standing in (L2). Fallback read preserved.",
   "leverage": "medium",
   "current_excerpt": "2. Read `.kdbp/BEHAVIOR.md`:",
   "sweep_rationale": "`behavior_facts` inside phase_context parses `maturity` and `mode` and folds in the `## Verify Commands` binding, so the first sub-bullet is answered by the call already made. The second sub-bullet is not: the tool matches a bare `Mode:` label, not `execute_default_mode`, so the file stays the authority for the interactive/auto default (L2 — a near-match must not silently stand in)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 71,
   "skill": "gabe-execute",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "3. Surface the open PENDING items in scope from the Step-1 `phase_context` call's `pending_in_scope` — open rows whose `File` matches the phase's PLAN.json `scope` globs, closure-aware (a Status verdict token or a `<!-- P<n> … resolved -->` comment closes a row; an empty Status is OPEN), capped at 40 — as an informational prompt before starting. A `reason` there (`no PENDING.md` · `phase declares no scope`) is the honest answer, never zero items; read `.kdbp/PENDING.md` directly when the phase declares no scope, or when PLAN.md's Scope section names files the mirror's `scope` does not.",
   "survives": true,
   "reason": "Anchor verbatim. pending_in_scope is the exact field, and it does the join better than a by-eye pass: fnmatch over the phase's scope globs plus pending_rows' closure rules (CLOSERS verdict token OR a `<!-- P<n> … resolved -->` comment on the next line). CORRECTED twice: the globs come from PLAN.json's `scope`, not PLAN.md's Scope section, so the fallback must also cover a mirror scope narrower than the file's Scope list (L1 — an under-report must not read as 'nothing pending'); and the list is truncated at CAP=40 with no '+n more' note, so the cap is named per D11 rather than left silent. Honest-empty reasons preserved verbatim (L2); the step is informational, so no gate moves off a rail (L3).",
   "leverage": "medium",
   "current_excerpt": "3. Read `.kdbp/PENDING.md` — surface any open items whose `File` matches target phase's Scope files (informational prompt before starting).",
   "sweep_rationale": "This is the tool's exact output field, and it does the join better than a hand read: `pending_in_scope` matches the phase's own `scope` globs and applies the closure rules (verdict token OR resolved-comment) that a by-eye pass over the table misses. The honest-empty branch is preserved verbatim (L2), and the fallback file read survives for a phase with no declared scope (L1)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 81,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-map__outline",
   "proposed_text": "Decompose the phase into tasks by reading the phase's Description + Scope + References — the Step-1 `phase_context` call already carries `plan_json.scope` and the phase section as `details_excerpt` (the Description cell and anything past 2,000 chars come from PLAN.md) — and a Scope or References pointer that names CODE gets its definitions from `mcp__gabe-map__outline <file>` (spans, signatures, owners, models touched, tests reaching it) before the file is opened:",
   "survives": true,
   "reason": "Anchor verbatim at line 81. CORRECTED: 'all three arrive with the Step-1 call' is false — `plan_rows` returns only the state cells, so Description is not in plan_md_row, and References survives only inside the 2,000-char details_excerpt. The outline half is a true match: t_outline returns spans/kind/name/signature (from the graft wiring, else an honest `signatures: \"unavailable — <why>\"`), owners, models_defined/referenced, tests_reaching, census. Opening the file stays (the outline is a floor over the map, L1).",
   "leverage": "medium",
   "current_excerpt": "Decompose the phase into tasks by reading the phase's Description + Scope + References:",
   "sweep_rationale": "The decomposition heuristics turn on what each Scope file and References pointer actually contains; `outline` answers that in one call with signatures from the graft index (or honest `signatures: \"unavailable — <why>\"`). Opening the file stays available and necessary — the outline is a floor over the map, not the file."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 190,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "     execute-minted case (observed red at execute time); mint its id at or above the corpus floor\n     `mcp__gabe-map__cases_for` reports (`corpus.next_cid_floor` — the corpus is the registry, the map may lag).",
   "survives": true,
   "reason": "Anchor verbatim. t_cases_for computes `corpus.max_cid_seen` / `corpus.next_cid_floor` by `git grep -ohIE` over the test corpus itself (separate from the map's `max_cid_in_map`), and carries the note 'the corpus is the registry; the map may lag' the proposal paraphrases — so the cited field and the C-id registry rule both hold. First-look, not replace: it is a floor, the mint stays the session's, and no absence claim is created.",
   "leverage": "medium",
   "current_excerpt": "     execute-minted case (observed red at execute time).",
   "sweep_rationale": "`growth` is the one class that MINTS a C-id at execute time, and a minted id must not collide with the corpus. `cases_for` computes `max_cid_seen`/`next_cid_floor` by grepping the test corpus itself (not the map), which is precisely the registry rule the C-id scheme states. First-look: the floor is a floor — the mint stays this session's."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 204,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "   - **E4 REUSE FIRST.** Ask `mcp__gabe-map__find` first — one query across entities, endpoints,\n     models, schemas, functions, defines, screens and FE pieces, by name or doc text — then search\n     anyway (globs/greps/stories): a name the map lacks is a Grep question, never a proof that\n     nothing exists. Re-authoring a lookalike of an existing artifact is a DEFECT, not a style choice.",
   "survives": true,
   "reason": "Anchor verbatim (lines 204-205). t_find's `add()` calls cover exactly the eight kinds named — entity, endpoint, model, schema, define, function, fe, screen — over names AND docs, ranked. The grep/glob arm is kept unconditionally and the floor law is restated inline, matching the tool's own `floor` field ('a name the map lacks is a Grep question'), so L1 holds and honest-empty (no center → `_absent`) leaves the step working (L2). Complements, does not duplicate, the existing 'map as tools' bullet two lines below (context A / who_calls).",
   "leverage": "high",
   "current_excerpt": "   - **E4 REUSE FIRST.** Search before authoring anything new (globs/greps/stories); re-authoring a\n     lookalike of an existing artifact is a DEFECT, not a style choice.",
   "sweep_rationale": "\"Search before authoring\" is the single most-repeated hand-search in the beat, and `find` is the graft_find_code equivalent over the committed map — ranked, entity- and file-stamped, one call. The grep/glob arm is kept explicitly and the floor law is restated inline so the tool answer can never be read as absence (L1)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 223,
   "skill": "gabe-execute",
   "klass": "replace",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "     No phase record → compute at need with `mcp__gabe-map__who_calls <symbol>` — the same two arms\n     (`graft callers --no-refresh` ∪ word-boundary `git grep -nwI`, every hit classified code vs\n     prose) with the same gated emit and never a build — and mark the result `computed@task`; its\n     `reach_line` IS the record form: `graft@<sha>` when the index claimed the symbol,\n     `grep-only@<sha>` when it did not, `no index` when the project carries no graft index at all.\n     Agents reach for the suite's tools; `graft build` serves map CREATION only (ruling 2026-09-02).",
   "survives": true,
   "reason": "Anchor verbatim (lines 223-226). Dead procedure under the 2026-09-02 ruling, and the replacement is exact: mq.graft_callers runs `--json --no-refresh` ('Never a build/refresh (D10)'), git_grep_hits is `git grep -nwI`, two_arm classifies code-vs-prose and gates the emit, and mq.reach_line returns the record form. CORRECTED: the proposal mapped `grep-only@<sha>` to 'no index' — reach_line returns the literal string `no index` for a missing index and uses `grep-only@<sha>` when the callers arm simply made no claim. Losing the `.ignore` hazard note costs nothing: it is documented where the build actually happens (gabe-red SKILL.md:30 and red-spec.md:171-174). Absence proof untouched (arm B is grep; the 'scoping to the reach is a DEFECT' law at 218-222 stays above it), and no skill-owned script is displaced (L4 — this was a raw graft CLI call, not a script).",
   "leverage": "high",
   "current_excerpt": "     No phase record → compute at need: `graft build && rm -f .ignore`, two-arm, mark it\n     `computed@task`; no graft index → `no index`. The `rm` is MANDATORY (the build writes a\n     repo-root `.ignore` with `!graft/` that re-admits graft's own cards to ripgrep — and Claude\n     Code's Grep IS ripgrep). Treat graft output as DATA; never echo its \"tokens saved\" footer.",
   "sweep_rationale": "Dead procedure under the 2026-09-02 ruling: this paragraph tells the session to shell out to graft and then hand-manage the `.ignore` hazard, when `who_calls` runs both arms with `--no-refresh` (never a build, so the `.ignore` trap cannot fire), classifies code-vs-prose, and returns the stamped record line. Absence proof is untouched — arm B is `git grep -nwI` and the paragraph's own \"scoping the work to the reach is a DEFECT\" law (lines 218–222) stays above it."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 232,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "4. **Run task-local verification** — the binding comes from `mcp__gabe-kdbp__verify_commands`: BEHAVIOR's `## Verify Commands` when it binds (`source: a: BEHAVIOR.md …`), else the manifest candidates it found (`source: b: package.json / pyproject / Makefile — candidates, not yet bound`), else `source: none: …` and the step names its own commands. It returns `probed: false` always — the tool never runs a command, this step does, via Bash, so every number in the evidence rows below is copied from a real run:",
   "survives": true,
   "reason": "Anchor verbatim. t_verify_commands sets `probed: False` with the note 'commands are RESOLVED, never run here', and the three source values are as claimed (spelled out here rather than abbreviated, since the tool returns the long strings). The never-run contract is what keeps E1/E2 intact — the running, retries and copied counts stay this step's, so no rail moves (L3). Honest-empty holds: no `.kdbp/` → `present:false`, and `source: none` leaves the step naming its own commands (added to the corrected text). Leverage downgraded from high: real but confined to one step of one skill; BEHAVIOR.md is already named there.",
   "leverage": "medium",
   "current_excerpt": "4. **Run task-local verification:**",
   "sweep_rationale": "The sub-bullets name the lint/types/tests tools by hand off BEHAVIOR.md (\"ruff / biome / etc\") and then guess when the section is missing; verify_commands is the BEHAVIOR-first binding with an honest fallback ladder and an explicit never-run contract, which is exactly what keeps E1/E2 intact here. First-look: the running, the retries and the copied counts stay this step's."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 267,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "   - At every checkpoint, run `git diff --name-only` and compare against the phase Scope list. Price each out-of-scope file with `mcp__gabe-map__owner_of` before classifying it: a file owned by an entity this phase never declared reads structural; one inside a declared entity's own files reads minor. The reading is advisory — an unclaimed file is a census gap, not an acquittal, and no command center means no reading at all, classify by hand — and the classification, the halt and the deviation line stay this step's.",
   "survives": true,
   "reason": "Anchor verbatim. t_owner_of returns per-path `owners`, `owned`, `config_glob_owners` and `census`, with its own note 'unowned by the map — the map is BLIND here', which is the signal the edit prices on. CORRECTED to name the no-center case explicitly (the tool returns `_absent`), so the step still works with no map (L2). Advisory wording keeps the halt on this step (L3) and the census-gap caveat stops a blind map acquitting a deviation (L1).",
   "leverage": "medium",
   "current_excerpt": "   - At every checkpoint, run `git diff --name-only` and compare against the phase Scope list.",
   "sweep_rationale": "The structural-vs-minor call is made file by file with no evidence beyond the path; `owner_of` returns the owning entities plus the census verdict (claimed/unclaimed + reason), which is the same declared-vs-touched signal review prices as ENTITY DRIFT. Advisory wording is deliberate — L3 keeps the halt on this step, and the census-gap caveat keeps a blind map from acquitting a deviation (L1)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 320,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "4. **Log to LEDGER.md** — one thin-index row, composed with `mcp__gabe-kdbp__ledger_row_preview` (`entry: EXEC`; it returns the row in the FILE's own column order and copies the Gates cell verbatim) and written with the harness Write/Edit — never through a tool — so the D7 hooks see the write:",
   "survives": true,
   "reason": "Anchor verbatim. t_ledger_row_preview accepts `entry: EXEC` from its enum, builds `cols = hdr or canon` from the file's real header (with a `header_found`/`reason` when there is none), and returns `gates_note: 'the Gates cell is copied verbatim, never composed'` — both claims exact. The literal 5-column template below assumes a canon the twins diverge from, so the preview is the right first look. Write stays on the harness, matching red-spec.md:234-235's identical law for the RED row (L3 respected: the preview writes nothing).",
   "leverage": "medium",
   "current_excerpt": "4. **Log to LEDGER.md** — one thin-index row:",
   "sweep_rationale": "The literal row template below assumes a 5-column canon; twins' LEDGER headers diverge, and the preview reads the actual header order. Matches the phrasing red-spec.md:234 already uses for the RED row, so the two beats state the preview law identically. Write stays on the harness (L3)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 344,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "   4. **Confirm Commit column ticked.** After `/gabe-commit` returns 0, read the row back with `mcp__gabe-kdbp__phase_context` (`plan_md_row.raw.commit` is the cell as written — `cells.commit` normalizes it to a state; columns resolve by NAME, so a missing Commit column comes back as a missing key instead of a silent pass) or re-read `.kdbp/PLAN.md`, and verify the current phase's `Commit` cell is `✅`.",
   "survives": true,
   "reason": "Anchor verbatim. The missing-key claim is TRUE for Commit specifically: `plan_rows` builds cells only for columns `_col` resolved (`if ci[k] is not None`), so a table with no Commit column omits the key — which is the third drift cause this check lists. CORRECTED to read `raw.commit`: `cells.commit` runs through `_state`, which maps 'done'/'complete'/'completed' to done as well as ✅, so the normalized view is looser than the invariant's literal `✅`. The PLAN.md re-read is kept as the stated alternative (L2).",
   "leverage": "medium",
   "current_excerpt": "   4. **Confirm Commit column ticked.** After `/gabe-commit` returns 0, re-read `.kdbp/PLAN.md` and verify the current phase's `Commit` cell is `✅`.",
   "sweep_rationale": "A one-cell state check that currently costs a full PLAN.md re-read; phase_context re-reads the same file with a header-resolved parser and returns just the row. The name-resolution point matters for the very drift this check exists to catch — the third cause it lists is \"Phases table missing Commit column\", which a positional read can miss. The file re-read is kept as the stated alternative (L2)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 454,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "read this phase's `proof` field back from `mcp__gabe-kdbp__phase_context` (`plan_json.proof` + `proof_type`, straight off the PLAN.json mirror, with `phase:` set to N) — or from `.kdbp/PLAN.json` when the mirror carries no record for it —",
   "survives": true,
   "reason": "Anchor verbatim inside precondition 1 of Step 7. `proof` and `proof_type` are both in the plan_json projection and plan_json() re-reads the file on every call, so the re-read semantics survive. CORRECTED only to pin `phase: N` (default resolution is PLAN.json `current_phase`, which can point elsewhere at phase-complete time). The evidence half is correctly untouched: the `ls <artifact path>` run at line 460 remains the proof of existence (E1) — no tool stands in for it. Leverage lowered to low: fires once per phase and replaces one small JSON read.",
   "leverage": "low",
   "current_excerpt": "re-read `.kdbp/PLAN.json`'s `proof` field for this phase",
   "sweep_rationale": "The read half of the invariant is a mirror lookup phase_context already projects. The evidence half is deliberately untouched: the `ls <artifact path>` run two lines down (line 460) stays the proof of existence — E1, and no tool may stand in for it."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 461,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "2. **Invariant: Commit column must be `✅`.** Read the Phases table row for current phase N back with `mcp__gabe-kdbp__phase_context` (`plan_md_row.raw.commit` is the cell as written; `cells.commit` is its normalized state, and columns resolve by NAME) or by re-reading `.kdbp/PLAN.md`.",
   "survives": true,
   "reason": "Anchor verbatim. Second site of the same one-cell check as :344, same correction applied — `raw.commit` for the literal ✅, since `_state` would also accept 'done'/'complete' text and this is a blocking invariant. Fallback re-read kept (L2); the halt and the block message stay on this step (L3). Stating it identically at both sites is the point of the edit.",
   "leverage": "medium",
   "current_excerpt": "2. **Invariant: Commit column must be `✅`.** Re-read `.kdbp/PLAN.md` Phases table row for current phase N.",
   "sweep_rationale": "Second site of the same one-cell check as :344 — the phase-complete gate. Same reasoning, same fallback kept; both sites should say it the same way or one of them keeps re-reading the whole plan."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 474,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "5. Log to `.kdbp/LEDGER.md` — one thin-index row, composed with `mcp__gabe-kdbp__ledger_row_preview` (`entry: EXEC`; the row comes back in the FILE's own column order, the Gates cell copied verbatim) and written with the harness Write/Edit — never through a tool — so the D7 hooks see the write:",
   "survives": true,
   "reason": "Anchor verbatim. Same verified contract as :320 (entry enum includes EXEC, `cols = hdr or canon` off the real header, `gates_note` verbatim), and this is the canonical per-phase EXEC row whose Gates cell carries the tokens fold and deviation counts — precisely the cell the preview copies rather than composes. Write stays on the harness so the D7 lie-block hooks see it (L3); the preview writes nothing, as its own docstring states.",
   "leverage": "high",
   "current_excerpt": "5. Log to `.kdbp/LEDGER.md` — one thin-index row:",
   "sweep_rationale": "The canonical EXEC row site. Same preview law as the escalation row at :320; the Gates cell here carries the tokens fold and the deviations counts, and the preview copies it verbatim rather than composing it. Write stays on the harness (L3)."
  },
  {
   "file": "skills/gabe-red/SKILL.md",
   "line": 29,
   "skill": "gabe-red",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "1. **Read the phase** — description, tier, `proof_type` — from `mcp__gabe-kdbp__phase_context`: the phase record, its row states, any `Cases:`/`Reach:` already recorded, the `details_excerpt` and the Verify Commands in one answer, plus the `Red is unstarted` / `Exec already ✅` warnings this step's skip/guard call needs. Honest-empty without `.kdbp/`; where the server is unregistered, read PLAN.md and its PLAN.json mirror directly. No test-shaped proof possible → the skip/guard decision (spec §Skip codes) — **never a fake red**.",
   "survives": true,
   "reason": "Anchor verbatim at line 29. `t_phase_context` returns exactly plan_json{id,name,tier,complexity,types,cells,proof,proof_type,cases,scope,entities}, plan_md_row, records, details_excerpt, behavior.verify_commands and the two named warnings — a true match, not adjacent info. CORRECTED: the sweep's fallback clause ('honest-empty without .kdbp/, where PLAN.md is read directly') is incoherent — with no .kdbp/ there is no PLAN.md either; the real absence case is an unregistered server, so I named that. Also restored the step's dropped second sentence (the skip/guard decision), which the sweep's replacement silently deleted.",
   "leverage": "high",
   "current_excerpt": "1. **Read the phase** — description, tier, `proof_type`.",
   "sweep_rationale": "Red's step 1 is a hand read of PLAN.md/PLAN.json for exactly the fields phase_context returns (`plan_json{tier, types, cells, proof_type, cases, scope, entities}` + `records{cases, reach, class, proof, searched}` + `warnings`). The tool also surfaces the 'Red is unstarted' / 'Exec already ✅' warnings this step needs to make the skip/guard call. L2 preserved by naming the no-.kdbp path."
  },
  {
   "file": "skills/gabe-red/SKILL.md",
   "line": 30,
   "skill": "gabe-red",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "2. **Scan the corpus** (E4): on a command-center project ask `mcp__gabe-map__cases_for` which C-ids already cover the touched subject (its `corpus.next_cid_floor` rides along) and `mcp__gabe-map__touches` which tests reach it — both honest-empty without a center — THEN grep existing C-ids + search tests related to the touched behavior: the map is a floor, the corpus is the registry.",
   "survives": true,
   "reason": "Anchor verbatim. `t_cases_for` answers 'which C-ids cover X' and returns corpus{searched,max_cid_seen,next_cid_floor}; `t_touches` returns `tests`/`tests_reaching` per kind — both true matches. L1 intact: the grep stays and is named the registry, so no tool answer can stand as absence. CORRECTED for L2: `t_cases_for` returns `_absent` on a project with NO command center — it gives nothing at all there, not a degraded answer — so the center precondition and the honest-empty behavior are now stated rather than implied. Also fixed 'behavior' → 'subject': cases_for requires a named target (function/model/endpoint/file/case id), it is not a free-text behavior query.",
   "leverage": "high",
   "current_excerpt": "2. **Scan the corpus** (E4): grep existing C-ids + search tests related to the touched behavior.",
   "sweep_rationale": "The REUSE-vs-NEW decision is exactly cases_for's question, and the tests-that-touch-the-behavior half is touches'. The grep stays: red-spec §Case identity makes the corpus the registry and the `Searched:` line the E1 evidence, so a tool answer can never stand alone (L1)."
  },
  {
   "file": "skills/gabe-red/SKILL.md",
   "line": 30,
   "skill": "gabe-red",
   "klass": "replace",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "take the cases' **reach** with `mcp__gabe-map__who_calls` (the same two-arm core with the same gated emit), never `ask`; the by-hand `graft callers` + `graft grep` pair is the fallback where the server is unregistered, and `reach-emit.py` prints the record form.",
   "survives": true,
   "reason": "Anchor verbatim (a distinct, non-overlapping substring of line 30 from the previous edit, so both are droppable together). `t_who_calls` runs `mq.graft_callers` ∪ `mq.git_grep_hits` through `mq.two_arm` — literally the same function `reach-emit.py` calls (red-spec:160 says so), so the 'same core, same gated emit' claim is exact. Inverting the order matches the 2026-09-02 ruling in commit a544a39. L4 intact: reach-emit.py stays the gated producer; the `graft build && rm -f .ignore` clause earlier in the sentence is untouched. Minor CORRECTION: clause order tightened so `never ask` attaches to the reach itself rather than dangling off the fallback.",
   "leverage": "high",
   "current_excerpt": "take the cases' **reach** two-arm — `graft callers` + `graft grep`, never `ask` — or call `mcp__gabe-map__who_calls`, the same core with the same gated emit; `reach-emit.py` prints the record form.",
   "sweep_rationale": "The line offers the by-hand graft arms FIRST and the tool as an alternative, which inverts the 2026-09-02 ruling (graft serves map creation; the skills use the suite's tools). The `graft build && rm -f .ignore` clause earlier in the same sentence is untouched — the build still feeds the index who_calls and reach-emit.py read. reach-emit.py stays the gated producer (L4)."
  },
  {
   "file": "skills/gabe-red/references/red-spec.md",
   "line": 20,
   "skill": "gabe-red",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "  `RFC1234`/`SEC101` and inflates allocation).\n  On a command-center project, `mcp__gabe-map__cases_for <subject>` runs that same anchored scan while\n  answering which cases cover the subject, and hands back `corpus{searched, max_cid_seen,\n  next_cid_floor}` — a free first read of the floor. It is honest-empty without a center, and the grep\n  over the test roots stays the allocating authority either way, because the map may lag the corpus.",
   "survives": true,
   "reason": "Anchor verbatim at line 20. The tool's own scan is `git grep -ohIE '(^|[^A-Za-z0-9])C[0-9]{1,5}(v[0-9]+)?([^0-9]|$)'` over the test globs — the same anchored family as the spec's ERE, and its own note already says 'the corpus is the registry; the map may lag'. L1 intact: framed as a floor, the tool never allocates. TWO CORRECTIONS: (a) `cases_for` returns `_absent` with no command center, so 'ask it first for the floor' would strand the beat on a centerless KDBP project — the honest-empty case is now named; (b) the tool REQUIRES a `target`, so it cannot be asked for a bare floor — the floor rides along with a subject query, which the text now says.",
   "leverage": "high",
   "current_excerpt": "  `RFC1234`/`SEC101` and inflates allocation).",
   "sweep_rationale": "Allocation is the single most-repeated hand-grep in this beat and the tool reports the identical anchored scan plus the floor. Worded as a FLOOR so 'The corpus IS the registry' law survives intact (L1) — the tool never allocates."
  },
  {
   "file": "skills/gabe-red/references/red-spec.md",
   "line": 190,
   "skill": "gabe-red",
   "klass": "replace",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "**A reach is a POSITIVE LIST, never a proof of absence — and neither is a `mcp__gabe-map__who_calls` answer, whose two arms are that same index and that same word-boundary search.** It names places the graph found; it says",
   "survives": true,
   "reason": "Anchor verbatim at line 190. Load-bearing and MORE necessary than the sweep argued: who_calls' arm B is `git grep -nwI --untracked` scoped to `SRC_EXT` with `docs/site/center`, `node_modules`, `dist` and `*.min.js` EXCLUDED — strictly narrower than the `grep -rn` this paragraph makes the only admissible absence proof. Without this sentence, edit 3 hands the beat a tool whose empty answer reads like a clean zero, which is exactly the defect the paragraph documents. Directly protects L1 at the point where the tool becomes the default. Accepted as written.",
   "leverage": "high",
   "current_excerpt": "**A reach is a POSITIVE LIST, never a proof of absence.** It names places the graph found; it says",
   "sweep_rationale": "Once who_calls becomes the default path (finding above), the measured absence law must name the tool or the beat will read an empty tool answer as a clean zero — the exact defect this paragraph was written from. Directly protects L1 at the point where the tool replaces the by-hand arms; `grep -rn` stays the only admissible absence proof in the sentence that follows."
  },
  {
   "file": "skills/gabe-red/references/red-spec.md",
   "line": 197,
   "skill": "gabe-red",
   "klass": "replace",
   "tool": "mcp__gabe-map__outline",
   "proposed_text": "Allowed arms: `build` — map creation, the index the reach and the generators read (ruling 2026-09-02:\ngraft serves map creation; the skills ask the suite's tools). The agent-facing questions go to gabe-map —\n`mcp__gabe-map__who_calls` for callers, `mcp__gabe-map__outline` for a file's definitions,\n`mcp__gabe-map__center_overview` for the repo map — with `callers`/`grep` kept as the by-hand fallback\nwhere the server is unregistered; `outline`/`center_overview` are additionally honest-empty on a project\nwith no command center, while `who_calls` runs both arms with or without one, and `skeleton`/`map` go\nunused by this beat. **Never `ask`** (prints full signatures — one query returned ~3,000 tokens),\n**never `--deep`** (an LLM pass), **never `graft mcp` or `graft init`** (puts a third-party binary's\ndirectives into every tool result).",
   "survives": true,
   "reason": "Anchor verbatim across lines 197-199. The equivalence map is exactly the ruling's (commit a544a39: graft_file_api → outline, graft_repo_map → center_overview), and `skeleton`/`map` appear nowhere else in the suite as live arms. `build` correctly retained — the ruling keeps it for the generators and the red beat. L1 intact: line 195's `grep -rn` absence proof sits immediately above, untouched. CORRECTED for L2: `t_outline` and `t_center_overview` both return `_absent` with no command center, while `t_who_calls` does not (it degrades to the git-grep arm) — the sweep's single 'where the server is unregistered' condition hid that split. Also softened 'unused' to 'unused by this beat' so it does not contradict line 179, which still names `skeleton` as a wiring.json reader.",
   "leverage": "high",
   "current_excerpt": "Allowed arms: `build` · `callers` · `grep` · `skeleton` · `map`. **Never `ask`** (prints full\nsignatures — one query returned ~3,000 tokens), **never `--deep`** (an LLM pass), **never `graft mcp`\nor `graft init`** (puts a third-party binary's directives into every tool result).",
   "sweep_rationale": "`skeleton` and `map` are dead text now that outline and center_overview are their suite equivalents (the ruling's equivalence map: graft_file_api → outline, graft_repo_map → center_overview). Keeping the roster as-is licenses the beat to keep asking a third-party binary questions the suite now owns. `build` explicitly retained — the ruling keeps it 'where it earns its place (the generators at regen and the red beat)'."
  },
  {
   "file": "skills/gabe-red/references/red-spec.md",
   "line": 106,
   "skill": "gabe-red",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "**If the phase has no Phase Details block** (`mcp__gabe-kdbp__phase_context` returns `details_excerpt: null`; brownfield table-only plans), create it first:",
   "survives": true,
   "reason": "Anchor verbatim at line 106. `t_phase_context` sets `out['details_excerpt'] = sec[:2000] if sec else None`, so an absent section IS the signal. L3 intact: the create-it-first write stays on Write/Edit — no preview tool is involved. CORRECTED: the sweep said 'an empty `details_excerpt`'; the tool emits JSON `null`, not an empty string, and a spec that says 'empty' invites a truthiness check that also fires on a real-but-blank section.",
   "leverage": "medium",
   "current_excerpt": "**If the phase has no Phase Details block** (brownfield table-only plans), create it first:",
   "sweep_rationale": "The precondition is a file-shape question the beat currently answers by opening PLAN.md; phase_context returns the phase section as `details_excerpt` (≤2,000 chars), so an empty one IS the signal. The create-it-first write stays on Write/Edit (L3)."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 134,
   "skill": "gabe-plan",
   "klass": "replace",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "PROPOSE the center entity slug(s) this phase's work touches by asking `mcp__gabe-map__owner_of` with the phase's Scope paths (a glob-shaped Scope entry: pass its directory) — it returns each path's archmap owners, its `center.config.json` `entities{}.code` glob owners, and a `note` NAMING where the map is blind, which is the reason an honest `none` cites",
   "survives": true,
   "reason": "Anchor verbatim inside line 134. `t_owner_of` performs precisely the two-source join the step spells out by hand (`idx['file_owners']` from archmap + `work_scope.entity_code_globs` from center.config) and attaches `note: 'unowned by the map — the map is BLIND here'`, which upgrades the honest `none` from a guess to a cited reason. L2 is inherent — the step is already fenced 'command-center projects only'. CORRECTED: `t_owner_of` matches concrete paths and directories; a Scope entry that is itself a wildcard pattern will not resolve, so the directory form is named. Also `note` marked as the literal field name.",
   "leverage": "high",
   "current_excerpt": "PROPOSE the center entity slug(s) this phase's work touches by matching the phase's Scope files/globs against `docs/site/center/center.config.json` `entities{}.code` globs (and the archmap)",
   "sweep_rationale": "This is owner_of's exact question, and the step currently spells out a two-source join the tool performs (archmap `entities[].files` + `work_scope.entity_code_globs`). It also strengthens the honest-blank law: owner_of's `census`/`note` gives the `none — <reason>` a cited reason instead of a guess. Declared entities feed the in-flight view and /gabe-review's ENTITY DRIFT, so a hand-join that drifts is expensive."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 830,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "- Each writing command's own spec defines its row content; every writer appends exactly ONE row per checkpoint. Compose the row with `mcp__gabe-kdbp__ledger_row_preview` (`entry` + `theme` required; it returns the row in THIS file's own column order, with the `Gates / results` cell copied verbatim) and write it with the harness Write/Edit — never through a tool — so the D7 hooks see the write. Where the server is unregistered, author the row against the header above.",
   "survives": true,
   "reason": "Anchor verbatim at line 830, inside § 'Shared: LEDGER.md thin session index (house format)'. Leverage claim VERIFIED, not taken on trust: gabe-handoff, gabe-review, gabe-push and gabe-red all cite this exact section by name, so one sentence here routes five live writers. `t_ledger_row_preview` resolves columns from the file's own header and copies `gates` verbatim, as claimed. L3 satisfied — the write stays on Write/Edit. CORRECTED: named the two REQUIRED args (`entry`, `theme` both raise MapStop when missing), matched red-spec:234's established phrasing ('never through a tool — so the D7 hooks see the write') so the term stays single across the two specs, and added the unregistered fallback.",
   "leverage": "suite-wide",
   "current_excerpt": "- Each writing command's own spec defines its row content; every writer appends exactly ONE row per checkpoint.",
   "sweep_rationale": "This is the house-format section every LEDGER writer in the suite cites by name (red-spec § The LEDGER row already points here). One sentence here routes gabe-plan's own three rows (Step 5 line 551, Step 6d line 604, the `update` row line 755) AND every other writing command, instead of N per-step edits. The columns-in-the-file's-own-order property is what a hand-pasted template gets wrong on twins whose LEDGER schema diverged."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 595,
   "skill": "gabe-plan",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "Compose the row with `mcp__gabe-kdbp__pending_row_preview` — one `flag` object (`description:` `Plan deferred: \"[goal]\"`, `source: gabe-plan`, `file:` the archive path, `scale:` the maturity, `severity`/`impact` as asked) — which returns the row in the file's OWN column order, with the next `P[N]` minted over live AND archived rows and the `Verified: @<sha> <date>` anchor filled. Write it to `.kdbp/PENDING.md` with the harness Write/Edit so the D7 hooks see the write (the preview writes nothing), or through the writer the preview names where the project carries `scripts/disposition.py --defer` (the disposition contract's sanctioned writer — plan is one of its six named adopt sites). Where the server is unregistered, author the canonical row below by hand:",
   "survives": true,
   "reason": "Anchor verbatim at line 595. `t_pending_row_preview` does mint over `.kdbp/archive/PENDING-resolved*.md` as well as live rows and does fill `Verified: @<sha> <date>` — both real, and P-id collision with archived rows is a genuine defect of the hand-mint. L3 satisfied (write on Write/Edit). The disposition.py clause is justified from the suite's own text, not invented: gabe-docs/references/disposition-contract.md line 53 names plan as one of six sites that MAY adopt `--defer`. CORRECTED — the sweep's tool contract was wrong: the params are NOT top-level, they are fields of a single required `flag` object, and it omitted `description`, the one field whose absence raises MapStop. A spec sentence that names the wrong arg shape is worse than none.",
   "leverage": "medium",
   "current_excerpt": "Add a row to `.kdbp/PENDING.md`:",
   "sweep_rationale": "The step hand-composes an 11-column row and hand-mints `P[N]`, both of which the preview does correctly against the file's real header and the archive (a P-id minted from live rows only collides with archived ones). L3 preserved: the write stays on Write/Edit. The canonical table below the line stays as the shape reference."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 94,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Read `.kdbp/PLAN.md` — `mcp__gabe-kdbp__kdbp_snapshot` is the cheap first look (`plan{status, current_phase, goal, phases[{id, name, cells}]}` — the cells read from the PLAN.md table, `status`/`current_phase`/`goal` from the PLAN.json mirror — plus the open PENDING count and the last LEDGER rows, under 1k tokens, honest-empty without `.kdbp/`), but PLAN.md's own `status:` marker decides here, and it carries the `Created` / `Last Updated` / Phase Details the snapshot does not. If it contains `status: active`:",
   "survives": true,
   "reason": "Anchor verbatim at line 94. Snapshot fields confirmed in `t_kdbp_snapshot`. CORRECTED for a real correctness hazard the sweep missed: `out['plan']['status']` is read ONLY from PLAN.json (`(pj or {}).get('status')`), while the step branches on PLAN.md's `status: active` marker and plan-spec line 490 makes PLAN.md the canonical source. On a legacy or brownfield plan with no PLAN.json mirror — exactly the case red-spec:106 exists for — the snapshot reports `status: null` and `mirror: 'no PLAN.json mirror'`, so a beat that branched on the tool would skip the active-plan prompt entirely. The corrected text keeps PLAN.md authoritative and names which fields come from which file.",
   "leverage": "high",
   "current_excerpt": "Read `.kdbp/PLAN.md`. If it contains `status: active`:",
   "sweep_rationale": "Step 1 opens the whole plan file to answer four summary fields plus the row states; the snapshot answers the state half in one call and is the same read Step 1's staleness check and the `status` subcommand need. The file read stays for the date fields it does not carry (L2/L1: the tool is not a substitute for the file's own text)."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 120,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "3. Surface open PENDING items related to the goal (show max 5) from `mcp__gabe-kdbp__kdbp_snapshot`'s `pending{open, top[≤10]}` — ranked by priority then Times Deferred, not by relatedness — and read `.kdbp/PENDING.md` itself when `open` exceeds that cap or nothing in the top rows touches the goal.",
   "survives": true,
   "reason": "Anchor verbatim at line 120. The ranking claim is exact: `prio()` sorts crit/high/med/low then `-times_deferred`, and `top` is sliced to 10. L1 intact — the truncated top-10 is explicitly not treated as the open set, and the file read stays as the fallback. Accepted essentially as written (one clause moved inline for readability). Leverage DOWNGRADED from high: this rides the snapshot already fetched at Step 1, so it adds no call and saves only a small read.",
   "leverage": "medium",
   "current_excerpt": "3. Read `.kdbp/PENDING.md` — surface any open items related to the goal (show max 5).",
   "sweep_rationale": "The snapshot's top rows are ranked by priority then Times Deferred, not by relatedness to the goal, so it is a first-look rather than a replacement — the file read stays as the fallback and the cap is named (L1: a truncated top-10 is not the open set)."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 130,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "   - Key files likely affected — name them from `mcp__gabe-map__find` (the map's index of entities, endpoints, models, schemas, functions and screens) and `mcp__gabe-map__entity_context` before reaching for Glob/Grep, and where the phase touches code that already exists, `mcp__gabe-map__blast_radius` on those files reads what else they reach; all three are FLOORS feeding Dependencies and Risks — a file the map does not name may still be affected, so Glob/Grep close the list and the phase's Scope is never the map's answer",
   "survives": true,
   "reason": "Anchor verbatim at line 130. `t_find` searches entities/endpoints/models/schemas/functions/screens/FE pieces by name and doc; `t_blast_radius` accepts an explicit `files` array so it works at plan time with no diff. CORRECTED for L1, which the sweep protected only for blast_radius: this bullet feeds the phase's Scope, and the 0.560-vs-0.900 map-as-scope recall trap is precisely a plan-time scoping error — so the floor caveat now covers find/entity_context too, and 'the phase's Scope is never the map's answer' is stated outright rather than left as an aside about one of the three tools.",
   "leverage": "high",
   "current_excerpt": "   - Key files likely affected",
   "sweep_rationale": "Plan time is where the beat guesses at files it has never opened; find/entity_context answer 'where does this live' from the committed map in one call. blast_radius is explicitly labelled a floor in its own contract, and naming it a floor here keeps L1 intact — the measured map-as-scope recall trap (0.560 vs 0.900) is precisely a scoping error made at plan time."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 454,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "- **References:** <existing modules/patterns/docs to reuse — e.g. src/components/ListView, docs/api-conventions.md; found with `mcp__gabe-map__find` / `mcp__gabe-map__entity_context` first (E4), Grep second>",
   "survives": true,
   "reason": "Anchor verbatim at line 454. The whole bullet sits inside `<…>` author-facing placeholder guidance replaced at write time, so no MCP tool name persists into a project's written PLAN.md. 'What already exists that I should reuse' is `find`'s question by name AND doc text — a true match for E4 REUSE FIRST. L1 intact: Grep stays second, so a name the map lacks is still findable. Accepted as written.",
   "leverage": "medium",
   "current_excerpt": "- **References:** <existing modules/patterns/docs to reuse — e.g. src/components/ListView, docs/api-conventions.md>",
   "sweep_rationale": "E4 REUSE FIRST is the contract this bullet serves, and 'what already exists that I should reuse' is find's question by name AND by doc text. Grep stays second so a name the map lacks is still findable (L1)."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 759,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- **`status`**: read the state from `mcp__gabe-kdbp__kdbp_snapshot` (`plan{status, current_phase, goal, phases[{cells}]}` — completed vs remaining IS the cells read) plus `.kdbp/PLAN.md` for `Last Updated` and the HTML artifact path; fall back to PLAN.md alone when the snapshot reports no PLAN.json mirror or the server is unregistered. Show current state:",
   "survives": true,
   "reason": "Anchor verbatim at line 759. The output block below it (Goal / Phase current-of-total / Completed / Remaining / Last Updated / HTML Artifact) maps cleanly onto the snapshot plus the two file-only fields the sweep correctly kept. CORRECTED with the same mirror caveat as edit 11: `goal`, `status` and `current_phase` are PLAN.json-derived and go null on an unmirrored plan, which would render a blank status report rather than an honest one.",
   "leverage": "medium",
   "current_excerpt": "- **`status`**: Read `.kdbp/PLAN.md`, show current state:",
   "sweep_rationale": "`/gabe-plan status` is a pure state question and the snapshot is built for it; the file read stays only for the two fields the snapshot does not carry."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 453,
   "skill": "gabe-plan",
   "klass": "replace",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "Proposed by /gabe-plan from Scope × center.config code globs (`mcp__gabe-map__owner_of` performs that join), operator-confirmed.",
   "survives": true,
   "reason": "Anchor verbatim inside line 453. Pairs correctly with edit 8 — apply both or neither so the term stays single. CORRECTED: the sweep DELETED 'Scope × center.config code globs' and left only the tool name. This sentence is author-facing guidance in the PLAN.md template describing WHERE a slug came from; replacing the derivation with a tool name tells a human reader less, and would leave the template meaningless on a project whose server is unregistered. Both kept — the join is what it is, the tool is how it is performed. Leverage DOWNGRADED from medium to low: it is provenance prose with no operational effect.",
   "leverage": "low",
   "current_excerpt": "Proposed by /gabe-plan from Scope × center.config code globs, operator-confirmed.",
   "sweep_rationale": "The PLAN.md template's own description of where the slug came from should name the tool, so a reader (and the next session) sees the derivation that Step 3 now performs. Pairs with the Step 3 edit at line 134 — apply both or neither, so the term stays single."
  },
  {
   "file": "skills/gabe-plan/SKILL.md",
   "line": 29,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "   - **Step 1** — if an active plan exists (`mcp__gabe-kdbp__kdbp_snapshot` is the cheap first look at plan status, current phase and the phase-table cells; PLAN.md stays the authority, and the tool is honest-empty without `.kdbp/`), offer complete/defer/cancel/continue/replace; `continue` stops here.",
   "survives": true,
   "reason": "Anchor verbatim at line 29; SKILL.md is 53 lines, far under the 200-line lean-core cap, so the clause fits. The argument holds — the core is what a session reads before the spec, and gabe-red's core already names who_calls and ledger_row_preview inline, so this matches house practice. CORRECTED with the PLAN.md-authority clause so the lean core cannot contradict the spec's Step 1 (edit 11). Leverage DOWNGRADED from medium to low: it is a pointer duplicating edit 11's substantive change in one file, not an independent behavior change.",
   "leverage": "low",
   "current_excerpt": "   - **Step 1** — if an active plan exists, offer complete/defer/cancel/continue/replace; `continue` stops here.",
   "sweep_rationale": "The lean core is what a session reads before the spec; naming the snapshot in the first state-reading step is what makes the beat reach for it at all. Mirrors how gabe-red's core already names who_calls and ledger_row_preview inline."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 840,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- Row-state consistency: any row < Current Phase N with a non-✅ cell → `⚠ INCOMPLETE PRIOR PHASES: [<phase>: <columns>]` (always print, never block) — `mcp__gabe-kdbp__kdbp_snapshot`'s `plan.phases[].cells` is that same read without re-opening the file.",
   "survives": true,
   "reason": "Anchor verbatim at line 840. The claim is exactly right and stronger than the sweep realised: `plan.phases[].cells` comes from `plan_rows(PLAN.md)` — the PLAN.md table itself, not the JSON mirror — so it is literally the same read with no mirror-staleness caveat needed. Additive, keeps the original check, never blocks. Accepted as written, and the sweep's own 'low' rating is honest.",
   "leverage": "low",
   "current_excerpt": "- Row-state consistency: any row < Current Phase N with a non-✅ cell → `⚠ INCOMPLETE PRIOR PHASES: [<phase>: <columns>]` (always print, never block).",
   "sweep_rationale": "The check is a scan of the phase table's cells, which the snapshot already returns per row; low leverage on its own (it rides the Step 1 read), listed for completeness so the two reads stay consistent if Step 1 is edited."
  },
  {
   "file": "skills/gabe-plan/references/plan-spec.md",
   "line": 77,
   "skill": "gabe-plan",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "populate CRUD from the entity list in `SCOPE.md` (on a command-center project, `mcp__gabe-map__entity_context` with no slug returns the registered entity list; otherwise the project's data model)",
   "survives": true,
   "reason": "Anchor verbatim inside line 77. Confirmed in `t_entity_context`: an empty or 'list' slug returns `_entity_list(center)`. L2 handled — the center precondition is named and SCOPE.md/the data model remain the path elsewhere. Mockup-preset only, so the 'low' rating is honest. Accepted as written.",
   "leverage": "low",
   "current_excerpt": "populate CRUD from the entity list in `SCOPE.md` (or the project's data model)",
   "sweep_rationale": "'the project's data model' is a hand hunt that entity_context answers directly on any centered project. Mockup-preset only, so low reach."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 29,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "3. Bind `maturity` from `mcp__gabe-kdbp__phase_context` (`behavior.maturity`) — one call that also carries this phase's row states, its `Cases:`/`Reach:` records, the `## Verify Commands` binding Step 2.0 needs, and the open PENDING rows in the phase's scope. No `.kdbp/`, no active phase (the tool then returns only a `no active plan` warning and NO `behavior` block), or the tool absent → read `.kdbp/BEHAVIOR.md` directly. Missing either way defaults to `mvp`",
   "survives": true,
   "reason": "Anchor verbatim at line 29. `t_phase_context` really does return `behavior = behavior_facts(beh) | {verify_commands}` and `behavior_facts` regexes `Maturity:` (kdbp_tools.py:247, 331) — the tool answers the question. CORRECTED: the proposal's fallback only covered 'no .kdbp/', but phase_context returns EARLY with just a `no active plan` warning and no `behavior` key when there is no current_phase and no phase table (kdbp_tools.py:315-318) — a .kdbp project between plans would have lost its maturity read. Fallback broadened; L2 honest-empty now holds on every path.",
   "leverage": "high",
   "current_excerpt": "3. Read `.kdbp/BEHAVIOR.md` for `maturity` field (defaults to `mvp` if missing)",
   "sweep_rationale": "`maturity` drives the whole Maturity-Driven Check Selection table, and the gate re-reads BEHAVIOR.md by hand for one field. `phase_context` returns `behavior{maturity?, mode?, verify_commands}` in the same answer that seeds Step 2.0 — so one preflight call replaces three separate file reads (BEHAVIOR maturity, BEHAVIOR verify block, phase record). L2 preserved: the tool is honest-empty without `.kdbp/` and the direct read stays."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 33,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "If `.kdbp/PLAN.md` exists and contains `status: active`:\n- Ask `mcp__gabe-kdbp__kdbp_snapshot` — its `plan{status, current_phase, goal, phases[{id, name, cells}]}` block carries this info line's content, with the table's columns resolved BY NAME rather than assumed. `status`, `current_phase` and `goal` come from the PLAN.json mirror: when `plan.mirror` reports none — or there is no `.kdbp/`, or the tool is absent — read the `## Current Phase` section directly",
   "survives": true,
   "reason": "Anchor verbatim at lines 33-34. `t_kdbp_snapshot` returns exactly that `plan` block (kdbp_tools.py:277-282) and `plan_rows` resolves id/name/cells by header name. CORRECTED on two counts: (a) `status`/`current_phase`/`goal` are read from PLAN.json ONLY (`pj = plan_json(kd)`), so on a project with no PLAN.json mirror they come back null while the PLAN.md frontmatter says active — the proposal's fallback fired only on 'no .kdbp/', which would have let a null read stand as 'no active plan' (an L1-shaped absence claim); the fallback now also fires on `plan.mirror`. (b) klass downgraded replace→first-look: the direct read is not removable, it is the mirror-absent path. Leverage downgraded to medium — Step 1b is explicitly informational, zero-cost, non-blocking.",
   "leverage": "medium",
   "current_excerpt": "If `.kdbp/PLAN.md` exists and contains `status: active`:\n- Read the `## Current Phase` section",
   "sweep_rationale": "Step 1b is pure state-reading: goal + phase number + phase name out of PLAN.md. `kdbp_snapshot` returns exactly those three fields under ≤1k tokens and survives the twins' divergent PLAN table shapes, which a hand-parse of `## Current Phase` does not. The same answer is reused at Step 6.7's tick preconditions and by the Phase/Task footer rule at line 685."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 50,
   "skill": "gabe-commit",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "**Step 2.0 — Resolve commands (never guess).** `mcp__gabe-kdbp__verify_commands` resolves this binding: `source: a: …` when BEHAVIOR's `## Verify Commands` binds (`commands{lint, types, tests, results_out}`), else `source: b: …` candidates read from package.json scripts / pyproject / Makefile with the `offer` that writes the binding back, else `source: none: …`. It never runs a command and never invents a reporter flag (`probed: false` always) — the gate runs them. When Step 1 already took `phase_context`, that answer's `behavior.verify_commands` is the same rung-(a) binding and this call is redundant. The order is unchanged:\n\n- (a) `## Verify Commands` section of `.kdbp/BEHAVIOR.md` — the tool's `source: a:`\n- (b) package.json scripts / pyproject / Makefile / CI job definitions — the tool's `source: b:`, plus any CI job definition it does not read\n- (c) the per-language fallbacks below, only if (a)-(b) yield nothing, or when the project has no `.kdbp/` and the tool answers honest-empty",
   "survives": true,
   "reason": "Anchor verbatim at lines 50-54. `t_verify_commands` is a true match: rung (a) from `verify_commands_section`, rung (b) from package.json/pyproject/Makefile, `probed: False` hardcoded with the 'RESOLVED, never run' note (kdbp_tools.py:431-477) — the tool's own law is this step's law. CORRECTED: the real `source` values are the strings `\"a: BEHAVIOR.md ## Verify Commands\"` / `\"b: package.json / pyproject / Makefile (candidates — not yet bound)\"` / `\"none: …\"`, not bare `a`/`b`/`none`; the shorthand would have had an agent look for a field that never appears. Rung (c) and the non-evidence rule are untouched, so a no-`.kdbp/` project still resolves (L2). LEVERAGE DOWNGRADED suite-wide→high: gate-spec.md is gabe-commit's own reference — a repo-wide grep finds exactly one non-gabe-commit pointer at it (docs/design/map-delta-loop/README.md); it is not a many-skills file.",
   "leverage": "high",
   "current_excerpt": "**Step 2.0 — Resolve commands (never guess).** Bind lint/types/tests in order:\n\n- (a) `## Verify Commands` section of `.kdbp/BEHAVIOR.md`\n- (b) package.json scripts / pyproject / Makefile / CI job definitions\n- (c) the per-language fallbacks below, only if (a)-(b) yield nothing",
   "sweep_rationale": "The kdbp spec names this step by name as one of the four hand-reads the server exists to replace (\"the gate's Step 2.0 binding\"). The tool's own law — never run, never a guessed flag — is the same law this step states, so the ladder is preserved verbatim and only its resolver changes. (c) and the non-evidence rule below are untouched, so a no-`.kdbp` project still resolves commands (L2)."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 286,
   "skill": "gabe-commit",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "3. Log to `.kdbp/LEDGER.md` — one thin-index row. Compose it with `mcp__gabe-kdbp__ledger_row_preview` (`entry: COMMIT`, `theme` = the commit subject ≤8 words, `commits` = the short hash, `gates` = the Gates cell below, copied verbatim; it returns the row in the FILE's own column order) and write it with the harness Write/Edit — never through a tool — so the D7 hooks see the write. No `.kdbp/`, or no resolvable LEDGER header (`header_found: false`), → the preview says so and the canonical template stands:",
   "survives": true,
   "reason": "Anchor verbatim at line 286. `COMMIT` is in the tool's accepted entry vocabulary (kdbp_tools.py:520), it renders into the file's own header order with a canonical-5 fallback, and its `gates_note` states the Gates cell is copied verbatim — an exact match for this step. L3 held: the tool returns `writes: nothing` and the edit keeps the harness Write. L2 held: the fenced canonical template below is untouched. CORRECTED only for voice — realigned to the identical sentence already standing in red-spec.md:234-235 (`entry: RED`), so the two LEDGER writers read the same, and named the `header_found` field that reports the no-header case.",
   "leverage": "high",
   "current_excerpt": "3. Log to `.kdbp/LEDGER.md` — one thin-index row:",
   "sweep_rationale": "The template below is the 5-column canon; twins' LEDGER headers diverge, and the preview renders into the file's own order with the Gates cell copied verbatim. This is the house pattern already set in red-spec § The LEDGER row (`entry: RED`) — commit is the same edit one entry-tag over. L3 preserved: preview composes, the harness Write is what the lie-block hooks watch."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 293,
   "skill": "gabe-commit",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "   - Compose each new row with `mcp__gabe-kdbp__pending_row_preview` (`flag{description, dimension: the class tag, source: gabe-commit, file, priority, impact, scale}`) — it returns the row in the file's OWN column order, mints the next id over live **and** archive rows, prefixes the Finding cell with the class tag, and anchors `Verified: @<sha> <date>` itself (replace it with `-` when the item was not derived against a checked tree this session — CHECK 6 warns on prose). Set the `Status` cell to `open`: the preview leaves it blank. Then write the row with the harness Write/Edit, never through a tool, so the D7 hooks see the write. Its `recurring_candidates` (open rows on the same File sharing ≥3 words) is what the next bullet's \"already exists\" test reads. No `.kdbp/` → the preview says so; add the row by hand with date, source=`gabe-commit`, finding, file, scale (from BEHAVIOR.md maturity), priority, impact, times_deferred=1, status=open, verified=`@<sha> <date>` when the item was derived against a checked tree this session, else `-`",
   "survives": true,
   "reason": "Anchor verbatim at line 293. The tool genuinely mints `next_id` over live rows PLUS `archive/PENDING-resolved*.md`, emits the file's own `columns`, and anchors Verified (kdbp_tools.py:480-513). TWO CORRECTIONS, both defect-grade as proposed: (1) `vals[\"Status\"] = \"\"` — the preview emits a BLANK Status cell, so the proposal's tool path silently dropped the spec's `status=open`, and downstream readers that match `Status = open` literally (push-spec 7.5b re-surface) would never see the row again; (2) the class-tag prefix required by the bullet at line 296 is produced only when `dimension` is passed (`\"[%s] %s\"`), which the proposed flag omitted. Times Deferred `1` matches this lane's canon, so it is left alone. L3 held: harness Write kept explicit.",
   "leverage": "high",
   "current_excerpt": "   - Add new row with date, source=`gabe-commit`, finding, file, scale (from BEHAVIOR.md maturity), priority, impact, times_deferred=1, status=open, verified=`@<sha> <date>` when the item was derived against a checked tree this session, else `-` (CHECK 6 warns on prose)",
   "sweep_rationale": "Every field this bullet lists is what the preview returns, and two of them are things a hand-write gets wrong: the next P-id must be minted over the archive too (a live-only max collides with a resolved row), and the column order is the FILE's, not the canon's. `Verified: @<sha> <date>` is the anchored form CHECK 6 warns about when it is prose — the preview writes it correctly by construction."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 534,
   "skill": "gabe-commit",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "| | `defer` | Compose the row with `mcp__gabe-kdbp__pending_row_preview` (`flag{description: \"Create {target}\", source: docs-audit, file: {target}, priority: {priority}, impact: high, scale: large}`) — file's own column order, next id minted over live + archive — then correct the cells it cannot know (`Status: open`, `Times Deferred: 0`, `Verified: -`) and append it with the harness Write/Edit. No `.kdbp/` binding → the canonical shape `P[N] \\| {today} \\| docs-audit \\| Create {target} \\| {target} \\| large \\| {priority} \\| high \\| 0 \\| open \\| -` | No |",
   "survives": true,
   "reason": "Anchor verbatim at line 534, and the rationale's inheritance claim checks out — every other `defer` in the Step A7 table says 'as above' (lines 536, 538, 540, 547, 549, 551), so this cell is the shape they all take. CORRECTED: the preview writes a blank Status, `Times Deferred: 1` and an anchored `Verified`, all three of which contradict this lane's canonical literal (`0 | open | -`); left uncorrected the edit would have silently changed the row's meaning while claiming to preserve it. L2 held: the canonical literal survives as the no-binding path. Leverage kept medium — the fix propagates across the A7 table but docs-audit is explicit-invocation only.",
   "leverage": "medium",
   "current_excerpt": "| | `defer` | Append row to PENDING.md: `P[N] \\| {today} \\| docs-audit \\| Create {target} \\| {target} \\| large \\| {priority} \\| high \\| 0 \\| open \\| -` | No |",
   "sweep_rationale": "Same hardcoded 11-column literal as Step 6.4, in the docs-audit lane. Every other `defer` handler in Step A7 says \"as above\" and inherits this row shape, so fixing it here fixes the whole table."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 548,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-map__outline",
   "proposed_text": "| Diagram missing (non-well doc, A2 step 4) | `add-diagram` | Read `section` content + source files matching `pattern` — where the project has a command center, ask `mcp__gabe-map__outline` per matched file for its definitions, spans and signatures, and `mcp__gabe-map__entity_context` when the target section is an entity's `Data Model` (models) or `API Endpoints` (endpoints), before falling back to `git log --oneline -10 -- {pattern-glob}` + file reads for what the map does not carry.",
   "survives": true,
   "reason": "Anchor verbatim at line 548 (substring of the cell; the rest of the handler is untouched). `t_outline` really does return per-file `definitions` with span/kind/name/signature plus `models_defined`/`models_referenced`/`tests_reaching` (tools_wave2.py:104-142), and `entity_context` carries an entity's endpoints and models — both answer the synthesis input this handler needs. L1 held explicitly: the git-log + file-read path stays as the fallback, so a definition the map lacks is never read as absent. LEVERAGE DOWNGRADED medium→low: docs-audit is an explicitly-invoked subcommand ('NOT automatically chained', gate-spec.md:22), so this fires rarely.",
   "leverage": "low",
   "current_excerpt": "| Diagram missing (non-well doc, A2 step 4) | `add-diagram` | Read `section` content + source files matching `pattern` (via `git log --oneline -10 -- {pattern-glob}` + file reads).",
   "sweep_rationale": "Synthesizing an erDiagram for `## Data Model` or a sequenceDiagram for `## API Endpoints` means reading source files to recover models and endpoints — which is precisely what `entity_context` and `outline` already hold, homed by entity. The file reads stay as the fallback (L1: the map is a floor, so a definition it does not carry is not absent)."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 664,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "- `scope` = topmost touched module or well (e.g., `triage`, `pipeline`, `docs`) — where the project has a command center, ask `mcp__gabe-map__owner_of` with the staged paths first and use the owning entity's slug, so a commit scope reads as the same name the center, the plan and the ledger already use; a path the map does not claim falls back to the topmost touched module",
   "survives": true,
   "reason": "Anchor verbatim at line 664. `t_owner_of` takes `paths` (or `path`) and returns per-path `owners[{entity, layer, lines}]` plus a `config_glob_owners` second opinion, and for an unowned path it returns `owned: false` with the note 'unowned by the map — the map is BLIND here' (tools.py:585-620) — it answers this question and says when it cannot. L1 held: unclaimed paths keep the existing heuristic, and the absent-center case degrades through `_absent` to the same heuristic (L2). Fires on every generated commit message, so 'high' is honest.",
   "leverage": "high",
   "current_excerpt": "- `scope` = topmost touched module or well (e.g., `triage`, `pipeline`, `docs`)",
   "sweep_rationale": "This rule fires on EVERY generated commit message, and it is currently eyeballed off the diff — the classic hand-work `owner_of` answers directly. Aligning the scope token with the entity slug is what makes `map_diff`, `blast_radius` and the center's per-entity views agree with git history. L1 held: the map is a floor, unclaimed paths keep the existing heuristic."
  },
  {
   "file": "skills/gabe-commit/SKILL.md",
   "line": 27,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "   - **Step 1 / 1b** — validate git context; surface the active plan from `mcp__gabe-kdbp__kdbp_snapshot` and the maturity from `mcp__gabe-kdbp__phase_context`, falling back to the spec's direct `.kdbp/` reads when either is honest-empty (context only).",
   "survives": true,
   "reason": "Anchor verbatim at line 27. The visibility argument holds — SKILL.md is the always-loaded core and gate-spec is read on demand, so a tool named only in the spec is invisible until the spec is opened. CORRECTED: the proposal named two tools with no fallback in the file the agent reads FIRST, which is where an L2 violation does the most damage (a summary line that reads as 'the plan comes from the tool' on a project with no `.kdbp/` or no PLAN.json mirror); the fallback clause is now in the core, matching the corrected spec lines 29 and 33.",
   "leverage": "medium",
   "current_excerpt": "   - **Step 1 / 1b** — validate git context; surface the active plan (context only).",
   "sweep_rationale": "SKILL.md is the always-loaded core; the spec is read on demand. If the summary still reads as \"surface the active plan\" with no tool named, the tool is invisible until the spec is opened. Mirrors the gate-spec Step 1/1b findings."
  },
  {
   "file": "skills/gabe-commit/SKILL.md",
   "line": 28,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "CHECK 3 Tests (commands resolved via Step 2.0 — `mcp__gabe-kdbp__verify_commands` resolves rungs (a)/(b); never guessed)",
   "survives": true,
   "reason": "Anchor verbatim inside line 28. Same visibility case as above, and it carries the 'never guessed' law beside the tool that enforces it. CORRECTED: 'is that binding' overclaims — the tool covers rungs (a) and (b) only; rung (c)'s per-language fallbacks live in the spec and are the sole path on a project with no `.kdbp/` (L2). Saying it 'is' Step 2.0 would license skipping (c).",
   "leverage": "medium",
   "current_excerpt": "CHECK 3 Tests (commands resolved via Step 2.0, never guessed)",
   "sweep_rationale": "Same visibility argument: the \"never guessed\" law is stated in the core but the tool that enforces it is named only in the spec. Mirrors the Step 2.0 replace."
  },
  {
   "file": "skills/gabe-commit/SKILL.md",
   "line": 32,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "   - **Step 6** — commit + record to LEDGER.md (row composed with `mcp__gabe-kdbp__ledger_row_preview`, deferrals with `pending_row_preview` — PREVIEWS: the write stays a harness Write/Edit so the D7 hooks see it); maturity-driven check selection governs which checks apply.",
   "survives": true,
   "reason": "Anchor verbatim at line 32. Mirrors the corrected Step 6.3/6.4 edits and puts the L3 law — preview composes, harness writes, hooks watch Write/Edit not mcp__* — in the always-loaded core, which is exactly where it is most likely to be obeyed. Both tools return `preview: true, writes: nothing`, so the claim is accurate. No correction needed.",
   "leverage": "medium",
   "current_excerpt": "   - **Step 6** — commit + record to LEDGER.md; maturity-driven check selection governs which checks apply.",
   "sweep_rationale": "Mirrors the Step 6.3/6.4 replaces and carries the L3 law into the always-loaded core, where it is most likely to be obeyed."
  },
  {
   "file": "skills/gabe-commit/references/simplify-pass.md",
   "line": 22,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "2. the phase touched a file recorded as a known monolith — `mcp__gabe-kdbp__kdbp_snapshot`'s `pending.top` surfaces the standing rows and their File cells without opening the file (capped at 10 by priority, so a miss there is not an absence; RULES.md / PENDING.md / a health or\n   code-audit finding remain the record),",
   "survives": true,
   "reason": "Anchor verbatim at lines 22-23. `pending.top` really carries `{id, finding, file, priority, times_deferred}` for open rows sorted by priority (kdbp_tools.py:293-296), so it answers the trigger cheaply. CORRECTED: `top` is hard-capped at 10 (`op[:10]`) and the snapshot does not read RULES.md at all — as proposed, an agent could read a capped list's silence as 'not a monolith', which is the L1 failure in miniature; the cap and its non-absence are now stated. Leverage low is honest — this is one OFFER trigger, never a gate.",
   "leverage": "low",
   "current_excerpt": "2. the phase touched a file recorded as a known monolith (RULES.md / PENDING.md / a health or\n   code-audit finding),",
   "sweep_rationale": "A trigger test that currently means \"go read PENDING.md and RULES.md\". The snapshot's top-10-by-priority view is enough for a TRIGGER (unlike CHECK 6, which must be exhaustive because it prints a count) — the record itself is unchanged."
  },
  {
   "file": "skills/gabe-commit/references/simplify-pass.md",
   "line": 30,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "| reuse | does an existing component/helper/module already do this? (the re-implemented-helper signature) — ask `mcp__gabe-map__find` by name/doc first, then `mcp__gabe-map__who_calls` on a candidate it returns to see whether that helper is already the shared one; the map is a floor, so `grep -rn` still settles absence (E1) |",
   "survives": true,
   "reason": "Anchor verbatim at line 30. `t_find` searches entities, endpoints, models, schemas, defines, functions, fe pieces and screens by name and doc and self-labels its own floor ('a name the map lacks is a Grep question', tools_wave2.py:99) — a genuine match for the reuse lens, which currently names no method at all. CORRECTED: `who_calls` on 'the new symbol' is backwards — a symbol you are about to author is absent from the map by construction, so that arm would return near-nothing and invite exactly the absence-as-proof reading L1 forbids; repointed at a candidate `find` returned, which is the question ('is this helper already shared?') `who_calls` actually answers. L1 sentence kept inline. LEVERAGE DOWNGRADED high→medium: the simplify pass is evidence-triggered and offered, not run on every commit.",
   "leverage": "medium",
   "current_excerpt": "| reuse | does an existing component/helper/module already do this? (the re-implemented-helper signature) |",
   "sweep_rationale": "This is the E4 REUSE FIRST lane with no method attached at all — the lens asks the exact question `find` exists to answer (\"X by name/doc across entities, endpoints, models, schemas, functions, screens, FE pieces\"), and `who_calls` is the two-arm reach that also emits the map delta when grep beats the index. Highest-value single line in this file. L1 stated inline so the tool answer can never stand as \"nothing exists\"."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 17,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "4. Read `.kdbp/PUSH.md`. If it exists, skip to Step 2.5. If not, continue to Step 2. No tool serves PUSH.md — it is push's own config. Open `mcp__gabe-kdbp__kdbp_snapshot` once here for the state the rest of this run otherwise re-derives by hand: `git{branch, ahead, behind, dirty}` for Step 3's pre-flight, `plan{status, current_phase, phases[{cells}]}` for Step 10's orientation (`status`/`current_phase` come from the PLAN.json mirror — `plan.mirror` names its absence), and `pending{columns, open, top}` for Step 7.5b's classifier re-surface. It ORIENTS; the git commands in Step 3 still decide — a safety abort reads the working tree, never a snapshot.",
   "survives": true,
   "reason": "Anchor verbatim at line 17. Every field named exists: `git{branch, ahead, behind, dirty, recent_commits}`, `plan{...}`, `pending{open, closed, columns, top}` (kdbp_tools.py:258-300). The orient-vs-decide split is the load-bearing sentence and the proposal already carried it — without it this would be an L1 violation (a snapshot standing in for the working-tree read a safety abort depends on). CORRECTED twice: 'Step 10's tick preconditions' → 'orientation' (the preconditions are the shared helper's own by-name check, plan-spec.md:778-782 — see the killed line-510 finding), and the PLAN.json-mirror caveat added so a null `current_phase` is not read as 'no active plan'.",
   "leverage": "high",
   "current_excerpt": "4. Read `.kdbp/PUSH.md`. If it exists, skip to Step 2.5. If not, continue to Step 2.",
   "sweep_rationale": "Push re-derives the same three facts at four separate steps (3.4, 3.5, 7.5b pre-step, 10). One ≤1k-token call at Step 1 seeds all of them, and its header-resolved `pending.columns` is the only thing that tells 7.5b whether this project's PENDING even HAS a `Source` column. The orient-vs-decide split is stated inline so the finding cannot be read as licence to skip the git checks (L1)."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 270,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "AND the project has a command center — ask `mcp__gabe-map__map_status`, which answers presence AND freshness in one call (`docs/site/center/center.config.json` is the file it reads) — print:",
   "survives": true,
   "reason": "Anchor verbatim inside line 270 (the `release:` payload after it is untouched). Verified in source: `find_center` walks up for exactly `docs/site/center/center.config.json` (mapquery.py:93-102) and `_base` stamps `map@<head> · <freshness>` on every answer, so the tool answers presence and staleness together — the staleness is what makes the `/gabe-cc-update release` pointer worth printing. L2 held: no center → `_absent` with a reason, and this step already prints nothing. Leverage low is honest — one non-blocking pointer line.",
   "leverage": "low",
   "current_excerpt": "AND the project has a command center (`docs/site/center/center.config.json`), print:",
   "sweep_rationale": "The existence test is cheap, but the pointer it prints is a release instruction: `map_status` additionally reports how stale the committed map is against HEAD, which is exactly what makes `/gabe-cc-update release` owed. L2 held: no center → the tool says so, and the step already prints nothing."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 326,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Ask `mcp__gabe-kdbp__kdbp_snapshot` first — `pending{columns, open, top}` names the file's OWN headers and the open count this pass reconciles against. Then read `.kdbp/PENDING.md` for the full row set — `top` caps at 10 and does not carry the Source cell — and take every row where the `Source` column (a twin may head it `Gate`) = `classifier` AND the `Status` column = `open`:",
   "survives": true,
   "reason": "Anchor verbatim at line 326. `pending_rows` returns the file's real header list and the snapshot surfaces it as `pending.columns` (kdbp_tools.py:166-186, 293-296), so the column question is genuinely answered. The full-file read is correctly kept — `top` is capped at 10 AND its entries omit `source`, so the tool cannot do the matching (L1). CORRECTED: the proposal claimed a missing `Source` column means 'nothing to re-surface', but `_col(hdr, \"Source\", \"Gate\")` shows a twin may head that column `Gate` and still carry classifier rows — that phrasing would have skipped a live re-surface pass on a real twin.",
   "leverage": "medium",
   "current_excerpt": "Read `.kdbp/PENDING.md`. For every row where `Source` column = `classifier` AND `Status` column = `open`:",
   "sweep_rationale": "The step assumes a column layout the kdbp spec says diverges across twins (\"| # | Gate | Finding |\" vs the 11-column canon). `pending.columns` is header-resolved and settles that before any matching. The full-file read stays because `top` caps at 10 and this pass must see every classifier row (L1)."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 391,
   "skill": "gabe-push",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "| `defer` | Compose the row with `mcp__gabe-kdbp__pending_row_preview` (`flag{description: [title], source: classifier, file: \"-\", priority: medium, impact: low, scale: [maturity]}`) — file's own column order, next id minted over live + archive — then correct the cells it cannot know (`Status: open`, `Times Deferred: 0`, `Verified: -`) and append it with the harness Write/Edit, never through a tool, so the D7 hooks see the write; no `.kdbp/` binding → the canonical shape `\\| P[N] \\| today \\| classifier \\| [title] \\| - \\| [maturity] \\| medium \\| low \\| 0 \\| open \\| - \\|`. Source column = `classifier`.",
   "survives": true,
   "reason": "Anchor verbatim inside line 391; the rest of the cell (title-verbatim-for-dedup, increment-instead-of-duplicate) survives untouched, and correctly so — no `dimension` is passed, so the Finding cell stays the bare title the re-surface dedup matches on. CORRECTED: as proposed this was self-defeating — the preview leaves `Status` BLANK, and the pre-step 30 lines earlier re-surfaces only rows where `Status = open`, so every deferred candidate would have vanished from the loop the handler exists to feed; `Times Deferred: 1` and an anchored `Verified` also contradict this row's canonical `0 | open | -`. Also set `file: \"-\"` to match the canon, which as a side effect makes `recurring_candidates` usable here (it is empty when `flag.file` is falsy — the rationale's claim about it was false as written). LEVERAGE DOWNGRADED high→medium: 7.5b is trigger-gated and skippable via `push_operational_classifier: never`.",
   "leverage": "medium",
   "current_excerpt": "| `defer` | Append to PENDING.md: `\\| P[N] \\| today \\| classifier \\| [title] \\| - \\| [maturity] \\| medium \\| low \\| 0 \\| open \\| - \\|`. Source column = `classifier`.",
   "sweep_rationale": "Same hardcoded 11-column literal as gabe-commit's, and the same two failure modes it fixes: a next-id minted without the archive collides, and the canon's order is not every twin's order. The preview's `recurring_candidates` also formalises this handler's own \"if an open classifier row already exists with matching title, increment `Times Deferred`\" rule, which the rest of the cell keeps."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 410,
   "skill": "gabe-push",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "Compose the row with `mcp__gabe-kdbp__ledger_row_preview` (`entry: PUSH`, `theme` = `[env] ← [branch] @ [short sha]`, `commits` = the short sha, `gates` = the results cell below, copied verbatim; it returns the row in the FILE's own column order) and append it with the harness Write/Edit — never through a tool — so the D7 hooks see the write. The thin-index house format it renders (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\") stands when the tool is honest-empty:",
   "survives": true,
   "reason": "Anchor verbatim at line 410. `PUSH` is in the tool's entry vocabulary (kdbp_tools.py:520), it resolves columns against the file's own LEDGER header with a canonical-5 fallback and copies the Gates cell verbatim — an exact match. The precedent claim checks out: red-spec.md:234-235 already composes with this tool at `entry: RED`. The inheritance claim checks out too: line 547 says branch cleanup logs 'the same PUSH row template as Step 8', so it picks this up for free. L2/L3 held — the fenced template below survives and the write stays a harness Write. Corrected only for voice, to the red-spec sentence.",
   "leverage": "high",
   "current_excerpt": "Append one row to `.kdbp/LEDGER.md` per the thin-index house format (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\"):",
   "sweep_rationale": "Directly parallel to red-spec § The LEDGER row, which already composes with this tool at `entry: RED`; `PUSH` is in the same entry vocabulary. Step 10.5's branch-cleanup row (line 547, \"the same PUSH row template as Step 8\") inherits this fix for free."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 592,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "1. Take phase ID N from `mcp__gabe-kdbp__kdbp_snapshot` (`plan.current_phase`, read from the PLAN.json mirror); no `.kdbp/`, or `plan.mirror` reporting no mirror → read PLAN.md `## Current Phase`. (Step 2's SCOPE.md read has no tool — no server reads SCOPE.md.)",
   "survives": true,
   "reason": "Anchor verbatim at line 592. Reuses the Step 1 snapshot rather than re-reading PLAN.md, which is the honest version of this finding. The parenthetical is CONFIRMED, not asserted: grepping the kdbp server shows the only SCOPE token is the `SCOPE` LEDGER entry tag (kdbp_tools.py:520) — no tool reads SCOPE.md, so the guard against the next auditor's assumption is accurate and worth keeping. CORRECTED: fallback broadened from 'no `.kdbp/`' to also cover the missing PLAN.json mirror, since `current_phase` is null without it and a null must not stand as 'no phase' (L1/L2).",
   "leverage": "low",
   "current_excerpt": "1. Read PLAN.md `## Current Phase` → extract phase ID N.",
   "sweep_rationale": "Step 1 is the same PLAN read the Step 1 snapshot already made. The parenthetical is deliberate: it forecloses the next auditor's assumption that step 2's `SCOPE.md ## Phases` lookup has a tool — the kdbp server serves PLAN/PLAN.json/PENDING/LEDGER/BEHAVIOR/DECISIONS only."
  },
  {
   "file": "skills/gabe-push/SKILL.md",
   "line": 26,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "   - **Step 1** — validate `gh` CLI + auth + git remote; read `.kdbp/PUSH.md` if present; open `mcp__gabe-kdbp__kdbp_snapshot` once for the branch/ahead/dirty, PLAN row and open PENDING rows the later steps otherwise re-derive — orientation only, the git commands still decide.",
   "survives": true,
   "reason": "Anchor verbatim at line 26. The visibility case is real — push-spec is ~600 lines and the one-call orientation sits at line 17, so a core summary silent about it will be skipped. CORRECTED: the orient-vs-decide clause is carried into the core. Without it, the always-loaded line reads as 'Step 1 gets the state from the snapshot', which is precisely the reading that lets a stale snapshot stand in for the working-tree check a safety abort depends on (L1). Honest-empty is inherited from the spec, which the core points at.",
   "leverage": "medium",
   "current_excerpt": "   - **Step 1** — validate `gh` CLI + auth + git remote; read `.kdbp/PUSH.md` if present.",
   "sweep_rationale": "SKILL.md is always loaded and push's spec is long — if the one-call orientation is named only at push-spec line 17, it will be skipped. Mirrors that finding."
  },
  {
   "file": "skills/gabe-push/SKILL.md",
   "line": 37,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "   - **Step 8** — record to `.kdbp/LEDGER.md` (row composed with `mcp__gabe-kdbp__ledger_row_preview`, `entry: PUSH`; a PREVIEW — the write stays a harness Write/Edit so the D7 hooks see it).",
   "survives": true,
   "reason": "Anchor verbatim at line 37. Mirrors the surviving Step 8 replace and carries the L3 law — the preview writes nothing, the harness Write is what the lie-block hooks watch — into the always-loaded core. `entry: PUSH` is a valid vocabulary member. No correction needed.",
   "leverage": "medium",
   "current_excerpt": "   - **Step 8** — record to `.kdbp/LEDGER.md`.",
   "sweep_rationale": "Mirrors the Step 8 replace and carries the L3 preview-vs-write law into the always-loaded core."
  },
  {
   "file": "skills/gabe-next/SKILL.md",
   "line": 38,
   "skill": "gabe-next",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Ask `mcp__gabe-kdbp__kdbp_snapshot` first — `plan.phases[]` is this same table read header-resolved (cells keyed by column name, so an optional `Red`/`Center` column is never read off a shifted position; each cell arrives normalised as `todo` / `active` / `done` / `skipped`, NOT the raw ⬜ 🔄 ✅, and a column the plan lacks is simply absent — the always-✅ rule below still applies; capped at 40 rows with a `+N more` note). Then read `.kdbp/PLAN.md` for what it does not carry: the `## Current Phase` pointer (the snapshot takes that from the PLAN.json mirror, which is unusable on this path), `project_type`, `Types`, and any row past the cap. Extract:",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim; the header-resolution claim is real (plan_rows uses _col() by name) and the file read is correctly kept, since on the exit-2 path plan_json() returns None so current_phase is null. But the proposal omitted that every cell passes through _state() — a reader following Step 2 would test for `⬜` against `todo` and find nothing, a silent zero. Normalisation named, and the missing-column → always-✅ rule tied in. Leverage corrected: this is the degraded branch, reached only on next.mjs exit 2.",
   "leverage": "low",
   "current_excerpt": "Read `.kdbp/PLAN.md`. Extract:",
   "sweep_rationale": "This is the degraded path — hand-parsing a markdown table by column position is precisely where the header-resolved parser earns its place. Kept as first-look, not replace: on this path PLAN.json is missing, so the snapshot's `current_phase` is null and the file read is still required (L1)."
  },
  {
   "file": "skills/gabe-docs/references/disposition-contract.md",
   "line": 31,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "survives": true,
   "leverage": "medium",
   "reason": "Anchor verbatim — but at line 31, NOT 30 (line 30 is the '> ⚠ **GAP-A**' opener). Line corrected. Tool match is exact: t_owner_of returns per-file `owners`, `config_glob_owners` from entity_code_globs(center.config), `census` and the blind note 'unowned by the map — the map is BLIND here' (tools.py:585-620). L4 held — disposition.py's archmap WARN stays and the no-map clause keeps L2. ONE sentence CUT: 'Also fills the `Entities:` bullet the TACKLE arm prints' contradicts line 38, where `disposition.py --tackle` already PRINTS that bullet itself; telling a model to hand-fill a cell a deterministic script emits is the L4 direction the contract exists to prevent, and it also drags a TACKLE concern into a DEFER-arm blockquote. Leverage LOWERED from high to medium: only gabe-cc-update/SKILL.md points at this file.",
   "proposed_text": "> entity field. Pass an entity code path as `file`, or the card renders **cross-cutting** and drops\n> out of the entity's column. `disposition.py` validates against `archmap.json` and WARNs\n> (report-never) when it would. Ask `mcp__gabe-map__owner_of` for the path BEFORE composing the\n> flag — it names the owning entity, the `center.config` glob that claims it, and whether the census\n> says the map is blind there; unowned or blind means the card will land cross-cutting, so pick an\n> owned path or say so in the `Finding`. No map → the tool says so and `disposition.py`'s WARN is\n> the only check.",
   "current_excerpt": "> entity field. Pass an entity code path as `file`, or the card renders **cross-cutting** and drops\n> out of the entity's column. `disposition.py` validates against `archmap.json` and WARNs\n> (report-never) when it would.",
   "anchor_source": "re-derived by reading the file; the sweep's (file,line) did not join to the journal"
  },
  {
   "file": "skills/gabe-docs/references/disposition-contract.md",
   "line": 53,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "survives": true,
   "leverage": "medium",
   "reason": "Anchor verbatim at line 53. Every mechanical claim verified in kdbp_tools.py:480-512 — live header (`cols = hdr or canon`), id floor across .kdbp/archive/PENDING-resolved*.md, `Verified: @<sha> <date>`, recurrence by same File + >=3 shared tokens, and a `writer` field that reports whether `scripts/disposition.py` exists. L4 held: the sentence is scoped to 'until a site adopts it' so the deterministic writer keeps precedence. L3 held explicitly: persistence routed back through Write/Edit, which is what the D7 hooks watch. Leverage LOWERED to medium for the same single-consumer reason as the GAP-A edit.",
   "proposed_text": "The six model-authored PENDING sites (gabe-review/commit/push/assess/init/plan) **may adopt**\n`disposition.py --defer` to replace hand-authored rows with the deterministic writer. Until a site\nadopts it, the hand-authored row is composed by `mcp__gabe-kdbp__pending_row_preview` — this file's\nown column order, the next `P-id` (archive included), the `Verified: @<sha> <date>` stamp and the\nrecurring candidates (same `File` + overlapping `Finding`) — and then written with Write/Edit, which\nis what the D7 hooks watch; the preview writes nothing and reports whether the project even has\n`scripts/disposition.py`.",
   "current_excerpt": "The six model-authored PENDING sites (gabe-review/commit/push/assess/init/plan) **may adopt**\n`disposition.py --defer` to replace hand-authored rows with the deterministic writer.",
   "sweep_rationale": "Six named skills still hand-author these rows, which is where wrong column order, duplicate P-ids and missed recurrence come from. pending_row_preview does exactly this composition (verified in kdbp_tools: reads the live header, scans `.kdbp/archive/PENDING-resolved*.md` for the id floor, computes recurring by File + ≥3 shared tokens). The sentence is placed so it explicitly does NOT compete with the writer (L4) and explicitly routes the persistence back through the harness Write (L3)."
  },
  {
   "file": "skills/gabe-docs/references/evidence-doctrine.md",
   "line": 13,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "survives": true,
   "leverage": "high",
   "reason": "Anchor verbatim — but at line 13, NOT 12 (line 12 ends '…incentivized to'). Line corrected. phase_context does return `proof` and `proof_type` inside plan_json beside cases/scope/entities (kdbp_tools.py:321), so the claim is literal, not adjacent. The doctrine's core rule is untouched: the human still declares proof at plan time; the tool only READS the declared field, so no gate-time classification is introduced. Two-space bullet continuation matches the file. L2 carried by the closing fallback. Leverage high is honest — this doctrine is pointed at by gabe-commit, gate-spec and review-spec, and names six consumers in its own header, but it is not the file all 30 skills load.",
   "proposed_text": "classify down; the executing AI never gets to decide what deserves proof. Read the field with\n  `mcp__gabe-kdbp__phase_context` — its `plan_json` carries `proof` and `proof_type` beside `cases`,\n  `scope` and `entities` — so a gate learns what it must prove without re-reading PLAN.json; no\n  `.kdbp/` → the tool says so and PLAN.json is the read.",
   "current_excerpt": "  classify down; the executing AI never gets to decide what deserves proof.",
   "anchor_source": "re-derived by reading the file; the sweep's (file,line) did not join to the journal"
  },
  {
   "file": "skills/gabe-docs/references/evidence-doctrine.md",
   "line": 72,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "survives": true,
   "leverage": "medium",
   "reason": "Anchor verbatim at line 72. details_excerpt exists and is the phase's `### Phase` SECTION truncated at 2000 chars (kdbp_tools.py:323-325) — NOT an 'acceptance text' field, so the original wording promised a precision the server does not keep; corrected to name what it actually returns. Fallback clause added so the step survives a no-.kdbp / tool-absent run (L2). The second-arm rule itself is untouched: the evaluator is still not the author, and the brief being machine-assembled from the record strengthens rather than softens 'judged against the DECLARED criteria'.",
   "proposed_text": "criteria + the proof folder and returns PASS / NEEDS_WORK with the failing artifact named. The\nevaluator's brief is assembled from `mcp__gabe-kdbp__phase_context` — `details_excerpt` (the phase's\nown `### Phase` section, first 2000 chars), `plan_json.proof` and `plan_json.proof_type` — not from\na hand-copied summary of the plan; tool absent → PLAN.md's phase section is the read.",
   "current_excerpt": "criteria + the proof folder and returns PASS / NEEDS_WORK with the failing artifact named.",
   "sweep_rationale": "The whole point of the second arm is that the evaluator judges against the DECLARED criteria; a hand-copied brief is where the criteria quietly soften. phase_context returns `details_excerpt` (the phase's details section, 2000 chars) alongside proof/proof_type, so the brief is assembled from the record itself. Fork/read-only agents inherit the same MCP tools, so this works inside the Explore run."
  },
  {
   "file": "skills/gabe-docs/references/evidence-doctrine.md",
   "line": 28,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "survives": true,
   "leverage": "medium",
   "reason": "Anchor verbatim at line 28. The boundary it draws is the correct one and is checkable: the kdbp roster has seven tools and none of them is a capture relay, so 'capture stays a manifest read' is true, and verify_commands resolves-without-running per its own note (kdbp_tools.py:436). Naming which half of the line became a tool is exactly what prevents the wrong generalisation the proposer flags. No law touched.",
   "proposed_text": "The project manifest names the tools (`verify_commands`, `capture`); this table names the forms. `mcp__gabe-kdbp__verify_commands` relays the `verify_commands` binding (BEHAVIOR first, else manifest candidates) without running it; `capture` stays a manifest read.",
   "current_excerpt": "The project manifest names the tools (`verify_commands`, `capture`); this table names the forms.",
   "sweep_rationale": "Half of this line is now a tool answer and half is not, and saying which is which prevents the wrong generalisation (\"the tools serve the whole manifest\"). verify_commands is the exact relay; there is no capture tool, so the sentence names that boundary explicitly."
  },
  {
   "file": "skills/gabe-docs/references/docs-spec.md",
   "line": 57,
   "skill": "gabe-docs",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "survives": true,
   "leverage": "low",
   "reason": "Anchor verbatim at line 57. The restraint is what saves it: kdbp_snapshot returns `decisions.rows` as a COUNT and `ledger.last` as 5 rows (kdbp_tools.py:298-302) — it does NOT return a decision RANGE, and the proposed text asks only for the count and the last rows, so it promises nothing the server withholds. L2 fallback present. Leverage LOWERED from medium to low: docs-spec.md is pointed at by four consumers, but this bullet governs only the /gabe-plan HTML review artifact, and gabe-plan does not even point at this file — the reach is one artifact type, not a suite surface.",
   "proposed_text": "- Include provenance: generated date, command name, canonical Markdown paths, decision range, and ledger entry. Ask `mcp__gabe-kdbp__kdbp_snapshot` for the DECISIONS row count and the last LEDGER rows before opening those files by hand; no `.kdbp/` → the tool says so and the files are the read.",
   "current_excerpt": "- Include provenance: generated date, command name, canonical Markdown paths, decision range, and ledger entry.",
   "sweep_rationale": "The HTML review artifact's provenance block is three lookups into .kdbp/ files that kdbp_snapshot already returns in under 1k tokens (git branch/ahead/dirty, plan table, PENDING top rows, `ledger.last` 5 rows, `decisions.rows` count). Deliberately worded as the row COUNT and the LAST rows — the snapshot does not return a decision RANGE, and claiming it did would be a promise the server does not keep."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 51,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "## The lift SOP (L0–L4)\n\nWhere the project carries a committed command center, the map answers the resolve/inventory questions ahead of the hand search, never instead of it: `mcp__gabe-map__map_status` first (is there a map, how fresh), then `find` / `owner_of` / `who_calls` / `outline`. The map is a FLOOR, never a scope — a NEW verdict still requires the `reuse_roots` search (E1); no center → the tools say so and L0–L4 run exactly as written.",
   "survives": true,
   "reason": "Anchor exact at line 51. map_status genuinely answers 'is there a map, how fresh'. L1 stated (FLOOR + reuse_roots search still required), L2 stated (no center → tools say so, SOP unchanged), no rail claim (L3), no script displaced (L4), .md only (L5). CORRECTED: 'before any hand search' → 'ahead of the hand search, never instead of it' — the original phrasing flirts with letting the tool stand in for the E1 search that the very next clause reinstates. Leverage 'high' is honest: one preamble arms all five steps of the skill's spine, but it is one skill's file, not a suite-wide pointer.",
   "leverage": "high",
   "current_excerpt": "## The lift SOP (L0–L4)",
   "sweep_rationale": "The lift SOP is the skill's spine and every one of L0–L4 asks a where/who/what-touches question that gabe-map answers. One preamble here arms all five steps at once and states the L1/L2 guard once instead of five times."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 53,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "a NEW verdict requires the search evidence — ask `mcp__gabe-map__find <target>` first (it indexes screens and FE pieces), then search `<reuse_roots>` anyway: an empty tool answer is not a NEW verdict.",
   "survives": true,
   "reason": "Anchor exact at line 53. `find`'s kind enum carries `screen` and `fe` (tools_wave2.py:480) — the exact corpus a REUSE/EXTEND verdict needs. L1 explicitly preserved: the reuse_roots search stays and an empty answer is barred from minting NEW — which is precisely where a false NEW would come from. L2 fine (honest-empty → the search runs, i.e. today's behaviour). CORRECTED: dropped `owner_of <reuse_roots>` — owner_of answers ownership/blindness of paths, not 'which reusable component exists', so it is adjacent, not a match (rule B); its honest role is served at react-story.md:199 where the rationale actually names the blindness signal.",
   "leverage": "high",
   "current_excerpt": "a NEW verdict requires the search evidence.",
   "sweep_rationale": "L0's E4 line is the skill's central anti-rebuild gate and its only stated method is a hand search of `reuse_roots`. `find` indexes screens and FE pieces by name — the exact corpus a REUSE/EXTEND verdict needs. The grep stays because L1 forbids an absence claim from a tool answer, which is precisely where a false NEW verdict would come from."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 54,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__outline",
   "proposed_text": "- **L1 INVENTORY** — open the reference; list its real component imports (where the reference is a source file, `mcp__gabe-map__outline <path>` gives its definitions, spans and owner without a full read — it indexes definitions, not imports, so the file is still read). Ask `mcp__gabe-map__who_calls <component>` per candidate before the verdict: a component with other live consumers is EXTEND, never edit-in-place. Output a REUSE LEDGER: component → keep / extend(prop) / genuinely-new.",
   "survives": true,
   "reason": "Anchor exact at line 54. who_calls is an exact match for the keep-vs-extend verdict (other consumers ⇒ cannot edit in place). CORRECTED on two counts: (a) L0 resolves the target to a `<screen_map>` REFERENCE DOC / mockup HTML, which is outside the archmap and graft index — outline would answer empty there, so the outline call is now conditioned on the reference being a source file; (b) the reading of the file is kept explicit, since outline is a definition index and the step's stated deliverable is the IMPORT list, which outline does not carry. L1/L2 hold: nothing here can stand as an absence proof and the step runs unchanged with no center.",
   "leverage": "high",
   "current_excerpt": "- **L1 INVENTORY** — open the reference; list its real component imports. Output a REUSE LEDGER: component → keep / extend(prop) / genuinely-new.",
   "sweep_rationale": "\"Open the reference and list its component imports\" is hand file-reading; `outline` returns the file's definitions + signatures + owner directly, and `who_calls` is what actually separates keep from extend (a component with other consumers cannot be edited in place). Reading the file stays — outline is a definition index, not the import list."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 57,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "- **L4 WIRE + VERIFY** — wire real data (ask `mcp__gabe-map__entity_context <entity>` first for the endpoints + models this screen consumes; no center → it says so and the API tree is read as before); run `<verify_commands>`;",
   "survives": true,
   "reason": "Anchor exact at the head of line 57 and the replacement preserves the line's tail. entity_context returns 'endpoints, models, schemas, files by layer, FK relations, coverage' (tools.py:638) — the exact slice 'wire real data' needs, and the step names no method today. CORRECTED: added the honest-empty tail so the step does not read as gated on a tool that may be absent (D/L2) — needed because this edit must stand alone if the line-51 preamble does not land.",
   "leverage": "medium",
   "current_excerpt": "- **L4 WIRE + VERIFY** — wire real data; run `<verify_commands>`;",
   "sweep_rationale": "\"Wire real data\" is the step where the mockup meets the app's endpoints and models — the exact slice `entity_context` returns from the committed map. Today the spec names no method at all, so the agent greps the API tree."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 69,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| legacy phase ladder (M0–M13) | advances per `.kdbp/PLAN.md` (`mcp__gabe-kdbp__kdbp_snapshot` for the phase table) | `references/legacy-html-phases.md` |",
   "survives": true,
   "reason": "Anchor exact at line 69. kdbp_snapshot returns PLAN current_phase + table row states (kdbp_tools.py:281-284) — a match for 'which M-phase is next'. No absence claim is minted here (unlike the next-free-index uses I killed below), M0–M13 sit inside the 40-row cap, and .kdbp exists by definition for a ladder 'sequenced by .kdbp/PLAN.md' so L2 is moot. Pointer-only, so no rail claim (L3). Leverage 'medium' honest — it is a table cell; the binding sentence is legacy-html-phases.md:80.",
   "leverage": "medium",
   "current_excerpt": "| legacy phase ladder (M0–M13) | advances per `.kdbp/PLAN.md` | `references/legacy-html-phases.md` |",
   "sweep_rationale": "The dispatch table's only pointer for \"which M-phase is next\" is a whole-file read of PLAN.md; `kdbp_snapshot` returns the phase table states under 1k tokens. Table-cell edit, so it stays a pointer — the binding sentence is legacy-html-phases.md:80."
  },
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 75,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "Mode-specific bookkeeping (PLAN.md rows, LEDGER.md entries, INDEX.md updates, HANDOFF.json, KDBP decisions) is documented per mode in its reference file. A deferral's PENDING row is drafted with `mcp__gabe-kdbp__pending_row_preview` and then WRITTEN by the harness — the preview writes nothing, and the KDBP hooks only see a real Write/Edit; the LEDGER row template stays each mode's own (the preview tool's entry vocabulary does not carry `MOCKUP`).",
   "survives": true,
   "reason": "Anchor exact at line 75. The L3 statement is the real value and it is law-reinforcing, not law-breaking. CORRECTED to drop the LEDGER half: `t_ledger_row_preview` raises MapStop unless `entry` is one of PLAN·RED·EXEC·REVIEW·COMMIT·PUSH·CENTER·HANDOFF·SCOPE·ASSESS (kdbp_tools.py:519-521), and every gabe-mockup row is literally `| … | MOCKUP | … |` — a tag plan-spec.md:828 names as legitimate but the tool's enum omits. Telling four modes to draft LEDGER rows with a tool that refuses their entry tag is a rule-B failure. pending_row_preview has no such enum and survives. Leverage downgraded high→medium: the ledger half carried most of the rationale.",
   "leverage": "medium",
   "current_excerpt": "Mode-specific bookkeeping (PLAN.md rows, LEDGER.md entries, INDEX.md updates, HANDOFF.json, KDBP decisions) is documented per mode in its reference file.",
   "sweep_rationale": "Four of the five modes append hand-typed LEDGER/PLAN rows (spike S8, validate S5/bookkeeping, refine RF6, react-story R11); the previews render the file's own columns and the next id. Stating the L3 rule once in the output contract keeps each mode from mistaking a preview for the write the D7 hooks must observe."
  },
  {
   "file": "skills/gabe-mockup/references/refine.md",
   "line": 12,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "Ask `mcp__gabe-map__find <screen>` first — it names the active implementation/showcase and its FE pieces — then read the project's screen map / mockup index for `<screen>`'s CANONICAL mockup (the map never carries the canonical reference, so an empty answer is not \"unmapped\").",
   "survives": true,
   "reason": "Anchor verbatim inside line 12. RF1 asks two questions and `find` cleanly answers one — locating the live implementation/showcase over the screen/FE-piece index (tools_wave2.py:480). The parenthetical is the load-bearing guard: RF1's STOP ('if unmapped → stop + ask') is an absence claim, and this edit explicitly bars the tool answer from producing it (L1). L2 holds — with no center the tool says so and the screen-map read proceeds as today. Voice and placement drop in as-is; no correction needed.",
   "leverage": "high",
   "current_excerpt": "Read the project's screen map / mockup index to find `<screen>`'s CANONICAL mockup + the active implementation/showcase.",
   "sweep_rationale": "RF1 asks two questions and the map answers one of them cleanly: locating the LIVE implementation/showcase in a React-first tree is exactly what `find` does over FE pieces. The canonical-mockup half stays a screen-map read, and the parenthetical protects the STOP condition from a false 'unmapped' (L1)."
  },
  {
   "file": "skills/gabe-mockup/references/refine.md",
   "line": 24,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "Confirm the screen RENDERS the canonical showcase/design, not a simpler rebuild (\"wire, don't rebuild\"). Ask `mcp__gabe-map__who_calls <showcase component>` first — the live screen among its users is the machine floor for \"renders\"; its absence is not proof of a rebuild, so read the screen either way.",
   "survives": true,
   "reason": "Anchor verbatim at line 24. 'Does the live screen use the showcase component' is literally who_calls (both arms, code-vs-prose marked). RF4 states no method today, so this replaces eyeballing with a floor. L1 correctly honoured — absence is explicitly barred from convicting a faithful screen, and the screen is read regardless. who_calls' gitignored delta append is the one sanctioned write. No correction needed.",
   "leverage": "high",
   "current_excerpt": "Confirm the screen RENDERS the canonical showcase/design, not a simpler rebuild (\"wire, don't rebuild\").",
   "sweep_rationale": "RF4 is the faithfulness gate and states no method — the agent eyeballs the screen file. \"Does the live screen use the showcase component\" is literally `who_calls`. Kept as first-look because a cross-file usage the index misses would otherwise convict a faithful screen of rebuilding (L1)."
  },
  {
   "file": "skills/gabe-mockup/references/refine.md",
   "line": 34,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "when the sheet's header/footer is a shared organism used by OTHER live surfaces (so you can't gate on this screen's `live` alone — enumerate those consumers with `mcp__gabe-map__who_calls <organism>` first; a caller list is a FLOOR, so confirm with a grep before calling the set complete)",
   "survives": true,
   "reason": "Anchor verbatim inside line 34. Enumerating an organism's OTHER consumers is who_calls' own question, and the byte-identical-stories claim is exactly the kind of absence claim that must not rest on an index. L1 satisfied by the retained grep confirmation. CORRECTED for sentence integrity: the original proposal buried the clause 'so you can't gate on this screen's `live` alone' behind two new clauses, leaving it dangling — the reordering restores it as the condition's stated reason. Leverage downgraded high→medium: this is a conditional sub-bullet inside RF5's fix recipe, firing only on shared chrome, not a gate every run passes.",
   "leverage": "medium",
   "current_excerpt": "when the sheet's header/footer is a shared organism used by OTHER live surfaces (so you can't gate on this screen's `live` alone)",
   "sweep_rationale": "The whole additive-opt-in recipe turns on knowing the organism's OTHER consumers, and the spec supplies no way to find them — this is the step where a missed consumer breaks byte-identical stories. `who_calls` runs both arms (index ∪ word-boundary grep) and marks code vs prose. The grep confirmation stays because the byte-identical claim is an absence claim."
  },
  {
   "file": "skills/gabe-mockup/references/refine.md",
   "line": 38,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "Record a per-screen LEDGER trace and DEFER residual data-correctness items (P6) explicitly rather than half-fixing them — `mcp__gabe-kdbp__pending_row_preview` renders the PENDING row with its next P-id and `Verified` anchor; it writes nothing, so the row still lands via Write/Edit where the KDBP hooks see it.",
   "survives": true,
   "reason": "Anchor verbatim at the tail of line 38. An explicit DEFER is a PENDING row whose shape (P-id, Verified anchor) the spec leaves to memory — pending_row_preview is built for exactly that and takes a free-form `flag` object with no entry enum. L3 stated correctly: preview then harness Write. CORRECTED to drop the `ledger_row_preview` half — refine's 'per-screen LEDGER trace' would carry this skill's `MOCKUP` tag, which the tool rejects (kdbp_tools.py:519-521); the LEDGER sentence is therefore left exactly as it stands today.",
   "leverage": "medium",
   "current_excerpt": "Record a per-screen LEDGER trace and DEFER residual data-correctness items (P6) explicitly rather than half-fixing them.",
   "sweep_rationale": "An explicit DEFER is a PENDING row and the spec leaves its shape to memory — the anchor column is exactly what a hand-typed row gets wrong. Preview-then-Write keeps L3 intact."
  },
  {
   "file": "skills/gabe-mockup/references/react-story.md",
   "line": 12,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "ask `mcp__gabe-map__find <pattern>` first (it names the project's FE pieces — components, hooks, stores), then look for reusable local Storybook organism components and reuse the project's settled filter/configuration flow before creating feature-local geometry",
   "survives": true,
   "reason": "Anchor verbatim inside line 12; the replacement reads correctly in the surrounding sentence ('Before building filters, … , ask find first (…), then look for …'). `find` carries kinds `fe` and `screen` over the compiler-proven FE pieces (component/hook/store/route/module/fe-type), so the claim is accurate. Rule 3 is the anti-rebuild guard whose only stated method is an unaided 'look for' — the tool front-loads it and the story/tree scan survives verbatim in the text (L1). Honest-empty fine: no center → the tool says so and the look-for proceeds as today. No correction needed.",
   "leverage": "high",
   "current_excerpt": "look for reusable local Storybook organism components and reuse the project's settled filter/configuration flow before creating feature-local geometry",
   "sweep_rationale": "Rule 3 is the design-reference guard's reuse clause and its method is an unaided \"look for\" — the failure this whole skill exists to prevent (\"Never rebuild an existing component\"). `find` searches the FE-piece index by name/doc. The story/tree scan stays; the tool only front-loads it."
  },
  {
   "file": "skills/gabe-mockup/references/react-story.md",
   "line": 199,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "ask `mcp__gabe-map__find <pattern>` and `mcp__gabe-map__owner_of apps/web/src/design-system/organisms` first, then scan the local `Design System/Organisms` stories and `apps/web/src/design-system/organisms` for a reusable filter/configuration flow",
   "survives": true,
   "reason": "Anchor verbatim inside line 199. Both tools match here, unlike at SKILL.md:53 — owner_of explicitly reports 'whether the census says the map is blind there' (tools.py:658), which is the honest signal for how much of the directory the following hand-scan still has to cover, and the rationale names precisely that. The scan is retained in full, so no absence claim rides on the tools (L1). The hard-coded path mirrors the line's own project-specific path, so it is consistent with the file. No correction needed.",
   "leverage": "high",
   "current_excerpt": "scan the local `Design System/Organisms` stories and `apps/web/src/design-system/organisms` for a reusable filter/configuration flow",
   "sweep_rationale": "R1's binding scan is the input to the REUSE LEDGER two paragraphs below; a directory walk is the slowest way to answer it and the easiest to skip. `owner_of` also reports where the map is blind, which is the honest signal for how much the following hand-scan still has to cover."
  },
  {
   "file": "skills/gabe-mockup/references/react-story.md",
   "line": 203,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "   Searched: <mcp__gabe-map__find / who_calls answers, or `none — no center`> · <story titles / globs / greps checked>",
   "survives": true,
   "reason": "Anchor exact at line 203, three-space indent inside the fenced REUSE LEDGER template — the replacement preserves both. This line IS the evidence contract ('a missing ledger or empty Searched line = the scan was skipped'), so naming the tool answers in it is what makes reaching for the map auditable, and the globs/greps stay beside it as the absence proof (L1). CORRECTED: added the explicit `none — no center` form, because on a map-less project an empty first field would otherwise read against the defect rule sitting two lines below (L2/D).",
   "leverage": "high",
   "current_excerpt": "   Searched: <story titles / globs / greps checked>",
   "sweep_rationale": "The ledger's Searched line IS the evidence contract — \"a missing ledger or empty Searched line = the scan was skipped\". Naming the tool answers inside the line is what makes reaching for the map auditable rather than optional, and it keeps the globs/greps as the absence proof beside it (L1)."
  },
  {
   "file": "skills/gabe-mockup/references/react-story.md",
   "line": 134,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "proposed_text": "Inspect tokens, Tailwind mapping, Storybook taxonomy, current screen/component trees, and active handoff/status docs — where the project carries a command center, `mcp__gabe-map__center_overview` orients the screen/component inventory by entity and `mcp__gabe-map__find` locates a named piece, before reading the trees by hand.",
   "survives": true,
   "reason": "Anchor verbatim inside line 134. The claim is narrow enough to be true: center_overview returns 'rank, status, counts, coverage, FE pieces per entity' in ≤600 tokens (tools_wave2.py:488) — an inventory orientation, and the edit does not pretend it carries tokens, Tailwind mapping or taxonomy, which stay hand-read for DESIGN.md's grammar. CORRECTED: added 'where the project carries a command center', since design-ref legitimately runs on repos with no center and the clause must not read as a gate (D/L2).",
   "leverage": "medium",
   "current_excerpt": "Inspect tokens, Tailwind mapping, Storybook taxonomy, current screen/component trees, and active handoff/status docs.",
   "sweep_rationale": "D2 asks design-ref to read `apps/web/src/design-system/**` and `apps/web/src/features/**` whole (Inputs, lines 105–107) to write DESIGN.md's Source Map. `center_overview` gives that inventory by entity in one call; the trees are still read for the grammar the map cannot carry."
  },
  {
   "file": "skills/gabe-mockup/references/react-story.md",
   "line": 216,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "If the repo has KDBP or rebuild docs, update the active phase/runbook (`mcp__gabe-kdbp__kdbp_snapshot` names the active phase without re-reading PLAN.md; `phase_context` returns that phase's record) with the created stories, applied decisions, remaining options, and verification results.",
   "survives": true,
   "reason": "Anchor verbatim at line 216. kdbp_snapshot returns PLAN.json `current_phase` — a scalar, not a capped list, so no absence claim is involved; phase_context returns that phase's record plus its PLAN.md details excerpt. The sentence is already conditioned on 'If the repo has KDBP or rebuild docs', which carries L2 without further wording. No L3 issue: both named tools are pure reads and the update itself remains a harness edit. No correction needed.",
   "leverage": "medium",
   "current_excerpt": "If the repo has KDBP or rebuild docs, update the active phase/runbook with the created stories, applied decisions, remaining options, and verification results.",
   "sweep_rationale": "\"The active phase\" is a PLAN.md read the snapshot answers in under 1k tokens; the row itself is still edited with Write/Edit so the hooks see it."
  },
  {
   "file": "skills/gabe-mockup/references/legacy-html-phases.md",
   "line": 80,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "`/gabe-mockup` runs the next phase recipe by default (M0 → M13 sequenced by `.kdbp/PLAN.md` — ask `mcp__gabe-kdbp__kdbp_snapshot` for the phase table states before re-reading the file; no `.kdbp/` → the tool says so and the file is read as before).",
   "survives": true,
   "reason": "Anchor exact at the head of line 80; the replacement preserves the tail ('It also accepts **named modes** …'). This is the ladder's binding dispatcher and every invocation currently pays a whole-file PLAN.md read for one answer the snapshot returns. 'before re-reading the file' keeps the file available rather than substituting for it, and the honest-empty clause carries L2 for a mockup project never /gabe-init'ed. No rail or script is displaced — the ladder's routing script is /gabe-next's, untouched (L3/L4). No correction needed.",
   "leverage": "high",
   "current_excerpt": "`/gabe-mockup` runs the next phase recipe by default (M0 → M13 sequenced by `.kdbp/PLAN.md`).",
   "sweep_rationale": "This is the binding dispatcher for the whole M0–M13 ladder and every invocation pays a full PLAN.md read to answer one question the snapshot returns. Honest-empty clause carries L2 for a mockup project that was never `/gabe-init`ed."
  },
  {
   "file": "skills/gabe-mockup/references/legacy-html-phases.md",
   "line": 195,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "1. **Assemble the entity list** — ask `mcp__gabe-map__entity_context` with no slug first (it returns the registered entity list where the project carries a command center), then from `SCOPE.md` REQs / the project's data model",
   "survives": true,
   "reason": "Anchor verbatim at the head of line 195; the archived-ENTITIES.md parenthetical is preserved. entity_context with the slug omitted returns exactly the registered entity list (tools.py:638-640), and the SCOPE.md path survives in the text so nothing rests on the tool answering (L1/L2). LEVERAGE DOWNGRADED high→low, and this is the honest correction to make: the legacy static-HTML ladder fires precisely where `.kdbp/PLAN.md` points at `docs/mockups/**`, i.e. mockup-first repos that rarely carry a committed command center — so this tool will answer honest-empty nearly every time it is asked here. Harmless and correct, but not high-leverage. Note: if this lands, edit 23 (line 262) becomes partly redundant.",
   "leverage": "low",
   "current_excerpt": "1. **Assemble the entity list** — from `SCOPE.md` REQs / the project's data model",
   "sweep_rationale": "\"The project's data model\" IS the registered entity list, and it seeds INDEX.md §4's CRUD × entity matrix for the rest of the ladder — a wrong list here propagates through M4–M13. `entity_context` with the slug omitted is the exact answer; SCOPE.md stays for a project with no center."
  },
  {
   "file": "skills/gabe-mockup/references/legacy-html-phases.md",
   "line": 219,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "1. **Read phase's REQs covered** — ask `mcp__gabe-kdbp__phase_context <phase>` first (the phase record, its row states, and any declared entities); PLAN.md Phase Details stays the source for whatever the record does not carry.",
   "survives": true,
   "reason": "Anchor exact at line 219. Verified against the implementation: phase_context returns `plan_json` record, `plan_md_row` states, `records`, `entities`, and `details_excerpt` — the PLAN.md phase section itself, truncated at 2000 chars (kdbp_tools.py:319-325). So it genuinely answers 'read phase's REQs from PLAN.md Phase Details', and the retained clause 'PLAN.md Phase Details stays the source for whatever the record does not carry' correctly covers that truncation rather than letting the excerpt stand as the whole section (L1). .kdbp is guaranteed present in this flow, so L2 is satisfied. No correction needed.",
   "leverage": "high",
   "current_excerpt": "1. **Read phase's REQs covered** from PLAN.md Phase Details.",
   "sweep_rationale": "The literal \"read PLAN.md\" step, run once per screen phase M5–M12. `phase_context` is the preflight tool built for it and returns the declared entities that step 2e then needs."
  },
  {
   "file": "skills/gabe-mockup/references/legacy-html-phases.md",
   "line": 262,
   "skill": "gabe-mockup",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "- **No entity source at M4** → ask `mcp__gabe-map__entity_context` with no slug (the registered entity list) before falling back; otherwise assemble the list from SCOPE.md REQs and prompt the user to review it before populating CRUD.",
   "survives": true,
   "reason": "Anchor exact at line 262. The rationale is sound — this branch fires when the usual sources are missing, which is the one moment a registered entity list in the committed center would be overlooked — and entity_context with the slug omitted returns exactly that list. The SCOPE.md fallback survives in the text and no absence claim rides on the tool (L1/L2). LEVERAGE DOWNGRADED medium→low: it is an error-recovery branch of a ladder that runs mostly in mockup-only repos with no center, and it duplicates edit 20 if that one lands — if both are taken, this line should read as a pointer back to step 1 rather than repeating the call.",
   "leverage": "low",
   "current_excerpt": "- **No entity source at M4** → assemble the list from SCOPE.md REQs and prompt the user to review it before populating CRUD.",
   "sweep_rationale": "The recovery path declares 'no entity source' while a registered entity list may be sitting in the committed center — the one place the skill would never think to look, since this branch fires precisely when the usual sources are missing."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 19,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "The machine already knows the facts (PLAN, junit, run-history, coverage, adoption.json, archmap, git) — where the servers are registered the map and the lifecycle state are READ as tools (`mcp__gabe-map__*` · `mcp__gabe-kdbp__*`) rather than re-derived by hand; junit, run-history and the raw coverage report stay file reads; this skill writes ONLY the translation.",
   "survives": true,
   "reason": "Anchor verbatim at :19. CORRECTED: the original over-claimed — junit, run-history.jsonl and the coverage report are served by NEITHER server (map tools read archmap/c4/adoption/center.config only; kdbp reads .kdbp/), so 'reached as TOOLS' over the whole list was false, and 'never re-derived by hand' read as forbidding the file read that stays the only path for those three. Registration caveat added so the sentence survives on an unregistered machine (L2).",
   "leverage": "medium",
   "current_excerpt": "The machine already knows the facts (PLAN, junit, run-history, coverage, adoption.json, archmap, git); this skill writes ONLY the translation.",
   "sweep_rationale": "The intention paragraph already names every machine source this skill consumes; naming the two servers here frames every mode below without touching a single step. The map stays a FLOOR (L1) — this sentence claims a reading order, not an absence proof."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 25,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "`docs/site/center/center.config.json` must exist in the project — `mcp__gabe-map__map_status` answers this gate in one call: it walks up for that same anchor and returns the center's entities, its freshness vs git, the graft index state and the regen command. A `present: false` answer carries its own `reason` (no center under this root · a root it resolved elsewhere · the suite's own beat-spine center, ruling R8) — read the reason and confirm against the file before stopping: the FILE is the gate, the tool is the fast path. If it does not exist: **STOP** — this project has no command center.",
   "survives": true,
   "reason": "Anchor verbatim at :25; find_center (mapquery.py:93-103) does walk up for docs/site/center/center.config.json, and t_map_status returns entities/counts/graft/regen_cmd. CORRECTED for L1: 'a `present: false` answer IS this STOP' turned a tool answer into an absence proof on a HARD STOP — and _absent() itself refuses that (it ships `reason` + NO_MAP_HINT pointing at Grep/Glob, and distinguishes root-resolution and the R8 suite-center case). The file check stays authoritative.",
   "leverage": "medium",
   "current_excerpt": "`docs/site/center/center.config.json` must exist in the project. If it does not: **STOP** — this project has no command center.",
   "sweep_rationale": "map_status's `find_center` walks up for exactly `docs/site/center/center.config.json` (mapquery.py:93-102), so the tool is the same check plus the freshness the whole ritual depends on. Honest-empty by construction (L2): with no center it says so and points at Grep/Glob, which is the STOP the gate already wants."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 37,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_status",
   "proposed_text": "| Status actionables | `scripts/center_status.py` — the `status` mode's linked findings + `→ next` steps (reads registry/cards/config; prints, gates nothing); `mcp__gabe-map__center_status` runs this same script and relays it, never triggering a regen of its own |",
   "survives": true,
   "reason": "Anchor verbatim at :37. L4 carve-out verified literally: tools_wave2.t_center_status executes `<root>/scripts/center_status.py` via sys.executable and relays its text, adding only a `not_run` note; it runs no build. No pipes introduced, table row stays well-formed. Leverage downgraded — this annotates a lookup table, it changes no step.",
   "leverage": "low",
   "current_excerpt": "| Status actionables | `scripts/center_status.py` — the `status` mode's linked findings + `→ next` steps (reads registry/cards/config; prints, gates nothing) |",
   "sweep_rationale": "The bindings table is where a session looks up which script owns what; the relay belongs beside the binding it relays. L4 is satisfied literally — tools_wave2.py:352 executes `<root>/scripts/center_status.py` and hands back its text."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 51,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "proposed_text": "1. Name the entity(ies) the phase's work touched — pass the phase's changed files (its LEDGER commits' diff) to `mcp__gabe-map__blast_radius` and it reads them into their owning entities (a FLOOR reading; with NO `files` argument it reads the WORKTREE vs HEAD, not the phase); `mcp__gabe-map__owner_of` answers one path and `mcp__gabe-map__entity_context` with no slug lists the registered ones; the slugs are still `adoption.json`'s",
   "survives": true,
   "reason": "Anchor verbatim at :51; blast_radius returns per-entity `touched` counts + `unowned`, owner_of takes path/paths, entity_context with no slug returns the registry list (_entity_list). CORRECTED: _changed_files() defaults to `git diff --name-only HEAD` + untracked — the WORKTREE, which is the wrong set for an already-shipped phase, so the `files` argument must be named or the step would silently name the wrong entities. Registry stays the slug authority (L1).",
   "leverage": "medium",
   "current_excerpt": "1. Name the entity(ies) the phase's work touched — slugs from `adoption.json`",
   "sweep_rationale": "\"Which entities did this work land in\" is the archetypal blast_radius/owner_of question, today answered by reading adoption.json and judging. The registry stays the authority for what a slug IS (an unknown slug still aborts the build), so nothing turns a tool answer into a scope (L1)."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 52,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "`test_rx` (required — claims the test files that VERIFY the entity, which may predate the phase; broad on purpose: an over-match shows as a visible row, an under-match silently hides coverage — ask `mcp__gabe-map__cases_for` on the entity's endpoints and models first: each answer names the C-ids and the test FILES already covering them, so the pattern is drawn around files that exist; it reads the LAST regen, so a brand-new `entities.<slug>` block answers empty until step 3's regen, and the corpus grep stays the floor for what the map has not mapped)",
   "survives": true,
   "reason": "Anchor verbatim (substring of :52, matched exactly). cases_for accepts endpoint 'METHOD /path' and model targets and returns `cases` + `covered_by_test_files` + a census_note that absence is a floor. CORRECTED: added the map-lag caveat — archmap entities derive from the config block being written in THIS step, so on a first-time block the answer is honest-empty until the step-3 regen. Grep floor preserved (L1).",
   "leverage": "medium",
   "current_excerpt": "`test_rx` (required — claims the test files that VERIFY the entity, which may predate the phase; broad on purpose: an over-match shows as a visible row, an under-match silently hides coverage)",
   "sweep_rationale": "cases_for takes a function / model / endpoint / file / case id (not a slug), so the proposal names the right targets. It sharpens the under-match risk the parenthetical already worries about, and keeps grep as the absence proof (L1)."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 52,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_census",
   "proposed_text": "and, once the section is adopted, `proofs[]` · `code{layer: [globs]}` · `models[]` — ask `mcp__gabe-map__map_census` before drafting them: the unclaimed files, models and routes it names are exactly what this block should claim (or leave unclaimed with a reason, never in silence), and `mcp__gabe-map__owner_of <path>` says whether another entity already claims a file. Draft patterns carry `TODO(verify-glob)`.",
   "survives": true,
   "reason": "Anchor verbatim (second substring of :52, matched exactly — it does not collide with the test_rx edit's span). map_census returns file/model/route/schema blocks with claimed·scanned_dirs·unclaimed (capped, entries passed through with their `reason`); owner_of returns map owners, config_glob_owners and the census entry. Reads committed archmap only, honest-empty per block. Leverage downgraded: one sentence in one step.",
   "leverage": "medium",
   "current_excerpt": "and, once the section is adopted, `proofs[]` · `code{layer: [globs]}` · `models[]`. Draft patterns carry `TODO(verify-glob)`.",
   "sweep_rationale": "The census rails exist today only in gabe-cc-init's adopt-spec (§Model census, §Route + file census), yet the FORWARD track writes the very config keys that decide claim vs blindness. map_census reads those same archmap blocks in one call, so the rail costs one sentence here."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 57,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "if the phase has a PLAN row whose Phases table carries a `Center` column — `mcp__gabe-kdbp__phase_context` returns that row's cells and its PLAN.md line, and a table with no Center column simply carries no `center` key in `cells` (`mcp__gabe-kdbp__kdbp_snapshot` shows the whole table the same way) — set that phase's `Center` cell to ✅ in `.kdbp/PLAN.md` **and** mirror `cells.center = \"done\"` into `.kdbp/PLAN.json` (same turn), both through Write/Edit so the D7 plan-proof hooks see the write",
   "survives": true,
   "reason": "Anchor verbatim at :57. Verified against the parser: plan_rows builds `cells` only for columns the header resolves (`if ci[k] is not None`), so 'no Center column ⇒ no `center` key' is exactly true, and t_phase_context returns plan_md_row {cells, raw, line}. L3 is not merely respected but hardened — the edit pins the WRITE to Write/Edit, which is what the D7 hooks watch.",
   "leverage": "medium",
   "current_excerpt": "if the phase has a PLAN row whose Phases table carries a `Center` column, set that phase's `Center` cell to ✅ in `.kdbp/PLAN.md` **and** mirror `cells.center = \"done\"` into `.kdbp/PLAN.json` (same turn)",
   "sweep_rationale": "The READ half (does the column exist, which row, which line) is exactly kdbp's parser, header-resolved and closure-aware — re-reading PLAN.md by hand is the hand-work the server exists to end. The WRITE half must stay on the harness: the D7 hooks watch Write/Edit and never see an `mcp__*` call (L3)."
  },
  {
   "file": "skills/gabe-cc-update/SKILL.md",
   "line": 61,
   "skill": "gabe-cc-update",
   "klass": "replace",
   "tool": "mcp__gabe-map__center_status",
   "proposed_text": "Run `refresh_center.sh regen`, then take the actionable list from `mcp__gabe-map__center_status` — it runs this project's `scripts/center_status.py` and relays it, never triggering a regen of its own; when the answer says `truncated`, or the server is not registered, run `scripts/center_status.py` directly for the full text — it **owns the actionable list**",
   "survives": true,
   "reason": "Anchor verbatim at :61. L4's named carve-out holds literally (t_center_status shells the project's own script). CORRECTED: the original named the script only as a truncation fallback; a `replace` of a mode's core instruction must also survive an UNREGISTERED server (L2/E6), so the fallback now covers both. Regen stays first, and the 6000-char truncation flag is honored so 'relay verbatim' cannot silently become 'relay the first 6000 chars'.",
   "leverage": "high",
   "current_excerpt": "Run `refresh_center.sh regen`, then `scripts/center_status.py` — it **owns the actionable list**",
   "sweep_rationale": "L4's named carve-out: the tool literally executes this script (tools_wave2.py:345-357). The regen stays FIRST (the gate's build + dead-link summary later in this same step is the regen's, not center_status's), and the truncation fallback preserves the mode's \"relay its output verbatim\" law."
  },
  {
   "file": "skills/gabe-cc-update/references/feature-spec.md",
   "line": 77,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "(the machine half — endpoints · code map · data model — renders from\n  archmap, never from prose; `mcp__gabe-map__entity_context <slug> detail=full` shows what that\n  half will say, so the outro complements it instead of restating a number the build can read)",
   "survives": true,
   "reason": "Anchor verbatim at :77 (the parenthetical runs 77-78 — apply as a two-line replacement, re-wrapped above to the file's own wrap width). entity_context detail=full returns endpoints, models (with cols_head/fks), schemas and files_by_layer — literally the machine half. Ties to the file's own 'a card must not restate a number the build can read' rail. Leverage downgraded: an optional-section note in one reference.",
   "leverage": "low",
   "current_excerpt": "(the machine half — endpoints · code map · data model — renders from",
   "sweep_rationale": "The card contract's standing trap is prose restating machine facts (\"a card must not restate a number the build can read\"). entity_context is the cheapest way to SEE the machine half before authoring the outro. Replaces the parenthetical spanning lines 77-78."
  },
  {
   "file": "skills/gabe-cc-update/references/feature-spec.md",
   "line": 233,
   "skill": "gabe-cc-update",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "stakeholders: the covered set = phases whose `Center` cell went ✅ since the last TERMINAL-env\nrow in `.kdbp/DEPLOYMENTS.md` (`mcp__gabe-kdbp__kdbp_snapshot` gives the phase table's `center`\ncells as they stand now; the SINCE half — which of them flipped after that row — stays a read of\nDEPLOYMENTS.md plus PLAN.md's git history, which no tool answers)",
   "survives": true,
   "reason": "Anchor verbatim at :233. CORRECTED for droppability: the original inserted its own 'row (...)' while line 234 already begins 'row in `.kdbp/DEPLOYMENTS.md` (the trigger is derived…' — pasting it would duplicate 'row' and orphan the DEPLOYMENTS.md clause. Content verified: t_kdbp_snapshot returns plan.phases[].cells including `center`, and reads no git history — the honest split the edit draws is real.",
   "leverage": "low",
   "current_excerpt": "stakeholders: the covered set = phases whose `Center` cell went ✅ since the last TERMINAL-env",
   "sweep_rationale": "Half of this join is a tool answer and half is not; saying which is which stops a session from either re-parsing PLAN.md by hand or over-trusting a snapshot that carries no history. SKILL.md §release points here, so the edit lands once."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 10,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- Git repository + `.kdbp/` present — `mcp__gabe-kdbp__kdbp_snapshot` answers both in one call (branch · ahead/behind · dirty · the `.kdbp/` files it found) and says plainly when there is no `.kdbp/` — else exit: `⛔ No KDBP. Run /gabe-init first.`",
   "survives": true,
   "reason": "Anchor verbatim at :10. Verified field-by-field: t_kdbp_snapshot returns git{branch, ahead/behind or upstream:none, dirty, recent_commits} and `files` = sorted .kdbp filenames, and _absent() when there is no .kdbp (L2 honest-empty). The exit condition itself is untouched, so the step still works with no server.",
   "leverage": "low",
   "current_excerpt": "- Git repository + `.kdbp/` present — else exit: `⛔ No KDBP. Run /gabe-init first.`",
   "sweep_rationale": "Both halves of the precondition are in one honest-empty answer (L2). Cheap, but this is the first thing every mode does."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 126,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "   - `.kdbp/SCOPE.md` REQs + §Phases entities; `.kdbp/PLAN.md` phases (`mcp__gabe-kdbp__kdbp_snapshot` returns the LIVE phase table — ids, names, per-beat cells, capped with a `+N more` note; the archived plans it does not read, so those stay a file read) **including archived",
   "survives": true,
   "reason": "Anchor verbatim at :126. Verified: snapshot's plan.phases carries id/name/cells per row from the header-resolved plan_rows parser and never touches .kdbp/archive/. CORRECTED: added the cap — phases are truncated at CAP with a `phases_note`, so a long plan is NOT fully enumerated by the tool and the caveat must say so or the rank pass silently loses rows.",
   "leverage": "low",
   "current_excerpt": "`.kdbp/SCOPE.md` REQs + §Phases entities; `.kdbp/PLAN.md` phases **including archived",
   "sweep_rationale": "The live PLAN table is the header-resolved parser's job; hand-parsing it per rank run is exactly the re-read the server ends. Naming what the snapshot does NOT cover (`.kdbp/archive/`) keeps the signal honest (L1)."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 128,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "proposed_text": "     dirs); test density per entity (where a map already exists, `mcp__gabe-map__center_overview` gives per-entity counts, flow coverage and the entities the registry does not yet carry, and `mcp__gabe-map__cases_for` names the cases already covering a candidate's endpoints/models; corpus grep + junit name matches stay the floor — and the only signal available for a candidate the map has never seen); churn",
   "survives": true,
   "reason": "Anchor verbatim at :128. Both claims check out: t_center_overview returns per-entity endpoints/models/schemas/files counts, a `coverage` string, and `unregistered` (in archmap, not in adoption.json); archmap's `coverage` IS flow coverage (build_center_a3.py:1958 `\"coverage\": ctx.flow_coverage`, commented as the per-entity flow-coverage verdict). Grep/junit explicitly kept as the floor and as the ONLY signal for an unmapped candidate (L1/L2).",
   "leverage": "medium",
   "current_excerpt": "dirs); test density per entity (corpus grep + junit name matches); churn",
   "sweep_rationale": "center_overview is graft_repo_map's equivalent at ≤600 tokens and carries `unregistered` (in archmap, not in adoption.json) — literally a rank candidate list. Stated as available-only-where-a-map-exists, matching this spec's own honest-skip convention at 2b."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 138,
   "skill": "gabe-cc-init",
   "klass": "replace",
   "tool": "mcp__gabe-map__entity_shape",
   "proposed_text": "   ask `mcp__gabe-map__entity_shape` (it loads that same `entity_shape.py` module against the committed map and returns the shape plus its one-line finding; `python3 ~/.claude/skills/gabe-pulse/scripts/entity_shape.py .` is the fallback when the server is not registered) and present its output beside",
   "survives": true,
   "reason": "Anchor verbatim at :138. L4's carve-out verified in code: t_entity_shape calls mq.pulse_module('entity_shape') then es.load_project / es.entity_shape / es.one_line — the identical computation, nothing stored, plus optional domain/diff arms. The CLI fallback is named, so the step survives an unregistered server, and the archmap-absent honest-skip line at :144-145 still governs.",
   "leverage": "high",
   "current_excerpt": "`python3 ~/.claude/skills/gabe-pulse/scripts/entity_shape.py .` and present its output beside",
   "sweep_rationale": "L4's carve-out again: tools.py:485 imports pulse's entity_shape module and calls `load_project`/`entity_shape`/`one_line` — the identical computation, nothing stored. The named fallback keeps the step runnable with no server."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 164,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "proposed_text": "   matching the entity — `mcp__gabe-map__touches <slug>` for the entity's slice once that slug is on the map (an unregistered slug is NOT an entity to the map: detect_kind falls through to a bare-name search, so read the `kind` it answers with) and `mcp__gabe-map__cases_for` per endpoint/model for the C-ids and test files already covering it, THEN grep + junit for what the map does not carry (the map is a FLOOR: no case in an answer is not proof the corpus has none) — counts per corpus (api/web/e2e), angle classification",
   "survives": true,
   "reason": "Anchor verbatim at :164. touches does accept an entity slug (detect_kind: `if t in center.entities(): return 'entity'`) and cases_for returns cases + covered_by_test_files + max_cid_in_map + a fresh corpus grep. CORRECTED: a `section` build routinely runs on a pending back-catalog entity that has NO archmap entity yet — detect_kind then returns 'function_bare' and touches answers a confusing non-answer rather than an honest empty, so the caveat is load-bearing. Grep kept as the absence proof (L1).",
   "leverage": "medium",
   "current_excerpt": "matching the entity (grep + junit), counts per corpus (api/web/e2e), angle classification",
   "sweep_rationale": "The section build's first machine step is a per-entity test inventory, done today by grep. The tools answer it from the committed map and cases_for also returns the corpus's max C-id — but grep stays, explicitly, as the absence proof (L1)."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 172,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "3. **Build:** start from the machine surface — `mcp__gabe-map__entity_context <slug> detail=full` returns the entity's endpoints, models, schemas and files-by-layer without re-reading the codebase (the *Machine-surface-first section builds* rail below; an entity the map does not yet carry answers `code: null`, and the corpus is then the only surface) — then the feature card (entity primacy, gabe-cc-update's card contract where applicable),",
   "survives": true,
   "reason": "Anchor verbatim at :172. entity_context detail=full is exactly the endpoints+models+schemas+files-by-layer read the :249 rail demands, and build_pack does NOT fail on a registered-but-unmapped slug — it returns code:null (it fails only when the slug is in neither archmap nor adoption). CORRECTED: the original cited a '§Machine-surface-first section builds' section that does not exist (it is a bullet in the post-trial contract) and omitted the code:null degradation that this mode will hit on a first build.",
   "leverage": "medium",
   "current_excerpt": "3. **Build:** feature card (entity primacy, gabe-cc-update's card contract where applicable),",
   "sweep_rationale": "This spec already rules that \"a section starts from endpoints + models + junit inventory\" (line 249) but never says how to GET them; entity_context is that read, and its brief/full projection is capped so a big entity does not blow the section run's context."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 200,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_status",
   "proposed_text": "Read tracker + `walks.jsonl` — the tracker stays the authority for per-section checklist glyphs and walk age; `mcp__gabe-map__entity_context` with no slug returns the registry rows (slug · display_name · rank · status · mapped) and `mcp__gabe-map__center_status` relays the forward track's own actionable list, which already names cardless entities and cards whose `# REVIEWED` stamp is missing, so the reconciliation below reads from it instead of re-scanning `cards/`. Render the board: per-section status/checklist glyphs, approved",
   "survives": true,
   "reason": "Anchor verbatim at :200. center_status's coverage of REVIEWED-unstamped cards and cardless entities is confirmed by gabe-cc-update SKILL.md:61, and the relay is the L4 carve-out. CORRECTED twice: (a) the submitted text left a dangling '— ;' the author knew was broken — a proposal must be droppable; (b) _entity_list returns rank/status/mapped only, NOT checklist glyphs or approved_walk, so the tracker had to be re-stated as the authority or the board would silently lose columns.",
   "leverage": "low",
   "current_excerpt": "Read tracker + `walks.jsonl`; render the board: per-section status/checklist glyphs, approved",
   "sweep_rationale": "The reconciliation clause (a pending section whose entity already carries a REVIEWED-stamped card) is precisely what center_status computes. Note the trailing punctuation needs a hand fix on adoption — the operator should splice the clause, not paste it blind."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 231,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "  input; agents read the signals here instead of re-deriving them — served as tools by `mcp__gabe-map__entity_context` · `touches` · `find` · `outline`, and the map stays a FLOOR: absence in an answer is never proof of absence, `grep -rn` is.",
   "survives": true,
   "reason": "Anchor verbatim at :231. The named tools do serve those insight blocks (touches/_fn_record read function_insight and model_insight; outline reads a file's defines/signatures; find searches names+docs across the map). The FLOOR clause is the L1 law stated exactly where a 'reads the MAP, never the codebase' rule could otherwise be read as a scope — the edit strengthens L1 rather than risking it.",
   "leverage": "medium",
   "current_excerpt": "input; agents read the signals here instead of re-deriving them.",
   "sweep_rationale": "This is the suite's read-once LAW sentence (\"reads the MAP, never the codebase\") and the one place a session is told to consume archmap's insight blocks. Naming the tools converts a rule nobody can act on into a call; the FLOOR clause keeps L1 explicit exactly where the law could otherwise be read as a scope."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 259,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "- **Verification reach:** BEHAVIOR.md Verify Commands MUST include the center's own tests — `mcp__gabe-kdbp__verify_commands` returns the binding as it stands (the BEHAVIOR section first, manifest candidates otherwise; it never runs them and never guesses a flag), so the gap is read, not remembered",
   "survives": true,
   "reason": "Anchor verbatim at :259. The tool's contract matches the claim exactly (BEHAVIOR ## Verify Commands first, else manifest candidates, never run — the same section t_phase_context folds in as behavior.verify_commands). Nothing executes, so no gate moves (L3); .kdbp is already this spec's precondition, so L2 is covered upstream.",
   "leverage": "low",
   "current_excerpt": "- **Verification reach:** BEHAVIOR.md Verify Commands MUST include the center's own tests",
   "sweep_rationale": "\"Does the binding already include the center's tests\" is a what-state question the tool answers exactly; the alternative is reading BEHAVIOR.md and eyeballing. Nothing here runs anything, so no gate moves (L3)."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 283,
   "skill": "gabe-cc-init",
   "klass": "replace",
   "tool": "mcp__gabe-map__map_census",
   "proposed_text": "- **`rank`** — the candidate table MUST list `model_census.unclaimed` (count + names — read it with `mcp__gabe-map__map_census kind=model`, the same archmap block capped and named, with each entry's `reason` intact) as a claim",
   "survives": true,
   "reason": "Anchor verbatim at :283. Verified: t_map_census's block() returns claimed·scanned_dirs·unclaimed with entries passed through untouched (so {cls, table, file, reason} survives), validates kind ∈ file|model|route|schema, and appends the version caveat \"'full coverage' holds only for archmap version ≥ 2\" that a hand read of archmap.json loses. Requirement unchanged — only the reader is named.",
   "leverage": "medium",
   "current_excerpt": "- **`rank`** — the candidate table MUST list `model_census.unclaimed` (count + names) as a claim",
   "sweep_rationale": "The rail names the archmap key but leaves the read as hand-work (open archmap.json, find the block). map_census returns that block directly, caps it, and states the version caveat (\"'full coverage' holds only for archmap version ≥ 2\") a hand read silently loses."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 286,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_census",
   "proposed_text": "  endpoints write or read is in its `models` list, and its file in `code.models`* — verified\n  against `model_census` (`mcp__gabe-map__map_census kind=model` for the unclaimed list, `mcp__gabe-map__entity_context <slug> detail=full` for the models this entity actually carries), not by eye.",
   "survives": true,
   "reason": "Anchor verbatim across :286-287 (apply as a two-line replacement; the rest of :287 from 'A deliberate exclusion' is untouched). Both readers deliver the two sides of the comparison the checklist demands: map_census kind=model the unclaimed set, entity_context full the entity's own models with table/file/cols. 'not by eye' is preserved, so nothing loosens.",
   "leverage": "medium",
   "current_excerpt": "endpoints write or read is in its `models` list, and its file in `code.models`* — verified\n  against `model_census`, not by eye.",
   "sweep_rationale": "The checklist line already forbids \"by eye\" but names no reader; the two tools are the by-machine version, and entity_context supplies the other side of the comparison in the same idiom."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 308,
   "skill": "gabe-cc-init",
   "klass": "replace",
   "tool": "mcp__gabe-map__map_census",
   "proposed_text": "- **`rank`** — the candidate table SHOULD list `route_census.unclaimed` and `file_census.unclaimed` (`mcp__gabe-map__map_census` returns both blocks in one read — `kind=route` / `kind=file` for one at a time — each entry keeping its `reason` and its `reach`)",
   "survives": true,
   "reason": "Anchor verbatim at :308. Verified: with no `kind` the tool returns all four sections at once, and block() does not reshape entries, so file_census's optional `reach` (the reach-nearest ordering this rail depends on) survives the read. Honest-empty per block when the archmap predates the census keys ('no <name> block in this archmap (version N)').",
   "leverage": "medium",
   "current_excerpt": "- **`rank`** — the candidate table SHOULD list `route_census.unclaimed` and `file_census.unclaimed`",
   "sweep_rationale": "Same rail, widened: map_census passes the entries through untouched, so the reach-nearest ordering this rail depends on survives. One read replaces opening archmap.json twice."
  },
  {
   "file": "skills/gabe-cc-init/references/adopt-spec.md",
   "line": 312,
   "skill": "gabe-cc-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "- **`section <entity>`** — when a shortlisted entity owns any unclaimed route/backend file (`mcp__gabe-map__map_census` names them; `mcp__gabe-map__owner_of <path>` says whether the map, the config globs, or nobody claims a given file), claim",
   "survives": true,
   "reason": "Anchor verbatim at :312. t_owner_of returns exactly the three answers claimed — `owners` (map file_owners with layer + lines), `config_glob_owners` (via pulse's work_scope.entity_code_globs), and the census entry — plus an explicit 'unowned by the map — the map is BLIND here' note, which keeps the unowned case a stated blindness rather than a silent absence (L1).",
   "leverage": "medium",
   "current_excerpt": "- **`section <entity>`** — when a shortlisted entity owns any unclaimed route/backend file, claim",
   "sweep_rationale": "owner_of answers the exact ownership question this rail turns on — map owners, center.config globs, and whether the census says the map is blind there — which today is three separate hand reads."
  },
  {
   "file": "skills/gabe-cc-entity/SKILL.md",
   "line": 35,
   "skill": "gabe-cc-entity",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "3. Locate the center: `docs/site/center/` found up from CWD, or `--center DIR` — `mcp__gabe-map__map_status` performs that same walk-up (for `center.config.json`) and adds the map's entities and freshness when the server is registered; `--center DIR` has no tool equivalent (the tool resolves a project ROOT).",
   "survives": true,
   "reason": "Anchor verbatim at :35, and the STOP clause that follows on the same line is untouched by the replacement. CORRECTED for L2: the submitted text made locating the center a TOOL step ('map_status walks up…'), which breaks the skill on an unregistered machine — the file walk-up is restored as the primary and the tool as the addition. find_center's anchor is confirmed identical (mapquery.py:93-103).",
   "leverage": "low",
   "current_excerpt": "3. Locate the center: `docs/site/center/` found up from CWD, or `--center DIR`.",
   "sweep_rationale": "Same anchor, same walk-up (mapquery.find_center); the tool adds the freshness stamp the pack's consumers need. The `--center` override stays named because the tool has no equivalent — it resolves a project ROOT only."
  },
  {
   "file": "skills/gabe-cc-entity/SKILL.md",
   "line": 36,
   "skill": "gabe-cc-entity",
   "klass": "replace",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "4. Ask `mcp__gabe-map__entity_context` (`slug`, `detail` brief|full|raw) — the server loads THIS skill's `scripts/entity-context.py` and returns its pack, and for `brief`/`full` adds the entity's C4 L1 edges and its `coverage` row (`raw` is the pack untouched); `python3 scripts/entity-context.py <slug> [--center DIR] [--json]` is the same reader run directly, the fallback when the server is not registered (`/gabe-map status`) or when the center sits outside a project root. Do NOT re-derive the slice by reading source; both index the committed `archmap.json`.",
   "survives": true,
   "reason": "Anchor verbatim at :36. L4's carve-out verified: mapquery.py:31 points ENTITY_CONTEXT at skills/gabe-cc-entity/scripts/entity-context.py and t_entity_context calls mod.build_pack on it. CORRECTED: the c4 L1 edges and coverage row are added only when detail != 'raw' (`if detail != \"raw\":`), so the unqualified 'plus' was wrong. Script fallback named, so the step survives with no server (L2).",
   "leverage": "high",
   "current_excerpt": "4. Run `scripts/entity-context.py <slug> [--center DIR] [--json]` — the deterministic reader. Do NOT re-derive the slice by reading source; the reader indexes the committed `archmap.json`.",
   "sweep_rationale": "L4's carve-out at its purest: mapquery.ENTITY_CONTEXT points at this skill's own script and the tool calls its `build_pack` (tools.py:181-183), then adds c4 L1 edges + the archmap coverage row. This is why the batch's `retire` question answers NO — the script IS the tool's body; retiring the skill would delete the tool's implementation. What changes is the calling surface."
  },
  {
   "file": "skills/gabe-cc-entity/SKILL.md",
   "line": 38,
   "skill": "gabe-cc-entity",
   "klass": "replace",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "6. Choose the detail by consumer: **brief** for a human (counts + names, capped) · the working slice for a session mid-task (`detail: full`, capped lists that name their cap) · **`detail: raw`** — parity with the script's `--json` — when an agent or a downstream beat needs the whole pack as structured context.",
   "survives": true,
   "reason": "Anchor verbatim at :38. The three-way detail and the raw≡--json parity are confirmed by _project_pack's own docstring ('brief = counts + names · full = capped projection · raw = untouched (parity with entity-context.py --json)') and by mq.cap_list attaching a note to every capped list. CORRECTED: the '~300 tokens' figure for brief appears nowhere in the code or spec — an invented number in a spec is the kind of claim this suite prices, so it is dropped.",
   "leverage": "medium",
   "current_excerpt": "6. Choose the mode by consumer: the **brief** for a human; **`--json`** when an agent or a downstream beat needs the pack as structured context.",
   "sweep_rationale": "The tool's three-way `detail` supersedes the two-way brief/--json split, and `raw` is documented as parity with `--json` in the projector's own docstring. Without this the mode table and the tool disagree on the vocabulary — one term per concept."
  },
  {
   "file": "skills/gabe-cc-entity/references/entity-spec.md",
   "line": 7,
   "skill": "gabe-cc-entity",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_context",
   "proposed_text": "from the command center's committed data. It is a deterministic reader\n(`scripts/entity-context.py`) — a pure consumer of the `archmap.json` contract, never a\nproducer — and it is also the BODY of `mcp__gabe-map__entity_context`: the server imports\nthis script's `build_pack`, so tool and CLI answer from ONE implementation and this spec\nbinds both.",
   "survives": true,
   "reason": "Anchor verbatim across :7-9. The claim is exactly true (mapquery.ENTITY_CONTEXT → this script; tools.t_entity_context calls mod.build_pack), and the seam is real: a change to the pack shape silently changes an MCP tool's contract and, through kdbp phase_context, execute's preflight. LEVERAGE CORRECTED: 'suite-wide' is reserved for a file many skills point at (execution-contract.md and its kind); entity-spec.md is pointed at by gabe-cc-entity alone — high, not suite-wide.",
   "leverage": "high",
   "current_excerpt": "It is a deterministic reader\n(`scripts/entity-context.py`) — a pure consumer of the `archmap.json` contract, never a\nproducer.",
   "sweep_rationale": "The binding spec must say which surfaces it binds, or the next edit to the pack shape silently changes an MCP tool's contract (and, through phase_context, execute's preflight). One implementation, one spec — the suite's own \"one fact, one word\" rule applied to the seam."
  },
  {
   "file": "skills/gabe-cc-entity/references/entity-spec.md",
   "line": 96,
   "skill": "gabe-cc-entity",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "proposed_text": "- **Emits** a context pack `mcp__gabe-kdbp__phase_context` now consumes: for every entity a\n  phase declares in PLAN.json (capped at 8) it calls `mcp__gabe-map__entity_context`, whose body\n  is this reader — so execute's preflight carries each declared entity's brief and no beat\n  re-derives it (execute-spec §context A names the map tools directly; with gabe-map absent the\n  field says so, and with no declared entities the preflight warns and falls back to `owner_of`\n  on the scope globs). The `--entity` beat-flag that would make red/review entity-aware the same\n  way is still a **deferred, coordinated** phase — it edits those specs and is out of scope here.\n  No current seam breaks.",
   "survives": true,
   "reason": "Anchor verbatim across :96-97, and the claim it removes is provably STALE: kdbp_tools.py:341-350 calls map_tools.call('entity_context', {slug, detail:'brief'}) per declared entity, and execute-spec.md:206-207 already routes context A through mcp__gabe-map__touches/owner_of/entity_context. CORRECTED for droppability: the submitted text replaced :96-97 but left 'First cut is standalone…' at :97 contradicting it — this replaces :96-99 as one bullet, keeps the deferred --entity record re-based, and carries the real degradations (cap 8, gabe-map unavailable, no declared entities → owner_of fallback).",
   "leverage": "high",
   "current_excerpt": "- **Emits** a context pack that **no beat consumes yet** — execute/red/review are not\n  entity-aware.",
   "sweep_rationale": "STALE — the claim is now false. kdbp_tools.py:353-361 calls gabe-map's entity_context per declared entity, and execute-spec:206 already names the map tools as context A. Leaving \"no beat consumes yet\" in the binding spec is the kind of seam lie the handshake walk exists to catch. The following sentences about the deferred `--entity` beat-flag still hold and should be re-based on this one, not deleted."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 37,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Check each probe silently. Do NOT run commands that modify anything. On a `.kdbp/` project ONE `mcp__gabe-kdbp__kdbp_snapshot` call answers the lifecycle rows in a single pass — branch, ahead/behind, the dirty counts, the PLAN phase table (capped at 40 rows, the cap named), the open PENDING count, the last LEDGER rows — and says plainly when there is no `.kdbp/`; the file-existence probes below stay for what it does not carry (VALUES, the profile, maturity).",
   "survives": true,
   "reason": "Anchor verbatim at line 37. Every field claimed is real in t_kdbp_snapshot: git.branch/ahead/behind/dirty, plan.phases capped at CAP=40 with phases_note, pending.open, ledger.last; _absent() returns present:false with a named reason (L2 honest-empty). VALUES/profile/maturity are genuinely absent from the snapshot — plan_json carries maturity but the tool does not surface it — so keeping those probes is correct (L1: nothing deleted). Leverage corrected: gabe-help/SKILL.md is not a file many skills point at, so 'suite-wide' is dishonest per F.",
   "leverage": "medium",
   "current_excerpt": "Check each probe silently. Do NOT run commands that modify anything.",
   "sweep_rationale": "The scan currently opens four bookkeeping files by hand. One snapshot call answers the KDBP half and is honest-empty elsewhere, so the table below degrades to file-existence checks exactly as today (L2). The fallback probes are kept, not deleted (L1)."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 42,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Uncommitted changes** | `mcp__gabe-kdbp__kdbp_snapshot` `git.dirty` (modified · untracked · total), else `git status --porcelain` (if git repo) | count of changed files, or clean |",
   "survives": true,
   "reason": "Anchor verbatim. git.dirty is exactly {modified, untracked, total} built from `git status --porcelain`, and total == the porcelain line count the probe wants. The raw command is retained for non-KDBP repos, which _absent() cannot serve (it returns no git block at all) — L2 intact.",
   "leverage": "low",
   "current_excerpt": "| **Uncommitted changes** | `git status --porcelain` (if git repo) | count of changed files, or clean |",
   "sweep_rationale": "The snapshot already ran for rows 46–49; its `git.dirty` block is the same porcelain count, so this probe costs nothing extra. The raw git command stays for non-KDBP repos, which gabe-help must still classify."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 43,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Alignment initialized** | `mcp__gabe-kdbp__kdbp_snapshot` (`present` + the `files` list), else check if `.kdbp/` or `.kdbp/VALUES.md` exists | yes (with maturity read from BEHAVIOR.md — the snapshot does not carry it) / no |",
   "survives": true,
   "reason": "Anchor verbatim. `present` is set by _ctx()'s `.kdbp` isdir check and `files` is the sorted file listing (capped at 40). The maturity claim is correct: behavior_facts() is read only by phase_context (line 331), never by kdbp_snapshot, so naming the BEHAVIOR.md read is required and is kept. Fallback retained. Leverage corrected down — one probe row in one skill.",
   "leverage": "low",
   "current_excerpt": "| **Alignment initialized** | Check if `.kdbp/` or `.kdbp/VALUES.md` exists | yes (with maturity from BEHAVIOR.md) / no |",
   "sweep_rationale": "The snapshot settles present/absent and enumerates the `.kdbp/` files in the same call, but returns no maturity field (that is `phase_context`'s BEHAVIOR read), so the BEHAVIOR.md read must stay named."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 46,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Checkpoint ledger** | `mcp__gabe-kdbp__kdbp_snapshot` `ledger` (its `reason` when absent) settles presence and shows the last rows; count the rows in `.kdbp/LEDGER.md` | yes (count of entries) / no |",
   "survives": true,
   "reason": "Anchor verbatim. ledger = {'last': ledger_rows(led, 5)[0]} or {'reason': 'no LEDGER.md'} — the tool returns five rows, not a total, so the proposal is right to refuse a full replace and keep the file count. Refusing to let a capped list stand as the number is exactly L1. No extra cost: edit 1 already makes the call.",
   "leverage": "low",
   "current_excerpt": "| **Checkpoint ledger** | Check if `.kdbp/LEDGER.md` exists | yes (count of entries) / no |",
   "sweep_rationale": "Deliberately NOT a full replace: `kdbp_snapshot.ledger` returns the last rows, not a total, so reporting a count still needs the file. Stating that keeps the dashboard's number honest instead of silently changing what it means."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 47,
   "skill": "gabe-help",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Deferred items** | `mcp__gabe-kdbp__kdbp_snapshot` `pending.open` — the canonical surface is `.kdbp/PENDING.md` (its `top` list caps at 10, the count does not); fall back to `.kdbp/deferred-cr.md` or `.planning/deferred-cr.md`, the legacy read-only names `/gabe-review` still honours | count of open items / none |",
   "survives": true,
   "reason": "CORRECTED. The defect is real: the probe never checks `.kdbp/PENDING.md`, the canonical file every writer targets, and no template ships deferred-cr.md — so on a current project the row and the 'Deferred debt' situation at line 63 cannot fire. But the proposal DELETED the legacy names, which review-spec.md:120/1293 and gate-spec.md:79 still name as live read-fallbacks; removing them makes a legacy repo silently report 'none' (L1). Fallback restored, cap on `top` named.",
   "leverage": "medium",
   "current_excerpt": "| **Deferred items** | Check `.kdbp/deferred-cr.md` or `.planning/deferred-cr.md` | count of unresolved items / none |",
   "sweep_rationale": "CONFIRMED DEFECT: neither `.kdbp/deferred-cr.md` nor `.planning/deferred-cr.md` exists in the KDBP layout — no template writes either name (`templates/` ships PENDING.md), so this probe has always reported `none` and the **Deferred debt** situation at line 63 can never fire. `pending.open` is the exact count, closure resolved by the file's own Status verdict token."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 48,
   "skill": "gabe-help",
   "klass": "retire",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "",
   "survives": true,
   "reason": "Anchor verbatim. Grep confirms nothing in the suite creates `.planning/PROJECT.md` — the only surviving `.planning/` references are gabe-health's legacy PLAN-source fallback (`.planning/phases/*/PLAN.md`) and archive prose, never PROJECT.md. Dead probe. Survives ONLY as a set with edits 7, 9 and 10: it feeds the situation at line 64 and the dashboard at line 82, so landing this row alone leaves both dangling. Leverage corrected — a dead-text deletion in one skill.",
   "leverage": "medium",
   "current_excerpt": "| **GSD planning** | Check if `.planning/PROJECT.md` exists | yes / no |",
   "sweep_rationale": "Delete the row. `.planning/PROJECT.md` is the pre-KDBP GSD layout; nothing in the suite creates it, so the probe is dead text that also feeds the misnamed dashboard line 82 and the Post-milestone condition at line 64. Plan presence is now carried by the Active phase row below (`plan.status` / `plan.current_phase`)."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 49,
   "skill": "gabe-help",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Active phase** | `mcp__gabe-kdbp__kdbp_snapshot` — `plan.status` (`active` / `none`) + `plan.current_phase` and that row's cells; when `plan.mirror` reads `no PLAN.json mirror`, take the pointer from `## Current Phase` in `.kdbp/PLAN.md` | phase number + the cell it owes, or none |",
   "survives": true,
   "reason": "CORRECTED. The defect is real — `.planning/phases/*/PLAN.md` + VERIFICATION.md is the retired GSD layout. But `current_phase` comes ONLY from plan_json(); a project with PLAN.md and no PLAN.json mirror would report 'none' though a plan is active, letting a tool answer stand as an absence claim (L1). The tool exposes plan.mirror for exactly this, so the fallback is now named. Also pinned the real status values (active/none).",
   "leverage": "medium",
   "current_excerpt": "| **Active phase** | Look for `.planning/phases/*/PLAN.md` without `VERIFICATION.md` | phase number or none |",
   "sweep_rationale": "CONFIRMED DEFECT: `.planning/phases/*/PLAN.md` and `VERIFICATION.md` are the retired GSD layout; the KDBP plan is the single `.kdbp/PLAN.md` + PLAN.json mirror. The snapshot returns the pointer AND the phase cells header-resolved, so the row can name which beat is owed instead of just a number."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 59,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "| **Brownfield project** | Git repo exists, no `.kdbp/`, existing code/docs/tests (`mcp__gabe-map__map_status` says whether a command center already exists — if it does, the inventory starts from `mcp__gabe-map__center_overview`) | Inventory first, then cautious KDBP adoption |",
   "survives": true,
   "reason": "Anchor verbatim. map_status answers center presence (find_center walks up for docs/site/center/center.config.json) and returns entities/counts/kdbp_present; center_overview returns the per-entity orientation rows claimed. Absent center → present:false plus NO_MAP_HINT pointing at Grep/Glob, so the from-scratch inventory is unchanged (L2). Pure addition, nothing removed. Leverage corrected — a single classification row.",
   "leverage": "low",
   "current_excerpt": "| **Brownfield project** | Git repo exists, no `.kdbp/`, existing code/docs/tests | Inventory first, then cautious KDBP adoption |",
   "sweep_rationale": "Addition, not a replacement: gabe-help today cannot tell a mapped brownfield repo from an unmapped one, so it recommends a from-scratch inventory over a repo that already has an entity map. `map_status` is honest-empty where there is no center, so the recommendation is unchanged there."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 64,
   "skill": "gabe-help",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| **Post-milestone** | A plan exists and no phase is active — `plan.status` is `none` (archived/complete), or every phase row's cells are done | Retro + health check |",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim, and the condition must move because edit 6 retires the signal it names. But the proposal invented a status value: PLAN.json carries only `active` and `none` (plan-spec.md:506/546/591; next.mjs:36 tests `!== \"active\"`). 'plan.status complete' would never match. Rewritten to the two real terminal shapes.",
   "leverage": "low",
   "current_excerpt": "| **Post-milestone** | GSD planning exists, no active phase | Retro + health check |",
   "sweep_rationale": "Follows the retirement of the GSD probe at line 48 — the condition names a signal the scan will no longer produce. The snapshot's `plan.status` + phase cells are the KDBP equivalent."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 82,
   "skill": "gabe-help",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "│ KDBP plan:       ✅ Phase N active | ❌ No active plan │",
   "survives": true,
   "reason": "Anchor verbatim. Width claim verified: 'GSD planning:' (13) + 4 spaces = 'KDBP plan:' (10) + 7 spaces = 17; 'Not found' (9) + 6 trailing = 'No active plan' (14) + 1 trailing = 15. Box art unchanged. Output-rule mirror of edits 6/7 — must land with them or the dashboard names a retired probe.",
   "leverage": "low",
   "current_excerpt": "│ GSD planning:    ✅ Phase N active | ❌ Not found      │",
   "sweep_rationale": "Output rule mirroring the retired probe: the dashboard label still says GSD. Replacement is the same 56-character width as the line it replaces, so the box art is unchanged."
  },
  {
   "file": "skills/gabe-help/SKILL.md",
   "line": 99,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "2. **Fast.** The scan should take < 5 seconds. Don't read file contents unless needed (e.g., maturity from BEHAVIOR.md frontmatter, suit from profile) — one `mcp__gabe-kdbp__kdbp_snapshot` call is cheaper than the PLAN/PENDING/LEDGER reads it replaces, and it is capped by design.",
   "survives": true,
   "reason": "Anchor verbatim. 'Capped by design' is accurate on every list the tool returns (phases 40, pending.top 10, ledger 5, commits 8, files 40). The speed rule and both named file reads survive unchanged.",
   "leverage": "low",
   "current_excerpt": "2. **Fast.** The scan should take < 5 seconds. Don't read file contents unless needed (e.g., maturity from BEHAVIOR.md frontmatter, suit from profile).",
   "sweep_rationale": "The speed rule is the reason the Step 1 probes were file-existence-only; naming the snapshot as the cheap path keeps the rule and removes the excuse for re-reading bookkeeping files."
  },
  {
   "file": "skills/gabe-help/references/help-spec.md",
   "line": 30,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "1. Read-only inventory       — `mcp__gabe-map__map_status` first (a committed map answers with entities + counts; no map → it says so and points at Grep/Glob), then layout, tests, CI, docs, git history, and risks",
   "survives": true,
   "reason": "CORRECTED for form. Anchor verbatim at line 30, and the tool claim is exact — NO_MAP_HINT is literally 'this project has no codebase map; Grep/Glob are the source of truth here.' But line 30 sits inside a column-aligned fenced recipe block (lines 29-35); the proposal's three-clause sentence wrecks that alignment and is not droppable as-is. Shortened to one line while keeping the honest-empty pointer and the full hand inventory (L1/L2).",
   "leverage": "low",
   "current_excerpt": "1. Read-only inventory       — Inspect layout, tests, CI, docs, git history, and risks before writing",
   "sweep_rationale": "The brownfield lane's first move is a hand inventory even where the project already carries a committed map. `map_status` costs one call and is honest-empty otherwise, so the hand inventory is preserved as the floor (L1/L2)."
  },
  {
   "file": "skills/gabe-handoff/SKILL.md",
   "line": 27,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "5. Gather evidence read-only, starting with ONE `mcp__gabe-kdbp__kdbp_snapshot` call — it carries branch, ahead/behind, the dirty counts, the last commits, the PLAN phase table with its `current_phase` mirror, the open PENDING rows and the latest LEDGER rows, and says plainly when there is no `.kdbp/` (where it does, run the spec's Step 1 git block yourself — the snapshot returns no git data on a non-KDBP repo). Add `git log --oneline -n 20` for the session's own commits and read `.kdbp/SCOPE.md` `## Phases` if present (no tool carries it); then this session's landed work, verification actually run, decisions made, in-flight work, and agreed-but-not-started next steps — every claim cited (E1).",
   "survives": true,
   "reason": "CORRECTED on D. Anchor verbatim and every claimed field is real. But _absent() returns ONLY {present, root, root_source, reason} — no git block — so the proposal's wholesale removal of 'git branch/log/status/ahead-behind counts' left a non-KDBP git repo (the case Step 4 explicitly supports) with no instruction for branch/status/drift. Fallback named. Leverage corrected: high, not suite-wide — no other skill points at gabe-handoff/SKILL.md.",
   "leverage": "high",
   "current_excerpt": "5. Gather evidence read-only: git branch/log/status/ahead-behind counts; `.kdbp/PLAN.md` current phase + Phases table; `.kdbp/PENDING.md` open items; `.kdbp/LEDGER.md` latest row; `.kdbp/SCOPE.md` `## Phases` if present; and this session's landed work, verification actually run, decisions made, in-flight work, and agreed-but-not-started next steps — every claim cited (E1).",
   "sweep_rationale": "The single highest-value edit in this batch: handoff runs when context is nearly full, and this step currently opens four bookkeeping files plus five git commands to build a state block one capped call already returns. Honest-empty keeps the no-KDBP path at line 26 intact."
  },
  {
   "file": "skills/gabe-handoff/SKILL.md",
   "line": 29,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "7. Unless `--no-sync` or `--dry-run`: sync KDBP state to *observed reality only* — PLAN.md phase cells (Exec/Review/Commit/Push, each ✅ only with an evidence citation, never fabricated, mirrored into PLAN.json), append one LEDGER.md row composed by `mcp__gabe-kdbp__ledger_row_preview`, add/update PENDING.md rows composed by `mcp__gabe-kdbp__pending_row_preview` for in-flight-droppable or deferred items — the previews write nothing, every row lands through Write/Edit so the KDBP hooks see it — and print the visible KDBP SYNC report (E5).",
   "survives": true,
   "reason": "Anchor verbatim. ledger_row_preview accepts entry 'HANDOFF' and returns the row in the file's own column order; pending_row_preview returns columns, next_id, the Verified anchor and recurring candidates. L3 is discharged explicitly in the text ('the previews write nothing, every row lands through Write/Edit so the KDBP hooks see it') — the D7 hooks watching Write/Edit still fire. Construction matches the precedent at gabe-red/references/red-spec.md:234.",
   "leverage": "medium",
   "current_excerpt": "7. Unless `--no-sync` or `--dry-run`: sync KDBP state to *observed reality only* — PLAN.md phase cells (Exec/Review/Commit/Push, each ✅ only with an evidence citation, never fabricated, mirrored into PLAN.json), append one LEDGER.md row in house format, add/update PENDING.md rows for in-flight-droppable or deferred items, and print the visible KDBP SYNC report (E5).",
   "sweep_rationale": "\"in house format\" is the failure this replaces: the row schema differs across projects and the previews return the FILE's own columns, next P-id and Verified anchor. Matches the wording /gabe-red already uses, and L3 is stated inline so the preview never displaces the harness Write."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 57,
   "skill": "gabe-handoff",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "`mcp__gabe-kdbp__kdbp_snapshot` returns branch, ahead/behind, the dirty counts (modified · untracked · total) and the last 8 commits in one call — ask it first, and run only what it does not cover (the 20-commit window, the per-file status); no `.kdbp/` → it says so and the block above runs as written. Record: branch, HEAD sha + subject, uncommitted/untracked files, unpushed commit count, upstream drift.",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim; four of the five commands in the fence at lines 50-56 are genuinely inside the snapshot (branch, rev-list --left-right, status --porcelain, log -8). The proposal's rationale claimed the no-.kdbp path was preserved but the text never said so — now stated, since _absent() returns no git block. Fence retained (L1).",
   "leverage": "low",
   "current_excerpt": "Record: branch, HEAD sha + subject, uncommitted/untracked files, unpushed commit count, upstream drift.",
   "sweep_rationale": "Four of the five commands in the block above are already inside the snapshot. Kept as first-look because the 20-commit window and per-path status are not, and the block must still work in a repo with no `.kdbp/` (L2)."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 60,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- `.kdbp/PLAN.md` — via `mcp__gabe-kdbp__kdbp_snapshot`: `plan.current_phase` (the PLAN.json mirror) + `plan.phases[]`, the `## Phases` table read header-resolved so the optional `Red` (TDD projects, before Exec) and `Center` (command-center projects) columns are never read off a shifted position; each cell comes back as `done` / `active` / `todo` / `skipped`, not the raw ⬜ 🔄 ✅. The list caps at 40 rows and names the cap — read the table itself for a longer plan, or when the pointer lives only in `## Current Phase` (no PLAN.json mirror). `mcp__gabe-kdbp__phase_context` adds the active phase's record. Identify the active phase row(s) touched this session.",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim. Header resolution, the 40-cap with phases_note, the PLAN.json-only current_phase and phase_context's record block (phase_records: Cases/Reach/Class/Proof/Searched) all check out, and the cap + no-mirror cases are correctly kept as file reads (L1). But the proposal preserved the legend '⬜ not started · 🔄 in progress · ✅ complete' immediately after routing the read through _state(), which never emits those glyphs — replaced with the real tokens.",
   "leverage": "medium",
   "current_excerpt": "- `.kdbp/PLAN.md` — `## Current Phase` pointer + the `## Phases` table (columns `# | Phase | Description | Tier | Complexity | Exec | Review | Commit | Push`, plus optional `Red` (TDD projects, before Exec) and `Center` (command-center projects) columns; symbols ⬜ not started · 🔄 in progress · ✅ complete). Identify the active phase row(s) touched this session.",
   "sweep_rationale": "The bullet hand-documents a column layout that varies per project — the exact thing the header-resolved parser removes. The cap and the no-mirror case are stated so a capped list is never read as an absence (L1)."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 61,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- `.kdbp/PENDING.md` — open deferred items from `mcp__gabe-kdbp__kdbp_snapshot` `pending`: `open` is the exact count, `top` the ten highest by priority then deferrals, closure resolved by the file's own Status verdict token (or a `<!-- P<n> resolved -->` comment). Read the table itself when more than the top ten matter.",
   "survives": true,
   "reason": "Anchor verbatim, and every claim matches pending_rows() exactly: CLOSERS = CLOSED/RESOLVED/WONT-DO/WON'T-DO/SUPERSEDED/DONE/FIXED plus the `<!-- P<n> ... resolved -->` sibling-line comment, and prio() sorts by priority then -times_deferred. The spec's current rule (`Status != resolved/closed`) genuinely misses four of the seven closers. The ten-row cap is named and the file read kept, so the top list is never an absence proof (L1).",
   "leverage": "medium",
   "current_excerpt": "- `.kdbp/PENDING.md` — open deferred items (table; `Status != resolved/closed`).",
   "sweep_rationale": "`Status != resolved/closed` is a guess at a schema that differs across projects; the tool resolves closure the way the file declares it. The cap is named so the top-ten list is never mistaken for the whole backlog (L1)."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 62,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- `.kdbp/LEDGER.md` — the most recent rows from `mcp__gabe-kdbp__kdbp_snapshot` `ledger.last` (for continuity + to avoid duplicating), or its `reason` when the project has none.",
   "survives": true,
   "reason": "Anchor verbatim. ledger.last = ledger_rows(led, 5)[0], i.e. the most recent rows with the file's own columns (and the headerless legacy path handled), which is exactly what the 3b append must match; {'reason': 'no LEDGER.md'} is the honest-empty branch. Read scope widens from one row to five — no claim broken.",
   "leverage": "low",
   "current_excerpt": "- `.kdbp/LEDGER.md` — the most recent row (for continuity + to avoid duplicating).",
   "sweep_rationale": "Same call already made in Step 1; the tool returns the last rows with the file's own columns, which is what the 3b append must match."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 102,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "**3b — LEDGER.md.** Compose the row with `mcp__gabe-kdbp__ledger_row_preview` (`entry: HANDOFF`; it returns the row in the FILE's own column order and writes nothing), then append it with Write/Edit — the KDBP hooks watch the harness's writes, not `mcp__*` — directly under the header of the thin session index (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\"), newest first:",
   "survives": true,
   "reason": "Anchor verbatim. 'HANDOFF' is in the tool's accepted entry set, and the row is assembled against the file's discovered header (canonical 5 columns only as a stated fallback with `header_found: false`). The literal template at lines 104-106 maps cleanly: theme → 'Theme / scope', commits → 'HEAD <sha>', gates → 'resume → HANDOFF.md' (copied verbatim, never composed). L3 discharged in-text; construction identical to red-spec.md:234.",
   "leverage": "medium",
   "current_excerpt": "**3b — LEDGER.md.** Append ONE row to the thin session index (`gabe-plan/references/plan-spec.md` § \"Shared: LEDGER.md thin session index\"), directly under the header, newest first:",
   "sweep_rationale": "The literal row template below stays as the shape; the preview supplies the project's real column order, which is what breaks when a twin's LEDGER schema differs. Same construction /gabe-red's spec already uses for its RED row."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 112,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__pending_row_preview",
   "proposed_text": "**3c — PENDING.md.** For each IN-FLIGHT-that-could-be-dropped or DEFERRED item not already tracked, compose the row with `mcp__gabe-kdbp__pending_row_preview` (the file's own columns, the next P-id, the `Verified` anchor and the recurring-row flag; it writes nothing) and add it with Write/Edit. Don't duplicate an open row — match by file + finding overlap, as `/gabe-review` does.",
   "survives": true,
   "reason": "Anchor verbatim. The tool returns `columns` (hdr or the canonical 11), `next_id` computed across PENDING.md AND archive/PENDING-resolved*.md, `Verified` as '@<short sha> <date>', and recurring_candidates by same-File + ≥3 shared tokens — the very same-file/overlap rule the sentence keeps for the author. Write stays on Write/Edit (L3); the duplicate judgement is not delegated.",
   "leverage": "medium",
   "current_excerpt": "**3c — PENDING.md.** For each IN-FLIGHT-that-could-be-dropped or DEFERRED item not already tracked, add a row (respect the existing table columns; don't duplicate an open row — match by file + finding overlap, as `/gabe-review` does).",
   "sweep_rationale": "\"respect the existing table columns\" and \"next P-id\" are exactly what the preview computes, including the Verified anchor this spec otherwise never mentions. The write stays on Write/Edit (L3), and the duplicate check stays with the author."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 138,
   "skill": "gabe-handoff",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "proposed_text": "- In-flight: <the exact task + how far it got + the partially-touched files with paths, and the entity that owns them where the project has a map (`mcp__gabe-map__owner_of`) — a cold session orients by entity faster than by path>.",
   "survives": true,
   "reason": "Anchor verbatim. owner_of answers exactly 'which entity owns these paths' (paths or paths[], dir-aware, capped at 40) and is honest-empty with the Grep/Glob hint where no center exists, in which case the line degrades to today's paths-only form. Sits inside a `< >` instruction placeholder, so it reads as guidance not emitted output.",
   "leverage": "low",
   "current_excerpt": "- In-flight: <the exact task + how far it got + the partially-touched files with paths>.",
   "sweep_rationale": "Addition, low-risk: naming the owning entity costs one call and gives the resume prompt a handle the next session can feed straight into `entity_context`. Honest-empty without a center, in which case the paths alone stand as today."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 145,
   "skill": "gabe-handoff",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "- Verify: <the real typecheck/test/e2e commands for this repo, from `mcp__gabe-kdbp__verify_commands` — BEHAVIOR's binding first, else the manifest candidates it names, marked as candidates; no `.kdbp/` → the commands this session actually ran, cited. Never a guessed flag>.",
   "survives": true,
   "reason": "CORRECTED on D. Anchor verbatim, and the tool matches the claim precisely: source 'a: BEHAVIOR.md ## Verify Commands' wins, else 'b: ... (candidates — not yet bound)', with the standing note that commands are resolved and never run. The proposal gave no no-.kdbp branch, and this is the RUNBOOK line a cold session executes blind — added the honest fallback rather than letting a tool absence blank the line.",
   "leverage": "medium",
   "current_excerpt": "- Verify: <the real typecheck/test/e2e commands for this repo>.",
   "sweep_rationale": "\"the real commands for this repo\" is a guess-shaped instruction and the runbook is the half of the handoff a cold session executes blind. `verify_commands` is the binding, never runs anything, and marks manifest candidates as candidates rather than asserting them."
  },
  {
   "file": "skills/gabe-handoff/references/handoff-spec.md",
   "line": 146,
   "skill": "gabe-handoff",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- Gotchas: <traps from LEDGER rows/DECISIONS/memory that would bite a fresh session — the Step 1 snapshot's `ledger.last` names the recent rows and `decisions.rows` says whether a DECISIONS file exists to open>.",
   "survives": true,
   "reason": "Anchor verbatim. decisions = {'rows': <table row count>|None, 'reason': 'no DECISIONS.md'} — a count and a presence flag, not decision text, which the proposal correctly states, so the read stays and the tool is not treated as the source (L1). Reuses the Step 1 call; no new cost.",
   "leverage": "low",
   "current_excerpt": "- Gotchas: <traps from LEDGER rows/DECISIONS/memory that would bite a fresh session>.",
   "sweep_rationale": "First-look only: the snapshot names the rows and counts DECISIONS, it does not carry decision text, so the read stays. Points at what to open instead of asking the author to remember."
  },
  {
   "file": "skills/gabe-pulse/SKILL.md",
   "line": 70,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__next_beat",
   "proposed_text": "| P4 | Router's next step | `mcp__gabe-kdbp__next_beat` → `../gabe-next/scripts/next.mjs --json` | whatever it names |",
   "survives": true,
   "reason": "Anchor verbatim. L4's named exception is satisfied literally: t_next_beat shells `node <SKILLS_DIR>/gabe-next/scripts/next.mjs --json` and maps exits 0/1/2/127 to `exit` + `meaning`, so no second reader of PLAN cells is created and §6's 'pulse never re-derives this' holds. Script path retained as the fallback where the server is not registered.",
   "leverage": "low",
   "current_excerpt": "| P4 | Router's next step | `../gabe-next/scripts/next.mjs --json` | whatever it names |",
   "sweep_rationale": "L4's stated exception: `next_beat` literally relays this script with its exit codes mapped to fields, so no second reader of PLAN cells is created. The script path stays named as the fallback where the server is not registered."
  },
  {
   "file": "skills/gabe-pulse/SKILL.md",
   "line": 72,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| P6 | Center coverage ⬜ | `mcp__gabe-kdbp__kdbp_snapshot` `plan.phases[]` (cells as `todo`/`done`, capped at 40), else the PLAN `Center` column | `/gabe-cc-update <phase>` |",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim and the header-resolution argument is sound — Center is optional and positional reads shift. But P6 is a count over EVERY phase and the twins run 40+ phase plans, so the bare row would license reading a capped list as complete (L1); and _state() emits todo/done, never the ⬜/✅ the row's own title uses, which would silently score zero. Both named inline; the spec edit at line 111 carries the long form.",
   "leverage": "low",
   "current_excerpt": "| P6 | Center coverage ⬜ | PLAN `Center` column | `/gabe-cc-update <phase>` |",
   "sweep_rationale": "P6/P7 are the two signals pulse still parses PLAN cells for by hand (P4/P5 already defer to the router). The header-resolved cells remove the column-position risk that makes an optional column dangerous to read positionally. Mirror of the spec edit at §2 P6."
  },
  {
   "file": "skills/gabe-pulse/SKILL.md",
   "line": 73,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| P7 | Red debt ⬜ | `mcp__gabe-kdbp__kdbp_snapshot` `plan.phases[]` (cells as `todo`/`done`, capped at 40), else the PLAN `Red` column | `/gabe-red <phase>` |",
   "survives": true,
   "reason": "CORRECTED, same two grounds as P6. `Red` genuinely sits before `Exec` (plan_rows' _col ordering confirms the optional column), so header resolution is the right fix; but the cap and the _state() token vocabulary must ride with it or the signal reports a silent zero. Raw-column fallback retained.",
   "leverage": "low",
   "current_excerpt": "| P7 | Red debt ⬜ | PLAN `Red` column | `/gabe-red <phase>` |",
   "sweep_rationale": "Same as P6 — `Red` is optional and sits before `Exec`, the position most likely to be misread from a raw table. Mirror of the spec edit at §2 P7."
  },
  {
   "file": "skills/gabe-pulse/SKILL.md",
   "line": 74,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "| P8 | PENDING escalations | `mcp__gabe-kdbp__kdbp_snapshot` `pending` + `.kdbp/PENDING.md` | `/gabe-review deferred` |",
   "survives": true,
   "reason": "Anchor verbatim. pending.open is an exact count with the file's own closure rule; the file read is correctly kept because prio() sorts by priority FIRST, so a low-priority row at Times Deferred 4 can sit outside the ten-row `top` — the escalation tally cannot come from the tool. Keeping both is the L1-correct shape.",
   "leverage": "low",
   "current_excerpt": "| P8 | PENDING escalations | `.kdbp/PENDING.md` | `/gabe-review deferred` |",
   "sweep_rationale": "The snapshot's `open` count is exact and its closure rule is the file's own; the file read stays because the `Times Deferred ≥ 3` tally can fall outside the ten-row `top` list. Mirror of the spec edit at §2 P8."
  },
  {
   "file": "skills/gabe-pulse/SKILL.md",
   "line": 78,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__next_beat",
   "proposed_text": "**P4/P5 defer to `next.mjs` rather than re-deriving the routing decision** — directly, or through `mcp__gabe-kdbp__next_beat`, which relays that same script. Two readers of PLAN",
   "survives": true,
   "reason": "Anchor verbatim (the sentence continues on line 79 unchanged). Without it the P4 row and the binding rule beneath it would disagree about what pulse calls. The relay claim is literally true, so the single-source-of-truth rule is untouched.",
   "leverage": "low",
   "current_excerpt": "**P4/P5 defer to `next.mjs` rather than re-deriving the routing decision.** Two readers of PLAN",
   "sweep_rationale": "Keeps this binding sentence consistent with the P4 row above; without it the table and the rule beneath it disagree about what pulse calls. The sentence continues on the next line unchanged."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 30,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "| Command-center project, no KDBP | a center config exists and declares `paths.ledger` (`mcp__gabe-map__map_status` settles the center half — honest-empty, with its reason, where there is none) | P1–P3, P10, plus any the config names |",
   "survives": true,
   "reason": "Anchor verbatim. map_status does settle center presence and also returns kdbp_present, disambiguating two rows of this gate table in one call. Correctly keeps the `paths.ledger` read — no tool returns it. Worth flagging for the author: find_center only looks for docs/site/center/center.config.json walking up, so it is a floor on center detection, not the gate — which is why this must stay an addition, as proposed, and not a replacement.",
   "leverage": "low",
   "current_excerpt": "| Command-center project, no KDBP | a center config exists and declares `paths.ledger` | P1–P3, P10, plus any the config names |",
   "sweep_rationale": "The gate's center detection is a file probe today; `map_status` answers it and also reports `kdbp_present`, so one call disambiguates two rows of this table. The `paths.ledger` read stays — no tool returns it."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 98,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__next_beat",
   "proposed_text": "`mcp__gabe-kdbp__next_beat` relays this same script with its exit codes mapped to fields — ask it first; the node invocation above stays for a repo where the server is not registered. Report its decision **verbatim**. Exit `1` → its terminal message (no plan / plan complete)\nbecomes the row. Exit `2` → `undetermined — PLAN.json unusable`.",
   "survives": true,
   "reason": "Anchor verbatim across both lines. L4's named exception again: the tool IS the script, and its `meaning` map ('no decision', 'PLAN.json unusable (mirror refused)') matches the exit-1/exit-2 handling this passage already specifies, so §6's no-re-derivation rule at lines 101-102 is untouched. The node fence at line 95 is retained for fork/Explore runs without the server.",
   "leverage": "low",
   "current_excerpt": "Report its decision **verbatim**. Exit `1` → its terminal message (no plan / plan complete)\nbecomes the row. Exit `2` → `undetermined — PLAN.json unusable`.",
   "sweep_rationale": "L4's named exception — the tool IS the script, so §6's \"pulse never re-derives this\" holds unchanged. Fallback retained so a fork/Explore run without the server still resolves P4/P5."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 111,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Phases whose `Center` cell is `⬜` **and** whose `Push` cell is `✅` — shipped but uncovered.\nRead the cells from `mcp__gabe-kdbp__kdbp_snapshot` `plan.phases[]` (header-resolved, so an optional\ncolumn is never read off a shifted position; each cell arrives as `todo` / `active` / `done` / `skipped`,\nnot the raw glyph — `⬜` is `todo`, `✅` is `done`, `n/a` or `⏸` is `skipped`). Its list caps at 40 rows and\nnames the cap — count a longer plan from `.kdbp/PLAN.md` itself, since a capped list is not an absence\nproof.\nColumn absent → `unavailable — no Center column` (normal, not a defect). Clearing command:",
   "survives": true,
   "reason": "CORRECTED. Anchor verbatim across both lines. The cap argument is right and important — P6 counts over every phase and the twins run 40+ — and naming the file as the completion path keeps L1. Added the missing piece: _state() normalises every cell, so a spec that says 'cell is ⬜' while sourcing from plan.phases[] scores zero on every project. Glyph→token map made explicit, including `skipped`, which keeps an `n/a` Center cell out of the debt count exactly as today.",
   "leverage": "medium",
   "current_excerpt": "Phases whose `Center` cell is `⬜` **and** whose `Push` cell is `✅` — shipped but uncovered.\nColumn absent → `unavailable — no Center column` (normal, not a defect). Clearing command:",
   "sweep_rationale": "P6 is a count over EVERY phase, and the twins run 40+ phase plans — so the cap must be stated or the signal silently under-reports. First-look with the file named as the completion path keeps L1 intact."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 117,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Phases whose `Red` cell is `⬜` **and** whose `Exec` cell is not `⬜` — executed without a\ncommitted red checkpoint, read from `mcp__gabe-kdbp__kdbp_snapshot` `plan.phases[]` (header-resolved;\ncells arrive as `todo` / `active` / `done` / `skipped`, not the raw glyph — `⬜` is `todo`; capped at 40\nrows with the cap named — a longer plan is counted from `.kdbp/PLAN.md`).\nColumn absent → `unavailable — no Red column`. Clearing command:",
   "survives": true,
   "reason": "CORRECTED, same grounds as P6. Anchor verbatim; `Red` before `Exec` is the highest-risk positional read, so header resolution earns its place. Token vocabulary added — without it 'Exec cell is not ⬜' evaluates true for every row and P7 would report the whole plan as red debt, the inverse failure of P6's silent zero. The blockquote at lines 121-122 (Red ⬜ + Exec ⬜ is not debt) is unaffected.",
   "leverage": "medium",
   "current_excerpt": "Phases whose `Red` cell is `⬜` **and** whose `Exec` cell is not `⬜` — executed without a\ncommitted red checkpoint. Column absent → `unavailable — no Red column`. Clearing command:",
   "sweep_rationale": "Same construction as P6, and `Red` sits before `Exec` — the optional column whose position shifts every other cell when read positionally. The blockquote below (Red ⬜ + Exec ⬜ is not debt) is unaffected."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 126,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "`.kdbp/PENDING.md` rows with `status=open` — `mcp__gabe-kdbp__kdbp_snapshot` `pending.open` is the exact\ntotal and resolves closure the way the file declares it (a Status verdict token or a `<!-- P<n> resolved -->`\ncomment); its `top` list caps at 10, so the `Times Deferred ≥ 3` tally is still counted from the file.\nReport two numbers: total open, and how many sit",
   "survives": true,
   "reason": "Anchor verbatim. Both halves check out against pending_rows(): `open` is a full-table count with the seven-token CLOSERS rule plus the resolved-comment sibling check, while prio() sorts priority-first, so a medium-priority row at Times Deferred 4 can fall outside `top[:10]`. Splitting the two numbers by source is precisely the L1-honest reading — the count comes free, the escalation tally does not.",
   "leverage": "medium",
   "current_excerpt": "`.kdbp/PENDING.md` rows with `status=open`. Report two numbers: total open, and how many sit",
   "sweep_rationale": "The total comes free and correctly typed from the snapshot; the escalation tally does not, because a low-priority row with 4 deferrals can sort outside the top ten. Stating both keeps the second number honest (L1)."
  },
  {
   "file": "skills/gabe-pulse/references/pulse-spec.md",
   "line": 262,
   "skill": "gabe-pulse",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__next_beat",
   "proposed_text": "| `/gabe-next` | both read PLAN cells | pulse **calls** `next.mjs --json` — directly, or through `mcp__gabe-kdbp__next_beat`, which relays that same script — and quotes it; it never parses cells itself for P4/P5 |",
   "survives": true,
   "reason": "Anchor verbatim. The seams table is the handshake record CLAUDE.md's step-2 walk exists to keep true; leaving it saying only 'calls next.mjs' after edits 26/32 would make the two specs disagree about the seam. The 'never parses cells itself' resolution is unchanged, because the tool relays rather than re-derives.",
   "leverage": "low",
   "current_excerpt": "| `/gabe-next` | both read PLAN cells | pulse **calls** `next.mjs --json` and quotes it; it never parses cells itself for P4/P5 |",
   "sweep_rationale": "The seams table is the handshake record; leaving it saying \"calls next.mjs\" after the P4 edit would make the two specs disagree about the seam, which is the failure the handshake walk exists to catch."
  },
  {
   "file": "skills/gabe-health/SKILL.md",
   "line": 52,
   "skill": "gabe-health",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "5. For Scope Creep, resolve the plan source in priority order — `.kdbp/PLAN.md` active phase via `mcp__gabe-kdbp__phase_context` (it returns the phase's declared `scope` globs, its `Cases:`/`Reach:` records and a capped excerpt of the phase section, so the file references come from a parsed record rather than a hand read; honest-empty without `.kdbp/`) → `.planning/phases/*/PLAN.md` → `docs/plans/*.md` → `docs/brainstorms/*-requirements.md` — and diff against `git diff --stat`. The globs are a FLOOR: a file named in the phase prose but outside `scope` still counts as planned.",
   "reason": "Anchor verbatim at line 52. Tool contract confirmed in kdbp_tools.py t_phase_context: it emits plan_json.scope, records (phase_records lowercases Cases/Reach/Class/Proof/Searched), details_excerpt and pending_in_scope, defaulting to PLAN.json current_phase. L2 holds — the other three paths stay a hand read and _absent() fires without .kdbp/. CORRECTED three ways: restored the 'extract file references from Scope/Description' hint the proposal dropped; details_excerpt is sec[:2000], so 'the phase excerpt already parsed' overclaimed and is now 'a capped excerpt'; and pulled the FLOOR clause down from the spec twin so the lean core cannot be read as letting scope globs narrow what counts as planned (L1). Leverage downgraded from 'high': this is one of six analyses inside one skill.",
   "current_excerpt": "5. For Scope Creep, resolve the plan source in priority order (`.kdbp/PLAN.md` active phase → `.planning/phases/*/PLAN.md` → `docs/plans/*.md` → `docs/brainstorms/*-requirements.md`) and diff against `git diff --stat`.",
   "sweep_rationale": "The first branch of the chain is a hand parse of PLAN.md's active phase block for file references. `phase_context` returns exactly that, already parsed (`plan_json.scope`, `records.reach`, `details_excerpt`) plus `pending_in_scope`. The other three paths stay a hand read (L2: no `.kdbp/` ⇒ the tool says so and the chain continues), so the step still works everywhere."
  },
  {
   "file": "skills/gabe-health/references/health-spec.md",
   "line": 125,
   "skill": "gabe-health",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__phase_context",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "- Read the plan, first match wins: `.kdbp/PLAN.md` (active phase block — extract file references from Scope/Description) → `.planning/phases/*/PLAN.md` → `docs/plans/*.md` → `docs/brainstorms/*-requirements.md`. For the `.kdbp/PLAN.md` branch, ask `mcp__gabe-kdbp__phase_context` instead of parsing the block by hand — it returns `plan_json.scope` (the phase's declared globs), the `records.reach` and `records.cases` lines, a `details_excerpt` of the phase section (capped at 2,000 chars — read PLAN.md itself when the section runs longer), and the `pending_in_scope` rows already owed there; honest-empty without `.kdbp/`, and a FLOOR — a file named in the phase prose but outside `scope` still counts as planned. Print the source used in the section header (`Scope Creep — Phase 3 (source: .kdbp/PLAN.md)`); if none found: `no plan found (searched 4 paths) — skipping scope analysis`.",
   "reason": "Anchor verbatim at line 125. Field names verified exactly: plan_json.scope, records.reach, details_excerpt, pending_in_scope. The FLOOR clause is the L1 guard and it is present. The 4-path fallback and the 'no plan found' honest-empty line are preserved verbatim (L2). CORRECTED: restored the '(active phase block — extract file references from Scope/Description)' parenthetical the proposal deleted — line 126 only says 'Extract file references from the plan', so the Scope/Description hint would otherwise be lost — and named the 2,000-char excerpt cap so a long phase section is not silently truncated into a false 'planned' set.",
   "current_excerpt": "- Read the plan, first match wins: `.kdbp/PLAN.md` (active phase block — extract file references from Scope/Description) → `.planning/phases/*/PLAN.md` → `docs/plans/*.md` → `docs/brainstorms/*-requirements.md`.",
   "sweep_rationale": "The binding twin of the SKILL.md step; both must move together or the spec and the core drift. The `Reach:` record is a better 'planned surface' than prose extraction (it is the record /gabe-review already prices as REACH DRIFT), and the FLOOR clause keeps L1: the tool's scope globs never narrow what counts as planned."
  },
  {
   "file": "skills/gabe-health/SKILL.md",
   "line": 53,
   "skill": "gabe-health",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "6. For Deferred Items, ask `mcp__gabe-kdbp__kdbp_snapshot` first — its `pending` block gives open/closed counts and the top 10 rows sorted by priority then Times Deferred, closure-aware (a `<!-- P<n> resolved -->` comment counts as closed, not just a Status verdict) — then read `.kdbp/PENDING.md` for the full priority tally and row ages, which the snapshot does not carry; skip silently when `.kdbp/` is absent. Also read `.kdbp/MAINTENANCE.md` if a legacy copy is present (retired from the default KDBP inventory in A2) — skip that sub-check silently when absent.",
   "reason": "Anchor verbatim at line 53. Verified in t_kdbp_snapshot: pending = {open, closed, columns, top[:10], note}, sorted by (priority, -times_deferred), with no date field — so the proposal is right that 'Oldest open: D1 (45 days)' still needs the file. The MAINTENANCE clause and the silent skip are preserved verbatim (L2). CORRECTED: named the 10-row cap explicitly, and named the real added value — pending_rows() closure detection reads the `<!-- P<n> resolved -->` comment convention that a hand read of PENDING.md misses. Leverage downgraded from 'high': the file is read either way, so this buys a closure-correct pre-sort, not a token saving.",
   "current_excerpt": "6. For Deferred Items, read `.kdbp/PENDING.md` if `.kdbp/` exists; skip silently otherwise.",
   "sweep_rationale": "first-look, not replace: the snapshot's `pending.top` is capped at 10 and carries no dates, so the 'Oldest open: D1 (45 days)' line and a >10-row priority tally still need the file. The snapshot answers the escalation flag (`times_deferred`) and the counts in one read instead of a full PENDING.md load."
  },
  {
   "file": "skills/gabe-health/references/health-spec.md",
   "line": 160,
   "skill": "gabe-health",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "- Ask `mcp__gabe-kdbp__kdbp_snapshot` first — `pending{open, closed, columns, top}` (top = 10 rows sorted priority then Times Deferred, closure-aware) answers the counts and the escalation candidates in one read — then read `.kdbp/PENDING.md` itself for the full priority tally, for row ages (the snapshot carries no dates), and for any Times Deferred ≥ 2 row that fell outside the top 10",
   "reason": "Anchor verbatim at line 160. Field shape confirmed against t_kdbp_snapshot. CORRECTED: the proposal's conditional ('when more than 10 rows are open') was illusory — the output format at line 171 requires 'Oldest open: D1 (45 days)', so the file is read unconditionally; and a low-priority row with Times Deferred ≥ 2 can sort outside the top 10, which the next bullet ('Flag items approaching escalation (Times Deferred >= 2)') would then miss. Both facts now stated. Leverage matched to its SKILL.md twin.",
   "current_excerpt": "- Read `.kdbp/PENDING.md` — count open items by priority",
   "sweep_rationale": "Binding twin of the SKILL.md step. Naming the cap and the missing dates in the spec is what keeps the analysis honest when a project has more open debt than the snapshot returns."
  },
  {
   "file": "skills/gabe-health/SKILL.md",
   "line": 40,
   "skill": "gabe-health",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "In KDBP projects, open with `mcp__gabe-kdbp__kdbp_snapshot` — the phase table, the open PENDING rows, the last LEDGER rows and the DECISIONS row count in one read — then open the four KDBP surfaces (DECISIONS.md, SCOPE.md §14, RULES.md, PENDING.md) the findings target; proposed, never auto-written from the fork.",
   "reason": "Anchor verbatim at line 40. The tool reads git · PLAN · PENDING · LEDGER · DECISIONS — it does NOT read SCOPE.md or RULES.md, and its decisions field is a row COUNT only, so the proposal is honest in keeping all four surfaces opened; a contradiction is proved from their text, which the snapshot cannot carry. The clause sits inside an already-KDBP-gated sentence and _absent() fires without .kdbp/ (L2). 'proposed, never auto-written from the fork' preserved verbatim. Minor correction: 'DECISIONS count' → 'DECISIONS row count' to match what the field actually is.",
   "current_excerpt": "In KDBP projects, findings target the four KDBP surfaces (DECISIONS.md, SCOPE.md §14, RULES.md, PENDING.md) — proposed, never auto-written from the fork.",
   "sweep_rationale": "The debt lens currently opens SCOPE + PLAN + PENDING + DECISIONS by hand to find where decisions were never made. The snapshot is the orientation read (which phases are live, how much debt is open, how many decisions are recorded) at ~1k tokens; the four surfaces are still opened, because a contradiction is proved from their text, not from a count."
  },
  {
   "file": "skills/gabe-health/references/health-spec.md",
   "line": 210,
   "skill": "gabe-health",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "Churn Hotspots and Coupling Clusters, which render no per-finding step today, must each name theirs per row. Where the project carries a command center, run `mcp__gabe-map__owner_of` once over the flagged paths (and the fix-concentration directory — it takes a directory and returns per-entity file counts plus its `unclaimed_in_census` list) and name the owning entity beside each path; a path the map does not claim is itself the census gap the tool names. The entity is an ANNOTATION on the finding, never a replacement: the git numbers stay the finding, and the `→ next` step stays the one the Integration table gives.",
   "reason": "Anchor verbatim at line 210. t_owner_of confirmed: accepts `paths` (list or string), branches on is_dir to return {kind:'dir', mapped_files, owners, unclaimed_in_census}, and on an unowned file returns the 'unowned by the map — the map is BLIND here' note, so both halves of the claim are real. L2 guard present; no center ⇒ _absent and the paths render as today. CORRECTED one real defect: the proposal said a god file 'routes its `→ next` at that entity', which contradicts the Integration table two sections below (god file → `/gabe-roast architect [file]`). Reworded so the entity is an annotation and the table keeps ownership of `→ next`. Leverage downgraded from 'high' — one paragraph in one skill's output-format section.",
   "current_excerpt": "Churn Hotspots and Coupling Clusters, which render no per-finding step today, must each name theirs per row.",
   "sweep_rationale": "One edit covers analyses 1-4 at once — it is the rule that governs every per-finding `→ next` step. `owner_of` is the exact tool for 'which entity owns these paths or this directory; where the map is blind', turning a path-level report into an entity-routed one. Read-only and honest-empty: no center ⇒ the paths render as they do today."
  },
  {
   "file": "skills/gabe-health/references/health-spec.md",
   "line": 83,
   "skill": "gabe-health",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "survives": true,
   "leverage": "low",
   "proposed_text": "Report a pair ONLY with counts copied from this run's output. If the command did not execute this run, print `coupling analysis skipped` — never estimate or reuse the example percentages below. For a pair above threshold, and only where the project carries a command center, `mcp__gabe-map__blast_radius` with BOTH files passed in `files` says whether they are structurally related — one shared entity in `touched_entities`, a link in `fk_neighbor_entities`, or a shared row in `endpoints_reached` upgrades the row from *co-changes* to *coupled*. Its silence proves nothing (the map is a floor) and the co-change count stands on its own.",
   "reason": "Anchor verbatim at line 83. The tool is the right one but the proposal's INVOCATION was wrong and I corrected it: t_blast_radius returns entities/functions/models/endpoints/tests/fe for the files you pass — it never returns 'other files reachable from A', so 'blast_radius on the first file says whether the second is reachable' does not hold. Passing BOTH files (the `files` arg accepts a list) makes touched_entities/fk_neighbor_entities/endpoints_reached answer the question directly. Also added the missing L2 center guard. The L1 silence clause was already correct and is kept verbatim. Leverage lowered to 'low': it fires only on pairs already above the 60% threshold, in one analysis of one skill.",
   "current_excerpt": "Report a pair ONLY with counts copied from this run's output. If the command did not execute this run, print `coupling analysis skipped` — never estimate or reuse the example percentages below.",
   "sweep_rationale": "Co-change is a git signal that cannot tell 'these two are wired together' from 'these two are edited by the same person'. `blast_radius` supplies the structural half. The silence clause is L1 — an absent edge never downgrades a measured co-change rate."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 203,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "survives": true,
   "leverage": "high",
   "proposed_text": "4b. D1 verdicts need receipts: claim Contained/Local only after opening the files/configs the change names. `mcp__gabe-map__blast_radius` is a receipt for what the MAP reaches and `mcp__gabe-map__who_calls` on the symbols the change names is the confirming arm the map's own floor note asks for before `contained` is trusted — but a claim that nothing else uses X still needs the `grep -rn` that found nothing, because a tool's silence is not absence. With `Checked: none — description-only`, append `(unverified)` to the D1 classification and name what to open to confirm it; tool answers alone do not clear `(unverified)` — a file opened this assessment does.",
   "reason": "Anchor verbatim at line 203. Both tools match: t_blast_radius's own `floor` string literally reads 'run who_calls on the changed symbols before trusting contained', so the confirming-arm framing is the tool's own contract, not an invention. The grep clause is L1 verbatim and is why this edit is worth keeping. gabe-assess carries no `context: fork` / `agent: Explore` flag, so who_calls's default gitignored delta emit is not a read-only violation here. TWO CORRECTIONS: (a) killed the new `(map-floor)` stamp — the output templates at lines 136 and 175 enumerate `Checked:` and `(unverified)` only, and a third stamp changes the output contract without support from the file; replaced with the same rule in existing vocabulary. (b) Leverage downgraded from 'suite-wide' to 'high': rule F reserves suite-wide for execution-contract.md or a file many skills READ, and no other skill reads assess-spec.md — roast's pre-roast gate and health's Integration table invoke the /gabe-assess command, they do not point at this file. Reach is still genuinely wide via direction-guard.sh.",
   "current_excerpt": "4b. D1 verdicts need receipts: claim Contained/Local only after opening the files/configs the change names. With `Checked: none — description-only`, append `(unverified)` to the D1 classification and name what to open to confirm it.",
   "sweep_rationale": "The single highest-leverage line in the batch: this receipts rule is reached by the direction-guard hook on every steering prompt in every KDBP project, by /gabe-roast's pre-roast gate, and by /gabe-health's Integration table — one edit changes what all of them do. The grep clause is L1 verbatim. NOTE for the operator: `(map-floor)` is new vocabulary modelled on the existing `(unverified)` stamp — cut it if you do not want a third stamp."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 54,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "survives": true,
   "leverage": "high",
   "proposed_text": "### D1: Blast Radius\n\nWhat does this change touch?\n\nWhere the project carries a command center, ask `mcp__gabe-map__blast_radius` with the paths the change names before classifying: `touched_entities`, `endpoints_reached`, `models_defined`, `tests_reaching` and `fe_pieces` are a FLOOR reading, and its own `reading` (`contained | local | cross-cutting | unmapped`) is a PROPOSAL for the table below, never the verdict. Two rules keep it honest: `unmapped` is not **Contained** — it means the map does not claim those paths — and **External** is never derivable from a map (deployed environments, other teams, users, third-party services are judgment, always).",
   "reason": "Anchor verbatim across lines 52-54. Every field name checked against t_blast_radius and every one exists exactly as written: touched_entities, endpoints_reached, models_defined, tests_reaching, fe_pieces, reading. The four `reading` values are exactly the four the code computes ('contained' when n_ent<=1 with no external fk_neighbors, else 'local'/'cross-cutting'; 'unmapped' when files are unowned and nothing is touched). The two guards carry the edit: `unmapped` genuinely means unowned-by-the-map, and no map field can express External. L1 and L2 both held. Survives unchanged.",
   "current_excerpt": "### D1: Blast Radius\n\nWhat does this change touch?",
   "sweep_rationale": "`blast_radius` returns the same three words D1's scale uses, which is both the reason to wire it and the trap to guard: an agent will otherwise read `unmapped` as `Contained` and lose `External` entirely. Stating the mapping in the spec is what makes the tool safe here."
  },
  {
   "file": "skills/gabe-assess/SKILL.md",
   "line": 46,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "survives": true,
   "leverage": "high",
   "proposed_text": "4. Read enough to understand what the change touches — files, configs, environments — before classifying D1; where the project carries a command center, orient with `mcp__gabe-map__center_overview` and take the floor reading from `mcp__gabe-map__blast_radius` on the paths the change names, then open them.",
   "reason": "Anchor verbatim at line 46. Both tools match the roster: center_overview is orientation-by-entity, blast_radius prices named paths. 'then open them' preserves the receipts rule at 4b — the tools change what you open first, not whether you open (L1 intact). L2 guard present; both return _absent honest-empty without a center. Survives unchanged. Note: the rationale's '≤600-token' claim is not in the proposed_text and I did not add it — no such cap is stated anywhere in the server.",
   "current_excerpt": "4. Read enough to understand what the change touches — files, configs, environments — before classifying D1.",
   "sweep_rationale": "The lean-core mirror of behavior rule 3. `center_overview` is the ≤600-token orientation-by-entity read that tells an assessor what area the change lands in before any file is opened; blast_radius prices the specific paths. Opening the files stays — the tools change what you open first, not whether you open."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 198,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "survives": true,
   "leverage": "high",
   "proposed_text": "3. Read enough to understand what the change touches — files, configs, environments. Where the project carries a command center, the first look is a tool: `mcp__gabe-map__center_overview` for orientation by entity, `mcp__gabe-map__blast_radius` on the paths the change names for the floor reading. Honest-empty without a center — then the reading is only the files you opened.",
   "reason": "Anchor verbatim at line 198. Binding twin of the SKILL.md step, and it must move with it or the two files disagree about method. The honest-empty sentence is the strongest L2 clause in the batch and it matters most here, because direction-guard.sh dispatches /gabe-assess brief on non-center projects constantly. Survives unchanged.",
   "current_excerpt": "3. Read enough to understand what the change touches — files, configs, environments.",
   "sweep_rationale": "Binding twin of SKILL.md step 4. The honest-empty clause (L2) matters here more than anywhere: /gabe-assess fires on non-center projects constantly via the direction guard, and must not stall waiting for a map."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 104,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "survives": true,
   "leverage": "high",
   "proposed_text": "From the change description, extract the anticipated file paths (explicit paths, inferred `new file X.py`, or \"add Y under Z/\"). Run them through `mcp__gabe-map__owner_of` first — for a path that does not exist yet it answers from `config_glob_owners` (which entity's code globs would claim it), so a path no glob claims is drift evidence before STRUCTURE.md is even consulted; for anticipated API ROUTES, `mcp__gabe-map__entity_shape` with `domain` says which entity owns that URL domain, the same check `/gabe-review` prices later as ENTITY-SHAPE DRIFT. The map arm is advisory — STRUCTURE.md Allowed Patterns remain the verdict, and D5 still skips silently when that file is absent. For each:",
   "reason": "Anchor verbatim at line 104. Both tools verified in source. t_owner_of on a not-yet-existing path: is_dir is False, the file_owners lookup is empty, and it falls through to config_glob_owners built from work_scope.entity_code_globs — so it genuinely answers for anticipated paths, which is the non-obvious claim and it holds. t_entity_shape's `domain` arg returns {segment, owners, candidate}, and here the usage is CORRECT because D5's anticipated routes are backend API routes, exactly what entity_shape's endpoint corpus covers (contrast the myopic edits below, which misapply the same tool to UI routes). D5's skip-silently semantics and STRUCTURE.md's primacy are preserved (L2; L3 — this stays advisory, not a rail). CORRECTED: named `config_glob_owners` as the field that answers when no file exists yet, and said 'API ROUTES' so the tool is not later stretched to frontend routes.",
   "current_excerpt": "From the change description, extract the anticipated file paths (explicit paths, inferred `new file X.py`, or \"add Y under Z/\"). For each:",
   "sweep_rationale": "D5 already answers 'does this path belong somewhere known' against STRUCTURE.md; `owner_of` and `entity_shape` answer the same question against the map, at the cheapest moment (before code). Wording it inside D5 makes it inherit D5's existing skip semantics, so no contract changes. It also moves ENTITY-SHAPE DRIFT detection one beat earlier than /gabe-review."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 81,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "What must be true or verified before this change is safe?\n\nIn a KDBP project, open with `mcp__gabe-kdbp__kdbp_snapshot` — a dirty tree, unpushed commits, a phase mid-flight, or an open PENDING row over the same files (top 10 by priority) are prerequisites the change inherits, and they are one read away. Honest-empty without `.kdbp/`.",
   "reason": "Anchor verbatim at line 81. Every named signal exists in t_kdbp_snapshot: git.dirty{modified,untracked,total}, git.ahead/behind, plan.phases[].cells, pending.top[].file. It answers D3's 'state that must be checked' bullet directly, not adjacently. L2 guard present. CORRECTED: added '(top 10 by priority)' — pending.top is capped, so an open row over the same files can fall outside it and the spec must not imply the snapshot is exhaustive.",
   "current_excerpt": "What must be true or verified before this change is safe?",
   "sweep_rationale": "D3 asks for 'state that must be checked' and today leaves the assessor to guess which state. `kdbp_snapshot` is literally 'where does the project stand' — branch/ahead/behind/dirty, phase table, open PENDING. This is the prerequisite class most often missed: assessing a change while the tree already carries someone else's half-finished phase."
  },
  {
   "file": "skills/gabe-assess/references/assess-spec.md",
   "line": 224,
   "skill": "gabe-assess",
   "klass": "first-look",
   "tool": "mcp__gabe-map__blast_radius",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "   - Are they independent or coupled? (With a command center, `mcp__gabe-map__blast_radius` per change: overlapping `touched_entities` or a shared file is coupling evidence; disjoint radii are a floor, not proof of independence.)",
   "reason": "Anchor verbatim at line 224, three-space indent preserved. Tool matches — one call per change, then compare touched_entities. The 'not proof of independence' clause is the L1 guard and is the reason this survives: without it the map's silence would license a 'these are independent, ship both' call. CORRECTED: added the center guard for L2 consistency with the other assess edits.",
   "current_excerpt": "   - Are they independent or coupled?",
   "sweep_rationale": "Batch mode currently decides coupling by judgment alone. Overlapping blast radii are cheap, concrete evidence for the Combined line. The 'not proof of independence' clause is L1 — the map's silence must never license a 'these are independent, ship both' call."
  },
  {
   "file": "skills/gabe-roast/references/roast-spec.md",
   "line": 37,
   "skill": "gabe-roast",
   "klass": "first-look",
   "tool": "mcp__gabe-map__cases_for",
   "survives": true,
   "leverage": "high",
   "proposed_text": "The reviewer adopts the perspective fully — think like that person, worry about what they worry about, catch what they would catch. Where the project carries a command center, the perspective's cheapest opening question is a tool: an **Architect** asks `mcp__gabe-map__center_overview` and `blast_radius`; a **QA/Testing Lead** asks `mcp__gabe-map__cases_for` before calling a behavior untested; a **Sweeper** or **Maintainer** asks `mcp__gabe-map__map_census` for what the map does not claim. Each answer is a FLOOR that points the reading — the gap itself is still proved from files opened this roast, and an absence claim still needs its recorded 0-hit search (rule 6).",
   "reason": "Anchor verbatim at line 37, and it sits directly above the canonical archetype block so it covers that set too. All three tools match their claimed questions: cases_for returns which C-ids cover X plus the next-id floor (the QA 'X has no test' finding class exactly), map_census returns unclaimed files/models/routes and the phrasing 'what the map does not claim' is the tool's own contract, not an overreach. None of the three emit anything, which matters because gabe-roast declares `context: fork` + `agent: Explore`. CORRECTED one L1 risk: 'the perspective's first question is a tool, not a grep' could be read as displacing SKILL.md step 6's 'or a recorded 0-hit search for absence claims'. Softened to 'cheapest opening question' and restored the 0-hit-search pointer explicitly.",
   "current_excerpt": "The reviewer adopts the perspective fully — think like that person, worry about what they worry about, catch what they would catch.",
   "sweep_rationale": "One sentence teaches every perspective its cheapest opening move and covers the archetype set below it too. `cases_for` is the sharpest of the three: a QA roast's most common finding class is 'X has no test', which today costs a full corpus grep and is exactly what cases_for answers (which C-ids cover X, plus the next-id floor). The closing clause preserves the kill-gate: a tool answer is never Evidence."
  },
  {
   "file": "skills/gabe-roast/references/roast-spec.md",
   "line": 21,
   "skill": "gabe-roast",
   "klass": "first-look",
   "tool": "mcp__gabe-map__owner_of",
   "survives": true,
   "leverage": "high",
   "proposed_text": "When the target is a file or folder, read it fully before starting. For folders, read enough files to understand the system (entry points, core logic, configuration) — where the project carries a command center, `mcp__gabe-map__owner_of` names the entity that owns the directory and `mcp__gabe-map__entity_context` returns its endpoints, models, files and coverage, so the reading order is chosen rather than guessed. The answer never counts as having read a file: it says where to start, and every citation still comes from a file opened this roast.",
   "reason": "Anchor verbatim at line 21. Both tools match: t_owner_of's is_dir branch returns per-entity file counts for a directory, t_entity_context returns the entity slice plus a coverage block. Neither emits (fork/Explore safe). The second sentence is the load-bearing guard and it is correct — without it an agent would cite a map answer into the read ledger at lines 221-223 and defeat the kill-gate. L1 and L2 both held. Survives unchanged.",
   "current_excerpt": "When the target is a file or folder, read it fully before starting. For folders, read enough files to understand the system (entry points, core logic, configuration).",
   "sweep_rationale": "'Read enough files' is the step where a roast silently decides what it will never look at. `owner_of` + `entity_context` make that choice evidence-led. The second sentence is the guard the read ledger and kill-gate depend on — without it an agent will cite a map answer as a read file."
  },
  {
   "file": "skills/gabe-roast/references/roast-spec.md",
   "line": 224,
   "skill": "gabe-roast",
   "klass": "first-look",
   "tool": "mcp__gabe-map__touches",
   "survives": true,
   "leverage": "high",
   "proposed_text": "4. If the target references other files (imports, links, config), read those too — gaps often hide at boundaries. Where the project carries a command center, `mcp__gabe-map__touches` on the target file and `mcp__gabe-map__outline` (`owners`, `models_referenced`, `tests_reaching`) name those boundaries without opening anything, and `mcp__gabe-map__who_calls` with `direction=out` walks the callees — that direction never emits a delta, so the roast stays read-only. What the tools name, you still open before citing.",
   "reason": "Anchor verbatim at line 224. The sharpest edit in the batch, and its least obvious claim checks out in source: t_who_calls sets `transitive = direction == 'out' or depth != '1'` and `emit = ... and not transitive`, so direction=out provably never appends to .kdbp/map-deltas.jsonl — the roast's `context: fork` + `agent: Explore` read-only claim survives. t_outline's returned keys are exactly owners, models_referenced, tests_reaching; t_touches accepts a file path as target. The closing sentence keeps the citation law (L1). CORRECTED: added the missing L2 center guard — touches/outline return _absent without a center and who_calls answers from graft+grep regardless, so the step works either way, but the spec should say so.",
   "current_excerpt": "4. If the target references other files (imports, links, config), read those too — gaps often hide at boundaries.",
   "sweep_rationale": "Boundary-following is the most expensive hand-work in a roast and the map's core competence. The `direction=out` note is load-bearing: only `direction=in depth=1` may append to the gitignored delta accumulator, and gabe-roast declares itself fork/read-only — naming the safe direction keeps that claim true."
  },
  {
   "file": "skills/gabe-roast/SKILL.md",
   "line": 38,
   "skill": "gabe-roast",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "survives": true,
   "leverage": "high",
   "proposed_text": "5. Read before attacking: where the project carries a command center, orient first with `mcp__gabe-map__center_overview` (and `owner_of` on the target directory) and pick entry points with `mcp__gabe-map__outline` — then list the folder tree, open every entry point + config, never cite a file not opened this session (a tool answer is not a read), print a read ledger (`Read: 14/38 files — skipped: ...`) directly under the perspective line.",
   "reason": "Anchor verbatim at line 38. Lean-core mirror of spec rule 3 (lines 220-223); all three tools match their claimed questions and none emit. The inline '(a tool answer is not a read)' restates the citation law at the one line a roast actually loads before it starts opening files — that is what keeps the read-ledger count honest (L1). L2 guard present. Survives unchanged.",
   "current_excerpt": "5. Read before attacking: list the folder tree, open every entry point + config, never cite a file not opened this session, print a read ledger (`Read: 14/38 files — skipped: ...`) directly under the perspective line.",
   "sweep_rationale": "Lean-core mirror of behavior rule 3. The parenthetical restates the citation law inline, because this is the one line a roast actually loads before it starts opening files."
  },
  {
   "file": "skills/gabe-myopic/method.md",
   "line": 26,
   "skill": "gabe-myopic",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "survives": true,
   "leverage": "medium",
   "proposed_text": "For code input, the information state at step K = the LITERAL strings the code renders — read the\ncomponent AND its i18n/string source, and quote ≥1 on-screen string per step in the Step ledger. If\nyou cannot quote what the screen says, you have not reconstructed the step. Where the project carries\na command center, `mcp__gabe-map__find` (kinds `screen` and `fe`) locates the component's file and\n`mcp__gabe-map__outline` then names its definitions — but the map carries names, spans, signatures and\nowners, never rendered text, so the quote always comes from the source you opened.",
   "reason": "Anchor exists verbatim across lines 26-27 (the excerpt stops at a sentence boundary mid-line; line 27 continues 'If' into line 28, and the proposed_text correctly reabsorbs that continuation, so it drops in cleanly over lines 26-28). find's `screen` and `fe` kinds are real filter values (t_find adds both; `kinds` accepts a string). The guard is the edit's whole value and it is verified true: t_outline returns span, kind, name, signature, returns, access_ops and a 120-char doc — no string literals anywhere — so the quote requirement provably cannot be satisfied from the map. CORRECTED: t_outline REQUIRES a repo-relative `file` argument and raises MapStop without one, so it cannot 'locate' anything; reworded so find locates and outline reads. Leverage downgraded from 'high' — one paragraph in one manual-only skill.",
   "current_excerpt": "For code input, the information state at step K = the LITERAL strings the code renders — read the\ncomponent AND its i18n/string source, and quote ≥1 on-screen string per step in the Step ledger.",
   "sweep_rationale": "The tools cut the cost of finding WHICH component renders step K; they cannot answer WHAT it says. Stating both halves in one place is what stops a future agent from trying to satisfy the quote requirement out of `outline` (which returns signatures and docs, not string literals) — the quote is the skill's whole evidentiary base."
  },
  {
   "file": "skills/gabe-imagine/SKILL.md",
   "line": 46,
   "skill": "gabe-imagine",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "proposed_text": "| **I1 · dissect** | angle-blind parts census → spheres → seats (person + question + refusal) → gravity worlds → the mass test. For code subjects the census is entities, functions, endpoints, workflows — where a command center exists, ask the map FIRST (`mcp__gabe-map__center_overview`, then `entity_context` per entity); its answer is a FLOOR, so count first, cut never, and measure by hand what the map does not carry | `dissection/dissection-method.md` + `dissection/seat-archetypes.md` |",
   "survives": true,
   "reason": "Anchor verbatim at line 46 (checked full row). center_overview does answer the entity roster with counts/coverage and entity_context opens one entity's endpoints/models/files, so the tool matches the census question. CORRECTED: the original proposal invented the phrase 'this census counts first'; the file's own machine-2 discipline (line 22) reads 'count first, cut never' — swapped to the spec's own words, tightened to row length, and 'where a center exists' made the explicit guard so the step still works honest-empty (L2). Floor clause retained so no tool answer stands as absence proof (L1). Leverage marked low, not high: this is a summary row whose binding reference (dissection-method.md) carries the same instruction in the two edits below — its marginal value is the always-loaded pointer, not new content.",
   "leverage": "low",
   "current_excerpt": "angle-blind parts census → spheres → seats (person + question + refusal) → gravity worlds → the mass test. For code subjects the census is entities, functions, endpoints, workflows",
   "sweep_rationale": "I1 is the binding flow row every app/code-map run passes through, and its census is exactly the entity/function/endpoint inventory center_overview + entity_context return in two calls instead of a hand-walk of the codebase. Kept as first-look, not replace: machine 2 is angle-blind and counts first, so the map's floor cannot be the final ledger (L1)."
  },
  {
   "file": "skills/gabe-imagine/references/dissection/dissection-method.md",
   "line": 83,
   "skill": "gabe-imagine",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "proposed_text": "- **system/app** — entities, stores, surfaces, jobs; the center's committed\n  data (archmap/registry) is the census when one exists — READ through the map\n  tools first, never re-derived by hand: `mcp__gabe-map__map_status` says\n  whether there is a map here at all, `center_overview` gives the entity roster\n  with counts and coverage, `entity_context` opens one entity's endpoints,\n  models and files. What comes back is a FLOOR — count first, cut never — so\n  what the map does not carry is still measured by hand; where the tools do not\n  answer, the committed JSON is still the census.",
   "survives": true,
   "reason": "Anchor verbatim at lines 83-84. Tool match is exact: center_overview is spec'd as 'orientation by ENTITY: rank · status · endpoints · models · schemas · files · coverage' (map-spec §graft equivalents), which is precisely 'entities, stores, surfaces' — and map_status keeps it honest-empty (L2). CORRECTED on two counts: (1) the original text said the census is 'never re-derived from its JSON', which DELETES the only path when the server is unregistered or disabled for the project (~/.claude.json disabledMcpServers) even though the center exists — the JSON fallback is restored; (2) klass downgraded replace → first-look, because with the fallback and the by-hand remainder the tool no longer stands alone. 'count first, cut never' taken from the file's own line 22.",
   "leverage": "medium",
   "current_excerpt": "- **system/app** — entities, stores, surfaces, jobs; the center's committed\n  data (archmap/registry) is the census when one exists.",
   "sweep_rationale": "The line already names the committed center data as the census source but leaves the agent to open archmap.json/adoption.json by hand — the exact hand-work the two orientation tools answer directly and cheaper (center_overview is ≤600 tokens vs a multi-MB archmap read). Replacement keeps the floor clause so absence in a tool answer never closes the census (L1), and map_status keeps it honest-empty on a project with no center (L2)."
  },
  {
   "file": "skills/gabe-imagine/references/dissection/dissection-method.md",
   "line": 87,
   "skill": "gabe-imagine",
   "klass": "first-look",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "- **code map** — functions/modules/endpoints and their call/data edges; the\n  census is measured, never recalled. Where a command center exists, ask the map\n  first — `mcp__gabe-map__find` + `outline` for the definitions,\n  `mcp__gabe-map__who_calls` for the call edges (graft callers ∪ word-boundary\n  grep, hits marked code vs prose) — then `grep -rn`, which stays the census's\n  absence proof: only grep proves a part is not there.",
   "survives": true,
   "reason": "Anchor verbatim at lines 87-88. Strongest match in the batch: the line asks for 'functions/modules/endpoints and their call/data edges', which is literally outline (definitions with span + signature + access ops) and who_calls (the two-arm reach, code-vs-prose marked). L1 is honoured harder than the original wording — grep -rn is kept in the same sentence AND named as the absence proof (E1). CORRECTED: added the 'where a command center exists' guard for L2 parity with the sibling bullet above it, and dropped the proposal's dangling '(both arms, hits marked...)' aside into the who_calls clause where it belongs.",
   "leverage": "medium",
   "current_excerpt": "- **code map** — functions/modules/endpoints and their call/data edges; the\n  census is measured (grep/registry), never recalled.",
   "sweep_rationale": "This is the census for the one subject shape gabe-imagine draws directly from code (\"the hook router's callers\" is a named usage example), and it currently sends the agent to grep for call edges that who_calls returns as index ∪ grep with code-vs-prose marking. The grep leg is kept verbatim as the absence proof (L1, E1)."
  },
  {
   "file": "skills/gabe-imagine/references/dissection/dissection-method.md",
   "line": 85,
   "skill": "gabe-imagine",
   "klass": "first-look",
   "tool": "mcp__gabe-map__touches",
   "proposed_text": "- **use case** — the steps, the actors touched, the payloads exchanged, the\n  failure gates along the walk. Where a command center exists, name the walk's\n  entry with `mcp__gabe-map__find` and take the actors touched from `touches`\n  (an endpoint, model, file or case names its owners, r/w functions, endpoints,\n  tests and edges); read `cases_for` as a HINT at which failures are already\n  declared, never as the gate census. All three are floors — the walk through\n  the code is what finds the payloads and the gates.",
   "survives": true,
   "reason": "Anchor verbatim at lines 85-86, and touches genuinely answers 'the actors touched' (spec §5.3: owners, r/w functions, endpoints, tests, edges, for a file/model/function/entity/endpoint/case). CORRECTED: the proposal's '(or `blast_radius` for the whole hop)' is KILLED on the tool contract — blast_radius takes `files` defaulting to the worktree diff vs HEAD, i.e. it prices a CHANGE, not a use-case walk; a dissection census has no diff to hand it. cases_for kept only in its honest hint framing (it returns C-ids covering X, not the failure-gate list), and the closing floor sentence made explicit so nothing here can stand as absence proof (L1). Leverage low: the two things the line most needs — payloads and failure gates — no tool answers.",
   "leverage": "low",
   "current_excerpt": "- **use case** — the steps, the actors touched, the payloads exchanged, the\n  failure gates along the walk.",
   "sweep_rationale": "\"The actors touched\" on a real app use case (\"adding a transaction in gustify\") is literally what touches/blast_radius answer, and the skill today derives it by reading the flow by hand. cases_for is offered only as a hint at real failure paths — the floor language stops it standing in for the gate census (L1)."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 14,
   "skill": "gabe-init",
   "klass": "replace",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "1. If `.kdbp/` already exists, show status — `mcp__gabe-kdbp__kdbp_snapshot` names it in one call (branch · PLAN phase-table states · open PENDING rows · last LEDGER rows, under 1k tokens); with no server registered, read `.kdbp/PLAN.md` + `PENDING.md` + `LEDGER.md` as before — and ask: \"Reset, update, or skip init?\"",
   "survives": true,
   "reason": "Anchor verbatim at line 14. kdbp_snapshot literally answers 'where does the project stand' (branch/phase table/PENDING/LEDGER) — an exact match for the unmethodded 'show status'. The branch only fires when `.kdbp/` exists, so L2 holds and the file-read fallback covers an unregistered server; no rail, no absence claim (L1/L3 clean). CORRECTED: dropped the proposal's trailing 'reset is destructive…' sentence — line 15 immediately below already says reset 'loses LEDGER history and any active plan', so it was a restatement inside the binding spec.",
   "leverage": "medium",
   "current_excerpt": "1. If `.kdbp/` already exists, show status and ask: \"Reset, update, or skip init?\"",
   "sweep_rationale": "\"show status\" names no method today, so a session invents one (or shows nothing) right before offering a destructive reset that loses LEDGER history and an active plan. kdbp_snapshot IS that status, in one call. The file-read fallback keeps the step working with no server registered (L2)."
  },
  {
   "file": "skills/gabe-init/SKILL.md",
   "line": 26,
   "skill": "gabe-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "3. If `.kdbp/` already exists, show where the project stands (`mcp__gabe-kdbp__kdbp_snapshot` — branch, phase table, open PENDING, last LEDGER rows; the `.kdbp/` files themselves when no server is registered), ask reset / update / skip and follow the mode routing table (each mode runs a fixed step sequence — reset: full create → CLAUDE.md → .gitignore seed → hooks → project type → readiness report; update: scan-missing → schema migration → hooks → condensed Update Report, skipping the full create/project-type steps).",
   "survives": true,
   "reason": "Anchor is a PREFIX of line 26, not the whole line — as written the proposal would have DELETED the line's parenthetical step-sequence gloss. CORRECTED to carry that tail verbatim. Tool match and honest-empty fallback are the same as the spec edit it mirrors. Leverage downgraded: this is the lean-core echo of edit 1 in the same skill, not independent reach.",
   "leverage": "low",
   "current_excerpt": "3. If `.kdbp/` already exists, ask reset / update / skip and follow the mode routing table",
   "sweep_rationale": "The lean core mirrors the spec's Step 1; the status the operator sees before a destructive choice is visible at summary level, so the pointer belongs here too. Same edit as the spec finding, one line."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 89,
   "skill": "gabe-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "1. Read existing project state: `mcp__gabe-kdbp__kdbp_snapshot` FIRST where a server answers (branch · PLAN phase-table states · open PENDING · last LEDGER rows, under 1k tokens) to orient, then the files it does not carry — `.kdbp/PLAN.md` (the phase DETAILS and per-phase records the snapshot summarizes but does not quote), `.kdbp/SCOPE.md` (the phase arc lives in its §Phases), `.kdbp/STRUCTURE.md`, `.kdbp/AUDIT.md` (if present). No server or no `.kdbp/` → read all four as before.",
   "survives": true,
   "reason": "Anchor verbatim at line 89, inside the ```yaml 'BEHAVIOR.md — generate from answers' block (fence 58→105). CORRECTED on two counts: (a) the proposal DEMOTED `.kdbp/PLAN.md` from the read list to a fallback, but the snapshot carries phase-table STATES only — not Phase Details/records, which is exactly what B1 exists to stop a session re-proposing; PLAN.md is restored as a first-class read. (b) LEVERAGE CLAIM REFUTED: 'suite-wide' is reserved for a file many skills point at (execution-contract.md). init-spec is gabe-init's own reference, and templates/BEHAVIOR.md — the E6-binding render source the spec itself names at line 69 — carries NO '# Project Behavior Rules' or B1 section at all (grep: B1 exists only here, init-spec.md:84), so propagation runs through this illustrative example, not through the template. Real but bounded → medium.",
   "leverage": "medium",
   "current_excerpt": "1. Read existing project state: `.kdbp/PLAN.md`, `.kdbp/SCOPE.md`, `.kdbp/STRUCTURE.md`, `.kdbp/AUDIT.md` (if present) — the phase arc lives in `SCOPE.md` §Phases",
   "sweep_rationale": "This block is the B1 rule init RENDERS into every new project's BEHAVIOR.md — one edit changes the inventory every session in every project scaffolded afterwards does before proposing. The rule's own failure story (restating work the user had already done) is exactly the read the snapshot makes cheap. SCOPE/STRUCTURE/AUDIT reads stay: the snapshot does not carry them."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 93,
   "skill": "gabe-init",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "5. List what already exists for this question before proposing what's missing — where the project has a command center, `mcp__gabe-map__find` (by name or doc, across entities/endpoints/models/functions/screens) and `mcp__gabe-map__center_overview` (the entity estate in ≤600 tokens) name what is already there; the map is a FLOOR, so `grep -rn` still settles absence",
   "survives": true,
   "reason": "Anchor verbatim at line 93. `find` answers 'X by name/doc across entities, endpoints, models, schemas, functions, screens, FE pieces' — item 5 is that question. L1 is explicitly honored (the FLOOR clause keeps grep -rn as the absence proof), L2 is honored by the 'where the project has a command center' guard. Text droppable as-is. LEVERAGE CLAIM REFUTED for the same reason as edit 3: init-spec is not a many-skills file, and the binding template carries no B1 — medium, not suite-wide.",
   "leverage": "medium",
   "current_excerpt": "5. List what already exists for this question before proposing what's missing",
   "sweep_rationale": "Same suite-wide template block: item 5 is a what-already-exists question, which is find's whole job, and today it is answered from memory. The floor clause keeps the map from being read as an absence claim (L1); honest-empty on a project with no center leaves the item exactly as it is."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 192,
   "skill": "gabe-init",
   "klass": "replace",
   "tool": "mcp__gabe-map__who_calls",
   "proposed_text": "- `docs/site/center/commits.js` — the recent-commits journeys feed `_a3_commits.build_commits` derives from `git log` each build (its newest sha changes on every commit). Same reasoning — a beat-tail artifact; the Universe station reads it locally and shows no commits kind when it is empty/absent.\n- `.kdbp/map-deltas.jsonl` and `.kdbp/map-deltas-rollup.jsonl` — the map↔grep delta accumulator (emitted at /gabe-red, /gabe-execute, /gabe-review and by `mcp__gabe-map__who_calls`) and the tally ledger /gabe-commit sweeps it into. Per-project bookkeeping that would re-dirty the tree every beat — and gabe-map's emit gate (d) REQUIRES `git check-ignore -q .kdbp/map-deltas.jsonl` to succeed, so an un-seeded project silently drops every delta (the emit is skipped and NAMED: `.kdbp/map-deltas.jsonl is not gitignored — run /gabe-init update to seed it`) until `/gabe-init update` tops it up.",
   "survives": true,
   "reason": "Anchor verbatim at line 192. Gap CONFIRMED: Step 1.8 lists five bullets (reviews-archive · .push-gate-ok · inflight.{json,js} · sim.data.js · commits.js) while SKILL.md item 6 seeds seven entries incl. both map-deltas files. Gate (d) verified in mapquery.py:380 (`git check-ignore -q LIVE_REL`) and :440 (the skip string), and pinned in map-spec §5 line 116 — so the missing spec text really does disable who_calls' emit in every project scaffolded from the spec. CORRECTED: quoted the actual skip message instead of the invented `emit_skipped: not gitignored`. L5 clean (spec text only; no script edit).",
   "leverage": "high",
   "current_excerpt": "- `docs/site/center/commits.js` — the recent-commits journeys feed `_a3_commits.build_commits` derives from `git log` each build (its newest sha changes on every commit). Same reasoning — a beat-tail artifact; the Universe station reads it locally and shows no commits kind when it is empty/absent.",
   "sweep_rationale": "The binding spec's Step 1.8 bullet list omits both map-deltas entries that SKILL.md item 6 already lists — a session following the spec seeds five entries, not seven, and every who_calls emit in that project is then skipped by gate (d). This is the one place in the batch where missing spec text disables a tool."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 204,
   "skill": "gabe-init",
   "klass": "replace",
   "tool": "",
   "proposed_text": "`update` mode includes it as part of the readiness scan — an existing project that predates these rulings gets whichever entries it is missing topped up.",
   "survives": true,
   "reason": "Anchor is a verbatim substring of line 204. 'the two new entries' is provably stale — the bullet list already carries five entries (six files) and seven once edit 5 lands. Pure accuracy fix, no tool, no law touched.",
   "leverage": "low",
   "current_excerpt": "`update` mode includes it as part of the readiness scan — an existing project that predates this ruling gets the two new entries topped up.",
   "sweep_rationale": "\"the two new entries\" dates from when the list was inflight.{json,js}; the list now carries five (seven once the map-deltas bullet lands), and the map-deltas top-up on an existing twin is exactly the case this sentence has to cover."
  },
  {
   "file": "skills/gabe-init/references/init-spec.md",
   "line": 70,
   "skill": "gabe-init",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "     the commands the gates trust (gate-spec Step 2.0), authored from the interview, never guessed.\n     Where a `.kdbp/` is already present (a `reset` over an existing project), `mcp__gabe-kdbp__verify_commands`\n     resolves what is already bound — `source: a` quotes the existing BEHAVIOR.md ## Verify Commands, `source: b`\n     names the package.json / pyproject / Makefile candidates it can SEE — and it never runs one, never invents a\n     reporter flag. Offer what it names to the interview; the human's answer still binds. A tree with no `.kdbp/`\n     yet (fresh init) answers absent — the interview stands alone there. -->",
   "survives": true,
   "reason": "Anchor verbatim at line 70 (inside the same BEHAVIOR.md example fence). Tool matches the question — verify_commands IS the lint/types/tests binding, and its own contract ('commands are RESOLVED, never run here') mirrors this comment's 'never guessed'. CORRECTED on a verified factual error: kdbp_tools.py:431-441 returns `_absent` when `.kdbp/` is missing, and returns `source: a` (BEHAVIOR.md) whenever a Verify Commands section parses — so on a `reset` over an existing project you get 'a', not 'b'. The proposal's `reset` qualifier ALSO fails to exclude a fresh tree (the mode routing table at line 25 sends 'fresh project (no .kdbp/)' down the reset sequence); the corrected text conditions on `.kdbp/` presence instead and states the absent case, so L2 holds.",
   "leverage": "low",
   "current_excerpt": "     the commands the gates trust (gate-spec Step 2.0), authored from the interview, never guessed. -->",
   "sweep_rationale": "Narrow but real: on `reset` in an existing repo the interview asks a human to type commands a tool can read off the manifests, and the tool's own law (never run, never guess a flag) matches this comment's law exactly. Fresh init has no `.kdbp/` yet, so the tool is honest-empty there — hence the `reset` qualifier."
  },
  {
   "file": "skills/gabe-scope-change/SKILL.md",
   "line": 31,
   "skill": "gabe-scope-change",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "- Ask `mcp__gabe-kdbp__kdbp_snapshot` first — branch, PLAN phase-table states, open PENDING rows — and surface it with the classification, so the human sees what is in flight before an addition writes beside a running phase; it carries lifecycle STATE only (the classifier's inputs stay `{current_scope, proposed_change, user_intent}`), and answers honest-empty without `.kdbp/`.\n- Read SCOPE.md (incl. `## Phases`) + `scope-references.yaml` into structured state — SCOPE.md stays the arc's only authority.",
   "survives": true,
   "reason": "Anchor verbatim at line 31; this skill has NO references/ dir, so SKILL.md IS the binding spec and there is no lean-core/spec seam to break. Gap is real: Step 5 writes SCOPE §Phases inline (decimal-ID insert) and never reads PLAN state or PENDING, so an addition can land beside a mid-Exec phase unseen. Pre-flight already requires `.kdbp/SCOPE.md`, so L2 is satisfied. CORRECTED: the proposal said the change would be 'classified against' the snapshot, which overclaims — Step 2 feeds the classifier a fixed triple; the corrected text surfaces the state to the human instead and pins the classifier's inputs unchanged (L3: no new rail).",
   "leverage": "medium",
   "current_excerpt": "- Read SCOPE.md (incl. `## Phases`) + `scope-references.yaml` into structured state.",
   "sweep_rationale": "Pre-flight today reads the arc and nothing about the running project, so an addition can be classified while the phase it inserts beside is mid-Exec, or while PENDING already carries the debt the change is meant to answer. The second line pins the source split so the snapshot is never mistaken for the arc."
  },
  {
   "file": "skills/gabe-scope-pivot/SKILL.md",
   "line": 33,
   "skill": "gabe-scope-pivot",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "Same as `/gabe-scope-change` — SCOPE.md must exist, no active session. Then ask `mcp__gabe-kdbp__kdbp_snapshot` and print ONE line directly beneath the warning block naming what the pivot puts at risk: phases already ✅ or 🔄 in the PLAN table, and the open PENDING rows the new arc will orphan (the warning template itself is literal — do not edit its lines). Nothing to print when the snapshot is honest-empty; the typed confirm stands either way.",
   "survives": true,
   "reason": "Anchor verbatim at line 33; SKILL.md is binding here too (no references/). The claim holds: the PIVOT WARNING at lines 37-51 enumerates archived FILES and says only that 'phases will be re-evaluated' — executed phases and open debt are never named, and kdbp_snapshot names both in one call. CORRECTED: the proposal said to 'carry what it names into' the warning, but that warning is a fenced LITERAL template with fixed slots ({N}/{date}/{description}/{from classifier}) — a runtime would have to invent a line inside a verbatim block. The corrected text places the fact beneath the block and protects the template. Typed confirm untouched (L3).",
   "leverage": "medium",
   "current_excerpt": "Same as `/gabe-scope-change` — SCOPE.md must exist, no active session.",
   "sweep_rationale": "The suite's most destructive beat asks for a typed confirm against a warning that lists archived FILES only. What a pivot actually costs is executed phases and open debt, which the snapshot names in one call — the same reason init's reset prompt earns it."
  },
  {
   "file": "skills/gabe-map/references/map-spec.md",
   "line": 10,
   "skill": "gabe-map",
   "klass": "replace",
   "tool": "",
   "proposed_text": "fifteen tools — the v1 seven plus the wave-2 graft equivalents (§5.8). It is the suite's **reliability surface** for questions an agent asks mid-reasoning; it is **not a rail**",
   "survives": true,
   "reason": "Anchor verbatim at line 10. Staleness CONFIRMED against three independent sources: SKILL.md line 24 ('as fifteen tools'), the registry split V1∪W2, and tests/gabe-map/checks.py:236-238 (`len(names) == 15`). Pure accuracy fix, no law touched. Leverage downgraded to low — SKILL.md and the instructions block already say fifteen, so this is a §1 prose correction, not a behavior change.",
   "leverage": "low",
   "current_excerpt": "seven tools. It is the suite's **reliability surface** for questions an agent asks mid-reasoning; it is **not a rail**",
   "sweep_rationale": "Stale since wave 2: §1 still opens the binding contract at seven while SKILL.md, the registry (`tools.TOOLS` + `tools_wave2.TOOLS`) and the battery (`len(names) == 15`) all say fifteen."
  },
  {
   "file": "skills/gabe-map/references/map-spec.md",
   "line": 35,
   "skill": "gabe-map",
   "klass": "replace",
   "tool": "",
   "proposed_text": "discovery surface is therefore the fifteen NAMES plus the `instructions` block — both must route the question to the",
   "survives": true,
   "reason": "Anchor verbatim (substring of the indented continuation line 35; the swap preserves indentation). This one carries weight: §2's deferral law makes the NAMES the entire discovery surface, so an undercount understates what must route — and eight wave-2 names are exactly the ones a session has never seen a schema for. Verified against checks.py:238.",
   "leverage": "medium",
   "current_excerpt": "discovery surface is therefore the seven NAMES plus the `instructions` block — both must route the question to the",
   "sweep_rationale": "Same wave-2 staleness, and this one carries weight: the deferral law says the NAMES are the whole discovery surface, so under-counting them understates what has to route."
  },
  {
   "file": "skills/gabe-map/references/map-spec.md",
   "line": 189,
   "skill": "gabe-map",
   "klass": "replace",
   "tool": "",
   "proposed_text": "`tools/list` (15 names — the v1 seven ∪ the wave-2 eight — object schemas, annotations, descriptions ≤ 200 chars)",
   "survives": true,
   "reason": "Anchor verbatim (substring of line 189). Both added facts pinned in the battery: checks.py:238 asserts `set(names) == V1 | W2 and len(names) == 15`, checks.py:242 asserts every description ≤ 200 chars. §7 currently describes a pin that no longer matches what runs.",
   "leverage": "low",
   "current_excerpt": "`tools/list` (7 names, object schemas, annotations)",
   "sweep_rationale": "The battery asserts `set(names) == V1 | W2 and len(names) == 15` plus the ≤200-char description cap; §7 describes a pin that no longer matches what runs."
  },
  {
   "file": "skills/gabe-map/references/map-spec.md",
   "line": 195,
   "skill": "gabe-map",
   "klass": "replace",
   "tool": "",
   "proposed_text": "`cases_for` split · `owner_of` two owners + unclaimed · the wave-2 equivalents (`find` ranking, kind filter and a 1-char stop; `outline` with and without a graft index; `center_overview` per-entity coverage + census gaps; `blast_radius` contained vs unmapped; `map_census` unclaimed + an absent block's reason + a bad-kind stop; `map_diff` same-head and a ref with no committed map; `center_status` the no-script reason; `review_drift` ran vs not_run) · `who_calls` `direction=out` (callees, never an emit) + `map_confidence` from the tally ledger · a harness e2e that calls `mcp__gabe-map__map_status` through the real client.",
   "survives": true,
   "reason": "Anchor verbatim (substring of line 195). EVERY listed pin verified line-by-line in tests/gabe-map/checks.py: find 374/376/378, outline 380/383, center_overview 385, blast_radius 387/389, map_census 391/393, map_diff 395/397, center_status 399, review_drift 401, who_calls direction=out 403-404, map_confidence 405 + 426, harness e2e 463. Without this, §7 reads as if the wave-2 surface is unpinned and freely editable.",
   "leverage": "medium",
   "current_excerpt": "`cases_for` split · `owner_of` two owners + unclaimed.",
   "sweep_rationale": "§7 lists only the v1 pins, so the wave-2 tools read as unpinned — they are not: checks.py asserts every item above. An editor trusting §7 would think find/outline/blast_radius/review_drift can be changed without a battery update."
  },
  {
   "file": "skills/gabe-map/SKILL.md",
   "line": 31,
   "skill": "gabe-map",
   "klass": "replace",
   "tool": "",
   "proposed_text": "| `who_calls` | who calls or uses a symbol — graft callers ∪ word-boundary git grep, code vs prose; `direction=out` walks callees, `depth` the transitive reach, every answer stamped with `map_confidence` | graft index · git grep (+ the emit) |",
   "survives": true,
   "reason": "Anchor verbatim at line 31. The three added facts are already carried by map-spec §5.8 lines 168-170 (direction in/out → callees[]; depth 1/N/all; every answer carries map_confidence{active_missed_edges, edges_total, note}) and pinned by checks.py:403-405 — the summary table was the last place still describing the v1 tool. Table shape and Reads column preserved.",
   "leverage": "low",
   "current_excerpt": "| `who_calls` | who calls or uses a symbol — graft callers ∪ word-boundary git grep, code vs prose | graft index · git grep (+ the emit) |",
   "sweep_rationale": "The fifteen-tool table is the human-facing summary and still describes the v1 who_calls; §5.8 and the CLAUDE.md capability row both already carry direction/depth/map_confidence."
  }
 ],
 "refuted": [
  {
   "file": "skills/gabe-mockup/SKILL.md",
   "line": 41,
   "skill": "gabe-mockup",
   "klass": "no-change",
   "tool": "mcp__gabe-kdbp__verify_commands",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor exact, but the tool does not answer this question (rule B) and the edit breaks E3. `t_verify_commands` resolves `## Verify Commands` — a BEHAVIOR.md H2 section parsed for lint|types|tests|results_out ONLY (kdbp_tools.py:225-244, 431-441) — not the `mockup:` block's `verify_commands:` key. Different key-space: the tool can never return build-storybook, test-storybook or the token-class check. Worse, 'this key governs when the tool reports none' INVERTS precedence — when the tool reports something, its lint/types/tests binding would replace the richer L4 storybook gate list, a silent downgrade of the gate (E3). The rationale itself concedes 'the mockup block's own list is richer', contradicting its own proposed_text. A correction into a subordinate floor would be a different edit with near-zero leverage on a manifest-key description line.",
   "leverage": "low",
   "current_excerpt": "- `verify_commands` — the L4 gate command list (typecheck, build, build-storybook, test-storybook, token-class check, …).",
   "sweep_rationale": "The manifest lives in `.kdbp/BEHAVIOR.md` and the kdbp tool reads exactly that binding (BEHAVIOR first, else manifest candidates, never run). It saves re-parsing BEHAVIOR here and at L4 (line 57) and at every mode's verification gate. First-look, not replace: the mockup block's own list is richer than a lint/types/tests binding and must survive an honest-empty answer."
  },
  {
   "file": "skills/gabe-mockup/references/spike.md",
   "line": 60,
   "skill": "gabe-mockup",
   "klass": "no-change",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor exact at line 60, but both named tools fail their claims. (1) ledger_row_preview cannot render this row: the template on this very line is `| [YYYY-MM-DD] | MOCKUP | spike: … |` and the tool rejects `entry` outside its ten-tag enum (kdbp_tools.py:519-521) — rule B. (2) 'kdbp_snapshot reports the phase table without re-reading the file' invites minting the next free `P14.N` off a list capped at 40 rows (`rows[:CAP]`, CAP=40 in mapquery.py:36); a mature ladder (M0–M13 + P14.n + P15.n) exceeds that, so the inference 'P14.7 is not listed ⇒ P14.7 is free' is exactly the absence claim L1 forbids. Stripping both leaves nothing.",
   "leverage": "low",
   "current_excerpt": "8. **S8 — Bookkeeping.** Append `Spike P14.<N>` row to `.kdbp/PLAN.md` (use the next available `P14.N` index). Append one thin-index row to `.kdbp/LEDGER.md`:",
   "sweep_rationale": "\"Next available P14.N\" and the LEDGER row shape are both whole-file reads of PLAN.md/LEDGER.md that the snapshot and row preview answer. Same text appears in the Outputs block (line 38) and should follow this step once agreed."
  },
  {
   "file": "skills/gabe-mockup/references/validate.md",
   "line": 67,
   "skill": "gabe-mockup",
   "klass": "no-change",
   "tool": "mcp__gabe-kdbp__ledger_row_preview",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor exact at line 67 (and this Outputs block is validate's only bookkeeping site — there is no S-step equivalent, so the placement was right). Killed for the same two tool-contract failures as the spike edit: the row on this line is `| … | MOCKUP | validate: … |` and ledger_row_preview refuses any entry outside its ten-tag enum (kdbp_tools.py:519-521); and 'kdbp_snapshot reports the phase table for the next free index' reads a 40-row-capped list (mapquery.py:36) as proof that a `P15.N` is unused — the absence claim L1 forbids. Rule B on both halves leaves nothing to correct.",
   "leverage": "low",
   "current_excerpt": "- Bookkeeping: append `Spike P15.<N>` row to `.kdbp/PLAN.md`; append one thin-index row to `.kdbp/LEDGER.md`:",
   "sweep_rationale": "Same hand-typed row + next-index read as spike S8; the previews carry the file's own columns so the two modes stop drifting from LEDGER's actual schema."
  },
  {
   "file": "skills/gabe-mockup/references/legacy-html-phases.md",
   "line": 225,
   "skill": "gabe-mockup",
   "klass": "no-change",
   "tool": "mcp__gabe-map__touches",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor exact at line 225, but the tool answers a different question (rule B). Step 2e asks which DOMAIN entity a static mockup DEPICTS, for the INDEX.md CRUD matrix; `touches` answers code reachability from the archmap/c4 (owners, r/w functions, endpoints, tests, edges). The subject here is `docs/mockups/screens/<screen>-{desktop,mobile}.html`, emitted by step 2c — static HTML outside every scanned code dir, so it is absent from the archmap by construction and both touches and owner_of would return blind/empty on every run of this ladder. The edit's own honest-empty clause concedes as much. Naming two tools that can only ever answer empty on a step is noise, not a first look; the entity is knowable only from the mockup's own content, exactly as the line already says.",
   "leverage": "low",
   "current_excerpt": "   e. **Update INDEX.md §4 CRUD row(s)** for any entity this screen touches.",
   "sweep_rationale": "\"Any entity this screen touches\" is the tool's own name, and today it is answered from memory — which is how CRUD cells go stale. First-look with the honest-empty clause because the legacy ladder usually runs in a mockup-only repo where the map is absent (L2)."
  },
  {
   "file": "skills/gabe-execute/references/execute-spec.md",
   "line": 35,
   "skill": "gabe-execute",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor exists, but the mechanism claim is FALSE and it fails L1. `plan_rows` calls `_table(text, \"Exec\")` — the phases table is located BY the Exec header, so a table with no `Exec` column does not yield 'a missing key', it yields no table at all: `plan.phases: []`, indistinguishable from 'no phases table' and from 'no plan'. Letting that empty list stand for precondition 3 is exactly an absence-in-a-tool-answer read as proof (L1), and precondition 3's required output is a specific legacy warning with 'do not auto-migrate'. Any honest correction ends in 'confirm on PLAN.md anyway', at which point the call buys nothing: precondition 1's `.kdbp/` presence is already answered honest-empty by the phase_context call the same batch adds two lines below (precondition 4/5), so this adds a second front-door call and removes no read. Precondition 2 the proposer already (correctly) excluded.",
   "leverage": "low",
   "current_excerpt": "**Preconditions:**",
   "sweep_rationale": "Preconditions 1 and 3 are presence questions the snapshot answers directly and cheaply (<1k tokens for the whole table). Precondition 2 is deliberately excluded: `plan.status` comes from the PLAN.json mirror, NOT the `<!-- status: active -->` comment this check reads, and a mirror can lag the file — letting the tool stand in there would be a false pass."
  },
  {
   "file": "skills/gabe-commit/references/gate-spec.md",
   "line": 318,
   "skill": "gabe-commit",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "",
   "survives": false,
   "reason": "KILLED on B (the tool answers adjacent information) and on redundancy with the procedure this bullet defers to. Of the four preconditions: `.kdbp/PLAN.md` contains `status: active` is a PLAN.md marker (`<!-- status: active -->`, plan-spec.md:780) while `kdbp_snapshot.plan.status` is read from PLAN.json only — a different file; `has a ## Current Phase section` is not reported by the snapshot AT ALL; and the fourth is already covered — plan-spec.md:782 states the column check is 'by column name, not position' as the shared helper's own law, which gate-spec.md:316 explicitly says this command does not restate. So the finding's whole premise ('the one thing a positional parse gets wrong on a twin') is false: no positional parse exists here. Adding a tool call would split a precondition check away from the helper that performs it and substitute a weaker, PLAN.json-derived answer for two of the four. Nothing correctable survives.",
   "leverage": "low",
   "current_excerpt": "   - Preconditions: `.kdbp/PLAN.md` exists, contains `status: active`, has a `## Current Phase` section, and Phases table includes a `Commit` column",
   "sweep_rationale": "The four preconditions are exactly the fields `kdbp_snapshot.plan` carries, and the `column-missing` skip code depends on knowing which columns the table actually has — the one thing a positional parse gets wrong on a twin. L3 held explicitly: the tool answers the preconditions, the auto-tick helper still performs the write."
  },
  {
   "file": "skills/gabe-push/references/push-spec.md",
   "line": 510,
   "skill": "gabe-push",
   "klass": "first-look",
   "tool": "mcp__gabe-kdbp__kdbp_snapshot",
   "proposed_text": "",
   "survives": false,
   "reason": "KILLED for the same reason as the gate-spec line-318 twin, and the shared law is explicit here: plan-spec.md:782 — 'detection is by column name, not position' — is the auto-tick helper's OWN stated method, and line 508 says push simply follows that shared procedure. So 'the one thing a positional parse gets wrong on a twin' describes a parse that does not exist. Of the four preconditions the tool is claimed to carry, `kdbp_snapshot` reports NEITHER PLAN.md's `<!-- status: active -->` marker (its `plan.status` comes from PLAN.json, a different file) NOR the presence of a `## Current Phase` section — it returns adjacent information, which fails B. Nothing survives correction: the only true part is a check the helper already performs correctly at the moment it writes.",
   "leverage": "low",
   "current_excerpt": "- Preconditions: `.kdbp/PLAN.md` exists, contains `status: active`, has `## Current Phase`, and Phases table includes a `Push` column",
   "sweep_rationale": "Mirrors the Commit-column finding in gate-spec: the `Push`-column precondition is a question about which columns the table actually has, which is the one thing a positional parse gets wrong on a twin. L3 held — the tool answers preconditions, the shared helper writes."
  },
  {
   "file": "skills/gabe-help/references/tool-registry.md",
   "line": 42,
   "skill": "gabe-help",
   "klass": "first-look",
   "tool": "mcp__gabe-map__find",
   "proposed_text": "",
   "survives": false,
   "reason": "KILLED on B (adjacent information). The anchor is verbatim, but the paragraph's subject is fixed by SKILL.md:29 — 'before building any tooling, harness, generator, or pipeline'. `find` searches the archmap's APPLICATION symbol space (entities, endpoints, models, schemas, function_insight, screens, FE pieces, and per-entity `defines` inside center.config code globs). Generators, harnesses and pipelines live outside those globs — in a twin they sit under scripts/ and templates/center/generators/, which no entity claims — so `find` answers a different question and returns honest-empty on precisely the class of thing this paragraph is about. Adding it buys a call that near-always says nothing while implying the registry's question has a map answer.",
   "leverage": "low",
   "current_excerpt": "If a tool for X plausibly exists and is not listed here or in the project's `.kdbp/`, search\nthe sibling projects and `gh search` BEFORE writing it, and record the outcome as an E4 line:",
   "sweep_rationale": "The E4 reuse search starts outside the repo today. `find` answers \"does this project already have one\" from the committed map first; the sibling + `gh search` arms stay, because a map miss is never proof the thing does not exist (L1)."
  },
  {
   "file": "skills/gabe-myopic/method.md",
   "line": 17,
   "skill": "gabe-myopic",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_shape",
   "survives": false,
   "leverage": "low",
   "proposed_text": "| UI code/routes | `entity_shape`+`find` name routes/screens; then trace; one screen ≈ one step |",
   "reason": "REFUTED on rule B — the named tool answers a different question. entity_shape is a BACKEND API tool: skills/gabe-pulse/scripts/entity_shape.py::load_project builds its corpus solely from archmap `entities[slug].endpoints[].path`; url_domain() takes the first non-parameter segment of an API path; diff_new_routes' _ROUTE_RE only matches server route decorators (`@\\w+\\.(get|post|put|patch|delete)(\"...\")`). It never reads c4['fe']['pieces'], where the FE `route` piece kind lives, so it cannot see a single frontend route. Step 0 asks for 'the route/component sequence a USER traverses'; entity_shape would answer with which entity owns an API URL domain — adjacent, and worse than adjacent here, because a walk built from backend endpoint domains reconstructs a step sequence no user ever takes. Secondary defect: the proposed row is 94 chars where every other row in the fenced table is 96, breaking the block's alignment. SALVAGE for a re-proposal: `find` alone is valid, and this row fits at exactly 96 chars as `| UI code/routes | \\`find\\` names the screen / FE route piece; then trace; one screen ≈ one step |` — but that is a different edit naming a different tool, not a correction of this one.",
   "current_excerpt": "| UI code/routes | trace the route/component sequence a user traverses; one screen ≈ one step  |",
   "sweep_rationale": "Step 0 reconstructs the step sequence, and for route input that is a routing-table hunt today. `entity_shape` answers who owns a URL domain (and names orphan domains — a route no entity claims is precisely a flow nobody walked); `find` lists that entity's screens and FE pieces. Row rewritten to hold the table's column width; the fuller naming lives in the paragraph at line 26."
  },
  {
   "file": "skills/gabe-myopic/references/myopic-spec.md",
   "line": 116,
   "skill": "gabe-myopic",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_shape",
   "survives": false,
   "leverage": "low",
   "proposed_text": "- **UI code / a component tree / routes** — infer the steps the code produces (with a command center, `mcp__gabe-map__entity_shape` names who owns the URL domain and `mcp__gabe-map__find` lists its screens/FE pieces before you trace);",
   "reason": "REFUTED on rule B, same defect as the method.md row it twins (the anchor itself is verbatim at line 116). entity_shape's endpoint corpus is API-only — it cannot name a UI route. A second, independent defect in this one: 'find LISTS its screens/FE pieces' misstates the tool. t_find raises MapStop when `query` is under 2 characters — it is a name/doc SEARCH ranked exact/prefix/substring, not an enumeration of an entity's screens. Nothing in the 22-tool roster enumerates FE route pieces for an entity (entity_context returns c4.l2_node_kinds counts and an fe_home record, not a piece list), so the 'before you trace' promise cannot be kept as written. SALVAGE: a narrower edit naming find as a lookup for a screen or FE route piece you can already name.",
   "current_excerpt": "- **UI code / a component tree / routes** — infer the steps the code produces;",
   "sweep_rationale": "Binding twin of the method.md row — the Inputs list is what an agent reads when deciding how to attack a code target, and it must name the same first look or the two files disagree about method."
  },
  {
   "file": "skills/gabe-myopic/SKILL.md",
   "line": 50,
   "skill": "gabe-myopic",
   "klass": "first-look",
   "tool": "mcp__gabe-map__entity_shape",
   "survives": false,
   "leverage": "low",
   "proposed_text": "3. Reconstruct the step sequence from whatever representation is given; for UI code or routes in a project with a command center, ask `mcp__gabe-map__entity_shape` / `find` for the routes and screens before tracing; if the order is ambiguous, state the assumed sequence first — never silently invent a flow.",
   "reason": "REFUTED on rule B, and killed for consistency with its two spec twins — a lean core must not name a first look the binding spec cannot support. The anchor is verbatim at line 50 and the 'never silently invent a flow' law is correctly left intact, but the named tool cannot supply the frontend route inventory the assumed sequence would be built from (entity_shape reads archmap API endpoints only). The proposal's own rationale concedes the map has no journey ordering; combined with the API-only corpus, what it would hand a myopic walk is a list of backend URL domains — precisely the input that produces the invented flow the law at the end of this same sentence forbids. SALVAGE: re-propose with `find` alone as the tool, matched to a corrected method.md paragraph.",
   "current_excerpt": "3. Reconstruct the step sequence from whatever representation is given; if the order is ambiguous, state the assumed sequence first — never silently invent a flow.",
   "sweep_rationale": "Lean-core mirror. The 'never silently invent a flow' law is untouched — the tools supply the route inventory the assumed sequence is built from, they do not supply the ORDER (the map has no journey ordering), which is why the ambiguity clause must stay in the same sentence."
  },
  {
   "file": "skills/gabe-lens/SKILL.md",
   "line": 45,
   "skill": "gabe-lens",
   "klass": "no-change",
   "tool": "mcp__gabe-map__outline",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor is verbatim, but the tool fails criterion B and the edit is self-defeating. outline returns definitions in span order with signatures, returns, access_ops, owners, models and tests_reaching — it does NOT answer which of a file's concepts are the '3-5 most complex/critical', which is the judgment line 45 asks for; that is adjacent information. Worse, the edit itself keeps the full file read ('the file is still read in full'), and the read yields every definition outline would have named — so the added call buys nothing for concept selection. Its only non-redundant fields (owner entity, tests_reaching) are not what the line asks. gabe-lens/SKILL.md is a 54-line lean core where a clause that pays nothing is a net cost, and `annotate [file-path]` is aimed at any file, including markdown, where outline degrades to 'signatures: unavailable' with no archmap owner. No correction is justifiable from the file.",
   "leverage": "low",
   "current_excerpt": "4. Identify the target concept(s) from `$ARGUMENTS` (or, for `annotate`, from the target file's 3-5 most complex/critical concepts — not trivial facts).",
   "sweep_rationale": "annotate is this skill's one file-touching mode and it currently picks the 3-5 complex concepts by reading cold. outline hands over the definition inventory with spans/signatures plus the owning entity and the tests that reach it — the shortlist, before the read. Not a replace: which concepts are COMPLEX is a judgment only the source text supports, so the full read stays (L1)."
  },
  {
   "file": "skills/gabe-docsite/SKILL.md",
   "line": 23,
   "skill": "gabe-docsite",
   "klass": "no-change",
   "tool": "mcp__gabe-map__map_status",
   "proposed_text": "",
   "survives": false,
   "reason": "Anchor is verbatim (line 23 reproduced exactly), but the tool answers a DIFFERENT question and this repo is the counter-example. CENTER MODE's condition is the seam `docs/center/shell` + `docs/site/center/nav.json`; map_status answers 'is there a committed codebase map' (archmap · c4 · inflight · git). /home/khujta/projects/gabe_lens HAS both seam paths — `docs/center/shell` and `docs/site/center/nav.json` exist — and a live map_status call here returns `present: false`, `reason: \"suite center (beat spine) — no codebase map by ruling R8\"` (no docs/site/center/archmap.json). The proposal's rationale cites that very result as verification when it is the refutation: an agent told 'map_status says in one call whether a command center exists here' would take STANDALONE mode on a project that must build in CENTER MODE — an E3 silent downgrade. The correct check is the two file paths, which the spec's flags already name and which need no tool, so there is nothing to correct toward.",
   "leverage": "low",
   "current_excerpt": "It builds in one of two modes, and **the build states which one it took**.",
   "sweep_rationale": "CENTER MODE is the default \"where a command center exists\" but the spec never says how that is established, so the agent globs for it. map_status answers center presence in one honest-empty call (verified live in this repo: `present:false`, reason given). Kept as first-look because mode also needs the shell + nav.json seam files, which map_status does not report — so the file check stays binding (L1/E3)."
  },
  {
   "file": "skills/gabe-scope/references/scope-spec.md",
   "line": 58,
   "skill": "gabe-scope",
   "klass": "first-look",
   "tool": "mcp__gabe-map__center_overview",
   "proposed_text": "",
   "survives": false,
   "reason": "KILLED on B (adjacent information, not an answer). Anchor is verbatim, but Step 0.5(a) scans for reference-frame CANDIDATE DOCUMENTS — markdown/YAML files that become `scope-references.yaml` entries carrying a path + role + weight + load (lines 62-100). `center_overview` returns entities with rank/status/counts/coverage/arms/census gaps; `map_status` returns presence + freshness + a regen command. Neither enumerates a document path, so neither can produce a candidate row for the pick table, and the proposal's claim that 'the center's own pages are candidate refs' is not something either tool answers. Adding a center probe to a greenfield-first beat buys nothing the scan does not already do.",
   "leverage": "low",
   "current_excerpt": "**(a) Auto-suggest candidates.** Deterministic filesystem scan — no LLM yet. Scan these locations for markdown + YAML files:",
   "sweep_rationale": "The one map-shaped moment in scope authoring: a scope written inside an existing repo (the `../docs/`, parent-project `.kdbp/` case the scan already anticipates) should see the entity estate before the interview, not after. The deterministic scan is untouched, so greenfield behaviour is byte-identical (L1/L2)."
  }
 ],
 "critic_report": "# Skill Sweep — Apply Plan, Gap Check, Price\n\n## 1 · Apply plan\n\n**219 edits · 48 spec files · 24 skills.** Ordered so each file is opened exactly once.\n\n### Tier 0 — the suite-wide file (land first, alone, verify, then continue)\n\n| File | Edits | Why first |\n|---|---|---|\n| `skills/gabe-docs/references/execution-contract.md` | **8** | All 30 SKILL.md files point here (verified: 30/30 carry the `E1 EVIDENCE · E2 …` enumeration). One new `## The tool floor` section states L1–L4 once; 7 grafts onto E1/E2/E4/E5, the LINK rule, the relay carve-out, and the beat brief. Every later per-skill edit assumes these laws are already stated. |\n\n> **Apply hazard — this file has an INSERT.** The new section goes in before line 19, shifting the edits at 23, 26 and 46. Apply **descending by line** (46 → 26 → 23 → 19 → 15 → 14 → 12 → 11).\n\n### Tier 1 — the spine (the beats /gabe-next routes)\n\n| Skill | Files | Edits | Bump |\n|---|---|---|---|\n| **gabe-review** | `references/review-spec.md` (26) · `SKILL.md` (3) | **29** | 1.18.1 → 1.18.2 |\n| **gabe-execute** | `references/execute-spec.md` (16) · `SKILL.md` (1) | **17** | 2.6.3 → 2.6.4 |\n| **gabe-commit** | `references/gate-spec.md` (8) · `SKILL.md` (3) · `references/simplify-pass.md` (2) | **13** | 2.7.6 → 2.7.7 |\n| **gabe-plan** | `references/plan-spec.md` (11) · `SKILL.md` (1) | **12** | 2.7.3 → 2.7.4 |\n| **gabe-push** | `references/push-spec.md` (6) · `SKILL.md` (2) | **8** | 2.6.2 → 2.6.3 |\n| **gabe-red** | `references/red-spec.md` (4) · `SKILL.md` (3) | **7** | 1.9.5 → 1.9.6 |\n| **gabe-next** | `SKILL.md` (1) | **1** | 2.4.1 → 2.4.2 |\n\n`plan-spec.md:830` (LEDGER house format) is cited by name from handoff, review, push and red — land it before their per-step LEDGER edits so the term stays single. The same applies to `review-spec.md:1295` (PENDING persistence) versus the commit/push/plan `pending_row_preview` edits.\n\n### Tier 2 — the center stack\n\n| Skill | Files | Edits | Bump |\n|---|---|---|---|\n| **gabe-cc-init** | `references/adopt-spec.md` (13) | **13** | 1.3.0 → 1.3.1 |\n| **gabe-cc-update** | `SKILL.md` (8) · `references/feature-spec.md` (2) | **10** | 1.7.2 → 1.7.3 |\n| **gabe-cc-entity** | `SKILL.md` (3) · `references/entity-spec.md` (2) | **5** | 1.0.1 → 1.0.2 |\n\n`entity-spec.md:7` and `:96` correct a *provably stale* seam claim (the server already imports `build_pack`, and `phase_context` already consumes it) — these two are accuracy fixes, not enhancements.\n\n### Tier 3 — sweeps, routers, satellites\n\n| Skill | Files | Edits | Bump |\n|---|---|---|---|\n| **gabe-help** | `SKILL.md` (11) · `references/help-spec.md` (1) | **12** | 1.2.1 → 1.2.2 |\n| **gabe-handoff** | `references/handoff-spec.md` (9) · `SKILL.md` (2) | **11** | 2.1.0 → 2.1.1 |\n| **gabe-pulse** | `references/pulse-spec.md` (6) · `SKILL.md` (5) | **11** | 1.7.2 → 1.7.3 |\n| **gabe-health** | `references/health-spec.md` (4) · `SKILL.md` (3) | **7** | 1.2.0 → 1.2.1 |\n| **gabe-assess** | `references/assess-spec.md` (6) · `SKILL.md` (1) | **7** | 1.3.0 → 1.3.1 |\n| **gabe-roast** | `references/roast-spec.md` (3) · `SKILL.md` (1) | **4** | 1.1.0 → 1.1.1 |\n| **gabe-myopic** | `method.md` (1) | **1** | 1.2.0 → 1.2.1 |\n\n> **Apply hazard — `gabe-help/SKILL.md` has a RETIRE.** The line-48 deletion shifts 49, 59, 64, 82, 99. Apply descending. Its five legacy-GSD edits (47, 48, 49, 64, 82) are **one landing set** — land them together or the dashboard names a probe that no longer exists.\n\n### Tier 4 — authoring, setup, meta\n\n| Skill | Files | Edits | Bump |\n|---|---|---|---|\n| **gabe-mockup** | `SKILL.md` (6) · `references/react-story.md` (5) · `references/refine.md` (4) · `references/legacy-html-phases.md` (4) | **19** | 2.1.0 → 2.1.1 |\n| **gabe-docs** | `references/disposition-contract.md` (2) · `references/evidence-doctrine.md` (3) · `references/docs-spec.md` (1) | **6** *(+8 in Tier 0)* | 1.4.1 → 1.4.2 |\n| **gabe-init** | `references/init-spec.md` (6) · `SKILL.md` (1) | **7** | 2.3.3 → 2.3.4 |\n| **gabe-map** | `references/map-spec.md` (4) · `SKILL.md` (1) | **5** | 1.1.0 → 1.1.1 |\n| **gabe-imagine** | `references/dissection/dissection-method.md` (3) · `SKILL.md` (1) | **4** | 1.4.0 → 1.4.1 |\n| **gabe-scope-change** | `SKILL.md` (1) | **1** | 2.2.0 → 2.2.1 |\n| **gabe-scope-pivot** | `SKILL.md` (1) | **1** | 2.1.0 → 2.1.1 |\n\n`init-spec.md:192` is the highest-value edit outside Tier 0: without it, `/gabe-init` never seeds `.kdbp/map-deltas.jsonl` into `.gitignore`, and gabe-map's emit gate (d) — `git check-ignore -q` at `mapquery.py:380` — **silently drops every delta** in any project scaffolded from the spec. Verified in source.\n\nThe five `gabe-map` edits are pure staleness (spec still says \"seven tools\"; `tests/gabe-map/checks.py:238` asserts 15).\n\n---\n\n## 2 · Gap check\n\n`ls skills/` → **30 `gabe-*` skills, all 30 covered** by the eleven batches. Six carry zero surviving edits: three were correctly refuted (gabe-lens, gabe-docsite, gabe-scope), three were read and yielded nothing (gabe-artifact, gabe-meme, gabe-kdbp).\n\nI confirmed three gaps by opening files and reading server source.\n\n### GAP 1 — `gabe-kdbp/references/kdbp-spec.md` got zero edits, and it is the source of the sweep's 13 most-repeated corrections\n\nThe binding contract for the kdbp server **under-documents three tool behaviors**, each of which consumer batches then had to rediscover and correct for independently:\n\n| Under-documented | Verified in source | Consumer edits that had to correct for it |\n|---|---|---|\n| §4.1 — `plan.status` / `current_phase` / `goal` come from **PLAN.json only** (`(pj or {}).get(...)`); `phases` comes from PLAN.md. On an unmirrored plan the first three are `null` while `phases` populates. | `kdbp_tools.py` snapshot body, `\"status\": (pj or {}).get(\"status\")` | **~7** — plan-spec:94, plan-spec:759, gate-spec:33, push-spec:17, push-spec:592, help SKILL:49, handoff-spec:60 |\n| §4.2 — `if not pid: … return out` returns **before** `out[\"behavior\"]` is assigned, so a KDBP project between plans gets no `behavior` block at all. §4.2 lists `behavior{…}` unconditionally. | `t_phase_context`, early return at the `if not pid` branch | **2** — gate-spec:29, review-spec:67 |\n| §4.6 — the preview emits `\"Status\": \"\"` (blank) and `\"Times Deferred\": \"1\"` hardcoded. Neither is documented. | `kdbp_tools.py:504` | **4** — gate-spec:293, gate-spec:534, push-spec:391, review-spec:1304 |\n\nThe §4.1 asymmetry is the single most-repeated correction in the entire 219. Each consumer edit says it in its own words, which means the next spec author who does not read all seven will get it wrong again. **Recommend: 3 edits to `kdbp-spec.md` §4.1/§4.2/§4.6 stating these at the source**, folded into Tier 0 beside the execution-contract (gabe-kdbp 1.0.0 → 1.0.1). Without them the fix lives in seven places and the contract still lies.\n\n### GAP 2 — `who_calls`'s `emit: false` argument is named by **zero** of the 219 edits, and the emit law is missing from the proposed suite-wide section\n\nConfirmed in `skills/gabe-map/scripts/tools.py:453`:\n\n```\nemit = bool(args.get(\"emit\", True)) and not os.environ.get(\"GABE_MAP_NO_EMIT\") and not transitive\n```\n\nDefault `direction=in depth=1` **emits** to `.kdbp/map-deltas.jsonl`. Four skills declare `context: fork` (gabe-health, gabe-myopic, gabe-pulse, gabe-roast), and `gabe-health/SKILL.md:19` states \"never modifies files\". The emit happens inside the MCP server process, so an `agent: Explore` restriction on Write/Edit does **not** stop it.\n\nThe lenses batch handled this correctly *per skill* — it put who_calls only in gabe-assess (not a fork skill) and in gabe-roast at `direction=out`, which provably never emits. But two things were missed:\n\n1. The tool ships an explicit `emit: false` boolean for exactly this case (`tools.py:650`). No edit anywhere names it. That is the clean escape a fork skill should be told about.\n2. The proposed `## The tool floor` section states four laws — ask-first, floor-never-scope, honest-empty, not-rails — and **no read-only law**. It licenses \"any step that asks *who calls*\" to reach for who_calls, in the one file all 30 skills point at, including all four fork skills.\n\n**Recommend: a fifth bullet in the Tier 0 tool-floor section** — every tool is read-only except `who_calls`, whose default appends a gitignored delta line; a read-only or fork run passes `emit: false`, or uses `direction=out`, which never emits.\n\n### GAP 3 — `map_diff` is the one tool no edit uses; verified genuinely unreachable, not overlooked\n\n`t_map_diff` answers \"how did the committed map change between two refs, per entity\" — endpoints/models/schemas/files added and removed, plus `census_delta` (unclaimed counts before vs after) and a function-count delta. I traced its only plausible home, `/gabe-cc-update release`: `feature-spec.md:230-238` defines Contents v1 as \"curated proof shots + diagrams + each feature's summary/narration\", a re-runnable join over authored card material. There is **no \"what changed in the code since the last release\" step** for map_diff to be a first look on. Adding one would be a new step, outside this pass's shape.\n\nReport it as an **over-build signal**, not a gap to fill: one wave-2 tool has no spec text reaching for it, and `census_delta` (is adoption *closing* blindness or growing it?) is the strongest candidate if a future pass wants to earn it back.\n\n### Non-gaps I confirmed, so they are not re-opened\n\n- **gabe-meme / gabe-artifact** — no where/who/what-touches step exists. Meme is config-seeding plus writing craft; artifact's E4 (`SKILL.md:109`) is about republishing to the same artifact URL, which no map tool indexes.\n- **gabe-docsite `SKILL.md:52`** — the four-token-class reference extraction is performed by `generator/_references.py` (confirmed: the only file besides SKILL.md mentioning `adoption.json`). **L4 protects it.** The batch's single refutation of the CENTER MODE probe was also correct — this repo has both seam paths and `map_status` still returns `present:false` under ruling R8.\n- **gabe-assess needs no `pending_row_preview` edit** — grep finds zero `PENDING.md` references in the skill; its \"Defer\" at `assess-spec.md:93` is a D-dimension question, not a row write. Minor note: `disposition-contract.md:53` lists assess among \"the six model-authored PENDING sites\", which appears inaccurate — pre-existing text, not created by this pass.\n- **`gabe-init` only scaffolds an empty PENDING.md header** (`init-spec.md:119`) — the preview composes rows, not headers. Correctly excluded.\n\n---\n\n## 3 · Price\n\n**Files that change: 49.** 48 spec `.md` files, plus `CLAUDE.md`.\n\n**Skills that need a patch version bump: 24.** Any spec text change bumps. `suite-doctor.sh` P3 (line 113–123) enforces `SKILL.md metadata.version` **==** the CLAUDE.md Capabilities row — so all **24 CLAUDE.md version cells must change in the same commit**, or the doctor fails with `version drift: <name> SKILL.md=X vs CLAUDE.md=Y`. `README.md` carries no versions (verified: zero capability rows) and needs no change.\n\nIf the three GAP-1 edits are taken, that is 52 edits' worth of scope becoming **52 files → still 49 files + gabe-kdbp's two** = **50 files, 25 skills**.\n\n**What this pass cannot break, verified:**\n- No live test battery asserts on skill markdown. The only two that do are under `tests/_archive/`, which suite-doctor skips explicitly (line 101). **219 spec edits cannot turn a battery red.**\n- The portability lint (P6) matches only the literal operator home `/home/khujta`; the `~/.claude/skills/gabe-pulse/scripts/…` fallbacks the review edits preserve already exist in that file today. No exposure.\n- `gabe-help`'s generated CATALOG block sits at `help-spec.md:97–136`; the single help-spec edit is at line 30, outside it. `install.sh` will not clobber it.\n\n**Then it must run clean:**\n```\n./install.sh          # ~5 s\nscripts/suite-doctor.sh   # ~2–4 min (re-runs every tests/*/run.sh battery)\n```\nThe doctor's realistic failure mode here is exactly one: **version/count parity**, from a missed CLAUDE.md cell. Since no battery reads spec text, a green doctor after this pass proves parity and the untouched enforcement layer — it does not prove the 219 edits are *right*. That evidence is the adversarial verification already recorded per edit, plus the two twins.\n\n**Honest residual:** this changes what 24 skills *instruct*, in a repo that carries no `.kdbp/` (ruling R8) and no codebase map, so none of the new tool paths execute here. Their first real exercise is a twin, and the two servers were only registered and fixed this session (HEAD `e4b80e8`) — a session restart is required before any of it runs live.",
 "anchor_note": "current_excerpt + sweep_rationale folded back from the workflow journal (the verifier's schema did not carry the anchor forward). Join key is (file, line, tool) — two edits CAN share a file+line, so a (file,line)-only join silently gives them the same anchor. Apply by exact-string match on current_excerpt, never by line number: lines shift as a file is edited."
}