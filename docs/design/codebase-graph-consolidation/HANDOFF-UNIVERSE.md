# Handoff — Gabe Universe: schema homing → coverage-class fix map → tiers (in that order)

> Generated 2026-08-27 · branch `graft-adoption` · HEAD after this commit · no `.kdbp` (suite advisory arm).
> Durable state: `~/.claude/projects/-home-khujta-projects-gabe-lens/memory/gabe-universe-station.md` (+ `febe-trace-model.md` for the gap audit).
> This file is the paste-able resume prompt for the next session.

## Resume prompt (2026-08-27, end of session 2 — paste this)

Continue the **Gabe Universe** station on branch `graft-adoption` (suite local, HEAD `58522c7`, UNPUSHED — 7 commits since `0245215`; gustify `graft-pilot` local at `d1880cf4`, UNPUSHED; push only on operator word). No `.kdbp` here by design. Regen: `cd docs/design/codebase-graph-consolidation/universe-build && bash regen-example.sh` (`--check` = the reproducibility contract, CLEAN at HEAD); edit `parts/*` + `assemble.py`, never the generated page; proofs solo-sequential (`verify-*.mjs`); artifact gates via the scratchpad system-chrome shim (memory `artifact-gate-chromium-wsl`: `_gate.mjs` patching `chromium.launch` to `/usr/bin/google-chrome-stable`).

READ FIRST: memory `gabe-universe-station.md` (tail: item 1 landed · item 2 delivered · the pre-flight amendments), then `docs/design/codebase-graph-consolidation/COVERAGE-FIX-MAP.md` (§0 waves · §1 class map · §2 journeys · §3 decisions · **§3′ pre-flight amendments** · §4 gaps), `SCHEMA-HOMING.md` (landed), `DISCLOSURE-TIERS.md` (tabled; item 3). Evidence: `fix-map/{findings-digest.txt, preflight-digest.txt, chain.py, before-chains.txt}`.

STATE (all evidence in git)
- ITEM 1 SCHEMA HOMING — LANDED: `a81fb42` (home_schemas at the archmap seat · S12 · +37 battery cases · estate) · `cff92ce` (nested-only fold + count badge · `verify-schemafold.mjs` 14/14) · `4af0f4b` (S12 nags only on live-unwired). Twin `d1880cf4` (config claims notifications/history → cooking, admin_review → pantry; propagated generators; center 565/0). Landed numbers: moved 14 · ambiguous 7 · unwired 2 (dormant) · nests cross 6→15 · 33/123 fold · endpoints 67→72. Artifact 🏠 `f1994c49-f28e-4516-bcc5-8be3a8cb861a`.
- ITEM 2 COVERAGE FIX MAP — DELIVERED + PRE-FLIGHT REVIEWED, not built: `6625ac6` + `58522c7`. Operator AGREED the plan, the 11 decisions and the recommendations. Artifact 🧩 `f91b3b59-a7ca-4c8b-a855-28a945f9a5bf` ("pre-flight amended"). Builders in `artifacts/` (`GABE_ART_OUT=<dir> python3 …build.py`, two gates, republish by `url:`).
- Verification this session: orm-access 45+23+7 · pulse-angles 42 · arch-graph 199 · levels 65 · codebase-graph 245 · gabe-universe ALL PASS · schemafold 14/14 · REGEN CHECK CLEAN · artifact gates 36/36 + motion 5/5 (🏠) / 4/4 (🧩). Decayed (owed baseline run before wave B, NOT a re-pin): verify-{panels,walk,explore,routes}.
- Default model set to `claude-opus-4-8` in `~/.claude/settings.json` (operator ask at 96% Fable usage).

TASK — BUILD WAVE A, exactly as amended (COVERAGE-FIX-MAP.md §0 + §3′; iterate-before-implement is satisfied — the operator said "we can proceed"):
1. `_a3_code.route_census(repo)` + `file_census(repo)` beside `model_census` (same pattern: dirs from the configured files, every unclaimed `.py` with route/fn/table counts + reason; `{}` honest-empty; cached) + `_a3_graft.derive_reach` (BFS from mapped handlers over graft `calls` → min hops per unclaimed file; honest-empty without graft) → `amap["route_census"]` / `amap["file_census"]` emitted ONLY when non-empty (§3′ P5).
2. Pulse **S13** (route + file census, standing reminder → `/gabe-cc-init`; S12 is taken) + the cc-init adopt-spec rail paragraph + CLAUDE.md / gabe-pulse SKILL.md parity (version bump).
3. Unify first-claim-wins to CONFIG order in `_a3_graft._file2slug` (today graft is alphabetical, function_insight is config order — §3′ P8); a build-time report line for a claim under an undeclared `code_layers` layer.
4. gastify one-time GRAPH-ARM ADOPTION (copy `_a3_graft/_a3_graph/_a3_levels/_a3_web/_a3_fe` + the shell station; propagate.sh is update-only) — dry-run on a COPY first; numbers in the commit.
5. Batteries first (red → green): orm-access (census FIRE/SILENT/honest-empty/deterministic), arch-graph (reach honest-empty), pulse-angles (S13 ×4). Then the twin-read-only regen (`regen-example.sh`), `install.sh`, the numbers in the commit message.
6. THEN hand the operator the gustify claims list (fix-map/findings-digest.txt, class 3: the 17 request-path files; route files; schema files consent/catalog/recipe_filter_mode; reference/* with `code_layers += reference`; pipeline runtime; main.py) — the claims are the operator's; the claims commit moves L1 coupling (say so).
Constraints: nothing gustify-shaped in the suite (second-twin idioms enter as idioms); every new key non-empty-only; `regen-example.sh --check` must stay CLEAN; no `.kdbp` in this repo; push only on operator word.

RUNBOOK + GOTCHAS: absolute paths in chained proofs · `git -C` when the cwd drifts · seed optional files rather than runtime-load them (link gate) · usecase core clusters a handler's fns WITH its endpoint (re-hide after goto in proofs) · `__d2wBand()` returns a colour int · popups born inside a fixed-height panel must be body-level + fixed · a `uses` body-name consumer nearly killed the schema fold — fold predicates key on ENDPOINT wires only · the cross-resolver's same-entity guard silently drops residue that homing made own (land intra edges) · hidden (folded) parents have no `__threeObj` (badges attach on reveal) · finders that measure "today" must name the HEAD they read (three of fourteen measured HEAD~3).

AFTER THAT: pre-C kind extensibility (station-first) → wave B (precedence first, then root-walk/symtab/attr-writes/sinks-guard/role-split/access-for-any-role, then methods + hub fold; a recorded baseline proof run before it) → wave C per floor → wave D → item 3 tiers (the operator proposes the control design; floor rows for every new kind).

## State snapshot (updated 2026-08-27, session 2)
- Landed: ITEM 1 SCHEMA HOMING — `a81fb42` (emitter rule + S12 + batteries + estate) · `cff92ce` (fold + count badge + proof). Record: `SCHEMA-HOMING.md` + artifact **Schema Homing** 🏠 `f1994c49-f28e-4516-bcc5-8be3a8cb861a` (builder in `artifacts/`). Operator agreed all recommendations; the 6 unwired = 2 unclaimed route files (claimed on the twin) + the dormant gastify contract lane (S12 counts, never nags).
- Twin (gustify graft-pilot): config claims api/notifications.py + api/history.py → cooking, models/admin_review.py → pantry (closes the 2 admin-lane models); `propagate.sh` ran → COMMIT the twin on graft-pilot (config + scripts + center regen). Push both repos on operator word.
- ITEM 2 DELIVERED (draft, not built): `COVERAGE-FIX-MAP.md` + artifact **Coverage Fix Map** 🧩 `f91b3b59-a7ca-4c8b-a855-28a945f9a5bf` (builder in `artifacts/`); evidence in `fix-map/` (findings-digest.txt · findings.json.gz · chain.py · before-chains.txt). Workflow `wf_37cd71af-e77`: 14 finders + anatomy cross-check → 14 refuters (4 refuted on a central claim; all corrected). THE VERDICT: four waves — A census + claims (additive + config) · B ONE substrate commit (ORM idioms · methods admission + hub relief · gate precedence · sinks re-scope · touches role split — change-in-place, one regen, one re-pin) · C roots + wires per floor (depends/gated_by · BOOT · dispatches bus+tables · serializes · provider/reaches · flag/walls · WS/transport) · D one consolidated levels §3c draw rule + station kinds. Eleven decisions owed (the twin's claims · B as one commit · attr-write gating · gate precedence · hub relief · gate-only deps · BOOT seat · session/http guard · §3c consolidation · defer AI prompts · touches split). AI prompts: DEFER until a twin with ≥3 prompts.
- PRE-FLIGHT REVIEW DONE (2026-08-27, `wf_8e7f30b3-f84`, 7 lenses, stopped on budget before the refuters): §3′ of COVERAGE-FIX-MAP.md — P1 kind extensibility is PRE-C station-first (`_L2_KINDS.index()` crashes on a new kind; column station NaN; adapter drops) · P2 `serializes` must not un-fold the schema fold · P3 levels attaches `access` only for accessor → attach for any role; precedence swap first · P4 gastify needs a one-time graph-arm adoption (propagate is update-only; no c4/levels there) and its idioms (Annotated-alias Depends · pydantic_ai · BACKGROUND TASKS root · websocket_connect) enter the detectors · P5 non-empty-only keys · P6 no new pseudo-entities (reuse `__unclaimed__`) · P7 the walk rel set gains dispatches+depends · P8 baseline proof run before B, _BASE fix is wave B, unify first-claim-wins, layer report, n/a for untestable kinds, trigger dropped. Operator AGREED plan + decisions before the review.
- NEXT SESSION = BUILD WAVE A: `route_census` + `file_census` (+`derive_reach`) beside `model_census` · pulse S13 + cc-init rail · first-claim-wins unified to config order (`_a3_graft._file2slug`) · undeclared-layer report line · gastify graph-arm adoption · then the operator's gustify claims (the 17-file list in fix-map/findings-digest.txt, class 3) → regen → numbers in the commit. · proof re-pins (panels/walk/explore/routes) · gastify propagation · then ITEM 3 tiers (operator proposes the control design; the new kinds enter the ladder's floors).

**WAVE A LANDED 2026-08-27 — suite `da5a10a` (graft-adoption, LOCAL/UNPUSHED).** Emitter (`route_census`/`file_census`/`derive_reach`/`reach_arm`/`undeclared_layers`, non-empty-only P5, `_file2slug`→config order, archmap version 2) · pulse S13 (v<2→predates unavailable, reach-ranked) + gabe-pulse 1.7.1 + cc-init rail + parity · batteries mutation-proven (orm-access +15 · arch-graph 199→203 · pulse-angles 42→48) · `regen-example.sh --check` CLEAN (only move: 7 fns `services/recipes.py` allergen→recipe) · adversarial review 1 HIGH+2 MED+3 LOW ALL FIXED. Measured (twin-read-only, trees untouched): gustify route 5 / file 19 (8 reach); gastify graph-arm dry-run c4 6 nodes/12 edges/graft present, census 12/44.

**GUSTIFY CLAIMS + GASTIFY ADOPTION + PRE-C ALL LANDED 2026-08-27 (session 3 cont.):**
- gustify `50f9aac7` (graft-pilot): 14 request-path files claimed + wave-A generators → endpoints 72→80 · route census CLEARED · L1 12→36 · v2. 6 orphan-dir fix-map files still need `code_layers`/dir-claim (middleware/idempotency · net/client_ip · reference/catalogs · reference/normalize · streaming/producer · observability/metrics).
- gastify `756758f8` (main): FIRST c4/levels/census (adopted the 7 graph-arm modules + wave-A generators) → c4 6 nodes/12 edges · census 12/44/14 to claim (operator's call). Its group-share config + 17-day center regenerated together (operator: on hold, OK to modify).
- pre-C `f27be20` (graft-adoption): the 4 wave-C kinds (middleware·provider·flag·prompt) + 7 rels (depends·gated_by·dispatches·serializes·reaches·walls·fnprompts) drawable — `_L2_KINDS` append-only, generic drop-guard (no silent drop), column no-NaN guard, RELCOL/LINKMETA/REL2KIND/relLabel rows. Batteries: arch-graph 205 · gabe-universe ALL PASS · codebase-graph 249. --check CLEAN. Claims catch-up split out as `b50392b`.

**WAVE B COMPLETE 2026-08-27 (3 commits, graft-adoption LOCAL):**
- B0 `453de4c` — precedence swap (gate before accessor) + P3 (levels access-for-any-role). 1 fn flips (ensure_principal_household accessor→gate, access kept — NOT byte-identical because the claims homed ownership.py).
- B1 `8fb1c83` — the ORM substrate: attr-write detector + column-select root-walk + 3 binders + http guard. gustify accessor fns 117→165, http false-sinks 5→0, 36 pure→accessor. Deferred: commits/flushes split · touches split.
- B2 `4da8b79` — methods admission + hub fold (require_household fan-in 50 folds under critical). methods drawn 2→5, fn_edges 191→243.
- Baseline recorded (P8): the 4 decayed proofs fail on PANEL pins, 0 console errors — not data. All 3 --check CLEAN, install 29/29, every data battery green + mutation-proven.

**PUSH all 3 repos on operator word.** **NEXT = wave C** (per-floor roots+wires, each with an honest-empty pin + station rows, using the pre-C plumbing: depends/gated_by middleware · BOOT root into graft_arm only · dispatches bus+PIPELINE_STAGES tables · serializes site>naming — must NOT un-fold the schema fold (P2) · provider/reaches · flag/walls polarity · WS/transport) → wave D (reveal rule + walkable rel set) → item 3 tiers. Then the deferred B1 label splits + touches split.
