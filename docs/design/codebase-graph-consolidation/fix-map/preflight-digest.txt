
## wave-C floors: honest-empty + determinism (key-conditional emission · sorted walks · L1 si
- [BLOCKER] C-1: Every new L2 node kind the wave mints (class 8 `middleware`, class 9 `provider`, class 12 `flag`) either crashes the whole c4 build or moves bytes on EVERY twin — the plan's 'additive, honest-empty each, byte-identical when the idiom is absent' cannot hold as written for the first floor that adds a kind.
    FIX: In §0 wave C, state: (1) all three kinds are appended to `_L2_KINDS` in ONE commit (the first C floor), so `layout.l2.order` moves once — a stated one-time byte move like `web`, re-land the example estate in that commit; (2) kinds are constants `middleware` · `provider` · `flag`, ids `provider:<name>` / `flag:<NAME>`; (3) add an arch-graph check that the order tuple is append-o
    wave C · ev: templates/center/generators/_a3_graph.py:71 `_L2_KINDS = ("endpoint","model","schema","external","web")`; :712 `nodes.sort(key=lambda n: (_L2_KINDS.index(n["kind"]), …))`, :803 `col = _L2_KINDS.index(kind)` in _stamp_l2, :1099 the unclaimed-bucket sort — a kin
- [SHOULD-FIX] C-2: Several floors as specified in the digest emit an EMPTY/absent record on no-idiom projects, violating the byte-identical claim the plan makes for them; the plan only states key-conditional for class 6's c4 stats.
    FIX: Add one line to §0 wave C: 'every new key — archmap (`boot_roots`, `dispatch_map`, `dispatch_tables`, `app_middleware`, `flags`, `providers`, `route/file_census` results), c4 `stats.<floor>`, endpoint `transport`/`flags`, fn `flags`/`externals` — is emitted ONLY when non-empty (the `schema_homing` idiom), never as [] / 0 / present:False'; add the per-floor honest-empty check as
    wave C · ev: findings-digest boot-writers: 'the only new bytes are stats.boot' and 'Emit amap[boot_roots] (list, [] when none)' — its refuter (CORR line 132) already flagged both as non-identical; the plan §1 row 7 carries no key-conditional statement.; digest event-bus HO
- [SHOULD-FIX] C-3: Where the `dispatches`/`depends` synthetic edges are folded decides whether L1 moves: folded as relation `calls` into `wiring["edges"]` they change L1 edge kinds, `stats.graft.cross_calls` and the `confidence.calls.extracted` trust split; extending `_RELATIONS` adds `cross_by_relation.dispatches`/`confidence.dispatches` keys to every project. The plan leaves 'the L1 derive_cross kinds decision' open.
    FIX: State the seam once in §1 rows 6 and 8: graft_arm builds `wiring2 = {**wiring, "edges": wiring["edges"] + synthetic}` with the synthetic edges tagged `relation: "dispatches"|"depends"` + `confidence: "extracted"`; `_behind_context` admits `relation in ("calls","dispatches","depends")`; derive_functions admits the same set and carries `rel`; `_RELATIONS` and `_L1_RENDER_RELATION
    wave C · ev: templates/center/generators/_a3_graft.py:207-220 `_behind_context` builds the adjacency from `relation == "calls"` only and is called SEPARATELY by derive_behind (:264), derive_fn_behind (:286), derive_endpoint_access (:303), derive_fn_roles (:371), derive_dis
- [SHOULD-FIX] C-4: The L1 layout has no seat for a `boot` (or `config`) bucket, and the plan's 'positioned like _L1_UNCLAIMED' collides with the unclaimed bucket on the second twin: gastify's L1 already carries the unclaimed bucket (FK-unresolved) AND has a lifespan, so both would land at (0,0).
    FIX: Replace the per-class bucket branches with ONE generic mechanism in the first C commit: `_BUCKETS = {"__unclaimed__": (0,0), "__boot__": (0,-150), "__config__": (0,150)}` read by `_stamp_l1`/`_stamp_l1_flow`; one `_mint_bucket(slug)` helper used by C3 minting, class 7, 8, 12; levels filters `l1_edges` on `slug.startswith("__")`; `_gcolors` seats all three; `stats.unclaimed` kee
    wave C · ev: _a3_graph.py:716-728 `_stamp_l1`: x/y only for `kind == "entity"` and `kind == "unclaimed"`; :731-795 `_stamp_l1_flow`: fx/fy likewise — a `boot` L1 node would carry NO coordinates → the column station reads undefined → NaN.; Measured gastify twin-read-only bu
- [SHOULD-FIX] C-5: Class 8's Depends resolver is gustify-shaped: the second twin declares its auth dependencies as MODULE-LEVEL `Annotated` aliases, which the existing detector cannot see — on gastify K1 would stay undrawn while reading as honest-empty.
    FIX: Row 8: add 'resolve a param annotation that is a bare Name through a module-level alias symtab (`X = Annotated[T, Depends(f)]`, same file or imported via _file_imports) before `_annotated_depends`'; add a gastify-shaped fixture (alias defined in one module, used in another) to the FIRE battery. ~15 lines in _a3_code.
    wave C · ev: gastify backend/app/api/*.py: `Auth = Annotated[AuthContext, Depends(get_auth_context)]` (1), `CurrentUser = Annotated[FirebaseUser, Depends(get_current_user)]` (1), `DB = Annotated[AsyncSession, Depends(get_db)]` (17 files) — handlers take `auth: Auth`, never
- [SHOULD-FIX] C-6: Wave C ships new node kinds/methods into every twin's feeds while the station work is deferred to wave D; the COLUMN station (shipped in each twin's shell) renders unknown kinds at NaN positions and draws BOOT/WS endpoints as GET in between.
    FIX: Move the MINIMUM tolerance into each C floor commit (same commit as the feed kind): column station fallback column `kc[n.kind] ?? ord2.length`, `KIND_LABEL`/glyph default, `METHOD.BOOT`/`METHOD.WS` entries in both stations; leave legend/detail/hub-fold in D. ~12 lines JS; state in the wave table's D row that C carries 'kind tolerance', D carries 'kind rendering'.
    wave C · ev: templates/center/shell/codebase-graph.html:902-905 `ord2=["endpoint","schema","model","external","web"] … p[n.id]={x:kc[n.kind]*COLW}` → `undefined*240 = NaN` for middleware/provider/flag; :609 `KIND_LABEL` and :1226 `ord3` hard-coded; :1250-1256 the glyph els
- [SHOULD-FIX] C-7: The class 9 provider registry as specified is gustify-shaped: the second twin's LLM provider is `pydantic_ai`, absent from the allowlist, so gastify would draw `provider:httpx` (an FX-rate fetch) and NO LLM provider on an LLM app.
    FIX: Row 9: make the registry `center.config`-extendable (`"providers": {"pydantic_ai": "llm", …}`) over a wider default that includes pydantic_ai · openai · anthropic · google.genai; state the id rule (global `provider:<name>`, homed to the sorted-first reader entity, other entities reach it via cross_edges `reaches`). ~10 lines + 1 config key.
    wave C · ev: gastify backend/app: `from pydantic_ai import Agent` in agents/{extraction,categorization,statement_extraction,store_categorization}.py (+ prompt_lab runners) = 8 import sites; `import httpx` in services/fx.py (3 sites); 0 sentry_sdk / firebase_admin / google.
- [SHOULD-FIX] C-8: Two floors read runtime-flavoured state whose determinism is not pinned: class 12's 'show the EFFECTIVE value' and class 7's `gate:'deploy-only'` — and the deploy-only predicate reader is gustify-shaped.
    FIX: Rows 7 and 12: state 'defaults and effective values come from the AST literal + the `or is_production` shape only — never instantiate Settings, never read env'; for deploy-only emit the predicate TEXT (`ast.unparse(if.test)`) as the floor and a classified `gate` only when a stated pattern matches (else honest-empty, text kept). Battery: build with a conflicting env var exported
    wave C · ev: Plan row 12: 'show the EFFECTIVE value' of `flag OR is_production` — if computed by instantiating Settings or reading env/.env, a build host with `RECIPE_CREATION_ENABLED=true` exported flips bytes (a wallclock-class leak); the no-wallclock law (regen-example.
- [SHOULD-FIX] C-9: New index-state-dependent counters would break regen-example.sh --check: its normalizer only masks the four existing `dropped` reasons.
    FIX: Rule in §4 'The numbers move': any counter derived from the graft INDEX (not from AST) lands under `stats.graft.dropped` AND its name is added to the `norm()` regex in the same commit; AST-derived counts (dispatch registry, flags, providers) stay unnormalized because they reproduce. One line of plan text + the regex edit per commit.
    wave C · ev: regen-example.sh:57-63 `norm()` masks `head`, `index_hash`, `reason` (as-found|rebuilt) and `"(intra_entity|noise|unmapped_file|unresolved_target)": N` only; the example is built as-found (`GABE_GRAFT_BUILD=0`, :31) while twins build rebuilt.; Class 3's 'tests

## Feed consumers — every c4/levels reader that must learn the new node kinds (middleware · p
- [BLOCKER] CONS-1: The column station has no fallback for an unknown L2 node kind: the default column layout computes x = kc[n.kind]*COLW with kc built only from [endpoint,schema,model,external,web], so every wave-C node (middleware · provider · flag · prompt) lands at x=NaN — a translate(NaN,…) transform and NaN path d for every wire touching it (console errors, nodes piled at the origin). Wave C 'per-commit, measured on gustify' with
    FIX: Before the first wave-C floor: give computeL2Positions (and surfaceLayout, which silently drops unknown kinds at :866-871) an 'other' column appended after web (kc default = ord2.length) + add the kind to KIND_COLOR/KIND_LABEL with a muted default; then land each wave-C floor with its station column/legend row in the SAME commit (station-first for the column station, never emit
    wave C · ev: templates/center/shell/codebase-graph.html:902-904 — `var ord2=["endpoint","schema","model","external","web"] … p[n.id]={x:kc[n.kind]*COLW, y:n.y}` (kc[unknown] is undefined → NaN); templates/center/shell/codebase-graph.html:1247 — `transform:"translate("+node
- [BLOCKER] CONS-2: Class 5b's `serializes` (schema→model, non-function ends) reverses the landed schema fold: __uniComputeSolo treats any non-`nests` wire whose ends are not functions as a CONTRACT, so a schema gaining a serializes wire stops folding. Measured on the committed gustify c4: 32 schemas fold today; 13 of them carry from_attributes (orm:True) and 10 have X.model_validate sites — i.e. 10–13 of 32 (the seven MeResponse/Settin
    FIX: Ship the predicate amendment in the serializes commit: make the contract set an ALLOWLIST (`touches · consumes · resp · bridge` — the comment at layout.js:2091-2092 already states it) instead of 'everything but nests/fn', so serializes (and depends/gated_by/walls, none of which are an exposure) never block the fold; re-pin tests/gabe-universe:444 and verify-schemafold.mjs. ~3 l
    wave C · ev: docs/design/codebase-graph-consolidation/universe-build/parts/layout.js:2096-2098 — `if(l.rel==="nests"){…return;} if(sn.kind==="function"||tn.kind==="function") return; if(sn.kind==="schema") _sW[sn.id]=1; …` (only nests and fn-ended wires are exempt); layout
- [SHOULD-FIX] CONS-3: Every new wire rel falls into the `calls` bucket and is marked PROVEN by default in the Universe: REL2KIND lacks depends/gated_by/dispatches/serializes/reaches/walls/fnprompts and the three draw seams do `REL2KIND[l.rel]||'calls'`; a calls-bucket wire is coloured by the d2w heat band of its TARGET (a model/middleware has no d2w → 'never reaches a write' colour), LINKMETA's default is {w:2,pv:1} so graft-inferred disp
    FIX: One wire-kind registration per new rel landed in the emitter's commit: REL2KIND bucket + CONN style (assemble.py:506-511, so the legend row draws the REAL wire) + LINKMETA + RELCOL + relLabel/CONNICO + __CONNDESC + a legend Connectors row; carry per-edge `conf` (and via/scope/on_fail) through adapter.js:111-112 and set l.proven from conf (extracted→true, inferred→false) instead
    wave C · ev: docs/design/codebase-graph-consolidation/universe-build/assemble.py:15 REL2KIND (no new rels); :555, :676, :695 `REL2KIND[l.rel]||'calls'`; :695 `_band=(_gk==="calls"&&window.__d2wBand)?__d2wBand(_ct):null`; parts/adapter.js:10-15 RELCOL/LINKMETA (no consumes/
- [SHOULD-FIX] CONS-4: The derived BACKEND journeys (the picker's 'backend' tab and the WALK) traverse only rel==='calls' fn links (+handler, +access). The plan's AFTER sequences for J3 (dispatches leg), K1 (gated_by → depends chain), K3 (dispatches → 10 stages) are drawn as wires but NOT walked — the journey count column ('journeys whose drawn sequence changes') overstates what the picker will show. The class-4 hub relief must also apply 
    FIX: Wave D's one draw rule needs its station twin: `_bkCollect` walks {calls, dispatches, depends} (gated_by as a hop-0.5 step from the endpoint, why:'gate'), and the hub-relief rule (gate-role method, fan-in ≥ N → folded into the endpoint step note, not a step) lives in the same function. ~12 lines + verify-backend-journeys.mjs re-pin.
    wave D · ev: parts/layout.js:822-824 `_FNLINKS.forEach(function(l){ if(l.rel==="calls") (calls[l.source]…) else if(l.rel==="handler") … else if(l.access) …` and :834-836 BFS over `calls[fid]` only; layout.js:257 `_FNLINKS=(D.fn_edges||[]).map(… rel:e.rel||"calls")` — a `di
- [SHOULD-FIX] CONS-5: 'Every twin regenerates once per wave' and 'gastify = the honest-empty / second-idiom test bed' are gustify-only as written: gastify does not vendor the c4/levels arms (_a3_graft/_a3_graph/_a3_levels/_a3_web/_a3_fe/_a3_sim absent from gastify/scripts), its build_center_a3.py is the 2039-line pre-arm version, and its center has no c4-graph/levels/codebase-graph/gabe-universe. propagate.sh is update-only, so a propagat
    FIX: §0/§3: state that wave B's 'one regen per twin' is gustify-only; gastify numbers come from the scratch recipe (`GABE_REPO_ROOT=<gastify> GABE_CENTER_OUT=<scratch>`) and adopting the arms there is a deliberate decision (propagate.sh's own rule). Also guard the twin: propagate.sh should refuse to overwrite build_center_a3.py when a module it imports is in the NEW list (~6 lines).
    wave pre-A · ev: ls /home/khujta/projects/apps/gastify/scripts → _a3_board _a3_code _a3_evidence _a3_feature _a3_guard _a3_ledger _a3_render _a3_tests build_center_a3.py refresh_center.sh (no graph/graft/levels/web/fe/sim); gastify/scripts/build_center_a3.py: 2039 lines, impor
- [SHOULD-FIX] CONS-6: Class 8 (depends) and decision #6 ('draw only gate=True deps') are gustify-tuned: on gastify the auth dependency is a module-level Annotated ALIAS (`CurrentUser = Annotated[FirebaseUser, Depends(get_current_user)]`) used as a parameter annotation, which the middleware floor never sees — the scratch build shows 16/49 endpoints with any dep, names only `limit`(14)/`shared_limit`(6), 0 gate-named. The proposed resolver 
    FIX: Class 8's parse step: resolve a param annotation that is a Name bound at module level to `Annotated[…, Depends(f)]` (same-file or via _file_imports) before the signature scan; add a gastify-shaped fixture (alias dep) to the FIRE side; decision #6 should key gate=True on the alias/return type too (a dep returning a *User* type) or the plan must record gastify's 0 as the known fl
    wave C · ev: /home/khujta/projects/apps/gastify/backend/app/auth/firebase.py:71 `CurrentUser = Annotated[FirebaseUser, Depends(get_current_user)]`; app/auth/deps.py:103 `firebase_user: CurrentUser,`; scratch c4 (gastify): endpoints with middleware 16/49; dep names Counter(
- [SHOULD-FIX] CONS-7: The WS/BOOT method tokens hit a WRONG-VERB fallback, not an honest-empty one: the column station's methodOf returns null for any verb outside GET/POST/PUT/PATCH/DELETE and all three endpoint draw sites do `methodOf(label)||"GET"` — a `WS …/ws` or `BOOT lifespan` endpoint draws as a green GET. The Universe side is honest (no badge) but the badge-key popup, __BADGE_COL/__BADGE_DESC and the gradient endpoint colour need
    FIX: Station-first for the tokens: change the three `||"GET"` fallbacks to a muted 'unknown' verb (graph-grammar's epMark already handles it), then add WS/BOOT to the six METHOD tables + the badge glyphs + popup keys in one commit, THEN admit `websocket` / mint BOOT in the emitter. ~15 lines across 5 files.
    wave C · ev: templates/center/shell/codebase-graph.html:1186 `function methodOf(label){ … return METHOD[m]?m:null; }`; :1250, :1509, :1669 `methodOf(label)||"GET"`; :610 METHOD map; templates/center/shell/assets/sim-panel.js:71 METHOD_COLOR; templates/center/shell/codebase

## Wave B as ONE commit — ordering constraints between the five fixes, shared helpers, intra-
- [BLOCKER] B-1: The gate-before-accessor swap lands in _a3_graft only, but _a3_levels attaches a fn_node's `access` block ONLY when role == accessor — so the swapped gate loses its drawn reads. The plan's stated AFTER (W2 detail 'the gate reads its 3-table snapshot', W4/W8 guard reads) cannot render on the node as written.
    FIX: In the same B commit change _a3_levels.py:362 to `if _role in ("accessor", "gate"):` (or attach `access` whenever FI has ops, role-agnostic — simplest, ~1 line) and add a levels FIRE case: a graft fixture with fn_roles {x: 'gate'} + FI access ops for x ⇒ the fn_node carries `access` (mutation: role 'pure' ⇒ still carries it under the role-agnostic form). ~3 lines + ~8 battery l
    wave B · ev: templates/center/generators/_a3_levels.py:362-365 — `if _role == "accessor":` gates `_node["access"] = _acc`; no other branch attaches access; universe parts/layout.js:267-269 — the fn→model fnreads/fnwrites wires are minted from `f.access.ops` only (role-agno
- [SHOULD-FIX] B-2: The commit/flush conflation (`_ORM_COMMIT = {commit, flush}`) is listed in class 14's 'where' column but NOT in the wave B row. If the commits/flushes/savepoint split lands later and flush stops anchoring, the d2w-0 anchor set B just pinned moves again — a second re-pin of the same numbers B exists to avoid.
    FIX: Decide in B, in the commit message: EITHER keep `commits` semantics (flush still anchors) and add an additive `flushes`/`boundary` field (anchors unchanged, ~4 lines), OR move the anchor rule now so wave C/D never re-pin d2w. Add the flush-only count (56) to the B numbers either way.
    wave B · ev: templates/center/generators/_a3_code.py:875 `_ORM_COMMIT = frozenset({"commit", "flush"})` → `commits=True` at :951; templates/center/generators/_a3_graft.py:419-425 — `writers` (d2w 0) = any 'w' op OR `commits`; _a3_levels §3b (:283-306) draws from those anch
- [SHOULD-FIX] B-3: The DECISION block's fallback split order ('_a3_code first, _a3_graft second') is backwards: the precedence swap is byte-identical on every twin TODAY (a free honest-empty commit), while landing the root-walk first flips the map's only gate to accessor for one regen.
    FIX: Rewrite the BREAKS-IF fallback to: `_a3_graft` (precedence swap + the _a3_levels L362 widening + the new gate-with-ops fixture) FIRST — byte-identical on both twins; then `_a3_code` (root-walk/symtab/attr-writes/sink drop); then methods admission + role split (each a levels/c4 re-pin). One sentence in §0 and §3 item 2.
    wave B · ev: Replay baseline: gate_named_with_ops = 0 on gustify (466 FI entries); gastify FI: 43 methods, 0 with ops — the swap changes nothing until the root-walk lands; Replay after the root-walk under today's order: role flip ('gate','accessor'): 1 = assert_recipe_alle
- [SHOULD-FIX] B-4: The 'session off the http receivers, guard: require an import-bound http lib' cannot be implemented where the plan puts it: `_detect_sinks(fnnode)` sees only the function node; the module's imports live in `_file_imports(f)` keyed by FILE. The guard is a signature change that touches the function_insight call site and the orm-access battery helper.
    FIX: State the seam: `_detect_sinks(fnnode, imports: dict | None = None)`; http fires when recv ∈ {requests, httpx, aiohttp, client, session} AND (imports is None OR any of {httpx, requests, aiohttp, ClientSession, AsyncClient, Client} ∈ imports); `_add` passes `_file_imports(f)`. Battery: keep `httpx.post → http` with `{"httpx": False}` passed, add `session.delete(row)` + no http i
    wave B · ev: templates/center/generators/_a3_code.py:966 `def _detect_sinks(fnnode)`; :988 the `"session" in recv` http rule; templates/center/generators/_a3_code.py:828-857 `_file_imports(f)` — {local name → app-internal?}, backed by `_PY_TEXTS`, which function_insight fi

## re-pin plan — which batteries/proofs pin numbers waves B/C move, the regen-example --check
- [SHOULD-FIX] RP-1: The plan's §4 re-pin list names the wrong batteries: tests/{orm-access,arch-graph,levels,gabe-universe,codebase-graph} are fixture-based or static and pin NO gustify number wave B moves; the numeric pins that DO move live in the committed example estate (regen-example --check) and in the chrome proofs verify-*.mjs — four of which are already decayed.
    FIX: Rewrite §4's last bullet + the DECISION ASSUMED line: the re-pin unit per moving wave = (1) `bash universe-build/regen-example.sh` (lands the 8-file estate, ~30 s) and `--check` CLEAN as both the pre- and post-condition of the wave-B commit; (2) the chrome proofs listed above (schemafold · workflows · backend-journeys · clustering + the four decayed) re-run solo-sequential; the
    wave B · ev: tests/orm-access/run.sh: every check is a synthetic fn (lines 40-120, 122-175) — the only literals are fixture shapes; tests/arch-graph/run.sh:197-199 pins pairs=={('alpha','beta'):{calls:1,imports:1}}, :455-458 derive_fn_roles on a 4-node synthetic wiring, :1
- [SHOULD-FIX] RP-2: The four decayed proofs fail at HEAD on chrome/panel pins, not on data, so wave-B breakage inside them is invisible either way; the honest minimum before B is a 6-minute baseline run recorded in the commit, with the full re-pin deferred to wave D where the station changes anyway.
    FIX: Add a pre-A step: run `for v in panels walk explore routes; do node verify-$v.mjs; done` at wave-B's parent and paste the failing sub-check NAMES into the wave-B commit message (cost ≈ 6 min, zero edits); after wave B run again and diff the names — any NEW failing sub-check is wave-B's. Schedule the actual re-pin (≈10-20 assertions across the four files: explore kindTabs 3→5, p
    wave pre-A · ev: Ran all four at HEAD 6625ac6 (solo-sequential, system chrome): verify-panels FAIL after 1m55s — 'review-53 fixes broken (stepsKeep:false, walkExpand:no-stashed-step) · wire-kind rows wrong · entity-gradient toggle wrong'; verify-walk FAIL after 33 s — 'planets
- [SHOULD-FIX] RP-3: The plan's J3 and K1 AFTER chains claim the backend-tab walk shows the dispatch leg and the 'Hop 0.5' gate chain, but the station's backend-chain collector follows only rel==='calls' + 'handler' + access wires, and verify-workflows pins the handler at index 1 — the new rels never enter the walk, and inserting depends hops before the handler moves a pinned index.
    FIX: Wave D gets an explicit _bkCollect item: descend over rel ∈ {calls, dispatches} (tagged why:'dispatch'), and prepend the endpoint's gated_by/depends chain as hop 0 pre-steps (why:'gate'/'dep') so the handler stays the first fn hop; re-pin verify-workflows.mjs:54 to locate the handler by why==='fn' first index rather than ===1. Until D lands, state in J3/K1 that the new legs are
    wave D · ev: parts/layout.js:822-843 _bkCollect: `if(l.rel==="calls") … else if(l.rel==="handler") … else if(l.access)` — a `dispatches`/`depends`/`gated_by` rel is ignored; steps start `{id:ep.id,hop:0}` then handler at hop 1; parts/layout.js:257 _FNLINKS maps levels fn_e
- [SHOULD-FIX] RP-4: Every new wave-C rel needs a REL2KIND + RELCOL + LINKMETA + card relLabel row or it renders as a calls-kind wire with no colour; the plan's class 5 says 'serializes is the only new REL2KIND' — the real count is six (gated_by · depends · dispatches · serializes · reaches · walls) plus fnprompts.
    FIX: In wave D's station row list the per-rel quartet (REL2KIND kind · RELCOL colour · LINKMETA {w,pv} · card.js:67-70 relLabel/CONNICO) for all six rels, and add one static pin per rel beside run.sh:740. Correct the class-5 sentence.
    wave C · ev: universe-build/assemble.py:15 REL2KIND = {fk,pk,nests,handler,touch,touches,resp,uses,calls,consumes,fetches,bridge,renders,mounts,reads,imports,typed,fecall,bundle,reads_from,writes_to,fnreads,fnwrites} — no wave-C rel; parts/adapter.js:111-112 maps c4 kinds 
- [SHOULD-FIX] RP-5: Wave C's new L2 node kinds (middleware · provider · flag · prompt) are silently DROPPED by the Universe adapter and drawn grey with no legend row on the column station until wave D — 'additive, byte-identical' holds for the feeds but the journeys the class rows promise cannot render in C.
    FIX: Wave C's table row 'what it unblocks' should say 'feeds only — nodes dropped-with-warn on the Universe (adapter.js:66) and grey/unlegended on the column station until D'; wave D adds `_dropped===0` to the gabe-universe render proof (run.sh:1003-1010) once KINDS carries the four kinds.
    wave C · ev: parts/adapter.js:66 `if(!KINDS[kind]){ _dropped++; return; }` + :79 console.warn only; parts/layout.js:2121 _GRPKINDS backend=[endpoint,function,schema,model,external,entity] — a new kind belongs to neither master; templates/center/shell/codebase-graph.html:60
- [SHOULD-FIX] RP-6: Wave B's role flips (~30 pure/caller → accessor with READ ops) interact with the Universe's write-fabric solo exemption, which exempts ANY fn with access ops (reads included) from the critical fold — boot density changes and a decayed verify-panels sub-check flips; the plan re-pins numbers but says nothing about boot visibility.
    FIX: Wave B commit message adds the per-entity count of fns newly exempt from the critical fold; decide (one line, decision 12) whether the exemption stays any-op or narrows to write-ops (`a.ops.some(o=>o.rw==='w')`) — the narrower rule keeps boot density and is a one-token station edit + re-pin of run.sh:310.
    wave B · ev: parts/layout.js:2084 `n.__solo=(ks.length===1 && cs[ks[0]]===n.kind && n.d2w==null && !(n.access&&n.access.ops&&n.access.ops.length))` — reads count; tests/gabe-universe/run.sh:308-311 pins that exact exemption string
- [SHOULD-FIX] RP-7: Decision 8 ('require an import-bound http lib' for the http sink) as written fails an existing orm-access fixture whose fn has no import; the parenthetical 'keep name-based for bound clients' is not a rule, and _detect_sinks has no module context to check imports against.
    FIX: State the rule: named libs (requests/httpx/aiohttp) stay name-based; only the GENERIC receivers (`client`, `session`) require an http lib bound in the module's imports; extend `_detect_sinks(fn, imports=None)` (the `_file_imports` the plan already extends for Depends) and add the fixture pair — `session.delete(x)` with no http import → [] · `session.delete` under `from aiohttp 
    wave B · ev: tests/orm-access/run.sh:116 `sinks('def h():\n    httpx.post(url, json=d)', "h") == ["http"]` — bare fn, no import; templates/center/generators/_a3_code.py:988-990 the receiver test `"requests" in recv or "httpx" in recv or "aiohttp" in recv or "client" in rec
- [SHOULD-FIX] RP-8: Class 8's 'draw only gate=True deps' draws nothing on the second twin: gastify gates via add_middleware, its only Depends are get_db and a metrics key, and its rate-limit dep names are not gate tokens — the middleware-site floor is the whole K1 chain there and the class's evidence is gustify-only.
    FIX: Land `parse_app_middleware` with (not after) the Depends resolution in class 8, and add a gastify-shaped fixture to tests/orm-access (auth in add_middleware, a `limit` Depends) proving the depends arm is honest-empty and the middleware arm fires; decide whether `limit` joins _MW_GATE_TOKENS (gastify gate_endpoints 0→16 — state the number).
    wave C · ev: gastify fresh build (scratch, GABE_GRAFT_BUILD=0, 17 s): stats gate_endpoints 0 · middleware_endpoints 16 · middleware names {limit:14, shared_limit:6}; grep backend/app/api: `Depends(get_db` ×17 · `Depends(_verify_metrics_key` ×1 — no auth dep; `add_middlewar
- [SHOULD-FIX] RP-9: Class 1's `_BASE` Name-resolution 'defect fix (~6 lines)' is change-in-place, not additive: it renames four endpoint node ids in every twin's C4 and can move stats.web.matched — it belongs in wave B's single byte-moving commit or must be stated as wave A's byte move.
    FIX: Move the `_BASE`/module-constant resolution into wave B's substrate commit (or split it into its own tiny change commit before A) and state '4 endpoint ids + stats.web.matched' in the numbers; add a parse_endpoints fixture (`_BASE = "/x"; router.get(_BASE)`) to tests/orm-access.
    wave A · ev: committed example c4-graph.js: endpoints `DELETE /` (delete_recipe_filter_mode) · `GET /` (get_recipe_filter_modes) · `POST /` (create_recipe_filter_mode) · `PUT /` (update_recipe_filter_mode) — ids that change when the prefix resolves; stats.web.unmatched tod
- [SHOULD-FIX] RP-10: gastify's center stack is behind — no c4-graph.js/levels.json committed — so for gastify wave B is a FIRST landing, not a re-pin, and the plan's 'every twin regenerates once per wave' assumes a currency that does not hold.
    FIX: Add to the DECISION block's ASSUMED line: 'gastify has no committed c4/levels — propagate.sh + a first regen precede wave A there; its numbers are recorded as a baseline, not re-pinned'. Also note the fresh gastify levels carries one fn_node with role None — a wave-B fixture case (a drawn fn absent from fn_roles).
    wave pre-A · ev: ls /home/khujta/projects/apps/gastify/docs/site/center/: archmap.json + center.config.json present, c4-graph.js + levels.json ABSENT; graft/.graph/wiring.json present; fresh gastify build: 49 endpoints drawn of 80 backend routes (38 get · 23 post · 7 patch · 2

## twin side — propagation recipe, twin claims side-effects (S9 · L1 cross edges · co-claim t
- [BLOCKER] T1: "Every twin regenerates once per wave" / wave B "yes, every twin, one regen" cannot land on gastify through the propagation recipe: gastify vendors _a3_code.py and build_center_a3.py but NONE of the graph-arm modules, and propagate.sh is update-only, so the next propagate overwrites gastify's build_center_a3.py with the suite's (which imports _a3_graft/_a3_graph/_a3_levels/_a3_sim/_a3_web/_a3_fe unconditionally) and 
    FIX: Add a pre-A step to §0 and to the DECISION block's ASSUMED line: 'gastify adopts the graph arm first — copy the 7 NEW files + shell codebase-graph.html/gabe-universe.html/assets/sim-panel.js + nav wiring, regen, commit as its own adoption commit (numbers above)'; until then every 'every twin' claim in the wave table reads 'gustify (gastify after adoption)'. Also harden propagat
    wave pre-A · ev: templates/center/generators/propagate.sh:11-13 (UPDATE-ONLY by design; NEW modules reported, not copied) and :24-32 (copies any existing basename, build_center_a3.py included); templates/center/generators/build_center_a3.py:39-44 — six top-level `import _a3_*`
- [SHOULD-FIX] T2: The twin's claims commit is itself a numbers-moving regen on the L1 coupling picture — the plan's wave-A row ("moves existing bytes? suite: no") and decision 1 don't say so, and the reference/catalogs.py → recipe claim makes every consumer look coupled to recipe.
    FIX: In §0 wave A and §3 decision 1 add the measured L1 delta (+55 cross calls, 1 new pair) and state that the claims commit re-pins the L1 numbers on the twin; offer the honest alternative for reference/*: co-claim by consumer (colours by the tie-break in T3) or a dedicated `reference` aspect entity (costs an adoption row, T5) — the operator should choose with the coupling picture 
    wave A · ev: measured on gustify graft/.graph/wiring.json with derive_cross's own rule (_a3_graft.py:456-465, first-wins over sorted slugs): L1 cross-entity calls 422 → 477 (+55), pairs 34 → 35, dropped unmapped_file 5371 → 5191 under the 16-file claim list from findings-d
- [SHOULD-FIX] T3: "First-claim-wins" is two different rules today: function_insight homes a co-claimed file to the entity that comes first in CONFIG order, graft homes it to the ALPHABETICALLY first slug — so a co-claimed file's drawn fns (levels, FI-authoritative) and its L1 calls edges (derive_cross) already disagree, and the wave-A claims for catalogs.py (4 consumers) will inherit the split unless the plan names which rule wins.
    FIX: Pick ONE tie-break and say it in decision 1: either (a) forbid co-claims for the new files (single home each), or (b) make `_file2slug`/`derive_cross` iterate `entities` in archmap insertion order (= config order) — two one-line changes, change-in-place on the 8 collided files, so fold it into wave B's single regen. Either way the claim guidance must say which entity a co-claim
    wave A · ev: _a3_code.py:1114-1121 — `for slug in ENTITY_CODE: … file_entity.setdefault(f, slug)` (config insertion order; gustify's is legal-consent, progression, auth, cooking, pantry, recipe, settings, allergen); _a3_graft.py:153-163 `_file2slug` and :456-465 `derive_cr
- [SHOULD-FIX] T4: The rootless-writer widening (CLI · MIGRATE) misses the root class that carries gastify's two critical write paths: BACKGROUND TASKS. `background_tasks.add_task(process_scan, …)` passes the callable as an argument, so graft records no call and the workers have no production caller — the same 'callable as argument' shape class 9 handles with rule b, but on the write side.
    FIX: Class 7 gains a `TASK` root beside BOOT/CLI/MIGRATE: `BackgroundTasks.add_task(F, …)` / `asyncio.create_task(F(...))` inside a handler → a `dispatches` wire from the handler to F (conf extracted, the anatomy's dispatch-table shape), so the walk continues past the enqueue; honest-empty on gustify (0 sites), first write leg on gastify's scan-receipt and statement entities. State 
    wave C · ev: gastify backend/app/api/scans.py:158, :255 and statements.py:158, :243, :471 — `background_tasks.add_task(process_scan|process_statement, …)`; gustify apps/api has 0 BackgroundTasks/create_task sites, which is why a gustify-measured plan never met the class; g
- [SHOULD-FIX] T5: `code_layers += reference` is supported by the generators but a claim under an undeclared layer is a SILENT no-op: `code_map` only walks layers listed in `code_layers`, and no build-time line reports a `code` key that isn't declared.
    FIX: Wave A, ~8 lines: add `reference` to `_LAYER_CLS` + `layer_desc` + a3.css `.tag.l-reference` + the two config templates' `code_layers`; and emit one build line per `code` key absent from `code_layers` (report-never-gate) so the operator's claim cannot disappear. Name the locale route file correctly in the claim list: it is apps/api/api/locale_routes.py:20, not locale.py.
    wave A · ev: _a3_code.py:67-68 `_CODE_LAYERS = _cd.CFG.get('code_layers', [...])` (config-driven; both twins declare the 6-list explicitly) and :380 `for layer in _CODE_LAYERS:` — an entity `code.reference` list with no `code_layers` entry yields no `files` rows → not home
- [SHOULD-FIX] T6: The plan's wave-A scale is gustify's (8 routes · 52 files · 23 schemas); on gastify the same census fires on roughly half the surface, and the plan should carry the second twin's numbers so decision 1 is sized honestly.
    FIX: Add a gastify column to §0 wave A (routes 35/84 · models 14/28 · fetches 43/69) and to decision 1, and state that the census must run there BEFORE wave B is pinned, since wave B's re-pin baseline on gastify depends on which of those files get claimed.
    wave A · ev: gastify: 84 route decorators in 20 files (all under backend/app/api), config claims 8 api files → 49 endpoints drawn (35 missing, 42%); gastify scratch build: model_census claimed 14 / unclaimed 14 (50%), `stats.unclaimed=True`; web arm 69 fetch sites → 26 mat
- [SHOULD-FIX] T7: Item 3 (tiers) introduces a NEW generator module (`_a3_tiers.py`) which propagate.sh will report and not copy while still updating the build_center_a3.py/_a3_levels.py/_a3_graph.py that import it — the same break as T1 on EVERY twin, gustify included.
    FIX: In §0's 'then item 3 · tiers' row add the adoption step: 'new module → `cp` into <twin>/scripts BEFORE propagate's regen (or the T1 import-probe in propagate.sh)'; and since _a3_code.py is at 2,759 lines with ≥8 new passes queued (route/file census · derive_reach · dispatch_map/tables · boot roots · app middleware · flags · prompt registry · provider pass · orm widening), state
    wave tiers · ev: docs/design/codebase-graph-consolidation/DISCLOSURE-TIERS.md:54 ('A NEW `templates/center/generators/_a3_tiers.py`') and :81 (decision 11: new module recommended); templates/center/generators/propagate.sh:30-32 — NEW basenames go to `new_gen` and are printed, 

## test attachment + dossiers for the new node kinds (methods · BOOT/provider/flag/middleware
- [BLOCKER] F1: Wave C mints NEW L2 node kinds (class 8 `middleware`, class 9 `provider:<name>`, class 12 `flag:<NAME>`, class 13 `prompt`) but the emitter sorts and lays out L2 nodes through a FIXED kind tuple — any node whose kind is not in it raises ValueError and aborts `build_c4_graph`. The plan names no `_L2_KINDS` change in any row, and the finders' digest never mentions it (grep `_L2_KINDS` → 0 hits).
    FIX: Add to wave C's FIRST node-kind commit: append the new kinds to `_L2_KINDS` (after `web`) in the same commit that mints them (~1 line each), and either emit `layout.l2.order` as the kinds PRESENT in the graph or state the one-time order byte-change in the honest-empty pin for that commit (arch-graph battery: assert the build with no idiom differs from HEAD ONLY in `layout.l2.or
    wave C · ev: templates/center/generators/_a3_graph.py:71 `_L2_KINDS = ("endpoint", "model", "schema", "external", "web")`; templates/center/generators/_a3_graph.py:712 `nodes.sort(key=lambda n: (_L2_KINDS.index(n["kind"]), n["id"]))` — `.index()` on an absent kind raises
- [BLOCKER] F2: The COLUMN station (`templates/center/shell/codebase-graph.html`) positions L2 nodes by a hard-coded kind list; a node of an unknown kind gets `x = undefined * COLW = NaN`, which poisons the drill's bounding box and `frame()` scale — the entire L2 drill for any entity carrying one wave-C node renders blank. The plan's station work (row 2 `methodOf`, wave D 'station kinds + legend rows') names the universe's `KINDS` o
    FIX: Move the column-station kind registration (`ord`/`ord2`/`ord3`/`KIND_LABEL`/`METHOD` at :609-610, :897, :902, :1224) INTO wave C, one edit per new kind, with a tests/codebase-graph check that every kind present in the example `c4-graph.js` appears in `ord2` (a 3-line data-vs-list assert). Or take F1's alternative (separate top-level key until D) so the 2D station never sees the
    wave C · ev: templates/center/shell/codebase-graph.html:902-904 `ord2=["endpoint","schema","model","external","web"]` … `p[n.id]={x:kc[n.kind]*COLW, y:n.y}` — `kc[newKind]` is undefined → NaN; templates/center/shell/codebase-graph.html:1196-1203 `minX=Math.min(minX,p.x-64)
- [SHOULD-FIX] F3: Class 2 defers the WS test-attachment join rule ('needs a stated join rule'); it is cheap and measurable on BOTH twins now, and the second twin's idiom (Starlette `websocket_connect`) is a one-token change. The plan should state the three-part rule and its numbers rather than ship a WS door that claims 0 cases on gustify and 0 on gastify while 13 real cases exist.
    FIX: State in class 2 (and land in the same commit as the `WS` method): (1) `_a3_tests._facts` maps `websocket_connect` → method `WS` (1 line at :158); (2) one-level same-file helper expansion — for a located case, union the `_facts` of every same-file def it calls that is not itself a junit case (~8 lines around :343-348); (3) a case whose facts name a HANDLER by imported name cred
    wave C · ev: templates/center/generators/_a3_tests.py:151-170 `_facts`: only `f.attr in _VERBS` (get/post/put/patch/delete, :80) yields an http hit — `websocket_connect` never does; /home/khujta/projects/apps/gastify/backend/app/api/scan_stream.py:137 `@ws_router.websocket
- [SHOULD-FIX] F4: Wave B admits methods as DRAWN fn nodes with role badges, but the test↔function join can never credit a method: T1 requires the called name to be an IMPORTED name, and a method's bare name is never imported. The Gaps page already lists every method as 'no case calls it' — after B those same nodes are drawn with badges while the ledger contradicts tests that exist for exactly the methods classes 4/6/9 promote.
    FIX: Add to wave B (same `_a3_tests` commit, ~10 lines): a `fn_by_qual` index `{"Cls.m": key}` built beside `fn_by_name` (:270), and after the name loop: `for cls in used & pf.imported_names: for m in facts["calls"]: for k2 in fn_by_qual.get(f"{cls}.{m}", [])` → the same `_once(by_fn[k2]["direct"])` credit. Mark it inferred-class (two imported classes sharing a method name both cred
    wave B · ev: templates/center/generators/_a3_tests.py:270-272 `fn_by_name[v["name"]]` — keyed by the bare `name` (for methods FI stores `name`=`m`, `fn`=`Cls.m`, `_a3_code.py:1150-1158`); templates/center/generators/_a3_tests.py:357-358 `used = facts["calls"] | facts["name
- [SHOULD-FIX] F5: Wave C's node kinds are neither tested nor untested: their dossier can carry no case join (by design), the Gaps page never lists them, and the universe card paints every case-less node with a red 'unguarded · no test covers this' flag — so a middleware that has two dedicated test files renders as an untested red flag with no way to clear it.
    FIX: (a) In wave C's emitter commit: pass `ti.get("by_file")` as `ins["ti_file"]` (`_a3_graph.py:950-955`) and, for kinds with no case join, emit `det["reach_files"] = by_file[file]["reach"]` (T3, the tier the web corpus already gets) — `by_file` covers `fent` files only (`_a3_tests.py:250`), so it lights exactly when the twin CLAIMS the file, matching wave A's order. (b) In wave D'
    wave C · ev: templates/center/generators/_a3_graph.py:461-476 `element_detail` else-branch is model/schema-shaped: `cols = obj.get("fields")`, `rec = mi.get(obj.get("cls") or "")`, `groups = ti_model.get("")` → for a middleware/provider/flag/prompt obj det = {doc?, file?, 
- [SHOULD-FIX] F6: Row 1 adds an endpoint `trigger` field (user · poll · boot · e2e) 'from the anatomy' with no emitter seat, no derivation rule and no honest-empty statement; two of the four values are not derivable from anything the generators read, and the field's copy points/pins are not named anywhere in the plan (they are named for `transport` in the digest only).
    FIX: Either drop `trigger` from row 1, or define it as a TWO-value floor (`boot` | `e2e`; key absent otherwise) minted in `_l2` (:600) beside class 2's `transport`, and give both fields the same four copy points + three pins above (adapter bag `transport:p.transport`, gabe-universe string pin, arch-graph 'no key when absent'). Also state that the endpoint card never renders `access`
    wave C · ev: COVERAGE-FIX-MAP.md:55 row 1 verdict: '+ `trigger` field (user · poll · boot · e2e) from the anatomy' — no file cite; `poll` needs the FE polling idiom: `_a3_web.py` extracts `(method, path)` per fetch only; MEASURED gustify `apps/web/src`: 3 `refetchInterval`

## consistency of COVERAGE-FIX-MAP.md with DISCLOSURE-TIERS.md, SCHEMA-HOMING.md + landed hom
- [BLOCKER] C1: Class 5's `serializes` wire silently un-folds the landed nested-only schema fold: the fold predicate marks a schema WIRED on any non-function wire touching it in either direction, and `serializes` is schema→model (neither endpoint nor function), so every folded Block/Summary that gains a serializes wire stops folding — 13 of the 32 nested-only schemas on gustify (the plan's own J1 line names 9 of them: UserSummary · 
    FIX: Class 5 station row + wave C: add `if(l.rel==="serializes") return;` beside the fn-wire clause in `__uniComputeSolo` (layout.js:2096, 1 line; keep the pinned literal at tests/gabe-universe/run.sh:444 intact) and state the rule: a serializes wire is data provenance, not an exposure — it never blocks the fold. Add a static pin for the new line and re-run verify-schemafold.mjs wit
    wave C · ev: docs/design/codebase-graph-consolidation/universe-build/parts/layout.js:2095-2097 — `if(sn.kind==="function"||tn.kind==="function") return; if(sn.kind==="schema") _sW[sn.id]=1;` — a schema as SOURCE of any non-fn wire is marked wired; COVERAGE-FIX-MAP.md:95 `+
- [SHOULD-FIX] C2: The `item 3 · tiers` row says the ladder 'already lists depends 2 · dispatches 1 · serializes 3 · boot root' and implies the rest is a re-run; DISCLOSURE-TIERS has NO floor row, node rule or `why` code for most of what waves C–D add: wires `gated_by` · `reaches` · `walls` · `fnprompts` · `rendered_by` · fe `mirrors`; node kinds `middleware` · `provider` · `flag` · `prompt`. Its only external rule (`external → T3 fk-p
    FIX: Replace the item-3 row with the explicit S4 delta the tiers doc must absorb: floors gated_by 2 (+fan-in demotion like depends) · reaches 2 · walls 2 · fnprompts 3 · rendered_by 3 · mirrors per FE ladder; node rules middleware T2 [request:gate] · provider T2 [request:reached-provider] · flag T2 [request:wall] · prompt T3 [context:prompt]; `why` codes for each; roots += depends; 
    wave tiers · ev: DISCLOSURE-TIERS.md:38 floors = fk handler fnwrites writes_to calls dispatches fnreads reads_from bridge depends consumes resp serializes touches nests imports (no gated_by/reaches/walls/fnprompts); DISCLOSURE-TIERS.md:30 `external → T3 [context:fk-proxy]`; :4
- [SHOULD-FIX] C3: K1 is promised as a walkable 4-hop chain (`depends get_auth_context → build_auth_context → resolve_or_create_user → load_household_context`) but the tiers' reach walk goes from roots over calls/dispatches only, and levels roots are `_handlers` only; a depends target with no d2w (get_auth_context, load_household_context — reads only) is `context:unreached` T3, hidden at both boot levels, and the 72 depends wires into 
    FIX: Class 8 + wave D: make gate=True depends targets ROOTS (join `_handlers` exactly as class 7's boot rule 0 does) so the existing handler-rooted fn_edges rule + §3b apply unchanged and the tiers' `roots:{handlers,boot,dispatch}` gains `depends`; state in the plan that under the tiers the 72 depends wires are demoted to floor 3 by design and the journey pin is the exemption (one s
    wave C · ev: DISCLOSURE-TIERS.md:27 fn → T2 `if REACHED from a ROOT over calls/dispatches`; :40 depends fan-in ≥15 → floor 3; templates/center/generators/_a3_levels.py:233-238 `_handlers` = the ONLY roots; :263 `if c["s"] not in _handlers: continue`
- [SHOULD-FIX] C4: Wave D's consolidated §3c rule is 'reveal ONE hop from a drawn fn into a gate-role fn · reference fn · dispatch/depends target · rendered_by fn', but class 10 it replaces had 'outbound: gate → callees to FIXPOINT', class 6 needs dispatch targets to keep walking (`recompute_node_progress_for_cook → _upsert_node_progress`, 2 hops — §3b's write gradient saves the write leg only), and K1 needs 4 hops with a read-only tai
    FIX: Split D into two stated clauses: (a) NEW ROOTS — boot · dispatch targets · gate=True depends targets join `_handlers` (the handler-rooted walk + §3b run unchanged, fixpoint by construction); (b) ONE-HOP REVEAL — gate-role callee · reference-layer fn · rendered_by fn from any drawn fn. One battery case per clause; the tiers' reach BFS then reuses the same root set.
    wave D · ev: COVERAGE-FIX-MAP.md:36 wave D `reveal one hop from a drawn fn into…`; :64 class 10 `outbound: gate → callees to fixpoint`; :106 J3 `dispatches … → recompute_node_progress_for_cook [d2w 1] → _upsert_node_progress`; :128 K1 `→ load_household_context [fnreads …]`
- [SHOULD-FIX] C5: New L2 kinds (`middleware`, `provider`, `flag`, `prompt`) crash the build as written: `_stamp_l2` and two sorts call `_L2_KINDS.index(kind)` on every L2 node, and the tuple is append-only by contract (the `web` byte-identity note). The column station also enumerates kinds by hand in three `ord` arrays + KIND_COLOR/KIND_LABEL. The plan lists universe KINDS/legend rows per class but never `_L2_KINDS` or the column stat
    FIX: Wave C header: every new L2 kind is APPENDED to `_L2_KINDS` (x/y of existing nodes byte-identical, one arch-graph pin) and added to the column station's three `ord` arrays + KIND_COLOR/KIND_LABEL (or the arrays read `DATA.layout.l2.order`, which the emitter already writes at _a3_graph.py:1118). ~6 lines, stated once.
    wave C · ev: templates/center/generators/_a3_graph.py:71-73 `_L2_KINDS = ("endpoint","model","schema","external","web")` + the append-only note; :712 `nodes.sort(key=lambda n: (_L2_KINDS.index(n["kind"]), …))`; :800-806 `_stamp_l2` `col = _L2_KINDS.index(kind)`; :1099 same
- [SHOULD-FIX] C6: The plan's `dispatches`/`depends` fn_edges are invisible to the station's derived BACKEND journeys and the card's calls list, which walk `rel==="calls"` only; 'the walkability of everything C added' (wave D's stated payoff) needs that filter widened and verify-backend-journeys re-pinned. The levels comment also states fn_edges consumers assume fn→fn calls.
    FIX: Class 6/8 station rows: a `_WALK_REL={calls:1,dispatches:1,depends:1}` set used by `_bkCollect` + card.js:116 (2 lines) and a verify-backend-journeys case walking a dispatch hop; list `fn_edges` consumers (levels-page, archive-lab, sim-panel, cc-entity) in the wave-C re-pin note.
    wave C · ev: docs/design/codebase-graph-consolidation/universe-build/parts/layout.js:824 `_FNLINKS.forEach(function(l){ if(l.rel==="calls") …` (_bkCollect); parts/card.js:116 `if(l.rel!=="calls" …) return;`; templates/center/generators/_a3_levels.py:212-213 `never folded i
- [SHOULD-FIX] C7: Class 14 widens `commits` into commits/flushes/savepoint (closing BACKEND-JOURNEYS #1) but never says whether a flush-only fn stays a d2w ANCHOR. `derive_distance_to_write` anchors on `commits` OR an own w-op; 10 drawn fns today have commits=True and NO own write op (complete_setup · _discard_claim · cancel_session · advance_stage · relief_accept · unplan_recipe ×2 · assign_location · patch_household_settings · patch
    FIX: Class 14 + decision 3: state 'd2w anchors = own w-op ∪ commits ∪ flushes (the flush IS the unit-of-work write moment); `commits` alone feeds the boundary badge' — 1 line in `derive_distance_to_write` + an orm-access case; dry-run prints the 10 fns' d2w before/after.
    wave B · ev: templates/center/generators/_a3_graft.py:420-424 writers = `a.get("commits") or any(rw=="w")`; templates/center/generators/_a3_code.py:875 `_ORM_COMMIT = frozenset({"commit","flush"})`; :951
- [SHOULD-FIX] C8: Every honest-empty proof and every number in the map is gustify-only; gastify exists as a second twin (config 7 entities · archmap · graft index, no committed c4/levels) and carries the SECOND IDIOMS wave B/C must survive: the ORM session is `db` (13 `db.delete(` sites → the `session`-http fix is silent there and must not key on the receiver NAME); dependencies ride module-level Annotated ALIASES (`DB = Annotated[Asy
    FIX: §0: name gastify as wave B's second dry-run bed (`GABE_GRAFT_BUILD=0 GABE_REPO_ROOT=<gastify> GABE_CENTER_OUT=<scratch>`) with its numbers in the commit; class 8: resolve module-level `Name = Annotated[T, Depends(f)]` aliases (one dict pass over `_PY_TEXTS`, ~10 lines) or state gastify's gate as a known floor; class 14: receiver-agnostic ORM idioms (already true for `.add/.get`
    wave B · ev: /home/khujta/projects/apps/gastify/docs/site/center/center.config.json (7 entities) · archmap.json · graft/.graph/wiring.json present; c4-graph.js/levels.json absent; gastify backend/app (163 py): counts above; auth/deps.py:154 `Auth = Annotated[AuthContext, D
- [SHOULD-FIX] C9: Class 7's `boot` L1 bucket and class 12's `__config__` home introduce new pseudo-entities, but `__unclaimed__` is special-cased by literal name at 25 sites in 6 files (levels cross_edges filter, column-station colour/label, pulse S11 text, `_a3_graph`/`build_center_a3`); the plan lists a `_layout` branch for boot and nothing for `__config__`, so both buckets leak into levels cross_edges / pieces and the column ring u
    FIX: Class 7/12: define ONE pseudo-bucket predicate (`_is_bucket(slug) = slug.startswith("__")` in _a3_graph, exported) and route the 25 literal checks through it in the same commit — or drop `__config__` and home flags by their first reader only (the plan already allows it).
    wave C · ev: grep `__unclaimed__|_UNCLAIMED` → 25 sites: templates/center/generators/{_a3_graph,build_center_a3,_a3_code,_a3_levels}.py · templates/center/shell/codebase-graph.html:1148/1153/1424 · skills/gabe-pulse/scripts/angles.py:356; templates/center/generators/_a3_le