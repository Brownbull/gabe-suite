
######## api-routes  [high]  tag=config+additive  REFUTED=True
TODAY: Committed example c4-graph.js: 67 endpoint nodes across 8 L2 groups, no `__unclaimed__` group (minted_models 0, stats.unclaimed false), head 74d0c981. Source has 80 route decorators in 25 files (grep); config claims 18 files; the 7 unclaimed files carry 13 routes → 13 journeys the map cannot start (0 nodes, 0 wires, 0 screens bridged). Web arm: 54 fetch sites, 48 matched, 6 unmatched — 4 of the 6 name these routes (GET/PUT /equipment, GET /history/dishes, GET /notifications); the other 2 are `${ENDPOINT}/${id}` from useRecipeFilterModes. 12 of the 13 carry Depends(get_auth_context); /healthz is the one auth-free route. Related but separate defect visible in chain.py --all: 4 endpoints drawn 
 WHY: The endpoint parser is only ever run over the config's per-entity `code.api` file list; there is no route census beyond the allowlist, so a route file nobody claims is never parsed at all.  ev=templates/center/generators/_a3_code.py:102 ENTITY_CODE = {slug: e['code'] ...} (config-only); templates/center/generators/_a3_code.py:412-415 layers = ENTITY_CODE.get(slug); eps = parse_endpoints(repo, layers.get('api', []))  — the ONLY parse_endpoints call; templates/center/generators/build_center
 WHY: gustify claims 18 of 25 route files; the 7 unclaimed files hold exactly the 13 missing routes (80 decorators − 67 drawn = 13). main.py carries 0 route decorators — it is the MOUNT table, and it mounts all 7 (e2e_seed conditionally).  ev=gustify docs/site/center/center.config.json entities.*.code.api = 18 files (measured); apps/api/api/equipment.py:30,40 GET/PUT /equipment; apps/api/api/notifications.py:28,47,60,72 GET /notifications · POST /notifications/mark-read · POST /notifications/delete · GET /profile/projection
 WHY: Every downstream arm keys off `entities[*].endpoints`, so with no endpoint record nothing else can land either: no L2 node, no `behind` pill, no access rollup, no bridge target, no levels piece.  ev=templates/center/generators/_a3_graph.py:602-604 for ep in code.get('endpoints'): nid = f'endpoint:{method} {path}' (the only endpoint mint); templates/center/generators/_a3_graft.py:269-276 derive_behind iterates ent.get('endpoints'); :310-316 derive_endpoint_access likewise; templates/center/gener
 WHY: The handlers themselves are also invisible as functions: function_insight walks only files the config maps, and the graft fold drops any call whose endpoint file has no entity — even though graft's index DOES resolve all 13 handlers.  ev=templates/center/generators/_a3_code.py:978-991 file_layer built from ENTITY_CODE maps; texts = {f ... for f in sorted(file_layer)}; templates/center/generators/_a3_graft.py:152-162 _file2slug over entities' files; :172-182 derive_functions keeps a call only if s in fn_slug and t in fn_slug; gustify
 SEAM _a3_code: NEW `route_census(repo)` beside model_census (L521 pattern): dirs = parents of every configured code.api file (gustify: apps/api/api); for each *.py in those dirs NOT in the claimed set → parse_endpoints(repo,[rel]) (L143, unchanged — it already yields method/path/fn/file/resp/middleware incl. decorator-level `dependencies=` L943-944); return {scanned_dirs, claimed:18, unclaimed_files:[7], unclaimed:[13 × {method,path,fn,file,resp,middleware,reason:'file not in any entity's api list'}]}; {} when
 SEAM _a3_code: build_center_a3.py:1962 archmap gains `'route_census': _a3_code.route_census(REPO_ROOT)` next to model_census (L1961).
 SEAM _a3_graph: build_c4_graph, before the l2 loop at L990: if amap['route_census'].get('unclaimed') → entities = dict(entities) + {'__unclaimed__': {'endpoints': census.unclaimed, 'models': [], 'schemas': [], 'files': [['api', f, 0] per unclaimed file]}} — a pseudo-entity. _l2 L602-640 then mints the 13 endpoint nodes unchanged (fn/resp/middleware/behind/access keyed <file>#<fn>), stamped `unmapped:True` + `reason`; refs go to touches_x (documented=∅) so the existing global resolve draws cross `touches` wires 
 SEAM _a3_graft: derive_behind L269 / derive_endpoint_access L310 receive the SAME augmented entities dict at their call site (one-line passthrough) — every one of the 13 handler ids is in wiring, so `behind` pills land immediately and `access` rollups land wherever the call-tree reaches a CLAIMED service (equipment→services/recipes.py→UserEquipment; notifications→services/notifications.py→Notification/ProfileProjection/DishHistoryEvent/RecipeCreationRequest; history→services/dish_history.py→DishHistoryEvent; _e
 SEAM _a3_levels: No code change expected: ep_home L171-178 setdefaults to `__unclaimed__` (FI has no entry), pieces loop L407 iterates l2 groups so the bucket gets its endpoints; fn_nodes L229-233 stay handler-only from FI → the 13 handlers get NO function node until function_insight also walks census files (optional step B: _a3_code.py:978-991 add the census files to file_layer — CHANGE-IN-PLACE, shifts `api` usage counts L1065-1069 and adds the locale handler's own two selects as fnreads).
 SEAM center.config: The other cure: home the 7 files (equipment→recipe or settings; notifications/history→cooking; meal_plan→recipe; locale→settings; health/e2e_seed→auth or an `ops` entity) — the census then reports 0 and the routes draw as first-class. The census is what makes the drift VISIBLE either way.
 SEAM new module: gabe-pulse scripts/angles.py: S12 `route census` mirroring s11_model_census L349-380 (reads amap.route_census.unclaimed; standing reminder → /gabe-cc-init) + a cc-init adopt-spec §Route census paragraph beside §Model census (L273). Batteries: tests/pulse-angles 3 cases (fires/silent/predates), tests/arch-graph honest-empty pin (web=None, census absent → byte-identical) + mint pin, tests/orm-access parse pin; regen the example fixture (67→80 endpoint nodes, web matched 48→52 / unmatched 6→2) → te
 SEAM station(adapter/layout/card): No new node/wire kind. adapter.js L50/L63/L109 iterate Object.keys(_C4.l2) generically and L53 colours from _C4.colors — build_center_a3.py:1994 already sets colors['__unclaimed__']='#8a8f98'. Optional ~5 lines in card.js to print the endpoint's `reason` row the way minted models carry file+reason.
 SKETCH: 1 dirs = {parent(f) for f in every entity's code.api}; claimed = that set of files | 2 for py in sorted(dir.glob('*.py')) if py not in claimed: eps += parse_endpoints(repo,[py]) (+reason) | 3 archmap.route_census = {scanned_dirs, claimed, unclaimed_files, unclaimed}  ({} if no api configured) | 4 build_c4_graph: if unclaimed: entities['__unclaimed__'] = pseudo-entity{endpoints:unclaimed} | 5 _l2 mints the endpoint nodes (fn/resp/middleware); refs→touches_x→global resolve draws schema/model touches | 6 derive_behind/derive_endpoint_access over the same dict → behind pills + access rollups via claimed services | 7 _index_endpoints now hits GET/PUT /equipment, GET /history/dishes, GET /notifications → bridge edges | 8 pulse S12 reads archmap.route_census every beat; cc-init lens lists the 7 files to home
 HONEST-EMPTY: A project whose api dirs contain only claimed files (or with no code.api configured) → archmap.route_census = {scanned_dirs, claimed:N, unclaimed:[]} (or {} with nothing configured), NO pseudo-entity synthesised, NO `__unclaimed__` L2 group, no new stats key → c4-graph.json, levels.json and the stat
 SIZE: ~45 lines _a3_code (route_census) + 1 line build_center_a3 + ~25 lines _a3_graph (pseudo-entity + bucket-branch generalisation; 840→~865, over the 800 CODE budget, report-never-gate) + ~3 lines graft call-site passthrough + 0–5 _a3_levels + ~30 lines angles.py S12 + ~10 lines adopt-spec + optional ~5 card.js; batteries: pulse-angles +3 cases, arch-graph +2 pins, orm-access +1 pin, example fixture 
 COUNTS: {"route_decorators_in_source": 80, "route_files_in_source": 25, "config_claimed_api_files": 18, "unclaimed_api_files": 7, "unclaimed_routes": 13, "endpoint_nodes_drawn": 67, "endpoint_nodes_after": 80, "routes_per_unclaimed_file": {"api/notifications.py": 4, "api/equipment.py": 2, "api/meal_plan.py": 2, "api/e2e_seed.py": 2, "api/locale_routes.py": 1, "api/history.py": 1, "api/health.py": 1}, "main_py_route_decorators": 0, "main_py_include_router_lines": 25, "unclaimed_routes_with_get_auth_context": 12, "unclaimed_routes_without_auth": 1, "web_fetch_sites": 54, "web_matched_today": 48, "web_un
 RISK: Step A alone gives endpoint nodes + behind + access + bridge but NO handler function node (fn_nodes come from function_insight, _a3_code.py:978-991 walks mapped files only): the drawn chain starts at the endpoint and jumps to the first CLAIMED callee
 RISK: meal_plan's whole chain (services/meal_plan.py) and credits_summary (services/ai_credits.py) are themselves unclaimed files — this class only opens the door; those two journeys still dead-end without the unclaimed-files class fix (or homing).
 RISK: e2e_seed's router is mounted only when settings.seed_controls_enabled (main.py:174-177): the census will draw two routes that production never serves — carry a `mounted: conditional` flag by scanning main.py include_router names, or the operator acce
 RISK: Related parse_endpoints defect surfaced while measuring: recipe_filter_modes.py:39,52,75,101 pass `_BASE`/`_BASE + '/{mode_id}'` (ast.Name/BinOp) so 4 endpoints are drawn as GET/POST/PUT/DELETE '/' (L172-174 str-Constant guard → L184 `or '/'`); the t
 J J1 POST /setup/complete: BEFORE endpoint POST /setup/complete (auth · behind 29 fns/depth 5 · screen useCompleteSetup) → handler setup_complete → calls _me_response_from_result → calls complete_setup ← reads SetupCompletionState (commits) → calls _discard_claim ← reads IdempotencyKey → calls ||| AFTER (all 15 BEFORE hops unchanged) → + endpoint POST /_e2e/seed [__unclaimed__ bucket · e2e_seed.py:63 · middleware get_auth_context + _require_seed_controls via dependencies=] (endpoint, minted from route_census) → + handler e2e_seed (handler; node only in step B, today the wire lands on the endpoint node) → + calls ensure_consent (services/consent.py:40, legal-consent) (calls) → + calls record_consent → fnwrites Consen ||| Δ The operator sees a second, grey-bucketed endpoint (POST /_e2e/seed) fan into complete_setup beside the real one, with its own consent leg — the programmatic twin of the flagship journey, gated by _re
 J K1 auth/identity provisioning on every request: BEFORE endpoint × 67 · gates (names only): get_auth_context [param-dep, gate=True], get_session — chain.py prints the NAME, never a hop to resolve_or_create_user ||| AFTER (the 67 gate rows unchanged) → + endpoint × 12 (GET/PUT /equipment, GET/POST×2 /notifications*, GET /profile/projection, POST /meal-plan/generate, GET /meal-plan/current, POST /_e2e/seed, POST /_e2e/seed-catalogs, GET /locale, GET /history/dishes) each carrying middleware get_auth_context (endpoint · middleware floor, parse_endpoints L189 → enode.middleware L611) → + endpoint GET /healthz with NO gate at all (health. ||| Δ The gate census becomes 79 gated + 1 ungated instead of 67 gated; the 'every request' claim is corrected to 'every request except /healthz'. The provisioning commit itself is still a name, not a hop.
 J K4 the seed/ops lane (seed_catalogs_on_startup): BEFORE (nothing — no request root; services/catalog_seed.py:352 seed_catalogs_on_startup is a lifespan call from main.py:82, not a handler; the 10 catalog models are reached only as reads via GET /taxonomy) ||| AFTER + endpoint POST /_e2e/seed-catalogs [__unclaimed__ bucket · e2e_seed.py:115 · middleware get_auth_context + _require_seed_controls] (endpoint) → + handler e2e_seed_catalogs (handler) → + calls seed_all_catalogs (services/catalog_seed.py:329, claimed by recipe) (calls) → + calls seed_taxonomy_dimensions, seed_taxonomy_values, seed_measurement_units, seed_ingredient_categories, seed_canonical_ingredients, seed_ingredie ||| Δ The seed lane gains a request root: the operator can start at POST /_e2e/seed-catalogs and walk into the same seed_all_catalogs the production boot runs, seeing the 10 catalog tables written; the boot
 UNCHANGED: W1 Initial setup — POST /con, W2 Look for recipes — GET /r, W3 Filter recipes — GET /tax, W4 Cook — POST /cooking/sess, W5 Store ingredients — POST , W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /recipes/{rec, J2 DELETE /me, J3 POST /cooking/sessions/{i, J4 POST /cookin
 --- REFUTER: wrong checks:
   ✗ main.py has 25 include_router lines ⇒ grep -c include_router apps/api/main.py → 26 (health + 24 api_v1 sub-routers incl. e2e_seed + app.include_router(api_v1))
   ✗ K1 BEFORE: all 67 drawn endpoints carry get_auth_context ⇒ committed c4-graph.js: 66 endpoints carry get_auth_context; GET /recipe-creation/gustify/stream carries get_auth_context_from_query (gate=True) — stats.gate_endpoints 67 is correct via the gate flag, the NAME is not uniform
   ✗ J1 BEFORE sequence quotes chain.py correctly (13 fns, touches SetupCompleteRequest+MeResponse, rollup 18 wires: writes_to ×9, reads_from ×9) ⇒ chain.py 'POST /setup/complete' → 'fns drawn on chain: 13', schema wires [(touches, SetupCompleteRequest),(touches, MeResponse)], rollup 18 entries = writes_to ×8 (SetupCompletionState, SubscriptionEntitlement, HouseholdFormatPreferences, UserDietaryProfile, UserExplorationPreferences, UserFormatPre
   ✗ J1/K4 AFTER: under the tagged fix (step A, route_census + pseudo-entity) the seed endpoints gain '+ calls ensure_consent / complete_setup / credits_summary / seed_all_catalogs / seed_* (calls)' hops;  ⇒ levels fn_edges are rooted ONLY at function_insight handlers: _a3_levels.py:233-238 `_handlers` built from FI entries with handler=True, :261-263 `if c['s'] not in _handlers: continue`, §3b BFS at :286 seeds from drawn_fn (no census handler there either); FI's handler flag comes from `handlers` buil
   ✗ J1 AFTER touches: 'MeResponse (cross → auth), SetupCompleteRequest' ⇒ MeResponse is defined in apps/api/schemas/responses.py:157, a file claimed by PROGRESSION (config progression.code.schemas = [schemas/responses.py]); committed c4 homes schema MeResponse under progression. SetupCompleteRequest is referenced only at module level (e2e_seed.py:45 `_DEFAULT_SEED_PAYLOAD
   ✗ K4 AFTER: seed lane writes 10 catalog models (hedged 'iff the _upsert_by_code idiom is visible') ⇒ Measured from gustify docs/site/center/archmap.json function_insight (catalog_seed.py is claimed by recipe): access ops exist only for _upsert_taxonomy_value (TaxonomyValue), seed_ingredient_restrictions (IngredientRestriction), seed_safety_warnings (IngredientWarning, ContextualWarningRule); _upser
   ✗ K4 BEFORE: the 10 catalog models are reached only as reads via GET /taxonomy ⇒ chain.py 'GET /taxonomy' → reads ['TaxonomyDimension','TaxonomyValue'] only; the other 8 catalog models are drawn as model nodes (recipe/allergen) but GET /taxonomy does not reach them; also seed_ingredient_restrictions / seed_safety_warnings ARE drawn fn_nodes today (levels.json fn_nodes: role acce
   ✗ Seam: inject the pseudo-entity 'before the l2 loop at L990' and _index_endpoints then bridges the 4 fetches ⇒ _a3_graph.py:902 `ep_index = _index_endpoints(entities)` runs inside the web-bridge block at :897-935, BEFORE the l2 loop at :991-995; an augmentation at L990 leaves ep_index without the census endpoints → 0 new bridges. To bridge, the augmentation must precede :902 — but then _l1 at :849 (`for slug
   ✗ _a3_graph.py is 840 lines now; +~25 → ~865 ⇒ wc -l templates/center/generators/_a3_graph.py → 1169 (989 non-blank/non-comment); size-budget.sh:55 counts with `wc -l`; 840 is the stale CLAUDE.md figure
   ✗ New pulse angle would be S12 'route census' mirroring s11_model_census L349-380 ⇒ skills/gabe-pulse/scripts/angles.py:349 s11_model_census, :382 s12_schema_homing already exists → the route census would be S13
   CORR: J1/K4 AFTER sequences (the '+ calls …' hops) → under the tagged step-A fix NOTHING draws as a calls chain: _a3_levels.py:233-238 builds `_handlers` from function_insight handler=True only, :263 drops every call whose source is not a handler, §3b (:286) seeds from drawn_fn; chain.py:48-49 would print 'handler: NOT DRAWN' for all 13. Step A yields: endpoint node + gates + behind pill + access ROLLUP
   CORR: J1 AFTER 'touches MeResponse (cross → auth), SetupCompleteRequest' → MeResponse lives in apps/api/schemas/responses.py:157, claimed by PROGRESSION (committed c4 homes it there); SetupCompleteRequest is used only at module level (e2e_seed.py:45), never in the handler body (parse_endpoints refs walk the handler node, _a3_code.py:178) → no touches wire; the handler names MeResponse + 11 *Summary/*Blo
   CORR: K4 AFTER 'fnwrites 10 catalog models (iff _upsert_by_code visible)' → measured NOT visible today: archmap.json function_insight has ops for only _upsert_taxonomy_value (TaxonomyValue), seed_ingredient_restrictions (IngredientRestriction), seed_safety_warnings (IngredientWarning, ContextualWarningRule); the 6 `_upsert_by_code(session, Model, …)` callers (catalog_seed.py:69,166,186,203,254,272) carr
   CORR: K4 BEFORE 'the 10 catalog models are reached only as reads via GET /taxonomy' → chain.py 'GET /taxonomy' reads TaxonomyDimension + TaxonomyValue only (2 of 10); seed_ingredient_restrictions and seed_safety_warnings are already drawn fn_nodes (levels.json, role accessor, w ops) with no request root.
   CORR: J1 BEFORE 'rollup 18 wires (writes_to ×9, reads_from ×9)' → chain.py prints writes_to ×8 + reads_from ×10 (IdempotencyKey and ConsentRecord are read-only on the walk).
   CORR: K1 BEFORE 'endpoint × 67 · gates: get_auth_context' → 66 carry get_auth_context; GET /recipe-creation/gustify/stream carries get_auth_context_from_query (committed c4-graph.js); '67 gated' is right only via the gate flag.
   CORR: Seam 'inject the pseudo-entity before the l2 loop at L990 … _index_endpoints bridges the 4 fetches' → ep_index is built at _a3_graph.py:902, BEFORE the l2 loop (:991); injection at :990 gives 0 new bridges. Injecting before :902 makes _l1 (:849) emit an L1 'entity' node for __unclaimed__ and moves stats.entities (:1108) — the sketch needs a _l1 filter (or a second _index_endpoints call) that the f
   CORR: '_a3_graph.py 840 lines → ~865' → wc -l = 1169 today (989 non-blank/non-comment); size-budget.sh:55 uses wc -l; the +~25 lands at ~1194.
   CORR: 'gabe-pulse S12 route census' → S12 is already s12_schema_homing (angles.py:382); the new angle is S13.
   CORR: 'main_py_include_router_lines: 25' → 26 (grep -c include_router apps/api/main.py).
   CORR: graft handler ids named in evidence ('notifications.py#get_notifications, meal_plan.py#post_generate_plan …') are fine, but the finder's implied names mark_read/get_current_plan do not exist in wiring — real ids are notifications.py#post_mark_read/#post_delete/#get_profile_projection and meal_plan.py#get_current.
   CORR: J1 AFTER omits two hops the seed handler takes: e2e_seed → middleware/idempotency.py#get_idempotency_key (e2e_seed.py:74; file unclaimed → honest-empty) and → config.py#get_settings (wiring calls) — same honest-empty class as credits_summary.
   CORR: GET /equipment rollup under step A is EMPTY (services/recipes.py::get_user_equipment_codes access {} in archmap FI); only PUT /equipment draws writes_to/reads_from UserEquipment — the finding says 'equipment→services/recipes.py→UserEquipment' for both.
   MISSING: The levels root gate itself: the finding never cites _a3_levels.py:233-238/:263 (`_handlers` from FI, `if c['s'] not in _handlers: continue`) — the one line that decides whether a census endpoint gets ANY drawn chain; it is the reason step A and step B differ by the whole journey, not by one node.
   MISSING: The step-B contract is under-specified: function_insight's handler flag (_a3_code.py:1159) needs the census (fn,file) tuples added to `handlers` (:987-988), and `det_of`/with_journeys for the endpoint node reads FI too — both stay honest-empty until then; the finding should state the AFTER twice (st
   MISSING: GET /equipment's rollup is empty (get_user_equipment_codes access {}) — a step-A endpoint node with a bridge but no data wire; worth naming as the one bridged route that still dead-ends.
   MISSING: The seam ordering in build_c4_graph (entities read :847 → _l1 :849 → ep_index :902 → l2 loop :991) was not walked; the pseudo-entity's insertion point determines whether bridges land and whether stats.entities moves.
   MISSING: The conditional-mount question is raised as a risk but not measured: parse_endpoints has no notion of mounting at all (it reads decorators, not main.py), so today's 67 also include nothing about mount state — a `mounted` flag would be a NEW field on every endpoint, i.e. change-in-place, not confined
   MISSING: The example-fixture regen blast radius is named but not measured: tests/gabe-universe, tests/codebase-graph, tests/sim and the docs quoting '67 endpoints' / '48/6' — a grep count of those literals would size the re-pin.

######## ws-sse-routes  [high]  tag=additive+change-in-place  REFUTED=True
TODAY: GET /recipe-creation/gustify/stream IS drawn — as a plain GET endpoint in the recipe entity (c4-graph.js: fn stream_gustify · behind 48 fns/depth 6 · middleware 3 with get_auth_context_from_query as the gate · access r RecipeCreationRequest, r UserDietaryProfile, w IngredientReconciliationQueue, commits true · 11 cases C1106-C1116 · resp absent; det.sig.returns=StreamingResponse is the only trace of streaming, buried in the dossier), chain of 6 drawn fns (stream_gustify → _sse_frames, stream_gustify_events → _finalize_stream, reclaim_if_abandoned → enqueue_unknown_ingredients), 'screens → none bridged' (sse.ts / useRecipeStream.ts have NO web screen node — 0 apiFetch sites — only fe pieces: 
 WHY: The WebSocket route is dropped at the decorator allow-list: parse_endpoints skips any decorator whose attr is not one of the five HTTP verbs, so `@router.websocket(...)` never becomes an endpoint dict.  ev=/home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:168-170 — `method = dec.func.attr` / `if method not in ("get", "post", "put", "patch", "delete"): continue`; /home/khujta/projects/apps/gustify/apps/api/api/recipe_stream.py:161-162 — `@router.websocket("/recipe-creation/gustify
 WHY: Everything downstream keys off parse_endpoints' output, so the WS handler is never a handler, never a root, never a behind/access key, never an endpoint node: the handlers set is built from the parsed endpoints; function_insight flags handler from that set; levels roots fn_edges only at handlers; graft behind/access iterate ent['endpoints']; _l2 emits endpoint nodes from code['endpoints'].  ev=/home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:415 — `eps = parse_endpoints(repo, layers.get("api", []))`; /home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:986-987 — `for e in v["endpoints"]: handlers.add((e["fn"], e["file"]))`; /home/khujta/projects/g
 WHY: The SSE route is captured because it is a plain `@router.get`, but its streaming nature is invisible: no response_model → resp '—' → no `resp` on the node; the `StreamingResponse` return only reaches det.sig.returns via function_insight; no node flag, no method/badge distinction — the station knows only five verbs.  ev=/home/khujta/projects/apps/gustify/apps/api/api/recipe_stream.py:114 — `@router.get("/recipe-creation/gustify/stream")` (no response_model); :123 `-> StreamingResponse`; :154-158 `StreamingResponse(_sse_frames(event_stream), media_type="text/event-stream", headers=_SSE_HEADERS)`; /home/khujta/projec
 WHY: The web side has no entry because the bridge extractor matches only apiFetch/axios/fetch/openapiFetch literal first-args (and their bare-identifier dynamic forms); `new EventSource(href)` with a URL built from a same-file const matches neither, so sse.ts yields 0 calls and 0 dynamic and is skipped as a screen — hence 'screens → none bridged' on the SSE endpoint.  ev=/home/khujta/projects/gabe_lens/templates/center/generators/_a3_web.py:64-77 — `_CALL_RES` = apiFetch | axios | fetch | openapiFetch; :80-83 `_CALL_DYN` same idioms; /home/khujta/projects/gabe_lens/templates/center/generators/_a3_web.py:206-208 — `calls, dyn = _extract_file(src, idiom)` / `if not ca
 WHY: Even the drawn SSE tail is shallow for reasons outside this class: stream_gustify_events' real hop list (idempotency read, snapshot read, credit/spend gates, 10 list-dispatched stages, persist + spend record + commit after the 200 line) is mostly undrawn because fn_edges are handler-rooted plus the §3b write-path gradient.  ev=/home/khujta/projects/apps/gustify/apps/api/services/ai_recipe_stream.py:151-153 (get_creation_request_by_key, load_resolution_snapshot, load_user_allergens), :168-169 (check_spend_cap, check_credits), :176-177 (build_cooked_ledger, get_pantry_ingredient_codes), :191-196 (StreamPipeline(...).events(
 SEAM _a3_code: parse_endpoints L169: widen `("get","post","put","patch","delete")` to admit `websocket` and emit `method: "WS"` for it; add a ~10-line `_transport_of(node, dec)` — `websocket` decorator → "ws"; a handler whose body names `StreamingResponse`/`EventSourceResponse` (ast.Name) or carries the Constant "text/event-stream" (recipe_stream.py:154-156) → "sse"; set `ep["transport"]` only when found. Nothing else in this file moves: the handlers set (L987 `handlers.add((e["fn"], e["file"]))` from `v["endp
 SEAM _a3_levels: No code change. `_method_of` L57 (`tok.isupper() and tok.isalpha()`) already accepts "WS"; the handler-rooting rule L230-233 + L257 (`if c["s"] not in _handlers: continue`) draws ws_gustify's 5 app `calls` edges once it is a handler; §3b d2w BFS (L263+) unchanged. Re-pin tests/levels (25 endpoint-mentioning checks) only if they count the example's endpoints/fn_nodes.
 SEAM _a3_graft: No code change. derive_behind L270-276 and derive_endpoint_access L311-316 iterate `ent["endpoints"]` keyed `<file>#<fn>` — `apps/api/api/recipe_stream.py#ws_gustify` exists in wiring.json (11 edges) so behind/access attach automatically. Optional ruling (separate): add "tests" to `_NOISE_PARTS` L49 — the 3 `apps/api/tests/test_streaming.py#_FakeWebSocket.*` inferred targets would otherwise ride the WS handler's behind count.
 SEAM _a3_graph: _l2 L602-610: copy `ep["transport"]` onto `enode["transport"]` (and into det) when present; L1110 stats block: `stream_endpoints` count emitted only when > 0. The bridge match at L903-909 needs nothing — the SSE screen's call is (GET, /api/v1/recipe-creation/gustify/stream), `_norm_path` L208 strips `/api/v1`, and the endpoint id `endpoint:GET /recipe-creation/gustify/stream` (L229) already exists; an unhomed screen is endpoint-homed to recipe by the L909-917 logic.
 SEAM new module: (existing `_a3_web.py`, not new) add an `eventsource` entry to `_CALL_RES` L64-77: `new EventSource(<literal>)` → (GET, path, transport:'sse'); plus a ~8-line same-file const resolution so sse.ts:163 `${API_BASE_URL}${STREAM_PATH}` resolves `STREAM_PATH` from sse.ts:29 — without it the site is a `dynamic` (named, unmatched) hit, honest but bridge-less. The `_DEF_RE` L87 guard is unaffected (sse.ts does not define apiFetch). Mirror the same matcher in `skills/gabe-pulse/scripts/fetch_bridge.py` L
 SEAM station(adapter/layout/card): adapter.js:8 `METHOD` += `WS` colour; :46 `_methOf` regex `^(GET|POST|PUT|PATCH|DELETE|WS)\b`; :71-75 carry `transport:p.transport` in the node bag; layout.js `__badgeGlyph('method','WS')` glyph (the method-badge source assemble.py:428-444 reads it); card.js:163-165 Signature row adds a chip `SSE stream` / `WebSocket` from `n.transport`, KINDTIP L289 endpoint tip mentions streaming routes; assemble.py:650-651 legend badge-key info lists WS. Re-pin tests/gabe-universe (18) + tests/codebase-graph 
 SEAM center.config: Optional, gustify only: add `apps/web/src/lib/api/sse.ts` to `entities.recipe.code.web` so the new screen is FILE-homed (today the config claims useRecipeStream.ts + GustifyGenerate*.tsx under recipe but not sse.ts); without it `_file2slug` returns None and the screen is endpoint-homed to recipe anyway — same drawing, different provenance.
 SKETCH: 1. parse_endpoints admits `websocket` → method WS; tag transport=ws; a get/post handler naming StreamingResponse/EventSourceResponse/"text/event-stream" → transport=sse. | 2. handlers set picks ws_gustify up → function_insight.handler=True → levels roots its graft calls → 5 new calls edges + behind/access via the existing <file>#<fn> key. | 3. web arm gains an EventSource matcher (+ same-file const resolution) → sse.ts becomes a screen with (GET, /api/v1/recipe-creation/gustify/stream) → existing bridge match lands on the existing SSE endpoint. | 4. _a3_graph carries `transport` onto the endpoint node; stats.stream_endpoints only when >0. | 5. station: METHOD['WS'] colour+glyph, transport chip on the card, legend key line. | 6. fetch_bridge.py mirrors the matcher so S10/review see EventSource sites. | 7. arch-graph pins honest-empty byte-identity; example regen moves 67→68 endpoints.
 HONEST-EMPTY: A project with no `@router.websocket`, no handler returning StreamingResponse/EventSourceResponse or naming "text/event-stream", and no `new EventSource(` in its web tree: parse_endpoints yields the same dicts (no `transport` key, no WS method — the widened tuple at _a3_code.py:169 matches nothing n
 SIZE: ~65 code lines (_a3_code ~15 · _a3_graph ~6 · _a3_web ~20 · fetch_bridge.py ~8 · adapter/layout/card ~15) + ~8 battery checks (arch-graph +3: websocket parsed as WS endpoint · sse transport flag · honest-empty byte-identical; pulse-angles +1 EventSource site; codebase-graph/gabe-universe +2 METHOD['WS'] + chip; levels re-pin if example counts are pinned) + 1 frozen-example regen (c4-graph.js/level
 NEW_KINDS: endpoint method token `WS` — a new METHOD badge colour + glyph (adapter.js:8 METHOD map, :46 _methOf regex, layout.js __badgeGlyph); NOT a new node kind, the node stays kind=endpoint || endpoint attribute `transport` ∈ {sse, ws} — a node flag rendered as a Signature-row chip on the card (card.js:163-165) and a legend badge-key line (assemble.py:650); no new wire kind — the new hops r
 COUNTS: {"source_streaming_routes": 2, "source_sse_routes": 1, "source_ws_routes": 1, "source_web_eventsource_openers": 1, "row_claimed_in_source": 4, "row_count_reconciliation": "4 = grep SITES (StreamingResponse( \u00b7 -> StreamingResponse \u00b7 @router.websocket( \u00b7 new EventSource() \u2014 2 backend routes + 1 browser opener", "source_route_decorators_api": {"get": 29, "post": 29, "patch": 10, "delete": 8, "put": 4, "websocket": 1}, "apiFetch_sites_in_sse_ts_and_useRecipeStream_ts": 0, "committed_endpoints_drawn": 67, "stream_endpoint_drawn": 1, "ws_endpoint_drawn": 0, "c4_mentions_stream_su
 RISK: The row's numbers do not reproduce: 2 streaming routes in source (apps/api/api/recipe_stream.py:114 SSE GET · :161 WebSocket) + 1 browser opener (apps/web/src/lib/api/sse.ts:173), not 4; and the SSE route IS drawn today as a plain GET endpoint (behin
 RISK: Both routes are 403-walled in production (apps/api/constants.py:68 RECIPE_CREATION_ENABLED=False; recipe_stream.py:135 and :195 reject before streaming) — the drawn journey is a dormant path; MED value holds only for the E2E-lane override (settings.r
 RISK: Change-in-place blast: widening _a3_code.py:169 moves existing outputs on any twin with a websocket route — endpoint count (gustify 67→68), levels pieces, behind/access keys, by_function test joins, the frozen example (c4-graph.js/levels.json) — ever
 RISK: The EventSource matcher needs same-file const resolution (sse.ts:29 STREAM_PATH used in a template literal at :163); a literal-only matcher lands the site as `dynamic` (named, unmatched) — honest but no bridge. Keep it a same-file `const X = "…"` loo
 J K3 POST /recipe-creation/gustify + its SSE/WS tail: BEFORE endpoint POST /recipe-creation/gustify (resp GustifyCreationResponse · behind 47/depth 5 · gates get_auth_context/get_session/get_settings · screens none bridged) → handler post_create_gustify [caller · d2w=0] → calls _gustify_response [pure · extracted] → cal ||| AFTER endpoint POST /recipe-creation/gustify … (all 9 POST hops unchanged) → + screen web:apps/web/src/lib/api/sse  [bridge → GET /recipe-creation/gustify/stream]  (sse.ts:29 STREAM_PATH · :163 url · :173 new EventSource(href); opened from useRecipeStream.ts:132 ← GustifyGenerateSheet.tsx:42) → endpoint GET /recipe-creation/gustify/stream  + transport:sse (node flag, no wire — recipe_stream.py:123 `-> StreamingResponse`, : ||| Δ The operator would see the AI-creation stream get its ENTRY from the web (a dashed bridge from a new `sse` screen into the SSE endpoint, which today sits unbridged), a second door `WS /recipe-creation
 UNCHANGED: W1 Initial setup [POST /cons, W2 Look for recipes [GET /re, W3 Filter recipes [GET /taxo, W4 Cook [POST /cooking/sessi, W5 Store ingredients [POST /, W6 Pantry locations [POST /p, W7 Shopping [POST /shopping/, W8 Plan [POST /recipes/{reci, J1 POST /setup/complete, J2 DELETE /me, J3 POST /cooking/ses
 --- REFUTER: wrong checks:
   ✗ The SSE node carries 11 cases C1106-C1116 ⇒ c4-graph.js det.cases on the stream node = C1106, C1107, C1108, C1109, C1110, C1111, C1112, C1113, C1114, C1115, C1123 (11); C1116 test_stream_disconnect_persists_nothing (test_streaming.py:369) is NOT attached; C1123 (the SSE+WS parity test, :501) IS attached via its route literal — twin archmap te
   ✗ AFTER: '+ 7 api cases C1117-C1123 attach (by_function via `_run_ws`→ws_gustify in wiring.json)' ⇒ test_insight never reads graft (_a3_tests.py:478 'Derived from junit + archmap ONLY (no graft)'); direct credit is per-def: `used = facts["calls"] | facts["names"]` ∩ `pf.imported_names` → fn_by_name (_a3_tests.py:345-366, _PyFile per-def facts :147-190). The WS test bodies call `_run_ws(fake, …)` (
   ✗ Seam: add an `eventsource` entry to `_a3_web._CALL_RES` (L64-77) + same-file const resolution → sse.ts becomes a screen with (GET, /api/v1/recipe-creation/gustify/stream) → existing bridge match lands ⇒ _a3_web.py is a SINGLE-idiom extractor: `_detect_idiom` (:137-142) picks the ONE dominant key by hit-count (`best = max(sorted(counts), …)`), and `_extract_file` (:145-148) runs `rx = _CALL_RES[idiom]` only. gustify's idiom = apiFetch (stats.web.extractor 'apiFetch', 54 sites) vs 1 EventSource site 
   ✗ _a3_code downstream: handlers set at L986-987, handler flag at L1025, _endpoint_middleware at L960 with the decorator tuple at L963 ⇒ Substance holds, lines do not: `handlers.add((e["fn"], e["file"]))` is at _a3_code.py:1121; `"handler": (node.name, f) in handlers` at :1159; `def _endpoint_middleware` at :1068; the non-route decorator tuple `nm not in ("get", "post", …)` at :1100 (and the `dec is route_dec: continue` guard at :109
   CORR: '+ 7 api cases C1117-C1123 attach (by_function via _run_ws→ws_gustify in wiring.json)' → 0 attach. test_insight is graft-free (_a3_tests.py:478) and credits a fn only when the CASE's own def body names it and the file imports it (_a3_tests.py:345-366; per-def facts _a3_tests.py:147-190). The WS tests call `_run_ws(...)` (test_streaming.py:418/431/445/458/474/491/514); `ws_gustify` is named only at
   CORR: '11 cases C1106-C1116' on the SSE node → C1106-C1115 + C1123 (c4-graph.js det.cases; twin by_function via_route). C1116 (test_stream_disconnect_persists_nothing, test_streaming.py:369) is not attached.
   CORR: _a3_web seam 'add an eventsource entry to _CALL_RES L64-77' → dead as written: `_detect_idiom` (_a3_web.py:137-142) selects ONE dominant idiom and `_extract_file` (:145-148) runs `_CALL_RES[idiom]` only; gustify = apiFetch (54 hits) so a 1-hit eventsource key never runs and sse.ts is still skipped at :206-208. The bridge needs a separate always-on transport pass in `_extract_file`/`web_arm` (plus 
   CORR: _a3_code line cites: handlers set L986-987 → :1121; handler flag L1025 → :1159; `_endpoint_middleware` L960 → :1068; its decorator tuple L963 → :1100 (the `dec is route_dec` skip at :1097-1098 stands).
   CORR: _a3_levels line cites: handler roots L227-233 → :233-239; `if c["s"] not in _handlers` L257 → :263; §3b L263+ → :269 (comment) / :283 (`_d2w`).
   CORR: sse.ts URL build ':163' → :165; useRecipeStream.ts openRecipeStream ':132' → :133; ai_recipe_stream.py reads ':151-153' → :150-152; ledger/pantry ':176-177' → :177-178.
   CORR: BEFORE sequences flatten chain.py's depth: in the POST chain reclaim_if_abandoned and enqueue_unknown_ingredients are h2 under generate_gustify_recipe; in the SSE tail enqueue_unknown_ingredients is h3 under reclaim_if_abandoned (not under _finalize_stream, which the AFTER text chains it to).
   CORR: sse transport rule 'a handler naming StreamingResponse/EventSourceResponse (ast.Name) OR text/event-stream' → over-fires on any StreamingResponse (file/CSV downloads); key on the `text/event-stream` Constant or EventSourceResponse only.
   CORR: fe arm 'one fecall edge useRecipeStream→sse module' → 1 fecall + 3 `typed` edges (GenerationModelTier ×2, RecipeStreamResultStatus); the fecall claim is right, the picture is slightly richer.
   MISSING: The COLUMN station (templates/center/shell/codebase-graph.html) is a second consumer of the method token the finder never lists: `METHOD` map at :610 (GET/POST/PUT/PATCH/DELETE) and `methodOf()` at :1186 returns null for 'WS' → `epMark(g, 6.5, m||"GET")` at :1250/:1509/:1669 draws the WS door with a
   MISSING: How the WS route's cases would EVER attach: with no route literal (WebSocket tests use `_FakeWebSocket` + `_run_ws`) neither the direct nor the via_route credit fires; the class needs a stated join rule (e.g. credit through a module-level test helper that names the handler, or a file-tier `via file`
   MISSING: `_detect_idiom` interaction: adding any transport regex to `_CALL_RES` changes idiom selection on projects whose only 'fetch' is an EventSource/WebSocket opener — the honest-empty statement must cover 'a project with a stream opener but no REST idiom', which today yields `present=True, screens=[]` (
   MISSING: `build_center_a3.py:129 _VERB_CLS` (GET/POST/DELETE/PUT/PATCH) — measured DEAD (1 occurrence, the definition), so no crash on 'WS'; worth stating since it is the driver's only method table.

######## backend-fns-unclaimed  [high]  tag=config+additive  REFUTED=False
TODAY: Committed gustify map: 67 handlers, 210 fn_nodes, 145 fn_edges (101 inferred / 44 extracted), 206 access edges; no __unclaimed__ bucket (stats.unclaimed=false, minted_models=0). center.config.json claims 95 of 150 backend .py files (168 incl. 18 package inits); the 55 unclaimed carry 238 graft callables (185 functions + 53 methods) and 13 route handlers in 7 files (equipment, notifications, meal_plan, history, locale, health, e2e_seed). Of the 238, 74 are reachable over graft `calls` from a mapped handler (depth 1:22 · 2:25 · 3:19 · 4:6 · 5:2) — 66 within ≤3 hops, in 18 files; 164 are unreached (the 13 unclaimed handlers' own trees, 9 scripts, seeders, middleware classes, config methods). On
 WHY: The function slice is homed by FILE → entity from center.config.json's code lists only; a function in an unclaimed file gets no slug and is dropped before any edge is considered (and every `calls` edge touching it goes with it — 9,631 edges counted as `unmapped_file` in the committed stats.graft).  ev=templates/center/generators/_a3_graft.py:152-162 (_file2slug — only archmap code.files rows); templates/center/generators/_a3_graft.py:175 (kind != function → methods never homed); templates/center/generators/_a3_graft.py:180-182 (slug = f2s.get(f); if slug: … — no slug, no fn)
 WHY: function_insight (the ORM-access pass that yields access ops, handler flags, layer) walks ONLY the mapped trees, so an unclaimed function has no `access`, hence no write-anchor, no d2w, no role — even where graft resolved the call. The comment names the exclusion explicitly.  ev=templates/center/generators/_a3_code.py:978 (for slug in ENTITY_CODE); templates/center/generators/_a3_code.py:1002 ('the fn walk below stays on the MAPPED trees only (no fns from unclaimed files)'); templates/center/generators/build_center_a3.py:2007-2011 (faccess passed to graft_arm is built from 
 WHY: The levels emitter draws a function only when it is a handler, a cross-entity model user, a handler-rooted callee, or a write-gradient descent; the write gradient itself is computed from faccess, so a claimed-file function whose only write callee sits in an unclaimed file is ALSO hidden (services/ai_recipes.py#record_spend_for is undrawn today for this reason).  ev=templates/center/generators/_a3_levels.py:229-233 (handlers), :249 (cross-entity model user), :257 (handler-only call rule), :292 (write-gradient descent test); templates/center/generators/_a3_graft.py:402-440 (derive_distance_to_write — anchors = faccess writers/commits only); levels.json: apps/api
 WHY: The model census (operator ruling 2026-08-27: config = ownership, never existence) exists for TABLE classes only; there is no file/function census, so the 55 unclaimed files are invisible to S11, cc-init's rank column and the `__unclaimed__` bucket (which the committed gustify map does not even instantiate: stats.unclaimed=false, minted_models=0).  ev=templates/center/generators/_a3_code.py:521-575 (model_census scans MODEL dirs for __tablename__ classes only); templates/center/generators/_a3_graph.py:1049-1087 (C3 mints MODELS into __unclaimed__; no fn/route minting); skills/gabe-pulse/scripts/angles.py:349-380 (S11 reads model_census only)
 WHY: The endpoint `behind` pill DOES count the unclaimed functions (no file filter, only the noise filter), so the station already shows 29 behind POST /setup/complete while the chain draws 13 — the pill and the walk disagree by exactly this class.  ev=templates/center/generators/_a3_graft.py:204-216 (_behind_context: callable kinds + noise only); chain.py 'POST /setup/complete' → behind=29 fns/depth 5 · fns drawn on chain: 13
 SEAM center.config: gustify claims the 18 request-path files that carry the 66 ≤3-hop functions (services/ownership.py + services/idempotency.py + middleware/idempotency.py + net/client_ip.py → auth · services/ai_credits.py + services/ai_spend.py → progression (or recipe, where models/ai_spend.py already sits) · services/recipe_filter_modes.py + reference/catalogs.py + reference/normalize.py + services/text_search.py + recipes/pipeline/runner.py + streaming/producer.py + observability/metrics.py → recipe · referenc
 SEAM _a3_code: file_census(repo, entity_code) beside model_census (L521): the root = common parent of every configured backend file (gustify: apps/api/); every .py under it (excl. tests/, __init__.py) not in any entity's code lists → {file, fns (top+methods via ast), routes (@router.<verb> count), tables, reason}; returned as {scanned_root, claimed, unclaimed:[…]}; serialised into archmap.json next to model_census (build_center_a3.py:1961). Cached like _CENSUS.
 SEAM _a3_graft: derive_reach(wiring, entities): a multi-source BFS from every mapped handler id (the endpoints' file#fn, same key as derive_behind L256) over `calls` within _behind_context's callable set; returns {file → min hops} for files NOT in _file2slug; graft_arm (L704) attaches it and the driver joins it onto file_census rows as `reach` (honest-empty: graft absent → no reach field, census still lists the files).
 SEAM _a3_code: OPTIONAL mint step (the C3 analogue for functions): function_insight (L968) parses census files with reach ≤3 in a SEPARATE tree set — computing access/handler/layer/span with entity '__unclaimed__' — WITHOUT adding them to `texts` (L982-985), because `texts` feeds the reference scan that sets internal/api/usage/base/sim on every EXISTING fn (L1063-1090): widening that corpus moves hub.usage on drawn nodes = change-in-place. _file2slug (_a3_graft.py:152) then homes census files to '__unclaimed__
 SEAM station(adapter/layout/card): No new kind. adapter.js:49 already bands every L1 node slug (the minted 'unclaimed' node included); layout.js:252/280 seeds fn nodes at EX[f.slug]||0, so unclaimed fns cluster on the bucket when it exists. Only needed: the fn card's entity chip should render '__unclaimed__' as 'unclaimed' (card.js) — cosmetic.
 SEAM _a3_levels: OUT OF SCOPE for this class but the boundary must be named: even claimed, a READ-only hop deeper than h1 stays undrawn (L257 handler-only rule; L292 draws only write-gradient descents). The allergen gate's reads (services/recipes.py:225,230 → reference/resolution.py:82,155) and idempotency's `_lookup` therefore do not appear under seam 1 alone; drawing 'callees of a gate-role fn' or 'reads ≤2 hops' is a separate change-in-place on this seam.
 SKETCH: 1. file_census: walk the configured backend root, list every .py no entity claims with fn/route/table counts. | 2. derive_reach: BFS from the 67 mapped handler ids over graft `calls`; stamp each unclaimed file with min hops (66 fns ≤3 on gustify). | 3. Emit census into archmap.json; S11 widens: 'model-census drift … + N request-path files unclaimed (min reach k)'; cc-init rank column lists them. | 4. Operator claims files in center.config.json (config) → function_insight covers them → access ops + d2w → derive_functions keeps their calls → levels draws handler-rooted (h1) and write-path hops. | 5. Optional mint: parse reach≤3 census files separately with entity '__unclaimed__', home them via _file2slug, let the existing C3 bucket receive them. | 6. Result on gustify (measured by re-running the draw rule): +26–28 fn nodes, +44–46 call wires, setup gains claim/ensure_principal_household writes, gust
 HONEST-EMPTY: A project whose config claims every backend file: file_census = {"scanned_root": "<root>", "claimed": N, "unclaimed": []} appears in archmap.json (the same new-key cost model_census paid); c4-graph.json, levels.json and every fn/edge count are byte-identical; S11 prints nothing for it; no bucket nod
 SIZE: ~60 lines _a3_code.file_census + ~30 lines _a3_graft.derive_reach + 3 lines driver + ~10 lines angles.py S11 (+ cc-init adopt-spec rank column text); optional mint ~45 lines in function_insight/_file2slug; batteries: tests/arch-graph (census fire/silent + reach + honest-empty), tests/pulse-angles (S11 line text at run.sh:170-174 re-pinned), tests/levels if the mint lands. gustify side: center.conf
 COUNTS: {"backend_py_files_excl_tests": 168, "backend_py_files_excl_inits": 150, "config_claimed_backend_files": 95, "unclaimed_files": 55, "unclaimed_files_excl_scripts": 46, "unclaimed_graft_callables": 238, "unclaimed_functions": 185, "unclaimed_methods": 53, "unclaimed_endpoints": 13, "unclaimed_route_files": 7, "total_backend_fns_ast_incl_scripts_inits": 710, "total_test_fns": 1423, "row_2133_equals_710_plus_1423": true, "drawn_fn_nodes": 210, "drawn_fn_edges": 145, "handlers": 67, "graft_calls_total": 26088, "graft_calls_extracted": 16653, "graft_calls_inferred": 9435, "graft_dropped_unmapped_fi
 RISK: The simulation re-implements the draw rule over functions only; the real d2w substrate includes methods (_a3_graft.py:204) and the use_edges source (levels L249), so the +26/+44 are a floor with ~±3 slack (the sim also 'lost' 31 nodes it cannot see —
 RISK: session.add_all([membership, principal]) at ownership.py:131 is a list literal the access pass does not bind (_orm_access L784-789) — Membership's write stays invisible after the claim; only a gap-2 widening fixes it. Do not let the map claim setup w
 RISK: Claiming files under an entity shifts existing numbers: hidden_fns per entity, hub.usage/internal/api on already-drawn fns (function_insight's reference scan widens with the corpus, L1063-1090), and the committed example map under templates/center/sh
 RISK: The optional mint step must parse census files in a SEPARATE tree set; adding them to `texts` turns an additive fix into change-in-place on every drawn fn's fan-in.
 J W1 Initial setup — POST /setup/complete (POST /consent, GET : BEFORE endpoint POST /setup/complete [behind 29 · gates get_auth_context·get_session·get_settings names only] → handler setup_complete [caller · d2w 1] → calls _me_response_from_result [pure] → calls complete_setup [accessor · d2w 0 · reads SetupCompletionState · com ||| AFTER endpoint POST /setup/complete → handler setup_complete → + calls get_idempotency_key [pure · h1 · api/setup.py:191 → middleware/idempotency.py:29] (wire: calls) → calls complete_setup [accessor · d2w 0] → + calls claim [accessor · d2w 0 · services/setup.py:342 → services/idempotency.py:114] (wire: calls, write-gradient 0→0) → + fnwrites IdempotencyKey [idempotency.py:131-139 session.add(record)] (wire: fnwrites) → +  ||| Δ The flagship journey's FIRST write finally appears: the walk shows the idempotency claim being INSERTED (not just read) and the household + principal Despensa being created; the endpoint's rollup grow
 J W2 Look for recipes — GET /recipes and GET /recipes/{recipe_: BEFORE ── GET /recipes ── → endpoint GET /recipes [behind 48] → handler search_recipes [caller] → calls search_recipes._norm_codes · build_score_input · load_user_match_context · match_score · compute_recipe_availability · get_user_cooked_recipe_ids · get_user_flags_ ||| AFTER ── GET /recipes ── → handler search_recipes → + calls allergen_codes · curiosity_technique_codes · equipment_codes · preference_codes · taste_axis_codes · temperature_codes · texture_codes [pure · h1 · api/recipes.py:326-479 → reference/catalogs.py; JSON readers, no DB] (wire: calls ×7) → (normalize_ingredient_code at h2 via _norm_codes — read path, stays undrawn) → ── GET /recipes/{recipe_id} ── → handler get_recipe ||| Δ GET /recipes gains 7 pure catalog-reader hops; GET /recipes/{recipe_id} gains the locale leg (3 hops, 1 new read); the allergen gate's three DB reads are homed but remain undrawn under this class alon
 J W3 Filter recipes — GET /repertorio/cupos (GET /taxonomy, PU: BEFORE endpoint GET /repertorio/cupos [behind 5] → handler get_cupos [caller] → calls list_cupos [accessor · r RecipeFilterMode] → rollup reads_from RecipeFilterMode ||| AFTER handler get_cupos → + calls subscription_tier_for [h1 · api/repertorio.py:45 → services/ai_credits.py:108; select(SubscriptionEntitlement.tier) is a column select → pure under the pass, gap 2] (wire: calls) → + calls max_slots_for_tier [pure · h1 · api/repertorio.py:21 import → services/recipe_filter_modes.py:24] (wire: calls) → calls list_cupos (unchanged) → PUT …/cupo: put_cupo → attach_cupo [d2w 0] → owned_mode [r ||| Δ The slot-cap gate (tier lookup + cap) becomes visible on the cupos read; the PUT keeps its 5 hops.
 J W4 Cook — POST /cooking/sessions (PATCH …/stage, …/steps/{st: BEFORE endpoint POST /cooking/sessions [behind 18] → handler post_start_session [caller · d2w 0] → calls _attach_photos · concurrent_cook_cap_for_tier · start_session [accessor · w CookingSession · r CookingSession,Recipe · commits] → calls seed_stage_schedule → _sch ||| AFTER handler post_start_session → + calls get_idempotency_key [pure · h1 · api/cooking.py:126 → middleware/idempotency.py:29] (wire: calls) → + calls subscription_tier_for [h1 · api/cooking.py:148 → ai_credits.py:108; column select → pure] (wire: calls) → calls start_session … (unchanged) → POST …/complete: complete_session → storage_method_shelf_life [reference/catalogs.py, h2 read] — not drawn ||| Δ Session start shows its idempotency-key read and tier lookup (2 pure hops); the four other cook endpoints are untouched.
 J W5 Store ingredients — GET /pantry/overview (POST /pantry/it: BEFORE endpoint GET /pantry/overview [behind 30] → handler get_pantry_overview [caller] → calls list_pantry_items [accessor · r PantryItem] · compute_cookability_summary [caller] → rollup reads_from PantryItem, UserDietaryProfile ||| AFTER handler get_pantry_overview → + calls load_user_language [accessor · h1 · api/pantry.py:94 → i18n/user_language.py:42] (wire: calls) → + fnreads UserFormatPreferences [user_language.py:50] (wire: fnreads) → + calls load_canonical_display_map [h1 · api/pantry.py:95 → user_language.py:58; column select → pure] (wire: calls) → + calls resolve_ingredient_display [pure · h1] (wire: calls) → rollup + reads_from UserFormatP ||| Δ The overview read gains the locale leg (3 hops, 1 new read); the three pantry writes are untouched.
 J J1 POST /setup/complete: BEFORE (same drawn chain as W1 — 13 fns; IdempotencyKey read-only; Household/Membership/Location absent) ||| AFTER (same as W1: + get_idempotency_key · + claim [w IdempotencyKey] · + _outcome_for · + complete · + ensure_principal_household [w Household, Location · r Membership, Household]; Membership's add_all write and the read-only load_household_context stay invisible) ||| Δ BACKEND-JOURNEYS J1's verified writes 1-3 (Household, Membership, Location) and the claim INSERT get drawn — 2 of the 3 ownership writes (Membership lost to add_all); the catalog's step order becomes 
 J J4 POST /cooking/sessions: BEFORE (as W4: post_start_session → _attach_photos · concurrent_cook_cap_for_tier · start_session → seed_stage_schedule → _schedule_next) ||| AFTER (as W4: + get_idempotency_key [pure] · + subscription_tier_for [pure/column-select]) ||| Δ Two pure gate hops appear at h1; the writes are unchanged.
 J J7 POST /recipe-creation/manual: BEFORE endpoint POST /recipe-creation/manual [behind 11] → handler post_create_manual [caller · d2w 0] → calls create_recipe_manual [accessor · w Recipe,RecipeCreationRequest,RecipeIngredient,RecipeStep · commits] ||| AFTER handler post_create_manual → + calls get_idempotency_key [pure · h1 · api/recipe_creation.py:77 → middleware/idempotency.py:29] (wire: calls) → calls create_recipe_manual (unchanged) — its h2 normalize_ingredient_code [reference/normalize.py] is a read hop, undrawn ||| Δ One pure hop (the idempotency-key read) joins the chain; the persist leg is unchanged.
 J K3 POST /recipe-creation/gustify (+ SSE tail GET /recipe-cre: BEFORE ── POST /recipe-creation/gustify ── → endpoint POST /recipe-creation/gustify [behind 47] → handler post_create_gustify [caller · d2w 0] → calls _gustify_response · _raise_for_pipeline_error [pure] → calls generate_gustify_recipe [accessor · d2w 0 · w RecipeCre ||| AFTER ── POST /recipe-creation/gustify ── → handler post_create_gustify → + calls get_idempotency_key [pure · h1 · api/recipe_creation.py:241] (wire: calls) → + calls client_ip [pure · h1 · recipe_creation.py:252 → net/client_ip.py] (wire: calls) → calls generate_gustify_recipe [d2w 0] → + calls record_spend_for [caller · d2w 1 · services/ai_recipes.py:306 — a CLAIMED-file fn hidden today because its only write callee is u ||| Δ The credit gate's LEDGER write (AiSpendLog) appears on both the REST and the SSE tail — the persist half of 'credit gate → LLM → persist'; the gate's own reads remain hidden (column selects + read-pat
 UNCHANGED: W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /recipes/{rec, J2 DELETE /me, J3 POST /cooking/sessions/{s, J5 POST /pantry/items, J6 POST /shopping/items/{ite, J8 POST /consent, K1 auth/identity provisionin, K2 GET /recipes/explore (pur, K4 seed/ops lane (seed_catal, K5 P
 --- REFUTER: wrong checks:
   ✗ station seam: adapter.js:49 bands every L1 node slug; layout.js:252/280 seeds fn nodes at EX[f.slug]||0; card.js entity chip is cosmetic ⇒ HEAD adapter.js:49 `var _ents=((_C4.l1&&_C4.l1.nodes)||[]).map(e=>e.slug||e.id)` confirmed; HEAD layout.js:252 `{ id:f.id, kind:"function", ent:f.slug, …}` confirmed; but the x-seed is at layout.js:284 and :306 `n.x=(EX[n.ent]||0)+…` — :280 is `_stashPurge("__fn")`. Wrong line, right mechanism (n.en
   ✗ seam 1 names the 18 request-path files that carry the 66 ≤3-hop functions ⇒ the measured 18 files are the finder's 17 PLUS apps/api/config.py (get_settings, reached at depth 2 from delete_me); the seam-1 claim list names 17 files, so it covers 65 of the 66. Minor: config.py is not request-path and should not be claimed, but the 66 figure includes it
   CORR: Line cites for _a3_code.py are against HEAD (2622 lines) — the working tree carries +158 UNCOMMITTED lines: `for slug in ENTITY_CODE` is at _a3_code.py:1115 (cited 978), the MAPPED-trees comment at :1139 (cited 1002), `_ORM_COMMIT` at :875 (cited 738), `_orm_access`/`_arg_model` at :903-925 (cited 784-789), function_insight def at :1105; _a3_levels.py rules are at :236/:252/:263/:298 in the tree (
   CORR: today_draws numbers are the COMMITTED baseline; both trees have moved uncommitted since the finder ran: gustify docs/site/center/center.config.json (M) now claims 98 .py files (+ apps/api/api/history.py, apps/api/api/notifications.py, apps/api/models/admin_review.py) → 52 unclaimed (43 excl scripts), 233 callables (180 fn + 53 methods), 8 unclaimed route handlers in 5 files (equipment, meal_plan, 
   CORR: sim_handlers_gaining_hops 15 → measured 17: every handler with ≥1 h1 `calls` edge into one of the 18 files gains at least that hop under the handler-only rule once both ends are homed (POST /cooking/sessions · GET /pantry/frequent-ingredients · GET /pantry/overview · POST /pantry/reset/apply · POST /pantry/resolve-batch · POST /recipe-creation/gustify · …/manual · …/{request_id}/relief-accept · GE
   CORR: W1/J1 AFTER rollup: +3 writes_to (IdempotencyKey, Household, Location) is right, but reads gain THREE not two — A2's derive_endpoint_access (_a3_graft.py:303-358) walks the WHOLE call tree, so get_principal_location's select(Location) (ownership.py:60, reached via ensure_principal_household → extracted edge) adds `reads_from Location`; Location is pantry-owned (config), so this lands as a CROSS-en
   CORR: 'NOT drawn even after claim: load_household_context, get_principal_location' is imprecise — they are not on the CHAIN (no edge: d2w None at :292), but rule 2 (_a3_levels.py HEAD:249, cross-entity model user via model-insight internal_refs, a `\bClass\b` regex over each def's span, _a3_code.py WT:772-783) DOES draw them as floating nodes once ownership.py is homed to auth, because both mention pant
   CORR: seam 1's file list names 17 files but the 66 ≤3-hop count spans 18 — the 18th is apps/api/config.py (get_settings, depth 2 from delete_me → delete_identity); 65 of the 66 sit in the 17 named files.
   CORR: seam 5 cite: the fn x-seed is layout.js:284/:306 `n.x=(EX[n.ent]||0)+…` (n.ent = f.slug via :252), not :280 (`_stashPurge("__fn")`).
   CORR: hidden_fns 'settings: 0' → the settings entity carries no hidden_fns key in HEAD levels.json (absent, not 0).
   CORR: The SSE tail handler stream_gustify lives in apps/api/api/recipe_stream.py:115 (client_ip_from at :145), not api/recipe_creation.py — the finder's K3 hop is right, the file is implied wrong by grouping it under recipe_creation.
   MISSING: POST /recipe-creation/{request_id}/relief-accept — the K3 sibling: today relief_accept [accessor · d2w 0 · commits] → enqueue_unknown_ingredients (5 fns); after claiming it gains client_ip (h1, wiring edge) AND the same ledger path relief_accept → record_spend_for → record_spend [w AiSpendLog] (ai_r
   MISSING: The four /recipe-filter-modes CRUD handlers (W3's 'Filter recipes' siblings; PUT …/cupo is in W3 but these are not): POST / → create_mode [session.add(mode) + flush, recipe_filter_modes.py:73-74 — a NEW write_to RecipeFilterMode] + subscription_tier_for + max_slots_for_tier; DELETE / → delete_mode [
   MISSING: POST /pantry/resolve-batch (W5 'Store ingredients' sibling): today 2 fns (resolve_batch → resolve_batch._label, rollup EMPTY); after: +5 h1 hops (i18n/user_language.py#load_user_language [r UserFormatPreferences], #load_canonical_display_map, reference/fuzzy.py#match_line, #parse_quantity_line, serv
   MISSING: The rule-2 (cross-entity model user) consequence of claiming: 13 defs become cross-entity model users under the proposed homes, adding ~6 floating fn nodes beyond the +28 floor and NEW L1 use_edges (auth→pantry via Location, progression→recipe via AiSpendLog and →auth via SubscriptionEntitlement, al
   MISSING: A DECAY note the finder should have stated up front: both trees have moved uncommitted since the measurement (gustify config +3 files; suite example map regenerated to 219/150/72) — any re-pin of the +26/+44 numbers in a commit must re-baseline against the tree, not HEAD.

######## class-methods  [high]  tag=change-in-place  REFUTED=False
TODAY: 0 method nodes among 210 fn_nodes / 145 fn_edges (119 intra-entity, 26 cross); today's max fan-in on the drawn call net is 5 (_attach_photos); 1 gate-role node (assert_recipe_allergen_safe). The method mass leaks through ONE channel only: the endpoint `behind` pill — `AuthContext.require_household` named in behind.names on 48/67 endpoints, ResolutionSnapshot.resolve/violations_for on 3, GeminiConfig.from_settings + Settings.ai_timeout_for/gemini_model_for on 3, EventBus.publish 1, StreamPipeline.events 1 — because _behind_context walks methods while derive_functions does not. Every gustify handler that calls `ctx.require_household()` (51 real call sites in 14 api/ files; graft resolves 54 ca
 WHY: derive_functions admits only kind=='function' into fn_slug, so no method is ever homed; the both-ends check then discards every calls edge with a method end (65 from homed functions, 54 with a homed method target, 48 of them cross-entity).  ev=templates/center/generators/_a3_graft.py:175 (`if n.get("kind") != "function": continue`); templates/center/generators/_a3_graft.py:183-186 (`if s in fn_slug and t in fn_slug and s != t`); measured: wiring.json calls fn(homed)->method(non-noise)=65, ->method(homed)=54, cross-entity 48 / intra 6
 WHY: The drop is ONLY the drawn-set feed: the sibling substrate already walks methods (_CALLABLE_KINDS includes 'method'), so behind/fn_behind/endpoint_access/fn_roles/distance_to_write all see them — which is why AuthContext.require_household is already NAMED in the behind pill of 48/67 endpoints yet has no node and no wire.  ev=templates/center/generators/_a3_graft.py:199-204 (`_CALLABLE_KINDS = ("function", "method")`); templates/center/generators/_a3_graft.py:213 (_behind_context), :264, :286, :305, :371, :418 (every floor uses it); templates/center/shell/example/codebase-graph-station/c4-graph.js: behind.names carries '
 WHY: _a3_levels draws only handler-rooted calls (§3) plus write-path descents (§3b); the §3b comment already admits a method next-hop 'dead-ends here undrawn'. fn_nodes hardcode kind 'function' and join function_insight on `file::Cls.m`, a key function_insight already emits for methods — so a method node would self-enrich (doc/params) the moment it enters drawn_fn.  ev=templates/center/generators/_a3_levels.py:251-262 (`if c["s"] not in _handlers: continue`); templates/center/generators/_a3_levels.py:263-303 (§3b), :272-274 (the admission); templates/center/generators/_a3_levels.py:326 (`"kind": "function"`), :328 (`FI.get(rfile + "::" + name)`)
 WHY: No method on gustify is a write anchor: function_insight indexes 90 methods, 0 with ORM access ops/commits and 0 with sinks — so §3b can never pull a method in; the AFTER picture is exactly the DIRECT handler->method calls (51 edges into 3 method nodes). Non-handler callers of require_household (e.g. _build_me_response at setup.py:114, _build_settings) stay undrawn under the handler-rooted rule.  ev=/home/khujta/projects/apps/gustify/docs/site/center/archmap.json function_insight: 461 entries, 90 method=true, 0 access.ops, 0 sinks; apps/api/api/setup.py:114 (`household = ctx.require_household()` inside _build_me_response, h2); templates/center/generators/_a3_levels.py:256-258
 WHY: The value is concentrated in ONE node: of 149 apps/api methods, 96 sit in config-claimed files, and only 6 homed methods have any caller among homed functions (require_household 49, EventBus.publish 1, ExplorationBias.from_lists 1, TokenVerifier.verify 1, EventBus.register_once 1, GeminiConfig.from_settings 1). 45 of the 96 claimed methods are Pydantic validators (schemas/preferences.py 25, settin  ev=measured AST over apps/api (no .venv/tests): 149 class methods; claimed 96 / unclaimed 53; wiring.json fan-in from homed functions: require_household 49, SlidingWindowLimiter.allow 2 + retry_after 2 (unclaimed), EventBus.publish 1, ExplorationBias.from_lists 1, TokenVerifier.verify 1, EventBus.regis
 WHY: ROW corrections: 'AI pipeline stages as methods' is refuted — stages.py:59-177 are module-level async functions dispatched from the PIPELINE_STAGES list by run_pipeline (a registry dispatch, not a call: graft reaches run_pipeline from post_create_gustify but NO stage). The ROW's '206 methods / 482 dead-end calls' do not reproduce under the generator's own noise filter: 194 non-noise method nodes (  ev=apps/api/recipes/pipeline/stages.py:59-177 (`async def validate_request … serialize`); apps/api/recipes/pipeline/runner.py:12,21,24 (`from recipes.pipeline.stages import PIPELINE_STAGES` / `for stage in stages:`); apps/api/services/ai_recipes.py:406,528 (`await run_pipeline(`)
 SEAM _a3_graft: derive_functions L175: `if n.get("kind") not in _CALLABLE_KINDS: continue` (reuse the L204 tuple; noise filter and file→entity homing unchanged). L183-186 then keeps the 54 homed↔homed method calls automatically. Optionally return `kinds: {id: kind}` beside fn_slug/calls — or let levels read `graft["node_facts"][fid]["kind"]`, already derived in-process at L731/L743.
 SEAM _a3_levels: fn_nodes builder L320-363: keep `"kind": "function"` (L326, no station kind) and add `"method": True` when node_facts says kind=='method' (honest-empty: key absent otherwise). §3 (L256-258) and §3b (L263-303) untouched — the handler-rooted rule is what draws the 51 new edges; role comes free (`_is_gate_name` at _a3_graft.py:352-356 strips `Class.` → require_household = gate); hidden_fns (L307-318) now counts methods.
 SEAM station(adapter/layout/card): layout.js _buildFnData L250-256: fill the existing `m:{…, method:null}` slot with `f.method||null`; card.js Function card: a 'method' tag when set; legend: nothing new (rides `function` + the existing gate badge). Hub relief (optional, recommended): card.js L956 `gates` list — append the handler's DIRECT gate-role method callees so the 409 gate reads on the endpoint card, and/or route the 49 auth-bound `calls` wires through the existing __uniDrawBundles (layout.js) so the auth sphere is not a 49
 SEAM new module: batteries only — tests/levels/run.sh: FIRE (graft fixture whose fn_slug carries a `Cls.m` id with node_facts kind=method, called by a handler → drawn, `method:true`, role gate) + SILENT (a method with no handler caller → hidden_fns only; graft-less stays byte-identical, L83/L109 pins). tests/arch-graph/run.sh: derive_functions admits a `kind:"method"` node (a noise-path method still dropped, sibling of L354). Re-pin the example map: levels.json fn_nodes 210→213, fn_edges 145→196, hidden_fns +93 
 SKETCH: 1. derive_functions admits kind ∈ {function, method} (noise-filtered, config-homed) into fn_slug. | 2. calls with both ends in fn_slug survive → the 51 handler→method edges (49 require_household + EventBus.publish + ExplorationBias.from_lists) enter `calls`. | 3. levels §3 (handler-rooted) draws them; §3b unchanged — no gustify method is a write anchor (0/90 with ORM access), so nothing deeper moves. | 4. fn_nodes: the FI join on `file::Cls.m` self-enriches (doc/params); role from fn_roles → require_household = gate; `method:true` from node_facts. | 5. hidden_fns = fn_slug − drawn now counts the 93 undrawn homed methods (validators, __init__, properties). | 6. station: same `function` kind + gate badge; m.method filled; gate folded into the endpoint card or wires bundled.
 HONEST-EMPTY: graft absent → `functions` missing → fn_edges [] and no hidden_fns keys (levels L254/L310) — byte-identical to today; an index with zero non-noise kind:'method' nodes in claimed files → fn_slug identical → byte-identical; c4-graph.json is untouched in every case (behind/endpoint_access/fn_roles/d2w 
 SIZE: ~1 line _a3_graft (L175) + ~3 lines _a3_levels (method flag) + ~3-15 lines station (m.method slot + card tag; +~10 if the gate-fold/bundle relief is taken) + ~20 lines batteries (levels FIRE/SILENT, arch-graph admit) + example levels.json re-pin (+3 fn_nodes / +51 fn_edges / hidden_fns +93); no c4 regen
 COUNTS: {"source_class_methods_apps_api_nontest_ast": 149, "source_class_methods_apps_api_incl_tests_ast": 162, "source_methods_in_config_claimed_files": 96, "source_methods_in_unclaimed_files": 53, "source_methods_pydantic_validators_claimed": 45, "unclaimed_method_files_top": {"config.py": 15, "middleware/rate_limit.py": 8, "integrations/circuit_breaker.py": 5, "reference/resolution.py": 4, "reference/catalogs.py": 3, "integrations/providers.py": 3, "streaming/producer.py": 2, "services/meal_plan.py": 2}, "graft_method_nodes_total": 2213, "graft_method_nodes_non_noise": 194, "graft_method_nodes_py":
 RISK: HUB: 49 `calls` wires converge on one auth node from 7 entities (today's max fan-in is 5); __uniComputeSolo (layout.js:2069-2076) will not fold it (49 callers), so the auth sphere becomes a 49-spoke star unless the gate is folded into the endpoint ca
 RISK: hidden_fns moves on every entity (+93 total on gustify) — a counter the levels page renders (tests/levels-page/run.sh:103-105); the commit must state the delta or the star field changes silently.
 RISK: TRUST: 63 of the 65 homed fn→method calls are `inferred` — graft resolves `ctx.require_household()` by attribute name; a same-named method on a second class would mis-wire (only one require_household exists in gustify today; the FLOOR caveat applies)
 RISK: The behind pill (48 endpoints) and the new node would double-name the gate — the endpoint card should not count it twice in 'Pulls in N function(s)'.
 J W1 Initial setup [POST /consent, POST /setup/complete, GET /: BEFORE POST /consent: endpoint POST /consent → handler post_consent → calls _status → calls record_consent → fnwrites ConsentRecord → POST /setup/complete: handler setup_complete → calls _me_response_from_result → calls complete_setup → calls _discard_claim · _stamp_ ||| AFTER POST /consent: unchanged (consent.py:7 — never calls require_household) → POST /setup/complete: unchanged (setup.py:97 reads `ctx.has_setup`, a @property — attribute access, no calls edge) → GET /me: unchanged on the drawn rule — setup.py:114 `_build_me_response → require_household` is an h2 non-handler call; §3 draws handler-rooted only (the behind pill already names it) → GET /settings: handler get_settings_route → ||| Δ GET /settings sprouts a second h1 wire from its handler into the auth entity — the 409 setup gate as a gate-badged node; the other three legs look identical.
 J W2 Look for recipes [GET /recipes/explore, GET /recipes, GET: BEFORE GET /recipes/explore: endpoint GET /recipes/explore → handler explore_recipes → calls build_candidate · build_cooked_ledger · filter_candidates · rerank_by_preferences · compute_recipe_availability → calls get_exploration_preferences → fnreads UserExplorationP ||| AFTER GET /recipes/explore: handler explore_recipes → + calls AuthContext.require_household [gate · auth · inferred; recipe_explore.py:66] → + calls ExplorationBias.from_lists [pure · recipe · inferred; guardrails/novelty.py] (its own hop → region_members stays undrawn: reference/catalogs.py unclaimed) → calls build_candidate … load_user_allergens (unchanged) → GET /recipes: unchanged (ReferenceCatalog.items reached only v ||| Δ The explore step gains two h1 wires — the auth gate and the novelty-bias constructor — so the heaviest read composition shows its gate and its bias object; the other two steps are unchanged.
 J W3 Filter recipes [GET /taxonomy, GET /repertorio/cupos, PUT: BEFORE GET /taxonomy: handler get_taxonomy · fnreads TaxonomyDimension · TaxonomyValue → GET /repertorio/cupos: handler get_cupos · calls list_cupos · fnreads RecipeFilterMode → PUT …/cupo: handler put_cupo · calls _parse_mode_id · attach_cupo · list_cupos · valid_ti ||| AFTER GET /taxonomy: unchanged (BFS 0 method hits) → GET /repertorio/cupos: unchanged (0 method hits) → PUT …/cupo: unchanged (0 method hits) → GET /recipes/explore: + calls AuthContext.require_household · + calls ExplorationBias.from_lists (as W2) → PATCH …/flags: unchanged (0 method hits) ||| Δ Only the shared explore step changes (the W2 delta); the four filter-mode steps draw exactly as today.
 J W4 Cook [POST /cooking/sessions, PATCH …/stage, PATCH …/step: BEFORE POST /cooking/sessions: handler post_start_session → calls _attach_photos · concurrent_cook_cap_for_tier → calls start_session → fnwrites CookingSession → calls seed_stage_schedule → _schedule_next → fnwrites CookingStageReminder → PATCH …/stage: handler patch ||| AFTER POST /cooking/sessions: handler post_start_session · + calls AuthContext.require_household [gate · auth · inferred; cooking.py — 13 sites L125–572] · calls … (unchanged) → PATCH …/stage: handler patch_stage · + calls AuthContext.require_household · … → PATCH …/steps: handler patch_step_progress · + calls AuthContext.require_household · … → PATCH …/timer: handler patch_session_timer · + calls AuthContext.require_house ||| Δ Every cook step gains the auth-gate wire at h1 (5 new cross-entity wires cooking→auth); the completion step additionally shows the bus.publish hop it takes after its commit — a dead-end node until the
 J W5 Store ingredients [POST /pantry/items, POST /pantry/items: BEFORE POST /pantry/items: handler create_pantry_item · calls add_pantry_item · fnwrites IngredientHistoryEvent · PantryItem → POST /pantry/items/batch: handler create_pantry_items_batch · calls add_pantry_item · fnwrites IngredientHistoryEvent · PantryItem → PATCH / ||| AFTER POST /pantry/items: handler create_pantry_item · + calls AuthContext.require_household [gate · auth · inferred; pantry.py — 15 sites L89–554] · calls add_pantry_item · … → POST /pantry/items/batch: handler create_pantry_items_batch · + calls AuthContext.require_household · … → PATCH /pantry/items/{item_id}: handler update_pantry_item · + calls AuthContext.require_household · … → GET /pantry/overview: handler get_pant ||| Δ All four pantry steps gain the h1 gate wire into auth; the write legs are unchanged.
 J W6 Pantry locations [POST /pantry/locations, POST …/assign-l: BEFORE POST /pantry/locations: handler create_location · calls add_location · fnwrites Location → POST …/assign-location: handler assign_location [accessor] · fnreads Location · calls edit_pantry_item · fnwrites PantryItem → PATCH …/locations/{id}: handler update_loc ||| AFTER POST /pantry/locations: handler create_location · + calls AuthContext.require_household [gate · auth · inferred; pantry.py] · … → POST …/assign-location: handler assign_location · + calls AuthContext.require_household · … → PATCH …/locations/{id}: handler update_location · + calls AuthContext.require_household · … → DELETE …/locations/{id}: handler remove_location · + calls AuthContext.require_household · … ||| Δ All four location steps gain the h1 gate wire into auth; nothing else moves.
 J W7 Shopping [POST /shopping/items, POST /shopping/from-plann: BEFORE POST /shopping/items: handler create_shopping_item · calls add_shopping_item · fnwrites ShoppingItem → POST /shopping/from-planned-recipes: handler post_from_planned_recipes · calls generate_from_planned_recipes · fnwrites ShoppingItem → GET /shopping: handler ||| AFTER POST /shopping/items: handler create_shopping_item · + calls AuthContext.require_household [gate · auth · inferred; shopping.py:42/65/85/104/119] · … → POST /shopping/from-planned-recipes: handler post_from_planned_recipes · + calls AuthContext.require_household · … → GET /shopping: handler get_shopping_dashboard · + calls AuthContext.require_household · … → POST …/confirm-bought: handler post_confirm_bought · + call ||| Δ All four shopping steps gain the h1 gate wire into auth.
 J W8 Plan [POST /recipes/{recipe_id}/plan, POST /shopping/from: BEFORE POST …/plan: handler post_plan_recipe · calls load_user_allergens · fnreads UserDietaryProfile · calls plan_recipe · fnwrites PlannedRecipe · calls plan_into_cupo → POST /shopping/from-planned-recipes: (as W7) → DELETE …/plan: handler delete_plan_recipe · call ||| AFTER POST …/plan: handler post_plan_recipe · + calls AuthContext.require_household [gate · auth · inferred; recipe_plan.py:46] · … (ResolutionSnapshot.violations_for at h4 via unclaimed resolution.py stays undrawn) → POST /shopping/from-planned-recipes: + calls AuthContext.require_household (as W7) → DELETE …/plan: handler delete_plan_recipe · + calls AuthContext.require_household [recipe_plan.py:120] · … ||| Δ All three plan steps gain the h1 gate wire into auth.
 J J3 POST /cooking/sessions/{id}/complete: BEFORE endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete → calls complete_session → fnwrites CookingSession · DishHistoryEvent · IngredientHistoryEvent · PantryItem → calls clear_pending_cooking_timer_notifications → fnwrites Notification ||| AFTER endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete → + calls AuthContext.require_household [gate · auth · inferred; cooking.py] → + calls EventBus.publish [pure · cooking · inferred; cooking.py:417 → events/bus.py] → calls complete_session → fnwrites … (unchanged) ||| Δ The domain-event completion finally shows the publish hop it takes after the commit (a leaf until the event-bus class) plus its 409 gate.
 J J4 POST /cooking/sessions: BEFORE endpoint POST /cooking/sessions → handler post_start_session → calls _attach_photos · concurrent_cook_cap_for_tier → calls start_session → fnwrites CookingSession → calls seed_stage_schedule → _schedule_next → fnwrites CookingStageReminder ||| AFTER endpoint POST /cooking/sessions → handler post_start_session → + calls AuthContext.require_household [gate · auth · inferred; cooking.py] → calls … (unchanged) ||| Δ One new h1 gate wire into auth.
 J J5 POST /pantry/items: BEFORE endpoint POST /pantry/items → handler create_pantry_item → calls add_pantry_item → fnwrites IngredientHistoryEvent · PantryItem ||| AFTER endpoint POST /pantry/items → handler create_pantry_item → + calls AuthContext.require_household [gate · auth · inferred; pantry.py] → calls add_pantry_item → fnwrites IngredientHistoryEvent · PantryItem ||| Δ One new h1 gate wire into auth.
 J J6 POST /shopping/items/{id}/confirm-bought: BEFORE endpoint POST /shopping/items/{item_id}/confirm-bought → handler post_confirm_bought → calls confirm_bought → fnwrites IngredientHistoryEvent · PantryItem · ShoppingItem ||| AFTER endpoint POST /shopping/items/{item_id}/confirm-bought → handler post_confirm_bought → + calls AuthContext.require_household [gate · auth · inferred; shopping.py] → calls confirm_bought → fnwrites … (unchanged) ||| Δ One new h1 gate wire into auth.
 J K2 GET /recipes/explore (pure read): BEFORE endpoint GET /recipes/explore → handler explore_recipes → calls build_candidate · build_cooked_ledger · filter_candidates · rerank_by_preferences · compute_recipe_availability → calls get_exploration_preferences · fnreads UserExplorationPreferences → calls ite ||| AFTER endpoint GET /recipes/explore → handler explore_recipes → + calls AuthContext.require_household [gate · auth · inferred; recipe_explore.py:66] → + calls ExplorationBias.from_lists [pure · recipe · inferred; guardrails/novelty.py] → calls … (unchanged) ||| Δ The pure-read journey shows its 409 gate and the bias constructor it builds before ranking (11 h1 fns instead of 9).
 J K3 POST /recipe-creation/gustify (credit gate → LLM → persis: BEFORE endpoint POST /recipe-creation/gustify → handler post_create_gustify → calls _gustify_response · _raise_for_pipeline_error → calls generate_gustify_recipe → fnwrites RecipeCreationRequest → calls reclaim_if_abandoned → calls enqueue_unknown_ingredients → fnwri ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify → + calls AuthContext.require_household [gate · auth · inferred; recipe_creation.py:240] → calls … (unchanged) → SSE tail: handler stream_gustify · + calls AuthContext.require_household [recipe_stream.py:140] · … (unchanged) → NOT gained: the pipeline stages — module-level functions (stages.py:59-177) dispatched from PIPELINE_STAGES (runner.py:12,21 ||| Δ Only the 409 gate appears at h1 on both the POST and its SSE tail; the credit-gate → LLM → parse chain stays invisible (registry dispatch + unclaimed files, not this class).
 UNCHANGED: J1 POST /setup/complete, J2 DELETE /me, J7 POST /recipe-creation/man, J8 POST /consent, K1 the auth/identity provisi, K4 the seed/ops lane (seed_c, K5 POST /recipes/demand (own
 --- REFUTER: wrong checks:
   ✗ 51 new handler-rooted edges into 3 method nodes; require_household has 49 callers from config-homed handlers; drawn fan-in becomes 49 ⇒ Measured: require_household graft callers 54, in apps/api/api/ 52, HOMED 49, homed HANDLERS 47 — the 2 homed non-handlers are setup.py#_build_me_response (setup.py:114) and user_settings.py#_build_settings (user_settings.py:47), which the finder itself says stay undrawn. Handler-rooted new edges = 4
   ✗ today_draws: 210 fn_nodes / 145 fn_edges (119 intra, 26 cross) / hidden_fns 2071 on the committed example ⇒ chain.py reads templates/center/shell/example/codebase-graph-station (chain.py:9). That committed levels.json has 219 fn_nodes / 150 fn_edges (124 intra / 26 cross) / hidden_fns sum 2067 (settings has no hidden_fns key) / max fan-in 5 (_attach_photos) / 1 gate node (assert_recipe_allergen_safe) / 0 
   ✗ 149 apps/api non-test methods, 96 claimed / 53 unclaimed; 45 of the claimed are Pydantic validators; unclaimed top files config.py 15 · rate_limit.py 8 · circuit_breaker.py 5 · resolution.py 4 · catal ⇒ AST over apps/api (no .venv, no tests): 149 methods (162 incl. tests), claimed 96 / unclaimed 53, unclaimed per-file breakdown reproduces exactly. Validators in claimed files = 50, not 45: schemas/preferences.py 25, schemas/settings.py 8, schemas/recipe.py 4, schemas/cooking.py 4, schemas/pantry.py 
   ✗ Pydantic validators' 24 outbound calls go into reference/catalogs.py (unclaimed) and enter fn_slug/hidden only ⇒ The 24 homed method→fn calls that survive the both-ends check target CLAIMED files: 16 → schemas/preferences.py#clean_tags, 2 each → preferences.py#_check_temperature_pref/_check_complexity_cap, verifier.py#_get_firebase_app/identity_from_firebase_claims, gemini.py#_build_genai_client/_to_result (so
   ✗ Station seams: layout.js _buildFnData L250-256 `method:null`; card.js L956 `gates`; __uniComputeSolo layout.js:2069-2076; tests/levels-page/run.sh:103-105 hidden_fns ⇒ layout.js:253 `m:{ … method:null }` confirmed; __uniComputeSolo at layout.js:2077 with `_SOLO_REL` incl. calls (a 47-caller node is not solo) confirmed; tests/levels-page/run.sh:104-106 hidden_fns check confirmed. But card.js is 439 lines — the `gates` list is at layout.js:960-961 (`var gates=(n.mid
   CORR: _a3_graft.py both-ends check: cited 183-186 → actual :188 (L175 filter and L204 _CALLABLE_KINDS correct).
   CORR: _a3_levels.py line cites are all offset: handler-root check :263 (not 251-262) · §3b :268-311 (not 263-303) · `"kind": "function"` :337 (not 326) · FI join :334 (not 328) · hidden_fns :313-323 (not 307-318) · fn_nodes builder :325-370 (not 320-363) · honest-empty branches :260/:316 (not 254/310).
   CORR: _a3_code.py: `qual = f"{cls}.{node.name}"` is :1155 (not 1018); methods indexed via ClassDef walk at :1181-1185 (not 1044-1047).
   CORR: W1 GET /settings AFTER hop cite: user_settings.py:47 is inside `_build_settings` (non-handler h1, stays undrawn like _build_me_response); the drawn handler-rooted call is `get_settings_route` at user_settings.py:113. Sequence holds, line wrong.
   CORR: W8 DELETE /recipes/{recipe_id}/plan: `delete_plan_recipe` calls require_household at recipe_plan.py:100 (def :91); :120 is `delete_planned_recipe` = DELETE /planned-recipes/{planned_id}.
   CORR: 'require_household 49 from config-homed handlers' → 49 HOMED callers, of which 47 are handlers (setup.py#_build_me_response :114 and user_settings.py#_build_settings :47 are the 2 non-handlers). Therefore after_new_fn_edges 51 → 49; after_max_fan_in 49 → 47 on the drawn net; 47 of 67 endpoints gain a method hop; journey_endpoints_gaining 21 → 23 of 35 (GET /settings, GET /recipes/explore, 5 cookin
   CORR: today_draws numbers (210 fn_nodes / 145 fn_edges / 119 intra / hidden 2071) are gustify's LIVE docs/site/center/levels.json; the committed example chain.py reads has 219 / 150 / 124 intra / 26 cross / hidden 2067 (settings has no hidden_fns key). Re-pin figures: fn_nodes 219→222, fn_edges 150→199, hidden_fns +93.
   CORR: behind.names: AuthContext.require_household appears on 49 l2 endpoint nodes (72 l2 endpoint nodes incl. cross-entity stubs), not 48; ReferenceCatalog.items (3) and MatchCorpus.from_entries (1) were omitted from the leak list.
   CORR: Pydantic validators in claimed files: 50, not 45 (preferences 25 · settings 8 · recipe 4 · cooking 4 · pantry 3 · agents/recipe_agent.py 2 · setup/notification/gastify/responses 1 each).
   CORR: 'their 24 outbound calls into reference/catalogs.py (unclaimed)' → the 24 homed method→fn edges target CLAIMED files (16 → schemas/preferences.py#clean_tags, plus preferences.py _check_*, verifier.py, gemini.py helpers); they enter `calls` after the fix but stay undrawn. Calls to catalogs.py never survive the both-ends check.
   CORR: Station seam: the endpoint `gates` list is layout.js:960-961, not card.js:956 (card.js is 439 lines; its Guards section is :130-137).
   CORR: ExplorationBias lives at apps/api/guardrails/novelty.py:128 (claimed: recipe), not recipes/guardrails/novelty.py.
   CORR: require_household source census (AST): 52 real call sites in 13 api/ files, 50 inside route handlers; the 53rd grep hit is consent.py:7's docstring (finder: 52 hits / 51 sites / 14 files).
   CORR: stages.py:67/76 are the `rate_limit_user`/`rate_limit_ip` def lines; the `deps.*_limiter.allow` calls are :70/:79.
   MISSING: The 24 method→fn `calls` edges that newly enter the levels feed (validators → schemas/preferences.py#clean_tags ×16, FirebaseTokenVerifier.verify → verifier helpers, _RealGeminiRecipeClient.generate_recipes → gemini helpers): undrawn today and after, but they change `functions.calls` and any battery
   MISSING: Non-journey endpoints that gain the same hop and should be in the re-pin count: PATCH /settings/household|preferences|exploration (user_settings.py:124/151/234), GET+POST /account/export (account.py:31/48), POST /recipe-creation/{request_id}/relief-accept (:278), DELETE /planned-recipes/{planned_id}
   MISSING: The 3 require_household callers in UNCLAIMED api files (history.py#get_dish_history, meal_plan.py#get_current, meal_plan.py#post_generate_plan) — named in counts but the endpoints GET /history/dishes etc. sit in the committed map with a behind pill naming the gate and will still draw no hop: a cover
   MISSING: The committed example vs live-twin sourcing: the finder measured 'today' on gustify's live levels.json while chain.py (the mandated BEFORE) reads the committed example (219/150); the two differ, and the re-pin numbers in the fix must be stated against the example the battery pins.

######## schemas  [high]  tag=config+additive+change-in-place  REFUTED=False
TODAY: 123 schema nodes (of 146 in schemas/*.py; 151 Pydantic classes repo-wide) · 109 intra `touches` (43 response-class · 27 body-class · 28 helper · 11 model) + 11 cross touches, all one undirected purple wire · 0 intra `consumes` (1 cross) · `resp` = a string on 57 endpoint nodes, no wire, det.payload field-count on the 50 whose class is drawn · 63 + 6 nests · 0 schema→model edges · 7 endpoints whose resp names a class the map has no node for (GET/POST /consent, GET/POST/PUT / [recipe-filter-modes], GET /catalog/{domain}, GET /taxonomy). Every response trace ends at the DTO: chain.py prints `schema wires: [('touches', X)]` and the model column is reached only through the handler's access rollup
 WHY: (a) parse_schemas only reads the files center.config.json lists under code.schemas; 6 of gustify's 17 schemas/*.py files are unclaimed, so their 23 classes never enter `documented` and can neither be nodes nor touch-targets. Zero classes are lost to the `if fields` filter in claimed files (measured 0).  ev=templates/center/generators/_a3_code.py:403 `schemas = parse_schemas(repo, layers.get("schemas", []))`; templates/center/generators/_a3_code.py:197-216 (walks only `files`; keeps a class only `if fields`); templates/center/generators/_a3_code.py:419-422 `documented = models|schemas; e["touches"] = s
 WHY: (a′) 7 drawn endpoints declare a response_model naming an undrawn class: the node carries `resp` as a string, det.payload is honest-empty (schema_fields has no entry), and no wire leaves the endpoint toward its contract.  ev=templates/center/generators/_a3_graph.py:608-609 (resp kept as a node field); templates/center/generators/_a3_graph.py:614-616 (payload only when schema_fields knows the class); committed c4-graph.js: GET /consent, POST /consent, GET /, POST /, PUT /, GET /catalog/{domain}, GET /taxonomy — resp name
 WHY: (b) The ORM-access pass records only session.add/delete/merge(x), select(Model), Core insert/update/delete(Model) and session.get(Model); `Schema.model_validate(row)` is not an idiom it looks for, and parse_schemas never records `model_config = ConfigDict(from_attributes=True)` (an Assign, not an AnnAssign). No edge kind exists for schema→model in the C4 builder, so the count is structurally 0.  ev=templates/center/generators/_a3_code.py:757-760 (_ORM_WRITE_M / _ORM_WRITE_CORE / _ORM_READ_CORE — the closed idiom set); templates/center/generators/_a3_code.py:766-817 `_orm_access` (symtab binds only `x = Model(...)` and `x: Model`; no model_validate branch); templates/center/generators/_a3_code.
 WHY: (c) `touches` is every bare Name in the handler body ∩ documented — request body, response class and nested helpers alike, undirected. The `consumes` wire (signature-typed classes) runs AFTER touches and skips any target touches already claimed, and it also skips the response class — so on gustify it emits 0 intra edges and no `resp` edge exists at all (resp lives only as the node field). The stat  ev=templates/center/generators/_a3_code.py:180-182 `refs = {n.id for n in ast.walk(node) if isinstance(n, ast.Name)}`; templates/center/generators/_a3_graph.py:637-642 (touches loop fills `_ep_tgts`); templates/center/generators/_a3_graph.py:655-666 (consumes: `if _cls == _rsp … continue` at 660; `_tgt
 SEAM center.config: gustify center.config.json: claim apps/api/schemas/consent.py under legal-consent; schemas/catalog.py + schemas/recipe_filter_mode.py under recipe → +17 schema nodes, 7 endpoints gain their resp target (+payload counts), +5 nests (TaxonomyTree→Dimension→Value, RecipeFilterModes→RecipeFilterMode, WeeklyPlan→PlanDay/ShoppingItemResponse). equipment.py/meal_plan.py/locale.py (6 classes) wait on gap 1 — their route files are unclaimed, so the nodes would be degree-zero and pruned by levels L474. Opt
 SEAM _a3_code: parse_schemas L211-216: also scan the class body for `model_config = ConfigDict(from_attributes=True)` / `class Config: from_attributes|orm_mode = True` → emit `"orm": True` on the schema dict (additive field; absent otherwise).
 SEAM _a3_code: _orm_access L766-817 (surfaced via fn_insight L1033 `access`): add a `serializes` list — every `X.model_validate(v)` call → {schema:X, model: resolve(v)|None, line}. resolve(v) = today's symtab (ctor / annot / select-bound) + the gap-2 extensions built ONCE and shared: callee return annotation ({fn name → returns} over _PY_TEXTS), tuple-return positions, `for x in <bound>` and `for x in f()`, `obj.attr` via the param/local annotation's class field (dataclass AuthContext.user / SetupResult.*), tu
 SEAM _a3_graph: build_c4_graph L980-995 + _l2 L576-601: a new edge kind `serializes` schema→model, two arms with a fixed precedence: (1) site provenance — any function's `serializes` entry with a resolved model (26 pairs); (2) naming — a schema with `orm:True` whose name minus Response/Summary/Item/Ref/Block/Out matches exactly one model in the global cls_index (24/44 on gustify); site wins a conflict (NotificationBlock: site→UserNotificationPreferences, naming→Notification); a schema with two candidate models 
 SEAM _a3_graph: _l2 L637-666 role split (change-in-place): compute the signature class set BEFORE the touches loop; emit `resp` endpoint→schema:_bare_cls(resp) when that node is drawn (43 today, up to 50 after the claim); `consumes` for every signature-typed drawn class ≠ resp (27); the residue stays `touches` (28 schema + 11 model). Drop the `_tgt not in _ep_tgts` guard at 664 for sig-typed classes (it is what zeroes consumes). stats: keep `consumes` = consumes+nests (re-pinned), add `resp` and `touches` count
 SEAM _a3_levels: L400 and L439 read `kind == "touches"` to build `_global_touched` / per-endpoint `touch`; widen to {touches, consumes, resp} or the 27 request-body schemas fall out of `touch` and are pruned at L474 (the pin at tests/levels/run.sh:72-79 stays meaningful). L512-515 communities read `touch` + resp — unchanged.
 SEAM station(adapter/layout/card): assemble.py:15 REL2KIND += `serializes:'fk', resp:'calls'` (today an unknown rel falls to 'calls' silently — assemble.py:551 `REL2KIND[l.rel]||"calls"`); adapter.js:10-15 RELCOL/LINKMETA += serializes {w:3, pv:0} (inferred floor; resp/handler already present); card.js:67-70 relLabel/CONNICO += serializes:"serializes"/"serialized from", icon "model"; one legend row rendered from the same draw fn (legend law). chain.py `schema wires` prints the new kinds unchanged.
 SEAM station(adapter/layout/card): templates/center/shell/codebase-graph.html:1588 and :1836 filter `e.kind==="touches"` for the L2 drill touch list and the change walk — widen to touches|consumes|resp or the response class vanishes from the drill after the split.
 SKETCH: 1. parse_schemas: orm=True when from_attributes/orm_mode is set. | 2. fn_insight/_orm_access: for each X.model_validate(v) record (X, resolve(v)); resolve = ctor|annot|select-bound|callee-return|tuple-position|attr-field|for-over-bound; None stays None. | 3. build_c4_graph: pairs = site-resolved ∪ {naming hit for orm schemas}; site beats name; ambiguous → none; emit serializes (l2 own / cross via cls_index); stats.serializes. | 4. _l2 per endpoint: sig = classes in gsig annotations; rsp = _bare_cls(resp); wire rsp as `resp`, sig−rsp as `consumes`, remaining body refs as `touches`. | 5. levels + shell + station read the widened kind set; legend gets the serializes row. | 6. Twin config claims consent/catalog/recipe_filter_mode → the 7 dangling resp strings become drawn nodes with wires. | 7. Re-pin: arch-graph (stats.consumes, +resp/consumes/serializes FIRE+SILENT), orm-access (+serializes), levels, 
 HONEST-EMPTY: A project with no Pydantic (no `model_validate` call, no `from_attributes`/`orm_mode`, no `response_model`, no typed body params): parse_schemas emits no `orm` key, fn_insight `access` carries no `serializes` key, build_c4_graph emits zero serializes/resp/consumes edges and `stats.serializes = {"pre
 SIZE: ~8 lines parse_schemas · ~60 lines _a3_code serializes pass + shared symtab extension (co-owned with gap 2) · ~50 lines _a3_graph (15 role split + 35 serializes/stats) · ~6 lines _a3_levels · ~12 lines station (REL2KIND/LINKMETA/RELCOL/card/legend) + ~4 lines codebase-graph.html filters · 3 config lines. Batteries: tests/arch-graph (re-pin stats.consumes at :1012/:1019; + resp/consumes/serializes 
 NEW_KINDS: serializes (schema→model, inferred floor; REL2KIND bucket fk; LINKMETA w:3 pv:0; card 'serializes'/'serialized from'; legend row) || resp (endpoint→schema, census — promoted from a node field to a wire; REL2KIND/LINKMETA/card entries already exist in the station, unfed) || consumes (endpoint→schema request body — exists as a kind, goes from 0 → 27 intra edges; no new station entry needed)
 COUNTS: {"pydantic_classes_source_repo_wide": 151, "pydantic_classes_in_schemas_dir": 146, "schema_files_in_source": 17, "schema_files_claimed_by_config": 11, "schema_nodes_drawn": 123, "absent_schema_classes_total": 28, "absent_in_unclaimed_schemas_files": 23, "absent_outside_schemas_dir": 5, "absent_by_fields_filter_in_claimed_files": 0, "absent_per_file": {"schemas/catalog.py": 12, "schemas/consent.py": 2, "schemas/equipment.py": 2, "schemas/locale.py": 1, "schemas/meal_plan.py": 3, "schemas/recipe_filter_mode.py": 3, "agents/recipe_agent.py": 3, "config.py": 1, "streaming/events.py": 1}, "drawn_en
 RISK: The role split is change-in-place on a kind three consumers filter by name: _a3_levels.py:400/:439 (`touch` → the L474 prune would drop the 27 request-body schemas from levels pieces), templates/center/shell/codebase-graph.html:1588 and :1836 (L2 dri
 RISK: Promoting `resp` from a node field to an edge duplicates information: keep the field (det.payload, the levels _bare_cls join at _a3_levels.py:442-450 and the station card read it) and add the edge — do not move it, or the payload floor and the levels
 RISK: Naming-arm false positives: NotificationBlock strips to Notification (a real, wrong model) while its 8 sites say UserNotificationPreferences — site provenance must take precedence and a naming hit must require `orm:True` + exactly one model; ambiguou
 RISK: from_attributes classes fed by non-table objects (ItemCandidate ← services/shopping.py:31 dataclass; GeneratedRecipe ← an LLM candidate dict; SkillUnlockSummary/SubscriptionSummary hand-built) must stay edge-less; a naive 'orm schema ⇒ some model' ru
 J W1 Initial setup — POST /consent · POST /setup/complete · GE: BEFORE POST /consent: endpoint POST /consent → handler post_consent → calls _status → calls record_consent → fnwrites ConsentRecord → schema wires: [] (resp=ConsentStatusResponse names an UNDRAWN class; payload honest-empty) → POST /setup/complete: endpoint POST /set ||| AFTER POST /consent (after the config claim): endpoint POST /consent → handler post_consent → calls _status → calls record_consent → fnwrites ConsentRecord → + consumes ConsentAcceptRequest (api/consent.py:47 signature) → + resp ConsentStatusResponse (api/consent.py:45; hand-built in _status :27-35 — no serializes, honest) → POST /setup/complete: endpoint POST /setup/complete → handler setup_complete → calls _me_response_f ||| Δ The first step finally has a contract on the map (consent's request + response nodes appear); on every later step the response wire is a distinct kind from the body wire, and walking past MeResponse/S
 J W2 Look for recipes — GET /recipes/explore · GET /recipes · : BEFORE GET /recipes/explore: endpoint GET /recipes/explore → handler explore_recipes → calls build_candidate · build_cooked_ledger · filter_candidates · rerank_by_preferences · get_exploration_preferences (fnreads UserExplorationPreferences) · compute_recipe_availabi ||| AFTER GET /recipes/explore: endpoint GET /recipes/explore → handler explore_recipes → calls … (unchanged) → resp ExploreResponse (was touches; api/recipe_explore.py:56) → touches RecipeAvailability · touches RecipeListItem (helpers — stay touches) + nests ExploreResponse → RecipeListItem → + serializes RecipeListItem → Recipe (pair established at api/recipes.py:668 for-bound; the :159 site here is an unresolved loop — coun ||| Δ Each read step's response wire is now a distinct kind, and the detail page's three sub-DTOs (ingredients · steps · equipment) point at the three child tables the handler read — while the root DTO→Reci
 J W3 Filter recipes — GET /taxonomy · GET /repertorio/cupos · : BEFORE GET /taxonomy: endpoint GET /taxonomy → handler get_taxonomy [accessor] → fnreads TaxonomyDimension · TaxonomyValue → schema wires: [] (resp=TaxonomyTreeResponse UNDRAWN, unclaimed schemas/catalog.py) → GET /repertorio/cupos: handler get_cupos · calls list_cup ||| AFTER GET /taxonomy (after the config claim): endpoint GET /taxonomy → handler get_taxonomy → fnreads TaxonomyDimension · TaxonomyValue → + resp TaxonomyTreeResponse (api/catalog.py:55) → + nests TaxonomyTreeResponse → TaxonomyDimensionResponse → TaxonomyValueResponse (schemas/catalog.py:17-38) → + serializes TaxonomyDimensionResponse → TaxonomyDimension · TaxonomyValueResponse → TaxonomyValue (naming, from_attributes cata ||| Δ Taxonomy stops being a bare accessor with no contract — its tree DTO appears and drills two levels down to the two taxonomy tables; the other four steps only change wire kind (body vs response), no ne
 J W4 Cook — POST /cooking/sessions · PATCH …/stage · PATCH …/s: BEFORE POST /cooking/sessions: handler post_start_session · calls _attach_photos · concurrent_cook_cap_for_tier · start_session (fnwrites CookingSession) → seed_stage_schedule → _schedule_next (fnwrites CookingStageReminder) · touches CookingSessionResponse · touches ||| AFTER POST /cooking/sessions: … (calls unchanged) · consumes SessionCreateRequest · resp CookingSessionResponse · + serializes CookingSessionResponse → CookingSession (api/cooking.py:168 `cs = await start_session(...)` callee-return services/cooking.py:107) · nests CookingSessionResponse → CookingPhotoRef · + serializes CookingPhotoRef → CookingPhoto (naming, schemas/cooking.py:136; site :108 unresolved loop) → PATCH …/sta ||| Δ Four of five cooking steps close the loop on the map: the DTO the screen receives points back at the CookingSession/CookingStepProgress row the handler just wrote; completion only changes wire kind.
 J W5 Store ingredients — POST /pantry/items · POST /pantry/ite: BEFORE POST /pantry/items: handler create_pantry_item · calls add_pantry_item (fnwrites IngredientHistoryEvent · PantryItem) · touches PantryItemCreate · touches PantryItemResponse → POST /pantry/items/batch: handler create_pantry_items_batch · calls add_pantry_item  ||| AFTER POST /pantry/items: consumes PantryItemCreate · resp PantryItemResponse · + serializes PantryItemResponse → PantryItem (api/pantry.py:133 ← add_pantry_item callee-return) → POST /pantry/items/batch: consumes CreateBatchRequest · resp CreateBatchResponse · touches BatchItemResult · PantryItemCreate · PantryItemResponse (helpers) · + serializes PantryItemResponse → PantryItem (:254) → PATCH /pantry/items/{item_id}: con ||| Δ PantryItemResponse gains one wire to PantryItem that all four steps share; body and response wires separate on the three writes.
 J W6 Pantry locations — POST /pantry/locations · POST …/assign: BEFORE POST /pantry/locations: handler create_location · calls add_location (fnwrites Location) · touches LocationCreate · touches LocationResponse → POST …/assign-location: handler assign_location [accessor, fnreads Location] · calls edit_pantry_item (fnwrites Pantr ||| AFTER POST /pantry/locations: consumes LocationCreate · resp LocationResponse · + serializes LocationResponse → Location (api/pantry.py:439) → POST …/assign-location: consumes AssignLocationRequest · resp PantryItemResponse · touches PantryItemUpdate (helper) · + serializes PantryItemResponse → PantryItem (:349) → PATCH …/{location_id}: consumes LocationUpdate · resp LocationResponse · + serializes LocationResponse → Locat ||| Δ Three steps gain the response→table wire; the delete step is untouched.
 J W7 Shopping — POST /shopping/items · POST /shopping/from-pla: BEFORE POST /shopping/items: handler create_shopping_item · calls add_shopping_item (fnwrites ShoppingItem) · touches ShoppingItemCreate · touches ShoppingItemResponse → POST /shopping/from-planned-recipes: handler post_from_planned_recipes · calls generate_from_plan ||| AFTER POST /shopping/items: consumes ShoppingItemCreate · resp ShoppingItemResponse · + serializes ShoppingItemResponse → ShoppingItem (api/shopping.py:107) → POST /shopping/from-planned-recipes: resp FromPlannedRecipesResponse · touches ShoppingItemResponse (helper; nests) · + serializes ShoppingItemResponse → ShoppingItem (:69 for-bound) → GET /shopping: resp ShoppingDashboardResponse · touches ItemCandidate · ShoppingIt ||| Δ ShoppingItemResponse → ShoppingItem lands once and is reached from three of the four steps; confirm-bought only relabels its single wire.
 J W8 Plan — POST /recipes/{recipe_id}/plan · POST /shopping/fr: BEFORE POST …/plan: handler post_plan_recipe · calls load_user_allergens (fnreads UserDietaryProfile) · plan_recipe (fnwrites PlannedRecipe) · plan_into_cupo · touches PlanRecipeRequest · touches PlannedRecipeResponse → POST /shopping/from-planned-recipes: as W7 → DE ||| AFTER POST …/plan: consumes PlanRecipeRequest · resp PlannedRecipeResponse · + serializes PlannedRecipeResponse → PlannedRecipe (api/recipe_plan.py:84 ← plan_recipe callee-return; also a naming hit, schemas/recipe.py:380) → POST /shopping/from-planned-recipes: as W7 → DELETE …/plan: unchanged (204, no schema wires) ||| Δ The plan step's DTO points at the PlannedRecipe row it wrote; the unplan step is untouched.
 J J1 POST /setup/complete: BEFORE see W1 step 2: touches SetupCompleteRequest · touches MeResponse · the read-back DTO ends the trace ||| AFTER see W1 step 2: consumes SetupCompleteRequest · resp MeResponse · + 9 serializes from the MeResponse sub-blocks to User/Household/Membership + the 6 preference models (api/setup.py:64-73) ||| Δ The flagship journey's response leg closes onto the same 6 preference tables its upsert fan writes — write and read-back meet at the model column.
 J J3 POST /cooking/sessions/{session_id}/complete: BEFORE see W4 step 5: touches CompletionRequest · touches CompletionResponse ||| AFTER consumes CompletionRequest · resp CompletionResponse (both hand-built; no serializes) ||| Δ wire kinds only — the DTO is assembled by hand, so no table is reached through it (honest)
 J J4 POST /cooking/sessions: BEFORE see W4 step 1 ||| AFTER see W4 step 1: consumes SessionCreateRequest · resp CookingSessionResponse · + serializes CookingSessionResponse → CookingSession · + serializes CookingPhotoRef → CookingPhoto ||| Δ the session DTO points back at the CookingSession row start_session wrote
 J J5 POST /pantry/items: BEFORE see W5 step 1 ||| AFTER see W5 step 1: consumes PantryItemCreate · resp PantryItemResponse · + serializes PantryItemResponse → PantryItem ||| Δ the created-item DTO points at PantryItem; the ledger event stays write-only (correct — no DTO serializes it)
 J J6 POST /shopping/items/{item_id}/confirm-bought: BEFORE see W7 step 4: touches BoughtConfirmationResponse ||| AFTER resp BoughtConfirmationResponse (hand-built; no serializes) ||| Δ one wire relabelled touches → resp; nothing new reached
 J J7 POST /recipe-creation/manual: BEFORE endpoint POST /recipe-creation/manual → handler post_create_manual → calls create_recipe_manual → fnwrites Recipe · RecipeCreationRequest · RecipeIngredient · RecipeStep (commits) → touches CreationRequestResponse → touches ManualRecipeCreate ||| AFTER endpoint POST /recipe-creation/manual → handler post_create_manual → calls create_recipe_manual → fnwrites Recipe · RecipeCreationRequest · RecipeIngredient · RecipeStep → consumes ManualRecipeCreate (was touches) → resp CreationRequestResponse (was touches) → + serializes CreationRequestResponse → RecipeCreationRequest (api/recipe_creation.py:87 tuple-return position from create_recipe_manual) ||| Δ the envelope DTO points at the RecipeCreationRequest ledger row, not at the Recipe aggregate — the map now shows the response is the request record, which is what the source does
 J J8 POST /consent: BEFORE see W1 step 1: schema wires [] — resp names an undrawn class ||| AFTER see W1 step 1: + consumes ConsentAcceptRequest · + resp ConsentStatusResponse (config claim of schemas/consent.py; no serializes — hand-built) ||| Δ the compliance journey gets its contract nodes; config-only for this endpoint
 J K2 GET /recipes/explore (pure read): BEFORE see W2 step 1 ||| AFTER see W2 step 1: resp ExploreResponse · nests → RecipeListItem · + serializes RecipeListItem → Recipe ||| Δ the heaviest read composition finally terminates at Recipe through its list-item DTO instead of at ExploreResponse
 J K3 POST /recipe-creation/gustify (credit gate → LLM → persis: BEFORE endpoint POST /recipe-creation/gustify → handler post_create_gustify → calls _gustify_response · _raise_for_pipeline_error · generate_gustify_recipe (fnwrites RecipeCreationRequest) → reclaim_if_abandoned · enqueue_unknown_ingredients (fnwrites IngredientRecon ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify → calls … (unchanged) → consumes GustifyCreateRequest (was touches) → resp GustifyCreationResponse (was touches) → nests GustifyCreationResponse → CreationRequestResponse · GeneratedCandidateOut · GustifyUsage → + serializes CreationRequestResponse → RecipeCreationRequest (api/recipe_creation.py:125 in _gustify_response, outcome.request attr-field)  ||| Δ the AI path's response reaches the request ledger row through the nested CreationRequestResponse; the LLM output shape stays undrawn unless the agents file is claimed, and even then correctly points a
 J K5 POST /recipes/demand (own rate-limit clock): BEFORE endpoint POST /recipes/demand → handler post_recipe_demand [accessor] → fnwrites RecipeDemand (commits) → touches RecipeDemandCreate → touches RecipeDemandResponse ||| AFTER endpoint POST /recipes/demand → handler post_recipe_demand → fnwrites RecipeDemand → consumes RecipeDemandCreate (was touches) → resp RecipeDemandResponse (was touches) → + serializes RecipeDemandResponse → RecipeDemand (api/recipe_demand.py:67 — `demand = RecipeDemand(...)` ctor-bound: the ONE site today's symtab already resolves) ||| Δ the smallest journey becomes the template for the response leg: body wire in, ctor-bound row out, DTO → table in one hop with zero resolver extension
 UNCHANGED: J2 DELETE /me, K1 the auth/identity provisi, K4 the seed/ops lane (seed_c
 --- REFUTER: wrong checks:
   ✗ Station: REL2KIND falls to 'calls' for unknown rels (assemble.py:551); `consumes` needs no new station entry ⇒ The fallback is at assemble.py:555 `(typeof REL2KIND!=="undefined"&&REL2KIND[l.rel])||"calls"` (also :676-695), and REL2KIND (:15) ALREADY maps resp:'calls' and consumes:'calls' — only `serializes` is missing there. But adapter.js:10-15 RELCOL/LINKMETA have NO `consumes` or `nests` keys: LINKMETA fa
   ✗ `select`-bound locals resolve under TODAY's symtab (W1 AFTER: 'hfp select-bound :120-126 — resolves under TODAY's symtab'; fix: 'today's symtab (ctor / annot / select-bound)') ⇒ HEAD _a3_code.py:770-780: symtab binds only `Assign` whose value is a Call with a BARE name in m2t (`_call_bare(n.value.func)`) or an AnnAssign annotation in m2t. `hfp = (await session.execute(select(HouseholdFormatPreferences)…)).scalar_one()` (api/setup.py:113-126) has func Attribute `.scalar_one`
   CORR: Baseline drift, not a finder error: the finding was measured against HEAD@0245215; three commits landed during this session (a81fb42 'schema homing — a schema lives where its CONSUMER lives', cff92ce 'nested-only schema fold + COUNT badge', 4af0f4b pulse S12). At current HEAD MeResponse and SettingsResponse are homed INTO auth/settings, so the seam note 'xtouch entries carry a role so L1013-1025 m
   CORR: A seam now partially captures sub-gap (c) at the FUNCTION level: _a3_code.py schema-homing pass (~:596-705 @4af0f4b) emits fn→schema `fn_wires` with rels returns/takes/uses (gustify: 76 = 44 uses · 16 returns · 16 takes), _a3_levels carries them as `schema_edges`, layout.js:273 draws them (LINKMETA returns/takes/uses at adapter.js:32). Handlers are explicitly skipped (`if not ent or rec.get("handl
   CORR: 'today's symtab (ctor / annot / select-bound)' and 'hfp select-bound :120-126 — resolves under TODAY's symtab' → HEAD _a3_code.py:770-780 binds only bare-name ctor Assigns and AnnAssigns; `(await session.execute(select(X))).scalar_one()` is not bound. select-binding belongs to the extension, and the 6+6 GET /me / GET /settings sites resolve only after it ships.
   CORR: Station seam: 'REL2KIND += serializes:\'fk\', resp:\'calls\'' → assemble.py:15 already has resp:'calls' and consumes:'calls'; only serializes is new. 'consumes — no new station entry needed' → adapter.js:10-15 RELCOL and LINKMETA have no consumes/nests keys (LINKMETA falls back to {w:2,pv:1} at adapter.js:137); a colour+meta row is owed for 27 consumes wires to be legible. The REL2KIND fallback li
   CORR: Line offsets (same content, wrong numbers): _ORM_WRITE_M/_ORM_WRITE_CORE/_ORM_READ_CORE are _a3_code.py:871-874 not 757-760; `schemas = parse_schemas(...)` is :417 (:403 is the `_collect_entity_map` def); payload is _a3_graph.py:618-620 not 614-616; arch-graph touches pins are tests/arch-graph/run.sh:156-158 (and :100/:108) not 133-134; stats.consumes pins are :1037/:1044 not 1012/1019; resp-decla
   CORR: absent_per_file 'config.py: 1' is `Settings(BaseSettings)` (pydantic-settings), not a request/response schema — harmless but it pads 'absent outside schemas dir' to 5; the drawn ItemCandidate is schemas/shopping.py:34 (BaseModel), the dataclass is services/shopping.py:31 — the finder's 'validated from a services dataclass' is right.
   CORR: W3 PATCH /recipes/{recipe_id}/flags BEFORE: chain.py also prints `upsert_recipe_flags … ← reads ['Recipe','UserRecipeFlag']`; the finder quotes only the write.
   MISSING: The finder should re-baseline against HEAD 4af0f4b before the operator reads it: the committed map is now 72 endpoints / 125 touches / 2 cross touches with MeResponse+SettingsResponse homed intra, `nests` 54+15, and a `schema_homing` stats block — every 'today' count and the churn risk are stale by 
   MISSING: The homing pass's fn→schema `returns`/`takes`/`uses` feed (76 wires, handlers skipped) is the nearest existing seam to sub-gap (c) and is not mentioned; the AFTER for W1 should show `_build_me_response ⟶returns MeResponse` and `_subscription_block ⟶returns SubscriptionSummary` as ALREADY drawn (Func
   MISSING: The 13/93 'today's symtab' number is unverifiable: the resolver it describes (with select-binding) does not exist in _a3_code.py; the honest floor for today's symtab is ctor+annot only — report which of the 93 sites are bare-ctor/annot bound (recipe_demand.py-style) and leave the rest as extension-o
   MISSING: PATCH /recipes/{recipe_id}/flags: the finder asserts RecipeFlagsResponse is hand-built but the handler is not in api/recipes.py (where the finder implies); the claim was not evidenced with a file:line — cite the actual handler file.
   MISSING: RELCOL/LINKMETA lack `consumes`/`nests` entries (adapter.js:10-15) — the risk list pins REL2KIND and the card label but not the wire colour/weight; the same fix commit should add them or the 27 consumes wires draw in the default meta.

######## event-bus  [high]  tag=additive+change-in-place  REFUTED=False
TODAY: POST /cooking/sessions/{session_id}/complete (J3 = W4 step 5) draws handler post_complete → complete_session (writes CookingSession, DishHistoryEvent, IngredientHistoryEvent, PantryItem; commits) → clear_pending_cooking_timer_notifications (writes Notification): 3 fns on chain, behind 13 fns / depth 4, rollup 5 writes_to + 5 reads_from, all cooking-owned. The second write leg is absent everywhere: neither on_cooked_meal_created is a fn_node (levels.json fn_nodes 210, progression 12 — only the STARTUP backfill lane backfill_node_progress_on_startup → recompute_all_node_progress_for_user → _upsert_node_progress is drawn, via a cross-entity model use, not the event); recompute_skills_for_cook /
 WHY: The bus is a TYPE-KEYED registry, not a call: publish looks handlers up by type(event) and invokes a loop variable, so there is no static callee name for graft to resolve.  ev=apps/api/events/bus.py:72 handlers = self._handlers.get(type(event), []); apps/api/events/bus.py:78 await handler(session, event); apps/api/events/bus.py:106-107 bus.register_once(CookedMealCreated, update_skills_handler) / recompute_progression_handler
 WHY: graft resolves post_complete → EventBus.publish, but publish's only outgoing calls edge is name-bound to a TEST closure literally named `handler`; the walk dead-ends there (measured on graft/.graph/wiring.json: edges OUT of EventBus.publish = 1 → apps/api/tests/test_event_bus.py#test_bus_dispatches_registered_handler_C321.handler; edges IN to each on_cooked_meal_created = 1, relation `contains`, 0  ev=apps/api/api/cooking.py:417 await bus.publish(session, CookedMealCreated(; graft/.graph/wiring.json: cooking.py#post_complete → events/bus.py#EventBus.publish (calls, inferred); graft/.graph/wiring.json: bus.py#EventBus.publish → tests/test_event_bus.py#…C321.handler (calls, inferred) — the sole out
 WHY: Registration passes the handler as a VALUE (an argument to register_once), never calls it, so graft emits no edge into the handlers; the aliasing import inside register_handlers is the only place the binding is written.  ev=apps/api/events/bus.py:94-98 register_handlers: from services.progression import on_cooked_meal_created as recompute_progression_handler / from services.skills import on_cooked_meal_created as update_skills_handler; apps/api/main.py:100 register_handlers() (main.py claimed by no entity: 0 hits in ce
 WHY: Every generator seam walks graft `calls` relations ONLY: derive_functions keeps `relation == 'calls'`; _behind_context builds the adjacency from `calls`; derive_behind / derive_endpoint_access / derive_fn_roles / derive_distance_to_write all BFS that adjacency; _a3_levels steps 3/3b iterate graft `functions.calls`. A registry dispatch is no relation in the index, so nothing downstream can see it.  ev=templates/center/generators/_a3_graft.py:185 if e.get("relation") != "calls": continue; templates/center/generators/_a3_graft.py:213-219 _behind_context: fn_ids from _CALLABLE_KINDS, adj only from relation == "calls"; templates/center/generators/_a3_graft.py:291-358 derive_endpoint_access BFS over t
 WHY: The AST pass DOES see the publish call, but files it as a `queue` SINK category — the wrong kind for an in-process, same-session dispatch — and that tag renders nowhere in the station (attached to the fn node, no card section reads it).  ev=templates/center/generators/_a3_code.py:848-850 if attr in ("publish","emit","dispatch","enqueue") and (… "bus" in recv …): cats.add("queue"); templates/center/generators/_a3_code.py:1040-1042 fns[…]["sinks"] = _snk; gustify docs/site/center/archmap.json function_insight apps/api/api/cooking.py::pos
 WHY: The handlers' writes ARE detected by the ORM pass (SkillProgress r/w, NodeProgress r/w, both with flush→commits) — the data is there; only the hop that reaches them is missing, so function_insight scores both handlers usage 0 and the progression entity has zero write edges.  ev=apps/api/services/skills.py:180-183 row = SkillProgress(...); session.add(row); :190 await session.flush(); apps/api/services/progression.py:417 session.add(NodeProgress(...)); :433 await session.flush(); archmap.json function_insight services/skills.py::recompute_skills_for_cook → access ops [Skill
 SEAM _a3_code: NEW dispatch_map(repo) beside function_insight (reuse its parsed trees + _file_imports L691 for alias resolution): (a) REGISTRY pass — every ast.Call whose attr ∈ {register, register_once, subscribe, add_handler} with args (EventType Name, handler Name) → resolve the handler Name through the enclosing fn's local ImportFrom (bus.py:97-98 `import on_cooked_meal_created as update_skills_handler`) or module imports → `<file>#<fn>`; unresolved (bound method, lambda) → counted; (b) PUBLISH pass — ever
 SEAM _a3_graft: graft_arm(…, dispatches=) (L706): union the synthetic edges into _behind_context's adj (L213-219, both ends must be in fn_ids) so derive_behind (L256), derive_endpoint_access (L291), derive_fn_roles (L360), derive_distance_to_write (L402) all walk through them; derive_functions (L165-192) appends them to `calls` tagged rel:'dispatches', conf:'extracted' (existing entries carry no rel → default 'calls'); emit stats.dispatch {registered, published, resolved, unresolved} ONLY when non-empty.
 SEAM _a3_levels: L262 and L298: "rel": c.get("rel", "calls") so the drawn fn_edge carries `dispatches`; no other change — step 3 draws post_complete→handler (handler-rooted), §3b's gradient rule (L290-293: from a d2w-0 anchor ANY write-reaching callee) pulls recompute_skills_for_cook (d2w 0), recompute_node_progress_for_cook (d2w 1) and _upsert_node_progress (d2w 0). Measured pull from post_complete after the fold: 7 gradient edges, 5 new.
 SEAM _a3_graph: No code change: the endpoint's `access` (L620-634) now unions SkillProgress/NodeProgress/TreeNode; they are progression-owned, so they ride xaccess and resolve globally into cross_edges writes_to/reads_from (L1039-1077). Re-pin the example c4-graph.js (behind 13→36, access ops 10→15, cross_edges 168→173).
 SEAM station(adapter/layout/card): assemble.py:15 REL2KIND add dispatches:'dispatch' (today `REL2KIND[l.rel]||'calls'` at :551/:661 would silently paint it as a calls wire); assemble.py:507 CONN add a dispatch style (legend must render the real wire); assemble.py:259-262 legend row `dispatches <i>an event-bus dispatch: publisher → registered handler (in-process)</i>`, shown only when a link carries it; adapter.js:10-15 RELCOL + LINKMETA dispatches:{w:5,pv:1} (registry match is census, not inferred — today's default {w:2,pv:1} at 
 SKETCH: 1. AST: collect registrations (register/register_once/subscribe(EventCls, handler)) → resolve handler via local/module import aliases → {EventCls: [handler_id]}. | 2. AST: collect publishes (bus.publish(session, EventCls(...))) → {publisher_id: [EventCls]}. | 3. join on EventCls → dispatches edges [{s: publisher, t: handler, event, conf: extracted}]; unresolved counted, never guessed. | 4. graft_arm folds them into the shared calls adjacency → behind / endpoint_access / roles / d2w all see the second leg. | 5. derive_functions emits them in functions.calls with rel:'dispatches'. | 6. _a3_levels step 3 draws handler→dispatch targets; §3b gradient pulls the writers (d2w 1→0, 2→1→0). | 7. _a3_graph: the endpoint's access gains the foreign models → cross-entity writes_to/reads_from into progression. | 8. station: new `dispatches` wire kind in REL2KIND/LINKMETA/CONN/legend/relLabel.
 HONEST-EMPTY: A project with no registry/publish sites: dispatch_map returns {registry: [], publishes: [], dispatches: [], unresolved: 0}; graft_arm receives an empty list → adjacency, functions.calls, behind, endpoint_access, fn_roles, distance_to_write unchanged → c4-graph.json and levels.json byte-identical to
 SIZE: ~70 lines _a3_code (dispatch_map + alias resolve + sink demotion) · ~25 lines _a3_graft (param, adjacency union, rel tag, stats) · 2 lines _a3_levels · ~6 lines build_center_a3 (call + pass-through) · ~15 lines station (REL2KIND/LINKMETA/RELCOL/CONN/legend/relLabel/CONNICO); ≈120 lines code. Batteries to re-pin: tests/arch-graph (graft-absent byte-identical + new FIRE/SILENT fixture), tests/levels
 COUNTS: {"event_types": 1, "handlers_registered": 2, "publish_sites_non_test": 1, "registration_fns": 1, "register_once_calls": 2, "bus_sites_total_reconciling_the_row's_5": "1 type (types.py:11) + 2 handlers (skills.py:213, progression.py:490) + 1 publish (cooking.py:417) + 1 registration (bus.py:94, called main.py:100) = 5", "test_files_referencing_bus": 5, "test_fns_calling_publish_per_wiring": 4, "tests_in_test_event_bus.py": 3, "wiring_nodes": 19954, "wiring_edges": 51118, "wiring_calls_edges": 26088, "wiring_edges_out_of_EventBus.publish": 1, "wiring_calls_edges_into_each_handler": 0, "wiring_ed
 RISK: Alias resolution is the whole trick: the handlers are imported INSIDE register_handlers under different names (bus.py:97-98 `as recompute_progression_handler` / `as update_skills_handler`); the registry pass must read function-local ImportFrom nodes,
 RISK: Folding dispatch edges into the SHARED adjacency moves existing numbers on this endpoint (behind 13→36, access ops 10→15, cross_edges +5, fn_nodes +4, fn_edges +5) — every pin on the committed gustify example (tests/arch-graph example-payload asserts
 RISK: Trust split: a registry match is a CENSUS (type-keyed), unlike graft's inferred calls; if the new edges are emitted with graft's default conf the station paints them as an inferred floor (LINKMETA default pv:1 but weight 2) — carry conf:'extracted' a
 RISK: The 'queue' sink tag on post_complete (_a3_code.py:848-850) stays a lie once dispatch resolves in-process — the same pass should demote it, else the card (when sinks are ever rendered) says the write leaves the process while the map now draws it stay
 J W4 Cook [POST /cooking/sessions, PATCH …/stage, PATCH …/step: BEFORE step 1-4 unchanged (POST /cooking/sessions: post_start_session → start_session writes CookingSession → seed_stage_schedule → _schedule_next writes CookingStageReminder · PATCH stage: patch_stage → update_stage writes CookingSession · PATCH steps: patch_step_pr ||| AFTER steps 1-4 unchanged → step 5: endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete [caller · d2w=0 · cooking] (commit #1 cooking.py:410) → calls complete_session … (as before) → calls clear_pending_cooking_timer_notifications … (as before) → + dispatches on_cooked_meal_created (services/skills.py:213) [caller · d2w=1 · progression · extracted] — event CookedMealCreated (cooking.py:417, bus.py ||| Δ The Cook walk's last step grows a SECOND write leg the operator never saw: from post_complete two dispatch wires fan into progression (a different entity), each descending to a red access wire — Skill
 J J3 POST /cooking/sessions/{session_id}/complete: BEFORE endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete [caller · d2w=0 · cooking] → calls complete_session [accessor · d2w=0 · inferred] (commits) → fnwrites CookingSession, DishHistoryEvent, IngredientHistoryEvent, PantryItem → fnreads  ||| AFTER endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete [caller · d2w=0 · cooking] → calls complete_session … (unchanged) → calls clear_pending_cooking_timer_notifications … (unchanged) → + dispatches on_cooked_meal_created (skills.py:213) [caller · d2w=1 · progression · extracted] — CookedMealCreated → + calls recompute_skills_for_cook (skills.py:146) [accessor · d2w=0 · extracted] (flush) → +  ||| Δ J3 stops being 'state flip + 2 ledgers + leftovers' and becomes the domain-event completion the catalog describes: 2 dispatch wires, 4 new fn nodes, 5 new fn_edges, 5 new cross-entity rollup wires; th
 UNCHANGED: W1 Initial setup [POST /cons, W2 Look for recipes [GET /re, W3 Filter recipes [GET /taxo, W5 Store ingredients [POST /, W6 Pantry locations [POST /p, W7 Shopping [POST /shopping/, W8 Plan [POST /recipes/{reci, J1 POST /setup/complete, J2 DELETE /me, J4 POST /cooking/sessions, J5 POST /pantry/items, 
 --- REFUTER: wrong checks:
   ✗ The AST pass files bus.publish as a `queue` SINK at _a3_code.py:848-850, attaches it at :1040-1042, _file_imports at L691, function_insight claimed-trees rule at L1027-1031 ⇒ Content is right, line numbers are not: the queue rule is at _a3_code.py:985-987, sinks attached at :1174-1176, `def _file_imports` at :828 (ast.walk of the whole tree → sees function-local ImportFrom, keys by local name → bool), the mapped-trees-only rule at :1137-1141. The file is 2,759 lines at H
   ✗ tests/arch-graph/run.sh:341-350 pins the graft-absent byte-identical build ⇒ run.sh:341-350 is the graft-only-pair / stat-only-relations test; the graft-absent TOPOLOGY byte-identical regression is at run.sh:366-375 (`graft absent: {part} is byte-identical to the pre-graft build`); the behind honest-empty check is at :493-497
   CORR: _a3_code.py line cites are ~137 lines stale: 'queue' sink rule is at _a3_code.py:985-987 (claimed 848-850); sinks attached at :1174-1176 (claimed 1040-1042); `_file_imports` at :828 (claimed 691); function_insight's mapped-trees-only rule at :1137-1141 (claimed 1027-1031). File is 2,759 lines at HEAD 4af0f4b.
   CORR: tests/arch-graph/run.sh graft-absent byte-identical regression is at run.sh:366-375, not 341-350 (that range is the graft-only-pair test).
   CORR: Before absolutes (fn_nodes 210 / fn_edges 145 / cross_edges 168 / access_edges 206 / progression nests 25) were exact at HEAD 0245215 and the twin, but commits a81fb42/cff92ce/4af0f4b landed during verification (regen: 219/150/172/212, nests 7). J3's behind/ops/chain are unchanged; re-base the after absolutes to 223 fn_nodes / 155 fn_edges / 177 cross_edges / 217 access_edges.
   CORR: BEFORE hop nesting: clear_pending_cooking_timer_notifications is h2 UNDER complete_session (services/cooking.py:544 def, :597 call), not a sibling call of post_complete — the finder's flat list drops chain.py's indentation.
   CORR: `REL2KIND[l.rel]||'calls'` fallbacks sit at assemble.py:555 and :676-695, not :551/:661.
   CORR: _a3_graph access block is at :626-634 (claimed 620-634); the cross-entity ORM access loop starts at :1057 (claimed 1039-1077). graft_arm def at _a3_graft.py:703-705 (claimed 706).
   CORR: W1 delta wording: GET /me (apps/api/api/setup.py:48-110) reads the SUBSCRIPTION tier, not a progression/mastery tier; the 'unchanged' verdict stands.
   CORR: skills.py:180-183 is `row = SkillProgress(...)`; the `session.add(row)` is :184. progression.py `_nodes_for_codes` def is :355, the TreeNode select :364.
   CORR: derive_functions calls absolute: measured 2132→2134 (twin archmap entities), the finder's 2127→2129 differs by a constant 5; the +2 delta is what matters and holds.
   MISSING: L1 seam omitted: `derive_cross` (_a3_graft.py:447) counts cross-entity pairs straight from wiring edges by relation; the proposed fold (adjacency + functions.calls only) leaves the L1 cooking→progression edge at `kinds {calls: 6}` (HEAD example) — the finder must decide whether dispatches fold into 
   MISSING: `derive_fn_behind` (_a3_graft.py:279, per-function behind for the drawn post_complete fn node) shifts with the same adjacency union — not listed among the pins to re-base.
   MISSING: HEAD drift: the example was regenerated and committed during verification (a81fb42/cff92ce/4af0f4b); every absolute before/after in `counts` must be re-measured at 4af0f4b before any test pin is written (J3 deltas are stable).
   MISSING: GET /profile/summary — progression's single endpoint — is the one place that READS NodeProgress/SkillProgress today and still has 0 reads_from edges; it is not this class (no bus), but the finder's 'progression gains its first write edges' should note that the entity's read side stays undrawn for a 
   MISSING: The `conf` of the two handler→recompute edges is graft `extracted` (same-file calls) while the dispatch edges would be the new pass's census; the after sequence tags both 'extracted' — fine, but stats.dispatch should distinguish resolved-by-registry from graft-extracted so the trust split the finder

######## boot-writers  [high]  tag=config+additive  REFUTED=False
TODAY: 67 roots, all endpoint handlers (levels.json handler=True: 67 of 210 fn_nodes; 145 fn_edges; c4 stats.behind.scored 67; 206 access edges, every one endpoint-sourced). Of the boot lane: `lifespan` and 5 of the 6 `*_on_startup` wrappers are ABSENT; `backfill_node_progress_on_startup` is drawn as a dangling caller (deg 1, pulled by rule 2 as a cross-entity DishHistoryEvent user); `seed_safety_warnings` and `reconcile_recipe_restrictions` are drawn as ISLANDS (deg 0, accessor role, their access ops attached but no wire in). 143 of 210 fn nodes are unreachable from any handler; 33 have no fn_edge at all. chain.py "BOOT lifespan" → NO SUCH ENDPOINT. The seeded reference corpus (TaxonomyValue, Ingr
 WHY: The boot root file is unclaimed, so the AST pass never sees `lifespan`: function_insight parses ONLY files listed under an entity's code map (ENTITY_CODE loop), and its `handlers` set is route-decorated fns only (parse_endpoints accepts get/post/put/patch/delete). `lifespan` therefore has no function_insight entry and can never carry handler=True.  ev=gustify docs/site/center/center.config.json — main.py claimed: False (372 files claimed, none is apps/api/main.py); gustify apps/api/main.py:74-109 — @asynccontextmanager lifespan(); writer callees at :82 seed_catalogs_on_startup · :85 seed_recipe_sensory_on_startup · :90 seed_recipe_restrictions_on
 WHY: LEVELS has exactly one root class: `_handlers` are 'the ONLY roots of fn_edges'; a graft call whose source is not a handler is dropped, and the §3b write-path BFS seeds from the already-drawn set, so a chain that starts at a non-handler never enters.  ev=templates/center/generators/_a3_levels.py:227 — `_handlers: set[str] = set()  # the handler ids — the ONLY roots of fn_edges`; templates/center/generators/_a3_levels.py:257 — `if c["s"] not in _handlers: continue`; templates/center/generators/_a3_levels.py:282 — `_q = sorted(f for f in drawn_fn if f
 WHY: The graft arm DOES resolve the boot chain — gustify's wiring carries `apps/api/main.py#lifespan` with 11 resolved `calls` (all inferred) — but derive_functions homes fns by file→entity and drops any call with an unhomed end, so all 11 land in dropped.unmapped_file.  ev=gustify graft/.graph/wiring.json — nodes 'apps/api/main.py#lifespan'; 11 calls from lifespan → ensure_verifier_ready · assert_allergen_seed_present · assert_least_privilege_role · register_handlers · install_access_log_redaction · seed_catalogs_on_startup · backfill_node_progress_on_startup · seed_p
 WHY: derive_behind and derive_endpoint_access walk from `ent['endpoints']` records only — no endpoint record for the boot entry ⇒ no behind pill, no access rollup; and C4 `_l2` mints nodes only from code['endpoints'|'models'|'schemas'], so the node-kind vocabulary {endpoint, model, schema, external, web} has no process-start root.  ev=templates/center/generators/_a3_graft.py:267-275 (derive_behind) and :307-313 (derive_endpoint_access) — `for ep in ent.get('endpoints') or []: key = f'{f}#{fn}'`; templates/center/generators/_a3_graph.py:598-607 — `for ep in code.get('endpoints') or []: nid = f'endpoint:{method} {path}'`; committed
 WHY: Seeders inside CLAIMED files reach the map only through rule 2 (cross-entity model users) and land as ISLANDS or dangling callers; sandbox_seed.py, ownership.py, idempotency.py, reference/catalogs.py are unclaimed, so the sandbox lane is invisible even as islands.  ev=templates/center/generators/_a3_levels.py:236-249 — rule 2 `drawn_fn.setdefault(rfile + '#' + fn, using)  # a CROSS-entity model-user is drawn`; committed levels.json — seed_safety_warnings deg 0 (accessor, ops on IngredientWarning/ContextualWarningRule) · reconcile_recipe_restrictions deg 0 (access
 WHY: The row's 'SSE terminal persist' is MIS-FILED in this class: it HAS a request root and is already drawn (h2 under GET /recipe-creation/gustify/stream); its write is dropped by the ORM-access symtab because `req` is a PARAMETER, not a constructor-assigned local — the ORM-idioms class.  ev=gustify apps/api/services/ai_recipe_stream.py:259-261 — `async def _finalize_stream(session, req: RecipeCreationRequest, …)`; :289 `session.add(req)`; :302 `await session.commit()`; gustify apps/api/api/recipe_stream.py:114-158 — @router.get('/recipe-creation/gustify/stream') stream_gustify → stream
 WHY: The 'reminder poller' is not a server writer: no background task exists in the API (the only ensure_future is the SSE heartbeat); the web client polls GET /cooking/reminders/due every 60 s and GET /notifications every 60 s — reads the map already draws.  ev=gustify apps/api — grep create_task/BackgroundTasks/asyncio.sleep outside tests: only api/recipe_stream.py:75 `asyncio.ensure_future(it.__anext__())` (heartbeat); gustify apps/api/api/cooking.py:265-270 — GET /cooking/reminders/due 'the client polls this; SC-07'; gustify apps/web/src/features/cookin
 WHY: Migrations and CLI scripts write with no root class at all AND outside every code glob: alembic/ sits at the repo root (not under apps/api), and its 4 data migrations write by TABLE NAME through op.execute/op.bulk_insert, which the ORM pass (model-class binding only) cannot see; the 6 CLI writers live in unclaimed apps/api/scripts/.  ev=gustify alembic/versions — 64 files; DML in 0019_ingredient_restrictions.py:73 op.bulk_insert(table, rows) · 0033_location_type.py:42 UPDATE locations · 0049_time_code_extended_rebucket.py:57,66 UPDATE recipes · 0054_drop_mixing.py:52 UPDATE cooking_sessions; gustify apps/api/scripts — cleanup_e2e_r
 SEAM _a3_code: ADD parse_boot_roots(repo): find the api root's entry module (any claimed-api-dir sibling `main.py`/`app.py` whose `FastAPI(...)` call carries `lifespan=F`, or `@app.on_event('startup')` defs) → records in the parse_endpoints shape (_a3_code.py:143-152: method/path/fn/file/resp/middleware) as `{method:'BOOT', path:'lifespan', fn:'lifespan', file:'apps/api/main.py', resp:'—'}`; and let function_insight (L975-986) parse that file under layer 'boot' so `lifespan` gets an FI entry (flag `boot:True`,
 SEAM _a3_graft: _file2slug (L152-162) + derive_functions (L172-181): the driver passes a pseudo-entity `__boot__ = {endpoints:[boot_root], files:[['boot','apps/api/main.py',N]]}` inside `entities`, so `apps/api/main.py#lifespan` homes to `__boot__` and its 11 calls survive L189; derive_behind (L267-275) and derive_endpoint_access (L307-313) then BFS from the boot record like any handler — no code change in either (measured on the wiring: behind 135 fns / depth 7; 70 fns / depth 6 staying inside claimed files). 
 SEAM _a3_graph: build_c4_graph (L828; entity loop L992-999): when boot_roots is non-empty mint `l2['__boot__'] = _l2('__boot__', boot_code, …)` (L503) → node `endpoint:BOOT lifespan` with `fn`, `behind`, `access` and its writes_to/reads_from rollup (foreign models resolve through xaccess, L622-636) + an L1 node `{id:'__boot__', kind:'boot', label:'boot'}` positioned like _L1_UNCLAIMED (L66, L724, L789); colour `_gcolors['__boot__']` beside `__unclaimed__` (build_center_a3.py:1994). Note _a3_graph.py is already 
 SEAM _a3_levels: rule 0 before rule 1 (L228-233): `for r in amap.get('boot_roots') or []: hid=r['file']+'#'+r['fn']; drawn_fn.setdefault(hid,'__boot__'); _handlers.add(hid)` — L257 then admits lifespan's calls, and the §3b d2w descent (L277-306) pulls the seeders in (every `*_on_startup` is a write-anchor: `flush` ∈ _ORM_COMMIT, _a3_code.py:738). Add `__boot__` to lv['entities'] (L199) so homing resolves; `_use_case_key('lifespan',1)` → 'lifespan' needs nothing (L120-124). hidden_fns per entity shrink (L308-318)
 SEAM center.config: NOT required for the boot root itself. OPTIONAL widening (the unclaimed-files class): claim apps/api/services/sandbox_seed.py (2 fns) + services/ownership.py (4) + services/idempotency.py (6) + reference/catalogs.py (15) — without them `seed_sandbox_on_startup` is a behind-count stub (its call target is unhomed → dropped at _a3_graft.py:189) and the sandbox's User/Household/IdempotencyKey writes stay off the chain.
 SEAM station(adapter/layout/card): adapter.js:8 `METHOD` gains `BOOT:'#8a8f98'` (muted, the ops hue); adapter.js:46 `_methOf` regex → `^(GET|POST|PUT|PATCH|DELETE|BOOT)\b`. layout.js:818-839 `_bkCollect` already emits one `bk:` chain per endpoint node with a `handler` wire — the boot node rides it with badge 'BOOT' for free; add the boot node LAST in `_fnAssignSub` seeding (layout.js:146) so a shared service fn keeps its HTTP handler's data cluster. assemble.py NEW_TYPES (~L636): one legend note row 'BOOT = process-start root (li
 SEAM new module: OPTIONAL follow-on roots in the same record shape (defer unless asked): `MIGRATE <rev>` from alembic/versions DML (op.execute INSERT/UPDATE/DELETE, op.bulk_insert(table)) mapped table→model through the inverse of _model_table_map (_a3_code.py:741) — 4 roots on gustify; `CLI <script>` from apps/api/scripts/*.py `main()` — 6 roots. Each ~40 lines + a battery case.
 SKETCH: 1. parse_boot_roots: find `FastAPI(..., lifespan=F)` / `@app.on_event('startup')` in the api entry module → boot records {BOOT, lifespan, fn, file}; [] when none. | 2. Driver injects pseudo-entity `__boot__` = {endpoints:[boot], files:[[boot, file]]} into the entities the graft arm, C4 and LEVELS see. | 3. graft: `main.py#lifespan` homes to `__boot__` → its 11 calls survive; derive_behind / derive_endpoint_access BFS from it unchanged (135 fns / depth 7 measured). | 4. C4: `_l2('__boot__')` mints `endpoint:BOOT lifespan` with behind + writes_to/reads_from rollup; L1 `__boot__` node beside `__unclaimed__`. | 5. LEVELS: rule 0 adds the boot fn to `_handlers` → handler-rooted calls + §3b d2w descent draw the seeder chains (+~59 fn_nodes, ≤98 fn_edges inside claimed files). | 6. Station: METHOD.BOOT + _methOf regex; `_bkCollect` yields the 'BOOT lifespan' backend chain; one legend note. | 7. Config (opt
 HONEST-EMPTY: A project with no `lifespan=` / `@app.on_event('startup')` hook → parse_boot_roots returns [] → no `__boot__` pseudo-entity is injected → fn_slug, calls, behind, endpoint_access, l1/l2 nodes+edges, layout, levels.entities/fn_nodes/fn_edges/hidden_fns are byte-identical to today's build; the only new
 SIZE: ~120 code lines: _a3_code ~45 · _a3_graft ~10 (stats.boot) · _a3_graph ~30 (over-budget file, state it) · _a3_levels ~10 · build_center_a3 ~15 · station ~8 (adapter 2, assemble legend note ~4, seeding order 2). Batteries ~60 lines: tests/arch-graph +3 checks (FIRE on a fixture with `lifespan=`, honest-empty byte-identical, determinism) · tests/levels +2 (rule 0 roots a non-handler; no boot_roots →
 NEW_KINDS: endpoint METHOD value 'BOOT' — adapter.js:8 METHOD colour + adapter.js:46 _methOf regex + the method badge-key popup; node kind stays `endpoint` so NODEDEF/KINDS/_GRPKINDS/LINKMETA/REL2KIND need nothi || L1 bucket '__boot__' (l1 node kind 'boot', sits beside '__unclaimed__'; colour in build_center_a3 _gcolors) — no new wire kind: handler · calls · fnwrites/fnreads · writes_to/reads_from are all reused
 COUNTS: {"lifespan_lines": "apps/api/main.py:75-109", "lifespan_writer_callees": 6, "lifespan_callees_total_in_source": 11, "graft_calls_from_lifespan_resolved": 11, "graft_calls_from_main_py": 12, "graft_confidence": "all inferred", "lifespan_behind_graft_bfs_fns": 135, "lifespan_behind_depth": 7, "boot_reach_inside_claimed_files_fns": 70, "boot_reach_inside_claimed_depth": 6, "boot_reach_fns_not_drawn_today": 59, "boot_reach_calls_edges_upper_bound_new_fn_edges": 98, "ast_boot_reachable_app_fns": 139, "ast_boot_reachable_writer_fns": 34, "row_claimed_writers": 38, "writer_fns_visible_to_orm_pass_app
 RISK: Two-root nodes: complete_setup / start_session / complete_session / create_recipe_manual / record_consent gain a BOOT caller; the station's _fnAssignSub (layout.js:146) seeds a fn's data cluster from 'the' handler endpoint first-wins — process the __
 RISK: Floor quality: every boot wire is graft-inferred (0 extracted); graft also mis-resolves `.commit` on seed_sandbox_on_startup to a DocsRenderer-*.js node — the noise filter drops it, but a `.commit`/`.date` method call colliding with an in-set name wo
 RISK: Residual invisibility inside the drawn chain: _upsert_by_code's parameterised `model(...)` (catalog_seed.py:48-58) hides 5 reference-model writes (TaxonomyDimension · MeasurementUnit · IngredientCategory · Restriction · DietPreference) and the sandbo
 RISK: Environment gating: every seeder returns early on LOCAL/SQLite (catalog_seed.py:360-363 etc.) and the sandbox only runs on STAGING (sandbox_seed.py:372-377) — the BOOT chain draws DEPLOY behaviour; without a gate tag an operator may read the sandbox 
 J K4 the seed/ops lane (seed_catalogs_on_startup) — BOOT lifes: BEFORE (nothing) — chain.py 'BOOT lifespan' → NO SUCH ENDPOINT; lifespan absent from levels.json; seed_safety_warnings + reconcile_recipe_restrictions drawn as deg-0 islands; backfill_node_progress_on_startup a deg-1 dangling caller ||| AFTER + boot BOOT lifespan (l2 endpoint node under __boot__; apps/api/main.py:75) [new METHOD value, node kind endpoint] → + handler lifespan [handler wire; main.py:75-109; behind 135 fns / depth 7] → + calls seed_catalogs_on_startup [main.py:82 → catalog_seed.py:352; commits (flush :375)] → + calls seed_all_catalogs [catalog_seed.py:329] → + calls seed_taxonomy_dimensions → _upsert_by_code [catalog_seed.py:61 → :48; write ||| Δ the operator gains a 7-deep BOOT walk in the journeys backend tab showing where the reference corpus (taxonomy · units · ingredients · restrictions · warnings · progression trees · catalog recipes) is
 UNCHANGED: W1 Initial setup — POST /con, W2 Look for recipes — GET /r, W3 Filter recipes — GET /tax, W4 Cook — POST /cooking/sess, W5 Store ingredients — POST , W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /recipes/{rec, J1 POST /setup/complete, J2 DELETE /me, J3 POST /cooking/ses
 --- REFUTER: wrong checks:
   ✗ today_draws: 67 handler roots of 210 fn_nodes, 145 fn_edges, behind.scored 67, 206 access edges, unmapped_file 9631, noise 30291 ⇒ Those are the HEAD~3 (8f2f2c5) numbers. The committed example at HEAD, HEAD~1, HEAD~2 AND gustify's live docs/site/center/levels.json all read fn_nodes 219 / fn_edges 150 / handlers 72; c4 stats.access_edges 212, behind.scored 72, dropped.unmapped_file 9679, noise 30298; chain.py --all prints 72 lab
   ✗ 143 of 210 fn nodes are unreachable from any handler; 33 have no fn_edge ⇒ BFS over fn_edges (keys s/t) from handler=True roots: HEAD~3 map → 31 unreachable / 33 isolated; HEAD map → 30 unreachable / 32 isolated. 143 = 210 − 67 exactly, i.e. a BFS that traversed no edges (only the roots counted as reached)
   ✗ writer_fns_visible_to_orm_pass_approx 28; distinct models named directly on boot paths 19 (24 incl. param-bound); BOOT writes_to rollup ≈ 20 with the sandbox lane off the chain ⇒ Ran the generator's own _orm_access over every fn on the 135-fn graft reach with a global m2t: 28 writer fns (exact match) but 34 distinct written models, 32 fns with commit/flush. derive_endpoint_access (_a3_graft.py:318-345) BFSes `adj` from _behind_context — ALL callable nodes, no homing filter —
   ✗ _a3_graph.py is 840 lines vs the 800 budget ⇒ wc -l templates/center/generators/_a3_graph.py = 1191 (clean tree at a81fb42); 840 is CLAUDE.md's stale figure. The +~30 lands on 1191
   ✗ Risk: layout.js _fnAssignSub seeds a fn's data cluster first-wins from 'the' handler endpoint, so shared service fns may re-home to __boot__ unless __boot__ is processed LAST ⇒ layout.js:144-147: `if(!s||!t||s.ent!==t.ent) return; // INTRA-entity only` guards BOTH the handler seed and the fn→fn adj; `cur[t.id]=s.sub` is plain assignment = LAST-wins over link order. A BOOT endpoint homed to __boot__ can neither seed complete_setup (auth) nor propagate to it; and under last-
   ✗ Honest-empty: no lifespan hook → byte-identical build, the only new bytes are stats.boot ⇒ The same fix row says `Emit amap['boot_roots'] (list, [] when none)` → archmap.json gains a key too. Also the committed gustify c4 at HEAD has NO __unclaimed__ L1 node (l1 kinds = {entity: 8}), so 'positioned beside __unclaimed__' has no neighbour on this map; _a3_graph._layout special-cases only ki
   CORR: today_draws counts (67 roots / 210 fn_nodes / 145 fn_edges / behind.scored 67 / 206 access edges / unmapped_file 9631 / noise 30291) → HEAD~3's map. At HEAD (templates/center/shell/example/codebase-graph-station/levels.json, byte-equal to gustify's live docs/site/center/levels.json): 72 handlers / 219 fn_nodes / 150 fn_edges; c4-graph.js stats: access_edges 212, behind.scored 72, dropped.unmapped_
   CORR: '143 of 210 fn nodes unreachable from any handler; 33 isolated' → 31 unreachable / 33 isolated on HEAD~3, 30 / 32 on HEAD (BFS over fn_edges s→t from handler=True roots). 143 = 210−67 is the roots-only count of a BFS that followed no edges
   CORR: _a3_code.py cites: ENTITY_CODE loop + handlers.add is :1115-1124 (not :975-986, the sinks pass); symtab :907-918 + _arg_model :920-925 (not :771-788, model similarity); Core write detection :948-950 (not :796-800); _ORM_COMMIT :875 (not :738); _model_table_map :878 (not :741). :168-169 method filter and :143-152 parse_endpoints shape are correct
   CORR: _a3_levels.py cites are 6 lines stale: _handlers comment :233 (not 227), `if c['s'] not in _handlers` :263 (not 257), `_q = sorted(...)` :289 (not 282), rule 2 :245-255, rule 1 :235-239, §3b :283-306, hidden_fns :309-324; _a3_graph.build_c4_graph :832 (not 828), unclaimed L1 mint :1096-1103; build_center_a3 _gcolors :1997-1999 (not 1994)
   CORR: '_a3_graph.py is already 840 lines against the 800 budget' → wc -l = 1191 at a81fb42 (840 is CLAUDE.md's stale number)
   CORR: BOOT node rollup '≈ 20 writes_to … the sandbox's writes stay off the chain' → derive_endpoint_access (_a3_graft.py:318-345) BFSes ALL callable nodes (no homing) and joins faccess per reached fn, so it passes through unclaimed sandbox_seed.py and unions complete_setup's _upsert_* (setup.py), start_session/complete_session (cooking.py), create_recipe_manual, record_consent, _schedule_next, clear_pen
   CORR: 'seed_sandbox_on_startup [UNCLAIMED file: a behind-count stub]' → no node at all is minted: the lifespan→seed_sandbox_on_startup call is dropped at _a3_graft.py:188 (target not in fn_slug), so it never enters drawn_fn; it only contributes to the `behind.fns` integer
   CORR: Risk 1 (layout.js _fnAssignSub 'first-wins … process __boot__ LAST') → layout.js:144-147 is INTRA-entity only (`s.ent!==t.ent → return`) for both the handler seed and the fn→fn adjacency, and `cur[t.id]=s.sub` is last-wins; a __boot__-homed root cannot reach auth/cooking fns' clusters at all, so the risk is moot and the ordering advice inverted
   CORR: Honest-empty 'only new bytes are stats.boot' → `amap['boot_roots'] = []` also adds a key to archmap.json; and HEAD's committed gustify l1 has no __unclaimed__ node ({entity: 8}) so 'beside __unclaimed__' has no neighbour; _a3_graph._layout (:727-728, :792-793) handles only kind 'unclaimed' → a 'boot' L1 kind needs its own placement branch
   CORR: Sketch step 2 'inject __boot__ into the entities the graft arm, C4 and LEVELS see' → _a3_graph._l1 (:137-148) mints an 'entity'-kind ring node for every slug in `entities`; inject ONLY into graft_arm(REPO_ROOT, entities, …) at build_center_a3.py:2004-2005 and mint the C4/levels boot node explicitly (as the per-seam rows say), or __boot__ becomes a 9th entity in the ring, entity_shape S9, cc-entity
   CORR: Minor line drifts: recipe_seed.py create_missing_recipes :267 (not 271), sync_changed_recipes :288 (not 295); catalog_seed.py TaxonomyValue add :138 (not 139); progression.py DishHistoryEvent select :475 (not 472); alembic 0049 op.execute :56 (not 57), 0054 :51 (not 52); claimed files 375 (not 372); layout.js _bkCollect starts :822 (not 818); _jrnGroupsHTML (:920) sorts by entity-span count then c
   MISSING: The WebSocket twin of the SSE stream: `@router.websocket('/recipe-creation/gustify/ws')` (api/recipe_stream.py:161) is a REQUEST root parse_endpoints drops at :168-169 (websocket ∉ get/post/put/patch/delete), so stream_gustify_ws → stream_gustify_events → _finalize_stream's persist has NO drawn root
   MISSING: The e2e_seed HTTP seed routes (api/e2e_seed.py:63 and :115, included at main.py:174-177 behind settings.seed_controls_enabled) — request-rooted seeders in an unclaimed file. They belong in the seed/ops lane's journey list with BEFORE (absent) → AFTER (config claim, NOT a BOOT root); the finder menti
   MISSING: MIGRATE and CLI got no BEFORE→AFTER sequence and no table→model mapping although the row reads 'migrations that write … which models they write': locations→Location (0033), recipes→Recipe (0049 ×2), cooking_sessions→CookingSession (0054), ingredient_restrictions table→IngredientRestriction (0019 bul
   MISSING: The rollup-vs-walk split on the BOOT node (derive_endpoint_access walks through unclaimed nodes; derive_functions does not) is a journey-visible artefact the K4 AFTER should state: the node's writes_to will name ~29 models while the drawn chain ends at seed_recipe_restrictions/progression — an opera
   MISSING: seed_sandbox_on_startup's gate is three-fold (sandbox_seed.py:372 STAGING, :374 sandbox_user_uid set, :376 non-sqlite) and every other seeder gates on LOCAL + sqlite (catalog_seed.py:360-363, restriction_seed.py:133-136…): the proposed `gate:'deploy-only'` floor should read all three predicates, not

######## middleware  [high]  tag=config+additive+change-in-place  REFUTED=False
TODAY: 67/67 endpoint nodes carry a `middleware` name list (stats: middleware_endpoints 67 · gate_endpoints 67): get_session on 67, get_auth_context on 66, get_auth_context_from_query on 1 (the SSE stream), get_settings on 12 — rendered as the card's Guards rows + 'Gated by get_auth_context' in the step note, 0 hops. Function nodes from the dependency path: 0 of 210 fn_nodes come from auth/context.py (only auth/verifier.py#delete_identity is drawn), 0 from services/ownership.py, 0 from middleware/*.py; get_auth_context · build_auth_context · resolve_or_create_user · load_household_context · AuthContext.require_household · RateLimitMiddleware.dispatch · SlidingWindowLimiter.allow · IdempotencyMiddle
 WHY: The gate detector records dependency NAMES, never a function id, so a Depends() target can never become a node or a hop: `_endpoint_middleware` returns [{name, via, gate}] from `ast.unparse(call.args[0])` with no import resolution; `_a3_graph` copies the list onto the endpoint node and counts it, nothing else.  ev=/home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:892-908 (_depends_target → unparsed expression, 'HONEST FLOOR: an ALIASED import … is not resolved — that needs per-file import tracking'); /home/khujta/projects/gabe_lens/templates/center/generators/_a3_code.py:934-965 (_endpoi
 WHY: Depends() is not a call, so graft has ZERO `calls` edges from any handler into get_auth_context (measured: 0 calls edges from apps/api/api/* → auth/context.py#get_auth_context; the only inbound edge is 1 `contains`). Every fn/rollup derivation is a BFS over `calls` rooted at the HANDLER, so the dependency chain is unreachable by construction.  ev=/home/khujta/projects/apps/gustify/graft/.graph/wiring.json (measured: relations = calls 26088 · contains 16553 · imports 8146 · extends 330; 0 calls into get_auth_context from api/); /home/khujta/projects/gabe_lens/templates/center/generators/_a3_levels.py:227 ('_handlers … the ONLY roots of fn_edg
 WHY: The gate the context feeds — `AuthContext.require_household` (409 on 47 of the 67 handlers; 55 graft call edges into it) — is a METHOD, and derive_functions drops every non-`function` node, so it can never be a drawn fn node even though the behind/d2w substrate includes methods.  ev=/home/khujta/projects/apps/gustify/apps/api/auth/context.py:52-56 (require_household → HTTP 409 'setup required'); /home/khujta/projects/gabe_lens/templates/center/generators/_a3_graft.py:175 (`if n.get("kind") != "function": continue`) vs :204 (_CALLABLE_KINDS = ('function','method')) and :212-213 
 WHY: The writer on the path lives in an UNCLAIMED file: `resolve_or_create_user` (User select + User(...) + session.add + flush) is in services/ownership.py, which no entity claims, and function_insight only walks ENTITY_CODE files — so even `build_auth_context` (claimed, auth·services) carries {commits:True, ops:[]} in archmap: a commit with no write op.  ev=/home/khujta/projects/apps/gustify/apps/api/services/ownership.py:30-51 (select(User) :33-39 · User(...) :43 · session.add :49 · flush :50) and :68-86 (load_household_context: Membership :78 · Household :83 · Location via get_principal_location :58-65); /home/khujta/projects/apps/gustify/apps/api/au
 WHY: App-level middleware is not parsed anywhere: no generator reads `add_middleware(` (grep across templates/center/generators: 0 hits), so RateLimitMiddleware (23 sensitive + 67 global gated endpoints), IdempotencyMiddleware (stamps every mutating request; read by 5 handlers) and CORSMiddleware have no node, no edge, no stats key. The only middleware-shaped floor is the per-handler Depends scan, whic  ev=/home/khujta/projects/apps/gustify/apps/api/main.py:124 (add_middleware(IdempotencyMiddleware)) · :127 (add_middleware(RateLimitMiddleware, settings=settings)) · :132-138 (CORSMiddleware) · :119-122 (order outer→inner: CORS → rate-limit → idempotency); /home/khujta/projects/apps/gustify/apps/api/mid
 WHY: The station can only show what it is handed: the endpoint node carries `middleware` to a Guards card section and a 'Gated by …' step-note string; there is no wire kind for a dependency, no node kind for a middleware, and fn links are built solely from levels fn_edges + access ops.  ev=/home/khujta/projects/gabe_lens/docs/design/codebase-graph-consolidation/universe-build/parts/adapter.js:75 (middleware:p.middleware → card); /home/khujta/projects/gabe_lens/docs/design/codebase-graph-consolidation/universe-build/parts/card.js:130-137 (guardsSec: rows of names, 'A FLOOR … gate is a 
 SEAM _a3_code: _endpoint_middleware (L934-965) + _depends_target (L892-908): add a per-file import map (the same map the L897 docstring already says aliased imports need) and resolve each Depends/Security target to a graft id `<file>#<fn>` → each entry gains `fn:` (name/via/gate unchanged; unresolvable → no `fn` key, today's row). NEW parse_app_middleware(repo): find the file that constructs `FastAPI(` and each `app.add_middleware(Cls, …)` call → [{cls, file, line, order}] + a literal-constant floor per middle
 SEAM _a3_graft: NEW derive_depends(wiring, entities): for every endpoint whose middleware entry carries a resolved `fn` present in _behind_context's fn_ids, emit {s: handler id, t: dep id, rel:'depends', conf:'extracted'} (a signature fact, not an inferred call). derive_endpoint_access (L305-323) and derive_behind (L268-278): seed the BFS frontier with the endpoint's dep ids as well as the handler (so build_auth_context's commit and, once ownership.py is claimed, resolve_or_create_user's User write reach the ro
 SEAM _a3_levels: After the handler-rooted pass (L251-262): add each endpoint's dep ids to drawn_fn and push the `depends` edges into _fedges (rel:'depends'); the §3b d2w BFS (L277-304) then walks build_auth_context (d2w 0: commits) → resolve_or_create_user on its own. Emit an `app_middleware` block in levels (nodes + which endpoints each gates, using the literal floor: path-prefix match for RateLimit-sensitive, method set for Idempotency, all for CORS/global).
 SEAM _a3_graph: build_c4_graph: (a) endpoint→dep-fn `depends` edges are function-level, so they ride levels like calls; on the C4 node keep `middleware[].fn` so a Guards row can link to the node. (b) NEW L2 node kind `middleware` (one per add_middleware, homed __unclaimed__ or the entity that claims its file) + cross_edges kind:'gated_by' endpoint→middleware carrying {order, scope:'sensitive'|'global'|'mutating'|'all'} + stats.app_middleware {count, gated_endpoints}. Rollup edges that arrive only through a dep 
 SEAM center.config: gustify: claim apps/api/services/ownership.py under auth.services (the User get-or-create writer); claim apps/api/middleware/rate_limit.py + apps/api/middleware/idempotency.py (auth.services or a `platform` entity) so derive_functions can home their fns and the 5 get_idempotency_key call edges resolve. Pure config; without it the fix still draws get_auth_context → build_auth_context (commit) but the User write stays invisible.
 SEAM station(adapter/layout/card): assemble.py:15 REL2KIND += depends:'gate', gated_by:'gate'; CONN (assemble.py:507-510) += gate:{…} (dashed, keyed colour) + a legend Connectors row drawn with the real wire; KINDS (spike-base.html:201 / adapter.js) += middleware node kind with glyph; layout.js:250-269 _buildFnData pushes levels `depends` edges as links; card.js:130-137 Guards rows become click-to-select when the entry has `fn`; layout.js:956 step note gains a Hop 0.5 'Gated by … (X fns behind, writes User)'; the walk (_bkCollect
 SKETCH: 1. parse: per handler, resolve Depends(x) → file#fn via the file's imports; per app file, list add_middleware(Cls) + each Cls file's literal prefix/method constants. | 2. graft: depends edges handler→dep (extracted); seed endpoint_access/behind BFS with dep ids; admit methods into fn_slug. | 3. levels: dep fns join drawn_fn; depends edges join fn_edges; §3b d2w BFS carries on from build_auth_context (commit anchor) to the User writer. | 4. c4: middleware nodes + gated_by cross_edges (scope from the literal floor: sensitive prefix · mutating method · all); rollup ops reached via a dep tagged via:'depends'. | 5. station: new `gate` wire kind (depends + gated_by), `middleware` node kind, Guards rows link to nodes, walk starts at the gate hop. | 6. config: claim ownership.py + middleware/*.py so the writer and the limiter are homed. | 7. honest-empty: no Depends → no depends edges; no add_middleware → n
 HONEST-EMPTY: A project with no Depends()/Security() on any handler and no add_middleware() call emits c4-graph.json and levels.json byte-identical to today's build: no `middleware` field (already absent today), no `depends`/`gated_by` edges, no `middleware` nodes, no `stats.app_middleware` key, no `via` tags. A 
 SIZE: ~45 lines _a3_code (import map + fn resolution + parse_app_middleware ~60) · ~55 _a3_graft (derive_depends + two BFS seeds + L175) · ~30 _a3_levels · ~45 _a3_graph (node kind + gated_by + via tag + stats) · ~50 station (REL2KIND/CONN/KINDS/legend/card/step-note) · 3 config lines. Re-pin: tests/arch-graph (C4 middleware checks L437-447 + new depends/app_middleware cases), tests/levels (7 fn_edges p
 NEW_KINDS: wire kind `depends` (handler → dependency fn; extracted, not inferred) || wire kind `gated_by` (endpoint → app-level middleware; scope sensitive|global|mutating|all) || node kind `middleware` (one per add_middleware site) || station CONN/legend bucket `gate` for both wires; KINDS.middleware glyph || endpoint middleware entry field `fn` (resolved id) and rollup edge field `via:'depends'`
 COUNTS: {"add_middleware_sites": 3, "middleware_order_outer_to_inner": "CORS \u2192 RateLimit \u2192 Idempotency (main.py:119-122)", "rate_limit_sensitive_prefixes": 7, "endpoints_total_on_map": 67, "endpoints_under_sensitive_prefix": 23, "endpoints_under_global_window": 67, "rate_limit_global_per_min_default": 120, "rate_limit_sensitive_per_min_default": 20, "idempotency_stamp_methods": 4, "handlers_reading_get_idempotency_key": 5, "Depends_get_auth_context_sites_all_api_files": 78, "Depends_get_session_sites": 82, "Depends_get_settings_sites": 13, "Depends_other": "_resolve_verifier 2 \u00b7 _requir
 RISK: Fan-in hub: AuthContext.require_household would take 47 `calls` wires (55 in graft) into one method node — fold it into the endpoint's gate badge or cap hub wires, or the 3D layout drowns in one star.
 RISK: Uniform noise: 66 endpoints gaining fnreads/fnwrites User at Hop 0.5 makes the User model the map's hottest node and every rollup grow by 4 models; the `via:'depends'` tag + a station toggle (default dim) keeps the handler-rooted rollup readable — de
 RISK: Admitting methods in derive_functions (L175) is a one-line change with index-wide effect (2213 method nodes in gustify's index; only handler-rooted/write-path ones draw, but fn_slug/calls counts and the levels pins move) — the change belongs in its o
 RISK: The app-middleware literal floor (SENSITIVE_PREFIXES / _MUTATING_METHODS as tuple/frozenset constants) is gustify-shaped; a middleware that computes its scope in code gets scope 'all' — say so in stats.app_middleware, never guess sensitive edges.
 J K1 the auth/identity provisioning commit on every request (D: BEFORE (no journey exists) endpoint <any of 67> — Guards row 'get_auth_context · param-dep · gate' (name only) → handler <fn> (chain.py starts here) ||| AFTER + gated_by CORSMiddleware [all] (main.py:132) → + gated_by RateLimitMiddleware [global 120/min; sensitive 20/min on 23] (main.py:127 · rate_limit.py:107-123) → + gated_by IdempotencyMiddleware [mutating: stamp request.state.idempotency_key] (main.py:124 · idempotency.py:23-25) → + depends get_auth_context (auth/context.py:90) → + depends bearer_scheme · get_session · _resolve_verifier [nested, param-dep of the dep] ( ||| Δ The walk gains a Hop 0.5 before every handler: a gate node (get_auth_context) that pulls 3 fns, reads 4 models and holds a COMMIT on User — the User model turns from 1 red wire (DELETE /me) into the m
 J W1 Initial setup [POST /consent, POST /setup/complete, GET /: BEFORE endpoint POST /consent → handler post_consent → calls _status · record_consent → fnwrites ConsentRecord (commits) → writes_to ConsentRecord → endpoint POST /setup/complete → handler setup_complete → calls _me_response_from_result · complete_setup → calls _disc ||| AFTER endpoint POST /consent → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (consent.py:46) → + calls build_auth_context → + calls TokenVerifier.verify → + calls resolve_or_create_user → + fnreads User → + fnwrites User (commits, context.py:79) → + calls load_household_context → + fnreads Membership · Household · Location → handler post_consent → call ||| Δ Setup's very first write is no longer POST /consent → ConsentRecord: the User row is created + committed by the auth gate BEFORE consent runs (first-sight), so the walk's first red wire moves to Hop 0
 J W2 Look for recipes [GET /recipes/explore, GET /recipes, GET: BEFORE endpoint GET /recipes/explore → handler explore_recipes → calls build_candidate · build_cooked_ledger · filter_candidates · rerank_by_preferences · get_exploration_preferences · compute_recipe_availability · iter_candidate_recipes · load_user_allergens → fnrea ||| AFTER endpoint GET /recipes/explore → + gated_by RateLimitMiddleware [global] → + depends get_auth_context (recipe_explore.py:56) → + [K1 chain: fnreads/fnwrites User (commits) · fnreads Membership · Household · Location] → + calls AuthContext.require_household [gate 409] → handler explore_recipes … (unchanged tail) → endpoint GET /recipes → + gated_by RateLimitMiddleware [global] → + depends get_auth_context (recipes.py:4 ||| Δ The 'pure read' discovery path shows its one hidden write: every step commits the User get-or-create at Hop 0.5; explore additionally shows the 409 household gate. No sensitive rate-limit pills (none 
 J W3 Filter recipes [GET /taxonomy, GET /repertorio/cupos, PUT: BEFORE endpoint GET /taxonomy → handler get_taxonomy → fnreads TaxonomyDimension · TaxonomyValue → endpoint GET /repertorio/cupos → handler get_cupos → calls list_cupos → fnreads RecipeFilterMode → endpoint PUT /recipe-filter-modes/{mode_id}/cupo → handler put_cupo → ||| AFTER endpoint GET /taxonomy → + gated_by RateLimitMiddleware [global] → + depends get_auth_context (catalog.py:56) → + [K1 chain] → handler get_taxonomy … (unchanged) → endpoint GET /repertorio/cupos → + gated_by RateLimitMiddleware [global] → + depends get_auth_context (repertorio.py:40) → + [K1 chain] → handler get_cupos … (unchanged) → endpoint PUT /recipe-filter-modes/{mode_id}/cupo → + gated_by RateLimitMiddleware [g ||| Δ Five uniform Hop-0.5 gate hops; the two mutating steps (PUT cupo, PATCH flags) additionally show the idempotency stamp nobody reads (no get_idempotency_key call in those handlers) — a visible stamp-wi
 J W4 Cook [POST /cooking/sessions, PATCH …/stage, PATCH …/step: BEFORE endpoint POST /cooking/sessions → handler post_start_session → calls _attach_photos · concurrent_cook_cap_for_tier · start_session → fnwrites CookingSession (commits) · fnreads CookingSession · Recipe → writes_to CookingStageReminder · reads_from CookingPhoto  ||| AFTER endpoint POST /cooking/sessions → + gated_by RateLimitMiddleware [sensitive: /api/v1/cooking/sessions, rate_limit.py:30] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (cooking.py:119) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_start_session → + calls get_idempotency_key (cooking.py:126) → calls _attach_photos · concurrent_cook_cap_for_tier · start_ ||| Δ All 5 cooking steps sit under the 20/min sensitive window — the whole workflow wears the sensitive pill, and every step shows the 409 household gate; only step 1 consumes the idempotency stamp.
 J W5 Store ingredients [POST /pantry/items, POST /pantry/items: BEFORE endpoint POST /pantry/items → handler create_pantry_item → calls add_pantry_item → fnwrites IngredientHistoryEvent · PantryItem (commits) → endpoint POST /pantry/items/batch → handler create_pantry_items_batch → calls add_pantry_item → fnwrites IngredientHisto ||| AFTER endpoint POST /pantry/items → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (pantry.py:125) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler create_pantry_item … (unchanged) → endpoint POST /pantry/items/batch → + gated_by RateLimitMiddleware [sensitive: /api/v1/pantry/items/batch, rate_limit.py:36] → + gated_by Idempot ||| Δ The batch step alone wears the sensitive pill (the single-item POST does not — a visible asymmetry the source comment explains at rate_limit.py:33-36); all four show the household gate.
 J W6 Pantry locations [POST /pantry/locations, POST /pantry/it: BEFORE endpoint POST /pantry/locations → handler create_location → calls add_location → fnwrites Location (commits) → endpoint POST /pantry/items/{item_id}/assign-location → handler assign_location (fnreads Location, commits) → calls edit_pantry_item → fnwrites Pantr ||| AFTER endpoint POST /pantry/locations → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (pantry.py:431) → + [K1 chain: … fnreads Location via get_principal_location] → + calls AuthContext.require_household [gate 409] → handler create_location … (unchanged) → endpoint POST …/assign-location → + gated_by RateLimitMiddleware [global] → + gated_by Idempotenc ||| Δ Location gets a second reader at Hop 0.5 (the principal-Despensa lookup in load_household_context) on every step, before the handler's own Location op — the model's 'read by' list grows by get_princip
 J W7 Shopping [POST /shopping/items, POST /shopping/from-plann: BEFORE endpoint POST /shopping/items → handler create_shopping_item → calls add_shopping_item → fnwrites ShoppingItem (commits) → endpoint POST /shopping/from-planned-recipes → handler post_from_planned_recipes → calls generate_from_planned_recipes → fnwrites Shoppin ||| AFTER endpoint POST /shopping/items → + gated_by RateLimitMiddleware [sensitive: /api/v1/shopping/items, rate_limit.py:32] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (shopping.py:99) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler create_shopping_item … (unchanged) → endpoint POST /shopping/from-planned-recipes → + gated_by RateLimitMiddleware [global — not a s ||| Δ Steps 1 and 4 wear the sensitive pill, step 2 (the bulk generator) does not — the prefix match, not the write mass, decides; all four show the household gate.
 J W8 Plan [POST /recipes/{recipe_id}/plan, POST /shopping/from: BEFORE endpoint POST /recipes/{recipe_id}/plan → handler post_plan_recipe → calls load_user_allergens · plan_recipe · plan_into_cupo → fnwrites PlannedRecipe (commits) · fnreads Recipe · UserDietaryProfile → endpoint POST /shopping/from-planned-recipes (as W7) → endp ||| AFTER endpoint POST /recipes/{recipe_id}/plan → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (recipe_plan.py:40) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_plan_recipe … (unchanged) → endpoint POST /shopping/from-planned-recipes (as W7 after) → endpoint DELETE /recipes/{recipe_id}/plan → + gated_by RateLimitMiddle ||| Δ Three uniform gate hops (global window · stamp · auth commit · 409 gate); no sensitive pills anywhere in Plan.
 J J1 POST /setup/complete: BEFORE endpoint POST /setup/complete → handler setup_complete → calls _me_response_from_result · complete_setup → calls _discard_claim (fnreads IdempotencyKey) · _stamp_completion · _upsert_dietary · _upsert_exploration · _upsert_format_prefs · _upsert_notifications  ||| AFTER endpoint POST /setup/complete → + gated_by CORSMiddleware [all] → + gated_by RateLimitMiddleware [sensitive 20/min: /api/v1/setup/complete] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (setup.py:184) → + calls build_auth_context → + calls TokenVerifier.verify → + calls resolve_or_create_user → + fnreads User · fnwrites User (commits) → + calls load_household_context → + fnreads Member ||| Δ The catalog's flagship journey gets its true first hop: the User row exists and is committed before complete_setup's transaction starts (the identity the household will be attached to), and the idempo
 J J2 DELETE /me: BEFORE endpoint DELETE /me → handler delete_me → calls delete_identity · delete_account → fnwrites AiSpendLog · DishHistoryEvent · Household · IdempotencyKey · Membership · Recipe · User → writes_to User (the map's ONLY User wire today) ||| AFTER endpoint DELETE /me → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, DELETE] → + depends get_auth_context (setup.py:217) → + [K1 chain: fnreads User · fnwrites User (commits) · fnreads Membership · Household · Location] → handler delete_me … (unchanged) ||| Δ The one journey that already writes User now shows the same row being get-or-created + committed one hop earlier by the gate — the create-then-delete shape becomes visible on one screen.
 J J3 POST /cooking/sessions/{session_id}/complete: BEFORE endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete → calls complete_session → fnwrites CookingSession · DishHistoryEvent · IngredientHistoryEvent · PantryItem (commits) · fnreads CookingSession · Recipe · RecipeIngredient → writes_to ||| AFTER endpoint POST …/complete → + gated_by RateLimitMiddleware [sensitive: /api/v1/cooking/sessions] → + gated_by IdempotencyMiddleware [stamp, POST — no reader in this handler] → + depends get_auth_context (cooking.py:387) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_complete … (unchanged) ||| Δ Sensitive pill + 409 gate + the auth commit at Hop 0.5; the stamp has no consumer here (the completion's idempotency is not header-driven).
 J J4 POST /cooking/sessions: BEFORE endpoint POST /cooking/sessions → handler post_start_session → calls _attach_photos · concurrent_cook_cap_for_tier · start_session → fnwrites CookingSession (commits) · fnreads CookingSession · Recipe → writes_to CookingStageReminder · reads_from CookingPhoto ||| AFTER endpoint POST /cooking/sessions → + gated_by RateLimitMiddleware [sensitive] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (cooking.py:119) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_start_session → + calls get_idempotency_key (cooking.py:126 → middleware/idempotency.py:29) → calls _attach_photos · concurrent_cook_cap_for_tier · start_session … (un ||| Δ One of the 5 handlers that actually reads the middleware stamp — the walk shows stamp → get_idempotency_key → start_session, plus the sensitive pill and the 409 gate.
 J J5 POST /pantry/items: BEFORE endpoint POST /pantry/items → handler create_pantry_item → calls add_pantry_item → fnwrites IngredientHistoryEvent · PantryItem (commits) ||| AFTER endpoint POST /pantry/items → + gated_by RateLimitMiddleware [global — /pantry/items is NOT a sensitive prefix; only /pantry/items/batch is] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (pantry.py:125) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler create_pantry_item … (unchanged) ||| Δ Global window only, the 409 gate, and the auth commit; the stamp is unread here.
 J J6 POST /shopping/items/{item_id}/confirm-bought: BEFORE endpoint POST /shopping/items/{item_id}/confirm-bought → handler post_confirm_bought → calls confirm_bought → fnwrites IngredientHistoryEvent · PantryItem · ShoppingItem (commits) · fnreads IngredientHistoryEvent · PantryItem · ShoppingItem ||| AFTER endpoint POST /shopping/items/{item_id}/confirm-bought → + gated_by RateLimitMiddleware [sensitive: prefix /api/v1/shopping/items] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (shopping.py:114) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_confirm_bought … (unchanged) ||| Δ Sensitive pill (by prefix inheritance from /shopping/items), 409 gate, auth commit at Hop 0.5.
 J J7 POST /recipe-creation/manual: BEFORE endpoint POST /recipe-creation/manual → handler post_create_manual → calls create_recipe_manual → fnwrites Recipe · RecipeCreationRequest · RecipeIngredient · RecipeStep (commits) · fnreads Recipe · RecipeCreationRequest ||| AFTER endpoint POST /recipe-creation/manual → + gated_by RateLimitMiddleware [sensitive: /api/v1/recipe-creation, rate_limit.py:29] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (recipe_creation.py:61) → + [K1 chain] → handler post_create_manual → + calls get_idempotency_key (recipe_creation.py body) → calls create_recipe_manual … (unchanged) ||| Δ Sensitive pill + stamp consumed by the handler + auth commit; no 409 gate (this handler does not call require_household).
 J J8 POST /consent: BEFORE endpoint POST /consent → handler post_consent → calls _status · record_consent → fnwrites ConsentRecord (commits) → writes_to ConsentRecord ||| AFTER endpoint POST /consent → + gated_by RateLimitMiddleware [global] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (consent.py:46) → + calls build_auth_context → + calls TokenVerifier.verify → + calls resolve_or_create_user → + fnreads User · fnwrites User (commits, first sight) → + calls load_household_context → + fnreads Membership · Household · Location → handler post_consent → calls _s ||| Δ The consent record is no longer the account's first row: the walk shows User committed at Hop 0.5 before ConsentRecord is written — the pre-consent identity write the legal-consent entity never draws 
 J K2 GET /recipes/explore (pure read): BEFORE endpoint GET /recipes/explore → handler explore_recipes → calls build_candidate · build_cooked_ledger · filter_candidates · rerank_by_preferences · get_exploration_preferences · compute_recipe_availability · iter_candidate_recipes · load_user_allergens → fnrea ||| AFTER endpoint GET /recipes/explore → + gated_by RateLimitMiddleware [global] → + depends get_auth_context (recipe_explore.py:56) → + [K1 chain: … fnwrites User (commits) …] → + calls AuthContext.require_household [gate 409] → handler explore_recipes … (unchanged) ||| Δ The 'pure read' journey is not pure: its Hop 0.5 holds a commit on User. The catalog's read/write framing should say 'read after the auth commit'.
 J K3 POST /recipe-creation/gustify (+ SSE tail GET /recipe-cre: BEFORE endpoint POST /recipe-creation/gustify → handler post_create_gustify → calls _gustify_response · _raise_for_pipeline_error · generate_gustify_recipe → fnwrites RecipeCreationRequest (commits) · reads_from UserDietaryProfile · writes_to IngredientReconciliation ||| AFTER endpoint POST /recipe-creation/gustify → + gated_by RateLimitMiddleware [sensitive: /api/v1/recipe-creation] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (recipe_creation.py:218) → + [K1 chain] → + calls AuthContext.require_household [gate 409] → handler post_create_gustify → + calls get_idempotency_key → calls generate_gustify_recipe … (unchanged; the AI per-user/IP SlidingWindowLimi ||| Δ Both the POST and its SSE tail wear the sensitive pill; the tail shows a DIFFERENT gate node (get_auth_context_from_query) converging on the same build_auth_context — the token-on-URL path becomes vis
 J K5 POST /recipes/demand (own rate-limit clock): BEFORE endpoint POST /recipes/demand → handler post_recipe_demand (fnwrites RecipeDemand, commits) ||| AFTER endpoint POST /recipes/demand → + gated_by RateLimitMiddleware [global — /recipes/demand is not a sensitive prefix] → + gated_by IdempotencyMiddleware [stamp, POST] → + depends get_auth_context (recipe_demand.py:33) → + [K1 chain] → handler post_recipe_demand (unchanged; its own clock is an in-handler query on RecipeDemand, recipe_demand.py:4/:49 — not middleware, unchanged by this class) ||| Δ Only the uniform gate hops appear; the route's 'own clock' stays a handler-internal read the ORM class must draw, so the operator sees the global window + auth commit, not the demand-specific limit.
 UNCHANGED: K4 the seed/ops lane (seed_c
 --- REFUTER: wrong checks:
   ✗ today_draws: 67/67 endpoints carry middleware (stats 67/67), get_auth_context on 66, from_query 1, get_settings 12; fn_nodes 210, fn_edges 145. ⇒ Committed example (c4-graph.js head 7628b19e, last touched by a81fb42 schema homing) measured: 72 endpoints, middleware_endpoints 72, gate_endpoints 72; get_session 72 · get_auth_context 71 · get_settings 12 · get_auth_context_from_query 1; levels.json fn_nodes 219, fn_edges 150. gustify's LIVE docs
   ✗ The `User` model has exactly 1 rollup wire (DELETE /me writes_to). ⇒ c4-graph.js measured: l2.auth has 1 in-entity rollup edge {writes_to, endpoint:DELETE /me → model:User}; cross_edges ALSO carries {kind:'reads_from', from:'endpoint:GET /account/export', from_slug:'legal-consent', to:'model:User'}. So today User has 1 WRITE wire and 1 READ wire (2 rollup wires), not
   ✗ honest_empty: a project with no Depends()/Security() and no add_middleware() emits c4-graph.json and levels.json byte-identical to today's build. ⇒ The fix bundle includes replacing _a3_graft.py:175 `!= "function"` with `not in _CALLABLE_KINDS`. That changes derive_functions' fn_slug/calls for ANY project whose handlers call methods in claimed files, regardless of Depends — on gustify measured 50 handler→METHOD calls edges in claimed files woul
   ✗ K1 AFTER: '+ depends bearer_scheme · get_session · _resolve_verifier [nested, param-dep of the dep] (context.py:91-93)'. ⇒ context.py:91-93 does carry those three nested Depends, but the proposed seam resolves only the HANDLER's Depends targets (_endpoint_middleware :1071-1103 reads node.args of the route fn; the L994-1000 floor states 'a dependency nested INSIDE another Depends is not walked') and the sketch adds no re
   ✗ Batteries to re-pin: tests/arch-graph C4 middleware checks at L437-447. ⇒ tests/arch-graph/run.sh: the C4 middleware checks sit at L462-472 (`_fixmw[...]['middleware'] = [`, 'C4: the endpoint node carries its middleware floor', 'C4 stats: middleware_endpoints + gate_endpoints', 'C4 honest-empty'); L437-447 are the A2/C3 access-edge + minted-model checks. Re-pin list itsel
   CORR: today_draws '67/67 endpoints … get_auth_context on 66 … fn_nodes 210 · fn_edges 145' → the committed example (templates/center/shell/example/codebase-graph-station/c4-graph.js, head 7628b19e) and gustify's live c4-graph.json both carry 72 endpoints, middleware_endpoints 72, get_session 72 · get_auth_context 71 · get_settings 12 · get_auth_context_from_query 1; levels.json fn_nodes 219, fn_edges 15
   CORR: 'handlers_calling_require_household_of_67: 47' → 48 of 72 (AST over apps/api/api/*.py joined to the map's handlers); 'graft_calls_edges_into_require_household: 55' → 54 (wiring.json now).
   CORR: 'the only inbound edge [into get_auth_context] is 1 contains' → wiring has 3 `calls` edges into auth/context.py#get_auth_context from apps/api/tests/test_auth_context.py (C133/C134/C135) + 1 contains; the '0 calls from api/' half is right.
   CORR: 'The User model has exactly 1 rollup wire (DELETE /me writes_to)' → 2: the auth-local writes_to from DELETE /me AND a cross_edges reads_from from endpoint:GET /account/export (legal-consent) → model:User in c4-graph.js.
   CORR: _a3_code citations are ~137 lines off: _depends_target is :1029-1043 (not 892-908), _endpoint_middleware :1071-1103 (not 934-965), function_insight `for slug in ENTITY_CODE` :1105-1115 (not 978-988), the 'level-2 gates' floor comment :994-1000 (not 857-863). _a3_graph: enode middleware :614-615 (not 610-611), stats :1126-1127 (not 1110-1111). _a3_levels: 'ONLY roots' :233 (not 227), handler-source
   CORR: honest_empty 'byte-identical to today's build' → false as bundled: the _a3_graft.py:175 `!= "function"` → `not in _CALLABLE_KINDS` relaxation changes fn_slug/calls for any project whose handlers call methods in claimed files with or without Depends (gustify: 50 such handler→method calls edges — 48 AuthContext.require_household, 1 EventBus.publish, 1 ExplorationBias.from_lists — would newly enter l
   CORR: K1 after '+ depends bearer_scheme · get_session · _resolve_verifier [nested]' → the proposed seam walks only the handler's own Depends (the :994-1000 floor explicitly does not descend nested deps and the sketch adds no recursion); bearer_scheme is an HTTPBearer INSTANCE (context.py:23) with no graft fn id, so it can never be a resolved `fn`. Drop those three hops from the AFTER or add a nested-sig
   CORR: derive_depends rule 'resolved fn present in _behind_context's fn_ids' → _behind_context (_a3_graft.py:207-220) admits every callable node whether or not its file is claimed, so db.py#get_session (72 endpoints) and config.py#get_settings (12) would ALSO become depends wires (84 extra), none shown in any after-sequence; db.py/config.py are unclaimed, so _a3_levels' 'add dep ids to drawn_fn' has no e
   CORR: _a3_code seam 'add a per-file import map' → one already exists: `_file_imports` at _a3_code.py:825-857 ({local name: app-internal bool}); it must be EXTENDED to carry the module path, and it depends on `_PY_TEXTS` which function_insight fills at :1116-1117 — after parse_endpoints (:190-192) has already run. Ordering must be handled.
   CORR: _a3_levels seam 'the §3b d2w BFS then walks build_auth_context → resolve_or_create_user on its own' → only after the center.config claim: the wiring edge build_auth_context→ownership.py#resolve_or_create_user exists, but derive_functions (_a3_graft.py:173-190) drops it because ownership.py has no entity in fn_slug; the config seam already says this, the levels seam text over-claims.
   CORR: Per-journey line cites (consent.py:46, cooking.py:119, …) are the handler `def` lines; the Depends() sits 1-3 lines below (consent.py:48, cooking.py:122, setup.py:187/209/218, pantry.py:86/127/207/317/356/433/446/476, shopping.py:39/62/101/116, recipe_plan.py:42/93, recipe_creation.py:64/221, recipe_stream.py:117, recipe_demand.py:35, user_settings.py:109, recipe_explore.py:57, recipes.py:45, reci
   CORR: BEFORE sequences for POST /cooking/sessions, PATCH …/timer, POST …/complete, POST /recipe-creation/gustify and GET …/gustify/stream fold DRAWN h2/h3 hops (seed_stage_schedule→_schedule_next; clear_pending_cooking_timer_notifications/schedule_cooking_timer; reclaim_if_abandoned→enqueue_unknown_ingredients; _finalize_stream) into rollup lines — chain.py prints them as function hops.
   CORR: 'graft_relations calls 26088 · contains 16553' / 'function 13459' → 26093 · 16555 · 13461 now (wiring drift on gustify's uncommitted tree); `+ calls TokenVerifier.verify` would draw the Protocol stub (verifier.py:36-39), not FirebaseTokenVerifier.verify (:103) — the external verification sink stays undrawn.
   MISSING: The WebSocket door into K1: `ws_gustify` (recipe_stream.py:161-178, `@router.websocket("/recipe-creation/gustify/ws")`) calls build_auth_context DIRECTLY (a real graft `calls` edge, the only non-dependency caller) and its docstring states 'WS bypasses the HTTP RateLimitMiddleware' — a third entrance
   MISSING: GET /healthz (health.py:14, unprefixed) is the ONE route outside the global window (rate_limit.py:40 EXEMPT_PATHS) and is also off-map; 'endpoints_under_global_window: 67' should say 'every mapped endpoint; the exempt liveness probe is unmapped'.
   MISSING: The resource-dependency noise class: under the proposed resolution rule, `get_session` (72 endpoints, db.py unclaimed) and `get_settings` (12, config.py unclaimed) resolve to graft fn ids and would ride the same `depends` wire as get_auth_context — 84 wires the finder neither draws in any AFTER nor 
   MISSING: GET /account/export already reads User today (cross_edges reads_from, legal-consent → auth) — the second User wire the today_draws paragraph misses; after the fix the User model's 'read by' list grows from 1 to 72, which the delta prose ('most-written model') should also count as reads.
   MISSING: The verification hop resolves to the Protocol stub (verifier.py:36-39 `TokenVerifier(Protocol)`); the FirebaseTokenVerifier.verify (:103) external call — the only non-DB side effect on the auth path — stays undrawn by this class and belongs to the sinks/external gap, which the finder should have nam

######## integrations  [high]  tag=config+additive+change-in-place  REFUTED=True
TODAY: 0 provider nodes, 0 outbound wires, for a backend with exactly 3 real SDK call sites (gemini.py:196 generate_content · verifier.py:113 verify_id_token · verifier.py:149 delete_user) + 2 SDK constructors (gemini.py:165 · verifier.py:91) + 1 module-level ops init (main.py:66 sentry_sdk.init). The 8 `external` L2 nodes in the committed map are all FK stubs (7× external:auth, 1× external:recipe). Sinks: 6 of 210 fn_nodes carry a sink (5 http + 1 queue) and 6 of 67 endpoints carry access.sinks — the 5 http are all `session.delete(row)` false positives (0 real http; the queue one is real: cooking.py:417 bus.publish) and no station renders the field. K3 POST /recipe-creation/gustify draws 6 fns and
 WHY: The sink detector only sees a ONE-level attribute call whose receiver is a bare Name; the Gemini SDK call `client.aio.models.generate_content(` is a 3-deep chain, so `recv` is "" and nothing fires (and `generate_content` is not one of the 5 http verbs anyway).  ev=templates/center/generators/_a3_code.py:838-839 (recv = func.value.id only if isinstance(func.value, ast.Name)); templates/center/generators/_a3_code.py:851-853 (http = receiver-name substring + attr in post/put/patch/delete/get); apps/api/integrations/gemini.py:196 (client.aio.models.generate_conte
 WHY: Both Firebase calls are passed as CALLABLES to asyncio.to_thread and the SDK is bound by importlib string, never an ast.Call on the SDK — the detector inspects only Call.func, so it is blind by construction.  ev=apps/api/auth/verifier.py:105 (importlib.import_module("firebase_admin.auth")); apps/api/auth/verifier.py:112-114 (await asyncio.to_thread(firebase_auth.verify_id_token, token)); apps/api/auth/verifier.py:146-149 (delete_identity: importlib + asyncio.to_thread(firebase_auth.delete_user, uid))
 WHY: The same receiver-name rule tags SQLAlchemy `session.delete(row)` as http: all 5 http sinks in the committed map are this false positive (0 real http), and one of them sits on K3's own service fn.  ev=templates/center/generators/_a3_code.py:851-853 ("session" in recv and attr == "delete"); apps/api/services/setup.py:333 · apps/api/services/ai_recipes.py:437 · apps/api/services/locations.py:132 · apps/api/services/recipes.py:284,339,382 (6 session.delete sites in 5 fns); templates/center/shell/exa
 WHY: There is no node to draw a provider AS: the `external` L2 kind is minted only for outbound-FK owners from the model FK loop, and the station's legend defines it as an FK target — the 8 external nodes in the committed map are all FK stubs.  ev=templates/center/generators/_a3_graph.py:553-560 (ext(owner) → external:<owner>) and :685-690 (the only call site, inside the model FK loop); templates/center/generators/_a3_graph.py:71 (_L2_KINDS = endpoint · model · schema · external · web); docs/design/codebase-graph-consolidation/universe-build/
 WHY: Even a correct sink tag is INVISIBLE today: the levels feed copies `sinks` onto fn nodes and the Universe copies it onto the piece, but no card, pill, legend or wire reads it (1 grep hit across parts/*.js, assemble.py, codebase-graph.html, sim-panel.js = the copy).  ev=templates/center/generators/_a3_levels.py:360-362 (sinks copied onto fn_nodes); docs/design/codebase-graph-consolidation/universe-build/parts/layout.js:254 (sinks:f.sinks carried, never rendered); templates/center/generators/_a3_graph.py:625-627 (enode["access"] = endpoint_access — sinks ride as a c
 WHY: The Gemini call site is unreachable by ANY call-tree walk — three dispatch breaks in a row: run_pipeline lives in an UNCLAIMED file (graft has the edge, the fold drops it as unmapped_file); run_pipeline/StreamPipeline.events dispatch stages from the PIPELINE_STAGES tuple (0 graft edges into gemini_call from either); gemini_call calls `deps.client.generate_recipes` through a Protocol (0 graft edges  ev=graft/.graph/wiring.json: ai_recipes.py#generate_gustify_recipe → recipes/pipeline/runner.py#run_pipeline (inferred) exists; center.config.json has no recipes/pipeline/runner.py or state.py (UNCLAIMED); templates/center/generators/_a3_graft.py:494-497 (src_slug/dst_slug None → dropped["unmapped_file
 WHY: Config coverage: gemini.py and gastify_exchange.py ARE claimed (recipe/services, pantry/services) but 3 of 5 integrations files, the breaker and the whole pipeline runtime are not; the 5 provider getters and the boletapp getter have 0 callers outside tests, so their absence from the map is honest (dead shells), not a gap.  ev=docs/site/center/center.config.json:545 (apps/api/integrations/gemini.py under recipe.services) · :396 (gastify_exchange.py under pantry.services); UNCLAIMED (measured vs config): integrations/providers.py · integrations/circuit_breaker.py · integrations/__init__.py · recipes/pipeline/runner.py · re
 SEAM _a3_code: Widen `_detect_sinks` (L829-856) into a per-module PROVIDER pass: build the module's SDK-binding table = names bound by `import X`/`from X import y` at module AND function scope whose root package is in an explicit PROVIDER registry (google.genai/google→gemini, firebase_admin→firebase, sentry_sdk, httpx/requests/aiohttp→http, redis, boto3, stripe, openai, anthropic…) PLUS locals assigned from `importlib.import_module("<pkg>…")` string constants (verifier.py:105,146). A fn gets `externals:[{provi
 SEAM _a3_graft: `derive_endpoint_access` (L300-351): union `externals` over the handler's reached set exactly like sinks (L342-343) → `out[key]["externals"]` (sorted providers). `derive_fn_roles` (L360-401) untouched by default — flag the decision: roles read ORM ops only (L372-373) so delete_identity stays `pure` although the legend calls a durable sink an accessor (layout.js:1967).
 SEAM _a3_graph: In `_l2` (L505-700): mint one `provider:<name>` L2 node per (entity, provider) homed by the file of the tagged fn (from insight.fi externals: gemini.py→recipe, verifier.py→auth); at the endpoint join (L625-632) add an endpoint→provider edge kind `reaches` when endpoint_access[bkey].externals is non-empty; `stats.providers = {present, count, by_provider, endpoints_reaching}` only when any fn is tagged. Do NOT reuse `external` (card.js:294 legend would lie).
 SEAM _a3_levels: fn_nodes block (L320-363): copy `externals` onto drawn fn nodes beside `sinks` (L360-362); fn_edges (L251-305): emit `{s:<fn>, t:"provider:<name>", rel:"reaches"}` for every DRAWN fn with externals, and list the provider ids so the station has both ends.
 SEAM station(adapter/layout/card): KINDS/NODEDEF: add `provider` (glyph + layer, spike-base.html:247 table; adapter.js:65 drops unknown kinds today); order array (assemble.py:196) + `_GRPKINDS.backend` (layout.js:2087) + KRADF (layout.js:625); REL2KIND (assemble.py:15) `reaches:'reaches'` + LINKMETA/CONN entry (adapter.js:14) with its own dash; `_buildFnData` (layout.js:250-270) pushes fn→provider links when Functions is ON, mirroring the fn→model access wires; card.js: `C.provider` builder (provider · pkg · call sites · 'provide
 SEAM center.config: gustify twin: claim apps/api/recipes/pipeline/runner.py + state.py + apps/api/integrations/circuit_breaker.py + apps/api/streaming/producer.py under recipe.services so the graft edges that ALREADY exist (generate_gustify_recipe→run_pipeline · stream_gustify_events→StreamPipeline.events · gemini_call→CircuitBreaker.*) stop being dropped as unmapped_file (_a3_graft.py:494-497). Dependency on the unclaimed-files class; without it K3's provider node hangs off an undrawn fn.
 SKETCH: 1. Per module: SDK-binding table = import/from-import names (module+fn scope) whose root ∈ PROVIDERS, + locals from importlib.import_module("pkg…"). | 2. Per fn: externals = providers of any Call whose attribute-chain root is bound, or any bound Attribute passed as a Call argument (to_thread idiom); http sink now requires a bound http lib. | 3. faccess carries externals → endpoint_access unions them over the handler's call-tree (as sinks are today); roles/d2w untouched. | 4. _a3_graph mints provider:<name> L2 nodes homed by the tagged fn's file; endpoint→provider `reaches` via endpoint_access; stats.providers. | 5. _a3_levels copies externals onto drawn fn_nodes and emits fn→provider `reaches` fn_edges. | 6. Station: KINDS.provider + order/_GRPKINDS/KRADF; REL2KIND.reaches + LINKMETA; _buildFnData pushes fn→provider links; card + legend draw the glyph/wire. | 7. No SDK binding anywhere → no external
 HONEST-EMPTY: A project whose Python binds no registry package (no import/from-import/importlib of google.genai · firebase_admin · sentry_sdk · httpx · requests · aiohttp · redis · boto3 · …) emits: no `externals` key on any function_insight entry, no `externals` in endpoint_access, no `provider:*` L2 node, no `r
 SIZE: ~45 lines _a3_code (registry + binding table + rules a/b + http re-scope) · ~15 _a3_graft · ~35 _a3_graph · ~20 _a3_levels · ~60 station (KINDS/order/_GRPKINDS/REL2KIND/LINKMETA/_buildFnData/card/legend) · 4 config lines (twin). Re-pin: tests/orm-access (sink cases L90-100: +provider cases incl. the to_thread idiom, +`session.delete(row)` → []), tests/arch-graph (external stub pins L136-141 stay; 
 NEW_KINDS: provider — L2/fn-feed NODE kind: an outbound SDK/provider a function reaches (Gemini · Firebase Auth · Sentry); distinct glyph, backend group, NOT the FK `external` stub || reaches — WIRE kind: fn → provider (drawn with Functions ON) and endpoint → provider (rollup via endpoint_access); REL2KIND + LINKMETA + legend row
 COUNTS: {"integration_modules": 4, "integration_lines": 506, "provider_getters_in_providers_py": 5, "provider_getters_callers_in_source": 0, "boletapp_client_callers_in_source": 0, "real_sdk_call_sites_backend": 3, "sdk_constructor_sites": 2, "module_level_ops_init_sites": 1, "sdk_binding_idioms_observed": 3, "the_12_reconstructed": "5 provider adapters + Gemini real/mock (2) + Boletapp real/mock (2) + CircuitBreaker (1) + Firebase verifier real/mock (2) = 12", "integrations_files_unclaimed": 3, "pipeline_runtime_files_unclaimed": 4, "api_py_files": 168, "api_py_claimed": 95, "api_py_unclaimed": 73, "
 RISK: Shipping this class ALONE draws provider:gemini as an orphan piece of recipe (no drawn fn reaches _RealGeminiRecipeClient.generate_recipes): K3's walk still dead-ends at reclaim_if_abandoned until runner.py/state.py/producer.py are claimed (config) A
 RISK: The PROVIDER registry is a curated allowlist — the exact 'gustify-only heuristics' trap from the 2026-08-26 codemap review. Keep it package-ROOT + importlib-string based and explicit; a 'tag every third-party import' rule would over-tag pydantic/sqla
 RISK: Re-scoping the http sink is change-in-place: 5 committed gustify outputs move (levels fn_nodes + c4 endpoint access.sinks) and tests/orm-access L97 (httpx.post → http) must keep passing while a new case pins session.delete(row) → []; the example map 
 RISK: Reusing the existing `external` kind would be ~60 lines cheaper but makes card.js:294 / the legend lie (operator ruling: legend = the drawn thing); a new `provider` kind touches KINDS, order, _GRPKINDS, KRADF, REL2KIND, LINKMETA, _buildFnData, card, 
 J J2 DELETE /me: BEFORE endpoint DELETE /me (screen useDeleteAccount bridges it) → handler delete_me [caller · d2w=0 · auth] → calls delete_identity [pure · d2w=– · auth · inferred]  (dead end — no outgoing wire) → calls delete_account [accessor · d2w=0 · legal-consent · inferred] →  ||| AFTER endpoint DELETE /me → handler delete_me → calls delete_identity → + reaches provider:firebase  (firebase_admin.auth.delete_user via asyncio.to_thread — auth/verifier.py:146-149; tagged by rule b, the bound-attribute-as-argument idiom; node homed under auth because verifier.py is auth.services) → calls delete_account → fnwrites AiSpendLog · DishHistoryEvent · Household · IdempotencyKey · Membership · Recipe · User → + ||| Δ the catalog's 'external-identity tail' becomes visible: delete_identity stops being a dead-end pure fn and grows a dashed reaches wire to a Firebase provider glyph inside auth; the endpoint card lists
 J K3 POST /recipe-creation/gustify (+ its SSE tail GET /recipe: BEFORE endpoint POST /recipe-creation/gustify (resp GustifyCreationResponse · behind 47 fns/depth 5 · screens none bridged) → handler post_create_gustify [caller · d2w=0 · recipe] → calls _gustify_response [pure] · calls _raise_for_pipeline_error [pure] → calls gener ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify → calls _gustify_response · calls _raise_for_pipeline_error → calls generate_gustify_recipe → fnwrites RecipeCreationRequest (commits)  (sinks http REMOVED — change-in-place) → calls reclaim_if_abandoned · calls enqueue_unknown_ingredients → fnwrites IngredientReconciliationQueue → + calls run_pipeline  (calls, inferred — the graft edge exists today ||| Δ a Gemini provider glyph appears in recipe wired from _RealGeminiRecipeClient.generate_recipes (Functions ON), and the false http sink on generate_gustify_recipe disappears; the operator sees K3's walk
 UNCHANGED: W1 Initial setup [POST /cons, W2 Look for recipes [GET /re, W3 Filter recipes [GET /taxo, W4 Cook [POST /cooking/sessi, W5 Store ingredients [POST /, W6 Pantry locations [POST /p, W7 Shopping [POST /shopping/, W8 Plan [POST /recipes/{reci, J1 POST /setup/complete, J3 POST /cooking/sessions/{s, J4 PO
 --- REFUTER: wrong checks:
   ✗ K3 AFTER: `+ calls run_pipeline … drawn once runner.py/state.py are CLAIMED: config` ⇒ _a3_levels.py:257-305: a non-handler-rooted call is drawn only by the §3b d2w descent, which requires the TARGET to carry a distance_to_write (`if _dt is None … continue`, L292-293). derive_distance_to_write (_a3_graft.py:402-441) anchors on ORM writes/commits only; stages.py has 0 `session` referen
   ✗ K3 AFTER: `+ calls StreamPipeline.events (producer.py CLAIMED: config)`, `+ calls CircuitBreaker.before_call/record_* (circuit_breaker.py CLAIMED: config)`, `+ implements _RealGeminiRecipeClient.gener ⇒ _a3_graft.py:176 derive_functions keeps only graft nodes with kind == "function"; wiring.json kinds measured: _RealGeminiRecipeClient.generate_recipes = method, StreamPipeline.events = method, CircuitBreaker.before_call/record_success = method. Methods never enter fn_slug → never in levels `calls` →
   ✗ session_delete_sites 6 in 5 fns (recipes.py:284,339,382 → unplan_recipe + unplan_recipe_by_recipe) ⇒ grep apps/api --include=*.py excl. tests: 8 sites in 8 fns — recipe_filter_modes.py:101 (delete_mode), setup.py:333, recipes.py:284, :339, :382 (set_user_equipment_codes, NOT unplan_recipe_by_recipe — awk on def boundaries), locations.py:132, repertorio.py:227 (downgrade_to_free), ai_recipes.py:437.
   ✗ committed_fn_nodes 210 · committed_fn_edges 145 · graft nodes 19369 / edges 51118 · dropped unmapped_file 9631 / intra_entity 5156 · api_py_claimed 95 ⇒ HEAD example levels.json: 219 fn_nodes / 150 fn_edges (gustify's own docs/site/center/levels.json agrees, same head 7628b19e); 210/145 is the 497b535 example (git show). wiring.json as-found today: 19956 nodes / 51125 edges; committed c4 stats.graft.dropped: unmapped_file 9679, intra_entity 5168; cl
   ✗ real_sdk_call_sites_backend 3 + 2 constructors + 1 ops init; backend-wide SDK grep hit only gemini.py, verifier.py, main.py ⇒ apps/api/auth/verifier.py:72-82 `_firebase_credential`: importlib.import_module("firebase_admin.credentials") at :73 then credentials.Certificate(...) ×2 (:75,:81) and credentials.ApplicationDefault() (:82) — 3 more SDK call sites the finder's own rule (a) would tag; they sit on K1's provisioning pa
   CORR: K3 AFTER '+ calls run_pipeline … drawn once runner.py/state.py are CLAIMED: config' → claiming is not sufficient: _a3_levels.py:292-293 draws a non-handler-rooted hop only when the target has a distance_to_write; run_pipeline has 0 outgoing graft calls and stages.py has 0 ORM/session references, so it never gets a d2w (_a3_graft.py:402-441 anchors on writes/commits only). The hop needs the dispatc
   CORR: K3 AFTER '+ calls StreamPipeline.events (producer.py CLAIMED: config)', '+ calls CircuitBreaker.before_call/record_* (circuit_breaker.py CLAIMED: config)', '+ implements _RealGeminiRecipeClient.generate_recipes', '+ reaches provider:gemini wired from that fn' → all four targets are graft `method` nodes and derive_functions keeps only kind == "function" (_a3_graft.py:176); _CALLABLE_KINDS (L204) fe
   CORR: 'gemini_call→CircuitBreaker.*' → before_call is called from the `circuit_breaker` stage fn (stages.py:86); gemini_call calls record_failure (:119) and record_success (:121).
   CORR: Generator line numbers are one commit stale (497b535): _detect_sinks is at _a3_code.py:966-991 (not 829-856), attach at :1174-1176 (not 1037-1039), _file_imports at :828; ext() call site _a3_graph.py:699 (not 685-690); endpoint join :629-631; levels sinks copy _a3_levels.py:366-368; unmapped_file drop _a3_graft.py:496-499; config lines gemini.py :550 (not 545), gastify_exchange.py :400 (not 396); 
   CORR: 'session_delete_sites 6 / fns 5; recipes.py:284,339,382 → unplan_recipe + unplan_recipe_by_recipe' → 8 sites in 8 fns; recipes.py:382 is inside set_user_equipment_codes; recipe_filter_modes.py:101 (delete_mode, unclaimed services file) and repertorio.py:227 (downgrade_to_free, claimed) were missed. Change-in-place on gustify: 5 drawn fn_nodes + 5 endpoints lose sinks (correct), but 7 function_insi
   CORR: 'committed_fn_nodes 210 / fn_edges 145 / endpoints 67' → HEAD example (head 7628b19e, regenerated in a81fb42): 219 fn_nodes / 150 fn_edges; c4 L2 endpoint nodes 72 (67 claimed labels per chain.py --all + unclaimed). graft as-found today: 19956 nodes / 51125 edges; committed stats.graft.dropped unmapped_file 9679 / intra_entity 5168; claimed api .py 98/168 (70 unclaimed).
   CORR: 'real_sdk_call_sites_backend 3, sdk_constructor_sites 2' → verifier.py:72-82 _firebase_credential adds credentials.Certificate ×2 (:75,:81) + credentials.ApplicationDefault (:82) — 3 more SDK call sites on the K1 provisioning path that rule (a) would tag (importlib binding at :73).
   CORR: K1 'graft has both edges, inferred' → get_auth_context→build_auth_context is EXTRACTED (context.py:98); only build_auth_context→TokenVerifier.verify is inferred. Also FirebaseTokenVerifier.verify is a `method` → excluded from derive_functions like the Gemini method, so 'K1 then needs only wires' is wrong: it needs the same methods admission.
   CORR: J7 'api/recipe_creation.py:61 → services/recipe_creation.py:34 imports no integrations module' → api/recipe_creation.py:20-21 does import integrations.circuit_breaker and integrations.gemini (error classes); the unchanged verdict survives because neither binds an SDK root.
   CORR: K3 SSE-tail BEFORE omits `_sse_frames` [pure·extracted] at h1 and mis-states stream_gustify's d2w (chain.py: d2w=2, not unstated).
   CORR: Fix seam 'http sink fires only when the receiver is bound to an http lib' + risk 'tests/orm-access L97 must keep passing' are in tension: the L97 fixture (`def h(): httpx.post(url, json=d)`, helper fn() at run.sh:30-34 parses the bare snippet) has no import, so under the rule as written it fails; and today's `"client" in recv` branch (_a3_code.py:988) catches a locally-assigned httpx client that t
   MISSING: derive_functions' method exclusion (_a3_graft.py:176, kind == "function" only) — the structural reason no class method (Gemini client, StreamPipeline.events, CircuitBreaker.*, FirebaseTokenVerifier.verify) can ever be drawn on a walk; it gates K3 and K1 more than the config claim does and belongs in
   MISSING: The d2w drawing rule (_a3_levels.py:257-305) as a blocker: a provider-reaching path that never reaches an ORM write has no d2w, so the walk cannot descend into it; the finder's 'roles/d2w untouched' decision needs to be re-opened (e.g. treat `reaches` as a write-anchor) or K3 never connects.
   MISSING: Two seams that already capture half the thing and were not cited: `_file_imports` (_a3_code.py:828-857) already builds a per-file import table over ast.walk (fn-scope imports included, keyed local-name → app-internal bool, used at :1990) — the natural place to widen with a root-package registry inst
   MISSING: verifier.py:72-82 `_firebase_credential` (credentials.Certificate/ApplicationDefault via importlib) — a K1 provisioning hop the class would tag, uncounted.
   MISSING: The two undrawn false-http fns in claimed files (recipes.py#set_user_equipment_codes, repertorio.py#downgrade_to_free) and the unclaimed services/recipe_filter_modes.py#delete_mode — the change-in-place footprint in function_insight is 7 entries, not 5.
   MISSING: POST /recipe-creation/{request_id}/relief-accept is named but its chain was not run; GET /recipe-creation/gustify/stream was paraphrased rather than quoted (chain.py draws _sse_frames + stream_gustify d2w=2).

######## guardrails  [high]  tag=config+additive+change-in-place  REFUTED=False
TODAY: 210 fn_nodes / 145 fn_edges on the committed gustify map; exactly 1 node carries role=gate (assert_recipe_allergen_safe, slug allergen) out of 25 gate-named callables graft indexes under apps/api (5 of them homed to claimed files). That gate is on ONE chain (GET /recipes/{recipe_id}, hop h1 from get_recipe), with no access ops and no outgoing wire; its `behind` badge says {fns:3, depth:2} and names the 3 callees the map has no node for. The wiring says the gate is reachable from 3 endpoints (GET /recipes/{recipe_id}, POST /cooking/sessions, POST /recipes/{recipe_id}/plan) — 1 drawn. load_resolution_snapshot is reachable from 6 endpoints — 0 drawn, 0 rollup reads of CanonicalIngredient/Ingred
 WHY: The gate's decision inputs live in an UNCLAIMED file. assert_recipe_allergen_safe calls load_resolution_snapshot + derive_restrictions (both edges present in graft wiring, inferred), but apps/api/reference/resolution.py is in no entity's code list, so derive_functions drops both calls (both ends must be homed) and the fold counts them under dropped.unmapped_file (9,631 on gustify). function_insigh  ev=gustify docs/site/center/center.config.json entities.allergen.code.services = [recipes.py, recipe_filters.py, safety.py, restriction_seed.py, reconciliation.py] — resolution.py absent from every entity; gustify apps/api/services/recipes.py:216 (local import of derive_restrictions, load_resolution_sn
 WHY: The gate's OWN read is an attribute select the access pass cannot bind: select(RecipeIngredient.ingredient_code) — `_name_model` accepts only a bare ast.Name, so the gate has 0 ops. The same idiom hides the snapshot's first read (select(CanonicalIngredient.code)); its other two reads are bare (IngredientAlias, IngredientRestriction) and WOULD bind once the file is claimed.  ev=gustify apps/api/services/recipes.py:220 `select(RecipeIngredient.ingredient_code).where(RecipeIngredient.recipe_id == recipe.id)`; gustify apps/api/reference/resolution.py:87 `select(CanonicalIngredient.code)` · :91 `select(IngredientAlias)` · :94 `select(IngredientRestriction)`; _a3_code.py:790 `d
 WHY: Two of the gate's three callers are never drawn. graft resolves plan_recipe→gate (extracted, same file) and start_session→gate (inferred, local import inside the fn body), but the levels emitter draws only handler-rooted calls (§3) or write-gradient steps (§3b); the gate has NO d2w (it never reaches a write), so `_dt is None` skips it from both writers. Only get_recipe→gate survives because get_re  ev=gustify apps/api/services/recipes.py:255 `await assert_recipe_allergen_safe(session, recipe, user_allergens)` inside plan_recipe; gustify apps/api/services/cooking.py:143-147 `from services.recipes import assert_recipe_allergen_safe` … `await assert_recipe_allergen_safe(...)` inside start_session; g
 WHY: Role precedence would ERASE the gate the moment its reads become visible: derive_fn_roles tests `g in accessors` before the gate-name test, and the levels emitter attaches `access` only when role == 'accessor'. So today a gate can never show reads, and fixing the select idiom alone flips the map's only gate badge to accessor.  ev=_a3_graft.py:391-394 `if g in accessors: out[g] = "accessor"` / `elif _is_gate_name(sym): out[g] = "gate"`; _a3_graft.py:352 _GATE_PREFIXES = (require_, assert_, ensure_, verify_, check_, guard_, authoriz); _a3_levels.py:356-359 `if _role == "accessor": … _node["access"] = _acc`
 WHY: The snapshot's two resolver METHODS (ResolutionSnapshot.resolve / violations_for) are excluded by kind before homing is even tried; and the SQL allergen wall (allergen_filter_conditions / ingredient_allergen_exclusions, reached from explore, list, pantry overview, shopping) is a query-shape — select(1).select_from(RecipeIngredient).join(IngredientRestriction) — so its readers draw PURE and, being   ev=_a3_graft.py:175 `if n.get("kind") != "function": continue` (derive_functions); gustify apps/api/services/recipe_filters.py:60-85 ingredient_allergen_exclusions — `select(1).select_from(RecipeIngredient).join(IngredientRestriction, …)`; :504-514 apply_recipe_filters applies both walls; gustify apps/
 SEAM center.config: gustify only: add apps/api/reference/resolution.py (and reference/normalize.py, which ResolutionSnapshot.resolve calls) to entities.allergen.code.services; optionally services/meal_plan.py + api/meal_plan.py for the R8 read-path guard in get_current_plan (meal_plan.py:404-427). Effect with NO emitter change: fn_slug +6 functions; function_insight now parses resolution.py so faccess gains IngredientAlias + IngredientRestriction reads in load_resolution_snapshot; derive_endpoint_access (_a3_graft.
 SEAM _a3_levels: §3c GATE-PATH enrichment after §3b (after line 306, before the `_fedges = [e for e in _fedges if …]` filter): using `_fn_roles = (graft or {}).get("fn_roles")`, (i) INBOUND — for every call c in _gf["calls"] with c["s"] in drawn_fn and _fn_roles.get(c["t"]) == "gate", draw the edge and add the gate to drawn_fn (a guard hop rides regardless of d2w); (ii) OUTBOUND — from every drawn gate, draw its calls to fixpoint over homed callees (the gate's decision inputs; bounded by the same depth cap). Sam
 SEAM _a3_code: _name_model (line 790): walk an ast.Attribute chain to its ROOT ast.Name before the m2t lookup, so select(Model.col) / select(Model.a, Model.b) bind Model as a read. This is the select-attr HALF of the ORM-idioms class (the select_from/join binding the SQL wall needs stays there). On gustify it gives the gate its RecipeIngredient read and the snapshot its CanonicalIngredient read.
 SEAM _a3_graft: derive_fn_roles (lines 391-394): gate-name wins over accessor — swap the `if g in accessors` / `elif _is_gate_name(sym)` order (a guard that reads to decide is still a guard). Keeps a.py#require_auth fixture green; adds one case: gate-named fn WITH ops → 'gate' (and its access still rides via the levels change). Optional, shared with the dependency-path class: _CALLABLE_KINDS in derive_functions line 175 → include 'method' so ResolutionSnapshot.resolve / violations_for get nodes.
 SEAM station(adapter/layout/card): No new wire kind required: the added hops are `calls` (REL2KIND calls→calls) and the gate's reads become `fnreads` (layout.js:266-269 already builds _FNLINKS from ANY fn node's access ops, role-agnostic). Two text edits: layout.js:1967 role.gate description 'guards — auth / consent / idempotency' → '… / safety'; card.js middleware note unchanged. If the operator wants the guard hop distinguishable, add `guard:true` styling on the calls wire (dashed amber, the gate badge colour #eab308) + one leg
 SKETCH: 1. claim reference/resolution.py (+normalize.py) under allergen → the 2 gate→resolution calls become homable; faccess gains the snapshot's 2 bare reads. | 2. _a3_code._name_model: root-of-attribute-chain → gate reads RecipeIngredient, snapshot reads CanonicalIngredient (3/3). | 3. _a3_graft.derive_fn_roles: gate-name before accessor → assert_recipe_allergen_safe stays 'gate' with ops. | 4. _a3_levels §3c: draw drawn-fn→gate calls (plan_recipe, start_session) and gate→callee calls to fixpoint (load_resolution_snapshot, derive_restrictions); attach access on gate nodes. | 5. rollup (derive_endpoint_access, unchanged code) now unions the snapshot reads onto the 6 endpoints that load it. | 6. station draws the new calls + fnreads with existing kinds; legend text gains 'safety'. | 7. regen example map; re-pin levels/orm-access/arch-graph cases + Universe wire-count proofs.
 HONEST-EMPTY: A project with no gate-named function (fn_roles has no 'gate'), no select(Model.attr) site, and no unclaimed guard file: §3c adds no edge/node, _name_model returns what it returns today for bare names, role precedence only differs when a gate-named fn also has ops, and the config edit is gustify-loc
 SIZE: ~25 lines _a3_levels (§3c + 1-line access widening) + ~6 lines _a3_code (_name_model chain root) + 2 lines _a3_graft (precedence swap) + 2 lines gustify center.config.json + 1 legend string; batteries: tests/levels +3 cases (inbound gate hop · outbound fixpoint · honest-empty without roles), tests/orm-access +2 (select(Model.col) binds · select(x.col) on a non-model stays silent), tests/arch-graph
 COUNTS: {"guardrail_fn_sites_measured": 14, "guardrail_sites_on_stored_or_browse_paths": 10, "guardrail_sites_ai_warn_surface": 3, "guardrail_sites_list_dispatched_pipeline": 1, "guardrail_fns_drawn_as_nodes": 3, "guardrail_fns_with_gate_role": 1, "guardrail_fns_on_a_journey_chain": 2, "guardrail_fns_with_visible_reads": 1, "gate_named_callables_in_graft_apps_api": 25, "gate_named_function_nodes_homed_to_claimed_files": 5, "gate_role_nodes_drawn": 1, "fn_nodes_total": 210, "fn_edges_total": 145, "roles": {"accessor": 87, "caller": 64, "pure": 58, "gate": 1}, "gate_callers_in_wiring": 3, "gate_callers_
 RISK: Name-heuristic scope: §3c keys off _GATE_PREFIXES, so the SQL allergen wall (apply_recipe_filters / allergen_filter_conditions / ingredient_allergen_exclusions — not gate-named) stays a chain gap on explore, list, pantry overview and from-planned; wi
 RISK: Side-effect hops: the inbound rule adds 6 credit/spend-cap gate edges on the three AI endpoints (check_credits, check_spend_cap → emit_pipeline_metrics) and would fan any hub gate into every drawn caller — on gustify the max is 3 callers for one gate
 RISK: Role precedence is a change-in-place on every twin: any gate-named fn that already has ops today is currently labelled accessor and flips to gate on regen (gustify: 0 such fns measured — the flip only matters once the select-attr root lands, which is
 RISK: _name_model chain-root is the select-attr half of gap 2: it flips readers that use select(Model.col) from pure to accessor project-wide (trace-anatomy measured 117 attribute/join sites in 47 fns; this fix touches the select(Model.attr) subset only) —
 J W2 Look for recipes — GET /recipes/{recipe_id}: BEFORE endpoint GET /recipes/{recipe_id} → handler get_recipe [caller] → calls get_recipe_detail [accessor] fnreads Recipe · RecipeEquipment · RecipeIngredient · RecipeNote · RecipeStep → calls load_user_allergens [accessor] fnreads UserDietaryProfile → calls assert_ ||| AFTER endpoint GET /recipes/{recipe_id} → handler get_recipe [caller] → calls get_recipe_detail [accessor] … → calls load_user_allergens [accessor] fnreads UserDietaryProfile → calls assert_recipe_allergen_safe [gate] → + fnreads RecipeIngredient (recipes.py:220, select-attr root) → + calls load_resolution_snapshot [accessor] (config + §3c outbound) → + fnreads CanonicalIngredient (resolution.py:87, select-attr root) → + f ||| Δ the gate stops being a dead-end badge: the operator sees the allergen snapshot's three tables light up under it and the recipe's ingredient read, with the hard wall (resolution) beside the soft wall (
 J W4 Cook — POST /cooking/sessions: BEFORE endpoint POST /cooking/sessions → handler post_start_session [caller · d2w 0] → calls start_session [accessor · d2w 0] fnwrites CookingSession fnreads CookingSession · Recipe (commits) → calls seed_stage_schedule [caller · d2w 1] → calls _schedule_next [access ||| AFTER endpoint POST /cooking/sessions → handler post_start_session [caller] → calls start_session [accessor] fnwrites CookingSession … → + calls assert_recipe_allergen_safe [gate] (cooking.py:147; §3c inbound — a guard hop off a writer) → + fnreads RecipeIngredient → + calls load_resolution_snapshot [accessor] → + fnreads CanonicalIngredient · IngredientAlias · IngredientRestriction → + calls derive_restrictions [pure] → c ||| Δ the cook start shows the safety decision BEFORE the session row is written: a gate hop off start_session with four read tables under it; today the walk goes straight from the recipe read to the write
 J W8 Plan — POST /recipes/{recipe_id}/plan: BEFORE endpoint POST /recipes/{recipe_id}/plan → handler post_plan_recipe [caller · d2w 0] → calls load_user_allergens [accessor] fnreads UserDietaryProfile → calls plan_recipe [accessor · d2w 0] fnwrites PlannedRecipe fnreads Recipe (commits) → calls plan_into_cupo  ||| AFTER endpoint POST /recipes/{recipe_id}/plan → handler post_plan_recipe [caller] → calls load_user_allergens [accessor] fnreads UserDietaryProfile → calls plan_recipe [accessor] fnwrites PlannedRecipe fnreads Recipe → + calls assert_recipe_allergen_safe [gate] (recipes.py:255, extracted; §3c inbound) → + fnreads RecipeIngredient → + calls load_resolution_snapshot [accessor] → + fnreads CanonicalIngredient · IngredientAlia ||| Δ the plan write shows its guard: a gate hop between the Recipe read and the PlannedRecipe write, with the allergen snapshot tables beneath it — today plan_recipe looks like an unguarded insert
 J J4 POST /cooking/sessions: BEFORE (same as W4 step 1) ||| AFTER (same as W4 step 1: + calls assert_recipe_allergen_safe [gate] → + load_resolution_snapshot [accessor] fnreads CanonicalIngredient · IngredientAlias · IngredientRestriction · + derive_restrictions · + fnreads RecipeIngredient) ||| Δ the catalog's cook-start journey gains its safety gate step (BACKEND-JOURNEYS J4 lists the allergen assert; the map omitted it)
 J K3 POST /recipe-creation/gustify: BEFORE endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller · d2w 0] → calls generate_gustify_recipe [accessor] fnwrites RecipeCreationRequest (commits) → calls reclaim_if_abandoned [pure] → calls enqueue_unknown_ingredients [accessor] fnwrite ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller] → calls generate_gustify_recipe [accessor] fnwrites RecipeCreationRequest → + calls check_credits [gate] (ai_recipes.py; §3c inbound side-effect — gate-named, homed) → + calls check_spend_cap [gate] (same) → + calls emit_pipeline_metrics (§3c outbound from both gates) → + reads_from CanonicalIngredient · IngredientAlias · IngredientRestrict ||| Δ the credit gate appears as two gate hops off the LLM writer (a bonus of the same rule), and the snapshot's three tables join the endpoint's rollup; the pipeline's allergen_filter stage remains invisib
 UNCHANGED: W1 Initial setup — POST /con, W2 Look for recipes — GET /r, W2 Look for recipes — GET /r, W3 Filter recipes — GET /tax, W4 Cook — PATCH …/stage · PA, W5 Store ingredients — POST , W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /shopping/fro, J1 POST /setup/complete, J2 DE
 --- REFUTER: wrong checks:
   ✗ _a3_code.py:790 _name_model bare-Name only; :808-810 the select() arg loop; :968-991 function_insight iterates ENTITY_CODE files only ⇒ At HEAD the lines are 927-928 (`_name_model`: `arg.id if isinstance(arg, ast.Name) and arg.id in m2t else None`), 944-950 (`if bare in _ORM_READ_CORE: for a in n.args: _put(_name_model(a), 'r')`), 1105-1115 (def function_insight; `for slug in ENTITY_CODE`). The cited numbers resolve correctly only a
   ✗ _a3_levels.py:257 handler-only rule · :292 `_dt is None` skip · :306 insertion point before the _fedges filter · :356-359 access attached only for role=='accessor' ⇒ At HEAD: 263 `if c["s"] not in _handlers: continue`, 298 `if _dt is None or (_ds > 0 and _dt > _ds - 1): continue`, 309 `_fedges = [e for e in _fedges if …]`, 362-365 `if _role == "accessor": … _node["access"] = _acc`; `_fn_roles` read at 329. All four lines exist verbatim at HEAD~3 at the cited num
   ✗ today_draws: 210 fn_nodes / 145 fn_edges; roles accessor 87 · caller 64 · pure 58 · gate 1; gate = fn_nodes[167]; graft dropped unmapped_file 9631 / intra_entity 5156 / cross_calls 421 ⇒ Committed example map at HEAD (templates/center/shell/example/codebase-graph-station/levels.json, also gustify docs/site/center/levels.json): 219 fn_nodes / 150 fn_edges; accessor 91 · caller 69 · pure 58 · gate 1; gate index 176; c4-graph.js stats.graft.dropped unmapped_file 9679 · intra_entity 516
   ✗ W1/J1 unchanged — 'none of the four handlers reaches a guardrail fn in the wiring' ⇒ POST /setup/complete's reach set contains apps/api/services/ownership.py#ensure_principal_household (gate-named, :89) — a real guard with Household/Membership reads and writes. The journey IS unchanged under the finder's fix (ownership.py stays unclaimed → not in fn_slug → no §3c edge), but the stat
   ✗ 25 gate-named callables graft indexes under apps/api; 5 homed to claimed files ⇒ Using the generator's own _is_gate_name over wiring nodes kind∈{function,method} with id prefix apps/api: 12 (11 functions incl. 2 test fns + 1 method AuthContext.require_household); 14 if classes are counted. The 5 homed functions match: ensure_verifier_ready, check_credits, check_spend_cap, ensure
   ✗ resolution.py = 5 functions + 3 methods ⇒ wiring: 5 functions (derive_restrictions, load_resolution_snapshot, resolve_candidate, snapshot_from_taxonomy, suspected_allergens) + 4 methods (CandidateResolution.unknowns, ResolutionSnapshot.resolve, ResolutionSnapshot.violations_for, ResolvedIngredient.unverified) + 3 classes
   ✗ Fix sizing: '+4 rollup reads on 3 endpoints (+2 on 3 AI endpoints)' ⇒ GET /recipes/{recipe_id} already carries reads_from RecipeIngredient (from get_recipe_detail) → +3 there, +4 on the two writers; the AI endpoints' rollups carry none of CanonicalIngredient/IngredientAlias/IngredientRestriction → +2 only in the config-only state, +3 once the _name_model root-walk lan
   CORR: Every _a3_code.py / _a3_levels.py line cite and every map count in the finding is from HEAD~3 (before a81fb42 'schema homing'). At HEAD: _name_model = _a3_code.py:927-928 (not 790); select loop :944-950 (not 808-810); function_insight :1105-1115 (not 968-991); levels handler rule :263 (not 257), `_dt is None` :298 (not 292), §3c insertion point = after :306 → before `_fedges = [...]` at :309, acce
   CORR: wiring callers of the gate = 4, not 3: the 3 real callers plus tests/test_stored_allergen.py#test_assert_safe_passes_clean_recipe_C1101 (unmapped, harmless). plan_recipe→gate is `extracted`, not inferred (the finder's W8 AFTER row says extracted; the today_draws prose does not).
   CORR: gate-named callables under apps/api = 12 (11 functions + 1 method) by the generator's own _is_gate_name, not 25; resolution.py has 4 methods, not 3.
   CORR: W1/J1 delta reason is false: POST /setup/complete DOES reach a gate-named guard in the wiring — services/ownership.py:89 ensure_principal_household (select(Household), select(Membership), 2× session.add). Unchanged under the fix only because ownership.py is UNCLAIMED (same cause as resolution.py); and because it HAS ops, claiming it without the precedence swap would label it accessor and §3c would
   CORR: K3 BEFORE omits two drawn h1 hops (_gustify_response, _raise_for_pipeline_error); W2 explore BEFORE omits compute_recipe_availability [pure · pantry]; W2 GET /recipes BEFORE abbreviates 5 pure hops with '…'.
   CORR: Sizing '+4 rollup reads on 3 endpoints (+2 on 3 AI endpoints)': GET /recipes/{recipe_id} gains +3 (RecipeIngredient already rolled up); the AI endpoints gain +2 config-only but +3 once seam 2 (_name_model root-walk) lands — the finder's own bundle.
   CORR: layout.js gate description string is at :1972 (not 1967); the access-wire builder is :267-269.
   CORR: The journey table under-reports the finder's OWN seam 2: the _name_model root-walk newly binds 35 claimed fns (91 sites), several ON journeys marked 'unchanged' — W2 explore (build_cooked_ledger ×3 sites, explore_recipes, compute_recipe_availability flip pure→accessor with fnreads), W2 detail (get_pantry_ingredient_codes, get_user_flags_map, recipe_cooked_by_user, recipe_planned_by_household flip)
   MISSING: K3's SSE tail is GET /recipe-creation/gustify/stream (the task defines K3 as 'credit gate → LLM → persist; SSE tail'): its drawn h1 stream_gustify_events gains +calls check_credits / check_spend_cap (§3c inbound) and, after config, +reads_from CanonicalIngredient·IngredientAlias·IngredientRestrictio
   MISSING: The credit gate's decision inputs are the SAME unclaimed-file failure the class is about: check_credits → assert_credits_available (services/ai_credits.py) and check_spend_cap → assert_under_spend_cap (services/ai_spend.py) are both UNCLAIMED, so after §3c the K3 gate hops are dead-end badges exactl
   MISSING: gustify has a literal apps/api/guardrails/ package (guardrails/novelty.py: passes_mode, filter_candidates, build_cooked_ledger, rerank_by_preferences — the explore novelty guardrail, claimed under recipe, drawn as 4 pure nodes on GET /recipes/explore). The finder's guardrail census (14 sites) never 
   MISSING: POST /setup/complete (W1/J1) reaches ensure_principal_household (ownership.py:89, unclaimed, HAS ops) — an ownership guardrail the census skipped; it is the one gustify fn where seams 1 and 4 interact (claim the file and, without the precedence swap, it draws as accessor and §3c skips it).

######## reference-data  [high]  tag=config+additive  REFUTED=True
TODAY: Nothing of the reference layer: 0 of 46 top-level reference fns (catalogs.py 31 · fuzzy.py 8 · normalize.py 2 · resolution.py 5 · safety_warnings.py 0; 1,151 lines) appear in the committed c4-graph.js or levels.json (0 string hits for apps/api/reference in either). Its callers are drawn without the hop: 66 production graft call edges into reference from 54 caller fns in 27 files (20 of those files config-claimed, carrying 57 edges); 23 of those edges start at a fn ALREADY drawn today (15 sources → 16 reference targets), 9 of them handler-direct (search_recipes ×7 catalogs, resolve_batch ×2 fuzzy). The models the layer reads ARE drawn (recipe/CanonicalIngredient, recipe/IngredientAlias, aller
 WHY: No file under apps/api/reference/ is claimed by any entity, and `reference` is not a declared code layer — so the archmap never lists these files and every emitter that homes by file drops them.  ev=gustify docs/site/center/center.config.json: 0 matches for "apps/api/reference (372 files claimed under code.*; the only 'reference' hits are models/schemas/preferences.py); gustify center.config.json:103-110 code_layers = [api, services, models, schemas, web, mobile] — no `reference`; templates/cen
 WHY: derive_functions homes graft function nodes by file→entity; a reference/* fn gets no slug and is dropped, and every `calls` edge into it is dropped because both ends must be homed (graft has 46 such function nodes and 161 inferred calls into them).  ev=templates/center/generators/_a3_graft.py:180-182 `slug = f2s.get(f) / if slug: fn_slug[n['id']] = slug` (reference files → None); templates/center/generators/_a3_graft.py:188 `if s in fn_slug and t in fn_slug and s != t:` — the 161 edges (all confidence=inferred) fail the `t in fn_slug` test; templa
 WHY: Even with the files homed, the levels trace would still hide most reference hops: fn_edges draws only handler-rooted calls plus the d2w write-gradient, and every reference fn is pure/read-only (no distance_to_write), so a mid-chain reference call is skipped.  ev=templates/center/generators/_a3_levels.py:252-257 'Only calls WHOSE SOURCE IS A HANDLER are drawn' / `if c['s'] not in _handlers: continue` — only api/pantry.py#resolve_batch and api/recipes.py#search_recipes call reference directly (9 of the 23 one-hop edges); templates/center/generators/_a3_levels
 WHY: The `behind` pill already counts the reference call-tree (noise-filtered only, not slug-filtered), so the operator sees mass the walk cannot show — e.g. POST /pantry/resolve-batch behind=15 fns, chain draws 2; POST /recipe-creation/manual behind=11, draws 2.  ev=templates/center/generators/_a3_graft.py:212-213 _behind_context keeps every callable non-noise node (no f2s filter) — contrast derive_functions :180; chain.py 'POST /pantry/resolve-batch': behind=15 fns/depth 3 · fns drawn on chain: 2 · handler resolve_batch drawn PURE although it reads 3 tables vi
 SEAM center.config: gustify center.config.json: add `"reference"` to code_layers (:103-110) and claim the five files — resolution.py + safety_warnings.py under `allergen` (owner of IngredientRestriction/reconciliation.py), catalogs.py + normalize.py under `recipe` (owner of CanonicalIngredient/IngredientAlias/Taxonomy*), fuzzy.py under `pantry` (only caller api/pantry.py#resolve_batch) — as `code.reference: [...]` rows. Effect with NO suite change: code_map (_a3_code.py:380) lists them → function_insight walks them
 SEAM _a3_levels: §3c REFERENCE REVEAL after the §3b d2w loop (~L305, before `_fedges = [e for e in _fedges if …]`): one BFS layer only — for every fn already in drawn_fn, for each graft call s→t where FI.get(t.replace('#','::',1)).get('layer') in REVEAL_LAYERS (default {'reference'}, overridable from CFG), add t to drawn_fn (homed by c['ts']) and append the {s,ss,t,ds,rel:'calls',conf} edge if not already in _have. No transitive walk into reference internals (match_line→_score etc. stay hidden_fns — a floor). Ad
 SEAM _a3_code: No code change required if the twin declares the layer in code_layers; hardening only: extend the _CODE_LAYERS default (:67-68) with 'reference' so a twin that omits code_layers still gets the layer glob, and let the layer's tab banner (icon map near :70-80) render for it.
 SEAM station(adapter/layout/card): Nothing: fn nodes carry kind:function + role (pure/accessor badge exists, layout.js:1964 __BADGE_COL.role.pure #8794ab); wires ride the existing `calls` REL2KIND bucket; adapter.js:73 takes layer from KINDS[kind], not from the fn. Optional polish: a card line 'reference layer' when n.layer==='reference'.
 SKETCH: 1. config: code_layers += 'reference'; 5 files claimed under allergen/recipe/pantry as code.reference rows. | 2. build: code_map globs the layer → function_insight walks reference fns (roles: 45 pure, load_resolution_snapshot accessor w/ 3 reads) → derive_functions homes 46 fns; calls with both ends homed survive (:188). | 3. levels §3 (existing): handler-direct reference calls draw (search_recipes ×7, resolve_batch ×2). | 4. levels §3c (new): for s in drawn_fn, for s→t in graft calls, if FI-layer(t) ∈ REVEAL_LAYERS: draw node t + edge s→t (one hop, no walk into reference internals). | 5. chain BFS on the station now walks handler → … → reference fn → (for load_resolution_snapshot) fnreads CanonicalIngredient/IngredientAlias/IngredientRestriction. | 6. hidden_fns drops by the drawn count; behind pills unchanged (already counted them).
 HONEST-EMPTY: A project whose config declares no `reference` layer (or whose function_insight carries no fn with layer=='reference') takes zero iterations in §3c: drawn_fn, fn_edges, fn_nodes, hidden_fns and every c4-graph.js/levels.json byte are identical to today (the existing tests/levels fixture has layers ap
 SIZE: config: +1 code_layers entry + 5 file rows (+1 for batch_resolve.py) in gustify center.config.json; _a3_levels.py: ~22 lines (§3c loop + REVEAL_LAYERS constant; file 631 → ~655, under budget); _a3_code.py: 1-line default extension (optional); tests/levels/run.sh: +1 FIRE case (fixture fn with layer 'reference' called from a drawn non-handler → 1 new edge) + 1 SILENT case (no reference layer → byte
 COUNTS: {"reference_py_files": 5, "reference_lines": 1151, "reference_top_level_fns": 46, "reference_all_defs_incl_methods": 54, "reference_top_level_by_file": {"catalogs.py": 31, "fuzzy.py": 8, "normalize.py": 2, "resolution.py": 5, "safety_warnings.py": 0}, "graft_reference_nodes": {"function": 46, "method": 8, "class": 13, "file": 6}, "config_files_claimed_total": 372, "config_reference_files_claimed": 0, "config_code_layers": ["api", "services", "models", "schemas", "web", "mobile"], "graft_calls_into_reference_all": 161, "graft_calls_into_reference_confidence_inferred": 161, "graft_imports_into_r
 RISK: Layer semantics: claiming reference/* under `services` would home the fns with NO suite change but mislabels them and gives §3c nothing to key on; the honest path needs `reference` in code_layers, which the current _CODE_LAYERS default (_a3_code.py:6
 RISK: Entity ownership is a judgment: catalogs.py serves 4 entities (recipe · pantry · settings · progression); first-claim-wins (_a3_graft.py:161 f2s.setdefault) means its 31 fns colour as one entity and every other consumer draws a cross-entity calls wir
 RISK: §3c is one hop by design: 2-hop residuals stay hidden (resolve_candidate behind build_candidate_resolutions; derive_restrictions behind stages.allergen_filter; storage_method_codes behind _resolve_storage_method on the chain; region_members behind ap
 RISK: 18 of the 54 production callers are METHODS (schema validators, ExplorationBias.from_lists, MatchCorpus.from_entries) — dropped at _a3_graft.py:175 regardless; the W1 write gate (setup's allergen/exploration allowlists) stays invisible until a valida
 J W2 Look for recipes — GET /recipes: BEFORE endpoint GET /recipes → handler search_recipes [caller] → calls search_recipes._norm_codes [pure] → calls build_score_input · load_user_match_context · match_score · compute_recipe_availability · get_user_cooked_recipe_ids · get_user_flags_map · get_user_equip ||| AFTER endpoint GET /recipes → handler search_recipes [caller] → + calls allergen_codes [pure · reference] (api/recipes.py:326) — handler-direct, config-only → + calls preference_codes (api/recipes.py:337) → + calls curiosity_technique_codes (api/recipes.py:349,394) → + calls equipment_codes (api/recipes.py:371) → + calls taste_axis_codes (api/recipes.py:416,506) → + calls texture_codes (api/recipes.py:445,477) → + calls te ||| Δ the facet-validation fan (7 catalog readers) + the code normalizer appear as pure satellites of the search handler: the walk shows WHY unknown facet codes are rejected before the query runs; behind=48
 J W2 Look for recipes — GET /recipes/{recipe_id}: BEFORE endpoint GET /recipes/{recipe_id} → handler get_recipe [caller] → calls get_recipe_detail [accessor] fnreads Recipe · RecipeEquipment · RecipeIngredient · RecipeNote · RecipeStep → calls assert_recipe_allergen_safe [gate · no access] → calls load_user_allergen ||| AFTER endpoint GET /recipes/{recipe_id} → handler get_recipe [caller] → calls assert_recipe_allergen_safe [gate] → + calls load_resolution_snapshot [accessor · reference] (services/recipes.py:225 → reference/resolution.py:82) — §3c → + fnreads CanonicalIngredient · IngredientAlias · IngredientRestriction (reference/resolution.py:86-93) — function_insight access once the file is walked → + calls derive_restrictions [pure ·  ||| Δ the map's only gate finally reads: the allergen wall shows its three-table snapshot + the derivation step instead of an empty gate badge (the prior audit's 'the one gate-role fn reads nothing' closes 
 J W4 Cook / J3 — POST /cooking/sessions/{session_id}/complete: BEFORE endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete [caller] → calls complete_session [accessor] fnwrites CookingSession · DishHistoryEvent · IngredientHistoryEvent · PantryItem fnreads CookingSession · Recipe · RecipeIngredient (comm ||| AFTER endpoint POST /cooking/sessions/{session_id}/complete → handler post_complete [caller] → calls complete_session [accessor] … → + calls storage_method_shelf_life [pure · reference] (services/cooking.py:604 → reference/catalogs.py:252) — §3c → calls clear_pending_cooking_timer_notifications [accessor] fnwrites Notification ||| Δ the stored-portion PantryItem write shows the shelf-life lookup that computes its expires_at; residual: _resolve_storage_method → storage_method_codes (cooking.py:603 → :31) becomes a drawn off-chain 
 J W5 (adjacent, same useBatchAdd screen, not in the curated li: BEFORE endpoint POST /pantry/resolve-batch (behind=15 fns/depth 3) → handler resolve_batch [pure] → calls resolve_batch._label [pure] ||| AFTER endpoint POST /pantry/resolve-batch → handler resolve_batch [pure → caller once batch_resolve.py is claimed] → + calls parse_quantity_line [pure · reference] (api/pantry.py:154 → reference/fuzzy.py:118) — handler-direct, config-only → + calls match_line [pure · reference] (api/pantry.py:157 → reference/fuzzy.py:211) — handler-direct, config-only → + calls build_match_corpus [accessor] fnreads CanonicalIngredient · In ||| Δ the heaviest reference consumer stops being a 'pure' two-node stub: the operator sees the deterministic matcher (parse → match) and, with batch_resolve.py claimed, the three corpus reads behind it; dr
 J J3 POST /cooking/sessions/{session_id}/complete: BEFORE (see W4 complete row) ||| AFTER + calls storage_method_shelf_life [pure · reference] (services/cooking.py:604) — §3c ||| Δ as W4 complete: +1 pure hop feeding the PantryItem expires_at; storage_method_codes drawn off-chain via _resolve_storage_method
 J J7 POST /recipe-creation/manual: BEFORE endpoint POST /recipe-creation/manual (behind=11 fns/depth 6) → handler post_create_manual [caller] → calls create_recipe_manual [accessor] fnwrites Recipe · RecipeCreationRequest · RecipeIngredient · RecipeStep fnreads Recipe · RecipeCreationRequest (commits) ||| AFTER endpoint POST /recipe-creation/manual → handler post_create_manual [caller] → calls create_recipe_manual [accessor] … → + calls normalize_ingredient_code [pure · reference] (services/recipe_creation.py:121 → reference/normalize.py:27) — §3c; the RecipeIngredient.ingredient_code write is normalized here (trim+NFC+lower) → (residual, not this class: create_recipe_manual → derive_time_code/compute_time_effort/compute_pr ||| Δ answer to 'does manual recipe resolve ingredients?': it NORMALIZES codes (one pure hop) but does NOT resolve against the snapshot — the walk now shows the normalizer between the handler and the Recipe
 J K3 POST /recipe-creation/gustify (credit gate → LLM → persis: BEFORE endpoint POST /recipe-creation/gustify (behind=47 fns/depth 5) → handler post_create_gustify [caller] → calls _gustify_response · _raise_for_pipeline_error [pure] → calls generate_gustify_recipe [accessor] fnwrites RecipeCreationRequest (commits) → calls recla ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller] → calls generate_gustify_recipe [accessor] … → + calls load_resolution_snapshot [accessor · reference] (services/ai_recipes.py:350 → reference/resolution.py:82) + fnreads CanonicalIngredient · IngredientAlias · IngredientRestriction — §3c → calls enqueue_unknown_ingredients [accessor] fnwrites IngredientReconciliationQueue → + calls sanitiz ||| Δ the LLM path shows its DB allergen source (the snapshot's three reads) before the pipeline and the raw-text sanitizer before the reconciliation-queue write; the in-pipeline derive_restrictions gate st
 UNCHANGED: W1 Initial setup — POST /con, W2 Look for recipes — GET /r, W3 Filter recipes — GET /tax, W4 Cook — POST /cooking/sess, W4 Cook — PATCH …/stage · PA, W5 Store ingredients — POST , W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /recipes/{rec, J1 POST /setup/complete, J2 DE
 --- REFUTER: wrong checks:
   ✗ _a3_code.py:979-985 function_insight builds file_layer/file_entity from collect_entity_map(slug).files; :1001-1002 'fn walk stays on the MAPPED trees only' ⇒ those lines are the non-ORM SINK floor (:979-985 is _sink_categories' recv/attr matching). function_insight is at :1105; file_layer/file_entity at :1112-1122; the 'MAPPED trees only' comment is :1139. Substance right, citations wrong by ~130 lines
   ✗ _a3_levels.py:252-257 handler-only rule; :292 d2w skip; §3c insertion at ~L305 before the `_fedges = [e for e in _fedges if …]` filter; file is 631 lines ⇒ actual: the 'Only calls WHOSE SOURCE IS A HANDLER' comment is :258, `if c["s"] not in _handlers: continue` is :263; `if _dt is None or (_ds > 0 and _dt > _ds - 1)` is :298; the `_fedges = [e for e in _fedges if e["s"] in drawn_fn …]` filter is :309; wc -l → 637. Logic as described, lines off by 6
   ✗ committed example map: fn_nodes 210 (accessor 87 · caller 64 · pure 58 · gate 1), fn_edges 145, hidden_fns total 2,071; 0 mentions of apps/api/reference in c4-graph.js or levels.json ⇒ HEAD a81fb42 (2026-08-27 13:14, schema homing) levels.json: fn_nodes 219 (accessor 91 · caller 69 · pure 58 · gate 1), fn_edges 150, entities[].counts.hidden_fns sums to 2,067. The finder's numbers are exactly commit 497b535's (`git show 497b535:…levels.json` → 210/87/64/58/1, 145, 2071) — one commi
   ✗ load_resolution_snapshot gets its 3 reads (CanonicalIngredient · IngredientAlias · IngredientRestriction, resolution.py:86-93) once the file is walked → role accessor ⇒ _a3_code.py:924-925 `_name_model` returns a model ONLY for an `ast.Name` arg; :946-948 `if bare in _ORM_READ_CORE: for a in n.args: _put(_name_model(a), 'r')`. resolution.py:87 is `select(CanonicalIngredient.code)` — an ast.Attribute (verified by AST scan) → no op. :91 `select(IngredientAlias)` and 
   ✗ W5-adjacent POST /pantry/resolve-batch AFTER: `+ calls build_match_corpus [accessor] fnreads CanonicalIngredient · IngredientAlias · Recipe (batch_resolve.py:29,36,45)` and `resolve_batch [pure → call ⇒ all three selects are column-attribute selects: :29 `select(CanonicalIngredient.code, CanonicalIngredient.display_es)`, :36 `select(IngredientAlias.canonical_code, IngredientAlias.alias_normalized)`, :45 `select(Recipe.id, Recipe.title)` — AST scan shows 6 Attribute args, 0 Name args → `_orm_access`
   ✗ W4 start BEFORE sequence as quoted by the finder ⇒ chain.py prints h1 `_attach_photos [caller · extracted]` and h1 `concurrent_cook_cap_for_tier [pure · inferred]` beside start_session; the finder's before omits both (journey is unchanged either way)
   ✗ trace-anatomy.txt:44 cites 9,631 unmapped_file drops ⇒ the 9,631 figure is at trace-anatomy.txt:45 ('graft dropped 9,631 calls as unmapped_file'); :44 is the 95-of-150 backend-files paragraph
   CORR: load_resolution_snapshot 'gets its 3 reads' → 2 reads: _a3_code.py:924-925 `_name_model` accepts only an ast.Name inside select(); resolution.py:87 `select(CanonicalIngredient.code)` is an Attribute and is invisible; :91 IngredientAlias and :94 IngredientRestriction are seen. Drop `CanonicalIngredient` from the AFTER of GET /recipes/{recipe_id}, K3, and sketch step 5.
   CORR: W5-adjacent POST /pantry/resolve-batch AFTER '+ calls build_match_corpus [accessor] fnreads CanonicalIngredient · IngredientAlias · Recipe' → build_match_corpus draws as [pure] with NO fnreads: all three selects (batch_resolve.py:29,36,45) are column-attribute selects, `_orm_access` returns {}, derive_fn_roles :375 needs ops for accessor. 'resolve_batch [pure → caller]' → stays [pure] (reverse-BFS
   CORR: committed-map counts (fn_nodes 210 · accessor 87 · caller 64 · pure 58 · gate 1 · fn_edges 145 · hidden 2,071) → HEAD a81fb42: 219 · 91 · 69 · 58 · 1 · 150 · 2,067 (the finder's are commit 497b535's, one regen stale).
   CORR: generator citations: _a3_code.py:979-985 → :1112-1122; :1001-1002 → :1139; _a3_levels.py:252-257 → :258-263; :292 → :298; '~L305' → :309; a3_levels_lines 631 → 637; _a3_graft.py:175 → :176, :180-182 → :181-183, :188 → :189, :497-499 → :496-498; trace-anatomy.txt:44 → :45.
   CORR: source citations: reference/resolution.py:86-93 → :87/:91/:94; 'reference/catalogs.py:31' → cooking.py:31 (`allowed = storage_method_codes()`; catalogs.py:31 is `code: str`, storage_method_codes is catalogs.py:221); 'api/me.py' → api/setup.py:207 (GET /me) / :216 (DELETE /me).
   CORR: W4 start BEFORE misquotes chain.py: add h1 `_attach_photos [caller · extracted]` and h1 `concurrent_cook_cap_for_tier [pure · inferred]`.
   CORR: production caller counts 66 edges / 27 files / 54 fns / 7 unclaimed → noise-filtered 64 / 25 / 52 (34 fn · 18 method) / 5 unclaimed (alembic/versions/0020…, api/equipment.py, scripts/reconcile_ingredients.py, services/batch_resolve.py, services/meal_plan.py); the two docs/site + templates force-graph.min.js copies were counted as production.
   CORR: §3c sketch `FI.get(t.replace('#','::',1)).get('layer')` → `FI.get(…, {}).get('layer')` — a graft-only fn with no function_insight entry (tests/levels/run.sh:86 `api/users.py#helper`, and every TS fn) returns None and would raise, breaking the honest-empty claim on the existing fixture.
   MISSING: GET /recipe-creation/gustify/stream — the SSE tail the finder folded into K3 — was never chained. `stream_gustify_events` IS drawn today (chain.py: h1 [caller · d2w=1] under stream_gustify, 6 fns, behind 48), so §3c adds `+ calls load_resolution_snapshot [accessor · reference] (services/ai_recipe_st
   MISSING: Rollup (reads_from) wires change on journeys the finder marked 'unchanged': derive_endpoint_access (_a3_graft.py, `_behind_context` substrate — every callable, not slug-filtered) unions faccess across the handler's whole call-tree, so once resolution.py is claimed the endpoint rollup gains `reads_fr
   MISSING: The `select(Model.col)` blindness of `_orm_access` (:924-925) is a coverage gap in its own right that this class trips over: 4 of the 6 reference/batch_resolve selects are column selects, and today's chain already shows the symptom (get_user_cooked_recipe_ids, get_pantry_ingredient_codes drawn [pure
   MISSING: Two handler-direct reference consumers live in unclaimed api files and were not surfaced as class-1 overlap: api/equipment.py#put_equipment/_response → equipment_codes (PUT /equipment, api file unclaimed per trace-anatomy:44) and services/meal_plan.py#get_current_plan → load_resolution_snapshot + de
   MISSING: relief_accept → load_resolution_snapshot (ai_recipes.py:511) is drawn today per the one-hop measurement; its endpoint (the relief/accept route) was not chained or named among the changed journeys.

######## feature-flags  [high]  tag=config+additive  REFUTED=False
TODAY: Nothing flag-shaped. The 3 drawn recipe-creation doors (POST /recipe-creation/manual · POST /recipe-creation/gustify · GET /recipe-creation/gustify/stream) each carry the same 3-entry middleware list as every other endpoint ([get_auth_context gate · get_session · get_settings], gate_endpoints 67/67), a `status` of 200/201 from the decorator, and their handler chains: manual = 2 fns (post_create_manual → create_recipe_manual, writes Recipe/RecipeCreationRequest/RecipeIngredient/RecipeStep, commits), gustify = 6 fns (behind 47/depth 5), stream = 6 fns (behind 48/depth 6). The 4th walled route (WS /recipe-creation/gustify/ws) is not an endpoint node at all; the 2 seed routes (e2e_seed.py:63, :1
 WHY: The endpoint middleware floor reads ONLY the route surface (decorator dependencies=[], signature Depends()/Annotated, non-route decorators) and never the handler BODY, where every feature-flag wall lives as `if not (FLAG or settings.flag): raise HTTPException(403)`.  ev=gabe_lens/templates/center/generators/_a3_code.py:934-967 (_endpoint_middleware: 3 sources, all on node.args / decorator_list); gabe_lens/templates/center/generators/_a3_code.py:859-863 (design note: 'C1's role BFS walks direct calls in the handler BODY; the framework injects Depends() BEFORE the bo
 WHY: The flag NAME is actually collected — parse_endpoints gathers every bare Name in the handler body into `refs`, so RECIPE_CREATION_ENABLED is in the set — then `refs` is popped and intersected with model/schema class names only; a flag matches nothing and is discarded.  ev=gabe_lens/templates/center/generators/_a3_code.py:181 (refs = every ast.Name in the handler); gabe_lens/templates/center/generators/_a3_code.py:421 (`_refs = e.pop("refs")` — consumed for endpoint↔type links only); gustify/apps/api/api/recipe_creation.py:18 (`from constants import RECIPE_CREATION_EN
 WHY: The endpoint's `status` field records only the decorator's literal status_code; no generator scans for `raise HTTPException(...)` / ast.Raise, so a 403/404 wall inside the body has no carrier.  ev=gabe_lens/templates/center/generators/_a3_code.py:178-179 (status from the decorator kw only); grep 'HTTPException|ast.Raise|Raise\b' over _a3_code.py + _a3_levels.py → 0 hits outside the docstring at :145 (measured); gustify/apps/api/api/e2e_seed.py:59-60 (`_require_seed_controls` → 404 when seed_c
 WHY: `get_settings` — the ONE dependency that names the flag reader — is deliberately filed as a non-gate resource dep, and the gate signal is saturated anyway: all 67 endpoints carry get_auth_context, so gate_endpoints=67/67 cannot single out the 3 walled doors.  ev=gabe_lens/templates/center/generators/_a3_code.py:866 ('NB: no session/settings — get_session/get_settings are resource deps, not guards'); gabe_lens/templates/center/shell/example/codebase-graph-station/c4-graph.js stats: middleware_endpoints=67, gate_endpoints=67 of 67 endpoint nodes (measured by 
 WHY: There is no flag CENSUS at all: the AST pass knows endpoints, schemas, models, the model census and functions — nothing reads a pydantic Settings class's bool fields or a module's `Final[bool]` constants; both flag-definition files are also unclaimed by center.config.json.  ev=gabe_lens/templates/center/generators/_a3_code.py:143 (parse_endpoints), :199 (parse_schemas), :934 (_endpoint_middleware), ~:970 (function_insight) — the pass inventory; no parse_flags; gustify/apps/api/config.py:104,112 + rate_limit_enabled/ai_spend_cap_enabled (4 bool fields on Settings, AST-meas
 WHY: The frontend half IS drawn but at file identity: the featureFlags module piece carries 5 `imports` edges from its readers, with `exports:[HOUSEHOLD_JOIN_ENABLED, RECIPE_CREATION_ENABLED]` on the piece — the edge does not say WHICH flag the importer reads, and no wire crosses to the backend endpoints the flag mirrors ('keep the two in lockstep').  ev=gabe_lens/templates/center/shell/example/codebase-graph-station/c4-graph.js fe.pieces[1033] = fe:apps/web/src/lib/featureFlags.ts (kind module, home app-shell); 5 in-edges kind imports from setupModel.ts, CookingLayouts.tsx#DesktopCookingLayout, #DeviceCookingLayout, CookingRecipesSection.tsx#Recipe
 WHY: App-level walls have no carrier either: the RateLimitMiddleware (429 on 7 sensitive prefixes + a global window, keyed on rate_limit_active) is mounted with add_middleware, and the seed router is included only when the flag is on — no generator reads add_middleware/include_router.  ev=gustify/apps/api/main.py:127 (add_middleware(RateLimitMiddleware)), :174-177 (`if settings.seed_controls_enabled: include_router(e2e_seed.router)`); gustify/apps/api/middleware/rate_limit.py:27-39 (SENSITIVE_PREFIXES: setup/complete, recipe-creation, cooking/sessions, pantry/reset/apply, shopping/it
 SEAM _a3_code: NEW parse_flags(repo): AST over the Settings class (pydantic BaseSettings subclass) → bool AnnAssign fields + @property bodies of the form `self.<flag> or self.is_production` (effective props), plus module-level `Final[bool]` constants → {name:{src,line,default,effective_of}}. NEW _flag_gates(fn_node, flags): every ast.If whose test references a flag leaf (bare Name ∈ constants, or `settings.<field>` ∈ Settings bools) and whose body contains ast.Raise (status literal unparsed → on_fail '403'/'40
 SEAM _a3_graph: _l2 at :610-611: beside `enode['middleware']`, copy `enode['flags'] = ep['flags']` when present. Mint ONE `flag` L2 node per flag with ≥1 reader (id `flag:<NAME>`, det = src/line/default/effective_of/readers), homed in the reader's entity when single-homed else `__config__` (the same bucket idiom as `__unclaimed__` C3 minting), + cross_edges kind `walls` flag→endpoint (and flag→fn for fn-level readers) carrying on_fail. stats at :1110: `flags: {declared, walled_endpoints, walled_fns}` — the key 
 SEAM _a3_levels: fn_nodes: pass `flags` through from function_insight onto the fn node (beside role/d2w/access) so the walk's step note can say 'gated by spend_cap_active' at check_spend_cap once that fn is drawn; fn_edges untouched. ~10 lines. NOTE: check_spend_cap is not a fn node today (levels/graft dropped it from generate_gustify_recipe's out-edges) — the K3 credit-gate hop only surfaces once the calls-floor class lands.
 SEAM station(adapter/layout/card): adapter.js:75 pass `flags:p.flags` beside middleware; KINDS/NODEDEF gains `flag` (a switch glyph, muted when default False, lit when effective in prod); card.js guardsSec (:130-137) gains a 'Flags' sub-row per entry: NAME · default OFF/ON · `→ 403` · src:line; layout.js:956 step note: 'Walled by RECIPE_CREATION_ENABLED (OFF → 403)' before the 'Gated by' clause; REL2KIND (assemble.py:15) + CONN (:507) + __CONNDESC (layout.js:1969) + __uniBeam (layout.js:320) + RELCOL (adapter.js:10) + the legend 
 SEAM _a3_fe: OPTIONAL second step — at :271 the importer's bound ident is known (`binds.get(idn)`); emit `sym` on the imports edge when the target piece is a `module` whose exports include it, so the station can draw a per-flag `reads` (CookingLayouts → RECIPE_CREATION_ENABLED, setupModel → HOUSEHOLD_JOIN_ENABLED) and a NAME-matched `mirrors` wire fe-flag ↔ be-flag (RECIPE_CREATION_ENABLED on both sides, featureFlags.ts:8). ~15 lines; skip for the first cut.
 SEAM center.config: CONFIG half, needed only for the walls that live in unclaimed files: claim apps/api/api/e2e_seed.py (seed_controls_enabled → 404 on 2 routes; also gated at include-time main.py:174) and apps/api/services/ai_spend.py (assert_under_spend_cap) — the unclaimed-files class already lists them. config.py/constants.py need NO claim: parse_flags reads them by path (Settings class discovery + `constants.py`/`*_ENABLED` Final[bool]), not via entity ownership.
 SKETCH: 1. parse_flags: Settings-class bool fields + `X or self.is_production` props + module Final[bool] constants → flag census {name: src, default, effective_of}. | 2. _flag_gates(fn): ast.If whose test leaf ∈ census AND whose body raises/closes/returns → {name, on_fail(status literal), line}. | 3. parse_endpoints attaches ep.flags (handler body ∪ route-dep bodies one level); function_insight attaches fn.flags. | 4. _a3_graph: endpoint/fn nodes carry `flags`; mint `flag:<NAME>` nodes + `walls` cross_edges (flag→endpoint/fn, on_fail on the edge); stats.flags omitted when census empty. | 5. _a3_levels passes fn.flags onto fn_nodes (no edge change). | 6. Station: `flag` kind + `walls` wire in KINDS/REL2KIND/CONN/legend; card Guards gets a Flags row; step note says 'Walled by X (OFF → 403)'. | 7. Later: _a3_fe emits the imported symbol on module imports → per-flag reads + a name-matched fe↔be `mirrors` wire.
 HONEST-EMPTY: A project with no pydantic Settings bool field, no `<flag> or self.is_production` property and no module `Final[bool]` constant → parse_flags returns {} → _flag_gates never fires → no `flags` key on any endpoint dict, endpoint node or fn node; no `flag:` nodes; no `walls` cross_edges; no `stats.flag
 SIZE: ~160 lines: _a3_code ~85 (parse_flags 35 · _flag_gates 40 · 2 attach sites 8) · _a3_graph ~30 (attach · flag nodes + walls · stats) · _a3_levels ~10 · station ~25 (adapter 1 · card 10 · layout 4 · KINDS/REL2KIND/CONN/legend ~10) · _a3_fe ~15 (optional). Batteries to re-pin: tests/arch-graph (+4 checks: fold · honest-empty byte-identical · dep-body seed flag · effective_of census; the C4 fixture at
 COUNTS: {"backend_settings_bool_flags": 4, "backend_settings_bool_flag_names": "seed_controls_enabled \u00b7 recipe_creation_enabled \u00b7 rate_limit_enabled \u00b7 ai_spend_cap_enabled (config.py, AST-measured)", "backend_effective_props": 2, "backend_effective_prop_names": "rate_limit_active (config.py:248-254) \u00b7 spend_cap_active (:256-262) \u2014 each = flag OR is_production", "backend_module_const_flags": 1, "backend_module_const_name": "RECIPE_CREATION_ENABLED Final[bool]=False (constants.py:68)", "backend_str_config_gates": 1, "backend_str_config_gate": "sandbox_user_uid (sandbox_seed.py:3
 RISK: Gate saturation repeats if the effective flags are drawn per endpoint: rate_limit_active walls every path except /healthz (rate_limit.py:108) — 66 of 67 endpoints would gain the same hop, exactly the 67/67 get_auth_context noise. Draw app-level walls
 RISK: The flag node must show the EFFECTIVE value, not only the default: rate_limit_enabled and ai_spend_cap_enabled default False but rate_limit_active/spend_cap_active are True in production (config.py:254,:262), while recipe_creation_enabled is prod-FOR
 RISK: K3's credit gate stays invisible until the calls floor draws check_spend_cap: it is not a fn node and not among generate_gustify_recipe's out-edges today (levels.json measured), so the fn-level `flags` on it has no node to land on; ai_spend.py is als
 RISK: The WS route (recipe_stream.py:161) is not an endpoint node (parse_endpoints method set, _a3_code.py:166) — 1 of the 4 recipe-creation walls stays undrawn whatever this class does; a `websocket` route kind is a separate class.
 J J7 POST /recipe-creation/manual: BEFORE endpoint POST /recipe-creation/manual · resp CreationRequestResponse · behind 11 fns/depth 6 → gates get_auth_context (gate) · get_session · get_settings → handler post_create_manual [caller · d2w=0] → calls create_recipe_manual [accessor · d2w=0 · inferred] → ||| AFTER endpoint POST /recipe-creation/manual → + walls RECIPE_CREATION_ENABLED (constants.py:68, default False) ∨ settings.recipe_creation_enabled (config.py:112, default False; prod-forbidden config.py:~300) → 403 'recipe creation is disabled' (recipe_creation.py:72-76)  [NEW wire kind `walls` from NEW node kind `flag`; NEW endpoint field `flags`] → gates get_auth_context (gate) · get_session · get_settings (now explained: ||| Δ The walk's first step gains a switch before the door: a `flag:RECIPE_CREATION_ENABLED` node wired `walls → 403` into the endpoint, the step note reads 'Walled by RECIPE_CREATION_ENABLED (OFF → 403)', 
 J K3 POST /recipe-creation/gustify (credit gate → LLM → persis: BEFORE endpoint POST /recipe-creation/gustify · resp GustifyCreationResponse · behind 47 fns/depth 5 → gates get_auth_context (gate) · get_session · get_settings → handler post_create_gustify [caller · d2w=0] → calls _gustify_response [pure · extracted] → calls _rais ||| AFTER endpoint POST /recipe-creation/gustify → + walls RECIPE_CREATION_ENABLED ∨ settings.recipe_creation_enabled → 403 BEFORE the pipeline (recipe_creation.py:235-239)  [walls, flag node] → gates get_auth_context (gate) · get_session · get_settings → handler post_create_gustify [caller · d2w=0] → calls _gustify_response · calls _raise_for_pipeline_error (unchanged) → calls generate_gustify_recipe [accessor · d2w=0 · infer ||| Δ The operator sees ONE flag node fanning `walls` into two doors (POST gustify + GET stream) plus the manual door from J7 — the 'whole surface walled' picture in a single hover — and, once the calls flo
 UNCHANGED: W1 Initial setup — POST /con, W2 Look for recipes — GET /r, W3 Filter recipes — GET /tax, W4 Cook — POST /cooking/sess, W5 Store ingredients — POST , W6 Pantry locations — POST /, W7 Shopping — POST /shopping, W8 Plan — POST /recipes/{rec, J1 POST /setup/complete, J2 DELETE /me, J3 POST /cooking/ses
 --- REFUTER: wrong checks:
   ✗ WHY-4: get_settings is filed as a non-gate resource dep and gate_endpoints is saturated at 67/67 ⇒ Non-gate part confirmed: _a3_code.py:1003 ('NB: no "session"/"settings" — get_session/get_settings are resource deps, not guards'), _is_mw_gate at :1022. The NUMBER is wrong: the committed example c4-graph.js has 72 endpoint nodes (72 unique labels, no dups, no stubs), stats.middleware_endpoints=72,
   ✗ AFTER K3 hop: `+ walls settings.spend_cap_active → SpendCapExceededError via assert_under_spend_cap … [walls, fn-level flags on check_spend_cap]` ⇒ The flag read is in assert_under_spend_cap (ai_spend.py:62 `if not settings.spend_cap_active: return`), NOT in check_spend_cap — ai_recipes.py:265-275 check_spend_cap only try/awaits assert_under_spend_cap and re-raises; it reads no flag, so function_insight's `flags` would land on assert_under_spen
   ✗ AFTER K3 hop: `+ walls settings.rate_limit_active → per-actor AI limiter in PipelineDeps (ai_recipes.py:109) [walls, fn-level]` ⇒ ai_recipes.py:109 is a kwarg (`rate_limit_enabled=settings.rate_limit_active`) inside build_pipeline_deps (def at :86), not in generate_gustify_recipe and not an ast.If — the finder's own risk row says the seam under-counts exactly this. The real wall is recipes/pipeline/stages.py:68 (rate_limit_use
   ✗ Seam: one-level walk of route-dep bodies lands e2e_seed.py:59's flag on the endpoint ⇒ e2e_seed.py:59 reads `get_settings().seed_controls_enabled` — an Attribute on a Call, not the `settings.<field>` Name.attr leaf the finder's _flag_gates specifies ('bare Name ∈ constants, or `settings.<field>` ∈ Settings bools'). As spec'd the leaf matcher misses it; the helper must key on the attri
   ✗ gustify_test_files_pinning_403_wall: 3 ⇒ Only apps/api/tests/test_recipes.py asserts the 403 (C969 :978, C971 :1055); conftest.py:223-236 monkeypatches the flag ON (no 403 assert); test_e2e_seed.py pins the seed 404 wall, not the 403. 1 file asserts the 403 wall; no stream-403 test exists (test_streaming.py has no match).
   CORR: Generator line citations are systematically off in _a3_code.py's back half: _endpoint_middleware :934-967 → actual :1071-1103; design notes :859-863 → :996-999; 'NB: no session/settings' :866 → :1003; 'nested Depends not walked' :862 → :999; function_insight ~:970 → :1105; parse_schemas :199 → :197; refs :181 → :182; status :178-179 → :177-178 + :188; method set :166 → :169. (_a3_code.py:934-967 i
   CORR: _a3_graph.py: middleware attach cited :610-611 → actual :614-615; stats middleware_endpoints/gate_endpoints cited :1110-1111 → actual :1126-1127. tests/arch-graph/run.sh middleware pin cited :436-447 → actual :462-472 (3 checks at :467-472).
   CORR: Endpoint count: claimed 67 endpoints / middleware_endpoints=67 / gate_endpoints=67 'measured by parse' → the committed example c4-graph.js holds 72 endpoint nodes (72 unique labels), stats.middleware_endpoints=72, stats.gate_endpoints=72; chain.py --all prints 72. Saturation (72/72) still supports the claim; the number and 'endpoints_drawn_total: 67' are wrong.
   CORR: K3 AFTER hop 'walls settings.spend_cap_active … fn-level flags on check_spend_cap' → the read is in assert_under_spend_cap (ai_spend.py:62); check_spend_cap (ai_recipes.py:265-275) reads no flag. And the gate is inverted (`if not flag: return` — OFF skips the cap; the raise is at :73 outside the If), so the spec'd _flag_gates would emit a `walls` edge with no on_fail and the wrong meaning. The hel
   CORR: K3 AFTER hop 'walls settings.rate_limit_active → PipelineDeps (ai_recipes.py:109) [walls, fn-level]' → :109 is a kwarg inside build_pipeline_deps (def :86), not an If, not in generate_gustify_recipe; the actual wall is recipes/pipeline/stages.py:68 (rate_limit_user) and :77 (rate_limit_ip) testing PipelineDeps.rate_limit_enabled (state.py:78) → RateLimitExceededError. The proposed seam cannot deri
   CORR: Seam leaf spec: `settings.<field>` misses e2e_seed.py:59's `get_settings().seed_controls_enabled` (Attribute on a Call); the finder claims that site lands via the dep-body walk, but only if the matcher keys on attribute name regardless of receiver.
   CORR: 'check_spend_cap is NOT a fn node and not among the out-edges' is true, but the map already carries its NAME: levels.json generate_gustify_recipe.behind.names lists check_spend_cap, assert_under_spend_cap, check_credits, build_pipeline_deps (fns 39 / depth 4) — a partial carrier the finder omitted.
   CORR: gustify_test_files_pinning_403_wall: 3 → 1 (apps/api/tests/test_recipes.py:978, :1055); conftest.py only flips the flag; test_e2e_seed.py pins the 404 seed wall.
   CORR: rate_limit.py SENSITIVE_PREFIXES: items/batch cited :39 → actual :36 (setup/complete :28, cooking/sessions :30, shopping/items :32 are right; EXEMPT :40 right).
   CORR: spend_cap_active property cited config.py:256-262 → def at :257, return at :262 (minor).
   CORR: 'grep HTTPException|ast.Raise|Raise → 0 hits outside the docstring at :145' → 0 hits, period; :145 has no such text.
   MISSING: W1 and W4 are not flag-free on the WEB side and the deltas say 'no flag read' flatly: W1's setup screen reads HOUSEHOLD_JOIN_ENABLED (setupModel.ts:14, :43 — the 'Tengo invitación' card) and W4's cooking screens read RECIPE_CREATION_ENABLED (CookingLayouts.tsx, CookingRecipesSection.tsx, CookingReci
   MISSING: The real rate-limit wall for K3's pipeline — stages.py:68/77 `if deps.rate_limit_enabled and …: raise RateLimitExceededError` over PipelineDeps.rate_limit_enabled (state.py:78), fed by build_pipeline_deps (ai_recipes.py:86, kwarg at :109) — is uncited; it is the name-collision case (a dataclass fiel
   MISSING: Flag POLARITY is unmodeled: recipe_creation.py:72 (`if not flag: raise` — flag OFF walls) vs ai_spend.py:62 (`if not flag: return` — flag OFF skips, ON arms). The proposed on_fail field cannot express the second; the census needs an arms/walls polarity or must require ast.Raise inside the If body.
   MISSING: The existing partial carrier on generate_gustify_recipe (levels.json behind.names incl. check_spend_cap / assert_under_spend_cap / check_credits / build_pipeline_deps) — the finder said the credit gate is invisible; its name is in the dossier today, only the edge is missing.
   MISSING: No K3 test evidence for the SSE wall: test_recipes.py pins 403 on manual (C969) and gustify POST (C971) only; the stream 403 (recipe_stream.py:135-139) and the WS close have no backend test — relevant to what a `walls` edge can claim as guarded vs named.

######## ai-prompts  [high]  tag=config+additive  REFUTED=False
TODAY: Zero prompt anything. On the committed gustify map (l2 kinds: schema 123 · endpoint 67 · model 55 · web 32 · external 8; levels fn_nodes 210 · fn_edges 145) the word 'prompt' appears in no node. K3's chain (chain.py POST /recipe-creation/gustify): 6 fns — post_create_gustify → {_gustify_response, _raise_for_pipeline_error, generate_gustify_recipe [accessor · commits · false http sink] → WRITES RecipeCreationRequest → {reclaim_if_abandoned, enqueue_unknown_ingredients → WRITES IngredientReconciliationQueue}}; behind=47/depth 5; schema touches GustifyCreateRequest + GustifyCreationResponse; screens none bridged. 0 of the 10 pipeline stages are drawn (build_prompt, gemini_call included), the LL
 WHY: The only prompt asset in gustify is ONE Python string constant in a one-entry registry — no prompt files/templates on disk. The AST pass reads function/class bodies only; a module-level string constant is never visited, so the template does not exist to the map.  ev=apps/api/prompts/registry.py:19 (RECIPE_SUGGESTION_V1 = "recipe_suggestion.v1"); apps/api/prompts/registry.py:77-84 (_RECIPE_SUGGESTION_TEMPLATE, 42 words, placeholders {n,mode,pantry,allergens}); apps/api/prompts/registry.py:86-91 (_REGISTRY: 1 entry)
 WHY: The consumer chain to the prompt is cut three times before the prompt is reachable: (1) the files that carry run_pipeline / StreamPipeline / the registry are unclaimed, so graft's real calls edges are dropped as unmapped_file; (2) the 10 stages run from a PIPELINE_STAGES tuple by list-dispatch, and graft resolves 0 outgoing calls from the dispatcher; (3) build_prompt→PromptTemplate.render is a cal  ev=apps/api/recipes/pipeline/stages.py:197-208 (PIPELINE_STAGES tuple, 10 async stage fns; build_prompt = stage 5, gemini_call = stage 6); apps/api/recipes/pipeline/runner.py:21-25 (for stage in stages: state = await stage(state, deps)) and apps/api/streaming/producer.py:45-46 (the SSE twin); apps/api/
 WHY: Even with the files claimed and the dispatch edges present, the levels emitter would not DRAW the stages: it draws only handler-rooted calls and write-path (d2w-descending) calls. All 10 stage fns have access {} in the archmap, so they carry no d2w; the prompt hop is a READ-path hop mid-chain — the one class the fn layer never pulls in. The endpoint's behind=47 also excludes the whole pipeline (BF  ev=templates/center/generators/_a3_levels.py:257-263 (§3: `if c["s"] not in _handlers: continue`); templates/center/generators/_a3_levels.py:269-298 (§3b: `if _dt is None or (_ds > 0 and _dt > _ds - 1): continue`); templates/center/generators/_a3_graft.py:402-420 (derive_distance_to_write: only fns tha
 WHY: The LLM call — the prompt's destination — is untagged: the sink detector reads the receiver only when it is a bare Name; `deps.client.generate_recipes(...)` has an Attribute receiver, so no sink category. Meanwhile the service wears a false `http` sink from `session.delete(req)`. So there is no external node for the prompt to point at.  ev=templates/center/generators/_a3_code.py:837-838 (recv = func.value.id only if func.value is ast.Name); templates/center/generators/_a3_code.py:851-853 ("session" in recv and attr in (…,"delete") → http); apps/api/services/ai_recipes.py:437 (await session.delete(req)) → committed levels.json fn_nodes
 WHY: The station has no kind for a prompt and no wire for fn→asset: an unknown L2 kind is dropped at the adapter; the fn layer wires only handler/calls/fnreads/fnwrites; REL2KIND/LINKMETA/legend/KINDS/GLYPH carry no prompt entry.  ev=docs/design/codebase-graph-consolidation/universe-build/parts/adapter.js:65 (`if(!KINDS[kind]){ _dropped++; return; }`); parts/layout.js:250-269 (_buildFnData: fn_nodes → function nodes; links = fn_edges calls + handler + fnreads/fnwrites only); parts/layout.js:818-822 (_bkCollect walks only calls/h
 SEAM center.config: gustify only: claim apps/api/prompts/registry.py, apps/api/recipes/pipeline/runner.py, apps/api/recipes/pipeline/state.py, apps/api/streaming/producer.py under recipe.services (4 files). Effect: generate_gustify_recipe→run_pipeline, relief_accept→run_pipeline, stream_gustify_events→StreamPipeline.events and build_prompt→get_prompt stop being dropped at _a3_graft.py:181-189 (they exist in wiring.json today).
 SEAM _a3_code: PREREQUISITE (the gap-audit '8+ list-dispatched pipelines' row, not this class): `dispatch_tables(trees)` — a module-level tuple/list literal whose elements are all bare Names bound to local defs (stages.py:197-208), plus every fn that iterates it (`for x in <name | param defaulting to name>` and calls `x(...)`: runner.py:24-25, producer.py:45-46) → synthetic edges {s: iterating fn, t: each element, rel:'dispatches', conf:'inferred'}. Without this the prompt node is an orphan (build_prompt is un
 SEAM _a3_code: THIS CLASS: `prompt_registry(trees)` beside function_insight (~L990). (a) asset: a module-level Assign/AnnAssign whose value is a str Constant / implicit-concat / JoinedStr with ≥1 `{placeholder}` AND that is consumed by a `.format(`/`.render(` call or a registry dict (registry.py:77-91) — a bare long string never qualifies (41 text() SQL sites must not fire); (b) id: the dict key str constant (`recipe_suggestion.v1`) or the assign name; (c) rendered_by: the fn that calls the registry getter (`g
 SEAM _a3_graft: graft_arm (~L471-521): fold `dispatches` edges into `functions.calls` after derive_functions (same both-ends-mapped rule L189, tagged via:'dispatch') so levels sees them like any inferred call; count in `dropped`/stats. Note: StreamPipeline.events is a METHOD — the SSE path needs the gap-3 `_CALLABLE_KINDS` relaxation at L176 or it stays undrawn; the POST path (run_pipeline, a function) does not.
 SEAM _a3_levels: build_levels after §3b (~L305): an ATTACHMENT pull — for each fn in prompt_registry.rendered_by (and nothing else, scope it), BFS the mapped calls adjacency backwards from that fn to the nearest already-drawn fn (cap 6 hops) and add that path's fns+edges to drawn_fn/_fedges (the first non-write pull rule; on gustify it pulls generate_gustify_recipe→run_pipeline→build_prompt — and, because dispatch edges fan out from run_pipeline, the other 9 stages ride in only if the operator wants the whole la
 SEAM station(adapter/layout/card): layout.js _buildFnData (L250-269): map D.prompt_nodes → nodes {kind:'prompt', __fn:true, ent:slug} and D.prompt_edges → _FNLINKS {rel:'fnprompts', access:true} so they toggle with Functions and _bkCollect (L818-822) walks them (add `why:'prompt'` so the step note reads 'renders prompt recipe_suggestion.v1'). spike-base/adapter: KINDS.prompt {col, form:'slab', type:'Prompt (LLM)', layer:'api'} + GLYPH.prompt + KINDCOL.prompt (adapter.js:18-21 web pattern). assemble.py: REL2KIND fnprompts:'access'
 SEAM _a3_code: ADJACENT, optional (the Sinks '8+' row): _detect_sinks L837-838 — read the receiver's ROOT name through attribute chains and add an `llm` category (generate_content / generate_recipes / chat.completions / messages.create), and drop `session` from the http receiver list (L851). Gives the prompt an external:Gemini destination; not required for the node.
 SKETCH: 1. AST: find registry dict {str-const → Call(prompt_id=, template=<module str const>)} + module str consts with {placeholders} consumed by .format/.render → prompts[] (id, file, words, placeholders, sanitized, output_schema). | 2. AST: find callers of the registry getter; resolve the id arg (const | single-entry registry) → rendered_by; else ambiguous, empty. | 3. AST (prereq): module tuple/list of local def Names + the fn that iterates and calls it → dispatches edges; graft arm folds them into calls (both ends mapped). | 4. levels: from each rendered_by fn, walk calls backwards to the nearest drawn fn (≤6 hops) → pull that path into fn_nodes/fn_edges. | 5. levels: emit prompt_nodes + prompt_edges (fnprompts) only when non-empty; tag fn_node.prompts. | 6. station: prompt kind + fnprompts wire in _buildFnData/_bkCollect; legend rows; card section. | 7. gustify config: claim registry.py, runner.py, st
 HONEST-EMPTY: A project with no registry-dict idiom and no `{placeholder}` string constant consumed by .format/.render: prompt_registry() → [] and dispatch_tables() → [] → no `prompts` field on any fn, no `prompt_nodes`/`prompt_edges` keys in levels.json, no `stats.prompts` key, no extra fn_nodes/fn_edges (the pu
 SIZE: ~60 lines _a3_code prompt_registry + ~50 lines dispatch_tables (prereq) + ~15 _a3_graft fold + ~45 _a3_levels pull+emit + ~40 station (KINDS/glyph/links/legend/card) + 4 config lines on gustify. Batteries: tests/arch-graph (+3 honest-empty pins: no-idiom byte-identical · graft-absent byte-identical · ambiguous id → not attached), tests/levels (+1 pull-path case), tests/gabe-universe (fn/wire-count
 COUNTS: {"prompt_registry_entries": 1, "prompt_ids": 1, "prompt_files_on_disk": 0, "template_words": 42, "template_placeholders": 4, "injection_tokens": 12, "max_input_len": 500, "get_prompt_call_sites_non_test": 1, "render_sites": 1, "rendered_prompt_consumers": 1, "gemini_client_impls_taking_prompt": 2, "pipeline_stages": 10, "dispatch_sites": 2, "prompt_tests_C_ids": 6, "llm_providers": 1, "other_llm_sdk_hits": 0, "config_claimed_ai_path_files": 8, "config_unclaimed_ai_path_files": 4, "wiring_nodes": 19954, "wiring_edges": 51118, "wiring_calls_run_pipeline_out": 0, "wiring_calls_StreamPipeline_even
 RISK: ROI is low and honest: gustify has exactly ONE prompt (a 42-word string), so the class yields 1 node + 1 wire; the mechanism only pays on multi-prompt apps. Its real value is as the visible payload of the list-dispatch floor that makes K3's chain hon
 RISK: The prompt_id→rendering-fn join is NOT statically resolvable on gustify (deps.prompt_id bound at ai_recipes.py:104, read at stages.py:91); the single-entry-registry shortcut binds it, but any multi-prompt project with dependency-injected ids lands `a
 RISK: Template detection false positives: i18n tables, SQL text() constants (41 sites on gustify), log format strings. The registry-dict OR `.format`/`.render`-consumed rule keeps it a floor; still verify on the twin (gastify) before pinning.
 RISK: The attachment pull is the first non-write draw rule in _a3_levels; scope it to prompts (path-only default) or fn_nodes grow on every project once sinks/others reuse it. Whole-lane mode adds 12 fns to K3 and would move the levels battery counts.
 J K3 POST /recipe-creation/gustify (credit gate → LLM → persis: BEFORE endpoint POST /recipe-creation/gustify [resp GustifyCreationResponse · behind 47/5 · screens none bridged] → gates get_auth_context · get_session · get_settings (names only) → handler post_create_gustify [caller · d2w=0] → calls _gustify_response [pure] → call ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller · d2w=0] → calls _gustify_response [pure] → calls _raise_for_pipeline_error [pure] → calls generate_gustify_recipe [accessor · commits] (ai_recipes.py:336-466) → fnwrites RecipeCreationRequest (:382-395 claim · :456-461 persist_outcome) → calls reclaim_if_abandoned [pure · d2w=0] → + calls run_pipeline [caller · recipes/pipeline/runner.py:18 ||| Δ The walk stops jumping from the service straight to its writes: between generate_gustify_recipe and persist a pipeline lane appears (path-only: run_pipeline → build_prompt; whole lane: 10 stages in D-
 UNCHANGED: W1 Initial setup [POST /cons, W2 Look for recipes [GET /re, W3 Filter recipes [GET /taxo, W4 Cook [POST /cooking/sessi, W5 Store ingredients [POST /, W6 Pantry locations [POST /p, W7 Shopping [POST /shopping/, W8 Plan [POST /recipes/{reci, J1 POST /setup/complete, J2 DELETE /me, J3 POST /cooking/ses
 --- REFUTER: wrong checks:
   ✗ AFTER role tags: run_pipeline [caller], rate_limit_user [gate], rate_limit_ip [gate] ⇒ _a3_graft.py:352 _GATE_PREFIXES = (require_, assert_, ensure_, verify_, check_, guard_, authoriz) + :357 ('authoriz'/'permission' substrings) — `rate_limit_*` matches none → 'pure'. derive_fn_roles :360-399: 'caller' = reverse-BFS reaches an accessor; run_pipeline has 0 outgoing calls in wiring and,
   ✗ Path-only pull yields 3 pulled fns; whole-lane 12 ⇒ By the finder's own rule (backward BFS from rendered_by=build_prompt to the nearest drawn fn) the path is generate_gustify_recipe (already drawn) ← run_pipeline ← build_prompt: 2 NEW fns (run_pipeline, build_prompt). get_prompt is a forward callee of build_prompt (wiring build_prompt→get_prompt), no
   ✗ Committed map counts: fn_nodes 210 · fn_edges 145 · roles accessor 87/caller 64/pure 58/gate 1 · l2 endpoint 67/model 55 · graft dropped.unmapped_file 9,631 · wiring 19,954/51,118 ⇒ HEAD a81fb42 (2026-08-27 13:14, the file chain.py reads) levels.json: fn_nodes 219 · fn_edges 150 · roles accessor 91/caller 69/pure 58/gate 1; c4-graph.js l2: endpoint 72 · model 57 · schema 123 · web 32 · external 8; stats.graft.dropped.unmapped_file 9,679. The finder's numbers match the PRIOR com
   ✗ The word 'prompt' appears in no node on the committed map ⇒ c4-graph.js L2 nodes: 0 hits (confirmed). levels.json: 0 fn_nodes, but `pieces`/`fe_edges`/`fe_scaffold` carry 3 FRONTEND nodes named DietaryChangeConfirmPrompt (component), GustifyPromptFilterSummary (component), DowngradePrompt (fe-type) — UI prompts, not LLM prompts. Zero LLM-prompt nodes stands;
   ✗ Config unclaimed AI-path files = 4 (registry, runner, state, producer); claimed = 8 ⇒ center.config.json vs the files the pipeline imports (stages.py:26-43, runner.py:12-13, producer.py, ai_recipe_stream.py:71): CLAIMED 7 (api/recipe_creation.py, services/ai_recipes.py, services/ai_recipe_stream.py, recipes/pipeline/stages.py, integrations/gemini.py, agents/recipe_agent.py, guardrail
   ✗ Seam 4: fold dispatches into functions.calls in graft_arm (~L471-521) after derive_functions ⇒ _a3_graft.py:447-525 is derive_cross (the L1 cross-entity pair counter whose dropped counters the finder cites at :498). graft_arm is :704-740; derive_functions is called at :725 and its result assembled as `"functions": fout` at :737 — the fold belongs there. Content of the seam is otherwise sound;
   ✗ tag config+additive; 'c4-graph.js bytes are untouched by design … tests/codebase-graph stay green' ⇒ True for the suite's generator changes (prompt lives in levels.json). False for gustify once seam 1 lands: claiming 4 files changes archmap function_insight (+get_prompt, sanitize_prompt_input, PromptTemplate.render, run_pipeline, StreamPipeline.*), lowers stats.graft.dropped.unmapped_file (_a3_graf
   CORR: _a3_code.py:1043-1049 'function_insight walks only defs' → function_insight is at templates/center/generators/_a3_code.py:1105, the def-only walk at :1178-1184; :1043-1049 is _dec_name.
   CORR: _a3_code.py:837-838 / :851-853 sink-receiver and http rules → _detect_sinks is :966; receiver-is-bare-Name at :974-975; the `session`+delete→http rule at :988-990. :837-838 is _file_imports.
   CORR: _a3_code.py:750-753 'the only module/class Assign handling is __tablename__/__table_args__' → :750-753 is schema-class collision bookkeeping; __table_args__ is :334-336 (class body) and model_census has its own class-body Assign at :508-512. The substance (no module-level Assign is ever read) holds.
   CORR: Seam 4 'graft_arm (~L471-521)' → that span is derive_cross (_a3_graft.py:447-525); graft_arm is :704-740, derive_functions called at :725, `functions` assembled at :737.
   CORR: AFTER role tags: run_pipeline [caller] → [pure] (0 outgoing calls in wiring; even with dispatch edges merged no stage is an accessor — derive_fn_roles _a3_graft.py:360-399); rate_limit_user/rate_limit_ip [gate] → [pure] (_GATE_PREFIXES :352 has no rate_limit_).
   CORR: '3 pulled fns (path-only)' → 2 (run_pipeline, build_prompt); get_prompt is a forward callee, drawn only under the whole-lane flag per the finder's own rule. Range is 2–12, not 3–12.
   CORR: Committed-map counts are one commit stale (they match 497b535): HEAD a81fb42 has fn_nodes 219 (not 210), fn_edges 150 (not 145), roles accessor 91 / caller 69 (not 87/64), l2 endpoint 72 (not 67), model 57 (not 55), dropped.unmapped_file 9,679 (not 9,631); wiring on disk 19,956 nodes / 51,125 edges (not 19,954/51,118). chain.py --all lists 72 labels. Chain shapes and behind values are unaffected.
   CORR: 'the word prompt appears in no node' → true for c4 L2 nodes and fn_nodes; levels.json pieces carry 3 frontend nodes DietaryChangeConfirmPrompt / GustifyPromptFilterSummary / DowngradePrompt (UI prompts). Say 'no LLM-prompt node'.
   CORR: 'config_unclaimed_ai_path_files: 4 / claimed 8' → unclaimed 8 (runner, state, producer, streaming/events.py, registry, integrations/providers.py, integrations/circuit_breaker.py, reference/resolution.py), claimed 7. The 4-file claim in seam 1 is sufficient for the prompt node but gemini_call→CircuitBreaker.record_* and allergen_filter→derive_restrictions remain dropped at _a3_graft.py:189 — the AF
   CORR: Risk 'c4-graph.js bytes are untouched by design' → only for the generator changes; seam 1 (gustify config claims) changes c4-graph.js stats.graft.dropped.unmapped_file and archmap function_insight, and levels.json recipe hidden_fns, on the next gustify regen. State it as: c4 unchanged on a no-config-change regen; gustify's c4 stats move with the claim.
   CORR: Honest-empty needs one more condition stated: the seam-4 dispatch counter must be key-conditional (like hidden_fns _a3_levels.py:319-324), or c4-graph.js gains a `dispatches: 0` on every no-idiom project.
   CORR: text() SQL sites: 42 measured (word-boundary, non-test, non-venv) vs the finder's 41 — ±1, immaterial to the false-positive argument.
   MISSING: POST /recipe-creation/{request_id}/relief-accept changes identically to K3 (relief_accept → run_pipeline at ai_recipes.py:528; wiring edge present) — the finder mentions it only in today_draws and counts journeys_changed: 1; the map delta is 2 endpoints + the SSE tail, and the relief chain (5 fns, 4
   MISSING: GET /recipe-creation/{request_id} (the poll endpoint) — not in the required list, but on the same router; it never reaches the pipeline (unchanged) and should be named so the recipe-creation quartet is fully accounted for.
   MISSING: The gemini_call hop in the AFTER stays a dead end even after seam 1: its only resolved calls go to integrations/circuit_breaker.py (unclaimed, not in the 4-file claim) and the generate_recipes call is unresolved by graft — the AFTER should say the LLM step is drawn as a node with 0 outgoing wires un
   MISSING: The stale-count problem: the finder measured the example map at 497b535; the schema-homing regen a81fb42 (today 13:14) moved fn_nodes/fn_edges/l2 counts. Any pinned battery numbers in the fix's size estimate should be re-based on HEAD.

######## orm-idioms  [high]  tag=change-in-place  REFUTED=False
TODAY: The committed gustify example (templates/center/shell/example/codebase-graph-station) draws 210 fn nodes (87 accessor · 64 caller · 58 pure · 1 gate), 206 endpoint→model access edges (stats.access_edges; gustify's live c4-graph.js at head bddf50f5 says the same 206), and 5 fn nodes wearing an `http` sink (_discard_claim · delete_location · unplan_recipe · unplan_recipe_by_recipe · generate_gustify_recipe) plus 5 endpoints carrying access.sinks=['http'] (POST /setup/complete · DELETE /pantry/locations/{id} · DELETE /planned-recipes/{id} · DELETE /recipes/{id}/plan · POST /recipe-creation/gustify) — all five are `session.delete(row)` sites, none is an HTTP call. The real gustify HTTP call (Gem
 WHY: The write symtab binds ONLY constructor-assigned locals (`x = Model(...)`) and annotated assigns; a row that comes back from a query (`cs = (await session.execute(select(CookingSession)...)).scalar_one_or_none()`), a `for row in rows` loop, a `session.get(Model, id)` result, a Model-annotated parameter, or a same-file helper's annotated return is never bound — so `session.delete(row)` on a query-b  ev=templates/center/generators/_a3_code.py:770-781 (symtab: ast.Assign with ast.Call whose bare callee is in m2t, or AnnAssign); templates/center/generators/_a3_code.py:783-788 (_arg_model: symtab lookup or inline Model(...) only); apps/api/services/cooking.py:223-230 (cs bound via scalar_one_or_none),
 WHY: There is NO attribute-assignment detector at all: the ops loop walks only ast.Call nodes, so `obj.field = value` (the unit-of-work write) never produces a 'w' op even when the same function flushes — the flush only sets `commits=True`.  ev=templates/center/generators/_a3_code.py:799-815 (`for n in ast.walk(fnnode): if not isinstance(n, ast.Call): continue` — the only op sources are session.<m>(obj), session.get, select(), insert/update/delete()); templates/center/generators/_a3_code.py:814-815 (flush/commit → commits flag only); apps/
 WHY: `_name_model` accepts only a bare ast.Name, so `select(Model.col)`, `select(func.count()).select_from(Model)`, `.join(Model, …)` / `.outerjoin(Model, …)` name no model — the reader stays op-less and derive_fn_roles labels it 'pure' (no ops, reaches no accessor).  ev=templates/center/generators/_a3_code.py:790-791 (`return arg.id if isinstance(arg, ast.Name) and arg.id in m2t else None`); templates/center/generators/_a3_code.py:808-810 (select() args fed through _name_model only; .join never inspected); templates/center/generators/_a3_graft.py:372-373, :391-398 
 WHY: The sink detector lists `session` among the HTTP-client receivers, so `session.delete(row)` (and `session.get`, `session.post`… on an ORM session) is tagged an `http` sink — 7 mapped functions today, 5 of them drawn, 0 real HTTP; the one real external call (Gemini via an SDK client) is untagged.  ev=templates/center/generators/_a3_code.py:851-853 (`"session" in recv` ∧ attr in (post,put,patch,delete,get) → cats.add("http")); templates/center/generators/_a3_graft.py:345-346, :352-353 (sinks unioned through the call-tree onto the endpoint's access block); committed example: 5 fn nodes + 5 endpoin
 WHY: One residual floor stays even after the widening: a row bound by a CROSS-FILE helper with no same-file annotation (`mode = await owned_mode(...)`, owned_mode in the unclaimed services/recipe_filter_modes.py) is still unbound, so attach_cupo/release_cupo's `mode.cupo_dish_type = …` + flush remain undrawn writes under this class alone.  ev=apps/api/services/repertorio.py:25 (import), :83-85 and :94-96 (mode = await owned_mode(...); mode.cupo_dish_type = …; flush); apps/api/services/recipe_filter_modes.py:105 (async def owned_mode — file not in center.config.json)
 SEAM _a3_code: _orm_access symtab (L770-781): add five binders — (1) `x = [await] session.execute(select(Model)…).scalar_one[_or_none]()/scalars()…` and `stmt = select(Model)…` → walk the RHS for select()/join() args and bind x:Model when exactly ONE model is named (MULTI → unbound, honest); (2) `for row in <bound rows>` → row:Model; (3) Model-annotated parameters (`cs: CookingSession`, `X | None`); (4) `x = session.get(Model, …)`; (5) `x = [await] helper(...)` where helper is a SAME-FILE def whose return anno
 SEAM _a3_code: _orm_access ops loop (L799-815): before the `if not isinstance(n, ast.Call): continue` gate, add an ast.Assign/AugAssign/AnnAssign branch — a target `ast.Attribute` whose `.value` is a bound Name → `_put(sym[name], 'w')`. Decision: gate it on flush/commit-in-same-fn (9 fns gain a write) or not (12 fns; seed_stage_schedule/persist_outcome/_refold_recipe_title become anchors like session.add already is without a flush). Recommend NOT gated — session.add(x) is a 'w' today without a flush, and `comm
 SEAM _a3_code: _name_model (L790-791) → root-of-chain: unwrap Attribute/Subscript/Call to the root ast.Name (`select(Recipe.id)`, `select(func.count()).select_from(Model)`, `.join(TreeNode, …)`/`.outerjoin(...)`) and apply it to select() args (L808-810) AND to `.join/.outerjoin/.join_from` call args (new branch). 91 + 10 sites on gustify.
 SEAM _a3_code: _detect_sinks (L851): drop `"session" in recv` from the http receiver list (keep requests/httpx/aiohttp/client). `session.delete(row)` is then a 'w' op via the symtab, never a sink. 7 false http sinks vanish; 0 real ones lost (session.get sites: 0).
 SEAM _a3_graft: derive_fn_roles (L391-398) — PRECEDENCE DECISION, not required: accessor is tested before gate, so assert_recipe_allergen_safe (the map's only gate) flips gate→accessor once its RecipeIngredient read lands. Either keep (a gate that reads is an accessor) or swap the two branches (gate name wins) — one line; the arch-graph C1 fixture (tests/arch-graph/run.sh:438-447) pins the current order.
 SEAM station(adapter/layout/card): No render change: fnwrites/fnreads already exist in REL2KIND (assemble.py:15) and LINKMETA, the access wires are minted from f.access.ops at layout.js:269, the role badge reads f.role, the model card's written-by/read-by lists read _FNLINKS (layout.js:963), the endpoint rollup is rebuilt by _a3_graph L626-640. Only DATA moves.
 SKETCH: 1. m2t as today; per fn build symtab: params/annotations → ctor → session.get → single-model select()/join() RHS → same-file annotated helper → for-loop → alias chains (3 passes to fixpoint). | 2. Walk fn: Assign/AugAssign whose target is <bound>.attr → put(Model,'w'). | 3. Call select(args…)/x.join(args…) → root_name(arg) ∈ m2t → put(Model,'r'); session.get(Model) as today. | 4. session.<add|delete|merge…>(x) → x via symtab (now query/for/param-bound too) → put(Model,'w'). | 5. flush/commit → commits=True (unchanged). | 6. _detect_sinks: receiver 'session' no longer implies http. | 7. Downstream unchanged: derive_fn_roles/derive_distance_to_write/derive_endpoint_access consume the wider faccess; _a3_levels §3b draws the new anchors' gradient; the station reads ops/role as-is.
 HONEST-EMPTY: A project with no attribute assigns on ORM-bound rows, no select(Model.col)/join(Model) reads, no session.delete(row) on a query-bound row, and no `session.<verb>` call → _orm_access returns exactly today's ops (same symtab entries, same op set, same commits flag) and _detect_sinks returns the same 
 SIZE: ~55 lines in _a3_code.py (symtab +~35, attr-assign +8, root-of-chain +10, sinks −1) + 0-2 lines in _a3_graft.derive_fn_roles if the gate precedence is swapped. Batteries: tests/orm-access (72 checks today) +~12 cases (query-bound delete · for-loop delete · param-annotated attr write · scalar_one attr write + flush · select(Model.col) · .join(Model) · same-file helper return · MULTI-model select st
 COUNTS: {"attr_write_sites_mapped": 61, "attr_write_fns_mapped": 21, "attr_write_fns_gaining_a_write_no_flush_gate": 12, "attr_write_fns_gaining_a_write_flush_gated": 9, "attr_write_fns_no_write_today_and_flush_in_fn_mapped": 4, "attr_write_fns_no_write_today_caller_flushes_mapped": 3, "attr_write_sites_unmapped": 11, "attr_write_fns_unmapped": 4, "col_read_sites_select_model_col_mapped": 91, "join_model_sites_mapped": 10, "col_or_join_fns_mapped": 40, "fns_no_access_today_reading_via_col_or_join_mapped": 29, "fns_with_access_today_gaining_extra_read_models_mapped": 7, "session_delete_sites_mapped": 7
 RISK: Gate precedence: derive_fn_roles (_a3_graft.py:391) tests accessor before gate, so assert_recipe_allergen_safe — the map's ONLY gate (1/210) — flips to accessor once its RecipeIngredient read (recipes.py:220) lands; the gate badge/legend row would th
 RISK: Attribute-write gating decision changes anchor semantics: non-gated (recommended, consistent with session.add without flush) makes seed_stage_schedule, persist_outcome and _refold_recipe_title d2w-0 anchors (12 fns gain a write); flush-gated gives 9 
 RISK: Every twin's committed map moves on the next regen (change-in-place): +81 rollup ops over 29 of 67 endpoints, 29 drawn nodes change role, 64 new access wires — the Universe proofs that count wires in-page, the levels example, and gustify's committed 
 RISK: Symtab widening is still a floor, not a census: a cross-file helper return (attach_cupo/release_cupo via owned_mode in an unclaimed file), a dict-comprehension binding (set_user_equipment_codes, recipes.py:373-382), and any multi-model select() RHS s
 J W1 Initial setup [POST /consent · POST /setup/complete · GET: BEFORE endpoint POST /setup/complete → handler setup_complete [caller · d2w 1] → calls complete_setup [accessor · d2w 0] fnreads SetupCompletionState (commits) → calls _discard_claim [accessor · d2w 0] fnreads IdempotencyKey (commits) · sinks=['http'] → calls _stamp_ ||| AFTER endpoint POST /setup/complete → handler setup_complete [caller · d2w 1] → calls complete_setup [accessor · d2w 0] fnreads SetupCompletionState (commits) → calls _discard_claim [accessor · d2w 0] fnreads IdempotencyKey → + fnwrites IdempotencyKey  (session.delete(row) on the query-bound row, setup.py:322-333) → calls _stamp_completion/_upsert_* unchanged → + rollup writes_to IdempotencyKey → − endpoint access.sinks ht ||| Δ The idempotency claim's DELETE lands as a red wire on IdempotencyKey and the false http tag disappears; complete()/fail()/_reclaim's UPDATE writes (idempotency.py:153-163, :97-100) still do not draw —
 J W2 Look for recipes [GET /recipes/explore · GET /recipes · G: BEFORE endpoint GET /recipes/explore → handler explore_recipes [caller · d2w –] → calls build_cooked_ledger [pure] → calls compute_recipe_availability [pure · pantry] → calls build_candidate / filter_candidates / rerank_by_preferences [pure] → calls get_exploration_p ||| AFTER endpoint GET /recipes/explore → handler explore_recipes [caller→ACCESSOR] → + fnreads RecipeIngredient  (select(RecipeIngredient.recipe_id, …) recipe_explore.py:130) → calls build_cooked_ledger [pure→ACCESSOR] → + fnreads DishHistoryEvent · Recipe · RecipeIngredient  (novelty.py:372-416 col/join) → calls compute_recipe_availability [pure→ACCESSOR] → + fnreads RecipeIngredient  (recipe_availability.py:59) → calls buil ||| Δ The heaviest read composition stops looking like a 3-table read with six pure helpers: 3 explore hops, 5 search hops and 5 detail hops turn accessor with pink wires to the tables they actually query; 
 J W3 Filter recipes [GET /taxonomy · GET /repertorio/cupos · P: BEFORE endpoint GET /repertorio/cupos → handler get_cupos [caller] → calls list_cupos [accessor] fnreads RecipeFilterMode → rollup reads_from RecipeFilterMode → endpoint PUT /recipe-filter-modes/{mode_id}/cupo → handler put_cupo [caller · d2w 0] → calls attach_cupo [ ||| AFTER endpoint GET /repertorio/cupos → handler get_cupos [caller] → calls list_cupos [accessor] fnreads RecipeFilterMode → + rollup reads_from Recipe  (cocinadas_for_cupo, drawn node pure→accessor, .join(Recipe…) repertorio.py:160 — off the drawn chain, list_cupos is not a handler) → endpoint PUT /recipe-filter-modes/{mode_id}/cupo → handler put_cupo [caller · d2w 0] → calls attach_cupo [pure · d2w 0]  — STILL no write: `m ||| Δ Two rollups gain the Recipe read behind cocinadas_for_cupo; the cupo attach write itself remains undrawn until the binder can follow a cross-file helper (or the file is claimed).
 J W4 Cook [POST /cooking/sessions · PATCH …/stage · PATCH …/st: BEFORE endpoint POST /cooking/sessions/{session_id}/cancel → handler post_cancel [caller · d2w 0] → calls cancel_session [accessor · d2w 0] fnreads CookingSession (commits) → calls delete_session_photos [accessor] fnwrites CookingPhoto → calls clear_pending_cooking_t ||| AFTER endpoint POST /cooking/sessions/{session_id}/cancel → handler post_cancel [caller · d2w 0] → calls cancel_session [accessor · d2w 0] fnreads CookingSession (commits) → + fnwrites CookingSession  (cs.status = "cancelled" · timer_step_id · timer_ends_at + flush, cooking.py:248-251) → calls delete_session_photos / clear_pending_cooking_timer_notifications unchanged → + rollup writes_to CookingSession (+ reads_from Recip ||| Δ cancel and advance-stage finally show the CookingSession state flip as a red wire and a writes_to rollup (the two journeys the audit named); the long-prep seed becomes an anchor; the timer path's step
 J W5 Store ingredients [POST /pantry/items · POST /pantry/item: BEFORE endpoint POST /pantry/items/batch → handler create_pantry_items_batch [caller · d2w 0] → calls add_pantry_item [accessor] fnwrites IngredientHistoryEvent · PantryItem (commits) → rollup writes_to IngredientHistoryEvent · PantryItem → endpoint GET /pantry/overv ||| AFTER endpoint POST /pantry/items/batch → handler create_pantry_items_batch [caller→ACCESSOR · d2w 0] → + fnreads Location  (select(Location.id), pantry.py:225) → calls add_pantry_item unchanged → + rollup reads_from Location → endpoint GET /pantry/overview → handler get_pantry_overview [caller] → calls list_pantry_items [accessor] fnreads PantryItem → calls compute_cookability_summary [caller→ACCESSOR] → + fnreads Recipe  ||| Δ The batch handler and the cookability summary stop reading as ORM-free; overview's rollup grows from 2 to 9 tables.
 J W6 Pantry locations [POST /pantry/locations · POST /pantry/i: BEFORE endpoint DELETE /pantry/locations/{location_id} → handler remove_location [caller · d2w 0] → calls delete_location [accessor · d2w 0] fnreads Location · fnwrites PantryItem (commits) · sinks=['http'] → rollup reads_from Location ; writes_to PantryItem  (Locati ||| AFTER endpoint DELETE /pantry/locations/{location_id} → handler remove_location [caller · d2w 0] → calls delete_location [accessor · d2w 0] fnreads Location · fnwrites PantryItem → + fnwrites Location  (session.delete(row) on the query-bound row, locations.py:100-108 → :132) → + rollup writes_to Location → − endpoint access.sinks http (vanishes) → POST /pantry/locations · POST …/assign-location · PATCH …/{location_id}: unc ||| Δ The location DELETE is drawn as the red wire it is instead of a phantom HTTP call; the map stops claiming this endpoint only re-homes pantry items.
 J W7 Shopping [POST /shopping/items · POST /shopping/from-plan: BEFORE endpoint GET /shopping → handler get_shopping_dashboard [caller] → calls frequent_candidates [pure] → calls list_shopping [accessor] fnreads ShoppingItem → calls suggested_candidates [caller] → rollup reads_from ShoppingItem · RecipeIngredient · UserDietaryPro ||| AFTER endpoint GET /shopping → handler get_shopping_dashboard [caller] → calls frequent_candidates [pure→ACCESSOR] → + fnreads IngredientHistoryEvent  (shopping.py:239+ col read) → calls list_shopping / suggested_candidates unchanged → + rollup reads_from IngredientHistoryEvent · IngredientRestriction · PantryItem · PlannedRecipe · Recipe (3→8; _planned_required_ingredients + PlannedRecipe/Recipe via .join, shopping.py:337 ||| Δ The dashboard's frequency helper turns accessor; both list-building endpoints roll up the 5 tables they actually consult.
 J W8 Plan [POST /recipes/{recipe_id}/plan · POST /shopping/fro: BEFORE endpoint DELETE /recipes/{recipe_id}/plan → handler delete_plan_recipe [caller · d2w 0] → calls unplan_recipe_by_recipe [accessor · d2w 0] fnreads PlannedRecipe (commits) · sinks=['http'] → rollup reads_from PlannedRecipe  (READ-only) → endpoint access.sinks=[ ||| AFTER endpoint DELETE /recipes/{recipe_id}/plan → handler delete_plan_recipe [caller · d2w 0] → calls unplan_recipe_by_recipe [accessor · d2w 0] fnreads PlannedRecipe → + fnwrites PlannedRecipe  (`for row in rows: await session.delete(row)`, recipes.py:328-340 — for-loop binder) → + rollup writes_to PlannedRecipe → − endpoint access.sinks http (vanishes) → endpoint POST /recipes/{recipe_id}/plan → handler post_plan_recipe  ||| Δ Unplanning is drawn as the PlannedRecipe delete it is (red wire + writes_to) instead of a read plus a phantom http sink.
 J J1 POST /setup/complete: BEFORE endpoint POST /setup/complete → handler setup_complete [caller · d2w 1] → calls complete_setup [accessor · d2w 0] fnreads SetupCompletionState (commits) → calls _discard_claim [accessor · d2w 0] fnreads IdempotencyKey (commits) · sinks=['http'] → calls _stamp_ ||| AFTER endpoint POST /setup/complete → handler setup_complete [caller · d2w 1] → calls complete_setup unchanged → calls _discard_claim [accessor · d2w 0] fnreads IdempotencyKey → + fnwrites IdempotencyKey  (query-bound session.delete(row), setup.py:333) → the upsert fan unchanged → + rollup writes_to IdempotencyKey → − endpoint access.sinks http → (claim()/complete()/fail() attribute writes in services/idempotency.py stay u ||| Δ The idempotency claim flips from 'read-only' to a written table on the flagship journey — the audit's gap 7 half-closes here; the other half is the config claim.
 J J2 DELETE /me: BEFORE endpoint DELETE /me → handler delete_me [caller · d2w 0] → calls delete_identity [pure] → calls delete_account [accessor · d2w 0] fnwrites AiSpendLog · DishHistoryEvent · Household · IdempotencyKey · Membership · Recipe · User → rollup writes_to 7 models ||| AFTER endpoint DELETE /me → handler delete_me [caller · d2w 0] → calls delete_identity [pure] → calls delete_account [accessor · d2w 0] fnwrites 7 models → + fnreads Recipe  (select(Recipe.id).where(creator_user_id…), account.py:59) → + rollup reads_from Recipe ||| Δ One pink wire: the cascade's pre-read of the user's authored recipes.
 J J4 POST /cooking/sessions: BEFORE endpoint POST /cooking/sessions → handler post_start_session [caller · d2w 0] → calls start_session [accessor · d2w 0] fnwrites CookingSession · fnreads CookingSession · Recipe (commits) → calls seed_stage_schedule [caller · d2w 1] → calls _schedule_next [acce ||| AFTER endpoint POST /cooking/sessions → handler post_start_session [caller · d2w 0] → calls start_session unchanged → calls seed_stage_schedule [caller d2w 1 → ACCESSOR d2w 0] → + fnwrites CookingSession  (cs.current_stage = 0 on the CookingSession-annotated param, long_prep.py:103; flush is in start_session:172 — draws only under the non-flush-gated rule) → calls _schedule_next unchanged → + rollup reads_from RecipeIngred ||| Δ The long-prep seed stops being an amber delegator and becomes a second CookingSession writer on the chain.
 J K2 GET /recipes/explore (pure read): BEFORE endpoint GET /recipes/explore → handler explore_recipes [caller · d2w –] → calls build_cooked_ledger [pure] → calls compute_recipe_availability [pure · pantry] → calls build_candidate / filter_candidates / rerank_by_preferences [pure] → calls get_exploration_p ||| AFTER endpoint GET /recipes/explore → handler explore_recipes [caller→ACCESSOR] → + fnreads RecipeIngredient  (recipe_explore.py:130) → calls build_cooked_ledger [pure→ACCESSOR] → + fnreads DishHistoryEvent · Recipe · RecipeIngredient → calls compute_recipe_availability [pure→ACCESSOR] → + fnreads RecipeIngredient → calls build_candidate / filter_candidates / rerank_by_preferences [pure] unchanged → calls get_exploration_p ||| Δ Same as W2's first hop: the pure-read journey stops pretending it touches 3 tables — 10 rolled up, 3 drawn hops turn accessor with 5 pink wires.
 J K3 POST /recipe-creation/gustify: BEFORE endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller · d2w 0] → calls _gustify_response / _raise_for_pipeline_error [pure] → calls generate_gustify_recipe [accessor · d2w 0] fnwrites RecipeCreationRequest (commits) · sinks=['http'] → ca ||| AFTER endpoint POST /recipe-creation/gustify → handler post_create_gustify [caller · d2w 0] → calls generate_gustify_recipe [accessor · d2w 0] fnwrites RecipeCreationRequest  (the session.delete(req) at ai_recipes.py:437 was already a 'w' via the ctor binding) → − sinks http (vanishes; the real Gemini call stays untagged — a separate ≤40-line sink floor, not this class) → calls reclaim_if_abandoned [pure→ACCESSOR · d2w 0]  ||| Δ The credit-gate → LLM → persist shape gains its persist leg (two RecipeCreationRequest writers, one newly drawn) and loses the false http tag; the LLM call itself is still invisible.
 J K5 POST /recipes/demand: BEFORE endpoint POST /recipes/demand → handler post_recipe_demand [accessor · d2w 0] fnwrites RecipeDemand (commits) → rollup writes_to RecipeDemand ||| AFTER endpoint POST /recipes/demand → handler post_recipe_demand [accessor · d2w 0] fnwrites RecipeDemand → + fnreads RecipeDemand  (select(RecipeDemand.id).where(user_id…, created_at >= cutoff).limit(1) — the rate-limit clock, recipe_demand.py:47) → + rollup reads_from RecipeDemand ||| Δ The route's own rate-limit clock — its one distinguishing read — appears as a pink wire on the same table it writes.
 UNCHANGED: J3 POST /cooking/sessions/{s, J5 POST /pantry/items, J6 POST /shopping/items/{ite, J7 POST /recipe-creation/man, J8 POST /consent, K1 auth/identity provisionin, K4 seed/ops lane (seed_catal
 --- REFUTER: wrong checks:
   ✗ today_draws: 210 fn nodes (87/64/58/1), stats.access_edges 206, gustify live c4 at bddf50f5 also 206 ⇒ HEAD example (regenerated in a81fb42/cff92ce): 219 fn_nodes (accessor 91 · caller 69 · pure 58 · gate 1), stats.access_edges 212 (counted 212 rollup edges), 72 endpoint nodes (not 67), 71 auth-gated (not 66). gustify docs/site/center/c4-graph.js says access_edges 212 at head d1880cf4. The finder's '
   ✗ W3: GET /repertorio/cupos and PUT cupo gain only `reads_from Recipe` (cocinadas_for_cupo .join(Recipe) repertorio.py:160) ⇒ Replay: both go 1→4 — +DishHistoryEvent (`.select_from(DishHistoryEvent)` repertorio.py:159, covered by the finder's own root-of-chain seam) +Recipe (:160) +PlannedRecipe (planeadas_for_cupo, a second drawn-tree helper the finder missed). Magnitude under-reported, direction right
   ✗ J2 DELETE /me: one pink wire (Recipe, account.py:59) ⇒ Replay 7→9: also +reads_from Membership via `.select_from(Membership)` at account.py:85-86 (delete_account def :47). Two pink wires, not one
   ✗ tests/arch-graph/run.sh:438-447 C1 fixture pins the accessor-before-gate precedence and must be re-pinned if swapped ⇒ The C1 fixture is at tests/arch-graph/run.sh:449-458 (438-447 is the C3 minting block). Its gate fn `a.py#require_auth` has NO faccess entry, so it exercises the four labels, not the accessor-vs-gate order — swapping the two branches at _a3_graft.py:391-393 leaves it green. A NEW fixture (gate-named
   ✗ tests/orm-access has 72 checks today (8 sink checks) ⇒ `bash tests/orm-access/run.sh` → orm-access: 45 passed (plus schema-homing 23 + model-census 7 = 75 in the same runner). No fixture asserts `session.<verb>`→http (only httpx.post at run.sh:97), so the receiver subtraction breaks no existing check
   CORR: Every _a3_code.py line citation is from commit 497b535 (one behind HEAD a81fb42, which added 137 lines above the access pass). HEAD: _orm_access L903, symtab L907-918, _arg_model L920-925, _name_model L927-928, ops loop L937-952, `if not m2t: return {}` L905-906, _detect_sinks L966-991 with `"session" in recv` at L988. _a3_graft sinks union is L342-347 (cited 345-353); accessors L372 and gate bran
   CORR: today_draws is a stale baseline: the committed example at HEAD has 219 fn nodes (91 accessor · 69 caller · 58 pure · 1 gate), stats.access_edges 212, 72 endpoints (not 67), 71 auth-gated handlers (not 66); gustify's live c4-graph.js says 212 at d1880cf4 (not 206 at bddf50f5). The 'replay 212 vs committed 206 head drift' risk is inverted — the replay matched HEAD, the baseline was old. All 67-endpo
   CORR: W3: GET /repertorio/cupos and PUT …/cupo roll up 1→4, not +Recipe only — +DishHistoryEvent via `.select_from(DishHistoryEvent)` (repertorio.py:159) and +PlannedRecipe via planeadas_for_cupo (a second helper on both trees), plus Recipe (:160).
   CORR: J2 DELETE /me rolls up 7→9: +Membership via `.select_from(Membership)` at account.py:85-86 as well as +Recipe (:59). Two pink wires.
   CORR: tests/arch-graph C1 fixture is at run.sh:449-458 (not 438-447) and does NOT pin accessor-vs-gate precedence (its gate fn has no ops) — swapping _a3_graft.py:391-393 leaves it green; a NEW fixture is needed to state the chosen order, no re-pin.
   CORR: tests/orm-access has 45 checks (the runner prints 45 + 23 schema-homing + 7 model-census = 75), not 72.
   CORR: recipe_availability.py:75 → the `select(Recipe.id)` is at :96 inside compute_cookability_summary (def :76). recipes.py for-loop delete is :339 (not 340). ai_recipes reclaim assigns are :180-185 (not 181-186). `pantry.py:225` means api/pantry.py:225 (services/pantry.py:225 is a select(PantryItem)). layout.js model-card written-by is :967-968 (not 963); gabe-universe.html sinks carry is :1710 (not 1
   CORR: fns_gaining_ops 51 / reads-only 39 under-count select_from: with `.select_from(Model)` folded (the seam sketch names it) it is ~55 / ~43; drawn flips ~30 and new pink ~59 on the HEAD map.
   MISSING: The `session`-receiver subtraction is a cross-project regression the finder scores as '0 real ones lost' on gustify alone: aiohttp's canonical idiom is `async with aiohttp.ClientSession() as session: await session.get(url)` and `session = requests.Session(); session.post(...)` is common — both would
   MISSING: The example map at HEAD already carries 5 more endpoints (notifications/history claimed by gustify d1880cf4) and 9 more fn nodes than the finder's baseline — the 'all67' replay, +81 rollup delta, 29/67 endpoints changing and 206→~287 projection must be re-run against the 72-endpoint map before any n
   MISSING: `.select_from(Model)` is named in the seam sketch but absent from the counts and from two journeys (W3 cupos, J2 DELETE /me) — state it as a third read-idiom branch (select_from alongside select/join) so the numbers and the journeys agree.
   MISSING: The gate-precedence decision needs a NEW arch-graph fixture (gate-named fn WITH ops) whichever order is chosen; the finder's 're-pin the existing C1 fixture' would leave the order untested.
   MISSING: Journeys: the finder covered every listed W/J/K journey; none skipped. Not covered but touched by the replay: PATCH /cooking/sessions/{id}/stage gains reads_from Recipe (off-chain _recipe_titles) — mentioned under W4 only in passing; DELETE /planned-recipes/{planned_id} (unplan_recipe, recipes.py:28

######## ANATOMY
 NOT_COVERED: PIPELINE_STAGES list dispatch — K3's 10 stages (recipes/pipeline/stages.py:197-208) run from a tuple via `for index, stage in enumerate(PIPELINE_STAGES)` (streaming/producer.py:45); no call edge names a stage, so validate_request/rate_limit_user/rate_limit_ip/circuit_breaker/build_prompt/gemini_call || why: K3 (the only external-spend journey) loses every gate, the LLM call and the serialiser between run_pipeline and persist — the same shape as event-bus but a different idiom (table dispatch), so an even || class: event-bus, widened to 'dispatch-tables' (registry AND list/tuple dispatch: a module-level tuple/list of function Names i
 NOT_COVERED: Transaction boundaries — commit vs flush conflated (_ORM_COMMIT = {commit, flush}, _a3_code.py:738; gustify's contract is services flush / the HTTP edge commits), begin_nested savepoints (idempotency.py:138, bus.py:65-68), the second commit after publish (api/cooking.py:427), the auth commit before  || why: Every 'commits' badge on a flushing service fn is a mislabel (BACKEND-JOURNEYS systemic finding 1); d2w anchors survive but the walk cannot show WHERE a write becomes permanent — J1, J3, K1, K3 all ha || class: new `tx-boundaries` (or orm-idioms widened: split `commits` into commits/flushes/savepoint, keyed per fn)
 NOT_COVERED: Rootless writers beyond boot: CLI scripts (apps/api/scripts/ — 9 files: seed_app_catalog.py, seed_frequent_ingredients.py, set_subscription_tier.py, reconcile_ingredients.py, mark_demand_fulfilled.py, cleanup_e2e_recipes.py, …) and alembic data migrations (63 version files at /home/khujta/projects/a || why: K4 (the ops lane) is where the production recipe corpus actually comes from per the critic; a 'boot' root draws the lifespan only — scripts and migrations stay the class of writes with no root at all. || class: boot-writers, widened to 'rootless-writers' (root kinds: boot · cron · cli · migration; each a pseudo-entry node with it
 NOT_COVERED: Test closures as graft call targets — the artifact: graft's only edge out of EventBus.publish goes to a TEST closure; _a3_graft._is_noise (L140-145) filters .js/dist/build/storybook-static/__pycache__ but not tests/. || why: A test file can be the sole 'caller' or 'callee' of a production fn, inflating behind/usage and masking dead ends (the bus handlers read as 'dead code' partly because their real callers are dispatch,  || class: backend-fns-unclaimed (a noise rule: tests/ paths are neither claimable nor callers — add to _NOISE_PARTS or a per-proje
 NOT_COVERED: Hub-gate render policy — require_household would fan 53 measured call sites (47 handlers per artifact) into one method node; the artifact proposes folding it into the endpoint's gate badge or capping hub wires. || why: Drawing methods (class-methods) is a one-line emitter change but the station has no hub-cap or badge-fold; without it the 5C layout gets a 53-spoke star on every household journey (W2–W8). || class: class-methods (with a station-side rule: a gate-role method with fan-in ≥ N renders as a badge on each caller, not a nod
 NOT_COVERED: Sink categories render nowhere — levels fn_nodes carry `sinks` (queue on post_complete; http ×5), adapter/layout copy them onto the node (layout.js:254) but no wire, glyph, or legend row exists for a sink. || why: The one signal the map DOES compute for the event bus and for external calls is invisible in the station — a fix in integrations/event-bus that only corrects the tag still shows nothing. || class: integrations (a sink → `external` node + `calls_out` wire, so the existing external kind carries Gemini/Firebase/queue i
 NOT_COVERED: Inline handler gates with no function — K5's 1-request/hour clock (recipe_demand.py:45-56: select(RecipeDemand.id)… → raise 429) and every `raise HTTPException` gate written inline in a handler body; role `gate` is name-based (_a3_graft.py:352-357) and Depends-based (middleware field), never body-ba || why: K5 is 'the only route with its own rate-limit clock' and the map shows a plain accessor writing RecipeDemand — the guardrail the founder designed is not on the map at all. || class: guardrails (widen: an AST rule tagging a fn `gate` when its body raises HTTPException/a domain error before its first wr
 NOT_COVERED: Trigger provenance on an endpoint — the reminder poll GET /cooking/reminders/due is client-scheduled (the artifact: 'label the reminder poll as a scheduled read'); nothing distinguishes user-driven, polled, boot, and E2E-only (e2e_seed) endpoints. || why: The journeys picker's backend tab is endpoint-keyed; a scheduled or seed-controls-gated endpoint walks like a user action, and the artifact's proposed 'boot' group has no field to key on. || class: api-routes (a `trigger` field: user · poll · boot · e2e; poll derived from the web arm's setInterval/refetchInterval cal
 nodes_mentioned: entity (L1 cluster; 8 on gustify), screen / web (a fetching frontend file; 32), endpoint (67 drawn; 13 in unclaimed route files), gate / Depends dependency (get_auth_context on 66, get_auth_context_from_query on 1, get_session 67, get_settings 12 — a NAME on the endpoint node, never a node), handler fn (the decorator's function; 67), service fn (graft function nodes; 210 drawn of ~1,900 homed), accessor fn (role accessor; 87), caller / pure / gate role fns (64 / 58 / 1), class METHOD (require_household, EventBus.publish, SlidingWindowLimiter.allow, StreamPipeline, CircuitBreaker.before_call — 
 connectors: bridge — screen → endpoint (web arm string-match; 48 of 54 sites, 6 unmatched named in stats.web), decorator census — the @router.<method> that MAKES an endpoint node (parse_endpoints _a3_code.py:143-195), Depends() — endpoint → dependency NAME only (middleware field; _endpoint_middleware _a3_code.py:934-965); no hop, handler — endpoint → handler fn (station-derived, layout.js:258-263), calls — fn → fn (graft inferred; 145 drawn fn_edges, 421 cross-entity; handler-rooted + §3b write-path gradient), call into a class METHOD — dead-ends (482 edges per artifact; derive_functions _a3_graft.py:175 