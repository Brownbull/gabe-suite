# Twin kickoffs — principal-entity adoption after the insight-arc propagation

Written 2026-07-23 at the close of the consolidation: suite `9a2416c..982aeed`
(insight lenses · reference graph · six-section architecture · Gabe Center
brand · reviewed, 2 blocking findings fixed with gate coverage) propagated to
gastify (`90be18b` + `d4ab18b`) and gustify (`a9ec8283`). Paste each block as
the first message of a fresh session in that project.

---

## Gastify (paste in `~/projects/apps/gastify`)

```
Resume gastify after the gabe-suite insight-arc propagation (2026-07-23,
commits 90be18b + d4ab18b on center/loop2-post-trial-contract — local,
unpushed; fold into the next PR per the ritual).

What landed: the Gabe Center now runs the data-model AND functions lenses on
every Code tab (icon tags, filter chips, two-bar api/internal usage, titled
row details with linked usage receipts, in/out signature roles, candidates
sections), the architecture station carries the same six sections app-wide,
undocumented app types render as dashed `tbd` pending links the crawl gate
counts per page, and the sidebar wears the snowshoe + semantic entity icons.
Verified at sync: regen exit 0 · crawl 5118 refs / 12 pages / 0 dead ·
chrome 233/233 · center pytest 95/95. Known machine finding: StatementLine
is defined in two files (model+schema) — the build warns each regen; a
rename or an explicit ruling would retire the warning.

THE WORK: adopt the principal pending entities, one per run, via
/gabe-adopt section — the tbd sweep is the work-list (transaction 60 ·
statement 17 · architecture 77 pending references). Registry order:
card-alias · group-share · consent-privacy · analytics-drilldown. Each
adoption: card (HANDLE · WHAT & WHY · FOR WHOM · # FLOWS with ★ golden ·
IS · IS NOT · DECIDED) → config entities.<slug> block → regen green → the
walk-briefed approval (/gabe-walk records it). Watch the tbd counts DROP as
each entity's types become documented — that is the adoption scoreboard.

Then /gabe-next routes the plan (P128+ was current at handoff).
Verify-first: bash scripts/refresh_center.sh regen → exit 0 / 0 dead;
python3 scripts/next_feature.py for the queue.
```

---

## Gustify (paste in `~/projects/apps/gustify`)

```
Resume gustify after the gabe-suite insight-arc propagation (2026-07-23,
commit a9ec8283 on staging — local, unpushed with the P151 bookkeeping row;
push per the ritual).

What landed: the full accumulated stack — the data-model and functions
lenses (icon tags, filter chips, two-bar api/internal usage, titled row
details, in/out signature roles, candidates sections), the six-section
app-wide architecture station, tbd pending links with the per-page gate
sweep, the snowshoe brand + semantic entity icons (recipe→book,
allergen→alert, pantry→basket, cooking→flame, auth→lock, progression→trend,
legal→shield), and the rows-seen NEW-badge engine (bootstrap seeded at
a9ec8283 — badges arm from the next commit). Verified at sync: regen exit 0
· crawl 1725 refs / 10 pages / 0 dead · chrome 184/184.

THE WORK: adopt the principal pending entities, one per run, via
/gabe-adopt section. START WITH RECIPE — the tbd sweep shows 82 pending
references on allergen + 82 on architecture, and Recipe /
ManualRecipeCreate / RecipeCreationRequest are most of them: adopting
recipe retires the largest debt block in one move. Then: pantry · cooking ·
auth · progression · legal-consent. Each adoption: card (# FLOWS with ★
golden) → config entities.<slug> block → regen green → walk-briefed
approval. The allergen golden gap stands too: `exclude ★` still has no e2e
proof set (integration fixtures only) — a born-red candidate whenever a
capture session fits.

Then /gabe-next routes the plan (P3 was current at handoff).
Verify-first: bash scripts/refresh_center.sh regen → exit 0 / 0 dead;
node scripts/verify_center_chrome.mjs docs/site/center → green.
```
