__pycache__/

# Investigations: the .md record is committed; heavy generated artifacts stay local
docs/investigations/**/assets/
docs/investigations/**/mockups/
docs/investigations/**/raw/
docs/investigations/**/site/
docs/investigations/**/*.html
docs/investigations/**/*.pdf
docs/investigations/**/*.png
docs/investigations/**/*.jpg
.claude/settings.local.json

# graft context graph — local cache, rebuilt in ~2s (`graft build`).
# Its own `.ignore` re-admits it to ripgrep; delete that file after every build.
graft/
.ignore
