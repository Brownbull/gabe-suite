# Gabe Suite — Project Context

Development suite for Claude Code. A growing collection of skills, templates, hooks, and docs that transform how you understand, review, decide, and ship. One of the skills inside this suite is called `/gabe-lens` (cognitive translation) — do **not** confuse the suite name (Gabe Suite) with that skill name (Gabe Lens).

**Repos:**
- Brownbull: https://github.com/Brownbull/gabe-suite
- khujta: https://github.com/khujta/gabe-suite

**Local folder:** `gabe_lens/` (legacy name; rename deferred — safe to rename to `gabe-suite/` later).

**Harness:** Claude Code only. Codex support was dropped 2026-07-09 (operator decision); the suite installs to `~/.claude/` exclusively.

## Project Structure

```text
gabe-suite/                   # current local folder: gabe_lens/ (rename deferred)
  skills/                     # ONE SKILL PER CAPABILITY (B2 skills-only migration)
    gabe-<name>/
      SKILL.md                # lean core (≤200 lines): frontmatter + E1–E7 pointer +
                              # summary + output contract
      references/             # the binding deep spec, loaded on demand
      scripts/                # deterministic helpers (e.g. gabe-commit/scripts/size-budget.sh)
  templates/
    *.md, *.yaml, *.json      # .kdbp/ init files, including architecture-principles.md
    tier-sections/*.md        # tier trade-off section files + rubric + index
    mockup/                   # mockup and Storybook workflow templates
    debt-patterns/            # decision-debt pattern catalog
  prompts/*.md                # /gabe-scope prompt library
  schemas/*.json              # JSON Schemas for scope-session + scope-references
  scripts/suite-doctor.sh     # drift check: repo vs ~/.claude
  tests/hooks/run.sh          # hook/router fixture harness — run after ANY enforcement-layer edit
  assets/                     # Images for README
  docs/                       # User docs (start at docs/WORKFLOW.md)
  docs/prisms/<slug>/         # AUTHORED prism pages (prism.json + body.html) —
                              # content, not machinery; the machinery is the skill's.
                              # docs/prisms/_fragments/*.html are the embeddables.
  README.md                   # Public-facing documentation
  CLAUDE.md                   # This file
  install.sh                  # Install suite to ~/.claude/
```

There is no `commands/` directory: it was retired in the B2 skills-only migration (2026-07-09) after the lifecycle dry-run passed via skills alone. Each skill IS its command — the skill name gives the slash invocation (`skills/gabe-plan/` ⇒ `/gabe-plan`).

## User Profile

`~/.claude/gabe-lens-profile.md` stores the user's chosen cognitive suit for the `/gabe-lens` skill. Created by `/gabe-lens calibrate`. Default suit (Spatial-Analogical) is used if absent. File name stays skill-level because it belongs to Gabe Lens, not the whole suite.

## Conventions

- One skill per capability: `skills/<name>/SKILL.md` (lean core, ≤200 lines) + `references/` (the binding deep spec) + optional `scripts/`.
- Frontmatter: `description` + `when_to_use` (trigger sentence; combined ≤1,536 chars), plus flags where they apply — `context: fork` (satellite analyses), `agent: Explore` (read-only runs), `disable-model-invocation: true` (human-initiated only), `user-invocable: false` (background knowledge), `paths:` (auto-trigger globs).
- The execution contract E1–E7 is stated ONCE in `skills/gabe-docs/references/execution-contract.md`; every SKILL.md carries a one-line pointer, never a pasted copy. That same file states the **tool floor** (ruled 2026-09-02) — the five laws governing how a skill reaches for the `mcp__gabe-map__*` / `mcp__gabe-kdbp__*` tools: ask-first-then-the-old-method · the map is a FLOOR never a scope · honest-empty · tools are not rails · read-only except `who_calls`, whose default emit a fork beat must turn off (`emit: false`). Because all 30 SKILL.md pointers resolve here, a clause added to this file changes every skill without editing 30 specs — the reason the tool sweep landed here first.
- Provenance lives in git, not in runtime files — no migration notes, dates, or "moved from X" headers in skills (see the ledger rationale in the migration log).
- Architecture Principles AP1–AP13 live in `templates/architecture-principles.md` and are advisory context for `/gabe-assess`, `/gabe-health debt`, and `/gabe-review`.
- The Archetype Map (Prototyper/Builder/Sweeper/Grower/Maintainer) lives in `templates/archetype-map.md` — advisory context for `/gabe-roast` (canonical perspectives) and `/gabe-assess` (maturity posture-mix); a reading of the existing maturity axis, not a new level system.
- Suite changes land in the REPO first; installs regenerate via `./install.sh`; `scripts/suite-doctor.sh` makes drift visible (incl. the suite invariants: hook harness green, version/count parity, portability lint, docsite staleness). Never patch `~/.claude` in place.
- **Command costs** (stated so sessions pick the targeted path — the one context effect the 2607.27250 ablation proved, Table 2): `suite-doctor.sh` ≈ **5–6 min** (measured 2026-09-11: 46 batteries · 2,666 assertions in ≈ 5½ min, `gabe-universe` alone ≈ 2 min; re-runs every `tests/*/run.sh` battery — eight of them launch a headless browser: artifact-chrome · artifact-motion · prism · prism-fit · prism-probe · checkers · evidence-nav · gabe-universe, only `gabe-universe` SKIPs with a named reason without chrome, the other seven go RED — so provision chrome first on a fresh machine (its recipe is in `tests/gabe-universe/run.sh`); when iterating on ONE battery, run that battery's `run.sh` directly); `install.sh` ≈ 5 s; `docs/center/generators/write_facts.py` ≈ 5½ min (the same sweep, RECORDED into `docs/center/data/facts.json` — run it before a suite-centre regen when batteries changed; `--only <name>` re-records one battery in seconds); `tests/hooks` ≈ 3 s; `tests/register` < 1 s; `tests/case-thread` < 1 s; full center regen (`docs/center/generators/refresh_suite_center.sh`) is browser-gated (**minutes**) — for a single page, regen that page only.
- A deterministic script that will run against real project data ships only after a dry-run against a COPY of that data — record the run's numbers in the commit message (meta-review P1: template-derived fixtures validate the template, not reality).
- Every hook/checker ships fixture cases in `tests/` proving it can both FIRE and stay silent; behavior edits update the battery in the same commit (`tests/hooks/run.sh` — meta-review P2/P4: a checker that cannot fail is non-evidence, and fixtures that live in session transcripts protect nothing).
- **The map↔grep delta loop** is trust-but-verify feedback that sharpens the codebase-map generators from real dev use — NOT a new graph element (the accumulator never renders; a generator fix yields more of the SAME `access.ops`/`calls`/`bridge` edges). One tool, `skills/gabe-commit/scripts/map-deltas.py` (`append` emit · `analyze --sweep`): the spine EMITS a one-line delta when a grep (context B) diverges from a map claim (context A) — `/gabe-red` deterministically via `skills/gabe-red/scripts/reach-emit.py` (the two-arm reach's arm-difference, `graft grep '\b<sym>\b'` finds a caller `graft callers` missed → `_a3_graft.calls`; `tests/reach-emit` 17-assert), `/gabe-execute` at E4 map→grep, `/gabe-review` at REACH/WEB-BRIDGE/ENTITY-SHAPE DRIFT — into the gitignored per-project accumulator `.kdbp/map-deltas.jsonl`; `/gabe-commit` ANALYZES+SWEEPS it as a Step-2 aux check (report-never-gate, WARN never blocks). **The sweep is a TALLY (11a, design-reviewed):** it UPSERTS the live deltas into an edge-keyed ledger `.kdbp/map-deltas-rollup.jsonl` keyed `(gen, subject, file)` — dedup + `count`=persistence, line drift never resets it, **nothing deleted so nothing detacheded** — and digests the ACTIVE edges (tier COMPUTED fresh: commits since `last_n` vs the `MAP_DELTAS_H` horizon, so a fixed/dormant arm goes cold on its own, re-touch RESUMES the tally; a v1 legacy rollup folds once). **pulse S14** nags the standing active tally per arm (the one accumulator-backed angle). The **decision-debt arm (11b: park + resurface + DECISIONS.md wiring) is DEFERRED** to its own designed+reviewed pass — brief = the review's confirmed holes. Reliable host is `/gabe-commit` (fires on EVERY KDBP project, unlike center-only `/gabe-cc-update`); honest-empty on a non-`.kdbp` or delta-free tree (silent exit 0). Batteries: `tests/map-deltas/run.sh` (25-assert, mutation-proven) + `tests/reach-emit` + `tests/pulse-angles` S14. Design record: [docs/design/map-delta-loop/README.md](docs/design/map-delta-loop/README.md).
- The **evidence navigator** is shell machinery, not a page: `templates/center/shell/assets/evidence-nav.{js,css}` renders one entity's workflows (map left · capture + provenance right) from a committed CENSUS at `docs/site/center/workflows/<entity>.json`. The census is an ACCUMULATOR; its drift is checked by `templates/center/generators/check_workflow_drift.py` (report-never-gate) and routed by `/gabe-review` (detects on the diff) + `/gabe-pulse` S8 (nags on deferred capture debt). Layout law and wiring contract: the asset's header + `templates/center/shell/README.md`.
- **The in-flight projection** is beat-tail machinery, not a page: `skills/gabe-cc-update/scripts/write-inflight.py` derives `docs/site/center/inflight.{json,js}` from PLAN.json + git (declared entities from the plan record · touched entities path-derived · no wallclock, byte-identical on an unchanged tree); the E8 beat tail refreshes it, `board.js` reads it at view time via the script sibling (file:// kills fetch). Declared-vs-touched drift is priced by `/gabe-review`'s ENTITY DRIFT subject. Battery: `tests/inflight/run.sh`.
- **The codebase-graph station** — RETIRED as a page 2026-09-10 (operator): `codebase-graph.html`, `codebase-archive.html` and the Levels lab `codebase-archive-lab.html` left the shell and are no longer emitted (`RETIRED_PAGES` in `build_center_a3.py` skips a stale twin copy; `sim-archive.{json,js}` is no longer written); the EMITTERS and FEEDS below stay because the Gabe Universe reads `c4-graph.js` · `levels.js` · `sim.data.js`. The Entities nav group now splits **backend** (adoption.json, as before) from **frontend** (`fe.homes` of the C4 feed — paired `fe·<slug>` + candidates deep-link into `gabe-universe.html?ent=`, buckets are counted), filled by a LATE `{{SIDEBAR_FE}}` pass. (The emitter/feed history — `_a3_graph.py` · `_a3_graft.py` · `_a3_sim.py` · the `behind` floor · class 14 module calls · the method roster — is in git and in the bullets below; this bullet keeps only what lives.)
- **The web→API bridge is the C4 FRONTEND arm** (Path A: the UI→API→data trace folded into `GABE_C4`, NOT a port of the LEVELS lab): `templates/center/generators/_a3_web.py` is a PLUGGABLE fetch extractor — auto-detects the api-call idiom (`apiFetch`|`axios`|`fetch`; a non-REST app yields `present=True, screens=[]`, a backend-only repo `present=False` with a reason) and returns, per fetching FILE, the raw `(method, path)` each call site names **plus the EXPORT enclosing the call** (D3 ruling 2026-09-05: the nearest column-0 declaration above an indented call; the bridge cross-edge then carries `export: fe:<file>#<hook>` and the universe's journey leg starts at the hook that fetched — `usePantryMutations.ts`'s 16 hooks no longer fold onto one piece; a call with no export lands on the file's principal piece, the floor) (graft cannot recover a fetch's path → a SEPARATE source-reading arm with its OWN try/except; a parser bug degrades the bridge to honest-empty, never touching graft topology or FK bytes). `_a3_graph.build_c4_graph(web=)` matches each call to the endpoint PIECE it names (normalize: strip `/api/vN`, collapse `{x}`/`${x}`→`{}`, snake↔camel; file-homed first, endpoint-fallback), emitting a new L2 `web` kind (the **Screens** column, `#e8590c`) + a new `cross_edges` `kind:'bridge'` per match + `stats.web` (matched · unmatched named, never dropped — a floor, cross-file fetches are inferred-by-design). **Honest-empty invariant:** `web=None` build byte-identical to the FK+graft build (arch-graph pins it). the retired `codebase-graph.html` rendered it (the universe draws the same bridge wires): the L2 drill draws the screen glyph + dashed `--c-fn-web` bridge wires (cross-entity endpoints get a synthetic stub, unmatched fetches mark their screen hollow-dashed), the SIM explode draws web pieces on an outer ring + a DATA-bridge pass. Two DRIFT detectors read the COMMITTED c4-graph (no re-glob): pulse **S10** (`fetch_bridge.py` — standing reminder, unmatched fetches at emit time) + review **WEB-BRIDGE DRIFT** (`fetch_bridge.py --diff` — a diff-added fetch matching no endpoint). Measured on gustify: 32 fetching files → 32 screens · 54 sites · 48 bridged / 6 unmatched / 4 dynamic (80% call-site coverage; the 6 are real archmap-coverage gaps). `_a3_graph.py` grew 704→**840** at the web arm and has kept growing since (fe fold `d06d578` + the universe-station arc — well over the 800 CODE budget, report-never-gate; `wc -l` is the number, a count in prose goes stale; the web arm + the endpoint `behind` floor). Batteries: `tests/codebase-graph` (RETIRED with the page, 2026-09-10) + `tests/arch-graph` (the web-arm extraction self-test) + `tests/pulse-angles` (S10).
- **The FE STRUCTURE arm + the Gabe Universe station** (distinct from the web bridge above — `_a3_web.py` is the fetch→endpoint arm; `_a3_fe.py` is the frontend STRUCTURE arm, and "FRONTEND arm" without a qualifier is ambiguous): `templates/center/generators/_a3_fe.py` derives TypeScript-compiler-proven pieces (component/hook/store/route/module/fe-type) + resolved edges onto a SEPARATE top-level `fe` key in `GABE_C4` (`_a3_graph.fold_fe`; `fe=None`/`present=False` byte-identical — honest-empty). Its DATA-FLOW layer (built 2026-08-28): a **cache detector** (F1 — a piece calling a query-library hook `useQuery`/`useSWR`/… with no project binding is a server-cache sink, via the `_CACHE_CALLEES` idiom roster, same class as `_STORE_`/`_ROUTER_CALLEES`, NEVER a project name-list); a **method-based write detector** (`_WRITE_METHODS` — a POST/PUT/PATCH/DELETE fetch = a write, read from the web arm's per-site method, so the read/write DIRECTION is LIBRARY-AGNOSTIC on any twin, NOT a react-query `useMutation`/`useQuery` split — the deliberate "no single-stack heuristic" call, rationale in the design record); **fed2w** depth (a level-order BFS, hops-to-a-write-fetch — the gradient's number); the **chrome·read·write** wire CHANNEL (backward reachability from the store/cache/fetch sinks); **feClass** per component (view = rendered by a route · detached = no drawn renderer · private/connector/container/leaf — the tier-control class; D1 ruling 2026-09-05); **hrole** per hook (D2 ruling 2026-09-05 — the frontend's function role, one by precedence from drawn wires: streamer · fetcher · store · orchestrator · effect · deriver; the store writer/reader split waits on setter extraction); **store shape + type members** (D5 ruling 2026-09-05 — the extractor records a type's members and a store's value type on `createContext<T>()`/`create<T>()`; the arm gives the store `fields` (the type's members, or the inline literal's) + a `typed` wire to the type piece; the station shows them as the store's columns — a store is the frontend's table, a type its schema). **A route's LABEL is its URL path** (tier0 review 2026-09-07): the extractor keeps the innermost call's first string argument as `arg0` (idiom-free); `_a3_fe.route_label` resolves a route piece's `label` — the file-router literal first (`createFileRoute("/_layout/admin")` → `/admin`: pathless `_seg` dropped, `$x` → `:x`, `/_layout/` → `/`), the FILE by the router's convention second (TanStack `routes/settings.cards.tsx`, Next `app/chat/[id]/page.tsx`), the export name last (a react-router JSX route carries NO `label` key — the example estate is byte-identical); `name` stays the export, the raw literal rides as `route`; the station labels `p.label || p.name`, the card says `export Route · route "/…"`, and the journey anchor is PATH-AWARE (a route whose first URL segment matches the journey's first endpoint leads — the URL domain is the join). **Homing** (D6, review 2026-09-05): layout FIRST — `features/<x>` pairs a piece to its backend twin `fe·<x>`; only a tree with NO feature layout (gastify's flat `src/{routes,hooks,components}`) homes by the config's own `code.web` claims (carried expanded on the archmap as `entities[slug].files`), said in `stats.fe.homing` = layout | config so the station names which one it shows. The FastAPI scanner (`_a3_code.parse_endpoints`) labels each handler by ITS router variable's prefix, not the file's last `APIRouter(prefix=)` — gastify's `groups.py` mounts `router` at `/groups` beside `invites_router` at `/invites`. **Foreign-repo resilience (pass 1, 2026-09-06, from the repo-study gap review):** the scanner resolves the whole app → `include_router(prefix)` chain through imports (`_mounts_for`; a non-literal include prefix is NAMED in the archmap's `route_mounts`, never guessed) and strips the leading `/api[/vN]` mount from every label — the web arm's normalization — so twin labels stay byte-identical while tier1's prefix-less module routers come out as `/users…` instead of `/{username}`; a SQLModel `class X(SQLModel, table=True)` is a table named `x` (`_table_of`; tier0/tier2 drew zero tables before); a module-level `CurrentUser = Annotated[…, Depends(f)]` alias resolves through one import hop (`_dep_aliases`) and a factory dep `Depends(require_permission(P))` keeps its exact display name but resolves by its `callee`; `code.api/models/schemas` accept globs with recursive `**` (`_expand_globs`, literals untouched); every `.py` the scanners could not parse is NAMED on the archmap (`unparseable`); an absent `adoption.json` makes the config's entities the registry out loud instead of a SystemExit; `GABE_GRAFT_INDEX` reads a graft index built out of tree (`graft --dir <out> build <repo>`). **Pass 2 (web + frontend):** the bridge strips a bare `/api` mount like `/api/vN` (`_API_BARE_RE`; onyx's proxy prefix took its match rate from 8% to 71%); a GENERATED SDK is an idiom — `_sdk_table` reads every `*.gen.ts` (`export class XService { public static fn(){ … .get({ url: '/…' }) } }`, @hey-api/openapi-ts) into {(Class, fn): (METHOD, path)} and a `XService.fn(` call site is a fetch site only when the table knows it (`sdkTable`; the table file itself is never a screen); a Next.js App Router `app/**/page|layout.tsx` default export is a route whatever its name (`_NEXT_APP_RX`); `stats.web.other_roots` names the second frontends the ONE-root rule did not scan (onyx `mobile/src`, `widget/src`) and the station's web row says so; the extractor's exit 3 reads as an actionable reason (run the project's frontend install, or `GABE_TS_DIR`), and a parse with a borrowed typescript over an uninstalled tree says `node_modules absent`. **Pass 3 (dispatch by name):** `_a3_code.task_map` joins Celery/ARQ/Taskiq enqueue sites (`send_task(NAME)` · `fn.delay()`/`apply_async()`/`kiq()` · `enqueue_job('fn')`) to their task functions (`@shared_task(name=…)`, `@app.task`, `@broker.task`, ARQ `WorkerSettings.functions`) by NAME — names resolved through string constants and class attributes (`OnyxCeleryTask.X`); the edges ride the existing `dispatches` wire (archmap `tasks`), a dynamic or f-string name is NAMED unresolved; every task fn is a TRACE ROOT (`parse_task_roots` → archmap `task_roots` → `endpoint:TASK <name>` nodes homed to the claiming entity, else `__unclaimed__`, seeded into the levels walk beside BOOT) so a worker's chain draws past the queue (onyx: 63 tasks · 44 of 46 sites resolved); a handler returning `StreamingResponse`/`EventSourceResponse` (or `response_class=`) carries `stream: true` on its endpoint node; `_PROVIDER_ROOTS` knows the AI stack (langchain · langgraph · litellm · mem0 · pgvector · qdrant · pinecone · cohere · voyage · ollama · vertex · mistral · groq · huggingface). **Pass 4 (the station at scale):** `window.__uniBudget` (1600) — above it the station boots at TIER 0 (`__uniScaleGuard`: Skeleton, a Sources row says so — the capsule fold left with its CAPSULES option, dropped 2026-09-07; the three config pills ENTITY MODEL · NAMES · FRONTEND MARK were REMOVED 2026-09-07 (operator settle — see the entity-models bullet)) instead of drawing every node (onyx ≈3.9k); `?journey=<name|cid>` opens ONE walk and `?ent=<slug>` one entity panel after settle (`__uniApplyDeepLinks`) — the ramp-up's one-trace-per-session law as a URL. **Scale (2026-09-06, onyx 9,234 defs):** `function_insight`'s reference scan is INDEXED (every file tokenized once, the regex runs only where the name is a token — exact) and its structural-twin pass stays the exact pairwise loop up to `_FN_TWIN_BUDGET` (2500 sizable fns), above it BLOCKED on rare identifiers (an approximation, said on the archmap as `fn_similarity`). **Membership EVIDENCE (Part C, 2026-09-06 — operator ruling: a piece is identified by its content, functionality and USAGE; its location is an INDICATOR, a prior and tie-breaker, never the DEFINITION):** today an entity is a name + FILE CLAIMS (`code.*` globs) + a models allowlist + `test_rx`, so membership IS the file claim and a function whose callers and data live elsewhere is homed by its folder. `templates/center/generators/_a3_homing.py` `evidence()` weighs three witnesses per piece — file · users (callers from levels `fn_edges`, the screens that fetch an endpoint, the pieces that render/use a fe piece) · data (the entities declaring the models it touches / the endpoints a fe piece fetches) — and records agree · stay · move candidate (≥60% of ≥2 users in ONE other entity, data agrees or abstains) · shared (≥3 consuming entities, none ≥60%); the per-piece detail rides `levels.json.homing`, the counts `c4.stats.homing`, and `home_ev` sits on every endpoint node / fe piece whose witnesses disagree. Surfaces: the universe's Sources `homing evidence` row + the card `evidence` row, pulse S17, gabe-map `touches.home_evidence` + `map_census kind=homing`. NOTHING re-homes — the opt-in `homing: usage-first` switch waits on an operator accepting ≥5 candidates by hand (the trigger in the plan). Measured on the example (gustify): 859 pieces weighed · 607 agree · 187 stay · 20 move candidates · 45 shared (agree = a strict majority per witness, review 2026-09-06). Battery: `tests/arch-graph` (the emitter, mutation-proven on a re-filed caller) + the universe probe + `tests/pulse-angles` S17 + `tests/gabe-map`. Design record: [docs/design/repo-study/README.md](docs/design/repo-study/README.md). The `fe` structure arm carries NO pulse/review DRIFT DETECTOR by design (unlike the web arm's S10 + WEB-BRIDGE DRIFT) — it is a VIEW, report-never-gate. **The Gabe Universe station** (`templates/center/shell/gabe-universe.html`) is the 3D/5C render surface of `GABE_C4` and the SOLE consumer of the `fe` key: its control system (disclosure tiers T0–T3 client-side · a binary legend · fleet tier pills) + the **FE write-spine HEAT** (a DISTINCT blue→magenta `FEBAND` gradient wire-banded by `fed2w`, on a SEPARATE default-off `__uniFED2W` toggle — beside, never merged with, the backend `BANDPAL`/`__d2wBand` green→orange heat). Batteries: `tests/frontend` (the arm) + `tests/gabe-universe` (the station — its render proof gates the FE-heat toggle behaviorally). Design record: [docs/design/frontend-model/README.md](docs/design/frontend-model/README.md) §9–10. **The legend pass (2026-09-06)** gave the station the `dispatches` WIRE kind (long-dash amber, enqueuer → TASK root, one `REL2KIND`/`CONN`/`DASHMAP` row + a `_HOP` hop, R-toggle + beam), two BADGE families beside `method`/`feclass`/`hrole` — `delivery:stream` (cyan, the endpoint's second slot via `__slot`) and `pclass` (eight provider classes under the ROOT-SCOPE rule: a class names what the SDK root is FOR, one per root; `null` → no badge, no Class section) — and three Sources rows (files skipped · route mounts · twin pass) that name the emitter's floors from `stats.unparseable`/`route_mounts`/`fn_similarity`; every new legend row paints from the live `CONN`/`__BADGE_COL` literal and carries a drawn example chip or `—` (`_exLink`/`_exStream`/`_exPc`). Both station copies (`shell/` + `shell/example/codebase-graph-station/`) are edited together — the two-file law. Plan + rulings: [docs/design/repo-study/legend-and-tools-plan.md](docs/design/repo-study/legend-and-tools-plan.md).
- **Element FORMS — what each endpoint DECIDES** (2026-09-13/14, plan [docs/design/element-forms/plan.md](docs/design/element-forms/plan.md), draft [docs/design/logic-map/element-forms.html](docs/design/logic-map/element-forms.html)): the map records what a piece REACHES; a FORM is the slots a kind of piece must define, so an empty slot is a finding (the operator's credit-scorecard idea). `templates/center/generators/_a3_forms.py` is the slot REGISTRY (data only — slots per kind, framework rules with their package cites, the findings catalogue; a `.py` because the generator copy glob is `*.py *.mjs *.sh`); `_a3_paths.py` is the FastAPI endpoint pass that fills U7 refusal reasons · K1 declared vs produced exits · U3 guards — a statement walk with guard/try/after stacks, catch resolution, ONE call level, the dependency chain (a security class + a validated parameter as framework `default` rows, version-gated by the lock file), middleware exits by path prefix, app exception handlers anywhere under the routes (one helper hop), HTTPException subclasses as refusals. The build writes `docs/site/center/forms.json` AFTER the archmap, reading it without writing it — ON by default since Phase 3 (2026-09-14), `forms: false` / `GABE_FORMS=0` turns it off; with it off, on, failing or absent every other output is byte-identical (proven on gustify · gastify · tier3). Surfaces: pulse **S20** (`form_drift.py` — refusals a client cannot handle correctly fire at ≥3; text-only and undeclared ride the line) · review **FORM DRIFT** (`form_drift.py --diff`) · gabe-map fields (`touches` `form`, `trace` `from_form`, `map_census kind=forms`, `review_drift` subject `form`). Measured: gustify 80 endpoints (shared-status 21 · reason-lost 11 · escape-500 8) · gastify 49 · tier3 512. Batteries: `tests/element-forms` (hermetic FastAPI fixture, mutation-proven) + `tests/center` + `tests/pulse-angles` S20 + `tests/gabe-map`. **Amendment 1** ([docs/design/element-forms/amendment-1.md](docs/design/element-forms/amendment-1.md), authorized 2026-09-14) carries the rest of the generation (Phases 4–5) as 11 slices of opt-in ARMS — paths · switches · effects · contract · kinds · short · tests · frontend; Slice 1 landed the foundation: `_a3_forms_build.extend_backend` stamps forms.json version 2 + `head` and runs the arms `center.config.json#forms_arms` / `GABE_FORMS_ARMS` select (all OFF by default, run per part, each isolated — a raising arm reads `present:false` and restores the feed), over the shared leaves `_a3_forms_ids` · `_a3_forms_settings` · `_a3_forms_catch` · `_a3_forms_reach`; `scripts/forms-dryrun.sh` proves a selection on real targets; battery `tests/forms-core`. Slice 2: once any arm is on, every produced exit carries an `x:` id and every precondition a `g:` id plus the `exit` it guards (no line numbers; the ordinal ranks distinct positions feed-wide, so a middleware exit filtered per endpoint keeps one id), with an `ids` block of counts and collisions; battery `tests/forms-paths`. Slice 3a: the kinds arm (`_a3_forms_mw`) writes `middleware{}` (stack order registered/runs/outer/inner, pass-through arms by kind, exits with the endpoint rows' ids + `applies_to` + `exempt`) and `dependencies{}` (parameter-order subdeps, own + inherited exits, commits one call down, teardown, `applies_to`), with findings `indistinct-exits` and `dependency-commits` in `arm_findings.kinds`; battery `tests/forms-kinds`. Slice 3b: the paths arm (`_a3_forms_paths`) writes per endpoint `returns[]` (return · implicit · catch-return; a literal 2xx/3xx response is `defined`, else the declared success as `default`), `branches[]` of DECIDING callees only (≥2 arms and the site contributes rows or the arms differ in a commit, D14), `collapsed[]` with the reason every other call does not decide, and top-level `conditions{}` with `when_for_path` / `applies` on middleware rows (a path-exempt row stays with `applies: false`, D19); the batteries' arms-on law is now "arms only add". Slice 3's review (52 findings, 41 kept) is fixed: guards are rebuilt from their `if` node (a 160-character or `match` guard no longer takes both arms down), path arms keep their polarity and read route templates, an exit no endpoint row carries mints its own id, `Depends(Cls)` reads `__init__` in the endpoint pass too, a commit after `yield` is teardown, and `paths` needs the middleware stack only in its unbuilt `paths` part. Slice 4: the short arm's schema part (`_a3_forms_schema` + the data-only `_a3_forms_short`, error types cited to the pinned pydantic-core) reads request schemas into `schemas{}` and gives every validation row its 422 `cases[]` (pydantic error type · full `loc` · the rule's line; a validator's repo helper followed one hop with the call-site keywords, so a guard they falsify drops its rule; a runtime allow-list reads `allowed: unknown`; a dependency parameter's cases are said unread), finding `extra-ignored`; `paths.framework` adds FastAPI's two body-parse `framework_exits[]` on every endpoint whose handler reads a body; battery `tests/forms-short`. Slice 4's review (72 findings, 51 kept) is fixed: forms are per class with same-named classes as `variants` (a third claim no longer crashes the arm), parameters are located the way FastAPI's `analyze_param` does (no path `missing`, header aliases, embed, dependency parameters read, factory arguments named), and validator rules drop what a catch, an early return or the call site decides. Slice 5a: the switches arm (`_a3_forms_switch`) writes `switches[]` per endpoint — `binding` (the `_a3_stacks_pydi` edges placed on each endpoint scope that reaches them, with `changes_exit` and the refusal rows agreeing branches `proves`), `value` (a settings choice the reach walk finds to `reach_depth`; one past it only counted) and `flag` (the settings a middleware condition reads, naming every row it gates on the route). Slice 5b: `paths.paths` (`_a3_forms_walk`) writes `paths[]` — one path per exit in request order (middleware steps and gates, the body read, dependencies in resolution order with the 422 split, the handler's passed guards, calls and branch arms, catches, switches), success returns combined linearly with the deciding calls' arms, `proven_by`, `partial` with its unknowns. Slice 6: the effects arm (`_a3_forms_effects`) writes top-level `steps{}` (every ORM/Core write, read, flush, commit, rollback and savepoint, `st:` ids without lines, four named widenings W1–W4) and on every path `effects{}` — its steps in request order read against the point the path leaves each function (an arm beside the one taken, or a block that leaves first, never ran; a loop or a non-exiting branch is `cond`), with each write in one bucket (committed · maybe_committed · rolled_back · uncommitted) — plus `failure{catches}` (U11) and `race` facts on adds of a bound instance to a table with a unique key; findings `refusal-writes` and `safe-method-commits` count the endpoint's own writes, a dependency's are counted beside them; battery `tests/forms-effects`. Slice 7: the contract arm (`_a3_forms_contract`) writes per endpoint `repeat{}` (U12 — the idempotency key the handler reads, through `request.state` to the middleware that set it, the refusal that requires it, and the unique inserts it claims two calls deep with their Slice 6 race, arms and idioms; an uncaught race is the finding `race-500`), `auth{}` (K2 — schemes and carriers, gates, the refusals a gate's return class raises, what the dependencies' commits provision), `rate{}` (K3 — each limiter's arguments resolved to settings defaults, its key, the switch and exempt paths) and `responses{}` (K4 — each exit's media type, body and headers); battery `tests/forms-contract`. Slice 8: the kinds arm's last parts — `functions{}` (`_a3_forms_fn`: every function reached from handlers, dependencies, task roots and event handlers that carries a fact — each raise joined to the endpoints that translate it and those that leave it to 500, or `beyond one level`; its refusals and where they surface, commits, savepoints, broad swallows), `tasks{}` (`_a3_forms_task`: decorator keywords, retry sites, the last-failure branch, dispatch and beat triggers, locks) and `handlers{}` (the publisher, the bus loop's isolation and catch, registration order, the publisher's commits around the publish), with findings `swallows-broad` · `untranslated-raise` · `retry-unbounded` · `handler-dropped`; battery `tests/forms-kinds`. Slice 9: the tests arm (`_a3_forms_tests`, over the pure extractor `_a3_test_asserts`) reads each pytest corpus's junit, extracts every test's HTTP calls, headers and assertions, gives each call a role (arrange · arrange-checked · act) and joins each act call to the exits it proves — by status, narrowed by detail, code and the headers sent, a bare 422 to the validation row, `pytest.raises` to the translating row raised in that service's file — writing `.tests` refs on rows, returns and paths, a `tests{}` summary per endpoint and top-level `test_cases{}`, with findings `test-detail-unmatched` · `asserted-unproduced` · `untested-exit`; no junit reads `present: false`; battery `tests/forms-tests`. form_drift now speaks the suite state words (no file = not_emitted · `present:false` = absent). Slice 10a: the short arm's `model` and `migration` parts (`_a3_forms_model` over the replay `_a3_forms_migrate`) replay every alembic tree's `upgrade()` into the schema it leaves and write `migrations{}` and `models{}` — constraints, columns by database name with effective nullability, hooks, `guard_use`, writers in and outside the map, overridden defaults, M10 by reference to the race facts and `race-500` — with the findings `migration-drift` (a column, type, nullability, server default or check the two sides disagree on, or a table a migration dropped that its model still maps) and `default-overridden`; battery `tests/forms-short` SF3–SF4. Slice 10b: the short arm's `setting` part (`_a3_forms_setting`, test values via `_a3_test_asserts.setting_writes`) writes `settings{}` — every `BaseSettings` field's declaration, allowed values and bounds, startup rules, readers whose settings receiver is proven (a copy is not a reader), the constant it pairs with, the exits it decides, what the tracked env files say (an allow-list, never `.env`, secrets redacted: `defined` · `external` · `default`) and the values tests give it — with the findings `unbounded-number` · `startup-unchecked` · `env-unset` · `one-value-tested`; a runner may now report one part `absent` while its stage stands; battery `tests/forms-short` SF5 · SF6 · SF-X · SF10. Slice 10c: the short arm's `mirror` part (`_a3_forms_mirror`) pairs the copies of one rule — a schema field and the column its value flows into, a schema and the sibling it is rebuilt into, a `from_attributes` response and its model, a setting and its paired or same-named constant — and writes `mirrors{}` with each pair's length · bound · allowed · default · nullable · pattern verdict (agree counted; disagree · schema-only · model-only listed) and the writers that bypass a schema-only rule, with the findings `mirror-disagree` and `schema-only-bound`; battery `tests/forms-short` SF7 + SF10. Slice 11a: the frontend arm's `guards` part — a SECOND extractor run behind `GABE_FE_FLOW=1` (`_a3_fe_extract.mjs` flow walker: rows with guard · after · callback stacks, checker-resolved literals, route-config trees) read by `_a3_fe_forms` over the data-only `_a3_forms_fe` rosters into `frontend.pieces` — each guard's exits with the hook-member atoms that decide them, `decided_by` through the bridge edge to the response model's field, the `useEffect` effects joined to the exits they cover, one chain row per way a request ends through the mounted guards, and the K3 router topology (loops · exclusive · unproven · unknown · safe), finding `redirect-loop`; `extend_frontend` runs after the c4 graph when `frontend` is selected and `forms.json` is written when the endpoint forms or the frontend arm are present (D21); battery `tests/forms-frontend`. Slice 11b: the `hooks` and `client` parts — every query and mutation call with its resolved key (the extractor follows key factories and names forwarded through `.map`), options, literal-path fetch and endpoint, a mutation's invalidations and cache seeds, each query's `invalidated_by` by TanStack prefix; the `new QueryClient` policy per option `defined` (a retry function summarised one level) · `default` (the TanStack value and its source line, when the lock file pins a version at or above the one read) · `unknown`, the hooks that override it, and the transport wrappers' `.status` branches and throws. Slice 11c: the `reason` part (`_a3_fe_reason`) — every status, detail and code comparison a client makes, walked back three hops (hook values, a literal-path fetch's response, aliases, parameters, a destructured parameter's object argument or JSX prop, a caught error's try block, a `mutate` `onError`) to the endpoints its receiver can hold, and each endpoint's exits routed to the first branch that covers them, with the findings `reason-collapsed` (a status-only branch on a status the endpoint's `shared-status` names) · `branch-unproduced` · `client-detail-unmatched`. Slice 11d: the `controls` and `stores` parts (`_a3_fe_controls`) — every button and link a component renders with its state (live · maybe-dead · dead · disabled · unknown; a component that hands a button its props is judged by those props, a button rendered only when its handler is given is live), each store action's transitions per field (reset · set-literal · set-param · append · remove · merge · expr), whether anything calls it, the store's persistence (persisted · partial · memory), and each optimistic mutation's rollback, with the findings `dead-control` · `action-uncalled` · `no-rollback`; every frontend part is now built. Slice 11e (2026-09-20, D29, design-context D-018): the `reason` part also says what each branch DOES — every site gains `does[]` (the rows inside the comparison's OWN branch, in walk order, capped at 24, each classed navigate · throw · render · return · retry · refresh · request · message · surface · state · log · other from a library binder or callee shape in `_a3_forms_fe.py`, never from a local name; `other` keeps its callee and every call says where its name came `from`), `does_state` (read · mixed · empty · beyond one level · no-rows · unread) and its `ctx`; a row belongs to the NEAREST preceding comparison that reads the same, so a comparison that only picks a value claims nothing and `mixed` is left for two on one line; arms only add — `branch`, ids, origins, routes and findings keep their bytes and the word is never recomputed from the narrowed rows (battery F21–F23 over two fixtures, eighteen source mutants caught; a four-lens review of the real feeds fixed eleven defects before the commit). Measured: gustify 12 sites · 6 read, gastify 3 · 1, tier3 45 · 32, keypro 1 · 0; 69 rows, 30 of them `other`. **Slice 12** (2026-09-21, D30–D32, design-context D-019): the kinds arm's part `inflight` — what is alive WHILE a request runs, as eight kinds of row per endpoint in request order (state · contextvar · dependency-value · background · lock · cache · built-once · setting-once), each saying where it is set (`set_at` · `set_by` · `set_in`), where it is read (`read_at`, capped), and whether it **goes with the answer · lives with the server process · unknown** — the CARRIER's lifetime (D31), never the object behind it, and only ever from a cited rule in `F.INFLIGHT["rules"]` (starlette · fastapi · PEP 567 · structlog · functools, version-gated by `FRAMEWORK_MIN`); a row an endpoint shares with others is SHORT and points at the top-level `inflight.process` row that says the rest. Modules `_a3_forms_inflight.py` over its read leaf `_a3_forms_inflight_read.py`, plus the shared `_a3_forms_carrier.py` (the request-carrier shapes, lifted out of the contract arm with `repeat{}` byte-identical). It looks only where the request goes — middleware, the dependencies FastAPI solves, the handler, ONE call level — never a repo-wide grep, and it ASKS `_a3_forms_falsify` so a fact under a branch the call site proves dead is no row. Measured: gustify 706 rows, 0 unknown lifetimes · gastify 243 · tier3 3,921 with the framework gate CLOSED (fastapi 0.133.1) · keypro absent. Battery `tests/forms-kinds` C30–C67, **96 one-line mutants all red** after three independent sweeps; 18 goldens (slice 12). **§A4 verification pass (2026-09-15/16) — DONE:** the 62 goldens as data with a checker (`docs/design/element-forms/goldens/`), a multi-lens review of the four feeds (gustify · gastify · tier3 · keypro) that confirmed 40 findings, a tier3 precision sweep and an id-stability proof; then the seven-fix order — the `_a3_paths` split (D26's own trigger) · the endpoint pass reading four FastAPI rules it had guessed at · ONE falsification leaf (`_a3_forms_falsify`: a branch the call site proves dead produces no row, path or effect) · the path/effect atomicity pass (a raise, its catch and a failed commit are one event; a streamed generator's steps kept apart; every `try` on a path) · eleven idioms (a limiter decorator IS a 429, verb-named client calls, named callbacks, `RENAME COLUMN`, an `Annotated` gate, the bridge per export) · the state-word sweep (`unknown` · `absent` · `beyond one level` where a bare 0 or `[]` stood; FRAMEWORK_MIN 0.136.1) · the §A1 reconciliation (nine promises built or struck in the spec). 37 fixed, V4 partial, V9b not reproduced, V40a (the nav-hook guard) struck with a build trigger by operator ruling. The acceptance sheet `goldens/acceptance.html` is GENERATED (`render-acceptance.py`, reusing the checker) — the operator's read per arm. `propagate.sh` now lands a new generator imported at any indentation, so the lazily-imported frontend parts reach a twin. Record: [docs/design/element-forms/review-a4.md](docs/design/element-forms/review-a4.md). Phase 6 (station surfaces) waits on the generation; backlog B13 (colour connections by path kind) and B14 (common vs specialist pieces) are its first two asks.
- **This repo never carries `.kdbp/`** (operator ruling 2026-07-22, R8 in the [design record §5 addendum](docs/design/verification-first/README.md)): the suite is built with the advisory arm only — suite-doctor (runs every `tests/*/run.sh` battery) + /gabe-roast + adversarial verify + dry-run-on-a-COPY numbers in commit messages. Do not propose dogfooding the KDBP lifecycle here.
- **The four ENTITY MODELS — claim · seeded · derived · proposed (2026-09-06, [docs/design/entity-models/README.md](docs/design/entity-models/README.md)):** `claim` is the REGISTRY and the JOIN KEY — every tool, hook, generator and station registry joins on the claim slug; the other three are VIEWS, per-piece home DELTAS resolved as `home = C4.models.homes[view][id] || GABE_LEVELS.models.homes[view][id] || entClaim`, and nothing ever joins on their names (`d:<table>` · `a:<gate>` · `fe·d:<table>` are cluster ids, never slugs). Emitted by `templates/center/generators/_a3_models.py` (seeded = Part C's move verdicts applied, hubs held, targets tier-consistent · derived = request atoms merged on the write-majority table PER ATOM, named by the URL domain at adaptive depth with the table in the user's words as the fallback, residue = gate aspects (moved) · spines + shared frontend (reported) · layers · proposed = candidates first, then one verdict per declared entity FEATURE · SPLIT · MERGE · ASPECT · LAYER, rendered as if accepted) into `c4.models` (the c4-id half) + `levels.json.models` (the function half) — byte-identical maps when stripped; a piece no witness reaches KEEPS its claim and is LISTED (abstain · held), never guessed. Read by: the universe station, which is **SETTLED on `seeded`** (2026-09-07 — the chooser, its setter and `?model=` were removed; the DELTA APPLIER survives internally as `_applyEntityModel` and runs once at boot, because `window.__uniModel` is only a label and a station that sets it without applying the deltas announces a re-cluster that never ran; a feed with no seeded view falls back to the claim registry and the Sources row says so; node arrays and ids never change, a walk survives) · gabe-map `entity_models` (the 18th tool; census · roster · members · the cross-model row) · pulse S9 arm B (the aspects the emitter measured) + S18 (the committed `entities.draft.json`) · the drafter `gabe-cc-update/scripts/draft-entities.py` (a projection, nothing re-derived; loaded by no page; committed) · `/gabe-cc-init rank`'s third lens (acceptance = one `entities.<slug>` edit). **Names are a RENDERING (naming-plan, 2026-09-06 — [docs/design/entity-models/naming-plan.md](docs/design/entity-models/naming-plan.md)):** `templates/center/generators/_a3_naming.py` computes every name a cluster could wear ONCE — `names{}` per derived/candidate row (table · class · path · action via the INSTALLED `draft-workflows.py` law, `GABE_DRAFT_WORKFLOWS` overrides · config from `center.config.json#naming` words/entities or adoption display names · both) beside the unchanged `row.name` (= the `domain` strategy) — and the frontend/backend CONVENTIONS as form templates with the project's words substituted (`case` — frontend camelCase · backend PascalCase, operator-ruled default · prefix · suffix · bracket · glyph · tint · none); `c4.models.naming` is the contract (default · source · coverage · collisions · long · disabled · fe forms · config_error · unused/unknown words). The universe's NAMES + FRONTEND MARK pills were REMOVED 2026-09-07 (operator settle): NAMES could never relabel a claim or seeded cluster — only a derived/candidate row carries `names{}` — so under the settled model it did nothing, by design; and the frontend mark is settled at `case` (frontend camelCase · backend PascalCase). `__uniStrategy()` / `__uniConvention()` SURVIVE as the label path and now read the feed's own defaults (`__uniIsFeEnt` reads the registry + the key, never a label); gabe-map, the drafter and pulse S18 speak the PROJECT DEFAULT with the raw id beside the name; `suggested_slug` never follows a strategy. Battery: `tests/naming` (incl. the join-key assert under every strategy × convention and the registries negative-grep guard). The element census (`_a3_code.element_census`) bounds "what could be an element": every unclaimed backend `.py` under a claim root is minted into the unclaimed area as an `element:` node so the blind spot has a shape. Batteries: entity-models · arch-graph · levels · center · gabe-universe (headless SETTLED proof — the model switch was removed 2026-09-07; the battery now demands the deltas, not a switch) · codebase-graph (claim-only) · gabe-map · pulse-angles · entity-drift (unchanged) · draft-entities.
- Size budget: 800 lines is a CODE budget, report-never-gate — state the numbers in any commit that grows a file past it; `references/` deep specs sit outside the cap (ruling R9, same addendum, with the deferral record for the two over-budget generators).

## The Gabe Register (project-scoped trial)

`.claude/output-styles/gabe.md` (movement register: action → consequence → next move; comptroller position-check on direction changes; DECISION blocks with mandatory defer-triggers; actors·elements·effects explanation format — naked chain, cast cards, risk dots) + `.claude/register-core.md` (4-rule payload re-injected by `.claude/settings.json` hooks on every prompt and after compaction). Canary battery: `tests/register/run.sh` — invariants pinned, mutation-proven. KILL/WIDEN conditions live in the style file's header; widen machine-wide only after it survives here. Full scope record: `docs/investigations/2026-07-30-gabe-suite-diet/`.

## Capabilities (30 skills)

| Skill | Version | Purpose |
|---|---|---|
| **gabe-artifact** | 1.3.1 | House chrome for published Artifacts — left-anchored content, one cog panel top-right, font roster + three suite skins (Catalog/Blueprint/Mission Console, light+dark), iconed section title pills (condensed grotesque, caps, the page's ONLY distinctive title face), a 12px legibility floor measured at the smallest base, H4 motion-first; 36-check render gate + 19-case chrome battery + a generic motion gate with a 6-case battery; two pattern libraries (25 motion + 31 static) |
| **gabe-cc-init** | 1.4.1 | Brownfield command-center adoption — archive-never-delete init, machine-ranked shortlist + the URL-domain SECOND LENS (advisory `entity_shape.py` view: detached domains → candidate entities · aspect entities the ranking missed) + the ENTITY-MODEL THIRD LENS (2026-09-06, advisory: the drafter's `entities.draft.json` — a verdict per declared entity FEATURE · SPLIT · MERGE · ASPECT · LAYER + NAMED candidate entities + the witness-coverage number; acceptance = one `entities.<slug>` edit in center.config.json then `rank` again; STALE when the draft's head ≠ the map's), one section per run at human speed, walk-recorded approval; tracker lives outside PLAN.md (human-initiated only) |
| **gabe-assess** | 1.3.1 | The direction guard — rapid impact assessment (blast radius, maturity scope, prerequisites) + the boundary check absorbed from gabe-align; auto-trigger LIVE: direction-guard.sh (UserPromptSubmit, KDBP projects) injects the steer rule → /gabe-assess brief before building |
| **gabe-commit** | 2.7.8 | Commit quality gate — deterministic checks incl. size-budget + the checkpoint task-record trailer (scripts/checkpoint-trailer.sh: a `Task:`-footered message must carry valid `Cases:`/`Class:`) + the map↔grep delta sweep (scripts/map-deltas.py analyze --sweep: UPSERTS the live deltas into an edge-keyed `(gen,subject,file)` tally ledger — dedup + `count`=persistence, line drift never resets it, nothing deleted so nothing detacheded — digests the ACTIVE edges (tier computed fresh: commits since `last_n` vs horizon), report-never-gate; folds a v1 legacy rollup once; pulse S14 nags the standing tally), triage, simplify tier, docs-audit; optional results_out digest, path or list (reports every tier, gates none) |
| **gabe-docs** | 1.4.4 | Documentation standards + diagrams library + the suite execution contract incl. the E8 beat tail (stated once, all six spine skills point at it) (background) |
| **gabe-docsite** | 1.1.1 | Publish docs onto the project's HTML site — renders INTO a command center's shell when one exists (nav.json seam, one skin, docs.html as the Docs station), extracts four reference classes so outbound chips and inbound 'Written up in' lines are derived in one pass, and takes the pre-render diagram path when a renderer probes clean |
| **gabe-cc-entity** | 1.0.2 | Entity-context reader — assembles one entity's slice (code map + registry + bindings) into a context pack from the center's committed data (archmap/adoption/config), joined on slug; a DATA lens not a per-entity skill (D7); brief or `--json`, plus `list` mode |
| **gabe-execute** | 2.6.4 | Phase execution with tier cap, escalation gate, checkpoint commits; the task RECORD is a commit-message trailer (`Cases:`/`Class:` beside the `Task:` footer, validated by /gabe-commit — the printed TASK CONTRACT retired 2026-08-07 after going 0-for-19) + `Reach:` (recorded, never binding — scoping to it is a defect) + case-scoped verify; narration legs authored hot |
| **gabe-cc-update** | 1.8.2 | Command-center feature coverage — card/diagrams/narration over machine facts; verdicts RENDERED from review triage (authored fallback); closes the PLAN `Center` cell on review; status, backfill, curate, release; bootstrap pointer → /gabe-cc-init; **curate-workflows** — journey CREATION for the one kind that needs a human: `scripts/draft-workflows.py` clusters the uncovered endpoints by entity·screen into `workflows.draft.js` — each entry NAMED in the user's words (`draft_name()`: verb-set phrase + path noun + actions, the legend reference's definitions logic) and LEVELED into its tier, so the station places it in Orientation · Core · Specialized with a DRAFT chip; the human ACCEPTS by moving it into `workflows.js` (rulings 2026-09-04/05); **`scripts/draft-entities.py`** (entity models Phase 4, 2026-09-06) — the ENTITY-MODEL drafter: a projection of the committed c4 `models` block into `docs/site/center/entities.draft.json` (a verdict per declared entity + candidate entities NAMED by `named_by` domain\|table with `draft_name()`'s action phrase + `coverage.witnessed`/`abstained`), loaded by no page, committed so pulse S18 + the cc-init third lens read it; acceptance is `/gabe-cc-init rank`'s |
| **gabe-handoff** | 2.1.1 | Session handoff — paste-able resume prompt + KDBP state sync |
| **gabe-health** | 1.2.1 | The suite scanner, three lenses (fork/read-only) — codebase health (god files, churn, coupling), decision-debt (absorbed from gabe-debt, AP citations), and the ask-first skill-estate sweep (promote/archive by usage); LIVE production-push gate via /gabe-push Step 3.7 |
| **gabe-help** | 1.2.2 | Context-aware guide + the P14 cross-project tool registry; Full Suite Catalog is GENERATED from skill frontmatter (scripts/gen-help-catalog.py, run by install.sh) |
| **gabe-imagine** | 1.4.1 | Understanding carried visually, not by prose — the I0–I5 flow (dissect · propose+ASK · index · produce · gate) yields INSTRUMENT pages: one variable model + consolidated hover-morph variable bar, shape-leads/mechanism-follows ordering, formulas as badged equations, floors with directed wires, ≤150 surface words incl. lede+handle. Six grammars (loop-grid new; its pattern page a named gap); the representation-lab move (contested representation ⇒ lettered live chooser artifact, runners-up stored); formula free-variable lever census; absolute-scale + time-anchored motion laws. **Two targets:** `disk` (default) · `artifact`. Gates: contract + fit + motion + the render probe (data-probe hooks, 10-case battery) |
| **gabe-init** | 2.3.7 | Project setup — `.kdbp/`, the 9 KDBP hooks, project type, maturity (human-initiated only) |
| **gabe-kdbp** | 1.0.2 | The suite's second MCP server (stdio · user scope · stdlib; wire framework = gabe-map's `mcpwire.py`) — a project's `.kdbp/` lifecycle state as SEVEN read-only tools the spine reaches for: `kdbp_snapshot` (branch · phase table states · open PENDING · last LEDGER rows, ≤1k tokens) · `phase_context` (execute's Step 0–1 preflight incl. Cases:/Reach: records, Verify Commands, scope debt, declared entities' briefs via gabe-map) · `review_target` (Review ⬜ ∧ Exec ✅/🔄 row → LEDGER shas → changed files + base) · `next_beat` (next.mjs --json) · `verify_commands` (BEHAVIOR binding, else manifest candidates; never run) · `pending_row_preview` · `ledger_row_preview` (the exact row to paste — PREVIEWS, write nothing: the D7 hooks watch Write/Edit, not mcp__*); header-resolved, closure-aware parsers; honest-empty without `.kdbp/`; `/gabe-kdbp status|register|probe` |
| **gabe-lens** | 2.5.0 | Cognitive translation — analogies, maps, constraint boxes, handles; + **plain** (`/gabe-lens plain X`): the ONE-sentence reader-side line a legend row, icon tooltip or hover card opens with — canonical set = the universe legend's `_LRDEF` |
| **gabe-map** | 1.4.0 | The suite's MCP server (stdio · user scope · stdlib; `mcpwire.py` shared with gabe-kdbp) — the project's committed codebase map as EIGHTEEN tools the agent reaches for mid-reasoning: `map_status` (+ `map_health`: where the map is PARTIAL — mounts · unparseable · twin pass · web roots, state words present · clean · not_emitted) · `entity_context` · `touches` (endpoint · `TASK <name>` · file → its FE pieces + fetched endpoints; method joins on `Class.method`; `home_evidence` when the membership witnesses disagree) · `who_calls` (two-arm reach, code-vs-prose classified, gated map-delta emit; `direction`/`depth` for callees + blast) · `entity_shape` · `cases_for` · `owner_of` + the graft equivalents `find` (tasks · providers · `stream=true`; generated clients de-ranked) · `outline` · `center_overview` (config-only registry mode) · `blast_radius` (tasks defined/dispatched → `cross-process`) · `map_census` (unparseable · mounts · twins · web · homing sections) · `map_diff` · `center_status` · `review_drift` + the repo-study pair (2026-09-06) `trace` (the ORDERED path from an endpoint/TASK/function over `levels.json` `fn_edges`, conf per hop, depth/fanout named — read lazily, never by `map_status`) · `gates` (the inverse of middleware: by callee, fn key or argument string; the ASGI middleware on every request) + `entity_models` (entity models Phase 3, 2026-09-06 — the four models on the map as ONE tool: `claim` the registry · `seeded` · `derived` · `proposed` as views; census · roster · members · the cross-model row of a piece; tri-state present · not_emitted · absent; claim is the JOIN KEY — nothing joins on a view's name, which is why it is one tool and not a `model=` flag on five; `entity_context` carries the proposed verdict on the slug, `touches` points a re-homed piece at its row); text-only results, honest-empty without a center, names + `instructions` = the discovery surface (schemas are always deferred); `/gabe-map status|register|probe` is the human management command (registration ask-first: `./install.sh --register-mcp`); graft serves map CREATION only (ruling 2026-09-02) · element forms (2026-09-13): an endpoint's `form` on `touches` and `trace` (`from_form`), `map_census kind=forms`, and the `review_drift` subject `form` — read lazily from `forms.json`; fields, not a tool (the count stays eighteen) |
| **gabe-meme** | 1.2.0 | Oblique-meme generation — persona-matched visual metaphors via memegen.link, verified PNGs, punch-up; + surface-wit mode (absorbed gabe-quip): witty titles/hooks/callouts for rendered HTML surfaces, proposes never rewrites |
| **gabe-mockup** | 2.1.1 | The lift SOP (L0–L4) over a per-project mockup manifest; Storybook + legacy HTML modes |
| **gabe-myopic** | 1.2.1 | Short-sighted-user walkthrough — foresight traps, overwhelm, recall, no-undo (fork); findings labeled M[N], never C[N] |
| **gabe-next** | 2.4.2 | Zero-logic lifecycle router over PLAN.md state — optional `Red` (routes /gabe-red BEFORE Exec) → Exec→Review→Commit→Push + optional `Center` (routes /gabe-cc-update) |
| **gabe-plan** | 2.7.5 | KDBP planning + per-phase tier decision (MVP/enterprise/scale); `proof_type` (test|visual|journey) declared at plan time; optional `Red`/`Center` columns (Red retrofits seed ⬜ only where Exec is ⬜) |
| **gabe-pulse** | 1.9.1 | Read-only completeness sweep — ten deterministic signals in four bands, plus the **ANGLE family** (`scripts/angles.py`): TWENTY evidence-backed triggers naming which satellite would find something now (S8 = workflow-census capture debt · S9 = entity-shape drift, TWO-ARMED since 2026-09-06 — arm A a detached URL domain computed fresh from `archmap.json` by `scripts/entity_shape.py` (nothing stored), arm B the ASPECTS the emitter MEASURED (gate fan-in · proposed ASPECT verdicts) read from the committed c4 `models` block, said `not_emitted` when the block is missing so a half-signal never reads clean · S10 = web-bridge drift, fetches that named no declared endpoint at emit time (the fetching files the bridge could home to no entity ride the same line as a count — drawn nowhere, said here), read from the committed `c4-graph` by `scripts/fetch_bridge.py` · S11 = model-census drift · S12 = schema-homing residue, a schema unwired in a LIVE file from the archmap's `schema_homing` block; multi-consumer shapes + dormant lanes counted, never nagged · S13 = route/file-census drift, a route or backend `.py` in a scanned code dir that no entity claims, read from the archmap's `route_census`/`file_census` — closest-to-the-request-path first via the reach hop, absence is full coverage · S14 = map-delta debt, a codebase-map generator arm whose ACTIVE missed edges — grep found what the map didn't, tallied per edge `(gen,subject,file)` by `/gabe-commit`'s upsert sweep — crossed the breadth threshold; the ONE accumulator-backed angle, but active/cold is computed fresh (commits since `last_n` vs horizon) so a fixed/dormant arm self-silences, from the `.kdbp/map-deltas-rollup.jsonl` tally ledger by `scripts/angles.py::s14_map_deltas` · S15 = frontend classification residue — a Pascal .tsx export the classifier could not prove (no JSX of its own, rendered nowhere) carries the honest kind `fe-unknown`, never a `module` claim, read from the committed c4-graph `stats.fe.by_kind` by `s15_fe_unknown` — O1 2026-09-03 · S16 = workflow coverage, screen-reachable endpoints no curated workflow names — the curate-workflows drafter's analysis run read-only over the committed `c4-graph.json` + `workflows.js` (nothing stored) — at ≥3 uncovered, or a `workflows.draft.js` proposal nobody moved into `workflows.js` (the REVIEW owed, not the run), by `scripts/angles.py::s16_workflow_coverage` · S17 = homing evidence — pieces whose USERS / DATA witnesses disagree with their FILE (Part C 2026-09-06: location is an INDICATOR, never the DEFINITION), read from the committed c4-graph `stats.homing` by `s17_homing_evidence`; fires at ≥3 move candidates (≥60% of ≥2 users in ONE other entity, data agrees) — the shared count is reported beside it, never a trigger (a structural constant of any codebase); NOTHING re-homes — the move is `/gabe-cc-init section`, re-home stays opt-in · S18 = entity proposals, the committed `entities.draft.json` (the entity-model drafter's projection: a verdict per declared entity + named candidates) carries a non-FEATURE verdict or ≥2 candidates — the REVIEW is owed via `/gabe-cc-init rank`, the RUN when the draft's head is stale, by `s18_entity_proposals`), printed as ONE line at the end of every spine beat so the 15 manual-only skills stop depending on memory; reset patterns are record-tight (prose collisions measured 2026-08-07), the diff source sees past `.kdbp` bookkeeping commits, S1's command is pasteable verbatim; decay record, stated kill condition, pulse-angles + entity-drift batteries · S19 = arms census — concepts whose zero is NOT the app's doing (`unmatched` · `unsupported_language`, ≥2), read from the archmap `arms` block; `empty` counted, never nagged · S20 = element forms (2026-09-13) — refusals a client cannot handle correctly (shared status · lost reason · escape to 500 · swallowed) fire at ≥3 from the committed `forms.json` via `scripts/form_drift.py` (the module review's FORM DRIFT runs with `--diff`); text-only and undeclared refusals ride the line, never fire it |
| **gabe-push** | 2.6.3 | Push, PR, CI watch, promotion — env-aware shipping via `.kdbp/PUSH.md`; terminal-env/--epic pushes run the PRODUCTION GATES (Step 3.7: /gabe-health three-lens scan, findings presented, ONE BLOCKING proceed/hold ask that mints `.kdbp/.push-gate-ok` — the machine-wide push-gate-guard hook fails a raw terminal push closed without it); terminal-env ship prints the /gabe-cc-update release pointer |
| **gabe-red** | 1.9.7 | TDD's first half as a beat — inspect the corpus, declare cases (C-ids in test names, corpus = registry), prove RED by assertion, commit the red checkpoint; GUARDs for refactors, enumerated skips; FOUR red outcomes (RED-WRONG-REASON + the FLIP test) + async-boundary rules for journey cases; writes the phase's `Reach:` record (two-arm graft callers+grep, `graft@sha` stamped) via `scripts/reach-emit.py` — the MECHANIZED reach that runs both arms, prints the Reach line AND auto-emits the map↔grep arm-difference as `_a3_graft.calls` deltas (word-boundary grep, noise-filtered, through map-deltas.py append) so producing the gated record can't skip the emit (map-loop axis-2 wire; `tests/reach-emit` 17-assert 3-mutant-proven); scripts/prove-guard.py re-proves a guard can still FAIL (mutate → assert red → restore), feeding the center's named-vs-guarded distinction; scripts/case-thread.py observes the red→green thread at both ends (execute entry assert-red, execute-finish/review assert-green + green@sha stamp; Review ✅ without the stamp BLOCKED by plan-proof-guard) |
| **gabe-review** | 1.19.0 | Code review — risk pricing, confidence scoring, plan alignment, triage; case-estate subjects (NEW CASE/BUMP/DRIFT, reserved C-ids) + absent-angle GROWTH triage (cap 7) + WORKFLOW DRIFT (census vs diff: census-lag · claim-drift · capture-debt) + REACH DRIFT (unreached · unused reach, vs the phase's `Reach:` record) + ENTITY-SHAPE DRIFT (a diff-added route landing in a URL domain no entity owns, vs the model — `entity_shape.py --diff`) + WEB-BRIDGE DRIFT (a diff-added fetch whose method+path matches no declared endpoint, vs the C4 endpoint key-space — `fetch_bridge.py --diff`) + STALE ANCHOR (5c on PENDING: cited files moved past a row's `Verified` sha) on the same pricing + FORM DRIFT (a diff-added `raise HTTPException` that is text-only, shares a status with a different text-only refusal, or produces a status the endpoint does not declare — vs the committed `forms.json`, `form_drift.py --diff`) |
| **gabe-roast** | 1.1.1 | Adversarial gap review from a required perspective (fork/read-only) |
| **gabe-scope** | 2.1.1 | Scope authoring — SCOPE.md (stable premise + §Phases arc) for a new project |
| **gabe-scope-change** | 2.2.1 | Scope evolution, one entry point — classifies pivot vs addition; additions execute inline (absorbed gabe-scope-addition), pivots route to the safety-flagged gabe-scope-pivot |
| **gabe-scope-pivot** | 2.1.1 | Direction-change scope rewrite (human/router-initiated only) |

## Archived skills

Decommissioned-not-deleted skills live in `skills/_archive/` (outside the install/doctor glob) with a
README covering why + how to reinstate; rulings in [docs/design/trim-ledger.md](docs/design/trim-ledger.md). Currently: **gabe-teach** + **gabe-arch** + **gabe-scope-addition** (archived 2026-07-15 —
2,740 lines serving ~2 observed uses; trim-matrix audit) + **gabe-walk** + **gabe-quip** + **gabe-align** +
**gabe-debt** (archived 2026-07-30 — operator rulings on the skill-map artifact: walk lost its why;
quip/align/debt absorbed into gabe-meme/gabe-assess/gabe-health). `~/.claude/gabe-arch/` user state is never
touched by decommission.

## Companion estate

`skills/dev-conventions/` — the operator's cross-project conventions (NOT a suite capability: no
`gabe-` prefix, no capability row, outside the doctor's parity globs). Carried in the repo so a
reinstall on a fresh machine lands it; installed to `~/.claude/skills/dev-conventions/` by
install.sh. Origin: the 2026-08-04 always-on diet (move №2) — content relocated from
`~/.claude/rules/common/`, loaded on demand only.

## Workflow Docs

- **[docs/design/verification-first/README.md](docs/design/verification-first/README.md) — the suite's design record (read BEFORE restructuring the suite):** the one-picture model (lifecycle produces · structure shapes · growth accrues), the mutated lifecycle incl. `/gabe-red` + `/gabe-walk`, the C-id test-identity scheme, decisions D1–D7 (block-lies/warn-debts hooks, report-never-gate MVP), and the landing plan.
- [docs/workflows/README.md](docs/workflows/README.md) — quick chooser.
- [docs/workflows/greenfield.md](docs/workflows/greenfield.md) — new app from idea to first phase.
- [docs/workflows/brownfield.md](docs/workflows/brownfield.md) — existing codebase adoption.
- [docs/design/suite-backlog.md](docs/design/suite-backlog.md) — considered-but-not-built, with the evidence that made each interesting (read before proposing suite work).
- [docs/suite-state-audit.md](docs/suite-state-audit.md) — runtime inventory audit (see its Updated line for currency).

Workflow docs are installed locally under `~/.claude/docs/gabe-suite/`.

## Adding a New Skill

1. Create `skills/<skill-name>/SKILL.md` with frontmatter (name, description, `when_to_use`, metadata.version) — lean core ≤200 lines with the E1–E7 pointer; deep spec goes in `skills/<skill-name>/references/`.
2. **Handshake walk:** read the ADJACENT beats' specs for seam contradictions — what this skill emits, do its neighbors accept, and vice versa? (Meta-review P5: the gate once blocked the very commit /gabe-red must produce; two specs disagreed on where `proof` lives. Seams break where each spec is written from its own seat.) Same walk applies to any spec change that alters a beat's inputs/outputs.
3. Add it to README.md and CLAUDE.md (the gabe-help catalog is generated at install).
4. Run `./install.sh` (≈5 s) then `scripts/suite-doctor.sh` (≈2–4 min, all batteries; must be CLEAN — the doctor also checks version/count parity, so a missed CLAUDE.md row fails here).
5. Update install/validation tests if the inventory count changes. (`install.sh` auto-discovers `skills/gabe-*/` — no list maintenance.)
